# Variable — Private Space（私人空间）

> [!IMPORTANT]
> **📦 30000字 移动大容量U盘随插随用虚拟系统 极其详细施工计划已发布**
> `任何软件不崩溃 · 任何电脑随插随用 · 10GB大软件不卡死 · 极致隔离 · 无限拓展`
> **👉 查看计划：[`docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md`](docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md)**
> 完成度总览：文档 100% (12/12章 40000字) | 实现 16% (2/12模块) - 每章右侧 ✅/⬜ 框打勾，完成即勾
> ![完成度](https://img.shields.io/badge/文档-40500字-blue) ![状态](https://img.shields.io/badge/实现-2%2F12-green)

> [!TIP]
> **完成度总览**
> | 模块 | 状态 |
> |---|---|
> | 1 总览 | ✅ 已完成 |
> | 2 架构 | ✅ 已完成 |
> | 3 存储 | ⬜ 待实施 |
> | 4 隔离 | ✅ 已完成 |
> | 5 防卡死 | ✅ 已完成 |
> | 6 兼容 | ⬜ 待实施 |
> | 7 真Windows | ✅ 已完成 |
> | 8 拓展 | ⬜ 待实施 |
> | 9 性能 | ⬜ 待实施 |
> | 10 安全 | ⬜ 待实施 |
> | 11 测试 | ✅ 已完成 |
> | 12 交付 | ✅ 已完成 |
> 详见计划顶部总表，打勾即完成

> [!IMPORTANT]
> **🚀 下一阶段 6大件 40000字计划已发布**
> `内核MicroVM · 万能开2.0 · 插件市场 · 云同步 · 企业安全 · 跨平台`
> **👉 AI工作内容与步骤：[`docs/PORTABLE_AI_SPLIT_PLAN.md`](docs/PORTABLE_AI_SPLIT_PLAN.md)（v1 五AI + v2 六AI 独立分工）**

**完全离线、无账号、无云同步的私人桌面环境 + 写作 + 思维导图 + 项目分析 + 命运推演 Windows 桌面应用。**

Variable 已经不只是一款"应用"——它是一个覆盖在 Windows 桌面之上的**私人桌面环境**：
全屏壁纸层与桌面图标、Win11 风格任务栏与开始菜单、文件管理器与回收站、通知中心与
快捷面板、硬件状态面板、系统托盘、全局快捷键（Win 全家桶）、**虚拟窗口管理器
（VWM）**——四大功能空间与文件管理器全部以「虚拟窗口」的形式在环境内部打开，
支持拖拽、贴靠、最小化、多开、编组与舞台编排；第三方 Windows 软件更可以被**整体嵌入**
到环境里的虚拟窗口中（从系统任务栏与 Alt+Tab 里消失），配合 Wallpaper Engine 全类型
壁纸接入与环境级显隐切换键位，把整个 Windows 改造成一个只属于你的、与真实桌面
「隔离」的第二空间。

同时内嵌四个彼此独立、又可以通过「引用」互相跳转的功能空间：**写作库、无限画布
思维导图、代码通俗化引擎、命运推演沙盒**。

它不注册、不登录、不上传，所有数据（记录、思维导图、项目分析档案、命运推演树、
媒体附件、备份、日志）都只存在于你自己的电脑上；便携模式甚至可以把整个数据目录
放进 U 盘随身携带。它同时是一款「审美驱动」的软件——原生 WebGL 星空与极光背景、
电影级七层启动氛围、低饱和冷调视觉语言 2.0、自定义无边框窗口与启动剧场，让一个
严肃的生产力工具拥有宇宙级的氛围感。

> **一句话总结**：Variable = 一个可以把你现有 Windows「关在门外」的私人操作系统
> 皮肤 + 四个彼此互通的生产力空间 + 一个可以把任意第三方软件"关进"自己窗口里的
> 嵌入式窗口管理器。

---

## 〇、版本与能力总览

### 0.1 当前工作线（1.0sno9u.vxe：从应用到容器化私人桌面环境）

> 代码面 33 批完成，详见 `docs/MASTER-PLAN-1.0sno9u.vxe.md` 里程碑仪表。

在 v1.0.0「私人空间」之上，1.0sno9u.vxe 工作线把 Variable 升级为**容器化的私人
桌面环境**：

- **AI Hub 云端 AI 矩阵**：任务栏一键唤起 Claude Code / Codex / ZCode；便携 Node
  运行时 + `ai install`（进度事件流）；Vault 2.0 账号身份库 + 多账号切换
  （凭据只存保险箱加密区）；`ai verify` 断言凭据零落宿主；
- **隔离执行档**：一切受管进程的 HOME/凭据目录强制重定向进容器，残留扫描可验证
  「退出零残留」；
- **Uxv 容器（8TB 路线）**：单文件容器 + 掉电安全 journal（100 轮注入 0 丢失）+
  BLAKE3 去重 + LZ4/Zstd 压缩 + XChaCha20 加密 + 多卷条带 + GC + 迁移向导；
- **浏览器矩阵**：五款浏览器多 profile 隔离（数据目录全在容器），任务栏按
  profile 分组，首次导入向导；
- **编码体系**：VS Code Portable 一键部署 + Python/Go/Rust 工具链 + Git 只读面板
  + SSH 金库（ed25519 封存）+ 并行全库搜索 + 大文件分块查看；
- **子环境**：环境档切换（{envhome} 凭据隔离）+ 嵌套实例（深度 ≤3）+ 环境克隆；
- **网络层**：环回代理（默认零出站的执行点）+ 域名白名单 + kill-switch + 流量仪表；
- **安全工作台**：PE 静态解析（熵/导入/可疑 API）+ 入口反汇编（iced-x86）+
  Sandbox 编排（不可用如实降级）+ 报告导出——样本绝不在宿主执行；
- **应急包**：`--export-rescue` / `--repair` / `--force-raster` / `--revoke-list`
  CLI（无需 GUI 抢救容器）+ 紧急吊销清单导出。

> 实机验收（三宿主矩阵）进行中；实机项结论以 `docs/acceptance/` 归档为准。

### 0.2 ENGINE Version 01XHI9DN.1.5xw：五计划合一，355 项功能全景

在 1.0sno9u.vxe 之上，项目以 **ENGINE Version 01XHI9DN.1.5xw** 为总纲完成了
五份递进规划的合并与实施（原文合并、一字未改，见
[`docs/ENGINE-Version-01XHI9DN.1.5xw-功能全景.md`](docs/ENGINE-Version-01XHI9DN.1.5xw-功能全景.md)）：

| 序 | 计划 | 代号 | 编号段 | 项数 |
|---|---|---|---|---|
| 1 | NEXT-40 | 第二曲线 | N-01…N-36 | 35 |
| 2 | ASCENT-60 | 登顶计划 | U-01…U-60 | 60 |
| 3 | APEX-70 | 工匠计划 | Z-01…Z-70 | 70 |
| 4 | SUMMIT-90 | 极顶计划 | M-01…M-90 | 90 |
| 5 | Version-1.2sein298h7mx.00wix | 化境计划 | V-01…V-100 | 100 |
| — | **合计** | — | Z 70 + U 60 + N 35 + M 90 + V 100 | **355** |

355 项功能按十五功能域重新归位：启动与品牌剧场、窗口与空间管理、桌面与图标表达、
任务栏与开始菜单、键盘与输入手感、文件与数据能力、效率与工具中枢、系统集成与硬件、
兼容性防线、安全与隐私、开放生态、视觉个性化与氛围、声音与通知、无障碍与本地化、
工程质量性能与收官。**全部 355 项已由 20 个 AI 协作组完成代码级交付**（分工见
《AI分工图》，进度见 [`docs/五路进度总览.md`](docs/五路进度总览.md)），全仓质量
收口：tsc 0 错误、vitest 1088 项通过、cargo check 干净、audit 与各专项门禁全绿。

---

## 目录

- 一、产品定位与设计理念
- 二、总体架构
- 三、桌面环境（Desktop Shell）：启动剧场 / 桌面分层 / 窗口管理
- 四、VWM 虚拟窗口管理器：环境内的一切窗口
- 五、桌面图标与文件架
- 六、任务栏与开始菜单
- 七、通知中心、快捷面板与硬件面板
- 八、文件管理器 2.0 与回收站
- 九、壁纸系统与 Wallpaper Engine 全类型接入
- 十、第三方软件启动器与嵌入子系统（embed）
- 十一、环境切换：双 Esc 与 Del+Backspace（kbdhook）
- 十二、写作空间（Library · 记录库）
- 十三、思维导图空间（Mindmap · 无限画布）
- 十四、项目分析空间（PVCCE · 代码通俗化引擎）
- 十五、命运推演空间（FTPE · 行为树命运引擎）
- 十六、视觉系统与视觉语言 2.0
- 十七、国际化、搜索与设置
- 十八、隐私、数据与便携（U 盘 / 保险箱 / 焚毁 / 数据安全中心）
- 十九、效率与工具中枢（命令面板 / 剪贴板 / 截图 / 宏）
- 二十、系统集成与硬件中枢（系统中枢七页）
- 二十一、兼容性防线（适配等级 / IME / 全屏独占）
- 二十二、开放生态与开放工具（variable:// / .vxs / IPC 网关）
- 二十三、无障碍 2.0（Accessibility）
- 二十四、氛围与个性化（音景 / 节律 / 农历 / 主题工坊）
- 二十五、声音与通知运行时（NotifyRuntime）
- 二十六、工程质量、性能与收官（门禁体系）
- 二十七、环境要求与开发运行
- 二十八、Windows 打包与验收
- 二十九、数据目录与隐私边界
- 三十、备份、恢复、导入与导出
- 三十一、快捷键手册
- 三十二、测试覆盖说明
- 三十三、AI 协作开发模式（20 组 × 355 项）
- 三十四、下一阶段：便携虚拟系统与 6 大件
- 三十五、常见问题（FAQ）
- 三十六、已知边界与路线图
- 附录 A：一次典型使用旅程
- 附录 B：命名与约定速查
- 附录 C：批次演进史（E-8 → E-18）
- 附录 D：项目文档索引
- 附录 E：功能全景十五域分布表

---

## 一、产品定位与设计理念

### 1.1 这是什么软件

Variable 是一个运行在 Windows 桌面上的**私人桌面环境 + 单窗口多空间应用**，它由
四个层次叠加而成：

1. **桌面环境层**：全屏壁纸 + 桌面图标网格 + 任务栏 + 开始菜单 + 文件架 +
   通知中心 + 快捷面板 + 系统托盘 + 全局快捷键 + 窗口管理（红绿灯 / Win+Tab /
   分屏贴靠 / 编组编排），把 Windows 桌面「接管」成一个属于自己审美与工作流的空间；
   通过双击 Esc 甚至可以在「环境」与「真实 Windows 桌面」之间瞬间切换——
   环境隐藏时看到的是你原本的 Windows 桌面与 Wallpaper Engine 壁纸，
   切回来时一切如初；
2. **虚拟窗口管理器（VWM）**：环境内部的轻量级窗口系统。四大功能空间、
   文件管理器、回收站，以及**被嵌入的第三方软件**，都以虚拟窗口的形式运行，
   拥有独立的 Z 序、聚焦态、拖拽移动、边缘贴靠、最小化飞行动效、多开、编组
   与舞台编排能力；
3. **四个功能空间**：
   - **写作空间（Library）**：层级文件夹 + 富文本记录 + 标签 + 收藏 + 回收站 +
     全文搜索 + 本地媒体；
   - **思维导图空间（Mindmap）**：无限画布、自由节点、丰富的连线样式、小地图、
     节点绑定记录；
   - **项目分析空间（PVCCE）**：把任意本地代码项目「翻译成大白话」，七级下钻、
     逐行解剖、推理链、意图生成、写回源文件；
   - **命运推演空间（FTPE）**：基于特质权重与确定性种子的行为树命运模拟引擎；
4. **第三方软件子系统**：开始菜单扫描、便携化登记、原生图标提取（128px 高清）、
   进程深度检测、Wallpaper Engine 壁纸项目接入，以及把第三方软件窗口**嵌入环境
   内部**的 embed 子系统。

在四大层次之上，v1.x 工作线还叠加了一整套**环境级能力中枢**：效率与工具中枢
（命令面板、统一搜索 2.0、剪贴板历史、截图标注、宏引擎）、系统中枢（七页硬件
与系统管理工具窗口）、兼容性防线、开放生态（variable:// 协议、.vxs 资源包、
开放 IPC 网关）、无障碍 2.0、氛围与个性化引擎、声音与通知运行时——它们共同构成
「比肩顶级桌面作品」的第二曲线。

### 1.2 设计理念

- **隐私即默认**：零网络请求、零遥测、零账号。数据库、媒体、备份全部落在本机；
  便携模式甚至可以把整个数据目录放进 U 盘随身携带。通讯软件提醒也只读取
  **窗口标题**里的未读计数标记，聊天正文永远不经过 Variable。任何联网能力
  （出站桥、IPC 网关、事件流）默认关闭，开启必须显式确认且可永久关闭。
- **隔离即安全**：环境与真实 Windows 之间是「软隔离」——第三方应用被启动为
  独立 OS 进程（DETACHED spawn），与 Variable 无 WebView 关系；嵌入只是窗口
  层级的重父级（SetParent），应用崩溃不影响环境，环境关闭也不牵连应用。
  Variable 对系统能力的边界保持诚实：它能做用户态进程管理与窗口集成，
  但绝不假装自己是内核。
- **审美驱动**：原生 WebGL 星空与极光背景、低饱和冷调语义色板（tokens.css 2.0
  语义层，禁止裸色值）、材质引擎与微交互矩阵、电影级七层启动氛围（双层极光幕 +
  三层尘埃粒子场 + 流星 + 地平线暖光 + 暗角 + 胶片颗粒 + CRT 扫描线）、自定义
  无边框窗口、启动剧场六件套——桌面环境本身就是作品。四大功能空间的图标各自
  拥有独立的视觉语言（钢笔尖墨迹 / 青色节点星图 / 像素扫描线 / 金色星盘），
  桌面、任务栏、开始菜单三处一致（iconRegistry 统一登记）。
- **启动即进入**：窗口打开的第一眼就是启动剧场（真实事件驱动，无合成时间线），
  落位之后桌面、图标、任务栏交错淡入；键盘可达、快捷键齐全，打字时背景自动
  降级渲染保证输入零延迟。
- **无模板哲学**：尤其在命运推演里，人格、性格、主义思想全部是抽象自构条目，
  没有任何真人模板——它是一个「思想实验沙盒」，不是算命工具。
- **零 AI 依赖**：项目分析的「大白话翻译」完全由本地词典 + 规则引擎生成，
  不调用任何在线大模型，同样的话术在十年后的离线机器上依然可用。
- **可重现与可追溯**：命运推演同种子完全可重现；源文件写入先备份再原子落盘、
  支持 Ctrl+Z 回滚；数据库写操作有事务与恢复文件兜底；窗口关闭事件写取证日志。
- **如实降级**：全局快捷键被系统/他方占用时逐项注册失败并诚实降级为任务栏 /
  开始菜单入口，仅记录日志；硬件能力（Wi-Fi 无线电、蓝牙设备）不可用时面板
  如实显示状态而不是假装成功；不可嵌入的第三方应用（UWP / 管理员权限 /
  无标准窗口）如实回退为独立窗口运行。**不提供假功能**是贯穿始终的纪律，
  全局 DoD 口径「无未经实测的 ✅」同样贯穿文档与代码。
- **绝不伤害用户进程**：嵌入失败、捕获超时等一切异常路径上，Variable 绝不
  终止第三方进程——应用总是保持运行，用户随时可以手动接管。宏引擎配急停、
  UAC 判停；计划任务工坊有动作白名单与 30 秒运行时上限。
- **克制的动效纪律**：所有动效遵循 reduce-motion 令牌与 80ms 全局降档规则，
  动效时长支持全局缩放（--dur-scale 0.5x/1x）；讲述人开启时自动叠加
  reduce-motion。宁可克制，不堆砌。

### 1.3 目标用户

- 需要一个完全私密写作环境、并想把 Windows 桌面改造成自己审美空间的个人作者；
- 用思维导图整理灵感、并希望节点能直接挂接长文记录的人；
- 想快速读懂一个陌生代码库（自己的旧项目、开源项目）的开发者；
- 喜欢做人生推演、角色设定、思想实验的创作者与玩家；
- 想要一个「工作时与娱乐/通知隔绝」，又能一键回到真实桌面的专注环境使用者；
- 需要在网吧、公司电脑等非自有设备上随身携带完整工作环境的重度用户
  （便携虚拟系统路线，见第三十四章）。

---

## 二、总体架构

### 2.1 技术栈

| 层 | 技术 |
| --- | --- |
| 桌面壳 | Tauri 2.x（Rust 后端 + 系统 WebView2，无捆绑 Chromium） |
| 前端框架 | React 18 + TypeScript 5（`strict` 严格模式全量通过） |
| 构建工具 | Vite 5（多页面入口，开发端口 1420，`tsc --noEmit` 先行类型检查） |
| 富文本编辑器 | TipTap 2（ProseMirror 生态：starter-kit + 颜色/图片/占位/对齐/下划线/自定义引用 Mark） |
| 本地数据库 | rusqlite（SQLite，WAL 模式，版本化迁移；通知存档同引擎） |
| 图标库 | lucide-react + 自研 AppGlyphs / iconRegistry 矢量图标（四空间专属视觉语言） |
| 视觉渲染 | 原生 WebGL / WebGL2（星空 worker 管线 + Ray-Marching 极光管线），无三方渲染库 |
| 音频 | Web Audio 程序化音景（零音频资产）+ 系统媒体会话控制 |
| Windows 集成 | windows crate（Win32 枚窗 / 进程快照 / ShellExecute / CM_ 设备 API）、WinRT（Wi-Fi 无线电） |
| 测试 | Vitest（前端单测 1088 项）+ cargo test（后端，含 e2e 命令面测试） |
| 门禁 | audit.cjs（IPC/i18n/TESTID/视觉四合一）+ keymap-audit + aria-audit + 链接检查 |

### 2.2 进程与通信模型

```
┌─────────────────── WebView2（前端，6 个页面入口）─────────────────┐
│ desktop.html      桌面环境 shell（壁纸/图标/VWM/任务栏/开始菜单…）  │
│ explorer.html     文件管理器（独立窗口遗留回退；环境内走 VWM 内嵌） │
│ app-write/mind/code/fate.html  四大功能空间（独立窗口遗留回退）     │
│   React 组件树                                                     │
│    ├─ lib/ipc.ts     类型化 IPC 封装 + 错误归一化                   │
│    ├─ lib/saveQueue  同键串行、latest-wins 的保存队列               │
│    └─ state/uiStore  全局 UI 状态（模式/焦点/吐司/托盘）            │
└───────────────△───────────────────────┬───────────────────────────┘
                │ invoke (JSON)         │ Tauri IPC / event
┌───────────────┴───────────────────────▽───────────────────────────┐
│ Rust 后端（src-tauri）                                             │
│  ├─ shell/      桌面环境集成（详见 2.3）                           │
│  ├─ library     文件夹与记录 CRUD / 搜索 / 回收站                   │
│  ├─ mindmap     导图与节点/连线批量存储                             │
│  ├─ media       媒体导入 / data-url 落盘 / 附件                     │
│  ├─ workspace   内置工作区目录树操作                                │
│  ├─ project_scan 项目扫描（噪声目录跳过/越界拒绝）                  │
│  ├─ backup      备份与恢复（quick_check 校验）                      │
│  ├─ export      记录/导图/工作区 导出与导入                         │
│  ├─ settings_cmd 设置 KV（事务 upsert + 跨窗口广播）/ 恢复文件管理  │
│  ├─ notify      通知存档 SQLite / 提醒中心 / 声音方案               │
│  └─ db/state    连接、迁移、日志、便携模式判定                      │
└────────────────────────────────────────────────────────────────────┘
```

前端通过 `lib/ipc.ts` 把全部 Tauri command 收敛为带类型的调用面；
`tools/audit.cjs` 静态审计「前端调用的命令都有 Rust 注册、i18n 键完整、testid
完整、视觉令牌契约不违约」，防止前后端命令面漂移。Rust 侧统一错误类型
（`error.rs`）把底层错误翻译成可展示的中性文案，避免裸 panic 直接击穿 UI。
`set_settings` 采用「事务写入 + 跨窗口事件广播」模型：设置变更以
`settings://changed` 事件（携带来源窗口与变更键列表）广播给所有 webview，
前端按 origin 过滤回声，保证壁纸、语言等设置在多窗口间实时同步；设置项全部
74 项由 `gen-settings-doc` 自动再生成 [`docs/SETTINGS.md`](docs/SETTINGS.md)，
杜绝文档漂移。

### 2.3 桌面环境后端（shell/ 模块族）

| 模块 | 职责 |
| --- | --- |
| `tray.rs` | OS 系统托盘：图标 + 菜单（打开桌面/退出），失败不阻断启动 |
| `winman.rs` | 全局快捷键注册与动作分发表（SHORTCUT_MAP → dispatch_action） |
| `kbdhook.rs` | 环境级显隐键：30ms `GetAsyncKeyState` 轮询线程实现双击 Esc 切环境 / Del+Backspace 真退出（全局状态与焦点无关） |
| `embed.rs` | 第三方应用嵌入：进程树窗口捕获、SetParent 重父级、边界/焦点/关闭控制（详见第十章） |
| `usb.rs` | U 盘拔出保护：仅便携模式生效，1s 轮询数据卷 + 周期 WAL checkpoint |
| `imwatch.rs` | 通讯软件未读提醒：3s 轮询窗口标题的「(N)」标记，只读标题不读正文 |
| `hardware.rs` | 硬件面板：CPU/内存/磁盘/电池、Wi-Fi 状态与无线电开关（WinRT Radio）、蓝牙设备列举与启用/禁用（CM_Enable/Disable_DevNode） |
| `launcher.rs` | 第三方软件：开始菜单 .lnk 扫描（COM IShellLink）、128px 高清图标提取（icon_dataurl_hd）、进程深度检测、ShellExecute 启动、便携化 |
| `app_windows.rs` | 四大空间 + 文件管理器的多窗口创建与标签路由（遗留回退通道） |
| `wallpaper.rs` | 壁纸池目录、多显示器信息、壁纸设置、Wallpaper Engine 扫描与打开 |
| `recycle.rs` | 回收站：系统回收站列举/还原/清除 + 容量统计 |
| `boot.rs` | 真实启动加载编排，进度以 `boot://event` 流式推给前端（无合成时间线） |
| `privacy.rs` / `netconsent.rs` | 联网确认弹窗与隐私横幅 |
| `notify` 族 | 通知存档（SQLite 全文搜索）、提醒中心（重启补发）、逐应用音量记忆、通信设备快切 |
| `a11y.rs` | 系统辅助功能桥：粘滞/筛选键/高对比度/讲述人只读探针（STICKYKEYS 等） |
| `sysinfo.rs` / `xflow.rs` / `appman.rs` | 系统信息与运行态支撑 |

### 2.4 前端代码结构

```
src/
├─ App.tsx                  空间路由、全局点击捕获、关闭保存流程、恢复提示、
│                           I18n Provider、IM 消息事件接入
├─ entries/                 六个 Vite 多页面入口（desktop/explorer/四空间）
├─ components/              TitleBar、Modal、ContextMenu、ToastHost、
│                           WindowControls、CloseLight、AppGlyphs、
│                           ErrorBoundary、ConfirmHost、EmptyState、
│                           SpotlightTour（新手导览）
├─ system/
│  ├─ boot/                 BootScreen 启动剧场 + BootAtmosphere 七层氛围
│  │                        （极光幕/尘埃场/流星/暖光/暗角/颗粒/扫描线）
│  │                        + CapsuleBar 游戏级胶囊进度 + FileTicker 文件流带
│  ├─ desktop/              DesktopShell（桌面 shell 总装）、Taskbar、StartMenu、
│  │                        WintabSwitcher（Win+Tab 切换器）
│  ├─ desktop-icons/        DesktopIcons（图标网格/多选/拖拽/文件架/隐藏模式/
│  │                        排列/锁定/密度/敲字定位/模板中心/归档建议）
│  ├─ windows/              vwm.ts（VWM 状态机）、VirtualWindowManager、
│  │                        VirtualWindowFrame、VwmAppContent、
│  │                        orchestrate.ts（编排中心）、stageRail（舞台侧幕）、
│  │                        multiselect（多选工具条）、appWindows（遗留窗口路由）、
│  │                        snap（分屏贴靠预览）
│  ├─ wallpaper/            WallpaperLayer（静态/轮播/视频/网页壁纸 + 多显示器）
│  │                        + LivingWallpaper（living 活化：粒子叠加 + Ken Burns）
│  ├─ explorer/             文件管理器 2.0 窗口（多标签/双栏/分栏/分页虚拟化、
│  │                        Variable 目录侧栏、内嵌模式）
│  ├─ recycle/              回收站窗口
│  ├─ taskbar/              任务栏（小组件区、托盘、时钟、VWM 运行态联动、
│  │                        农历节气悬停卡、今日简报卡）
│  ├─ quick/                QuickPanel 快捷面板（Wi-Fi/蓝牙/媒体/勿扰…）
│  ├─ hardware/             硬件面板（CPU 波形、内存、磁盘、电池）
│  ├─ launcher/             第三方软件管理（扫描/添加/便携化/嵌入启动）
│  ├─ tray/                 托盘横幅（隐私提示）
│  ├─ welcome/              欢迎向导（首次启动）
│  └─ startmenu/            最近使用记录（recent.ts）
├─ apps/                    write / mind / code / fate 四大空间实现
├─ features/                编辑器、导图画布、项目分析、命运推演、搜索、设置、背景、
│                           命令面板、剪贴板历史、截图标注、宏引擎、系统中枢、
│                           数据安全中心、开放生态、无障碍、氛围引擎
├─ lib/                     ipc、settings、sanitize（HTML 净化）、pathGuard（路径防穿越）、
│                           saveQueue、mindmapFile（.mindmap/.project/.fatetree 读写）、
│                           pinyin.ts（拼音/首字母搜索）、shortcuts.ts（快捷键表/冲突检测）、
│                           xref.ts / xrefStale.ts（跨空间引用协议）、localeFormat（区域格式）
├─ i18n/                    简体 / 繁體中文 / English 三语文案 + s2t.ts 简繁转换
│                           + 用户词表（CSV 导入导出）
└─ state/uiStore.ts         全局 UI 状态（模式/焦点/恢复提示/吐司/托盘）
```

### 2.5 全局交互细节（App 层的工程质量）

- **捕获阶段「点击空白即销毁」状态机**：`App.tsx` 在 capture 阶段监听 `pointerdown`，
  任何不属于浮层面板（右键菜单/节点菜单/连线气泡/检查器）与文本框的左键点击，
  都会强制销毁全局激活状态——没有任何浮层能靠 `stopPropagation` 逃避关闭。
- **打字降级渲染**：监听可编辑区域的单字符按键，1.5 秒内标记 `editing`，
  让 WebGL 背景自动降帧/降级，保证输入永远跟手；空闲渲染冻结（idle freeze）
  进一步在无人操作时停掉非必要渲染。
- **关闭前保存兜底**：窗口关闭先走保存流程，失败时弹出
  「取消关闭 / 重试保存 / 强制关闭」三选模态，绝不静默丢稿。
- **异常关闭恢复**：崩溃或强杀后，重启时恢复提示宿主会列出恢复文件
  （标题/时间/预览），一键恢复为新记录或删除。
- **宿主齐全性**：桌面分支挂载全部交互宿主（ConfirmHost / ChoiceHost /
  PromptHost / ConfirmBubbleHost / NetConsentHost），所有 `ask*` Promise 都有
  resolve 归宿——历史上曾因漏挂宿主导致「点了没反应」，已根治并有回归意识。
- **错误叙事统一**：EmptyState 空态组件与错误叙事规范（AI-17 视觉语言组）让
  每一处失败都有人话解释与下一步动作，不出现裸报错。

---

## 三、桌面环境（Desktop Shell）：启动剧场 / 桌面分层 / 窗口管理

### 3.1 启动剧场（BootScreen · 六件套）

启动时**真实加载**：数据库迁移、库统计、媒体索引等真实进度以 `boot://event`
流式推给前端，全程**没有合成时间线**——进度条走多快取决于机器实际多快。启动
剧场六件套（U-01…U-06 + Z-43…Z-49）包括：

- **真实事件驱动剧场**：阶段推进、字标、进度、文件流全部由后端真实事件驱动；
- **品牌字标**：开机字母落位动画，与产品视觉语言一致；
- **CapsuleBar 胶囊进度**：全字标宽度、游戏 HUD 风格加载条——右上角百分比、
  24px 分段刻度、扫光周期掠过；进度严格跟随真实加载事件；
- **FileTicker 文件流带**：底部滚动的当前加载文件走马灯，纯英文 Consolas
  大写字距微文案（无中文分支、零 emoji），与游戏 HUD 风格统一；
- **启动交响三档**：可选的三档启动音效方案（关闭/轻/全），声音方案包有事件
  白名单与数值范围校验；
- **仪式编排器三节奏**：加载完成后进入阶段 4/5 退出编排——字母落位 → 任务栏
  从中央展开 → 桌面图标交错淡入 → 红绿灯由暗态激活，三种节奏曲线可选。

**启动氛围工程（BootAtmosphere，七层光效）**：启动仪式的背景不是一张静态图，
而是电影级七层合成——① 双层极光幕（WebGL 褶皱飘带，纯白蓝→冰蓝→边缘樱花
冰晶粉）；② 三层尘埃粒子场（不同深度、不同漂移速度）；③ 偶发流星（带头部
辉光与拖尾）；④ 地平线暖光（日出前的暖色渐变）；⑤ 电影暗角（vignette）；
⑥ 胶片颗粒（film grain）；⑦ CRT 扫描线。动画强度随真实加载进度同步
（0.35→0.55 渐强），`reduce-motion` / `safeMode` / `static` 设置下完全停用。

数据就绪后推送一次「本地数据就绪」通知（数字来自真实 BootStats：工作区
文件数、记录数、导图数），以及一条「磁盘同步通知」；首次启动（向导未完成）
显示**欢迎向导**，引导隐私声明与基础设置，点击窗口关闭灯即可跳过并完成向导。

### 3.2 桌面 Shell（DesktopShell）

桌面窗口全屏覆盖 Windows 桌面（默认覆盖系统任务栏，实现环境视觉完全独立；
可在设置→外观切换为「避让任务栏」），纵向分层：

```
壁纸层（静态/轮播/视频/网页/living 活化壁纸，多显示器，1:1 原样渲染无叠加滤镜）
  └─ VWM 虚拟窗口层（z=15：图标之上、任务栏之下）
      └─ 桌面图标网格（三档大小、多选、框选、拖拽、文件架、抖动编辑模式、
          可整体隐藏、排列/锁定/密度/敲字定位/模板中心/归档建议）
          └─ 底部 Win11 风格任务栏（小组件区 + 托盘 + 时钟）
              └─ 开始菜单 / 快捷面板 / 通知中心 / 硬件面板
```

壁纸层对桌面壁纸做了「原样渲染」处理：没有应用内文字背景专用的模糊/暗罩/
暗角叠加，壁纸 1:1 清晰锐利；四款软件内部的背景光影不受影响。**living 活化
模式**（LivingWallpaper）在静态壁纸之上叠加本地粒子层与 Ken Burns 缓动推拉，
让静态图也拥有呼吸感——该模式遵循与动态壁纸相同的性能档位与 reduce-motion
纪律。

### 3.3 窗口红绿灯

Variable 的窗口控制灯统一为三处：桌面壳层、VWM 虚拟窗口、Modal/浮层
（CloseLight 单灯组件），顺序为 **左绿 / 中黄 / 右红**：

- 🟢 **绿灯**：桌面壳层=退出选择框（隐藏到托盘 / 完全退出，未保存先走保存
  冲刷流程）；VWM 窗口=关闭本窗口（170ms 缩小淡出关闭仪式）；
  Modal/浮层=关闭本界面（浮层无最小化/最大化语义，不提供假灯）；
- 🟡 **黄灯**：全屏切换（toggleMaximize），VWM 中为最大化/还原，带平滑滑入动效；
- 🔴 **红灯**：最小化——桌面壳层收起；VWM 窗口播放 200ms「飞向任务栏」动效后
  隐藏（display:none 保活挂载，恢复零重载）。

### 3.4 窗口管理

- **虚拟窗口优先**：四大空间、文件管理器、回收站默认在桌面环境内以 VWM
  虚拟窗口打开（详见第四章）；OS 级独立窗口通道（app-*.html 拆窗入口）完整
  保留为遗留回退——xref 跳转若发生在非桌面窗口、或用户直接运行独立入口，
  仍走旧路径，两条通道互不干扰；
- **Win+Tab 切换器**（可选功能）：后端注册 `super+tab` → `sys://wintab` 事件 →
  唤起自绘切换器，列出全部 Variable 窗口（已并入 VWM 虚拟窗口的 Z 序），
  ↑/↓ 选择、Enter/点击聚焦、Esc 关闭；
- **分屏贴靠**：拖动虚拟窗口到屏幕边缘出现贴靠预览（SnapPreviewHost），
  松手即贴靠左右半屏/四角 1/4；顶部最大化；Win+方向键贴靠、Win+M 最小化；
  双击标题栏切换最大化；Alt+Tab（环境内）轮转窗口；
- **编排中心（Ctrl+Alt+O）**：WindowOrchestrator 提供舞台级窗口编排——
  StageRail 舞台侧幕列出全部窗口，多选工具条支持互换位置、一起最小化、
  一起关闭（含确认门）、编组拖拽与组贴靠；焦点历史 Ctrl+Alt+[ / ] 逐级回溯；
  Esc 退出编组（与拖拽回弹 Esc 互斥仲裁）；
- **全屏应用避让**：检测到全屏应用运行时，任务栏自动避让，
  不遮挡游戏/视频画面（全屏独占协议详见第二十一章）。

### 3.5 覆盖 Windows 行为

- **联网确认弹窗**：任何首次尝试出站请求的动作前弹窗确认（默认无出站）；
- **Win 键全家桶**：Win+E 文件管理器、Win+D 显示桌面、Win+N 通知中心、
  Win+S 开始菜单搜索、Win+Tab 窗口切换（默认表采用 ctrl+alt+* 变体规避
  Win11 系统保留键，详见第三十一章）；
  被系统占用时逐项降级并记日志；
- **托盘常驻**：关闭到托盘后 Variable 继续运行（IM 提醒、快捷键等后台能力
  保持），托盘菜单可随时唤回；
- **桌面图标隐藏模式**：右键「隐藏图标/显示图标」，隐藏时跳过图标与框选
  渲染（`variable:icons:hidden` 持久化），右键菜单仍可用——纯壁纸观影模式；
- **桌面纯净模式（Ctrl+Alt+P）**：一键收起图标、小组件与通知横幅的深度专注
  模式（比隐藏图标更进一步，含任务栏最小化选项）。

---

## 四、VWM 虚拟窗口管理器：环境内的一切窗口

VWM（Virtual Window Manager）是 Variable 桌面环境的「窗口系统」：它把原本
各自弹 OS 窗口的应用全部收进桌面环境内部，以 DOM 层虚拟窗口的方式托管。

### 4.1 核心能力

- **独立 Z 序与聚焦态**：点击置顶、点击外部失焦；Alt+Tab 轮转按 Z 序降序；
- **拖拽移动 / 八向缩放**：标题栏拖拽移动（拖拽半透明 + 抬起阴影），边缘
  八向手柄缩放；**实时尺寸提示**（V-23，px 双列显示，Shift 等比锁定）；
  **拖拽 Esc 回弹**（V-30，拖拽中按 Esc 撤销本次移动）；
- **边缘贴靠**：拖到屏幕左/右缘=半屏，四角=1/4，顶部=最大化，带实时预览；
  Win+方向键键盘贴靠；贴靠平滑滑入（.snapping 动效）；
- **最小化到任务栏**：200ms「飞向任务栏」动效后隐藏——**display:none 保活
  而非卸载**，恢复零重载（Write 的光标与滚动位置、导图的视口都不丢）；
- **同应用多开**：任务栏图标中键 / forceNew 参数新开实例（例如同时开两个
  Write 看两篇记录、两个 Explorer 看两个目录）；
- **编组与舞台**：多选编组（V-24）、组贴靠、组拖拽、互换位置（V-26）、
  一起最小化/关闭（确认门）；StageRail 舞台侧幕 + 焦点历史回溯（V-25）；
- **经典系统菜单（V-21）**：Alt+Space 唤起虚拟窗口的经典菜单（还原/移动/
  大小/最小化/最大化/关闭），键盘党福音；该键位按 Z-09 处置「yield 桌面层
  可消费」；
- **画中画（N-04）**：任意虚拟窗口可切画中画小窗置顶；
- **色带（V-29）**：按应用分配窗口色带，多开同类窗口一眼区分，配置带版本号
  即时重渲染；
- **无响应检测**：窗口内容无响应时的视觉提示与恢复建议（AI-01 窗口手感组）；
- **几何持久化**：每个虚拟窗口的位置/尺寸按 `variable:vwm:geom:v2` 存储，
  工作区边界按任务栏四向位置自动推导；重开还原；
- **红绿灯**：左绿（关闭）/中黄（最大化）/右红（最小化），与整体视觉一致。

### 4.2 托管对象

| 虚拟窗口 | 打开方式 | 说明 |
| --- | --- | --- |
| Write / Mind / Code / Fate | 桌面图标、开始菜单、launchIndex | 四空间视图原样挂载，CosmicBackground 置于窗口包含块内，多窗口光影零差异 |
| Explorer 文件管理器 | Win+E / Ctrl+E / 任务栏 / 文件架 | 内嵌模式（无自带标题栏），支持带路径新开实例 |
| Recycle 回收站 | Ctrl+Alt+B / 任务栏 | 单实例 |
| 系统中枢 / 数据安全中心 | 快捷面板 / 设置 / 命令面板 | 独立工具窗口（七页 / 十二面板，见第二十、十八章） |
| 第三方应用（tp:\<id\>） | 桌面第三方图标 / 开始菜单 | 占位虚拟窗口 + 原生窗口嵌入（见第十章） |

### 4.3 关键实现细节

- **translateZ 包含块技巧**：`.vwm-app` 用 `transform: translateZ(0)` 建立
  包含块，把 `position:fixed` 的 `.bg-root`（WebGL 背景）约束进虚拟窗口——
  这是「每个窗口自带光影、随窗口移动」的关键；同理，内嵌的
  `.ex-window`（Explorer）降级为 `absolute`，避免 fixed 层盖住标题栏；
- **弹簧动画**：打开/恢复用 vwm-in 弹簧动画（scale/translate 使用独立 CSS
  属性而非 transform，避免破坏 translateZ 包含块）；
- **任务栏联动**：任务栏运行态 = VWM 窗口列表；点击 = 开/聚焦/最小化切换；
  中键 = 强制多开；悬停关闭 = 关最上层实例；
- **贴靠与桌面窗口自跳过**：applySnap 对桌面窗口自身跳过，VWM 贴靠与
  遗留 snap 逻辑互不冲突；
- **编排定时器治理**：orchestrate.ts 全部定时器返回清理句柄，杜绝泄漏
  （终检会话修复项）。

---

## 五、桌面图标与文件架

### 5.1 图标网格

- **三档图标大小**（32 / 48 / 64px，字形尺寸对齐 Windows 标称值）一键切换，
  密度档位（紧凑/舒适/宽松）与网格几何（对齐/流水）可调；
- **多选与键盘操作**：Ctrl 点选、Shift 范围选、Ctrl+A 全选、鼠标框选；
  Delete 删除、F2 重命名、Ctrl+C/X/V 复制剪切粘贴、Ctrl+Z/Y 撤销重做、
  Alt+Enter 属性；
- **敲字定位（V-96）**：图标网格上直接敲击应用名首字母（拼音）即高亮定位，
  与开始菜单搜索同源；
- **排列与锁定**：自动排列（名称/类型/频率）与自由布局双模式，布局可锁定
  防误拖；
- **模板中心（V-08）**：新建右键内提供模板（空白记录/会议纪要/周报…），
  模板可自定义；
- **归档建议（V-09）**：长期未使用的图标给出归档建议，一键移入文件架；
- **拖拽归架 / 拖入回收站**：把图标拖到文件架上完成归类，拖到回收站删除；
- **长按抖动编辑模式**：类似手机桌面，长按进入抖动状态后可拖拽重排、
  批量整理；
- **图标质感**：内高光 + 底部投影 + hover 抬起 + active 按压；右键「刷新」
  重放入场动画；
- **图标自定义与原生图标（128px 高清）**：支持 `.ico` / `.png`（≤512KB）
  替换应用图标；第三方应用无自定义图标时自动从目标 exe 提取 Windows 原生
  **Jumbo 128px** 图标（Rust 后端 `icon_dataurl_hd`，HICON→data URL，
  `.lnk` 先解析目标再提取），桌面/任务栏/开始菜单显示与系统一致且清晰锐利
  （此前 32px 提取在高分屏上发糊，已整体升级）；
- **隐藏模式**：右键一键隐藏/显示全部图标。

### 5.2 文件架（File Racks）

- **嵌套文件架**：架子可以无限嵌套，像文件夹一样组织桌面入口；
- **六色标识**：每个架子有六种颜色可选，一眼区分用途；
- **链接文件夹**：架子可以直接链接到磁盘上的真实文件夹，打开即文件管理器
  （环境内以 VWM 窗口呈现）；
- **飞出面板**：点击架子飞出悬浮面板展示内容，拖出即用。

### 5.3 右键菜单

桌面空白处与图标各自拥有完整右键菜单（含悬停展开的子菜单）：新建（记录/
导图/文件夹/文件架/从模板）、排序、刷新、显示设置、个性化（壁纸，含「切换到
系统桌面」）、任务管理器入口等，与 Windows 桌面习惯一致；右键高级菜单
（V-14）补充「以管理员运行/打开文件位置/复制路径」等进阶项。

---

## 六、任务栏与开始菜单

### 6.1 任务栏

- Win11 风格居中布局：开始按钮 + 固定应用 + 运行中应用 + **小组件区** +
  系统托盘 + 时钟；任务栏四向位置可调（上/下/左/右），VWM 工作区边界
  随之自动推导；
- **小组件区**：CPU 波形图（实时采样）、内存进度条、媒体控制
  （播放/暂停/上一首/下一首，读取系统媒体会话）、天气卡、今日简报卡
  （日程/待办/节律摘要，AI-18 氛围组）；
- **运行态**：运行中的应用来自 VWM 虚拟窗口列表，点击开/聚焦/最小化切换，
  中键多开，多实例徽标（同应用多开时角标计数）；
- **系统托盘**：Variable 托盘图标 + 第三方托盘图标；
- **时钟**：点击展开日历与通知中心；日期格式随 i18n 语言（含繁体「週」制）；
  悬停显示**农历节气节日卡**（本地万年历 2020–2040，含置闰计算，零网络）；
- **声景与音量**：托盘音量支持逐应用音量记忆与音量淡变防爆音。

### 6.2 开始菜单

- **Win 键 / ctrl+alt+s 变体呼出** + 弹出动画；固定网格支持**拖拽重排**
  （顺序持久化）；
- **搜索框（拼音/首字母）**：输入 `sz` 命中「设置」、`swdt` 命中「思维导图」、
  `bj` 命中「笔记」；支持原文子串、全拼、首字母三种匹配；Enter 转全局搜索；
- **索引条与高频分组（V-11/V-12）**：左侧索引条按类别快速跳转，高频应用
  自动分组（基于本地使用统计，14 天窗口）；
- **固定文件夹（V-13）**：开始菜单可固定常用文件夹直达；
- **最近使用**行：点击直达最近打开的记录/应用/系统位置（搜索时同步过滤）；
- **电源完整菜单**：锁屏 / 注销 / 重启 / 关机（危险操作二次确认，关机
  倒计时可取消）/ 退出 Variable；
- 本机用户名显示（仅本机读取，不出网）。

---

## 七、通知中心、快捷面板与硬件面板

### 7.1 通知中心（Win+N 变体 / 点击时钟）

- 按来源分组：系统 / 写作 / 通讯 / 提醒；
- **勿扰模式**一键开关（`toggleDnd`），开启后 IM 提醒等被抑制；**勿扰日程**
  支持跨午夜时段（如 23:00–07:00）、手动优先、排队回放（勿扰结束后按序补发）；
- 通知横幅从屏幕右上角滑入，自动消失，可在中心回看；
- **通知存档**：全部通知落 SQLite 存档，支持全文搜索、按应用筛选与保留
  策略（过期自动清理）；
- **通知交互进化**：支持动作按钮、进度条与持久组的有序回放；
- **智能通知三堆**：基于 14 天本地学习的「重要/普通/可延后」三堆分类，
  用户可手动覆盖（纯本地，零网络）。

### 7.2 通讯软件提醒（边界功能 · 如实声明）

- 监测对象：微信 / QQ / 企业微信 / 钉钉 / 飞书 / Telegram；
- 原理：3 秒轮询这些应用的**顶层窗口标题**，检测标题里的未读计数标记
  （「微信(3)」「【12】」等）；计数变化 → 推送通知 + 横幅；同一计数不重复打扰；
- **隐私边界**：聊天正文属于对方进程内存，Variable 不读取、也无法读取；
  消息正文永远不经过 Variable——只看标题，零网络；
- 应用关闭后自动解除记忆，下次新未读会重新提醒。

### 7.3 快捷面板（QuickPanel）

任务栏托盘区点击唤起，分区包含：

- **网络区**：Wi-Fi 状态展示 + **无线电开关**（WinRT Radio API，真实开关
  Wi-Fi 收音机，不可用时如实降级为状态展示）；
- **蓝牙区**：蓝牙设备列举 + **连接/断开**（CM_Enable / CM_Disable_DevNode
  实现设备级启用禁用，面板设备行可点击切换）；
- **媒体控制区**：播放/暂停/切曲；**通信设备快切**（播放/通话设备一键
  切换，eCommunications COM）；
- **勿扰开关**、屏幕亮度等快捷开关；
- 面板加载态使用骨架屏（skeleton shimmer，reduce-motion 降级为静态占位）。

### 7.4 硬件面板

- **CPU 波形图**：实时采样绘制的使用率曲线；
- **内存进度条**、磁盘分区容量、电池状态（含充电状态与电池健康度）；
- **显示器信息**：多显示器列举（设置页壁纸联动）；
- **麦克风使用指示**：ConsentStore 同源只读，应用占用麦克风时横幅提示
  （隐私透明）。

---

## 八、文件管理器 2.0 与回收站

### 8.1 文件管理器（Win+E / Ctrl+E 呼出）

- 环境内以 **VWM 虚拟窗口内嵌**打开（支持多实例，Ctrl+N 新开窗口带路径）；
- **多标签页浏览**、**双栏/分栏**（对照浏览）、**分页虚拟化**（万级目录
  不卡）；
- **Variable 目录侧栏节点组**：记录库、工作区、媒体、备份、日志等数据目录
  一键直达；
- 基础文件操作（新建/重命名/删除/复制粘贴）；
- **焚毁文件**：右键「焚毁」对敏感文件做安全删除（区别于普通删除）；
- 支持从资源管理器拖入工作区复制（`ws_copy_in`）、新建目录、重命名与移动、
  删除进回收站；工作区面板双击 `.mindmap` / `.project` / `.fatetree`
  直接进入对应空间；
- **文件管理器 2.0 十项强化（V-31…V-40）**：视图记忆（每目录记住布局/
  排序/筛选）、过滤高亮（按扩展名/大小/日期着色筛选）、复制路径（含 POSIX
  与反斜杠双格式）、拖拽徽标（拖拽中显示复制/移动标识）、属性对比（两文件
  并排 diff）、特殊名防呆（保留名/超长路径/非法字符就地拦截）、智能选取
  （同前缀/同类型一键加选）、预览锁定（预览窗格钉住后不随选中切换）、
  模糊跳转（敲字母模糊定位到当前目录条目）、提取向导（批量解压带格式
  识别与覆盖策略）。

### 8.2 回收站

- **系统回收站集成**：列举/还原/彻底清除；
- **排序与搜索**：按名称/删除时间排序，按关键字过滤；
- **删除确认**：清空回收站与彻底删除均有二次确认；
- **容量显示**：实时统计回收站占用空间；
- **回收站策略**：可配置按容量/按天数自动清理（数据安全中心）；
- 桌面图标可直接拖入回收站删除。

---

## 九、壁纸系统与 Wallpaper Engine 全类型接入

### 9.1 Variable 壁纸（五模式 + 工坊）

- **五类壁纸模式**：静态图片（image）/ 壁纸池轮播 / 视频（video）/
  **网页壁纸（web）**（本地 html 壁纸以 iframe 内嵌渲染，本地 asset 协议、
  允许 autoplay）/ **living 活化**（静态图 + 本地粒子叠加 + Ken Burns 缓动
  推拉，呼吸感动态化）；另有 **system 让位模式**：把桌面还给系统
  （Wallpaper Engine / Windows 壁纸），Variable 隐藏到托盘，点任务栏
  托盘 V 图标返回；
- **多显示器**：读取系统显示器列表，按显示器设置壁纸；
- **壁纸池目录**（`wallpaperPoolDir`）：放入图片自动轮播；
- **昼夜壁纸组**：按本地时间在晨/昼/暮/夜四组壁纸间自动过渡（节律助手联动）；
- **壁纸滤镜**：饱和度/明度/对比度可调（不破坏原文件，渲染层叠加）；
- **壁纸工坊 2.0（四引擎 + .vwp）**：壁纸方案存为 `.vwp` 工程文件——图层
  （静态/视频/网页/粒子四引擎）、变换（Ken Burns 参数）、滤镜、多显示器
  分配一站式编排，可导入导出分享；
- 右键「切换壁纸」对 image/video 类型在未选过文件时直接弹文件选择框，
  选完即用；
- 性能档位（`bgTier`）与减少动效（`reduceMotion`）全链路生效。

### 9.2 Wallpaper Engine 扫描与导入（零网络）

- **自动探测安装位置**：注册表 `HKCU\Software\Valve\Steam\SteamPath` 优先，
  补 C 盘默认路径 + `libraryfolders.vdf` 其余 Steam 库——Steam 装在任意盘
  都能找到；
- **扫描范围**：创意工坊 `workshop/content/431960/*` 与
  `common/wallpaper_engine/projects/{myprojects,projects}/*`；
  解析 `project.json`（兼容 utf-8-sig BOM）→ 标题/类型/入口文件/preview.jpg；
- **类型如实标注**：video/image → 可导入（视频壁纸/图片壁纸，一键导入后
  立即生效）；scene/web/application → 标注为不可直接渲染；
- **全类型打开**：
  - video/image → 走 Variable 壁纸链路；
  - web → 网页壁纸 iframe 内嵌；
  - **scene/application** → `wp_engine_open` 命令：前端只传项目目录名 id
    （校验拒绝路径分隔符/`../`/盘符），Rust 在白名单根内解析
    `project.json` 与 `wallpaper64.exe`，生成 `<数据目录>/we-open.cmd`
    包装脚本（`chcp 65001` 兼容中文路径），登记为第三方启动项（幂等）
    后复用既有启动通道独立进程执行
    `wallpaper64.exe -control openWallpaper -file <project.json>`——
    这是 Wallpaper Engine 官方控制接口，壁纸应用在系统桌面；
    配合「system 让位模式」即可完整体验场景壁纸；
- **安全边界**：路径全部服务端白名单解析，前端输入只有项目 id；
  进程启动复用已审查的通道，不在壁纸模块新起炉灶。

---

## 十、第三方软件启动器与嵌入子系统（embed）

### 10.1 登记与便携性三级

- **登记任意第三方 Windows 软件**（exe/lnk/bat/cmd），登记表仅存本机
  `<数据目录>/apps.json`；
- **便携性三级**：🟢 portable（数据目录内，随 Variable/U 盘整体迁移）/
  🟡 standalone（自包含单文件但绑定本机路径）/ 🔴 shortcut（安装型），
  自动判定 + 用户可覆盖；
- **开始菜单扫描**：递归扫描系统 + 用户开始菜单 `.lnk`（COM IShellLinkW
  解析目标），跳过卸载器/帮助项，目标去重后返回候选，含名称与图标；
- **拖入添加** + **便携化**（把已安装软件整目录复制进数据目录转为 portable，
  复制失败如实报错、登记不变）；
- **原生图标**：登记项无自定义图标时自动提取 Windows 原生 128px Jumbo
  图标（`icon_dataurl_hd`）；
- **深度检测**：进程级检测应用是否正在运行（含运行中标记）；
  软件已卸载时图标置灰并提示，可一键清理残留条目；
- **删除护栏**：仅数据目录内的文件允许彻底删除（防误删系统软件）；
  删除失败（软件运行中/文件被锁）报错并保留登记，绝不半途而废。

### 10.2 启动通道（安全模型）

- **直接启动**：登记 exe 用参数列表方式（`std::process::Command`，无 shell
  拼接）以 DETACHED + 新进程组启动，工作目录设为 exe 所在目录；
- **快捷方式启动**：`.lnk` 经 **ShellExecuteExW**（SEE_MASK_NOASYNC |
  NOCLOSEPROCESS）直接解析启动——不经任何 cmd 字符串拼接，且能取回目标
  进程 pid（供嵌入的进程树捕获使用）；
- **管理员运行**：ShellExecuteW `lpVerb="runas"`，UAC 弹窗由系统展示，
  用户取消（SE_ERR_CANCELLED）如实报错；
- 启动时自动撤销桌面窗口置顶，让第三方窗口浮于桌面之上；回到桌面自动恢复。

### 10.3 嵌入子系统（embed.rs）：第三方软件在环境内打开

第三方应用启动后，其主窗口被 **SetParent** 成 Variable 桌面窗口的子窗口
（WS_CHILD），随虚拟窗口移动/缩放，从系统任务栏与 Alt+Tab 消失——
实现与 Windows 桌面的视觉隔离。

**捕获流程（含启动器型软件的根治方案）**：

1. 启动前记录该应用已有的顶层窗口（避免把旧窗口误嵌）；
2. 通过启动通道取得**根进程 pid**；
3. 以 **Toolhelp32 进程快照展开 pid 树**（根 + 全部后代进程）——启动器型
   软件（如 Wallpaper Engine 的 wallpaper32/64.exe）真正的窗口属于它派生的
   子进程，按 exe 名匹配不到，按进程树就能命中；
4. 轮询 EnumWindows 等待新主窗口，**最长约 30 秒**（200ms 一次，命中即返回，
   慢启动应用不误报）；pid 树为空时按映像名后缀兜底匹配；
5. 命中 → 去标题栏/边框/系统菜单（WS_CAPTION|WS_THICKFRAME|WS_SYSMENU
   剥离 + WS_CHILD），SetParent 到桌面 HWND，先给占位边界，随后由前端
   随虚拟窗口上报精确物理像素边界（embed_bounds）。

**嵌入后的控制**：`embed_bounds`（随虚拟窗口移动缩放）、`embed_visible`
（最小化=SW_HIDE 隐藏，恢复=SW_SHOW）、`embed_close`（投递 WM_CLOSE，
应用自行退出）、`embed_focus`（点击虚拟窗口时移交键盘焦点）。

**失败路径（绝不伤害应用）**：

- 同一时刻只嵌入一个应用；新嵌入前，先前的嵌入先脱离回桌面；
- 捕获超时（30 秒）：应用**保持独立窗口运行，进程绝不终止、登记绝不删除**，
  提示「应用已在系统桌面独立运行，未受影响；稍后重新从环境内打开即可再试」，
  前端关闭占位虚拟窗口；
- 无法嵌入的应用（UWP / 管理员权限 / 无标准窗口）如实回退为独立窗口运行。

**嵌入声明协议（variable-embed.json）**：第三方应用可在安装目录放置
`variable-embed.json` 声明自己的主窗口类名/子窗口结构/ DPI 行为，Variable
据此优化捕获与缩放；规范见 [`docs/EMBED-MANIFEST.md`](docs/EMBED-MANIFEST.md)。

**隔离边界（如实说明）**：嵌入是用户态窗口层级操作——第三方应用依然运行在
系统同一会话的独立进程里，与 Variable 无父子进程关系、无沙箱、无内存配额。
Variable 对此保持诚实：它提供的是**视觉与窗口层面的隔离**，不是内核级沙箱
（那需要裸机 OS 项目，超出用户态应用的能力边界）。

---

## 十一、环境切换：双 Esc 与 Del+Backspace（kbdhook）

Variable 提供两组**全局键盘手势**（`kbdhook.rs`，30ms `GetAsyncKeyState`
轮询线程实现，全局状态与焦点/窗口可见性无关）：

| 手势 | 动作 |
| --- | --- |
| **双击 Esc**（500ms 内两次按下） | 环境 ↔ Windows 切换：环境可见 → 隐藏（露出真实桌面 / Wallpaper Engine 壁纸）；隐藏 → 显示 + 取消最小化 + 置焦 |
| **Delete + Backspace 同按** | 真正退出 Variable：先显示桌面窗口（保证 webview 活跃），再走保存冲刷 + 关闭流程（无确认框，未保存内容已由保存队列兜底） |

**为什么用轮询而不是低级钩子**：WH_KEYBOARD_LL 低级钩子在本环境的宿主进程里
会静默失效，且窗口隐藏后 webview 不再处理事件——「隐藏后再切回」不能依赖
前端监听。所以切换完全在 **Rust 侧**完成：轮询线程读到双 Esc 直接
show()/hide() 窗口，不经过任何事件链路。该方案经实机按键注入验证
（合成 Esc 序列 → 日志 `double-Esc -> toggle environment` → 窗口显隐正确）。

**设计原则**：绝不吞键（钩子/轮询只观察键盘状态，不拦截击键，输入法与
其他应用零影响）；轮询频率 30ms（CPU 占用可忽略）；手势状态机与
焦点/可见性完全解耦。

---

## 十二、写作空间（Library · 记录库）

### 12.1 层级文件夹

- 无限层级文件夹树，支持新建、重命名、拖拽/命令移动、排序；
- 删除进入**回收站**（软删除 `deletedAt` 时间戳），可恢复、可彻底清除、
  可一键清空；
- 重名拦截、跨层级移动的环检测在 Rust 侧 `library.rs` 以事务保证。

### 12.2 富文本记录

- 基于 TipTap 的所见即所得编辑器：标题层级、粗斜下划线、颜色、对齐、列表、
  引用、代码块、占位符提示、图片与**内嵌视频节点**（`VideoNode.ts` 自定义
  NodeView），字号档位由 `FontSize.ts` 统一管理；
- **跨空间引用锚点**（xref）：Write 中的 `XrefMark.ts` 自定义 Mark 承载
  `xref:` 锚点，随正文持久化；点击锚点可打开 Mind 窗口并定位节点；Mind
  右键「复制为引用」，Write 中 Ctrl+V 直接插入；引用位置用 ProseMirror
  Decoration 原地显示「已更新」徽标，保存后自动重校验（xrefStale）；
- 内容双列存储：`contentHtml`（渲染）+ `contentText`（全文搜索），保存前经
  `lib/sanitize.ts` 白名单净化——`<script>` 整段剥离、`on*` 事件属性剔除、
  `javascript:` 等危险 URL 拦截（均有 Vitest 用例覆盖）；
- 元数据：收藏星标、多标签（事务写入标签表）、创建/更新时间。

### 12.3 全局搜索（统一搜索 2.0）

`search_all` 一条命令同时返回四类命中：记录、文件夹、思维导图、导图节点，
带片段（snippet）与更新时间；LIKE 转义在 Rust 侧处理，用户输入 `%`、`_`
不会失控。`Ctrl+Shift+F` 唤起全局搜索浮层，`Ctrl+F` 为文内查找；
**纯字母查询时启用拼音/首字母回退**，对文档与导图标题做拼音匹配。
统一搜索 2.0（N-14）在此基础上支持自然语言式过滤（「本周修改的 带#工作标签」）、
结果聚合分组与搜索历史。

### 12.4 本地媒体

- 图片/视频/音频/任意附件通过对话框或**粘贴/拖拽**导入；
- `media.rs` 把文件复制进数据根 `media/`（按内容特征命名避免重名冲突），
  并在数据库登记媒体与附件两级关系；支持 data-url 直接落盘；
- 删除记录时未被引用的媒体可一并清理；媒体展示走受控的 Tauri asset 协议。

### 12.5 侧栏与工作区面板（Sidebar / WorkspacePanel）

- **文件夹树**：层级缩进展示，支持右键菜单、拖拽移动、计数徽标；回收站
  作为特殊节点出现，条目显示「恢复 / 彻底删除」；
- **记录列表**：标题、收藏星、标签胶囊与相对更新时间，支持排序与筛选；
- **工作区面板**：列出内置工作区目录中的 `.mindmap` / `.project` / `.fatetree`
  与任意文本文件，双击直接进入对应空间（环境内即虚拟窗口）；
- **标签云/筛选**：点击任意标签快速过滤全库记录。

### 12.6 保存模型（saveQueue）

- **同键串行**：同一记录/导图的保存请求按序执行，绝不交错；
- **latest-wins**：队列中同键的多个待存项自动合并为最新一次；
- **错误传播**：保存失败抛给调用方并触发 Toast，计入关闭前检查
  （有专门单测覆盖三种语义）。

### 12.7 字数统计与专注模式

- 实时字数统计（中英文混合计数规则在 `format.ts`，有单测）；
- F11 专注全屏；打字期间背景星野降帧，长文写作不因视觉特效掉帧；
- **焦点舱 2.0**（AI-18）：中央任务卡 + 台账（完成项归档）+ 节律顺延
  （番茄式节奏建议），写作专注的场景化封装。

---

## 十三、思维导图空间（Mindmap · 无限画布）

### 13.1 节点系统

- **八种形状**：矩形、圆角、圆、三角、菱形、五边形、六边形、七边形；
- 每个节点独立拥有：位置/尺寸、圆角、边框色、填充色、字号、透明度、旋转、
  层级、锁定、隐藏、折叠、分组、预设样式一键套用；
- 节点文本为富文本（textHtml + textPlain 双列）；
- **批量操作**：框选多选后统一改样式、对齐、编组，浮动操作条；
- 剪贴板：Ctrl+C/V/X/D 复制粘贴剪切复制节点（含样式与相对位置）。

### 13.2 连线系统

- 三种方向、三种线型、三种路径（曲线/直线/直角正交）、自定义颜色宽度标签、
  流动动画与辉光；
- 连线几何由 `geometry.ts` 统一计算：锚点永远位于节点边中点、圆形直径约束、
  正交与曲线路径生成、命中检测（均有单元测试）；
- 点击连线弹出气泡就地改样式；删除节点时连线级联清理（后端测试覆盖）。

### 13.3 画布操作

- 左键拖拽平移（工具栏可切框选）、中键平移、空格临时平移；
- 滚轮 / Ctrl+滚轮缩放（指针锚点缩放）；WASD / 方向键移动视图、Shift 加速；
- `Home` 回中、`F` 适应全部、`Shift+F` 适应所选、`G` 网格、`+/-` 缩放；
- `N` 新建文本框、双击节点编辑、三击取消编辑、`Esc` 退出；
- **Minimap 小地图**实时同步视野框；检查器就地改选中节点全部属性；
- 视口、网格、吸附设置随图保存，重开还原。

### 13.4 存储与文件

- 节点/连线采用**批量 upsert**，同一次拖动只提交一次（后端测试验证事务安全）；
- 「导出为 `.mindmap` 文件」把整图（含视口）写入工作区，双击即开；
- 导图右键**「复制为引用」**：把节点变成 `xref:` 锚点，粘贴到 Write 正文，
  双击锚点跳回本节点；源节点内容更新后，Write 侧徽标提示「已更新」。

---

## 十四、项目分析空间（PVCCE · 代码通俗化引擎）

**PVCCE（Project Visualization & Code Comprehension Engine）**：把任意本地
代码项目变成一棵「大白话知识树」。整个空间**没有任何弹窗表单**。

### 14.1 导入（四种途径）

整个项目文件夹；单个/多个源文件（增量追加）；工作区拖入；非代码文件
（README/配置/设计说明）以「说明书/清单/配置」通俗卡呈现。Rust 侧
`project_scan.rs` 自动跳过 `node_modules`、`.git`、`target` 等噪音目录，
**拒绝符号链接越界**、有界读取，`lib/pathGuard.ts` 在前端再做一层防护。

### 14.2 七级无限下钻

```
根 › 目录 › 文件 › 类/结构块 › 函数 › 逻辑块（可折叠）› 逐行
```

- L4 为**双轨制虚拟滚动**：左源码右大白话，6000 行文件也只渲染可视区约 30 行；
- 函数行可**块级折叠**只看大白话总结；`Esc` 逐级返回。

### 14.3 深度语义翻译与词典

- 五维分析（意图/手段/输入/输出/副作用）驱动文件/函数/行级叙述；
- `dedupeCheck` 品控：任意两条比喻文本词级 Jaccard > 0.9 判退化重写；
- 通俗风格三选一：**生活比喻 / 故事叙事 / 工程说明**（持久化）；
- 词典七级体系（L1 通用 / L2 语言 / L3 框架 / L4 领域 / L5 意图变换 /
  L6 惯用短语 / L7 模式识别）+ 十大类扩展包，支持 `.dict.json` 离线导入导出。

### 14.4 意图生成与写回

- 工具栏灯泡放置意图节点 → 双击内联输入大白话 → Enter 生成通俗步骤 +
  代码节点；八类意图变换（改颜色/加日志/加注释/改数字/重命名/删行/改文字/
  加异常）；
- 顶栏「**写入源文件**」二次确认 → 备份 `.backups/*.bak` → **原子写盘**；
  **Ctrl+Z 两级回滚**：先撤会话内改动，再回滚已落盘写入；
- 推理链三视角（纵向目标步骤/横向依赖/时间因果）全部带真实行号映射。

---

## 十五、命运推演空间（FTPE · 行为树命运引擎）

### 15.1 六大面板

| 面板 | 内容 |
| --- | --- |
| 人格 | 1–3 个主导人格按权重加权混合，带相容/冲突网络 |
| 性格 | 正反极滑块（每个特质标注其对立面） |
| 主义思想 | 高值互斥对触发「内心挣扎」 |
| 事件库 | 7 层分类，种子 100+ 条，可自定义扩展 |
| 随机因素 | 运气/时代/健康…每项独立权重与强度旋钮 |
| 推演参数 | 年数 / 每步分支 1–4 / 混沌强度 / 起始年龄 / **确定性种子** / 增益 |

增益（Buff）系统：对携带特定标签的事件做概率乘法，多个 buff 叠乘后
整体钳制在 0.05..4，避免一格 buff「掀桌」。

### 15.2 推演算法（确定性、可重现）

```
最终概率 = 基础概率 × 特质乘子 × 随机因素扰动 × buff 乘数 × 混沌扰动
```

- **mulberry32 PRNG**：同一种子 → 完全相同的命运树；
- 特质乘子走 **SIGMOID 映射**（k(0)=0，k(±100)≈±0.93）：中段灵敏、两端饱和；
- 每个时间步按权重采样分支；终点按路径基调分配 **10 种结局原型**；
- 命运树横向时间轴无限延伸，双击事件节点续分子树；可导出为思维导图。

---

## 十六、视觉系统与视觉语言 2.0

### 16.1 宇宙星空（WebGL + Worker）

- 完整的原生 WebGL 管线：主循环、渲染器、34KB 着色器库、数学工具（含单测）、
  **Web Worker 星场模拟**（主线程只做绘制）；
- 分层视差星野、缓慢漂移与呼吸式明暗；打字时自动降级；
- `safeMode` / `reduceMotion` 下渲染静态单帧甚至完全停用。

### 16.2 极光（WebGL2 Ray-Marching）

- 三维褶皱极光飘带：纯白蓝 → 冰蓝 → 边缘樱花冰晶粉，向下投**丁达尔光柱**；
- 泊松圆盘采样的高熵星河，最亮的 2% 恒星带四角冷蓝十字衍射星芒；
- `tools/aurora-*.html/cjs` 离线预览与半分辨率调参工具。

### 16.3 视觉语言 2.0（Design System 2.0 · AI-17 视觉语言组）

在星空与极光的「背景视觉」之上，v1.x 建立了完整的**界面视觉语言体系**：

- **tokens.css 语义令牌 2.0**：色彩/间距/圆角/层级/动效时长全部语义化，
  界面代码**禁止裸色值**（audit.cjs 视觉门禁静态强制，违者 CI 拒绝）；
- **材质引擎（Material Engine）**：玻璃/亚克力/金属/织物的统一材质配方，
  模糊、噪点、高光、阴影参数化，一处调整全局生效；
- **微交互矩阵（Micro-interaction Matrix）**：悬停/按下/聚焦/禁用/加载
  五态的统一动效配方，遵循 80ms 全局降档与 reduce-motion 令牌；
- **iconRegistry 图标语义登记**：全部功能图标（含四空间 AppGlyphs）统一
  登记、统一命名、统一尺寸档位，桌面/任务栏/开始菜单/设置四处一致；
- **响应式断点体系**：从窄窗口到超宽屏的自适应布局档位；
- **EmptyState 空态组件**：每一处「没有内容」都有插画级空态与下一步动作
  建议，不再是一篇空白；
- **SpotlightTour 新手导览**：聚光灯式分步引导，可随时从帮助中心重放；
- **错误叙事规范**：错误不是裸报错，而是「发生了什么 / 为什么 / 怎么办」
  三段式人话叙事；
- **帮助中心**：内置离线帮助与快捷键手册（与 UI-COPY 文案规范联动）；
- **视觉门禁（audit.cjs visual gate）**：令牌契约 / 裸色值=0 / iconRegistry
  完整性三项静态检查进 CI。

### 16.4 主题工坊（OKLCH + AA）

主题工坊（AI-04）支持自定义主题：强调色、背景、圆角、密度五档位（密度/
几何/字体/焦点环/图标尺寸）全部可调；颜色系统基于 **OKLCH 色彩空间**，
**强调色对比度守护**自动校验文字对比度（WCAG AA 4.5:1 正文 / 3:1 大字与
图形），不达标自动给出推荐色；系统应用深浅分离（跟随系统浅色/深色时，
第三方嵌入窗口保持原样）；主题可导出分享。

---

## 十七、国际化、搜索与设置

### 17.1 三语界面（简体 / 繁體 / English）

- `Lang = "zh" | "zh-TW" | "en"` 全链路贯通：UI 文案、日期格式
  （繁体「週五」、英文 "Friday, September 5"）、命运词典、窗口标题、
  安装器语言选择器；
- **zh-TW 繁体语言包**：`src/i18n/s2t.ts` 字符级简→繁转换覆盖 UI 词条实际
  用字，未收录字符原样保留（优雅降级）；台湾惯用词汇（軟體/網路/設定/
  檔案總管）由 `zhTwOverrides` 键级覆盖修正，语境多音字以 UI 语境高频映射为准；
- **简繁用户词表**：用户可维护最多 500 条自定义简繁映射（最长优先匹配 +
  冲突检测 + CSV 导入导出），纠正个人惯用词；
- 设置 → 通用 → 语言下拉即切，重启保持。

### 17.2 多语言中心 2.0（AI-19）

- **运行时覆盖层**：不改源码即可在运行时覆盖任意词条（个人定制文案）；
- **伪本地化（Pseudo-localization）**：开发期一键把词条变成「[伪_翻_译]」
  形态，暴露硬编码文案与截断风险；
- **RTL 试点**：界面方向翻转预览（为未来 RTL 语言铺路）；
- **词条工作台**：CSV 四列（键/zh/zh-TW/en）导入导出、占位符逐语言校验；
  `tools/i18n-crowd.cjs` 社区翻译格式工具实测 2529 键回环 0 错误；
- **区域格式跟随**：日期/时间/数字/容量格式跟随系统 locale（Intl 实现），
  容量单位支持二进制/十进制双口径（1 KB = 1000 B 或 1024 B）。

### 17.3 拼音/首字母搜索

- `src/lib/pinyin.ts`：内置常用字拼音表（覆盖 UI 词汇与常见应用名词），
  `pinyinOf`（全拼）/ `initialsOf`（首字母）/ `matchPinyin`（原文子串 +
  全拼包含 + 首字母三重匹配）/ `isAsciiQuery`；
- 生效位置：**开始菜单搜索框**（实时过滤固定网格与最近使用）、
  **全局搜索浮层**（纯字母查询时对文档/导图标题做拼音回退）、
  **桌面图标敲字定位**；
- 未收录汉字按原样保留，不猜音——如实降级为子串匹配。只读匹配，零网络。

### 17.4 设置页（Ctrl+,）

设置弹窗按域分标签页（全部设置项由 `gen-settings-doc` 自动生成
[`docs/SETTINGS.md`](docs/SETTINGS.md)，共 74 项，杜绝文档漂移）：

| 标签页 | 内容 |
| --- | --- |
| 外观 | 主题/背景/字体/任务栏位置联动/多显示器壁纸/Wallpaper Engine 导入/主题工坊 |
| 编辑器 | 写作空间编辑器行为 |
| 思维导图 | 导图画布行为与默认样式 |
| 通用 | 语言/启动动画与剧场/性能档 |
| 快捷键 | 自定义 accel + 冲突检测 + 导入导出（快捷键中心） |
| 数据 | 备份/恢复/保险箱/U 盘向导/自检 |
| 关于 | 版本号读自 `tauri.conf.json`、Tauri/React 版本、Schema 版本与便携标识、离线声明、接管边界诚实清单 |
| 兼容（CompatTab） | 应用适配等级查看/IME 兼容开关/全屏独占名单/慢速设备档位 |
| 性能（PerfTab） | 内存压力档位/帧预算/IO 治理/启动加速流水线开关/Bench 入口 |
| 开放接口（OpenHubTab） | variable:// 协议状态/IPC 网关/事件流/数据连接器 |
| 开放工具（OpenToolsTab） | winget/环境变量编辑器/计划任务工坊/服务依赖图/CLI 教程入口 |
| 无障碍（A11yTab） | 色觉模拟器/动效缩放/辅助功能探针状态/词表管理 |
| 氛围（AmbienceTab） | 音景/节律/昼夜壁纸/情绪引擎/简报卡 |
| 声音通知（SoundNotifyTab） | 声音方案/逐应用音量/勿扰日程/通知保留策略 |
| 质量（QualityTab） | 错误聚合看板/IPC 追踪/诊断包导出 |

设置弹窗/软件管理/全部对话框/欢迎向导/全局搜索/切换器均带统一 CloseLight
关闭灯。

---

## 十八、隐私、数据与便携（U 盘 / 保险箱 / 焚毁 / 数据安全中心）

### 18.1 基础隐私能力

- **U 盘便携**：设置页向导把应用 + 数据打包到 U 盘（真实文件计数进度），
  打包后自动校验完整性；便携模式拔出保护：1s 轮询数据卷 + 周期 WAL
  checkpoint，拔出前横幅提示；
- **保险箱（Vault）**：本地加密存储敏感条目，独立口令，自动锁定；
  Vault 2.0 账号身份库支持多账号切换（凭据只存加密区，`ai verify` 断言
  零落宿主）；
- **焚毁**：文件管理器右键对敏感文件安全删除；
- **数据自检**：一键检查数据目录完整性（`AuditFinding` 列表）；
- **隐私横幅**：托盘常驻时的隐私状态提示（PrivacyBanner）；
- 全部功能离线可用，无任何遥测。

### 18.2 数据安全中心（独立工具窗口 · 12 面板 · AI-10）

数据安全中心把分散的数据能力收拢为一个独立 VWM 工具窗口，包含十二个面板：

- **版本时间机**：数据目录的历史版本浏览与按时间恢复；
- **全局标签**：跨记录/导图/文件的统一标签管理；
- **回收站策略**：按容量/按天数自动清理规则；
- **传输指挥台**：大批量导入导出的任务队列（进度/暂停/取消/校验）；
- **存档柜（.vxa）**：把任意目录打包为 `.vxa` 加密存档（含清单与校验和）；
- **隐私仪表盘 + 金丝雀**：哪些数据被哪个功能读过的一览；**金丝雀文件**
  （预埋的敏感命名文件）监测任何越权访问尝试；
- **信任链**：数据目录内文件的哈希信任链，篡改可见；
- **血缘**：记录/导图/媒体之间的引用血缘图（xref 全景）；
- **防火墙 2.0**：出站动作白名单管理（与 netconsent 联动）；
- **隐身会话**：临时关闭历史/统计/缓存的一次性会话模式；
- **紧急擦拭**：一键安全抹除指定类别数据（多级确认）；
- **使用洞察**：本地使用统计可视化（14 天窗口，纯本地）。

---

## 十九、效率与工具中枢（AI-07 效率中枢组 + AI-08 基础工具组）

### 19.1 命令面板与快捷键中心

- **命令面板**（Ctrl+Shift+P 风格）：模糊搜索全部环境命令（打开应用/切壁纸/
  切勿扰/进设置……），键盘党的一站式入口；命令**注册表**统一登记，路由
  公开表自动生成 [`docs/ROUTES.md`](docs/ROUTES.md)（gen-routes）；
- **快捷键中心**：全部快捷键的集中浏览/自定义（accel）/冲突检测（同表
  互斥校验）/导入导出（V-50），与 `shortcuts.ts` 单一事实源同步。

### 19.2 剪贴板历史（N-15）

- 系统级剪贴板监听与历史回放（文本/图片/文件列表）；
- **DPAPI 加密**落盘（Windows 用户级加密，换机不可读）；
- **敏感名单**：密码管理器类窗口（标题匹配）复制的内容不记录；
- **关闭即焚**：可配置退出时清空剪贴板历史。

### 19.3 截图标注（V-44）

- 全屏/区域截图（环境内与系统桌面均可）；
- 内置标注工具：矩形/箭头/画笔/文字/马赛克/序号；
- 直接入写作库（作为记录附件）或存文件/复制到剪贴板。

### 19.4 宏引擎（V-45）

- 键鼠动作序列的录制与回放（本地脚本，可编辑）；
- **急停**：任何时刻一键中止宏回放；
- **UAC 判停**：检测到提权弹窗自动暂停，绝不代用户点击安全桌面；
- **cron 调度**：定时执行宏（如「每天 18:00 打开日报记录」）；
- 与计划任务工坊（第二十二章）互补：宏面向键鼠自动化，任务面向进程/动作。

### 19.5 基础工具箱（AI-08 · 迷你应用框架五件首发 + 全局拖放总线）

| 工具 | 说明 |
| --- | --- |
| 时钟中心 | 世界时钟/秒表/计时器/闹钟（与提醒中心联动） |
| 天气卡 | 本地解析的城市天气卡片（数据源可配置，默认离线占位如实降级） |
| Emoji 面板 | 分类搜索的 emoji 选择器，可直接插入写作正文 |
| 放大镜取色 | 屏幕放大镜 + 像素取色（HEX/RGB/HSL，复制即用） |
| 换算中心 | 长度/重量/温度/面积/速度/数据量单位换算 |
| 系统信息 | 本机硬件与系统概览（与系统中枢概览页同源） |
| 运行对话框 | Ctrl+Alt+R 唤起的「Win+R」等价物：命令/路径/URI 直达 |

全部迷你应用运行在**全局拖放总线**之上——任意工具产物（颜色/文本/文件）
都可以拖进其他窗口使用；右键打印 + 打印队列查看器补充系统级纸面输出能力。

---

## 二十、系统集成与硬件中枢（AI-11 · 系统中枢七页）

系统中枢是一个独立 VWM 工具窗口，按七页组织全部系统与硬件能力：

| 页 | 内容 |
| --- | --- |
| 概览 | 系统摘要：CPU/内存/磁盘/网络/运行时长一屏读完 |
| 显示器 | 多显示器档案（分辨率/刷新率/缩放/DPI 感知）、色温伽马调节（退出时字节级还原出厂） |
| 电源 | 电源模式三档（省电/平衡/性能）+ Windows 电源计划切换 + 电池健康（设计容量 vs 满充容量）、保持唤醒（临时禁息屏） |
| 网络 | 代理状态与设置、内网测速（本地回环/网关延迟，不出网）、Wi-Fi 无线电开关 |
| 存储 | 磁盘分区容量、**大文件雷达**（Top-N 大文件扫描与清理建议）、TRIM/健康状态只读 |
| 系统 | 性能 HUD（可悬浮的实时指标）、可靠性时间线（系统崩溃/挂起事件流）、重启建议、断电自检干净关机标记 |
| 外设 | 蓝牙/USB 设备只读探针（枚举与状态）、优先级预设（前台进程优先级建议） |

**性能治理四件套（AI-13 性能与长跑组）**与硬件中枢联动：

- **内存守护**：5 秒采样的进程内存监控 + 泄漏看门狗（同进程持续增长告警）；
- **帧预算治理**：渲染帧预算超限时自动降档（动画/背景逐级关闭）；
- **IO 治理**：大文件操作可暂停/恢复/取消，不阻塞界面；
- **Bench 2.0**：六指标基准（冷启动/热启动/内存/帧率/IO/搜索延迟）+ 性能门
  （回归即报警）+ 8 小时 soak 档（长跑稳定性）。

**启动加速流水线（P0/P1/P2）**：P0（首屏必需）同步加载 → P1（常用面板）
空闲加载 → P2（低频功能）按需加载；配合冷启动 A/B 对照测量，启动时间
数据化管理。**安全模式启动**（--safe）以最小功能集启动用于排障。

---

## 二十一、兼容性防线（AI-12 兼容纵深组）

### 21.1 应用适配等级库

- 每个登记应用带 **A/B/C 适配等级**（matrix.json / matrix.ts 双源）：
  A = 完整支持（嵌入/贴靠/多开全部正常）、B = 部分支持（如实标注短板）、
  C = 建议独立窗口运行；
- 等级库内置常见应用预置数据，用户实测可覆盖并回写。

### 21.2 深度兼容能力

- **IME 深度兼容**：中文/日文输入法在虚拟窗口内的组合窗口定位与焦点
  修正（候选框跟随输入位置）；
- **全屏独占协议**：全屏游戏/视频的独占检测 → 任务栏避让 + 嵌入让位 +
  快捷键让渡（F-1 反作弊看门：检测反作弊进程主动停用一切监听）；
- **UWP 识别去重**：开始菜单扫描时 UWP 应用与桌面快捷方式的重复项归并；
- **便携路径漂移自愈**：U 盘换盘符后，portable 登记按**卷 GUID** 重新绑定
  路径（而非死盘符）；
- **慢速设备档位**：检测数据目录所在设备速度（HDD/U 盘/SSD），自动切换
  缩略图/索引/搜索的激进程度；
- **高刷折算**：高刷新率显示器上动效时长按 0.85× 折算，保持观感一致；
- **多实例徽标 / 崩溃善后 / 热插拔退避派发**：同应用多开角标；应用崩溃后
  的重启建议与登记状态修正；显示器热插拔时的事件退避重放（不雪崩）；
- **提权提示 / 驱动共存扫描**：以管理员运行时的可见提示；与常见安全软件
  驱动共存性的只读扫描报告；
- **遗留协议 Shim**：旧版登记文件/旧配置的迁移适配（纯记录，不改语义）。

### 21.3 接管边界（与 29.5 诚实清单联动）

兼容性防线的原则是**让位优于强占**：全屏独占、反作弊、UAC 安全桌面、
系统维护界面四类场景，Variable 主动退让并如实记录，绝不硬接管。

---

## 二十二、开放生态与开放工具（AI-14 开放接口组 + AI-15 开放工具组）

### 22.1 开放能力（AI-14）

- **variable:// 协议中枢**：系统级自定义协议（词表路由：打开记录/导图/
  设置页/搜索……），便携态自动注册、退出时退订，不留系统残留；
- **数据开放导出**：全库数据的只读开放导出（自动跳过密钥类字段）；
- **.vxs 四合一资源包**：主题/词典/图标包/声音方案的统一分发格式（清单 +
  校验 + 抽取安装），配套**安全扫描**（zip-slip 路径穿越/可执行文件/
  体积上限三项拦截）；
- **本地事件流**：环境事件（窗口开合/通知/设置变更）的 JSONL 流式导出
  （默认关闭）；
- **插件清单校验**：第三方插件的权限白名单与 entry 防逃逸校验；
  **插件热重载**（--plugin-dev 开发模式）；
- **开放 IPC 网关**：127.0.0.1 环回 REST + WebSocket 接口（token + scope
  授权，**默认关闭、零监听**），供本地自动化工具集成；
- **分享信封**：把单条记录/导图打包为带 FNV-1a 校验的分享文件；
- **数据连接器**：SQLite/JSON/CSV 三源只读连接（SELECT 白名单），让外部
  数据在文件管理器/搜索中可查；
- **variable-cli**：`--doctor`（自诊）/ `--api-token-gen`（网关令牌生成）/
  `--tour`（五课交互教程）等命令行入口；
- **浏览器伴侣收件箱**：配套浏览器扩展的本地收件箱（环回，零云端）；
- **扩展兼容承诺与版本矩阵**：对外接口的语义化版本承诺（Z-56）。

### 22.2 开发者工具（AI-15）

- **路由公开表**：gen-routes 自动生成 [`docs/ROUTES.md`](docs/ROUTES.md)；
- **设置自动文档**：gen-settings-doc 自动生成 [`docs/SETTINGS.md`](docs/SETTINGS.md)
  （74 项），设置漂移有前后端测试兜底；
- **本地出站桥**：默认关闭的环回/内网白名单出站代理（供需要联网的第三方
  插件使用，宿主出站仍走 netconsent）；
- **嵌入声明协议**：`variable-embed.json`（见 10.3）；
- **测试钩子规范**：[`docs/TEST-HOOKS.md`](docs/TEST-HOOKS.md)——15 条主
  路径 testid 约定、boot-ready 哨兵事件、`--test-ready` 启动旗标，配合
  audit 门禁防漂移；
- **changelog 自动化**：gen-changelog 按 conventional commits 分节生成草稿；
- **社区翻译格式**：i18n-crowd.cjs 四列 CSV 往返（2529 键回环实测）；
- **winget 开放安装器**：逐次确认的 winget 软件安装通道；
- **环境变量编辑器 / 计划任务工坊（动作白名单 + 30s 运行时上限）/
  关联快照还原（HKCU 文件关联的快照与回滚）/ 卸载善后（残留报告 + 勾选入
  回收站）/ 启动延迟编排（安全类软件避嫌错峰）/ 服务依赖图（只读四查询）/
  配置语义 diff（三色高亮）/ 沙盒试用面板**。

---

## 二十三、无障碍 2.0（AI-19 无障碍与本地化组）

### 23.1 界面无障碍

- **aria-live 通告**：重要状态变化（保存完成/复制成功/勿扰切换）以屏幕
  阅读器可读的方式播报；
- **焦点跟随提示**：焦点所在控件的语义提示（tooltip 与 aria-label 双通道）；
- **四类色觉模拟器**：红盲/绿盲/蓝盲/全色盲的实时滤镜，设计者与用户都能
  验证配色可达性；
- **粘滞键序列累积器**：系统粘滞键开启时，组合键（如 Ctrl+S）可分键依次
  按下，环境内快捷键全部兼容；
- **动效时长全局缩放**：`--dur-scale` 0.5x/1x 两档（为前庭障碍用户准备的
  减动效入口，与 reduce-motion 独立）；
- **aria:audit 静态标注审计**：861 个交互元素 aria 标注 100% 覆盖
  （`npm run aria:audit`，CI 门禁）。

### 23.2 系统辅助功能桥（a11y.rs）

- **只读探针**：粘滞键/筛选键/高对比度/讲述人的系统开关状态实时读取；
- **高对比度跟随**：系统高对比度主题开启时，环境自动切换高对比配色；
- **讲述人联动**：检测到讲述人运行时自动叠加 reduce-motion（动效会干扰
  屏幕阅读器的焦点追踪）。

---

## 二十四、氛围与个性化（AI-18 氛围与个性化组）

### 24.1 氛围引擎

- **氛围音景引擎**：Web Audio 程序化合成的五种场景音景（雨夜/林间/海浪/
  壁炉/白噪音），可叠加、带睡眠定时，**零音频资产**（全部实时合成，
  不增加安装体积）；声景三主题 + 深夜自动减半；
- **环境辉光**：从壁纸主色采样（OKLCH 空间 k-means 三步分离）驱动界面
  辉光与任务栏染色，壁纸换色环境跟着换 mood；降级矩阵保证极端壁纸下
  回退中性色；
- **节律助手**：四节律（晨/昼/暮/夜）建议（壁纸组/音景/勿扰/亮度联动），
  深夜降级计数（连续深夜使用给出休息提示，不打扰）；
- **情绪引擎**：用户自选情绪向量 → 环境偏移包络（壁纸滤镜/音景/辉光的
  定量偏移），「今天有点丧」就按下「丧」；
- **昼夜壁纸组**：见 9.1。

### 24.2 个性化细节

- **桌面纯净模式（Ctrl+Alt+P）**、**屏保时钟**（闲置时的极简时钟屏保）、
  **今日简报卡**（任务栏小组件：日程/待办/节律一句话）；
- **壁纸快捷操作**（右键壁纸直达常用壁纸动作）、**悬停延迟档位**（提示
  出现速度三档）、**本地精选轮换**（壁纸池加权随机，不重复直到池尽）；
- **农历节气节日**：本地万年历 2020–2040（含置闰），任务栏时钟悬停卡与
  日历联动，节气/传统节日全量覆盖；
- **系统应用深浅分离 / 强调色对比度守护**：见 16.4；
- **氛围会话恢复**：崩溃/重启后提示（而非自动）恢复上次的音景与滤镜组合，
  超龄配置自动清理。

---

## 二十五、声音与通知运行时（AI-16 启动与声音通知组）

NotifyRuntime 是环境的声音与通知中枢运行时：

- **声音方案包**：事件（启动/保存/通知/错误/最小化…）→ 音效的方案化配置，
  包格式带事件白名单与数值范围校验，损坏的包如实拒绝；
- **逐应用音量记忆**：每个第三方应用的音量独立记忆（volmem.json，多实例
  同步），游戏和音乐软件互不抢音量；
- **通信设备快切**：播放设备/通话设备一键切换（eCommunications COM）；
- **音量淡变防爆音**：切设备/切方案时音量先淡出再淡入；
- **勿扰日程**：见 7.1（跨午夜 + 手动优先 + 排队回放）；
- **通知存档 / 智能三堆 / 交互进化**：见 7.1；
- **麦克风使用指示**：见 7.4；
- **提醒中心**：时钟锚定的提醒（运行时计算，不受系统睡眠影响）+ 重启补发
  （错过的一次性提醒开机补送）+ 重复推进（周期提醒按错过次数推进而非补发
  一串）。

---

## 二十六、工程质量、性能与收官（AI-20 质量门禁与收官组）

### 26.1 门禁体系（全部进 CI / pre-commit）

| 门禁 | 内容 |
| --- | --- |
| tsc | TypeScript strict 全仓 0 错误 |
| vitest | 前端单测 1088 项通过（4 跳过为环境相关诚实跳过） |
| cargo test / check | 后端单测 + e2e 命令面测试全绿 |
| audit.cjs | IPC 命令面注册校验 + i18n 键完整 + TESTID 完整 + 视觉门禁（令牌契约/裸色值=0/iconRegistry） |
| keymap-audit | 键位注册基线（53 处，含 5 处有据裸监听：alt+space / ctrl+alt+o / ctrl+alt+[ ] / Esc×3，全部登记理由） |
| aria-audit | 861/861 交互元素 aria 标注 100% |
| 链接检查 | docs 内全部相对链接断链清零（P0） |
| release.ps1 | 发版门禁聚合（版本一致性/测试/构建/产物校验/changelog/文档再生成） |

### 26.2 质量工具

- **错误聚合看板（QualityTab）**：全仓错误日志的聚合视图（频次/首现/分布）；
- **IPC 追踪**：开发模式下 的 IPC 调用瀑布图（release dist 实测零命中，
  不进产物）；
- **设置漂移测试**：前后端设置项定义一致性的自动测试（docs/SETTINGS.md
  漂移 13 项已根治并防复发）；
- **视觉回归矩阵**：135 个基线的截图比对基建（像素级回归可发现）；
- **性能影响声明**：PR 模板必填性能影响栏 + `bench --snippet` 轻量基准；
- **依赖审计自动化**：周任务扫描依赖漏洞（**只审计不升级**红线，升级必须
  显式评审）；
- **三规范**：Tooltip 规范 / 单位数字规范 / 跨午夜时间规范（统一口径）。

### 26.3 收官体验

- **演示模式**：一键隐藏个人数据（演示/录屏用假数据壳）；
- **关机倒计时取消**：关机确认后 10 秒内可反悔；
- **偏好搬家向导**：换机/重装时把全部偏好（设置/主题/键位/词表）导出迁移；
- **卸载器数据抉择**：卸载时询问用户数据去向——**回收站式可反悔删除**
  （不是直接抹掉）；卸载器钩子经编译级证据链验证（Tauri 模板
  `Section Uninstall` 宏调用 + installerHooks 注册 + makensis 零错编译）；
- **依赖诚实声明 v2**：关于页如实列出全部依赖及其用途与许可；
- **收官毕业页**：首次全绿后的一次性庆祝页（也承担版本说明职责）。

---

## 二十七、环境要求与开发运行

| 工具 | 版本 |
| --- | --- |
| Node.js | ≥ 20 |
| npm | ≥ 10 |
| Rust (rustup) | stable-msvc（需 MSVC Build Tools / Windows 10 SDK） |
| WebView2 | Win11 自带；Win10 由安装器自动部署 |
| 操作系统 | Windows 10/11 x64（桌面环境功能依赖 Win32 API） |

### 怎么打开（三种方式）

**方式一：开发模式（改代码/调试）**

```bat
cd -Un-Real-0d23d9ux-Engine-main
npm install
npm run tauri dev      :: 首次编译 Rust 较慢，之后增量
```

**方式二：安装版（推荐日常使用）**

1. 双击 `src-tauri/target/release/bundle/nsis/Variable_1.0.0_x64-setup.exe`；
2. 按向导安装（支持中英文安装界面）；
3. 从桌面 / 开始菜单启动 Variable → 启动剧场 → 进入私人桌面环境。

**方式三：免安装直接运行**

- 编译产物：`src-tauri/target/release/Variable.exe` 双击即可；
- 便携目录：`build-windows.bat portable` 产出 `dist-portable\`，连同 `data\`
  一起拷到任意位置（含 U 盘）运行。

### 常用命令

```bat
npm run typecheck        :: 仅类型检查
npm test                 :: 前端单测（Vitest，1088 项）
npm run keymap:audit     :: 键位注册基线审计
npm run aria:audit       :: aria 标注审计（861/861）
cd src-tauri && cargo test  :: 后端测试（含 e2e 命令面）
node tools\audit.cjs     :: IPC 命令面与 i18n/TESTID/视觉门禁审计
```

---

## 二十八、Windows 打包与验收

```bat
build-windows.bat            :: 默认 NSIS 安装包
build-windows.bat msi        :: 追加 MSI（需要 WiX，Tauri CLI 自动下载）
build-windows.bat portable   :: 额外产出便携版目录
```

脚本会依次校验 node/npm/rustc/cargo/tauri-cli，再执行类型检查、前端构建、
`tauri build`；任一步失败都会以非零码退出并打印原因（**不静默成功**）。
打包前自动调用 `stop-variable.ps1` 终止仓库目录内正在运行的 Variable 实例
（target\release 与 dist-portable），避免可执行文件被占用导致的
`os error 5`；随后做独占模式文件锁复检（最多 5 秒），锁仍存在则中止并
以退出码 1 报错。

### 已验收产物（v1.0.0）

| 类型 | 路径 | 大小 |
| --- | --- | --- |
| NSIS 安装包 | `src-tauri/target/release/bundle/nsis/Variable_1.0.0_x64-setup.exe` | 6.5 MB（NSIS-3 Unicode） |
| MSI 安装包 | `src-tauri/target/release/bundle/msi/Variable_1.0.0_x64_en-US.msi` | 5.1 MB |
| 绿色 exe | `src-tauri/target/release/Variable.exe` | — |

**M-87 发版演练（全流程模式）14/14 全 PASS**：版本一致性 / tsc / vitest /
audit / 键位门禁 / 链接检查 / 构建 / M-80 dist 零命中（dev-only 代码不进
产物）/ cargo check / NSIS 构建 / 产物校验 / changelog / ROUTES + SETTINGS
再生成。清单归档
[`docs/acceptance/release-rehearsal-2026-09-09.md`](docs/acceptance/release-rehearsal-2026-09-09.md)。

安装器支持卸载；卸载**不会删除用户数据目录**（V-95 数据抉择见 26.3）。

### 全流程手测清单

见 [`docs/ACCEPTANCE_CHECKLIST.md`](docs/ACCEPTANCE_CHECKLIST.md)：
安装/卸载、多语言、拼音搜索、IM 提醒、红绿灯、全局快捷键、桌面图标与
文件架、回收站、U 盘便携、全屏避让、勿扰等 7 大项验收点。

---

## 二十九、数据目录与隐私边界

```
<数据根>/db/variable.db          SQLite（WAL 模式）
        /media/                 复制进来的图片/视频/附件
        /backups/               手动/自动备份（时间戳命名，恢复前 quick_check 校验）
        /recovery/              异常关闭的恢复文件（JSON）
        /apps.json              第三方软件登记表（含便携性分级/图标/解析目标）
        /volmem.json            逐应用音量记忆
        /we-open.cmd            Wallpaper Engine 打开包装脚本（自动生成）
        /logs/variable.log      前后端错误日志（含窗口关闭取证，轮转配额管理）
```

- 默认数据根：`%APPDATA%\com.variable.app`；便携模式下为 `exe目录\data`；
- 应用在启动与每次窗口关闭事件上写日志：区分「用户请求关闭」与
  「窗口自行销毁」（webview 崩溃/系统），为异常排查留证据；
- **隐私边界**：除用户显式确认的联网动作外无任何出站请求；所有解析、词典、
  推演、IM 未读检测均在本地完成；卸载或删目录即彻底销毁全部数据。

### 29.5 能力诚实声明（D-5 接管边界诚实清单 · 做不到的事）

与蓝图「与宿主的边界总表」合并；以下为全域软件接管场景下 Variable **做不到、
也不应做到**的事——每条对应实现批次与回归/文档锚点：

| # | 边界 | 为什么做不到 | 锚点 |
| --- | --- | --- | --- |
| 1 | **UAC 安全桌面（Secure Desktop）提权弹窗**永远不属于 Variable | Windows 安全机制：安全桌面独立于用户桌面，用户态程序无法绘制/接管；不应绕过 | 文档批次（本章）；[desktop shell](src/system/desktop/DesktopShell.tsx) 不注册任何 Secure Desktop 钩子 |
| 2 | **直跑档（未替换 Shell）时，用户从 Windows 开始菜单手动启动的程序**只能事后回收 | 启动发生在 Explorer 上下文，Variable 无法事前拦截；D-3 看门狗仅做事后窗口回收/迁移 | 蓝图 22.3 D-3；[watchdog 链接](docs/终极形态施工总计划.md) |
| 3 | **内核级全屏独占 + 反作弊游戏**：只让位不收编 | 反作弊在内核态检测注入/挂钩；Variable 主动停用键盘监听与嵌入并完整让位 | F-1 反作弊看门（ac* 通知）；E-1 嵌入白名单测试 |
| 4 | **VM 档内 Windows Update / 系统对话框**白名单放行 | 系统维护界面属于 VM 宿主职责，接管会破坏更新与修复链路 | 蓝图 22.1 D-1 白名单；diagnostic.rs 诊断包如实上报 |
| 5 | **直跑档锁屏/登录画面（LogonUI）是 Windows 的**——可换壁纸但结构不可换 | LogonUI 属系统安全边界（凭据隔离）；仅 VM 档可整层规避 | 蓝图 22.1/22.2；settings壁纸层仅作用于用户会话 |

同一清单已内置于 **设置 → 关于 → 接管边界诚实清单**（运行时可见）。

---

## 三十、备份、恢复、导入与导出

- **备份**（`backup.rs`）：一键创建时间戳备份并记录大小/校验和/状态；恢复前
  先做 SQLite `quick_check`，损坏即拒绝；可删除、可导出到任意位置；
- **恢复文件**（`settings_cmd`）：异常关闭场景下定期写入 `recovery/`，
  重启后弹窗预览并一键「恢复为记录」；
- **导出**（`export.rs`）：记录批量导出、导图 JSON、整工作区打包
  （文件夹 + 记录 + 导图 + 媒体）与整包导入（返回 `ImportSummary`）；
- **数据开放导出**（22.1）：全库只读开放导出（跳过密钥字段）；
- 导入/导出全部走系统对话框选择目标，不隐式写文件系统；
- **偏好搬家向导**（26.3）：设置/主题/键位/词表的整体迁移。

---

## 三十一、快捷键手册

### 全局（系统级注册，占用时如实降级）

| 快捷键 | 功能 |
| --- | --- |
| 双击 Esc | 环境 ↔ Windows 桌面切换（kbdhook 全局轮询，任何焦点下生效） |
| Del + Backspace 同按 | 真正退出 Variable（先保存冲刷再关闭） |
| Ctrl+E / Win+E 变体 | 打开文件管理器（环境内 VWM） |
| Ctrl+Alt+D 变体 | 显示桌面 / 恢复 |
| Ctrl+Alt+N 变体 | 打开通知中心 |
| Win+Tab | Variable 窗口切换器（可在设置关闭） |
| Ctrl+Alt+B | 打开回收站 |
| Ctrl+Alt+K | 打开快捷面板 |
| Ctrl+Shift+M | 全局搜索 |
| Ctrl+Shift+D | 勿扰模式开关 |
| Ctrl+Alt+1..9 | 快速启动固定应用 |
| Ctrl+Alt+O | VWM 编排中心（WindowOrchestrator） |
| Ctrl+Alt+[ / Ctrl+Alt+] | 焦点历史回溯（前/后） |
| Ctrl+Alt+P | 桌面纯净模式 |
| Ctrl+Alt+R | 运行对话框 |

> 默认表刻意避开 Win11 系统保留的 Win+E/D/M/N/数字键，统一改用
> ctrl+alt+\* 变体；如个别键仍被占用，逐项诚实降级为任务栏/开始菜单入口。

### 桌面环境与 VWM

| 快捷键 | 功能 |
| --- | --- |
| Ctrl+, | 设置 |
| Ctrl+A / Shift+点选 / 框选 | 桌面图标多选 |
| F2 / Delete | 重命名 / 删除（需确认） |
| Ctrl+C/X/V/Z/Y | 复制 / 剪切 / 粘贴 / 撤销 / 重做 |
| Alt+Enter | 图标属性 |
| Win+方向键 / Win+M | VWM 贴靠 / 全部最小化 |
| Alt+Tab | VWM 窗口轮转 |
| Alt+Space | 虚拟窗口经典系统菜单（V-21） |
| Esc（拖拽中） | 拖拽回弹（V-30）/ 退出编组 / 关闭浮层（按上下文仲裁） |
| 任务栏图标中键 | 同应用多开 |

### 写作空间

| 快捷键 | 功能 |
| --- | --- |
| Ctrl+N / Ctrl+S | 新建记录 / 立即保存 |
| Ctrl+F / Ctrl+Shift+F | 文内查找 / 全局搜索 |
| Ctrl+B / I | 粗体 / 斜体 |
| Ctrl+Z / Shift+Z | 撤销 / 重做 |
| F11 | 专注全屏 |

### 导图 / 项目分析画布

左键拖拽平移、中键平移、空格临时平移、滚轮/Ctrl+滚轮缩放、WASD/方向键移动、
Shift 加速、Home 回中、F 适应全部、Shift+F 适应所选、G 网格、+/- 缩放、
N 新建文本框、双击节点编辑、三击取消编辑、Esc 逐级返回；
框选多选、Ctrl+C/V/X/D 复制粘贴剪切复制节点。

---

## 三十二、测试覆盖说明

### Vitest（`npm test`，1088 项 / 全部通过，4 项环境相关诚实跳过）

- 字数统计、HTML 净化（script 剥离/事件属性/危险 URL）、路径遍历防护；
- 保存队列：同键串行 + latest-wins + 错误传播；
- 思维导图几何：锚点/圆径约束/正交曲线路径/命中检测、引导线；
- FTPE：引擎概率（sigmoid 乘子/混沌/随机因素）、增益叠乘、行为树；
- 项目分析：解析器、逐行解剖、生成管线；
- 桌面环境：快捷键表冲突检测（`shortcuts.ts`）、拼音匹配（`pinyin.ts`）、
  简繁转换（`s2t.ts`）、跨空间引用协议（`xref.ts`）；
- v1.x 新域：命令面板注册表、剪贴板敏感名单、宏引擎急停、文件管理器
  视图记忆/特殊名防呆/模糊跳转、系统中枢探针、兼容等级库、variable://
  路由、.vxs 校验、a11y 累积器、多语言占位符校验、氛围引擎、通知跨午夜、
  设置漂移（前后端）、编排中心、vwm 多选/焦点历史等。

### Rust（`cargo test`，含 e2e 命令面测试）

- 迁移幂等性、文件夹增删改/重名拦截、文档 CRUD 与 LIKE 转义搜索、
  标签事务写入；
- 节点批量 upsert（重复执行两次，验证事务安全）；
- 连接线随节点删除级联、设置版本化 upsert；
- `commands_e2e.rs`：直接驱动 Tauri command 层的端到端测试
  （含 `set_settings_inner` 纯 DB 部分直测）；
- v1.x 新域：通知存档、提醒中心补发/推进、声音方案校验、逐应用音量、
  适配等级、卷 GUID 重绑定、协议退订、网关 token/scope、计划任务白名单、
  环境变量编辑器、卸载善后、键位注入验收等。

### 静态审计与门禁

`node tools\audit.cjs`：前端 `ipc.*` 调用与 Rust `generate_handler!` 注册面
一一对应校验 + i18n 键完整性 + TESTID 完整性 + 视觉门禁（令牌契约/
裸色值=0/iconRegistry）；`npm run keymap:audit` 键位基线（53 处）；
`npm run aria:audit`（861/861）；文档链接检查断链清零。全部进 CI 与
pre-commit（见 26.1）。

---

## 三十三、AI 协作开发模式（20 组 × 355 项）

Variable 的 v1.x 工作线由 **20 个 AI 协作组**并行完成，是「多智能体工程」的
一次完整实践：五份递进规划（NEXT-35 → ASCENT-60 → APEX-70 → SUMMIT-90 →
化境-100，合计 355 项功能，后一计划对前一计划严格去重）由
《ENGINE-Version-01XHI9DN.1.5xw-AI分工图》切成 20 个互不越界的责任域，
每组独立交付、独立自检、独立提交，最终全仓收口。

### 33.1 分工总览（代码级全部交付）

| 组 | 域 | 代表性交付 |
| --- | --- | --- |
| AI-1 容器 | 存储 · 子环境 · 零残留 | library/export/db/backup 存储核、residue-check 零残留链 |
| AI-2 窗口 | VWM · 四层兼容 · 接管 | W-1…5 / C-1…8 / B-27 / D-3、嵌入验证工具、compat.hint 全链路 |
| AI-3 引导 | 引导器 · Shell 接管 · 便携 | boot.rs 引导/降级链、vm_agent.rs（VM 档）、exec.rs env 注入 |
| AI-4 智能 | 云AI · 扩展 · 安全 | shell/ai.rs、extensions/ext_plugin 扩展生态 |
| AI-5 体验 | 基础功能 · 精修 · 收口 | F/A/D/B 系列、骨架屏、NSIS 发版物 |
| AI-04 桌面 | 桌面与开始菜单 | 桌面图标 V-01…V-10/V-96、开始菜单 V-11…V-14、壁纸工坊 2.0、锁屏、主题工坊、小组件、图标包 |
| AI-05 键位 | 键位纪律 | 全仓键位注册收编与门禁基线 |
| AI-07 效率 | 效率中枢 | 命令面板/注册表、统一搜索 2.0、剪贴板历史、截图标注、快捷键中心、宏引擎 |
| AI-08 工具 | 基础工具组 | 时钟/天气/Emoji/取色/换算/系统信息/运行框、全局拖放总线、打印队列 |
| AI-10 数据 | 文件管理与数据安全 | 文件管理器 2.0（V-31…V-40）、数据安全中心 12 面板 |
| AI-11 系统 | 系统集成与硬件 | 系统中枢七页、色温伽马、性能 HUD、大文件雷达、可靠性时间线 |
| AI-12 兼容 | 兼容纵深 | 适配等级库、IME 深度兼容、全屏独占协议、卷 GUID 自愈、CompatTab |
| AI-13 性能 | 性能与长跑 | 安全模式、内存守护、帧预算、IO 治理、Bench 2.0、事件风暴削峰、PerfTab |
| AI-14 开放 | 开放接口 | variable:// 协议、.vxs 资源包、开放 IPC 网关、数据连接器、variable-cli、OpenHubTab |
| AI-15 工具 | 开放工具组 | ROUTES/SETTINGS 自动文档、插件热重载、嵌入声明协议、TEST-HOOKS、i18n-crowd、winget、OpenToolsTab |
| AI-16 启动 | 启动与声音通知 | 启动剧场六件套、逐应用音量、勿扰日程、通知存档、提醒中心、NotifyRuntime |
| AI-17 视觉 | 视觉语言 | tokens 2.0、材质引擎、微交互矩阵、iconRegistry、EmptyState、SpotlightTour、视觉门禁 |
| AI-18 氛围 | 氛围与个性化 | 音景引擎、焦点舱 2.0、环境辉光、节律助手、情绪引擎、农历、五档位、AmbienceTab |
| AI-19 无障碍 | 无障碍与本地化 | 无障碍 2.0、多语言中心 2.0、系统辅助功能桥、aria 审计、简繁词表、A11yTab |
| AI-20 收官 | 质量门禁与收官 | 错误聚合看板、IPC 追踪、视觉回归、键位 CI、链接检查、发版演练 14/14、卸载器抉择 |

### 33.2 协作纪律（多智能体工程的经验结晶）

- **单一事实源**：功能全景（355 项原文合并）、AI 分工图、五路进度总览三份
  文档互为锚点，任何组不得私改他人条目；
- **不越界**：每组只改自己的域；发现他域缺陷只「通报」不越权代改
  （例外：阻塞全仓编译的回滚遗留，修复后在报告中显式声明）；
- **并发回滚恢复**：多会话并发曾出现覆盖/回滚，各组用幂等重放脚本恢复并
  即时暂存锁定——这是多写者仓库的生存技能；
- **自检三件套**：交付前 tsc 0 错误 + 本域 vitest 全绿 + audit 门禁通过，
  报告里附证据（测试数/提交号）；
- **无未经实测的 ✅**：全局 DoD——文档里的勾只代表代码级完成，真机验收
  统一留待 B-30 三宿主阶段；
- **终检会话**：全部交付完成后由收口会话做全 20 组交叉复核
  （AI-02 UI 接线补齐、orchestrate.ts 定时器泄漏修复、非功能内容清理、
  键位门禁基线更新 48→53），最终 tsc / vitest 1088 / cargo check /
  audit / 键位 / 链接六项全绿。

---

## 三十四、下一阶段：便携虚拟系统与 6 大件

### 34.1 便携虚拟系统（v1 计划，40000 字施工图）

**目标**：在 1TB 固态 U 盘中装入完整的、真 Windows 11 内核的便携系统，
实现「任何软件都能跑、任何崩溃都不传染、任何电脑随插随用、加载 10GB
大软件不卡死、可无限拓展」。一盘双用：

- **A 模式（免重启）**：宿主 Windows 中双击 `PortableVM/启动.exe`，弹出一个
  窗口即是你的系统——无需管理员、无需改 BIOS，网吧/公司电脑可用；
- **B 模式（重启原生）**：重启按 F12 从 U 盘启动，整机直接进入你的系统，
  性能 100% 直通显卡/硬盘；此时 Variable Engine 是真 Windows 的 Shell
  替换，负责桌面/任务栏/开始菜单。

技术骨架（详见 [`docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md`](docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md)）：

| 模块 | 核心方案 | 状态 |
| --- | --- | --- |
| 总体架构 | 三层洋葱（宿主 → Hypervisor → VHDX 系统）+ 微内核（Core 守护/Shell/Worker 池） | ✅ |
| 存储架构 | VHDX 差分链（Base 20GB 只读 → Apps 50GB → User 动态）+ mklink 读写分离 | ⬜ |
| 极致隔离 | 7 层隔离：硬盘（COW 子盘）/内存CPU（JobObject 限额）/进程（Low IL）/文件（受控摆渡）/网络（NAT+防火墙）/注册表（RegLoadKey）/痕迹 | ✅ |
| 永不卡死 | 6 件套：假启动壳/按需分页/限额/读写分离/预热/熔断（800ms） | ✅ |
| 完全兼容 | 任何软件 5 原则 + 像素/行为/系统三还原 | ⬜ |
| 真Windows 体验 | 透明 tile / IExplorerBrowser / IContextMenu | ✅ |
| 无限拓展 | 层式镜像 + MSIX App Attach + 插件化 | ⬜ |
| 性能与寿命 | NVMe 选型、64KB 簇、TRIM、CompactOS、RamCache | ⬜ |
| 安全与合规 | BitLocker To Go / Defender / 授权 | ⬜ |
| 测试与验收 | Compat-Matrix / Chaos-Inject / Bench-Perf / Accept-Gate 四脚本 | ✅ |
| 交付与运维 | Deploy-To-USB / Maintenance / AI-Integration 三脚本 | ✅ |

### 34.2 6 大件（v2 计划，40000 字）

在 v1 地基之上的下一级台阶（详见
[`docs/PORTABLE_AI_SPLIT_PLAN.md`](docs/PORTABLE_AI_SPLIT_PLAN.md) v2 章节）：

| 件 | 内容 |
| --- | --- |
| 内核 MicroVM | Hyper-V 隔离容器每 App 一 VM（`New-VM -EnableVBS`）、HvSocket、层 8–10（HVCI/Secure Boot/SBOM） |
| 万能开 2.0 | UWP（IApplicationActivationManager）/ MSIX / PWA / WSLg / Docker / Flatpak / Wine 全格式打开 |
| 插件市场 | market.json 轻市场（GitHub Releases 分发）+ IPluginHost SDK + VSCode/Chrome 插件兼容 |
| 云同步 | rclone 增量加密、10 分钟整机恢复、版本历史（端到端加密，密钥不出本机） |
| 企业安全 | 沙箱 + 0day 隔离网 + 离线杀毒 |
| 跨平台 | Mac（UTM/QEMU）+ Linux（KVM + virtiofs）宿主支持 |

---

## 三十五、常见问题（FAQ）

**Q: 怎么打开 Variable？**
安装版双击 `Variable_1.0.0_x64-setup.exe` 安装后从开始菜单打开；或直接运行
`src-tauri/target/release/Variable.exe`；开发模式 `npm run tauri dev`。
详见第二十七章。

**Q: 提示缺少 WebView2？**
安装 Evergreen Runtime：https://developer.microsoft.com/microsoft-edge/webview2/

**Q: 双击 Esc 没反应？**
双 Esc 由 Rust 侧 30ms 轮询识别，与焦点无关；若与某些全屏游戏的 Esc 语义
冲突，可稍放慢两次按键间隔（500ms 窗口）。确认 kbdhook 线程已启动可查
日志中的 env 切换记录。

**Q: 第三方软件嵌不进去（提示回退独立窗口）？**
三类原因：UWP/商店应用、管理员权限应用、无标准主窗口的应用。嵌入子系统
会等待最长约 30 秒；超时后应用保持独立窗口运行（不会被害），可稍后重试。
启动器型软件（如 Wallpaper Engine）已通过进程树捕获支持。也可检查应用的
适配等级（兼容性防线，A/B/C）。

**Q: 嵌入的应用崩溃了会拖垮 Variable 吗？**
不会。嵌入只是窗口层级重父级，应用运行在完全独立的 OS 进程里；崩溃只影响
它自己的窗口（虚拟窗口内容失效，手动关闭该虚拟窗口即可），环境与其他窗口
不受影响。

**Q: 通讯软件提醒会不会看我的聊天记录？**
不会，也做不到。Variable 只轮询微信/QQ 等应用的**窗口标题**（标题里带
「(3)」这类未读计数），聊天正文在对方进程内存里，Variable 不读取、
也无法读取，且全程零网络。

**Q: Win+E / Win+D 被占用了怎么办？**
默认表已改用 ctrl+alt+\* 变体规避 Win11 保留键；若仍有冲突，被占用的
快捷键逐项注册失败并诚实降级——功能仍可从任务栏 / 开始菜单 / 快捷面板 /
命令面板进入，日志里会记录哪一项不可用。

**Q: Wallpaper Engine 的 scene 壁纸为什么"打不开"？**
scene 类型是 WE 引擎实时渲染的，Variable 无法在 WebView 里复现；点击后
走 `wp_engine_open` 在**系统桌面**上应用（官方控制接口），配合
壁纸模式「系统桌面」（system 让位）或双击 Esc 临时隐藏环境即可观看。

**Q: 想彻底离线构建？**
先在联网机器执行一次 `npm install` 与 `cargo fetch`（或完成一次
`build-windows.bat`），之后 `cargo build --offline` + `npm ci --offline` 可复现。

**Q: 卸载后数据在哪？**
`%APPDATA%\com.variable.app`，重新安装后自动继续使用；便携版在 `data\`。
卸载器会询问数据去向（回收站式可反悔），不会静默删除。

**Q: 命运推演为什么叫「推演」而不是「预测」？**
它是基于你设定的特质权重与随机种子的**确定性模拟**：同种子可重现，
改一个滑块结果就不同——思想实验工具，不是对现实的预言。

**Q: 项目分析的「大白话」是 AI 生成的吗？**
不是。全部由本地七级词典 + 规则引擎生成，零网络、零模型调用。

**Q: 环境里的动效太多/太少？**
设置 → 氛围 → 密度/几何/字体/焦点环/图标尺寸五档位；设置 → 无障碍 →
动效时长全局缩放（0.5x）；系统讲述人开启时自动叠加 reduce-motion。

**Q: 开放 IPC 网关安全吗？**
默认关闭、零监听；开启后仅绑定 127.0.0.1 环回，需 token + scope 授权，
数据连接器只读 + SELECT 白名单。出站动作另走 netconsent 确认。

---

## 三十六、已知边界与路线图

- 1.0sno9u.vxe 代码批次（B-1…B-35）已全部落地，剩余为三宿主实机验收
  （B-30 发版门禁）与 B-11 云 AI 真机矩阵点验（需真实账号）；
- 实机项如实清单：真实下载部署（VS Code/工具链）、Electron 实嵌、10GB
  迁移、8TB 卷、云 AI 出站经代理、三层嵌套、杀软误报观察；
- 1.x 阶段（收官后）：扩展生态 .uxpack（B-36…B-40）；
- 便携虚拟系统 v1（12 模块）剩余 6 模块按
  [`docs/PORTABLE_AI_SPLIT_PLAN.md`](docs/PORTABLE_AI_SPLIT_PLAN.md) 五 AI
  分支推进；v2 六大件（MicroVM/万能开 2.0/插件市场/云同步/企业安全/
  跨平台）随后启动；
- 真机验收总口径：B-30 三宿主矩阵 13 条逐格勾选，**无未经实测的 ✅**。

---

## 附录 A：一次典型使用旅程

1. **清晨开机**：双击图标 → 启动剧场（七层氛围 + 胶囊进度 + 文件流带，
   全部真实事件驱动）→ 桌面图标交错淡入、任务栏展开。Win+N 扫一眼通知
   中心（昨夜排队回放的提醒按序补发），快捷面板确认 Wi-Fi 与勿扰状态，
   时钟悬停看一眼今日节气。
2. **白天写作**：桌面双击「Write」→ 星空下的虚拟窗口编辑器。Ctrl+N 新建
   记录，粘贴截图与视频素材，打上标签 Ctrl+S；在 Mind 画布上把灵感节点
   「复制为引用」粘回正文，双击即可来回跳。第二个 Write 窗口中键任务栏
   图标即开，两篇对照着写。觉得背景吵？Ctrl+Alt+P 进纯净模式，音景切到
   「雨夜」，音量淡变不炸耳。
3. **读别人的代码**：切到项目分析，导入一个开源仓库；对核心函数
   「查看推理链」，再「逐行解剖」读 6000 行文件；想加功能就放意图节点，
   「写入源文件」（自动备份 + 原子写盘 + Ctrl+Z 可回滚）。
4. **把游戏关进环境里**：把 WorldBox 拖进启动器 → 双击图标 → 游戏窗口
   出现在环境内的虚拟窗口里，系统任务栏与 Alt+Tab 里完全没有它；
   逐应用音量记忆让游戏声与音乐软件互不打架；双击 Esc 瞬间切回真实桌面
   回个消息，再双击 Esc 回到环境，游戏还在原地。
5. **傍晚整理**：把常用软件拖进桌面文件架，六色标识分好类；文件管理器
   双栏对照，大文件雷达扫一遍 C 盘；回收站里搜索清掉垃圾，焚毁两个敏感
   文件；数据安全中心的信任链全绿。
6. **夜晚推演**：命运推演里搭一个角色，种子 20260909，双击关键转折续分
   子树，导出为思维导图归档。设置里点开 Wallpaper Engine 的场景壁纸 →
   system 让位模式 → 真实桌面上星河流动，双击 Esc 随时回来。

## 附录 B：命名与约定速查

| 约定 | 说明 |
| --- | --- |
| `.mindmap` | 导图档案（JSON：`app: "variable-mindmap"` + formatVersion） |
| `.project` | 项目分析档案（目录树快照/解读/坐标/引用表） |
| `.fatetree` | 命运推演档案（画像/参数/树/版本/自定义词典） |
| `.dict.json` | 用户词典包（离线导入/导出） |
| `.vwp` | 壁纸工坊工程（图层/变换/滤镜/多显示器分配） |
| `.vxs` | 四合一资源包（主题/词典/图标包/声音方案，带校验与安全扫描） |
| `.vxa` | 存档柜加密存档（清单 + 校验和） |
| `.backups/*.bak` | 源文件写回前的自动备份 |
| `xref:` 锚点 | 跨空间引用（Write 正文 ↔ Mind 节点），随内容持久化 |
| `variable://` | 开放协议（词表路由，便携态注册/退出退订） |
| `variable:vwm:geom:v2` | VWM 虚拟窗口几何持久化键 |
| `variable:taskbar:pins:v1` | 任务栏固定项持久化键 |
| `variable:icons:hidden` | 桌面图标隐藏模式持久化键 |
| `volmem.json` | 逐应用音量记忆 |
| 语义色板 | 根 #4f709c · 目录 #8babc6 · 源码 #a8c8e8 · 配置 #cfe4f8 · 文档 #dce4ec · 意图/引用 #f8d4e4 |
| 通俗风格 | 生活比喻 / 故事叙事 / 工程说明（三风格，全词典通用） |
| 适配等级 | A 完整支持 / B 部分支持（标注短板）/ C 建议独立窗口 |

## 附录 C：批次演进史（E-8 → E-18）与本会话改动

Variable 采用小批次演进，每个批次都有明确的验收与教训记录
（完整开发日志见 [`project_memory.md`](project_memory.md)）：

- **E-8**：zh-TW 繁体语言包（s2t.ts 字符级转换 + 词汇覆盖）、拼音/首字母
  搜索、通讯软件提醒（imwatch.rs）、NSIS/MSI 打包与验收清单；
- **E-9**：**VWM 虚拟窗口管理器**落地——四软件改由桌面层内虚拟窗口托管，
  translateZ 包含块实现多 WebGL 光影随窗口走，display:none 保活最小化；
- **E-10**：用户实测缺陷集中修复——交互宿主补全、explorer/recycle 收进
  VWM、红绿灯顺序统一（绿|黄|红）、快捷键默认表改 ctrl+alt+* 规避
  Win11 保留键；
- **E-11**：实机自检修三根因——右键子菜单死锁、双快捷键表并存、
  VWM 红绿灯错排；
- **E-12/13**：Wallpaper Engine 扫描导入（注册表探测 Steam 任意盘）、
  图标尺寸对齐 Windows、壁纸 1:1 清晰化（去 blur/暗罩）、环境完全覆盖
  Windows 任务栏；
- **E-14**：四款功能图标独立视觉语言重设计（AppGlyphs）、VWM 窗口弹簧/
  关闭/飞行/贴靠全套动效、图标质感；
- **E-15/15b**：Wallpaper Engine 全类型打开（web iframe 内嵌 +
  wp_engine_open 官方控制接口 + we-open.cmd 包装脚本）、system 让位模式；
  证伪「透明窗口透出」方案（WebView2 透明与视频渲染冲突）；
- **E-16**：第三方应用嵌入子系统（embed.rs）初版——SetParent、边界/焦点/
  关闭控制、桌面图标隐藏模式、原生图标提取、双 Esc 退出；
- **E-17/18**：环境切换键位定型——kbdhook.rs 重写为 GetAsyncKeyState
  轮询线程（双击 Esc 切环境、Del+Backspace 真退出，实机注入验证）；
  图标一致性（.lnk 先解析再提取）；
- **v1.x 五计划**：355 项功能由 20 个 AI 协作组代码级全部交付并全仓收口
  （见第三十三章）；
- **实机反馈修复批次**：动态壁纸活化（living 模式：粒子叠加 + Ken Burns
  缓动）、128px Jumbo 高清图标提取（icon_dataurl_hd）、启动氛围七层重铸
  （BootAtmosphere：双层极光幕/三层尘埃场/流星/暖光/暗角/颗粒/扫描线，
  强度随真实进度 0.35→0.55）、游戏级胶囊进度条（CapsuleBar：全宽 HUD/
  右上角百分比/24px 分段刻度/扫光；启动文案全英文化 Consolas 大写字距）、
  build-windows.bat 打包前自动停掉仓库内运行实例（stop-variable.ps1，
  防 os error 5 文件锁）。

## 附录 D：项目文档索引

| 文档 | 面向 | 内容一句话 |
| --- | --- | --- |
| 本 README | 使用者 | 现有能力的完整产品手册 |
| [project_memory.md](project_memory.md) | 协作者 | 逐批次开发日志与教训（工程记忆） |
| [docs/BLUEPRINT-1.0sno9u.vxe.md](docs/BLUEPRINT-1.0sno9u.vxe.md) | 协作者/架构评审 | v1.0sno9u.vxe 目标形态：六层架构、子系统规格、契约、坑位与边界 |
| [docs/MASTER-PLAN-1.0sno9u.vxe.md](docs/MASTER-PLAN-1.0sno9u.vxe.md) | 协作者/实施 | 已做/待做矩阵、里程碑 M0–M9、批次 Checklist、验收与应急演练 |
| [docs/ENGINE-Version-01XHI9DN.1.5xw-功能全景.md](docs/ENGINE-Version-01XHI9DN.1.5xw-功能全景.md) | 全员 | 五计划合一的 355 项功能总集（十五功能域） |
| [docs/ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md](docs/ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md) | 全员 | 20 个 AI 组的责任域切分 |
| [docs/ENGINE-Version-01XHI9DN.1.5xw-实施步骤.md](docs/ENGINE-Version-01XHI9DN.1.5xw-实施步骤.md) | 全员 | 355 项的施工顺序与依赖 |
| [docs/五路进度总览.md](docs/五路进度总览.md) | 全员 | 20 组实时进度快照与终检结论 |
| [docs/AI1…AI20 完成报告](docs/AI20-完成报告.md) | 协作者 | 各组交付清单、证据与自检结果 |
| [docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md](docs/PORTABLE_VIRTUAL_SYSTEM_PLAN.md) | 全员 | 40000 字便携虚拟系统施工计划（12 主章 + 18 扩充章） |
| [docs/PORTABLE_AI_SPLIT_PLAN.md](docs/PORTABLE_AI_SPLIT_PLAN.md) | AI 执行者 | v1 五 AI + v2 六 AI 的独立分工与验收 |
| [docs/终极形态施工总计划.md](docs/终极形态施工总计划.md) | 协作者 | 早期总计划与批次定义 |
| [docs/ARCHITECTURE_V2.md](docs/ARCHITECTURE_V2.md) | 架构评审 | V2 架构说明 |
| [docs/DESIGN.md](docs/DESIGN.md) / [docs/INTERACTION.md](docs/INTERACTION.md) / [docs/UI-COPY.md](docs/UI-COPY.md) | 设计 | 视觉/交互/文案规范 |
| [docs/ICON-SPEC.md](docs/ICON-SPEC.md) | 设计 | 图标规范（含 128px 提取口径） |
| [docs/SYSTEM-COMBOS.md](docs/SYSTEM-COMBOS.md) | 使用者 | 系统级组合键说明 |
| [docs/COMPAT-WALLPAPER-ENGINE.md](docs/COMPAT-WALLPAPER-ENGINE.md) | 协作者 | Wallpaper Engine 兼容细节 |
| [docs/EMBED-MANIFEST.md](docs/EMBED-MANIFEST.md) | 第三方 | variable-embed.json 嵌入声明协议 |
| [docs/TEST-HOOKS.md](docs/TEST-HOOKS.md) | 测试 | testid 约定与 boot-ready 哨兵 |
| [docs/ROUTES.md](docs/ROUTES.md) / [docs/SETTINGS.md](docs/SETTINGS.md) | 开发者 | 路由公开表 / 74 项设置自动文档（gen-* 再生成） |
| [docs/ACCEPTANCE_CHECKLIST.md](docs/ACCEPTANCE_CHECKLIST.md) | 验收 | 全流程手测清单 |
| [docs/acceptance/](docs/acceptance) | 验收 | 发版演练、三宿主矩阵等实机证据归档 |
| [docs/licenses.md](docs/licenses.md) | 法务 | 依赖许可汇总 |
| [docs/false-positive.md](docs/false-positive.md) | 运维 | 杀软误报观察记录 |

> 演进规则：契约以 BLUEPRINT 为准；进度以 MASTER-PLAN 状态矩阵为准；
> 功能全集以 ENGINE-Version 功能全景为准；三者随每批次同步修订，冲突时以
> 最新批次日志（project_memory.md / 五路进度总览）为准。

## 附录 E：功能全景十五域分布表

（摘自《ENGINE-Version-01XHI9DN.1.5xw-功能全景》，355 项按域归位）

| 域 | 名称 | 项数 | 来源构成 |
| --- | --- | --- | --- |
| 1 | 启动与品牌剧场 | 6 | ASCENT-60 域 S（U-01…U-06） |
| 2 | 窗口与空间管理 | 33 | APEX-70 域 F + NEXT-40 域 A + ASCENT-60 U-14 + SUMMIT-90 域 W + 化境域 C |
| 3 | 桌面与图标表达 | 18 | NEXT-40 域 B + ASCENT-60 U-13 + 化境域 A + V-96 |
| 4 | 任务栏与开始菜单 | 20 | ASCENT-60 U-15 + SUMMIT-90 域 T + 化境域 B |
| 5 | 键盘与输入手感 | 29 | APEX-70 域 B + ASCENT-60 U-58/U-59 + SUMMIT-90 域 K + 化境域 G + V-94 |
| 6 | 文件与数据能力 | 34 | APEX-70 域 E + ASCENT-60 域 N + N-31 + SUMMIT-90 域 F + 化境域 D |
| 7 | 效率与工具中枢 | 27 | APEX-70 域 D + NEXT-40 域 C + ASCENT-60 U-17/U-18 + 化境域 E + V-97/V-98 |
| 8 | 系统集成与硬件 | 23 | ASCENT-60 域 H + NEXT-40 域 D + 化境域 F |
| 9 | 兼容性防线 | 16 | APEX-70 域 C + SUMMIT-90 域 C |
| 10 | 安全与隐私 | 6 | ASCENT-60 域 K（U-31…U-36） |
| 11 | 开放生态 | 35 | APEX-70 域 H + ASCENT-60 U-37…U-39/U-42 + NEXT-40 域 E + SUMMIT-90 域 O + 化境域 I |
| 12 | 视觉、个性化与氛围 | 47 | APEX-70 域 A/J + ASCENT-60 域 V + N-33 + SUMMIT-90 域 A + 化境域 H |
| 13 | 声音与通知 | 10 | APEX-70 域 G + ASCENT-60 U-51/U-52 + N-32 |
| 14 | 无障碍与本地化 | 8 | ASCENT-60 U-40/U-41 + SUMMIT-90 域 U |
| 15 | 工程质量、性能与收官 | 43 | APEX-70 域 I + ASCENT-60 域 P + NEXT-40 域 G + SUMMIT-90 域 S/Q/X + 化境域 J |
| — | **合计** | **355** | Z 70 + U 60 + N 35 + M 90 + V 100 |

---

*Variable — Private Space · v1.0.0 · 完全离线的私人桌面环境、虚拟窗口管理器
与四重空间 · 五计划 355 项功能 · 20 组 AI 协作交付。*




