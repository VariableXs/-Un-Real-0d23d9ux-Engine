[package]
name = "varix"
version = "0.1.0"
edition = "2021"
description = "Varix kernel — AI-01 boot & platform domain (F001~F025)"

[lib]
name = "varix"
test = true

# The kernel image target; unit tests live in the lib and run on the host,
# so the bin is excluded from the test/bench profiles.
[[bin]]
name = "varix"
path = "src/main.rs"
test = false
doctest = false
bench = false

[dependencies]
