# AI-07 交付报告 · 效率中枢组

> 交付日期：2026-09-08 ｜ 范围：N-13…N-18、V-41…V-50（分工图 Wave 2 效率路）
> 口径：代码级完成；真机验收统一待 B-30 三宿主阶段。

## 交付清单

### N-13 命令面板（Command Palette）
- `src/lib/commands/{types,registry,builtin}.ts`：命令注册表（应用/动作/工具三类）、
  模糊匹配 + 拼音首字母、频率/新近度学习排序、钉选；
- `src/system/palette/CommandPalette.tsx`：三类一框 UI，V-41 内联计算 / V-49 时间戳结果卡内嵌；
- 命令注册纪律：AI-07 全部动作（38 条）已注册进注册表。

### N-14 统一搜索 2.0
- `src/lib/search/unified.ts`：聚合五类（应用/文件/内容/命令/设置）、高级语法过滤、
  V-47 搜索历史隐私联动（三态：off 不留痕 / 本地 / 跨重启）。

### N-15 剪贴板历史中心
- 后端 `src-tauri/src/shell/cliphist.rs`：Win32 序列号轮询（300ms 去抖）录制
  文本/文件路径/图片 DIB；DPAPI 加密落盘（换机/换用户诚实清空）；
  敏感应用名单期间零记录；容量 500 条 / 200MB FIFO（钉选不淘汰）；
  图片 >16MB 拒存提示；「关闭即焚」模式；
- 前端 `src/system/tools/ClipboardHistoryApp.tsx`：事件驱动（`cliphist://changed`），
  后端写回（SetClipboardData），敏感名单/关闭即焚设置面板。

### N-16 截图标注与贴图
- `src/lib/snip/annotations.ts`：标注模型（画笔/箭头/马赛克/文字）+ 裁剪。

### N-17 快捷键中心
- `src/lib/keys/registry.ts`：统一键位注册表、冲突检测、Profile、
  V-50 速查表导出（Markdown/HTML 双格式，占用/冲突标记）。

### N-18 自动化宏引擎
- 后端 `src-tauri/src/shell/macros.rs`：全局急停（硬编码安全分叉）、
  UAC/密码框判停、cron 子集调度（时间触发器）、剪贴板正则触发事件、
  SendInput 键鼠模拟（护栏全绿才执行）；
- 前端 `src/lib/macros/engine.ts`：宏定义、五类触发器、动作执行。

### V-41…V-50 十项效率模块（`src/lib/quick/`）
| 项 | 模块 | 说明 |
|---|---|---|
| V-41 | inlineCalc.ts | 内联计算（路径/命令/URL 零误判让位） |
| V-42 | （N-13 学习排序） | 使用频率加权 |
| V-43 | qrcode.ts | 文本→二维码（本地生成） |
| V-44 | scrub.ts | 纯文本粘贴（去格式/实体解码） |
| V-45 | （N-15 钉选收藏） | 跨重启保留 |
| V-46 | quicknote.ts | 速记（ctrl+alt+n） |
| V-47 | privacy.ts | 搜索历史隐私三态 |
| V-48 | autocomplete.ts | 运行框自动补全 |
| V-49 | timestamps.ts | 时间戳插入（多格式） |
| V-50 | keys/registry.ts | 键位速查表导出 |

## 测试与验证（2026-09-08）
- vitest：AI-07 域 93 通过（quick-modules 31 + commands-registry 14 + keys-registry/keymap 62）；
- cargo test：AI-07 域 8/8 通过（cliphist 4 + macros 4）；
- tsc：AI-07 文件零错误；
- audit：AI-07 IPC 全注册、i18n 键补齐（scCheat*）；全局 audit 未过项均属其他组
  （lockscreen/weather/taskbar 本地词典设计、theme-studio/widgets 既有失败）；
- 顺手修复（仅为解除共享测试目标编译阻塞，不涉功能）：explorer.rs 测试 use、
  transfer.rs TrErr derive(Debug)、archive.rs 测试类型不匹配。

## 遗留
- Win32 路径（剪贴板录制/键鼠模拟）真机验收待 B-30；
- 宏录制回放（坐标→窗口相对化）未在本轮范围，待 Wave 3。
