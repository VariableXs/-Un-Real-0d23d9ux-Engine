# 依赖收口（步骤 0034）

> 改 VARIX/PULSAR/QUASAR/TRINITY 既有文件前先在此登记。

## 2026-09-11 · AURORA-1000 底盘批次（步骤 0001~0190）
- 登记人：AI（WorkBuddy 会话）
- 本次未改动 VARIX 既有模块的任何行为；仅：
  - 新增 `kernel/varix/src/metrics.rs`（步骤 0028，顶层新模块，无既有模块改名/覆盖）
  - `kernel/varix/src/lib.rs` 追加 `pub mod metrics;`（纯追加一行区段）
  - 新增 `kernel/varix/tests/fuzz.rs`（纯新增）
  - 顶层 display/input/audio 等既有名占用不触碰，aurora 命名空间不动
- 遵守既有约定：无 Vec/String/Box 的 no_std 域代码风格仅适用于域模块；metrics 为底层
  设施，使用 core 原子类型，不引入分配。

## 2026-09-11 · W1 收口 + M1 里程碑（步骤 0191~0280）
- 登记人：AI（WorkBuddy 会话，步骤 191~380 批次）
- 对既有文件的改动（均为追加，不覆盖他域内容）：
  - `kernel/varix/src/checks.rs`：`MAX_DOMAINS` 32→96（原注册表已 >32，静默截断修复）
  - `kernel/varix/src/robust.rs`：`run_kernel_checkup()` 注册表追加 AURORA 15 域 + W1 集成（纯追加行）
  - `kernel/varix/src/aurora/mod.rs`：追加 `pub mod integration;`
- 新增：`kernel/varix/src/aurora/integration.rs`（步骤 0249~0255 跨域集成，纯新增）
- 新增文档/脚本：`docs/CHECKSET.md`（填 W1 域行）、`docs/M1-验收.md`、`docs/perf/基线.json`、
  `docs/降级链.md`（W1 归档段）、`docs/知识/M1-显示地基.md`、`docs/知识/M1-复盘.md`、
  `docs/交接-W1→W2.md`、`scripts/gen-numbers.py`、`scripts/audit-status.sh`
- 不触碰：display/input/audio 等既有顶层模块、他域（W2/W3 及之后批次）文件与注册行。

## 2026-09-11 · W3 收口批次（步骤 0729~0760）
- 登记人：AI（WorkBuddy 会话，W3 桌面体验与基础应用收口）
- 本次对既有模块的改动（均为纯追加）：
  - `kernel/varix/src/lib.rs` 追加 `pub mod w3gate;`
  - `kernel/varix/src/robust.rs` `run_kernel_checkup()` 末尾追加
    `checkup.register(crate::w3gate::run_w3gate_checks());`
- W3 八域（designsys/fileman/settings/apps/terminal/editor/imageview/player）对既有
  基座的依赖：应用框架（appfw）、UI 组件（widgets）、剪贴板（clipboard）、字体
  （gfx::text）、2D 渲染（render2d）、播放器接视频编解码（HORIZON H01）与音频域。
  W3 域代码均为新增 `kernel/varix/src/<domain>/mod.rs`，不覆盖既有模块语义。
- 新增 `kernel/varix/src/w3gate.rs`（W3 联调集成门禁，纯追加）。
- 修复 `kernel/varix/src/metrics.rs` M002 render 自检的借用生命周期错误（`&'static`
  detail 悬垂），逻辑断言不变。
- 修复 `kernel/varix/tests/fuzz.rs` compositor::make_layer 调用缺参（按当前签名补
  color/alpha/blend 三参，fuzz 语义不变）。
