# AI-20 完成报告 — 质量门禁与收官组

任务来源：`docs/ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md`（M-79…M-90、V-91…V-93、V-95、V-99、V-100，共 18 项）。
使命：每一次发版都比上一版更可信；最后十针把成熟度缝合成艺术品。
纪律：只做 AI-20 分内任务 + 全仓门禁必须全绿的协同修复；红线项（V-95 本地完整构建验证、M-80 dist 零命中验收）逐项实测。

## 交付清单

| 任务 | 内容 | 落点 |
|---|---|---|
| M-79 前端错误聚合看板 | 近 7 天错误 Top10（组件/消息/次数/首末时间）+ 一键复制 markdown 报告 + 手动清空；PII 采集层清洗；首末时间走 M-89 智能时间口径 | `src/lib/errBoard.ts` + `src/features/settings/QualityTab.tsx` |
| M-80 IPC 调用追踪 | dev 构建统一 IPC 埋点（命令/耗时/成败 → 环形缓冲）；Ctrl+Alt+F12 瀑布图面板（>100ms 标红 + 命令过滤）；release 死代码剔除——dist `ipcTrace` 零命中（实测 0 处） | `src/lib/ipcTrace.ts` + `src/system/devtools/IpcTracePanel.tsx` + `src/lib/ipc.ts` |
| M-81 设置漂移测试基建 | 前端：DEFAULT_SETTINGS 快照 + 新增键必须登记说明的双向漂移测试；后端：Rust 侧默认值与前端口径一致性集成测试 | `src/lib/__tests__/settingsDrift.test.ts` + `src-tauri/tests/settings_drift.rs` |
| M-82 视觉回归多主题矩阵 | 15 路由 × 3 主题（dark/light/hc）× 3 缩放（100/125/150%）= 135 张基线；阈值按主题标定（HC 收紧）；承 A-3 不替代 | `tools/visual-matrix.cjs` |
| M-83 键位审计 CI 门禁 | 扩展 keymap-audit：默认表功能冲突（error）+ 系统保留键重叠（warn）+ 裸 keydown 基线；pre-commit 钩子（安装器 tools/hooks/install.ps1）；CI job L4.5；注入冲突实测拦截 | `tools/keymap-audit.cjs` + `tools/keymap-binds-dump.ts` + `tools/hooks/` + `.github/workflows/ci.yml` |
| M-84 性能影响声明 | PR 模板必填三选一段（无影响/<1ms/>1ms 必须贴 bench）；`bench --snippet` 生成可直贴对比表（对上一基线 + 预算口径 + 回归红线 >10%） | `.github/PULL_REQUEST_TEMPLATE.md` + `tools/bench.cjs` |
| M-85 依赖审计自动化 | dep-audit.cjs 聚合 npm+cargo 审计（零网络、报告归档 docs/selfcheck/）；sysmaint 周任务到点拉起（安装档如实报「无开发工具链」）；QualityTab 只读状态区；红线：只审计不升级 | `tools/dep-audit.cjs` + `src-tauri/src/shell/sysmaint.rs` + `QualityTab.tsx` |
| M-86 文档链接检查（P0） | 全库 markdown 相对链接 + 锚点检查（外链不查、零网络）；内联代码段中的 `[]()` 样例排除；断链清零后上门禁（CI job L4.6） | `tools/link-check.cjs` + `.github/workflows/ci.yml` |
| M-87 发版演练 | release.ps1：版本号三处一致性（不一致即拦截）→ 全量门禁 → M-80 剔除 → cargo check → NSIS 构建 + 产物校验 → changelog → ROUTES/SETTINGS 再生成一致性 → 清单归档 docs/acceptance/ | `tools/release.ps1` + `docs/acceptance/release-rehearsal-2026-09-09.md` |
| M-88 全局 Tooltip 规范（P0） | 三统一：样式令牌化（--tooltip-*/.vx-tip）、延迟统一引用 M-71 档位、快捷键后缀统一圆括号式；超长截断 + data-tip-full 无障碍兜底 | `src/lib/tooltip.ts` + `src/styles/tooltip.css` |
| M-89 单位与数字规范（P0） | 容量（formatCapacity 兼容现状口径 + M-78 三模式）/ 速度 / 智能时间（formatSmartTime 相对/绝对切换）统一出口 | `src/lib/formatUnits.ts` |
| M-90 跨午夜会话正确性（P0） | 统一日界事件 `day://rollover`（时钟/日历/简报/DND/统计订阅同一事件）；系统时间手动调整监测 + 唤醒回查 | `src/lib/dayRollover.ts` |
| V-91 演示模式 | 快捷面板一键：勿扰 + 保持唤醒（会话级）+ 可选指针增强；退出原样恢复（toast 诚实清单 + beforeunload 双保险）；红线：不做投屏自动进入 | `src/system/tray/DemoMode.tsx` |
| V-92 关机倒计时取消 | 全屏轻遮罩环形进度 + 大取消按钮；Esc = 取消（M-34 浮层栈）；无倒计时零 DOM | `src/system/power/PowerCountdown.tsx` + `powerGate.ts` |
| V-93 Windows 偏好搬家向导 | 只读读取壁纸/强调色/深浅色/区域格式/24h 制 → 建议清单逐项应用；`prefsImportDone` 首启一次；设置页可重开 | `src/features/onboarding/PrefsWizard.tsx` + `src-tauri/src/shell/quality.rs` |
| V-95 卸载器数据抉择 | NSIS PREUNINSTALL 钩子：是 → 数据目录整体移入回收站（SHFileOperationW + FOF_ALLOWUNDO，可反悔绝不覆写）；否/静默 → 完整保留（默认）；失败如实报告保持原样；**本地完整构建安装包验证** | `src-tauri/nsis/uninstaller-hooks.nsh` + `tauri.conf.json` |
| V-99 依赖诚实声明页 v2 | 「关于→外部依赖」：winget/OCR/打印机/字体回退逐项标注用途/缺失行为/获取方式 + 状态检测按钮；与 V-42/V-81 降级提示互链；离线完整可读 | `src/features/settings/about/DependencyHonesty.tsx` |
| V-100 收官毕业页 | 完成度墙（gen-completion.cjs 从进度文档自动生成，未完成如实灰色）+ 本地使用摘要（M-28 统计，仅本地可关闭）+ 数据导出入口 | `src/features/settings/about/GraduationWall.tsx` + `tools/gen-completion.cjs` + `src/generated/completion.ts` |

## 自检结果（M-87 发版演练全绿 · 全流程模式）

`tools/release.ps1` **全流程模式**（非 DryRun，含 NSIS 真实构建）14/14 PASS，清单归档 `docs/acceptance/release-rehearsal-2026-09-09.md`：

| 步骤 | 状态 |
|---|---|
| 1.版本号一致性（package.json = tauri.conf.json = Cargo.toml = v1.0.0） | PASS |
| 2a.tsc 类型检查 | PASS |
| 2b.vitest 单测（全量） | PASS |
| 2c.静态审计 audit.cjs（IPC/i18n/TESTID/视觉门禁） | PASS |
| 2d.键位门禁 keymap-audit | PASS |
| 2e.文档链接 link-check | PASS |
| 2f.前端构建 vite build | PASS |
| 3.M-80 IPC 追踪剔除（dist 零命中） | PASS（实测 0 处） |
| 4.cargo check | PASS |
| 5.NSIS 安装包构建 | PASS |
| 5b.NSIS 产物校验 | PASS — `Variable_1.0.0_x64-setup.exe`（6.5 MB，NSIS-3 Unicode） |
| 6.CHANGELOG 草稿生成 | PASS |
| 7a.ROUTES.md 再生成一致性 | PASS |
| 7b.SETTINGS.md 再生成一致性 | PASS（漂移 13 项已再生成） |

- AI-20 域测试：`ai20-quality.test.ts`（errBoard/ipcTrace 环形缓冲与过滤/formatUnits/dayRollover/tooltip）+ `settingsDrift.test.ts` 全部通过；全量 vitest 通过（此前他组 6 处失败已由各会话修复）
- M-83 注入验收：注入默认表功能冲突 → keymap-audit 退出码 1 拦截；注入系统保留键 → WARN 提示（均实测后还原）
- M-85 注入验收：伪造 last_run_ms → due=true → tick 拉起脚本 → 状态落盘回读一致（实测）
- M-86 验收：上线前全库断链清零（修复 N 处历史断链/锚点漂移）
- V-95 验收（编译级证据链闭环）：`release.ps1` 全流程模式产出 `Variable_1.0.0_x64-setup.exe`（NSIS-3 Unicode，6.5 MB）；7-Zip 解包确认插件运行时齐备；Tauri 官方 NSIS 模板（CLI 二进制内嵌）`Section Uninstall` 顶部以 `!ifmacrodef NSIS_HOOK_PREUNINSTALL` + `!insertmacro` 调用钩子，本仓钩子文件定义该宏且 `tauri.conf.json` `installerHooks` 已注册，makensis 全量编译零错——四环相扣，钩子必然编入卸载器；数据抉择先于任何文件删除执行
- PS 5.1 兼容：release.ps1 两处原生命令 stderr 误报坑（NativeCommandError）已修复（Invoke-Step / Invoke-GenCheck 内 Continue 化，成败只看退出码）

## 协同修复（并发会话覆盖遗留，均为「不修全仓门禁不绿」项）

1. **tsc 全仓修复**：6 个窗口域测试文件 `Object is possibly 'undefined'`（multiselect/snap2/stages/timeline/rescue/winops）；FileTicker.tsx 未用导入；QualityTab depAudit 状态接线恢复（被覆盖丢失）+ `ipc.ts` 补 `DepAuditStateView` 类型别名导出
2. **audit.cjs i18n 审计修复**：
   - 真缺失键补齐：`fsPath`/`privacyShred`/`orchClear`（zh+en）、`exDualPane`/`exCopyPath`（en）、`sceneGuardTitle`→复用既有 `scGuardTitle`（改组件引用而非重复加词）
   - 误报修正：本地词典车道（lockT/sceneT/wpT，`const t = useMemo(() => xxxT(), [])` 绑定模式）从全局 i18n 审计豁免——其键由各 labels.ts 本地解析，TS 类型即完整性保障，原先整片误报 72 键
3. **M-80 双重修复**：i18n 键 `q20IpcTraceNote` 改名 `q20DevPanelNote`（键名含 ipcTrace 子串撞验收字符串，文案不变）；IpcTracePanel 在 App.tsx 的 dev-only 接线恢复（被覆盖丢失）
4. **sysmaint.rs 域内修复**：civil_from_days 日期算法多 `+doe/1460` 项致 1970-01-01 算成 1969-10-01（civil_stamp_formats 既有测试红）→ 对齐标准 Hinnant 算法
5. **Z-14 键位预置冲突**：leftHand 剖面 clipboardHistory 与 runDialog 撞 ctrl+alt+r → 改 ctrl+alt+t（keymap-audit 实测拦截后修复）
6. **SETTINGS.md 漂移**：13 项（本组 V-93 的 1 项 + AI-16/AI-18 会话漏再生成的 12 项）已再生成同步
7. **AI-04 U-13 接线丢失通报**：`DesktopProfiles.tsx`（AI-04 已交付组件）当前无任何引用方——`desktop/profiles.ts` 被 DesktopIcons 引用已一并入树，但组件渲染接线疑似被并发覆盖丢失，请 AI-04 会话按原设计重接（本组不越权猜测挂载点）

## 已知边界（诚实声明）

- M-82 视觉矩阵为基线生成与比对基建；全量基线采集需真机多缩放环境（CI 侧按需接入），HC 标定与 AI-19 协作口径已内置
- M-85 cargo-audit 本机未安装（报告如实标注）；依赖升级一律人工审阅（当前 34 项 npm 通报已归档 `docs/selfcheck/dep-audit-2026-09-09.md`，critical 1 项为 vitest dev 依赖，不进产物）
- M-87 发版演练已在全流程模式本地实测（14/14 PASS，含 NSIS 真实构建与产物校验）；DryRun 模式按设计跳过 NSIS 构建（日常迭代快跑用）
- V-95 实机安装/卸载测试**有意不做**：本开发机已装 Variable 0.1.0（HKCU 卸载项）且 `$APPDATA\com.variable.app` 存在真实用户数据，实机测试会升级/移除现有安装并触碰真实数据目录——验证以编译级证据链 + 全量构建为限，真机 E2E 留待独立测试环境
- V-93 只读读取偏好，不迁移凭据类数据；V-91 演示模式不做投屏自动检测（误判风险，红线）
- M-88 规范面向新代码与 tooltipLabel 出口；存量散点 tooltip 的迁移属各域持续债务（audit 侧未设强制门禁）
- `src-tauri/src/shell/hardware.rs`、`boot.css` 等本组名下改动的合并冲突痕迹均已逐文件复核过编译与测试
