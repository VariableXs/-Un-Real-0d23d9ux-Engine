# 交接清单 · W3 → W4（步骤 0755）

> M3（桌面体验与基础应用，A401~A600）已收口，交棒 W4（系统服务与互联，AI-25~32，A601~A850）。

## 移交物

1. **八域代码**：`kernel/varix/src/{designsys,fileman,settings,apps,terminal,editor,imageview,player}/mod.rs`，各 25 项 CheckSet，全部接入 `run_kernel_checkup()`。
2. **W3 联调门禁**：`kernel/varix/src/w3gate.rs`（9 项：五场景联调 + 性能/降级/fuzz/真机占位），`cargo ktest` 全绿。
3. **文档**：`docs/CHECKSET.md`（W3 段）、`docs/M3-验收.md`、`docs/知识/M3-桌面体验.md`、`docs/知识/M3-复盘.md`、`docs/依赖收口.md` W3 批次、里程碑图 M3 ✅，tag `aurora-m3`。

## W4 需要消费的 W3 接口

- **通知中心（AI-26）**：托盘/角标语义沿用 shell 托盘；设置开关接 settings key 100 段。
- **搜索（AI-27）**：fileman `search` 与 settings `search` 的前缀/全文匹配算法可复用其固定容量 out 缓冲模式。
- **包管理/主题商店（AI-29）**：designsys `ThemeStore`（容量 8、查重安装/卸载）即商店接口的内核形态。
- **打印（AI-30）**：imageview `print_layout(img_w,img_h,page_w,page_h)` 已定义打印排版判定。
- **系统监视（AI-28）**：w3gate/metrics 的 `*_budget_ok` 模式可扩展为系统级预算面板。

## 纪律提醒

- W4 新域一域一目录（`kernel/varix/src/<domain>/mod.rs`），25 项 CheckSet + `run_*_checks` 接入 checkup。
- 改 W3 域的任何 `pub` 签名前先在 `docs/依赖收口.md` 登记并全仓 grep 调用点。
- 提交格式 `aurora1000(ai-XX): ...`；禁止 `git pull --rebase --autostash`。
