# VARIX 三体系统 · AI 分工完成图

> **定位**：三件套之三。把《实施总步骤图》的全部步骤分配给 6 个 AI 工位（AI-1 ~ AI-6，编号从 1 起），含职责、任务勾选清单、依赖、完成定义（DoD）、交付物与禁区。
> **总量依据**：第 0-3 批 + 五件长线基建 + 全程质量线 → 按线切分定 6 席；第 2 批（核心战役）拆两席防单点过重。
> **纪律**：多会话并行铁律——动手前 git status 盘点；只 add 自己的显式路径；push 前 fetch，远端有新提交先 rebase；推送超时走 gh_api_push 通道；验收产物一律归档 docs/acceptance/。

---

## 0. 分工总原则

1. **按线不按层**：每个 AI 端到端 owning 一条线（含编码/测试/验收/归档），不做"只写代码不验收"的半工位。
2. **接口最小**：AI 之间只通过归档产物（映射表/协议文档/基线数据）与既有总线（settings://、boot://、垫片协议）协作，禁止直接改对方辖区文件。
3. **独立可验收**：每个 AI 的每项任务自带自检六问与门禁，不依赖别人兜底。
4. **可停性**：任一 AI 在任一任务停住，系统整体仍处于可用中间态。
5. **质量线横切**：AI-6 不生产功能，只做全线的门禁执行、体验走查与归档执法。
6. **安全横切**：《实施总步骤图》1.7 实机安全宪法（三红线/五重保障/三纪律）对全体 AI 无差别生效——触实机的步骤必答安全三问（1.6）、走实机操作 SOP（1.8）；红线违例 = 任何 AI 的一票否决项，先停手后复盘。

---

## 1. 协作总图（依赖与接口）

```
                 ┌──────────────────────────────────────────┐
                 │            AI-6 质量与体验线               │
                 │   门禁执法 · 体验走查 · 基线 · 归档 · 演练   │
                 └──────╥─────────────╥─────────────╥────────┘
                        ║(执法)       ║(执法)       ║(执法)
 ┌──────────────┐  ┌────╨──────────┐  │  ┌──────────╨───────┐
 │ AI-1 引导衔接 │  │ AI-3 垫片命令面 │  │  │ AI-5 内核基建长线 │
 │ S0.* S1.3-5  │  │ S2.01-S2.04   │  │  │ S4.1-S4.5(并行)  │
 └──┬───────┬───┘  └────┬──────────┘  │  └──┬────────┬──────┘
    │引导项  │handoff目标 │ 归属表/协议   │     │MSC直写  │GPU/网络
 ┌──╨───────╨───┐  ┌────╨──────────┐  │  ┌──╨──────╨──────┐
 │ AI-2 部署引擎 │  │ AI-4 桌面承载  │◄─┘  │ (服务 AI-2/AI-4)│
 │ S1.1-2 S3.*  │  │ S2.05-S2.12   │      └────────────────┘
 └──────────────┘  └───────────────┘
```

- AI-1 → AI-2：ESP 引导项与 handoff 目标登记。
- AI-3 → AI-4：三色归属表与定版协议（S2.05 起的消费方）。
- AI-5 → AI-4：真机 USB 键鼠（S4.1）、SHARED 直写（S4.2）、GPU/网络（远期能力）。
- AI-5 → AI-2：引擎通道的基建前置（GPU/网络影响引擎体验）。
- AI-6 → 全体：门禁红绿灯与验收归档（不通过即打回）。

---

## 2. AI-1 · 引导与衔接线

- **职责一句话**：让"插盘开机→进对系统→交接顺畅"这条骨干全通，并长期持有引导配置桥。
- **管辖**：第 0 批全部（S0.1-S0.4）；第 1 批的 S1.3-S1.5；bootselect 四卡演进；last_boot 写回闭环。
- **任务清单**：
  - [x] S0.1 handoff 三件套提交入库（显式 add 新文件）——✅ 2026-09-21 `24f277e`，四线门禁单跑全绿，防自锁闸门用例在跑且 QEMU 实测 PASS
  - [x] S0.2 L0 配置桥方案 A（limine.conf module_path + 部署刷 ESP + Variable 同步通道）——✅ 2026-09-21 `8689966`/`487436f`/`8c98318`/`88a6a44`；机制升级为内核声明 internal module（Limine 源码证实：conf module_path 缺文件=panic 引导，违背「副本缺失→回退默认」验收条款；internal module flags=0 缺失=警告跳过，全部条款满足），QEMU 三变体+闸门断言全过，验收记录 docs/acceptance/ai1-引导衔接-S0批-2026-09-21.md
  - [◐] S0.3 U 盘内核重建重刷（补鼠标接线+handoff，三证齐）——✅ 2026-09-21 21:00 部署完成：HEAD 15f9dd8（ktest 3113/kcheck 0，worktree 复现构建与 AI-5 isoroot 逐字节一致）双端哈希 B0BEAC22 ✓、boot-select.json 双副本 5D3AFFC3 ✓、三证齐；剩余真机 F12 引导验证（随 S1.4 会话）
  - [◐] S0.4 关闭快速启动（HiberbootEnabled=0 双重启确认）——✅ 2026-09-21 21:02 键已置 0 且独立回读确认；剩余双重启后 --verify 复核（用户在场）
  - [ ] S1.3 ESP 补 U 盘 Windows 引导项 + handoff_target 参数化——⛔ 依赖 AI-2 S1.2（Windows 镜像未部署）
  - [ ] S1.4 bootselect 三卡真机走查 ×3——⛔ 依赖 S0.3 刷盘 + S0.4 + 1.8 SOP 实机会话
  - [ ] S1.5 handoff 全链走查 ×5 + 防自锁闸门拒绝路径演练——⛔ 依赖 S1.3/S1.4
  - [ ] last_boot 写回闭环（连续交替 ×5 正确）——⛔ 依赖 S1.5；另有设计缺口待拍板：内核 exFAT 只读无写路径，「进入 varix」侧归因需走 Variable 提权通道代写（详见验收记录 §5）
  - [ ] （远期）bootselect 四卡 UI 演进与 Variable 视觉对齐
- **依赖**：S1.3-S1.5 依赖 AI-2 的 S1.2（镜像就位才有引导目标）。
- **DoD**：M0+M1 达成；S0.2 后 Variable 设置页 found=true；走查记录全归档。
- **交付物**：引导走查报告、部署三证记录、handoff 全链计时表。
- **禁区**：不碰 Servo 线（AI-3/4）、不碰 Windows 镜像制作（AI-2）、固件只读+一次性 BootNext 永不刷固件；**内置盘 ESP/BCD 永只读**（操作前 GUID 定位+四件套 hash 基线对比）；触实机一律走 1.8 SOP（上机前 7 项不齐=不上机）；BitLocker 纪律（反复重启必 Suspend -RebootCount 0）。

## 3. AI-2 · Windows 部署与引擎线

- **职责一句话**：Windows 三形态（U 盘系统/隐形引擎/内置盘协作）的部署与编排全包。
- **管辖**：第 1 批 S1.1-S1.2；第 3 批全部（S3.1-S3.12）。
- **任务清单**：
  - [x] S1.1 盘体性能实测（顺序≥400MB/s 且 4K≥20MB/s 门）——✅ 2026-09-21 实测完成；4K PASS（20.4-21.5MB/s）但**顺序门 FAIL**（六轮 386.8-395.0，均值 391.9，USB 3.2 Gen1 链路饱和特征）——按纪律停下问询，S1.2 待用户拍板；报告 docs/acceptance/deploy/ai2-盘体实测-S1.1-2026-09-21.md
  - [◐] S1.2 Hasleo 部署 Win11 至 WIN_ENGINE（不启用 BitLocker/关快速启动/激活确认）——部署手册+只读预检就绪（docs/deploy/W1-U盘Windows部署手册-2026-09-21.md；_attic/ai2-w1-preflight.py 实跑 PREFLIGHT=PASS）；执行待 S1.1 拍板 + 用户在场（Hasleo GUI）
  - [◐] S3.1 W2 差分三级链脚本化（幂等+回滚 ×10）——✅ 脚本交付 portable/engine/（Engine-Chain.ps1 三后端 Diskpart|HyperV|Mock + engine-chain.json 容量配置化 + README）；**Mock 回滚演练 ×10 全轮 PASS**（Base/Apps 哈希每轮不变）；真实 VHDX ×10 待 S1.2 后在部署机以真后端执行
  - [◐] S3.2 引擎编排底座（五态状态机/心跳/幂等拉起）——✅ 代码级交付 src-tauri/src/shell/engine.rs：五态状态机显式建模+步进式短持锁（状态查询全程可响应）+心跳 47631 同源+幂等双保险（状态机锁+BUSY 原子标志）+心跳超时自动重启（预算 1 次）+拔盘联动安静收束+7 条命令注册+EngineBackend 可插拔（Mock/HyperV）；单测 17/17 全绿；真机端到端待 S1.2（含 Hyper-V 功能启用）
  - [ ] S3.3-S3.4 全屏→窗口级画面流（断流重连/缩放同步 ×20）——⛔ 依赖 S1.2
  - [ ] S3.5 输入注入+IME 专项（30s 无丢键）——⛔ 依赖 S1.2
  - [ ] S3.6 VWM 接管引擎窗（×100 无泄漏）——⛔ 依赖 S1.2（vwm 引擎窗类型与能力对照表已在位）
  - [◐] S3.7 占位卡拉起协议（取消/失败撤卡）——✅ 协议闭环（前端占位卡立即出现+唤醒接线+就绪聚焦不双开+防双击双窗+取消/失败撤卡+唤醒失败三要素透传；27 项测试全绿；thirdApps 按 channel 分流且 engineEnabled 门槛）；端到端自动化验证待 S1.2
  - [ ] S3.8 ramcache（关机整盘 hash 零残留校验）——⛔ 依赖 S1.2
  - [◐] S3.9 延迟拆解三档画质（办公 ≤50ms）——✅ 模型层在位（五段拆解/P95/三次方差≤5%/三档参数化+自定义覆盖）；实测归档待引擎实机
  - [ ] S3.10 异常三场景 ×10 演练——⛔ 依赖 S1.2（三场景前端语义已建模并测试：拉起中拔盘/VM 崩溃/就绪后拔盘）
  - [x] S3.11 设置页引擎组 UI——✅ 2026-09-21 验证关闭（双域已交付：开关默认关/自动休眠/画质三档/配额/延迟面板/能力申请/诚实声明全走 settings 总线；挂载点/默认值/负向 coerce/持久化四层核对通过）
  - [ ] S3.12 里程碑 M4 整合验收——⛔ 依赖 S3.1-S3.11 全绿
- **依赖**：S3.* 依赖自家 S1.2 与 AI-1 的 S1.3（引导项）；体验依赖 AI-5 的 S4.1（真机键鼠）与远期 GPU/网络。
- **DoD**：M1+M4 达成；Hyper-V 过渡声明双落点；零残留字节级校验 PASS。
- **交付物**：部署手册、差分链脚本、引擎编排验收报告、延迟基线。
- **禁区**：不改内核引导逻辑（AI-1）；不实现内核 VMX（只配合 AI-5 评估）；**部署目标盘三重确认**（盘符+容量+落点实证，绝不触内置盘一个字节）；引擎镜像与差分链挂载点严格限定 WIN_ENGINE 分区内，虚拟磁盘文件不落内置盘；ramcache 零残留门禁不许豁免。

## 4. AI-3 · 垫片与命令面线

- **职责一句话**：把"界面要能力的 554 个名字"翻译成内核服务的整套契约与账本。
- **管辖**：第 2 批 S2.01-S2.04。
- **任务清单**：
  - [ ] S2.01 垫片协议定版（单源再生/三段式/事件反向通道/变更流程）
  - [ ] S2.02 三色审计 554 方法（100% 归属+❌ 逐条排期）
  - [ ] S2.03 内核 KV 语义桥（断电 ×10 零丢失/命名空间隔离）
  - [ ] S2.04 vwmStore 持久化迁移（前端 diff 零改动）
- **依赖**：无上游（本批起点）；向下交付给 AI-4。
- **DoD**：协议文档+双侧生成物一致；三色占比 ≥30%（✅+🔶）公示；ktest 全绿。
- **交付物**：映射表（JSON 归档）、协议规范、语义差异公示清单。
- **禁区**：不碰渲染/输入（AI-4）；不 fork 前端；禁手改生成物。

## 5. AI-4 · 桌面承载线

- **职责一句话**：让 Variable 桌面真的在内核屏幕上亮起来、动起来、一模一样。
- **管辖**：第 2 批 S2.05-S2.12；R2 ushell 视觉对齐配合。
- **任务清单**：
  - [ ] S2.05 输入事件管道（焦点 ×1000 无串键/IME 组合期/延迟 <30ms 基线）
  - [ ] S2.06 渲染通路单窗全屏（逐屏对照/双分辨率/脏矩形）
  - [ ] S2.07 三件套闭环 —— 里程碑 M2
  - [ ] S2.08 BootScreen 对接（boot://event 同语义）
  - [ ] S2.09 多窗合成（×100 无泄漏/贴靠支持矩阵）
  - [ ] S2.10 降级提示 i18n（缺键即 fail 门禁）
  - [ ] S2.11 性能基线归档（三次方差 ≤5%）
  - [ ] S2.12 里程碑 M3 整合验收（全流程 ×5）
  - [ ] （并行）ushell 兜底桌面视觉对齐 Variable 契约
- **依赖**：消费 AI-3 的归属表与协议；S4.1 后补真机 USB 鼠标体验（AI-5）。
- **DoD**：M2+M3 达成；vitest 全绿证明前端零语义改动；体验五子项全过。
- **交付物**：三件套走查清单、对照截图集、性能基线、里程碑验收报告。
- **禁区**：不改协议单源（找 AI-3）；不动 Windows 侧 Tauri 本体。

## 6. AI-5 · 内核基建长线

- **职责一句话**：给全系统补"感官与四肢"——USB 键鼠、USB 存储、GPU、网络，及 VMX 评估。
- **管辖**：第 4 批 S4.1-S4.5（五件相互独立，可内部并行）。
- **任务清单**：
  - [ ] S4.1 xHCI HID（M1 寄存器→M2 环→M3 枚举→M4 HID，≈6-8 天；纯轮询 IMOD=0）
  - [ ] S4.2 xHCI MSC（新立项评估→BOT+SCSI 读为主写限 SHARED→直写落地→ESP 副本退役）
  - [ ] S4.3 GPU 加速（评估→POC→落地；帧率/功耗对比归档）
  - [ ] S4.4 网络栈（最小协议集；内核侧防火墙默认拒绝先行设计）
  - [ ] S4.5 内核 VMX 评估报告（只评估，拍板后另立项）
- **依赖**：向下服务 AI-2/AI-4；S4.2 与 AI-1 协作完成 ESP 副本退役切换。
- **DoD**：S4.1 真机 USB 键鼠进引导菜单与桌面；S4.2 真数据直读写；每件独立验收归档。
- **交付物**：各基建评估/设计/验收三段文档、探测脚本（_attic 范式）。
- **禁区**：每件立项前先出评估报告再动工；不影响 PS/2 既有通道（增量不替代）；**内置盘 NVMe 控制器永不初始化**（驱动枚举范围白名单限定 U 盘 xHCI 域）；S4.2 写路径必须过 vfsguard+SnapshotArea 快照+断电注入 ×10 复测三道闸；S4.3 GPU 以 IOMMU 评估为开工前置，无隔离不开工。

## 7. AI-6 · 质量与体验线（全程横切）

- **职责一句话**：全线的门禁执法官+体验检察官+档案管理员。
- **管辖**：所有批次的验收关口（不生产功能）。
- **任务清单**：
  - [ ] 门禁基线维护（四线单跑判绿；已知 flaky 清单看护）
  - [ ] 每里程碑六维验收+体验五子项执法（不过即打回）
  - [ ] 体验日志体系落地（挫败信号捕获→改进闭环）
  - [ ] 性能基准常态化（>10% 回归即红）
  - [ ] 灾备演练执法（掉电/强拔/半损坏 SOP 定期重放）
  - [ ] 验收档案库（docs/acceptance/ 全归档+索引）
  - [ ] 多机型 ≥3 台走查组织（M6 前完成）
  - [ ] 文档与作品集化（CHANGELOG/演示模式/对照截图集）
  - [ ] **实机安全审计**：1.8 SOP 上机前 7 项/下机后 4 项抽查执法；安全三问抽考；三红线违例一票打回
  - [ ] **Windows 回归验证常态化**：每里程碑验收含"进原生 Windows ×2"并单列签名；M1 关卡组织连续 10 次交替引导演练
- **DoD**：每个里程碑的验收报告有 AI-6 签发；档案库可检索。
- **交付物**：验收报告、基准数据、体验改进清单、档案索引。
- **禁区**：不直接改功能代码（发现缺陷开单给对应 AI）。

---

## 8. 多会话并行纪律（全体 AI 共同遵守）

1. 动手前 `git status` 盘点，认领自己路径，他人的未提交工作视而不见更不 stash。
2. 提交只 add 自己的显式路径；commit 信息规范（feat/fix/docs + 范围 + 要点）。
3. push 前 fetch；远端有新提交先 `pull --rebase`（有未暂存改动被挡时改走 gh_api_push 通道）。
4. 验收产物一律 `docs/acceptance/<域>-<主题>-<日期>.md`；过程探针脚本入 `_attic/`。
5. 遇阻断级发现（如 L0 类缺口）：停下登记方案矩阵，等拍板，不自行绕过。
6. 内核戒律全适用（>64KB 不上栈、Edit 回读、并行互踩清单、部署三证）。
7. **触实机一律走《实施总步骤图》1.8 实机操作 SOP**：上机前 7 项不齐=不上机；上机中单步推进+落点实证；下机后 4 项核验+归档；物理逃生路径（拔盘）全程常开。

---

## 9. 总进度完成图（里程碑 × AI）

| 里程碑 | AI-1 | AI-2 | AI-3 | AI-4 | AI-5 | AI-6 | 状态 |
|---|---|---|---|---|---|---|---|
| M0 起航 | S0.1-S0.4 | — | — | — | — | 门禁基线 | ◐ 进行中（S0.1✅ S0.2✅ S0.3 已刷盘 S0.4 键已置 0；剩真机 F12 引导验证+双重启复核两项用户到场动作） |
| M1 U盘双系统 | S1.3-5 | S1.1-2 | — | — | — | 走查执法 | ☐ |
| M2 桌面点亮 | — | — | S2.01-04 | S2.05-07 | — | M2 验收 | ☐ |
| M3 内核版成型 | — | — | 收尾 | S2.08-12 | — | M3 验收 | ☐ |
| M4 引擎可用 | — | S3.1-12 | — | — | — | M4 验收 | ◐ 进行中（S3.1 脚本+Mock×10✅、S3.2 底座+单测✅、S3.7 协议闭环✅、S3.11 验证关闭✅；S3.3-S3.6/S3.8/S3.10/S3.12 依赖 S1.2 镜像；S1.1 顺序门 FAIL 待拍板） |
| M5 基建齐 | 副本退役 | — | — | 体验升级 | S4.1-4.2 | 归档 | ☐ |
| M6 终态作品 | 四卡演进 | 收尾 | — | — | S4.3-4.5 | ≥3机走查+作品集 | ☐ |

> 状态图例：☐ 未开始 / ◐ 进行中 / ✅ 完成归档。开工令下达后由各 AI 在自己的会话内维护本列，AI-6 每里程碑复核一次。
