# AI13 完成报告 — 性能与长跑组

> 日期：2026-09-08 ｜ 分工依据：[ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md](ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md)
> 口径：代码级交付 + 单测验证；真机长跑验收统一待 B-30 阶段。

## 交付范围（24 项）

### Z 线 — 健康与自适应
| 任务 | 交付 | 位置 |
|---|---|---|
| Z-57 安全模式启动 | 检测上次会话崩溃 → 建议/进入安全模式；白名单降级加载器清单 `SAFE_SKIPPED_LOADERS` | `src/system/perf/safeBoot.ts` |
| Z-58 UI 线程健康面板 | 线程卡顿采样（long task 记账）+ 设置页开关与实时状态 | `src/system/perf/threadHealth.ts` + `PerfTab.tsx` |
| Z-59 空闲渲染冻结 | document.hidden / 空闲期暂停高频渲染循环，恢复即续跑 | `src/system/perf/idleFreeze.ts` |
| Z-60 内存压力自适应 | 堆/内存采样 → L0…L3 四档分级，每档动作表（降动画/清缓存/ shed 事件） | `src/system/perf/memPressure.ts` |
| Z-61 增量更新通道 | delta 预算校验 + 回滚标记（`deltaBudgetOk`/`markRollback`），通道切换持久化 | `src/system/perf/updateChannel.ts` |
| Z-62 本地使用统计 | 动作计数 top-N / 每日汇总 / 应用时长，纯本地落 localStorage，可一键清空 | `src/system/perf/usageStats.ts` |
| Z-63 回归基线扩展 | boot 基线纳入 bench2 六指标（boot/heap/long task/GC/IO/风暴） | `tools/bench2.cjs` |
| Z-64 启动加速流水线 | P0=阻断桌面 / P1=可延迟 / P2=托盘化 三级阶段标记 + 可延迟清单 | `src/system/perf/bootTimeline.ts` + 后端 `perf_boot_stage` |
| Z-65 崩溃叙事 | 会话叙事环形缓冲（最近动作），崩溃后下次启动生成可读叙事 | `src/system/perf/sessionNarrative.ts` |

### U 线 — 性能
| 任务 | 交付 | 位置 |
|---|---|---|
| U-19 启动加速 | 阶段时间戳落后端（M-52 快照数据源）+ 剧场 ready 提前判定依据 | `bootTimeline.ts` + `perf.rs` |
| U-20 内存守护 | 后台 5s 采样 + 历史环形（容量上限）+ 泄漏看门狗阈值 | `src-tauri/src/shell/perf.rs` `spawn_mem_warden` |
| U-21 帧预算治理 | 帧预算记账（chargeFrame/avgCost）→ 超预算自动降档（downgradeTier） | `src/system/perf/framebudget.ts` |
| U-22 IO 治理 | 拷贝任务管理：进度/暂停/恢复/取消，四命令全链路 | `perf.rs` `perf_io_*` |
| U-23 会话接力（崩溃叙事前端侧） | 叙事快照 + `previousSessionCrashed()` 判定 | `sessionNarrative.ts` |
| U-24 Bench 2.0 | 六指标 bench + 性能门（回归即红）；soak 长跑档（8h +100MB 泄漏阈值） | `tools/bench2.cjs` |

### N 线 — 长跑
| 任务 | 交付 | 位置 |
|---|---|---|
| N-35 多实例分身沙箱 | 命名实例锁 + 心跳 + 桌面接管互斥 + 焚毁（`instance_create/delete/heartbeat/list`） | `perf.rs` |
| N-36 跨设备接力 | 文件载体导出/导入（sha256 校验 + 清单）；导入清单诚实 best-effort | `perf.rs` `perf_relay_export/import` |
| M-46 日志轮转配额 | 活跃日志 >5MB 轮转；归档 >7 份或总量 >50MB 删最旧 | `perf.rs` `rotate_logs` |
| M-47 设置迁移预检 | dry-run 未知键/损坏值清单（JSON 截断/空值/伪 JSON 全拦截），不落库 | `perf.rs` `preflight` |
| M-48 DB 紧凑会话 | 执行前强制快照 + VACUUM + auto_vacuum=INCREMENTAL 一次性迁移 | `perf.rs` `perf_db_compact` |
| M-49 图标缓存 LRU | 字节预算 LRU（last_used 淘汰，单批 ≤32）+ 命中率统计 | `src/system/perf/lruCache.ts` |
| M-50 事件风暴削峰 | 三档声明式频道 merge/latest/queue + 收敛统计（1000 事件/秒最新必达 100%） | `src/system/perf/eventShed.ts` |
| M-51 性能门 | bench2 判红即门禁失败；boot-diff 阶段回归 >10% 判红并给归因建议 | `tools/bench2.cjs` + `tools/boot-diff.cjs` |
| M-52 冷启动 A/B 对照 | `boot-<version>.json` 快照 + `boot-diff` 对照工具 | `bootTimeline.ts` + `tools/boot-diff.cjs` |
| M-53 崩溃转储管线 | 崩溃钩子落 .dmp + 保留配额 + 列表命令 | `perf.rs` `install_crash_hook`/`crash_retain` |
| M-54 CPU 配额 | 按进程设置优先级/配额（后台降权） | `perf.rs` `perf_cpu_quota_set` |

### 前端面板
- **PerfTab 设置页**（[PerfTab.tsx](../src/features/settings/PerfTab.tsx)）：Z-58 健康采集 / Z-59 空闲冻结 / Z-60 内存压力 / Z-61 更新通道 / Z-62 使用统计 / M-46 日志轮转 / M-48 DB 紧凑 / M-53 崩溃转储 / N-35 实例列表，全部接 `ipc.ts` 22 条 perf 命令。
- i18n zh/en 双语补齐（`pf*` 键位对齐）。

## 后端命令注册（22 条）
`perf_mem_snapshot` / `perf_mem_warden_status` / `perf_io_copy|pause|resume|cancel|progress` / `perf_log_usage|rotate` / `perf_settings_preflight` / `perf_db_compact` / `perf_instance_list|create|delete|heartbeat` / `perf_relay_export|import` / `perf_cpu_quota_set` / `perf_crash_dumps` / `perf_boot_stage|stages`

## 自检结果
| 项 | 结果 |
|---|---|
| cargo test（perf::，隔离 worktree HEAD+AI13） | **9/9 通过** |
| vitest `src/system/perf/__tests__/perf.test.ts` | **25/25 通过** |
| tsc --noEmit（AI13 范围：perf/* / PerfTab / ipc / settings / dictionaries） | **0 错误**（仅存错误均在 AI-2 在途文件 src/system/windows/*，非本组） |
| bench2 冒烟 | latest 必达 100%、IO 64MB 写读通过 |
| boot-diff 冒烟 | 回归判定 + 归因建议输出正确 |

> 注：主工作区另有其他 AI 在途半成品（envedit/workshop/assocguard 等未跟踪文件 50 处编译错误），为不触碰他路任务，Rust 自检在 `HEAD+仅AI13 增量` 的隔离 worktree 完成（`git worktree`），逐字节未改他人文件。

## 边界与诚实声明
- soak 8h 长跑为脚本档位，句柄计数需真机侧采集（报告内标注）；
- M-54 CPU 配额在 Windows 侧以优先级类实现（非硬性 cgroup 式配额）；
- Z-59 空闲冻结作用于前端渲染循环，后端线程不受其管辖；
- N-36 接力导入为 best-effort 清单（应用不强制重放全部窗口布局）。
