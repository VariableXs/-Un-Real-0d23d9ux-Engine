# AI-16 完成报告 — 启动与声音通知组

**范围**：Z-43…Z-49、U-01…U-06、U-51、U-52、N-32（共 16 项，见《AI分工图》§AI-16）。
**纪律**：只做本组任务；进度真实性铁律、bootSoundMode 尊重静音、全本地零网络均遵守。

## 一、启动剧场六件套（U-01…U-06）

| 项 | 交付 | 位置 |
|---|---|---|
| U-01 启动剧场 | 真实事件驱动（boot://event/replay），无预设时间线；Esc/空格 <30% 拒绝跳过 | `src/system/boot/BootScreen.tsx` |
| U-02 字标资产 | BootWordmark 八字母 VARIABLE 描边 + glyph 资产（含单测） | `src/system/boot/BootWordmark.tsx`、`src/assets/wordmark/` |
| U-03 胶囊进度条 | CapsuleBar 进度胶囊（真实进度映射） | `src/system/boot/CapsuleBar.tsx` |
| U-04 文件流带 | FileTicker 当前任务/文件名滚动 | `src/system/boot/FileTicker.tsx` |
| U-05 启动交响 | Web Audio 三层（低频铺底 + 30%/80% 节拍脉冲 + 就绪双音）；full/chime-only/mute 三档，静音永远优先 | `src/lib/sounds.ts` `startBootSymphony` |
| U-06 仪式编排器 | pacingTimings(cinematic/brisk/instant) 驱动阶段机（纯 reducer，可单测） | `src/system/boot/ceremony.ts` |

## 二、声音与通知（Z-43…Z-49）

| 项 | 交付 | 位置 |
|---|---|---|
| Z-43 逐应用音量记忆 | volmem.json 持久化 + volmem_sync 套回会话（复用 audioime mixer，不抢系统权威）；4 命令 | `src-tauri/src/shell/soundnotify.rs` |
| Z-44 勿扰日程 | 纯函数（跨午夜窗口/手动优先/诚实降级）+ NotifyRuntime 每分钟回写 + 排队回放 | `src/lib/dndSchedule.ts`、`src/system/notify/NotifyRuntime.tsx` |
| Z-45 系统声音方案 | 事件白名单 + 数值范围校验（无效即拒）；前端三主题（玻璃/木质/暗夜） | `soundnotify.rs` `sound_scheme_validate`、`src/lib/sounds.ts` `SOUND_THEMES` |
| Z-46 音频设备快切 | eCommunications 角色切换（PolicyConfig COM 槽位 13）；console 角色沿用 hardware.rs | `soundnotify.rs` `audio_set_default_comm` |
| Z-47 通知存档与搜索 | SQLite（notify-archive.db）全文 LIKE + 按应用筛选 + 保留策略（30/90/永久）到期清理；pushNotify 自动入档 | `soundnotify.rs` 5 命令、`notifyStore.ts` |
| Z-48 麦克风使用指示 | ConsentStore 同源只读（hardware::privacy_usage 过滤），仅指示不拦截 | `mic_usage_state` |
| Z-49 提醒中心 | reminders.json 持久化 + 系统时钟锚定运行时（1s 粒度、重启补发、重复推进 next_due）+ 前端提醒中心 UI | `soundnotify.rs`、`SoundNotifyTab.tsx` |

## 三、通知交互进化（U-51 / U-52 / N-32）

- **U-51**：操作通知（actions）/ 进度通知（progress + completeNotifyProgress）/ 持久组（groupId）；勿扰排队礼仪（action > progress > normal 有序回放 + 汇总卡片）。
- **U-52**：SOUND_THEMES 三主题 × 全事件调参 + 深夜（22:00–6:00）×0.5 自动减半（可关）。
- **N-32**：本地学习表（划掉/点开 14 天滚动窗口）→ 即时/摘要/静默三堆；冷启动 ≥8 条才分层；结论透明呈现 + 用户覆盖优先。

## 四、设置页与接线

- 新增设置页「声音与通知」标签（`src/features/settings/SoundNotifyTab.tsx`，SettingsModal 注册 `sndnotify`）。
- IPC 绑定 17 命令（`src/lib/ipc.ts` AI-16 段）；lib.rs 注册 17 命令 + `spawn_reminder_runtime` 启动即挂。
- i18n zh/en 各 70 键（sn* 前缀）；zh-TW 走 s2t 自动转换。
- `NotifyRuntime` 挂载于 DesktopShell（零 UI：reminder://due → 通知中心、日程勿扰分钟级刷新、保留策略清理）。

## 五、自检结果

- `npx vitest run`：dndSchedule 11/11、notifySmart 12/12、ceremony 5/5 全绿（本组 28/28）。
- `cargo test soundnotify`：5/5 通过；`cargo test` 编译全绿（lib.rs 注册有效）。
- `npx tsc --noEmit`：本组文件零错误（仅存其他组遗留错误，见「已知边界」）。
- 并发回滚修复：BootScreen symphony 三 ref、Settings 接口字段、soundnotify mod 声明、notifyStore require()→静态 import 均已恢复验证。

## 六、已知边界（如实声明）

1. Z-46 通信设备切换仅 Windows（非 Windows 如实报错）。
2. Z-47 存档搜索为 LIKE 全文（非 FTS5 分词），千条量级实测足够。
3. Z-48 麦克风指示按需拉取（零轮询），Windows 隐私仪表板同源，某些驱动级占用可能不显示。
4. N-32 学习数据存 localStorage（14 天窗口），跨设备不迁移（隐私优先）。
5. 仓库其余组（AI-18 ambience、AI-19 pseudoLocalize、AI-15 OpenToolsTab）存在遗留 tsc 错误，属各组领地，本组未越界代修（仅恢复被回滚破坏的 settings.ts ambience 接口字段以解阻塞）。

## 七、红线复核

- 进度 100% 只来自后端真实事件 ✓；剧场组件无 emoji/裸色值 ✓；bootSoundMode=mute 或系统静音时绝不发声 ✓；全模块零网络零遥测 ✓；错误如实上抛（AppError）✓。
