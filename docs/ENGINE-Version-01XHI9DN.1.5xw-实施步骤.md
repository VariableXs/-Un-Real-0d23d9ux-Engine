# ENGINE Version 01XHI9DN.1.5xw — 实施步骤总手册（五计划合一）

> 本手册由五份规划的实施步骤**原文合编**而成（每部分为一份原文全量，一字未改），覆盖 355 项功能与全部公共基座批的施工步骤、风险登记、DoD 与回滚策略。功能定义见《ENGINE-Version-01XHI9DN.1.5xw-功能全景.md》，二十路分工见《ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md》。

---

## 0. 整合说明与施工总排布

### 0.1 合并原则

1. **原文保留**：五份实施步骤全文逐字保留（各部分标题即原文标题）；本节仅为导读，不替代任何原文纪律。
2. **施工顺序**：五计划按立项递进关系排布为五个阶段；其中化境计划声明可与 SUMMIT-90 / APEX-70 并行错峰施工（其 0.4 并行兼容协议原文保留在第五部分）。
3. **公共纪律适用口径**：各计划的验证基线、共享文件守则、DoD 以各自原文为准；后阶段计划显式继承前阶段纪律（如 SUMMIT/化境沿用 project_memory 全部教训），合编后全部继续有效。

### 0.2 五阶段总排布

| 阶段 | 计划（代号） | 批次体系 | 功能数 | 关键路径 |
|---|---|---|---|---|
| 阶段一 | NEXT-40（第二曲线） | Wave 0 公共基座（P-1…P-4）→ Wave 1…4 → Wave 5 收口（B-31） | 35 | P-1 命令注册表 / P-2 动效编排器先行；N-05 场景最后收口；N-35 分身最高风险 |
| 阶段二 | ASCENT-60（登顶计划） | AB-0 地基令牌 → AB-1 启动剧场 → … → AB-12 收口门禁 | 60 | AB-1 启动剧场为心脏；boot phase 协议先冻结 |
| 阶段三 | APEX-70（工匠计划） | PB-0 地基（Z-08 键位注册表 / Z-01 图标规格）→ PB-1…PB-10 | 70 | PB-0 双关键路径首周收口；键位注册表为全项目公共地 Base |
| 阶段四 | SUMMIT-90（极顶计划） | P0 先行批 → P1 主体批 → P2 纵深批（三波次门禁） | 90 | P0 十二项全绿才进 P1；soak ≥6/7 才进 P2 |
| 阶段五 | 化境计划（Version 1.2sein298h7mx.00wix） | P0 立项核查 → P1 地基 → P2…P6 主体 → P7 收官（八波次） | 100 | P1 地基波最先稳定；可与阶段三/四并行错峰 |

### 0.3 阅读顺序

1. 先读本节总排布，建立五阶段全景；
2. 按阶段顺序进入各部分原文：第一部分 NEXT-40 → 第二部分 ASCENT-60 → 第三部分 APEX-70 → 第四部分 SUMMIT-90 → 第五部分化境计划；
3. 任一阶段开工前，先读该部分第 0 节的施工总纪律/总则原文，再按批次推进。

---

## 第一部分：NEXT-40（第二曲线）实施步骤 · 原文全量

# NEXT-40 实施步骤：四十项升级的全量施工手册

> 版本：v1.1 ｜ 日期：2026-09-08（v1.1：随功能全景 v1.1 移除 N-34 与域 H N-37…N-40 施工批次）
> 性质：**施工文档**——回答「怎么做、先做哪步、怎么验证、失败怎么回滚」。
> 功能定义见 `NEXT-40-功能全景.md`；分工调度见 `NEXT-40-AI分工图.md`。
> 总则继承 [AI五路分工计划.md](AI五路分工计划.md) 第 0 节与主计划 25.1「批次自检十步」、25.2「十二条」、25.3「五军规」，本文不再重复，仅列增量纪律。

---

## 第一部分：全局增量纪律（NEXT-40 专属）

1. **命令注册纪律**：每个 N-* 功能收口前，必须把其全部用户可执行动作注册进 N-13 命令注册表（`src/lib/commands/registry.ts`），未注册不算完成——这是「40 个功能缝合为一个整体」的结构性保证。
2. **动效单源纪律**：NEXT-40 期间新增的一切批量/编排动效必须走 N-06 编排器（`src/lib/motion/`），禁止新增裸 CSS animation 编排场景；存量动效按域分批迁移，visual-audit 基线只允许在迁移批次重拍。
3. **数据分片纪律**：一切新增时序数据（快照/洞察/剪贴板/设备日记/焚毁日志）统一格式：`data/<domain>/YYYYMMDD-NN.<ext>`，zstd 压缩、纯追加、按天分片、FIFO 淘汰、上限可查（设置→存储页显示各域占用）。
4. **降级测试纪律**：每个声称「如实降级」的功能，其测试计划必须包含一条**降级注入用例**（模拟 USN 无权限/SMART 读不到/GPU 计数器缺失/语言包缺失），断言 UI 显示降级态而非假数据。
5. **安全分叉不可放宽纪律**：L4 让位、密码框判停、沙箱越权、签名校验、接管互斥五处为硬编码安全分叉；任何 N-03 规则、N-18 宏、N-26 插件、N-28 scope 均不得触碰——code review 门禁项。
6. **性能预算纪律**：后台常驻能力逐项预算——索引 ≤3% CPU、洞察采集 ≤1%、HUD 自身 ≤0.5%、组件刷新空闲 ≥30s 间隔；超预算自动降级并在 N-31 洞察中如实记录。
7. **共享文件流程**：`src/lib/ipc.ts`、`src/i18n/dictionaries.ts`、`src-tauri/src/lib.rs`、`vwm.ts` 等共享文件，修改前 `rg` 核实现状、最小化编辑、改后立即跑 `tools/audit.cjs` 与 `tsc --noEmit`——沿用 project_memory 全部教训。

---

## 第二部分：公共基座（Wave 0，先于一切功能）

任何 N-* 功能开工前，四个公共基座必须先落地，否则多个功能会重复造轮子：

### P-1 命令注册表骨架（N-13 的先行切片）

1. 新建 `src/lib/commands/types.ts`（Command 声明类型）与 `src/lib/commands/registry.ts`（注册/查询/权重排序内核）；
2. 把开始菜单现有「启动应用」与设置页「打开设置」两个动作迁移进注册表作为首批命令（验证管道）；
3. 模糊匹配内核：倒排索引 + 拼音首字母（zh 用户高频）+ 频率/新近度加权字段（先留空接口，N-13 本体接数据）；
4. 单测：注册/查询/权重/条件可见性四组；`tools/audit.cjs` 增加「命令 id 与 i18n key 对齐」检查。
5. **验收**：注册表管道 tsc/vitest/audit 三绿；后续功能从此只做「注册 + 实现 action」。

### P-2 动效编排器内核（N-06 的先行切片）

1. 新建 `src/lib/motion/orchestrator.ts`（rAF 批调度 + 30ms 默认错峰）、`flip.ts`（First/Last 量测与反演）、`spring.ts`（把既有 vwm spring keyframes 参数化为 JS 模型）；
2. reduce-motion/高对比度下 80ms 直落（读 A-2 既有根属性）；
3. 帧预算守卫：单帧超 8ms 自动放弃剩余动效直落位，事件上报（bench 可见）；
4. 用一个最小改造试点验证：desktop-icons 网格重排走 FLIP（此为第一个迁移样板，重拍该域 visual-audit 基线）。
5. **验收**：编排器单测（错峰/降级/帧预算三组）；试点动效 60fps；reduce-motion 全绿。

### P-3 数据分片与淘汰框架

1. 新建 Rust `src-tauri/src/shell/timeline_store.rs`：分片写入（zstd）、按天滚动、FIFO 淘汰、容量上限回调、损坏分片隔离（单片损坏不影响整读）；
2. IPC：`StoreAppend/StoreList/StoreRead/StoreStats/StorePurge` 五命令进 `ipc.ts` 契约（`rg` 先核对再插入）；
3. 契约测试进 `commands_e2e.rs`；损坏注入用例（截断/乱码分片）必须优雅跳过。
4. **验收**：契约测试绿；损坏注入不 panic；淘汰按 LRU 正确。

### P-4 权限与账本框架（为域 E/F 铺路）

1. 扩展 `netconsent.rs` 为通用「能力同意账本」：网络访问之外，登记 API 网关连接、远程源访问、接力传输三类条目（时间/对端/类别/大小）；
2. 账本查询页骨架进设置→隐私（先只展示既有网络条目）；
3. **验收**：账本写入/查询契约测试；账本自身永不外传（取证扫描）。

---

## 第三部分：分批施工步骤（按 Wave 顺序）

> 每批八步法：**①契约冻结 → ②数据结构 → ③后端命令 → ④IPC 绑定 → ⑤前端 UI → ⑥命令注册 → ⑦测试三绿（tsc/vitest/cargo test --no-run）→ ⑧验收归档**。
> 下列步骤均按此八步展开；篇幅所限，⑤⑥⑦⑧按域合并表述，①-④逐批展开。

### Wave 1：窗口路与体验路主力（N-01…N-12）

#### N-01 窗口时间机器

1. 契约：`TimelineSnap { ts, scene?, vwm: Vec<VwmWin>, embed: Vec<EmbedWin> }`；`VwmWin { id, x, y, w, h, z, focus, snap, group }`；`EmbedWin { pid, exe_path, hwnd, rect, tier_hint }`；
2. 落盘走 P-3 框架（`data/timeline/`）；触发器挂 `winman.rs` 布局变化事件（去抖 8s）与 VWM store 变更；
3. 后端 `shell/timeline.rs`：`snapshot_now / snapshot_manual / list / load / restore`；restore 三级策略（精确/尽力/诚实报告）——尽力级重启复用 `launcher.rs` 登记项，**tier 必须现场复跑 compat_probe 而非读快照**（写死注释）；
4. IPC 五命令 + 前端时间轴浮层（横向时间带，缩略图用 `capture.rs` 异步批量抓，LRU 缓存）；
5. 验收用例按功能文档四条；bench：快照写入 p95 < 30ms、时间轴滚动 60fps。

#### N-02 舞台管理器

1. 契约：`StageGroup { id, name, members: Vec<winId> }`；持久化 `data/stages.json`（原子写 + 备份）；
2. 纯前端：VWM store 增加 groups 维度；侧幕 UI（气泡列）+ 上台/入组/轮换手势与 `Ctrl+Shift+←/→`；
3. 动效全部走 P-2（上台复用 spring 模型，收拢走 squash）；侧幕热区与贴靠热区错开 24px（常量进 tokens）；
4. 命令注册：「创建组/切换组/解散组」；
5. 验收：重启持久化、60fps bench、贴靠共存、键盘可达。

#### N-03 窗口规则引擎

1. 契约：`Rule { id, trigger, cond, actions, priority, enabled }`（JSON Schema 随文档发布）；
2. 后端 `shell/rules.rs`：规则装载/校验（行级错误报告）/裁决（优先级+specificity）/执行日志环形缓冲 500 条；
3. 执行适配层：动作映射到既有 IPC（snap.applySnap / embed 策略层 / stage 入组）——**不新增任何绕过安全审查的执行路径**；L4 与管理员窗口的动作白名单硬编码在适配层；
4. 前端规则中心（列表+编辑器+实时日志页）；12 条内置模板；
5. 验收：触发 < 500ms、导入合并、L4 不可放宽渗透用例、语法错误行级报错。

#### N-04 任意窗口画中画

1. 后端扩展 `embed.rs`：`pip_enter(hwnd, opts) / pip_exit(hwnd) / pip_list`；PiP 样式（置顶/透明/角落磁吸 8px）在 winman 侧实现；
2. 尺寸/透明度档位记忆 `data/pip_memory.json`（按 exe 记忆）；L4 禁入 PiP（复用 C-6 判定结果）；
3. 前端：红绿灯菜单入口 + `Ctrl+Alt+P`（经 N-17 注册表）+ 托盘聚合图标；
4. 失败路径：detach 抖动→回原位+toast，绝不卡中间态（沿 embed.rs 显式分臂教训）；
5. 验收：5 类样本应用矩阵（真机）、往返无残留、宿主退出回收通知。

#### N-05 工作区场景

1. 契约：`Scene { id, name, visual(DRESS 字段), orchestration{ snapshot_ref?, stage_ref? }, behavior{ dnd, sound, ime }, triggers[] }`；`data/scenes/*.vscene`；
2. 后端 `shell/scenes.rs`：场景 CRUD / 切换编排（视觉包→设置管道；编排包→N-01/N-02 执行器；行为包→通知/声音设置）+ 全量回滚（任一包失败回滚整套并报错）；
3. 触发器：定时（复用 sysmaint 调度内核）/上下文（shell_watch 前台事件→toast 建议非强制）；
4. 前端：场景管理页 + 转场幕（240ms 极光，走 P-2）+ 未保存窗口守卫（标题特征匹配→确认门）；
5. 验收：三触发型全链路、往返 100 次无漂移、守卫生效。

#### N-06 动效编排系统（本体收口）

1. P-2 内核之上补齐：动效实验室 UI（全域 spring 参数可视化调节+实时预览）、`.vmotion` 档案导出/导入；
2. 迁移第一批存量：VWM 开合/最小化飞行（vwm.css keyframes 参数化进 spring 模型）、通知中心进出场、开始菜单展开——**一次只迁一个域**，每域迁移后重拍该域 visual-audit 基线；
3. lint：新增 `tools/motion-lint.cjs` 检查新增编排场景禁用裸 animation（存量白名单制）；
4. 验收：三模式 bench、reduce-motion ≤80ms、档案重放一致、基线 diff < 0.5%。

#### N-07 壁纸工坊 2.0

1. 后端扩展 `wallpaper.rs`：`.vwp` 包解析（manifest 校验/资源解包/拒绝远程 URL）、四引擎会话管理（Video 硬解、Shader 编译缓存、Web 本地沙箱、Generative 参数化预设——现有星空/极光渲染器迁入）；
2. 降级链：shader 编译失败→静态帧+提示；全屏→10fps；电池模式（N-20 信号，先留接口默认不降）→暂停视频；
3. 前端工坊（左素材库/右预览/底 uniform 滑杆 per-preset 声明）；导入导出往返；
4. 命令注册：切换壁纸/暂停引擎；
5. 验收：四引擎预览/应用/持久化、坏包优雅降级、打字降级联动。

#### N-08 主题工坊

1. 前端为主：`src/features/settings/theme-studio/`（三区编辑器+AA 对比度实时校验+三变体存档）；
2. `.vtheme` 导入导出（版本校验+冲突字段报告）；预览渲染复用真实组件缩样；
3. 裸色迁移第一批：desktop.css 与 tools.css 的语义位（每批 audit 裸色计数只降不升）；
4. 验收：往返一致、AA 红牌门、高对比变体随系统切换、audit 计数下降。

#### N-09 桌面小组件系统

1. 契约：`widget-manifest.json`（尺寸档/权限/刷新策略）+ 宿主容器（桌面常驻网格吸附 + 组件板侧滑）；
2. 沙箱：复用 N-07 Web 引擎沙箱技术；数据桥=白名单 IPC（性能只读/待办读写/搜索只读三白名单先行）；
3. 内置 8 组件逐个落地（世界时钟→日历→待办→性能迷你条→快捷启动→倒计时→网络→灵感卡）；失败 3 次自动收起+通知；
4. 生命周期联动接口：全屏暂停/低电暂停（先留事件位）；
5. 验收：8 组件全功能、越权 IPC 渗透用例、布局持久化。

#### N-10 锁屏与仪式屏

1. 后端 `shell/lockscreen.rs`：口令哈希（argon2）存 `data/lock.dat`、恢复码（OOBE 集成补生成流程）、错误 5 次指数退避；
2. 前端全屏锁层（N-07 引擎背景+时钟+通知来源计数不显内容）；`Win+L` 注册失败→托盘降级入口；
3. 双角色（离开锁/仪式锁）+ 专注倒计时（与 N-05 场景联动接口）；解锁动效走 P-2；
4. 环境隐藏（双 Esc）时锁屏失效的联动挂到既有环境切换事件；
5. 验收：1000 次锁解、零内容泄漏、退避、恢复码、失效联动。

#### N-11 实况场景组件

1. 粒子层：独立无边框透明窗口（WS_EX_TRANSPARENT+点击穿透切换），canvas 自绘四类天气粒子；无 GPU 自动关闭粒子只留色温；
2. 昼夜引擎：本地时区计算日出日落（经纬度可选手填），全局色温四段渐变（作用于壁纸引擎 uniform 与主题色温层——与 N-08 变体系统对接）；
3. 天气源：默认手工模式；授权模式走 netconsent（P-4 账本留痕）；
4. 季节轮转：四套 hue 偏移曲线只作用于 accent 语义 token；
5. 验收：穿透正确、离线完整、网络条目对账、高对比度下色温禁用。

#### N-12 图标包系统

1. 后端 `shell/iconpack.rs`：`.vicon` 解析、图标缓存（分批渐进替换节流）、优先级链（用户包>内置>原生提取）、缺键回退计数；
2. 作用面挂接五处（desktop-icons/launcher/taskbar/explorer 类型图标/recycle 两态）+ 四空间入口变体；
3. 前端：包管理页+安装前 12 宫格对比预览+「从系统图标导出包骨架」工具；
4. 验收：往返/缺键回退/四空间变体/千图标换包无冻结（帧耗时采样）。

### Wave 2：效率路 + 系统路（N-13…N-25）

#### N-13 命令面板（本体收口）

1. P-1 骨架上补：浮层 UI（居中、三类一框、`>`/`?` 前缀切换）、学习排序接数据（本地频次+新近度）、钉选、链式参数模式；
2. 全域命令大迁移：设置项/开始菜单动作/快捷面板开关/文件管理器动作全量注册（目标 ≥120 条）；
3. 全局热键 `Ctrl+K` 注册+冲突降级；p95 < 50ms（倒排索引基准测试进 bench）；
4. 验收：四条全过+学习排序命中率用例。

#### N-14 统一搜索 2.0

1. 后端 `shell/usn_index.rs`：USN Journal 增量索引（MFT 读权限探测→不可用降级定时扫描并标注）；索引分片落 `data/index/`；排除目录黑名单；**保险箱路径硬编码排除**；
2. 聚合五类（应用/文件/内容/命令/设置），类别权重+Tab 锁定；高级语法解析器（ext/size/modified 子句）；
3. 预览：文本前 3 行/图片缩略/音频时长（复用 capture 与 explorer 既有能力）；行内快捷动作三键；
4. 挂接 N-13（搜索框 `?`/命令入口「搜索一切」）；
5. 验收：百万文件 p95 < 200ms、保险箱零泄漏渗透、降级诚实。

#### N-15 剪贴板历史中心

1. 后端 `shell/cliphist.rs`：序列号轮询（300ms 去抖）、四类型识别、16MB 图片上限、AES-GCM 分片（密钥=保险箱 KDF 派生独立子钥）；
2. 敏感名单：窗口标题/进程匹配→暂停记录（取证用例验证零记录）；「关闭即焚」会话模式；
3. 前端：`Ctrl+Shift+V` 浮层（网格卡+搜索+钉选+来源分组）；回贴=写剪贴板+SendInput 粘贴（**仅用户显式回车触发**，焦点漂移检测失败禁止粘贴）；
4. 定时自动清空挂既有 sysmaint 调度（复用既有焚毁体系）；验收四条照功能文档。

#### N-16 截图标注与贴图

1. 后端：全屏截图（DXGI 抓帧）、区域取框参数；OCR 走本地引擎（Windows.Media.Ocr 离线语言包优先，缺包给安装指引，绝不云 OCR）；
2. 标注层：矢量六工具（Mindmap 画布技术复用），马赛克破坏性操作确认门；
3. 贴图=PiP 容器装位图（复用 N-04 全套行为）；滚动截图能力探测（不可滚动窗口禁用该项）；
4. 流向四选一（复制/存盘/进记录库/贴图）；验收照功能文档。

#### N-17 快捷键中心

1. **契约先行**：热键注册表 `src/lib/keys/registry.ts` 单一事实源；先写 `commands_e2e.rs` 迁移契约测试（旧路径行为快照→新路径逐条对照），再逐域迁移 kbdhook 绑定；
2. 冲突检测：改键实时标红（内置常见三方占用表）+解决建议；Profile 档案 `.vkeys` 分享；
3. 学习模式（按键→角落功能卡）+ 速查表导出 PNG/PDF；
4. 验收：审计脚本验证零散落注册、改键回滚、学习模式全键覆盖、速查表零偏差。

#### N-18 自动化宏引擎

1. 后端 `shell/macro.rs`：动作库（N-13 命令执行器直连+SendInput 护栏+文件/剪贴板操作）、五类触发器（热键/进程事件/时间 cron 子集/剪贴板正则/场景切换）、录制回放（坐标→窗口相对化）；
2. 护栏首日落地：全局急停 `Ctrl+Esc` 长按 1s；UAC/密码框焦点智能判停；运行角标；
3. 双形态（可视化步骤/YAML 脚本）同源同步；`.vmacro` 分享导入敏感动作逐条确认；
4. 验收：五触发器、判停渗透、急停 100%、录制参数化回放。

#### N-19 性能 HUD

1. 后端扩展 `sysinfo.rs`/`hardware.rs`：PDH 计数器组、GPU 引擎计数器（探测失败隐藏该项）、前台 FPS 轻量采样（不可用隐藏）；
2. 前端三形态（贴边条/组件卡/全屏仪表），60s canvas 曲线（bench 绘图复用）；自身开销显示在尾部；
3. 采样自适应（可见 500ms/隐藏 5s/全屏游戏暂停）；告警线通知走勿扰管道；
4. 验收：对照任务管理器误差 < 5%、开销达标、降级隐藏。

#### N-20 电源与电池管家

1. 后端 `shell/power.rs`：powercfg 三档+自定义档（内部节流：组件刷新/壁纸引擎/索引降频三联动接口）；电池数据抽象层（厂商接口探测表，探测不到隐藏）；
2. 低电四级策略链（20 提示/15 省电场景+降级清单通知/10 自动保存+休眠提示）；台式机转功耗名义估算（标注口径）；
3. QuickPanel 面板位+月度健康报告（存记录库）；
4. 验收：三档生效、四连降级、无电池转型、月报生成。

#### N-21 网络指挥台

1. 后端扩展 `network.rs`：Wi-Fi 扫描/已存配置、rasdial VPN 管理（只管理不提供，UI 如实标注）、系统代理读写+方案档、显式测速（ping/下载/上传）、per-应用用量（WFP 探测，不可用降级总量并标注）；
2. **透明账本页**：P-4 账本的环境网络行为全量呈现（与 N-28/N-30/N-36 共用）；
3. 验收：三链路真机、账本逐条对账、测速仅显式、降级标注。

#### N-22 存储健康仪表盘

1. 后端 `shell/storage_health.rs`：NVMe/SATA SMART 读取（厂商属性解码表常见型号，读不到显示「不可读」）、综合判分红绿灯+异常通知（联动 sysmaint 备份提醒）；
2. 空间分析：低 IO 后台扫描（可暂停、24h 缓存）、treemap 钻取；大文件 Top100+重复文件（同名同大小→可选哈希确认），删除走回收站；
3. 便携盘视角挂 usb.rs 弹窗体系；
4. 验收：对照 CrystalDiskInfo、雷达误删防护（哈希确认门）、无权限降级。

#### N-23 设备与外设中心

1. 后端扩展 `usb.rs`/`hardware.rs`：WM_DEVICECHANGE 监听→结构化通知；五类设备卡数据源；音频输入输出切换动作（挂 QuickPanel 混音器）；
2. 设备日记 30 天（P-3 分片）；插拔历史页；耳机插入策略三态（问/自动/不动）；反复掉线检测建议；
3. 边界：不做驱动安装（链接设备管理器）；
4. 验收：五类真机核对、日记对账、策略三态、掉线检测样机。

#### N-24 预测性启动预热

1. 后端 `shell/predict.rs`：四维频次表（时间×星期×前台×场景）+拉普拉斯平滑；L1 预读/L2 预建缓存/L3 预启白名单（用户允许才开）；
2. 冷启动 3 天只学习不动作；5 分钟未用自动回收（预启进程优雅退出）；开机预热前 3 应用（默认 L1 可关）；
3. 反馈环数据进 N-31；设置页透明展示模型+一键清空；
4. 验收：白名单启动提速 ≥30%（bench 归档）、回收 100%、清空取证、冷启动零动作。

#### N-25 健康自愈中心

1. 后端 `shell/health.rs`：30 项自检树（每项 {名称/状态/证据/修复器}），启动后台跑、单项超时 3s 跳过标 UNKNOWN；FAIL→安全模式启动（最小环境：无嵌入/壁纸/组件/默认主题）；
2. 修复器绑定（重建索引/回滚配置/清事务残留/重导登记库），全部留痕可回滚+修复历史页；
3. 月度体检报告（交叉 N-22/N-19/N-31）存记录库；`variable doctor` CLI 无头化（N-29 前先用既有 `--repair` 扩展）；
4. 验收：注入故障逐项检出修复、FAIL→安全模式→修复→正常重启全链路、超时跳过。

### Wave 3：生态路 + 智能数据（N-26…N-33）

#### N-26 插件运行时 2.0

1. 契约：Manifest v2 Schema 冻结（权限↔入口一一对应表）；沙箱运行时（独立 isolate、IPC 桥逐命令权限校验、CPU/内存配额熔断）；
2. 生命周期：安装权限审查页→更新权限 diff 重确认→卸载 residue-check 级验证；崩溃隔离（坏插件只死自己）；
3. 四入口（命令进 N-13/面板页/小组件进 N-09/事件订阅按权限）；
4. 渗透用例集（越权 IPC/原型污染/资源耗尽/坏包）是批次门禁；开发者工具包文档+脚手架；
5. 验收：样例四入口、越权 100% 拒绝、崩溃隔离、卸载零残留。

#### N-27 插件市场

1. 后端 `shell/market.rs`：本地目录源扫描（默认形态）+可选远程 registry（用户手动加 URL、HTTPS+ed25519 签名清单、netconsent 留痕）；五品类（plugin/theme/wallpaper/iconpack/macro）统一 zip+manifest 家族；
2. 安装页权限 diff+签名状态（无签名醒目警示）；更新治理（版本对比/变更日志/一键更新回滚）；
3. 创作者侧：上架向导（本地已装导出为包）；签名密钥管理；
4. 验收：本地源五品类全链路、签名篡改 100% 拒绝、账本对账、关闭态抓包零网络。

#### N-28 开放 IPC API 网关

1. 后端 `shell/gateway.rs`：127.0.0.1 硬编码监听（默认关闭）、REST+WS 双通道、token 生成+首次连接授权页（类本地 OAuth）、scope 到命令级（对齐命令注册表）、QPS 限流、全调用入 P-4 账本；
2. API 面首批：窗口（枚举/贴靠/PiP/舞台）、场景（查询/切换）、搜索查询、通知（读态/发本地）、命令代执行（已授权 scope）；
3. SDK：TypeScript+Python 官方客户端（类型定义+文档）；
4. 验收：全 API 契约测试、越权 100% 拒绝、限流生效、默认态端口扫描零监听。

#### N-29 variable-cli

1. 依赖 N-28 在线族；离线族（doctor/pack/api）先行；命令族八组（win/scene/search/notify/plugin/doctor/macro/api）；
2. 通信：网关 token 连接运行中环境；表格/`--json` 双输出；退出码规范（0/1/2）；
3. 示例库 PowerShell/bash 各 10 例；与既有应急 CLI 帮助页统一三段式（应急|控制|生态）；
4. 验收：双态全命令测试、json schema 契约、退出码、示例可跑。

#### N-30 浏览器伴侣扩展

1. 扩展（manifest v3，127.0.0.1 权限+显式标签读取）：右键剪藏/投送/下载接管三功能；环境侧接收端经 N-28 网关；
2. 剪藏：净化 HTML→记录库指定文件夹（URL+时间随附）；投送→通知卡片点击系统浏览器打开；下载接管可开关→文件管理器自动刷新高亮；
3. 分发：先 sideload（开发者模式安装文档），商店提交为后续项；
4. 验收：剪藏往返一致、投送链路、接管可开关、断开账本零新条目。

#### N-31 本地使用洞察

1. 后端 `shell/insights.rs`：五类事件采集（前台时长/窗口切换/通知处理/场景驻留/搜索查询含未命中）→ P-3 分片（`data/insights/`，独立密钥，90 天滚动）；
2. 前端四页仪表盘（时间流河流图/专注报告/通知报告/搜索报告）+行动建议规则卡（一键执行）；
3. 焚毁：独立密钥销毁（复用焚毁体系）+无痕会话开关（期间零记录，取证扫描验证）；
4. 验收：对账、建议触发、焚毁零残留、无痕零增长。

#### N-32 智能通知整理

1. 数据：来源×时段×动作三元频次表（7 天冷启动全即时堆）；通知中心改三堆路由（即时/摘要/静默）；
2. 摘要堆定点两档（用户选时段）汇总卡+展开逐条；场景策略覆盖挂 N-05；
3. 透明页：每来源学习结论+依据展示，用户覆盖优先；
4. 验收：结论对账、三堆路由、覆盖优先、定点触发。

#### N-33 情绪引擎

1. 内核 `src/lib/mood/`：情绪向量（唤醒/专注）计算（时段先验+场景标签+前台类别+可选天气）→映射表（用户可编辑，±15% 包络硬校验）→驱动 N-07 uniform/N-08 色温/P-2 速度系数/声音音调；
2. 120s 缓动过渡；高对比度模式色温维度禁用；「此刻感受」滑杆手动校正；
3. 默认保守开启+一键暂停；验收四条照功能文档。

### Wave 4：容器路收口（N-35…N-36）

#### N-35 多环境分身

1. 后端改造（**根级，先写迁移方案评审**）：`single_instance.rs`→命名实例锁（`Variable-<name>` 全局互斥量+心跳双保险）；`envs.rs` 扩展 `instances.json` 清单；每实例独立 data 根/IPC 命名空间；
2. 接管态硬边界：至多一个实例接管桌面（互斥量仲裁，并发抢启动压测必须过）；其余实例普通窗口模式；
3. 分身管理器（创建/克隆=数据目录复制/删除=焚毁链路）；托盘切换=当前实例休眠（状态进 N-01 快照）→目标接管；
4. 验收：三分身 24h 无串扰（IPC/数据/热键独立）、并发抢启动压测、克隆完整、删除彻底。

#### N-36 跨设备接力

1. 后端 `shell/relay.rs`：mDNS/UDP 发现（默认关闭，双端开启才可见）、短码配对（ECDH→AES-GCM 会话）、点对点 TCP 断点续传；
2. 载荷打包：场景定义+快照可执行版+指定记录条目+可选剪贴板；目标端「尽力展开」（未安装应用诚实清单、记录冲突走既有导入合并）；
3. 双端账本（P-4）；关闭态零广播零监听（抓包用例）；
4. 验收：双机全链路、诚实清单、续传、账本对账、关闭态抓包。
---

## 第四部分：联调点与契约冻结（提前一个 Wave）

| 联调点 | 双方 | 契约内容 | 冻结时点 |
| --- | --- | --- | --- |
| P-2 动效 × 全域 | AI-5 / 全体 | 编排器 API（stagger/flip/spring 参数与降级语义） | Wave 0 末 |
| N-03 规则 × embed/snap | AI-2 内部 | 规则动作→既有 IPC 的适配层映射与 L4 不可放宽白名单 | Wave 1 中 |
| N-09 组件 × N-26 插件 | AI-5 / AI-4 | widget-manifest 与插件权限模型的合并字段 | Wave 1 末 |
| N-20 电池 × N-07/N-09 | AI-3 / AI-5 | 节流信号事件（低电/恢复）与引擎降级动作表 | Wave 2 中 |
| N-24 预热 × N-31 洞察 | AI-3 / AI-1 | 频次表数据格式与反馈环事件 | Wave 2 末 |
| N-28 网关 × N-29/N-30/N-36 | AI-4 内部 / AI-1 | token/scope 语义与 WS 事件频道命名 | Wave 3 前 |
| N-35 分身 × 全部数据域 | AI-1 / 全体 | 实例隔离边界清单（IPC 命名空间/data 根/热键集） | Wave 4 前 |

## 第五部分：全局收口（B-31 拟）

1. **总验收矩阵**：`docs/acceptance/b31-NEXT40验收矩阵.md`——35 项 × {代码完成/测试三绿/bench 归档/降级用例/真机验证} 五列；未实测不打 ✅ 口径不变；
2. **README 增章**：四十一章「NEXT-40：第二曲线能力」总览 + 各能力短节（诚实边界逐项写入，如锁屏不替代系统锁、VPN 只管理不提供、漏洞库离线包）；
3. **能力声明更新**：README 21.5 节诚实声明表新增条目（PiP 反作弊限制/接管态单实例/接力非程序迁移）；
4. **bench 汇总**：全部性能门（搜索 200ms/命令 50ms/HUD 0.5%）逐项归档 `docs/bench/`；
5. **审计与回归**：audit.cjs（i18n+命令 id 对齐+动效 lint）、residue-check、visual-audit 全绿；`cargo test --no-run` 与 vitest 全量通过；
6. **发布物**：NSIS/便携目录/1TB 胶囊三件套更新 NEXT-40 清单页。

## 第六部分：回滚策略（每批通用）

- 每批一个 git 分支，收口合入前必须 `cargo test --no-run`+vitest+audit+tsc 四绿；
- 涉及数据格式的新功能（快照/洞察/剪贴板/分身）**必须带版本号与迁移器**（旧版本读新数据→拒绝并提示升级，绝不半解析）；
- 根级改造（single_instance/热键注册表迁移）必须「新旧路径并存一个 Wave + 行为快照对照测试」后才删旧路径；
- 任何批次失败回滚后，在 `project_memory.md` 记录教训（沿用既有流程）。

---

*分工与调度（六路谁做哪批、Wave 时钟、并行策略）见 `NEXT-40-AI分工图.md`。*

---

## 第二部分：ASCENT-60（登顶计划）实施步骤 · 原文全量

# ASCENT-60 — 实施步骤（从批次到验收的完整施工手册）

**版本**：`1.1ascent.vxe`
**姊妹篇**：`ASCENT-60-功能全景.md`（做什么）、`ASCENT-60-AI分工图.md`（谁来做）。
**前置阅读**：本文假设读者已读功能全景的 60 项定义与附 A 依赖矩阵。

---

## 0. 施工总纪律（全批次共同遵守）

### 0.1 验证基线（每个批次收口前必须全绿）

```powershell
npm run typecheck          # tsc --noEmit 零错误
npm run test               # vitest 全绿（禁止跳过失败用例）
node tools/audit.cjs       # i18n 完整性 + 裸色值 + 无障碍段（按批次逐步启用）
cargo check                # Rust 侧零告警
cargo test --no-run        # 测试可编译（lib check 绿 ≠ test 可编译，教训在案）
cargo test                 # 其他 AI 域的失败用例如实标注归属，不越界修
```

### 0.2 共享文件守则（血泪教训成文）

| 共享文件 | 守则 |
| --- | --- |
| `src/lib/ipc.ts` / `src-tauri/src/ipc.rs` / `src-tauri/src/lib.rs` | 改前 `rg` 核实当前状态，最小化 diff，一次只加自己的段 |
| `src/i18n/dictionaries.ts` | zh 块曾多次被并发回滚：改前 `rg` 核键、改后立即复验，提交信息注明新增键清单 |
| `src/system/windows/vwm.ts` | 新窗口类注册分批提交，注册表段保持追加式 |
| `src-tauri/src/shell/taskman.rs` / `kbdhook.rs` / `desktop.css` / `StartMenu.tsx` | 历史冲突高发区，动前先 git log 看对方最近提交 |

### 0.3 批次（AB = Ascent Batch）编号与总排布

| 批次 | 主题 | 覆盖 | 依赖 |
| --- | --- | --- | --- |
| AB-0 | 地基与令牌 2.0 | U-07 | 无 |
| AB-1 | 启动剧场（P0 心脏） | U-01…U-06 | AB-0 |
| AB-2 | 视觉语言落地 | U-08、U-09、U-10 | AB-0 |
| AB-3 | 交互与布局 | U-11、U-12、U-55、U-56 | AB-2 |
| AB-4 | 桌面工作流 | U-13、U-14、U-15、U-17 | AB-0 |
| AB-5 | 文件管理与数据面板 | U-16、U-25、U-26、U-27 | AB-4 |
| AB-6 | 传输与存档 | U-22、U-28、U-29、U-30 | AB-5 |
| AB-7 | 性能与可靠 | U-19、U-20、U-21、U-23、U-24 | AB-1 |
| AB-8 | 安全与隐私 | U-31…U-36 | AB-0 |
| AB-9 | 开放生态 | U-37…U-42 | AB-3 |
| AB-10 | 硬件协同 | U-43…U-48 | AB-7 |
| AB-11 | 氛围与品质 | U-49…U-54、U-57…U-59 | AB-3、AB-4 |
| AB-12 | 收口与门禁 | U-60 + 全域验收 | 全部 |

并行波次：`AB-0 → [AB-1 ‖ AB-4 ‖ AB-8] → [AB-2 ‖ AB-5 ‖ AB-7] → [AB-3 ‖ AB-6 ‖ AB-9 ‖ AB-10] → AB-11 → AB-12`。

---

## 1. AB-0：地基与令牌 2.0（U-07）

**目标**：令牌体系升级到位，为全部视觉工作铺轨。

**步骤**：
1. `src/design/tokens.css` 扩展五组令牌（排版比例 / 间距全档 / 圆角三档 / 遮罩三档 / 噪声参数），每组的语义命名先在 `docs/DESIGN.md` 增补词条再落 CSS；
2. `tools/audit.cjs` 升级：裸色值计数器从 info-only 改为「阈值门禁」模式（初始阈值 = 当前实测值，每批收紧，最终 0）；新增「组件层引用原始变量层」的 lint 规则（先 warning，AB-3 转 error）；
3. 存量迁移第一批（低风险文件）：`tools.css`、`boot.css`（即将被 AB-1 重写，只迁不改）、`vwm.css` —— 每文件独立 commit，`rg '#[0-9a-fA-F]{3,8}|rgba?\(' <file>` 复核为零；
4. 三主题（dark/light/high-contrast）全量走查 `src/features/settings/`，对照度不足处只调 token 值不动组件。

**验收**：
- [ ] `node tools/audit.cjs` 以门禁模式运行，迁移文件零裸色值；
- [ ] 三主题切换截图（visual-audit 15 路由）与基线 diff <0.5%；
- [ ] DESIGN.md 新词条与 tokens.css 一一对应（人工核对清单入 PR 描述）。

**回滚**：令牌扩展纯增量，revert 单 commit 即可；audit 阈值改动独立 commit，可单独回退。

---

## 2. AB-1：启动剧场（U-01…U-06）★关键路径

**目标**：3A 级开机仪式上线，本计划最高优先级。

### 阶段 1.1 字形资产（U-02，先行）
1. 建 `src/assets/wordmark/`：8 字母 SVG + `glyphs.ts` 路径表导出（路径数据 + 实测 `getTotalLength()` 常量表）；
2. 单测 `src/assets/wordmark/glyphs.test.ts`：锁定每条路径长度（路径被误改即 fail）+ viewBox 合法性；
3. 装配组件 `src/system/boot/BootWordmark.tsx`：props = { scale, tone, progress }，progress 驱动逐字母 dashoffset（沿用 12.5%/字母 区间映射）。

### 阶段 1.2 胶囊与流带（U-03、U-04）
1. `CapsuleBar.tsx`：纯展示组件，props = { progress, breathing }；CSS 走 stadium 圆角（radius=height/2）+ 白填充 + 流光头；满格呼吸由 progress===1 触发一次性 class；
2. `FileTicker.tsx`：消费现有 `LoadEventPayload` 流，内部 useMemo 派生速度/剩余项/剩余时间（3s 滑动窗口均值）；行切换动画 transform-only；
3. `boot.css` 重写：从零新文件，只允许引用语义 token；emoji 全量替换为 `src/components/icons/` 的线性 SVG（此批先内置 6 枚最小集，全量等 AB-2 的 U-10）。

### 阶段 1.3 编排与整合（U-01、U-06）
1. `BootScreen.tsx` 重构：保留全部对外契约（onDone/onExitStart/onStats、boot_replay、boot://event、跳过机制），内部换成 `entering → streaming → readyHold → exiting` 状态机（纯函数 reducer + 单测四剧本：快机/慢机/跳过/回放）；
2. `settings.ts` 增 `bootPacing: cinematic|brisk|instant`（sanitize 三值），`DEFAULT_SETTINGS` 默认 cinematic；i18n 增 pacing* 键（zh/en，audit 过）；
3. 入场/离场编排全部 CSS 化（keyframes + 延迟），reduce-motion 媒体查询整段覆盖为 ≤80ms 淡入淡出。

### 阶段 1.4 启动交响（U-05）
1. `sounds.ts` 增 `bootSwell/bootPulse/bootReady` 三合成函数 + 进度调度器（监听剧场状态机而非自建定时器）；
2. `bootSoundMode: full|mute|chime-only` 设置项 + SettingsModal 声音页一行三选；
3. 音频设备热插拔容错（devicechange 监听，失败静默不抛）。

**验收（除功能全景 U-01…U-06 各自验收项外）**：
- [ ] 人为注入事件序列（vitest mock boot_replay）跑四剧本，状态机转移与快照全部符合预期；
- [ ] 真机（或 WebView2 环境）冷启动录屏逐帧检查：入场编排 900ms 内完成、无闪白、无布局跳动；
- [ ] 全程 emoji 扫描：`rg "[\x{1F300}-\x{1FAFF}]" src/system/boot/` 为空；
- [ ] 1440p + 200% 缩放下字标/胶囊/流带三者的比例与 100% 缩放一致（token 驱动，无 px 硬编码字标尺寸）。

**回滚**：旧 BootScreen 逻辑在 git 历史（重构前打 tag `pre-boot-cinema`），整批可 revert；期间 `bootAnim=none` 用户始终有直通逃生门。

---

## 3. AB-2：视觉语言落地（U-08、U-09、U-10）

**步骤**：
1. **材质引擎（U-08）**：tokens.css 材质三档段 + `src/styles/material.css`；`bgTier` 联动：低档全局 solid；DEBUG 环境变量 `VARIABLE_DEBUG_MATERIAL=1` 时显示同屏 frosted 计数徽标；快捷面板/通知中心两处先行试点 frosted；
2. **光影系统（U-09）**：`--elev-1/2/3` 令牌 + 交互态语义（hover 抬升/active 下压/拖拽投影）；`desktop.css`、`vwm.css`、`tools.css` 的既有阴影声明分三 commit 迁移（每文件迁移后截图回归）；
3. **图标语言（U-10）**：`src/components/icons/` + `src/lib/iconRegistry.ts` 语义字典；动效图标八件套（transform-only，reduce-motion 一键停）；registry 单测扫描全部 tsx 的 lucide import 做语义冲突检测。

**验收**：
- [ ] 同屏 5 个 frosted 面板自动降级 3 个（DEBUG 徽标如实显示）；
- [ ] 抽查 10 组件三态光影全部来自令牌（rg 验证零裸 box-shadow 十六进制）；
- [ ] registry 冲突单测在故意注入冲突时 fail（自测单测有效性）。

---

## 4. AB-3：交互与布局（U-11、U-12、U-55、U-56）

**步骤**：
1. **微交互精修（U-11）**：`src/styles/interactions.css` 六类控件四态规范；各控件文件删除自制 transition（`rg 'transition:' src/system src/components` 逐个归口）；危险操作红色内闪 + error 音接入（删除/焚毁/清空回收站三处先行）；
2. **响应式重构（U-12）**：`src/lib/breakpoints.ts`（matchMedia 三档上下文）+ tokens.css 容器查询段；任务栏/开始菜单/设置三处先行；`tools/screenshot-route.cjs` 增 1280/2560 两档宽度基线；
3. **空状态（U-55）**：`EmptyState.tsx` 通用组件 + 首批 12 处接入（清单见功能全景）；i18n 全覆盖；
4. **错误叙事（U-56）**：`src/lib/errors.ts` 词典（首批 20 个高频错误码 zh/en）+ ErrorBoundary、传输、嵌入三站点接入；重试退避按钮组件化。

**验收**：
- [ ] 六控件 × 四态视觉回归截图通过；位移/缩放超限扫描脚本（临时 tools 脚本）零告警；
- [ ] 1280/1920/2560/3840 四档截图回归通过；
- [ ] 人为触发 10 类错误全部人话化（手动演练清单入 PR）。

---

## 5. AB-4：桌面工作流（U-13、U-14、U-15、U-17）

**步骤**：
1. **桌面 Profiles（U-13）**：`src/system/desktop/profiles.ts` 数据模型（壁纸/主题/图标布局/任务栏/窗口集引用）；切换服务 + 卡片 UI；与 DRESS_PRESETS 打通（预设=Profile 的一个切面）；
2. **智能吸附 2.0（U-14）**：`src/system/vwm/snap2.ts`（AI-2 域内）；幽灵预览独立图层；分区方案库（1/2、1/3、2+1、四象限、自定义 ≤3×3）；与既有 applySnap 合并重构，snap 音效复用；
3. **任务栏进化（U-15）**：`Overflow.tsx`（30 窗压测）+ `JumpList.tsx`；IPC 新增 `taskbar.recent.register`（共享文件守则：ipc.ts/ipc.rs 最小段追加）；文件管理器外壳先注册最近打开项；
4. **拖放总线（U-17）**：`src/lib/dnd/bus.ts` + `DragGhost.tsx`；Drop 目标声明式 API；收藏托盘（屏幕边缘寄存）。

**验收**：
- [ ] Profile 切换后多显示器图标位置逐个还原（脚本断言）；
- [ ] 吸附幽灵预览与落位偏差 0px；3×3 自定义网格可用；
- [ ] 30 并发窗口任务栏零像素错位；Jump List <80ms；
- [ ] 文件→便签（若 AB-11 未到则用写作占位面板）、窗口→工作区两类跨域拖放通。

---

## 6. AB-5：文件管理与数据面板（U-16、U-25、U-26、U-27）

**步骤**：
1. **Explorer 2.0（U-16）**：Tabs/DualPane/ColumnView/RenameDialog 四组件 + shell 侧列视图批量读目录 IPC；>5000 项虚拟化滚动（与 AB-6 的 IO 治理目录虚拟化对齐接口，先做分页后接治理）；视图记忆按目录持久化；
2. **版本时光机（U-25）**：`shell/versions.rs`（内容寻址 + 增量块 + 保留策略 GC）+ 保存队列挂点（钩子级，不改进各应用保存逻辑）+ `src/features/versions/` 时间轴浏览器（双栏 diff）；
3. **全局标签（U-26）**：tags 表 + explorer/桌面图标标签角点 + 智能文件夹（保存查询）；
4. **回收站 2.0（U-27）**：来源记录 + 恢复预览 + 策略引擎（容量/时间阈值，星标豁免）。

**验收**：
- [ ] 万项目录滚动 60fps（bench 项入 AB-12 基线）；
- [ ] 1000 版本文件秒开；还原后 mtime/权限保留；GC 后校验全过；
- [ ] 标签随移动/重命名跟随（path key 监听单测）；
- [ ] 断电恢复测试：回收站 DB 与文件一致性通过（人为 kill 进程模拟）。

---

## 7. AB-6：传输与存档（U-22、U-28、U-29、U-30）

**步骤**：
1. **IO 治理（U-22）**：`shell/iogov.rs`（IoPriority 尽力而为设置 + 大文件分块管道 + 暂停/恢复 + 目录分页）；explorer 虚拟化接通；
2. **传输指挥台（U-28）**：`shell/transfer.rs` 队列中枢（并发 3、冲突四策略、失败重试面板）+ `src/features/transfer/` 前端（速度迷你曲线与启动剧场流带同源风格组件复用）；explorer/拖入/版本导出三来源全接入；
3. **存档柜（U-29）**：`shell/archive.rs`（zstd + SHA-256 清单 + 挂载只读浏览）+ 柜卡片墙 + 审计修复；
4. **数据血缘（U-30）**：lineage 表 + 三类事件采集（拖入/使用/离开）+ LineageWindow + 整体导出/焚毁。

**验收**：
- [ ] 索引扫描中复制 1GB 波动 <15%；暂停/恢复校验和一致；
- [ ] 10GB 归档→挂载→还原往返校验一致；翻转 1 字节被审计捕获；
- [ ] 拖入→编辑→导出全链路事件不漏；血缘焚毁后 DB 与磁盘残留为 0。

---

## 8. AB-7：性能与可靠（U-19、U-20、U-21、U-23、U-24）

**步骤**：
1. **启动流水线（U-19）**：加载任务 P0/P1/P2 分级（boot.rs 域）；mtime 快照预热缓存（checksum 防脏读）；boot_replay 事件加 phase 标记；剧场 ready 提前、P1/P2 转托盘微型胶囊；
2. **内存守护（U-20）**：`shell/memward.rs`（5s 采样 + 泄漏看门狗 + 软回收）+ IPC `mem.snapshot`；省内存模式（<10% 可用内存联动降档）；
3. **帧预算（U-21）**：`src/lib/framebudget.ts`（三档策略 + 动画成本账本 + 长任务 idle 切片）；星场/氛围光/通知入场三站点先行接入；
4. **崩溃叙事（U-23）**：ErrorBoundary 全量铺设（窗口/面板级）+ `shell/session.rs` 30s 会话快照与恢复卡片 + 安全模式（连续 2 次异常退出自动进入）；
5. **Bench 2.0（U-24）**：`tools/bench2.cjs` 六指标 + 预算断言；归档纪律沿用 docs/bench mtime 检查。

**验收**：
- [ ] 冷启动 ready 缩短 ≥40%（bench2 对比报表）；
- [ ] 注入泄漏 10 分钟被识别；软回收后 RSS 下降且功能正常；
- [ ] 万行列表+星场+通知三并发主线程 >50ms 长任务计数 0；
- [ ] 杀渲染进程重启会话还原率 ≥95%；连续异常后安全模式自动触发；
- [ ] bench2 连续 3 次空跑方差 <10%；人为 20% 回归被拦。

---

## 9. AB-8：安全与隐私（U-31…U-36）

**步骤**：
1. **隐私仪表盘（U-31）**：`shell/privacy.rs` 审计表（剪贴板/敏感区/出站三类）+ 仪表盘前端；暂停期显式标注「记录中断」；
2. **防火墙 2.0（U-32）**：netconsent.rs 扩展应用级档案（禁网/白名单/放行）+ 出站告警中心 + 日流量配额；
3. **诱饵文件（U-33）**：privacy.rs canary 段 + 模板生成器 + 触发警报链（托盘/仪表盘/可选锁屏）+ 白名单豁免；
4. **紧急擦拭（U-34）**：`shell/panic.rs` 三级协议 + kbdhook 长按 800ms 进度环；演练模式（dry-run 文件系统 diff 断言为零）；
5. **信任链（U-35）**：`shell/trust.rs` WinVerifyTrust 封装 + 登记卡签名状态 + 信任墙；
6. **隐身会话（U-36）**：session.rs 隐身层（内存 + 加密临时盘）+ explorer/剪贴板/通知三站点开关 + 四角标识 + 焚毁。

**验收**：
- [ ] 诱饵打开 <1s 警报；误报白名单生效；
- [ ] L2 真实触发后临时数据残留 0；演练模式磁盘 diff 为空；
- [ ] 四类签名样本验证全对；100 软件并发验证 <10s；
- [ ] 隐身会话 100 文件操作后扇区级扫描为 0（复用焚毁引擎的验证工具）。

---

## 10. AB-9：开放生态（U-37…U-42）

**步骤**：
1. **协议中枢（U-37）**：`shell/deeplink.rs` 路由 + 单实例转发；HKCU 注册仅便携部署态（退出退订）；环境内按钮逐步改走协议动词；
2. **脚本安全屋（U-38）**：`shell/script/safehouse.rs`（.vxsc 清单 + 能力授予 + 5s 配额）+ 脚本管理页 + 导入确认流（契约式逐项勾选）；
3. **资源包（U-39）**：`shell/pack.rs`（.vxs 导入/导出/部分应用/校验）+ 外观包管理页 + Profile 内嵌支持；
4. **无障碍 2.0（U-40）**：audit.cjs 无障碍段转 error；ARIA 分批补齐（StartMenu/Taskbar/Explorer/Settings 四大件优先）；色觉模拟器（设置内四滤镜）；
5. **i18n 中心（U-41）**：翻译工作台（双栏对照/缺失高亮/引用跳转，写回 dictionaries.ts）；伪本地化模式 + audit 扫描；RTL 试点两面板（逻辑属性迁移）；
6. **诊断导出（U-42）**：`shell/diag.rs`（六类内容 + 预览勾选 + 脱敏）+ 关于页入口。

**验收**：
- [ ] 浏览器 `variable://open` 唤起定位成功；未注册宿主友好降级；
- [ ] 越权脚本 100% 被拒并记录；死循环 5s 被杀环境无恙；
- [ ] .vxs 往返还原一致；畸形包拒绝且叙事清晰；
- [ ] audit 无障碍段 0 告警；纯键盘完成开窗→吸附全流程；
- [ ] 伪本地化 0 截断；RTL 两面板截图正确；
- [ ] 诊断包 0 绝对路径 0 口令字段（扫描断言入 diag 单测）。

---

## 11. AB-10：硬件协同（U-43…U-48）

**步骤**：
1. **多显示器 2.0（U-43）**：monitor.rs 显示器档案（壁纸/任务栏/布局记忆）+ 拓扑可视化编辑器 + 热插拔窗口迁移（VWM 布局引擎承接）；
2. **外设快捷层（U-44）**：`shell/periph.rs` + 宏录制编辑器（与 N-17 词表共享）+ 鼠标手势 8 向识别（轨迹采样本地实现 + 渐隐光痕）+ 媒体键接管（MediaControl 扩展）；
3. **音频路由（U-45）**：IAudioSessionEnumerator 每应用音量/路由/效果三档 + 峰值表 + 音频 HUD（4 段柱）；
4. **色彩与时辰（U-46）**：`shell/circadian.rs`（2800K–6500K 时段插值 + 手动覆盖浮条 + 退出还原）；
5. **无线中心（U-47）**：`shell/wireless.rs` BLE 枚举 + 电量预警（24h 防骚扰）+ 快速配对向导；
6. **性能模式（U-48）**：`src/lib/perfmode.ts` 三档上下文 + framebudget/iogov/预热三消费点 + 切换微光提示 + 本地智能建议（可忽略）。

**验收**：
- [ ] 双屏拔插 20 次窗口零丢失；混合 DPI 清晰度合格；
- [ ] 手势 100 采样准确率 ≥97%；宏冲突即时标红；
- [ ] 路由切换 <150ms 无爆音；峰值表与系统读数一致；
- [ ] 退出后宿主 gamma 字节级还原；过渡 ≤100K/分钟；
- [ ] 三类无线设备状态与系统一致；配对可取消无残留；
- [ ] 三档切换 10 次无漂移；静音档 GPU 降 ≥30%。

**注**：本批多项需真机（多屏/蓝牙/音频外设），无真机项按既有 DoD 口径**不勾选**，挂 docs/acceptance/ascent-硬件真机清单.md 待实机验收（沿用 B-30 矩阵诚实纪律）。

---

## 12. AB-11：氛围与品质（U-49…U-54、U-57…U-59）

**步骤**：
1. **音景引擎（U-49）**：`src/lib/soundscape.ts` 五场景程序化合成（叠加/渐变/睡眠定时/番茄联动）；
2. **焦点舱（U-50）**：`src/system/focus/`（舱体 + 通知静默队列 + 白名单穿透 + 专注账本）；
3. **通知交互（U-51）**：notifyStore 模型扩展（actions/progress/组）+ 通知中心升级 + 勿扰回放；
4. **声景反馈（U-52）**：sounds.ts 参数表化 + 三主题 + 深夜自动 ×0.5（醒目开关）；
5. **环境辉光（U-53）**：`glow.ts` 壁纸主色提取 + 内发光层（省内存联动关闭）；
6. **节律助手（U-54）**：`src/system/rhythm/` 四节律 + 仪表（MiniApp 承载，依赖 AB-4 未含的 U-18——**U-18 在本批补做**：`src/apps/mini/` 五件套 + vwm mini 窗口类）；
7. **引导体系（U-57）**：onboarding spotlight 5 站 + coach marks 注册表 + 技巧卡片；
8. **键盘全景（U-58）**：`src/lib/hotkeys/` 集中注册表 + Ctrl+/ 速查浮层 + 覆盖审计表清零；
9. **触控基础（U-59）**：tokens touch 变体 + 44px 目标 + 四手势 + 平板模式询问。

**验收**：
- [ ] 音景 1h 无内存增长；场景切换无爆音；
- [ ] 舱内通知全静默、出舱有序回放；账本可导出；
- [ ] 操作通知一次点击到位；100 条积压回放 <5s；
- [ ] 辉光 <0.5ms/帧；纯白壁纸不过曝；
- [ ] 五 MiniApp 冷启动合计 <400ms；便签持久化；
- [ ] 导览全程 Esc 可退；速查表与键位 100% 同源；纯键盘 10 分钟深度操作；
- [ ] 长按呼出 450ms±50ms；触屏无死按钮。

---

## 13. AB-12：收口与门禁（U-60 + 全域验收）

**步骤**：
1. **品质关卡（U-60）**：`.github/workflows/quality.yml` 五段流水线（tsc → vitest → audit 全段 → bench2 预算 → visual-audit 15 路由 × 3 主题矩阵）；main 全阻断 + 特性分支白名单机制；品质看板 markdown 报告；
2. **文档收口**：`docs/acceptance/ascent-验收矩阵.md`（60 项 × 三宿主，未实测不勾）；CHANGELOG 顶部新条目「ASCENT-60 收口」；README 能力声明表更新（含 D-5 诚实声明表的对应新增行）；
3. **发布物三件套**：`npm run build` + `tauri build`（NSIS）+ portable 构建，沿用 B-30 发版纪律；
4. **全域回归**：四关卡全绿 + bench2 基线归档 + 三主题截图矩阵入库。

**验收**：
- [ ] CI 四关卡 <8 分钟；注入 1px 漂移被捕获；
- [ ] 验收矩阵中每项勾选均有证据链接（测试/截图/录屏归档路径）；
- [ ] 版本号升至 `1.1.0`，代号 `1.1ascent.vxe`。

---

## 14. 风险登记表

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| 启动剧场与 U-19 流水线互相牵制（同改 boot 路径） | 高 | AB-1 先冻结 phase 协议字段（预留枚举），AB-7 只填充分级逻辑不改协议 |
| 字形资产设计反复导致 AB-1 返工 | 中 | 阶段 1.1 单独验收（路径长度单测锁定）后才进 1.2 |
| Explorer 2.0 与 IO 治理接口漂移 | 中 | AB-5 开工前先与 AB-6 负责路签「目录分页接口契约」（写进两批 PR 描述） |
| dictionaries.ts 并发回滚复发 | 高 | 每次 zh 块修改后立即 `rg` 复验 + commit message 附键清单（守则 0.2） |
| 硬件批次无真机 | 确定 | 诚实纪律：不实测不勾，挂真机清单，不伪造验收 |
| safehouse JS 引擎引入体积/安全面 | 中 | 评估独立轻量引擎 crate；若不可控则降级为「受限宏脚本」并如实缩小 U-38 范围 |
| Cargo.toml 无效 feature 名再次阻塞 | 低 | 新增依赖先 `cargo check` 单独验证再合入（教训在案） |

## 15. 完成定义（DoD，每批次）

1. 功能全景对应项的全部验收框勾选（或有真机豁免标注）；
2. 0.1 验证基线全绿；
3. audit.cjs 该批新增门禁段通过；
4. 共享文件改动经 `rg` 复验无回滚冲突；
5. PR 描述含：改动文件清单 / 新增 i18n 键清单 / 共享文件触碰声明 / 回滚方式；
6. 验收矩阵（AB-12 建，之前先记在批次 PR）对应行更新。

---

*批次执行顺序与并行波次见第 0.3 节；分工到 AI 路见姊妹篇 AI 分工图。*

---

## 第三部分：APEX-70（工匠计划）实施步骤 · 原文全量

# APEX-70 — 实施步骤（从地基到收口的完整施工手册）

> 配套文档：`APEX-70-功能全景.md`（70 项是什么）、`APEX-70-AI分工图.md`（谁来做）。
> 本手册回答：按什么顺序做、每一步怎么做、做到什么程度算过。

---

## 0. 施工总纪律（全批次共同遵守）

### 0.1 验证基线（每个批次收口前必须全绿）

```powershell
# 前端
npm run build            # tsc + vite 零错误
npm run audit            # audit.cjs AUDIT PASSED（i18n 键 zh/en 同步、术语抽查）
npx vitest run           # 单测全绿

# 后端（涉及时）
cargo check              # lib 绿
cargo test --no-run      # 测试可编译（血泪教训：check 绿 ≠ test 可编译）

# 工具
node tools/keymap-audit.cjs    # PB-0 之后加入：键位零 error 冲突
node tools/theme-validate.cjs  # PB-8 之后加入：示例主题全过
```

### 0.2 共享文件守则（沿承既有血泪教训，全文有效）

1. `src/i18n/dictionaries.ts`、`src/lib/ipc.ts`、`src-tauri/src/lib.rs` 三个共享文件：**改前 `rg` 核对当前状态，改后立即 `rg` 复验未回滚**（zh i18n 块历史上被并发进程反复回滚）。
2. 每次只做最小 diff，不顺手重构、不格式化整文件。
3. 跨路文件冲突：等持有人收口后再动，不抢写。
4. `.ps1` 脚本如经 Edit 工具修改，**必须重新写回 UTF-8 BOM**（已两次翻车）。

### 0.3 批次编号（PB = Peak Batch）与总排布

| 批次 | 内容 | 域 | 关键点 |
|---|---|---|---|
| PB-0 | 地基：键位注册表 + 图标规格 | Z-08、Z-01 | 一切新键位/新图标的前置 |
| PB-1 | Windows 一致性工程落地 | Z-02…Z-07 | 控件规格 → 字体 → 清晰度 |
| PB-2 | 键盘纪律完成 | Z-09…Z-14 | 让位协议 + 全键盘导航 |
| PB-3 | 兼容性防线 | Z-15…Z-21 | 数据库先行，真机验证并行 |
| PB-4 | 基础工具 | Z-22…Z-28 | 独立性强，可与其他批次并行 |
| PB-5 | 文件能力 | Z-29…Z-35 | 只读优先原则 |
| PB-6 | 窗口微调 | Z-36…Z-42 | 全部默认关闭 |
| PB-7 | 声音与通知基础 | Z-43…Z-49 | 与既有 F-5 / sounds.ts 对接 |
| PB-8 | 开放性与拓展 | Z-50…Z-56 | 校验器先行，接口默认关闭 |
| PB-9 | 质量与性能基础 | Z-57…Z-63 | 与 PB-1..8 持续联动 |
| PB-10 | 观感收口 + 全域验收 | Z-64…Z-70 + 收口 | 最后的最后一毫米 |

**总原则**：PB-0 必须最先且单独收口；PB-4 可在任何时点并行插入；其余按序但允许相邻批次重叠 30%。

---

## 1. PB-0：地基层（Z-08、Z-01）

### 阶段 0.1 键位注册表（Z-08）

1. 新建 `src/lib/keymap/types.ts`：`KeyBinding { id, combo, scope: "global"|"window"|"context", priority, source, descKey }`；`Combo` 用规范串（如 `"ctrl+alt+t"`）。
2. 新建 `src/lib/keymap/registry.ts`：`register()/unregister()/snapshot()`；注册时立即做静态冲突检测（同 scope 同 combo = error；跨 scope 同 combo = warn 记录）。
3. 新建 `src/lib/keymap/arbitrate.ts`：作用域栈裁决，输入焦点元素内自动降级（联动 Z-10 机制）。
4. **存量收编**：`rg -n "addEventListener\(['\"]keydown" src/` 全量列出，逐个迁入注册表（预计 30–60 处，分目录小步提交）。
5. `tools/keymap-audit.cjs`：静态扫描源码中的裸 keydown 与注册表 dump 比对，输出冲突报告；进 package.json scripts 与 CI。
6. 单测：注册/注销/冲突检测/作用域裁决各一组；面板卸载后键位释放（snapshot diff 空）。

### 阶段 0.2 图标网格规格（Z-01 基础部分）

1. `docs/ICON-SPEC.md`：16/20/24/32 四档网格、keyline、笔粗档位规格表。
2. 新建 `src/components/AppGlyphs` 旁的 `SystemIcon.tsx`：Segoe Fluent Icons 字体封装（字形名 → codepoint 映射表，仅用系统已装字体，缺失回退 lucide）。
3. 高频 30 图标映射表（回收站、网络、音量、电源、通知、搜索、设置、文件夹…）。
4. 单测：映射表 codepoint 有效性；回退路径触发。

**收口门禁**：keymap-audit 报告零 error；tsc/audit/vitest 全绿；图标映射表评审通过（并排截图对比）。

---

## 2. PB-1：Windows 一致性工程（Z-02…Z-07）

### 阶段 1.1 控件规格令牌化（Z-02）

1. `tokens.css` 增加 `--ctl-*` 尺寸族：`--ctl-hit: 44px`（触控命中）、`--ctl-bar-btn: 32px`、`--ctl-dialog-btn-h: 32px`、`--ctl-dialog-btn-min-w: 120px`、`--ctl-input-h: 32px`、`--ctl-menu-item-h: 36px`、`--ctl-check: 20px`、`--ctl-radius: 4px`。
2. `audit.cjs` 新增检查：`src/components/`、`src/system/` 下按钮/输入框样式中的裸 width/height 计数（基线登记，只减不增，目标清零）。
3. 逐组件替换（小步提交：WindowControls → ContextMenu → Modal → 设置面板 → 工具面板）。
4. 与 Win11 截图逐像素 diff（8 控件，阈值 2px）。

### 阶段 1.2 字体链（Z-03）

1. global.css 字体链替换为完整回退链（Segoe UI Variable Text → Segoe UI → Microsoft YaHei UI → system-ui）。
2. 标题/正文/代码三档光学尺寸 class（`type-display/type-text/type-mono`）。
3. 雅黑混排字号补偿（-0.5px）与行高策略（中文 1.7 / 西文 1.5）。
4. 验证：FOUT 无（本地字体零网络加载）；基线一致性截图。

### 阶段 1.3 清晰度管线（Z-04）

1. 核对 Tauri manifest DPI 感知为 PerMonitorV2。
2. 前端 `devicePixelRatio` 变化监听 → 图标/位图缓存按 DPR 键（联动 Z-66 缓存层，此处先做接口）。
3. 像素对齐审计：边框/分隔线取整函数 `snapToPixel()` 进 `src/lib/`，关键组件替换。
4. 回归：125%/150%/200% 三档截图基线（联动 Z-63 基线建设，先手动手跑通）。

### 阶段 1.4 状态与光标（Z-05、Z-06）

1. `:focus-visible` 全局样式统一（2px 环、主题色、鼠标点击不显示）；逐面板走查。
2. 光标语义审计表（`rg "cursor:"` 全量 → 表格 → 逐项修正）。

### 阶段 1.5 文案规范（Z-07）

1. `docs/UI-COPY.md` 术语表（60+ 词条）。
2. i18n 双语审校一轮（重点：按钮动词化、省略号、术语统一）；audit.cjs 术语抽查项。

**收口门禁**：裸尺寸计数只减不增；三档 DPI 基线截图入库；键盘焦点走查通过。

---

## 3. PB-2：键盘纪律完成（Z-09…Z-14）

1. **Z-09 让位协议**：`src/lib/keymap/system-combos.ts` 内置 40+ 系统组合表（combo → translate/passthrough/yield + 适用条件）；kbdhook.rs 侧透传路径核对；全屏自动全让位（联动 Z-18 的检测接口，先以全屏标志位对接）。产出行为表 `docs/SYSTEM-COMBOS.md`。
2. **Z-10 作用域**：`useHotkey(combo, handler, { scope, priority })` hook；上下文进入/退出与组件 mount/unmount 绑定；单测覆盖作用域栈。
3. **Z-11 全键盘导航**：`useGridNav()` 封装 roving tabindex；12 个主要面板逐个接入（每面板一提交，走查一过）；Home/End/Page 快跳与 scroll-into-view。
4. **Z-12 速查浮层**：从 registry 实时读当前激活键位；`Ctrl+/` 呼出（本身经注册表注册，自举验证）；毛玻璃样式不抢焦点。
5. **Z-13 提示条**：`<CommandHintBar/>` 组件，注册表派生；设置开关（默认开）；容器高度 > 600px 才显示。
6. **Z-14 方案管理**：`keymap-profiles.ts` 三套预设（JSON 补丁集）；设置页 UI（列表/导入/导出/重置）；导入过 schema 校验 + 冲突列表用户裁决。

**收口门禁**：keymap-audit 零 error；12 面板纯键盘全流程走查记录归档；三套预设往返切换注册表终态一致。

---

## 4. PB-3：兼容性防线（Z-15…Z-21）

1. **Z-15 适配库**：`resources/app-compat/matrix.json`（schema：进程名/特征/等级/症状/建议）+ CI schema 校验；设置内查询面板（列本机已装应用 × 等级）；与既有 compat.hint 登记库对接（B/C 级 → hint 徽标，复用 C-5 通道，**rg 核对 embed.rs/launcher.rs 现状后再动**）。
2. **Z-16 遗留 shim**：tray.rs 增加旧格式回调识别转译；气球通知 → 通知中心条目；多 DPI 菜单坐标换算修正；诊断日志命中计数。
3. **Z-17 IME**：输入法会话激活信号接入 Z-10 让位；候选框跟随定位（Windows IME 位置 API）；VWM 过滤清单增加输入法悬浮窗类名；4 款输入法实测表。
4. **Z-18 全屏协议**：全屏检测统一接口（embed.rs 已有判定抽出为公共）；覆盖层抑制（HUD/横幅 → 角标）；Alt-Tab 让位开关（默认让位）；独占切换期静默窗口。
5. **Z-19 混合 DPI**：跨屏拖动几何换算（物理像素锚定）；每屏缓存命名空间；双屏实测 20 次记录。
6. **Z-20 宿主模式**：三源检测（会话 API/显卡特征/性能特征）；宿主档内容落地（动效 0.25×、壁纸静态、透明关闭）；设置可见可覆盖；不确定按本机跑。
7. **Z-21 慢速设备**：信号采集（磁盘/内存/GPU）；L0–L3 降级阶梯实现；只降不升策略；设置透明呈现档位依据。

**收口门禁**：10 款旧协议软件托盘实测记录；4 款输入法 30 分钟实测零冲突；RDP/Hyper-V 降级实测；HDD 测试机 L2 档走查。

---

## 5. PB-4：基础工具（Z-22…Z-28）——可并行批

1. **Z-22 时钟中心**：`src/system/tools/Clock.tsx`；世界时钟/秒表/计时器/闹钟四 tab；任务栏时钟入口；闹钟豁免勿扰接口预留。
2. **Z-23 天气卡**：`src/system/tools/Weather.tsx`；数据源抽象（默认 Open-Meteo 无 key）；30min 缓存；失败静默降级。
3. **Z-24 Emoji 面板**：`src/system/tools/Chars.tsx`；emoji 数据集本地化（分类/名称中英）；最近使用持久化；复制走剪贴板历史。
4. **Z-25 放大镜与取色器**：`src/system/tools/`；放大镜走 capture.rs 截图 + 前端放大（像素网格 8×+）；取色器复用截图链路；历史 16 色。
5. **Z-26 换算中心**：`src/system/tools/Converter.tsx`；单位表/进制/汇率三 tab；公式表文档化；与计算器联动入口。
6. **Z-27 系统信息**：`src/system/tools/SysInfo.tsx`；聚合 hardware.rs/sysinfo.rs；诊断摘要一键复制。
7. **Z-28 运行对话框**：`src/system/tools/Run.tsx`；输入类型识别（可执行/路径/URI/别名）；历史 + 自动补全；Win+R 转译对接 Z-09。
8. 全部工具：注册进工具面板 + 各自键位经 Z-08；i18n zh/en 同步；`tools.ts` 后端命令按需补充（**rg 核对 mod.rs 注册表现状**）。

**收口门禁**：7 工具全部纯键盘可操作（Z-11）；天气三异常场景优雅降级；换算 60+ 单位权威值比对通过。

---

## 6. PB-5：文件能力（Z-29…Z-35）

1. **Z-29 预览扩展**：Quick Preview 增 pdf.js（本地 vendor）、字体预览、zip/7z/tar 只读清单（后端 shell/explorer.rs 增清单命令，不落盘解压）、代码高亮（只读复用配置）；>100MB 提示拦截。
2. **Z-30 打开方式**：后端关联查询/写入命令（系统 API）；设置面板 UI（扩展名 × 默认应用 × 候选）；操作日志表。
3. **Z-31 位置侧栏**：explorer 侧栏重构（快速访问/常用/网络三段）；映射驱动器 UI（系统 API）；离线灰化；固定项持久化。
4. **Z-32 批量重命名**：`src/system/tools/Rename.tsx` + 后端批量命令；规则管线（替换/序号/大小写/扩展名）；预览 diff 表（冲突标红）；撤销点日志；1000 文件压力测试。
5. **Z-33 重复报告**：后端流式哈希命令（快速抽样 → 确认全量）；前端分组报告虚拟滚动；CSV/JSON 导出；**UI 明示"只报告不删除"**。
6. **Z-34 空间分析**：后端目录扫描 worker；treemap 前端组件（Canvas 绘制，squarified 算法）；双视图联动；快照对比。
7. **Z-35 发送到**：右键菜单"发送到"子菜单（合并系统 SendTo 目录）；自定义目标管理；最近 3 目标置顶。

**收口门禁**：全部工具"只读优先"审查（任何删除/写入操作必须有显式确认 + 可撤销）；压力测试记录归档。

---

## 7. PB-6：窗口微调（Z-36…Z-42）

1. **Z-36**：winman.rs 透明度/置顶命令核对现状 → 标题栏右键菜单 + 键位（经注册表）；每应用记忆；设置总开关默认 off。
2. **Z-37 几何记忆**：关闭时记录（类名+显示器键）；启动恢复；断屏回退主屏居中。
3. **Z-38 滚轮行为**：非活动窗口滚轮透传核对；hover 激活/Alt+滚轮置顶两开关（默认关）；全屏禁用。
4. **Z-39 标题栏**：hover 气泡提示、关闭红底、双击最大化——对齐 Win11 逐项；不改按钮排布。
5. **Z-40 布局快照**：几何采集/恢复（句柄映射）；命名管理 UI；导入导出（Z-54 信封）；上限 20。
6. **Z-41 手势**：判定引擎（方向+距离阈值）；失败还原右键菜单（无感回退）；默认关闭；误触率测试 ≥ 200 次。
7. **Z-42 桌面切换**：切换预览（缩略图 + 首应用图标，简渲染）；边缘热区联动 Z-69；170ms 横移动效复用令牌。

**收口门禁**：全部功能默认关闭状态下的行为与改动前 100% 一致（回归对照）；开启后各自的实测矩阵归档。

---

## 8. PB-7：声音与通知基础（Z-43…Z-49）

1. **Z-43 音量记忆**：Mixer 面板（F-5 既有）音量持久化（应用会话键）；"记住此应用"开关；与系统 Mixer 数值一致性核对。
2. **Z-44 勿扰日程**：时段规则表 + 日历联动（默认关）；任务栏勿扰图标（SystemIcon）；通知静默入档链路（对接 Z-47 存档表，先建表）。
3. **Z-45 声音方案**：方案 = 事件→文件映射 + manifest；内置 3 套（默认=现 sounds.ts 6 音、柔和、无声）；预听/单事件替换/恢复默认；导入校验（格式/时长 ≤ 2s/采样率）。
4. **Z-46 设备快切**：tray.rs 设备枚举/切换命令；托盘菜单（输出/输入分组、当前打勾、默认通信设备分离）；热插拔事件驱动。
5. **Z-47 通知存档**：SQLite 表（migration 0003）；保留策略三档；全文搜索（FTS5）；历史通知操作按钮重放（发送方支持时）；存量通知迁移。
6. **Z-48 麦克风指示**：会话回调监听；任务栏图标 + 使用应用 tooltip；仅指示零拦截。
7. **Z-49 提醒中心**：提醒表（文本/时间/重复）；番茄钟（25/5 + 自定义）；到点通知走通知中心 + 可选声音（Z-45 方案联动）；重启未触发提醒不丢（启动补扫）。

**收口门禁**：跨午夜勿扰正确；1 万通知搜索 < 100ms 实测；麦克风指示零轮询（netstat/事件日志佐证）。

---

## 9. PB-8：开放性与拓展（Z-50…Z-56）

1. **Z-50 主题规范**：schema（JSON）→ 校验器 `tools/theme-validate.cjs`（语法/完备性/对比度 ≥ 4.5:1）；2 套示例主题进 `examples/themes/`；应用流程：校验→预览→应用。
2. **Z-51 数据导出**：导出范围聚合（设置/主题/键位方案/通知存档/笔记/剪贴板可选/布局）；manifest 自描述；进度与校验（文件数/大小核对）；1GB 媒体压力测试。
3. **Z-52 事件流**：内部事件总线接入点盘点（20+ 事件）；localhost WebSocket（默认关）；JSONL schema 文档；订阅过滤。
4. **Z-53 状态 API**：REST 只读端点（/status /perf /audio /desktop）；数据复用命令层；127.0.0.1 绑定；默认关；`dashboard.html` 示例（examples/）。
5. **Z-54 分享格式**：统一信封 `{kind, version, payload, checksum}`；三 kind 接入（keymap/layout/theme）；导入预览（"将覆盖 3 个现有绑定"）；坏 checksum 拒绝。
6. **Z-55 数据连接器**：SQLite/JSON/CSV 只读连接器；SQL 白名单解析（SELECT-only，拒 UPDATE/INSERT/ATTACH/PRAGMA 等 20 种写法测试）；数据卡两型展示；失败静默。
7. **Z-56 兼容矩阵**：文档 + `minShellVersion` manifest 必填校验；加载比对提示；弃用流程文档化（CHANGELOG-PIPELINE.md）。

**收口门禁**：8 种坏主题全部拦截；SELECT-only 注入测试 20/20 通过；默认关闭态 netstat 零监听端口；导出→恢复演练记录。

---

## 10. PB-9：质量与性能基础（Z-57…Z-63）

1. **Z-57 安全模式**：`--safe-mode` 参数 + 失败自动建议；跳过加载全部第三方资源（主题/连接器/事件流/手势/方案）；角标明示；不改用户配置。
2. **Z-58 健康面板**：performance observer 采集（FPS/长任务/提交耗时/内存）；设置诊断页；归因标记；关闭态零采集（代码级验证）。
3. **Z-59 空闲冻结**：最小化/遮挡/非当前桌面检测 → rAF 暂停；恢复预渲染一帧再上屏（< 100ms）；豁免清单（视频/下载/音频）；10 窗口 CPU 对比报告。
4. **Z-60 内存压力**：水位三档监听；清缓存/冻结后台/提示（不代杀）；恢复不反向升级；填充测试。
5. **Z-61 更新通道**：稳定/先行双通道；增量包（diff 下载 < 30% 目标）；签名+哈希+双版本回滚点；用户确认制。
6. **Z-62 本地统计**：注册表埋点（键位使用 Top 20）+ 应用时长；周/月图表；一键清除；零外发技术报告（抓包佐证）。
7. **Z-63 回归基线**：DPI 三档 15 界面截图基线；12 面板键盘脚本走查；visual-audit 纳入固定基线；CI 独立 stage。

**收口门禁**：坏主题启动 → 安全模式 → 恢复全流程演练；三类基线各自捕获故意退化（破坏性试验后还原）。

---

## 11. PB-10：观感收口 + 全域验收（Z-64…Z-70 + 全域）

1. **Z-64 CJK 排版**：间距 polyfill 层（批量文本节点）；避头尾；中文行高策略；豁免四大内置应用（隔离声明硬校验：PR 检查 `src/apps/` 零改动）。
2. **Z-65 拖动清晰度**：拖动中降质策略（关亚克力采样/阴影重算，位图直搬）；松手一帧恢复；高性能机自动全程满清。
3. **Z-66 零闪烁**：worker 解码 + 中性灰剪影占位 + DPI 命名空间 LRU；万图标滚动录屏逐帧检查。
4. **Z-67 滚动与动效一致**：滚动参数令牌化收编；动效速度滑杆（× 全部 --dur-*；reduce-motion 优先）；即时预览。
5. **Z-68 零闪白**：离屏样式快照 + 双层交叉淡入 170ms；壁纸层不参与；连续切换防抖；20 次录屏无纯白/黑帧。
6. **Z-69 热区**：四角+三边可配置；默认仅左下开始菜单；悬停 300ms 防误触；全屏自动禁用；可视化配置面板。
7. **Z-70 帮助中心**：F1 上下文映射表（15 界面）；离线文档（功能说明 + 键位表从注册表生成 + 设置项解释）；全文搜索；"这是什么"模式。
8. **全域验收**：
   - 全量回归：0.1 基线命令 + 三类基线全绿；
   - 隔离声明核查：`git diff --stat` 确认 `src/apps/` 全程零改动；
   - 70 项逐项对照功能全景的"验收"条目打勾（未实测不打 ✅，沿承诚实口径）；
   - 验收文档 `docs/acceptance/apex-70-验收.md` 归档。

---

## 12. 风险登记表

| # | 风险 | 概率 | 缓解 |
|---|---|---|---|
| R1 | 存量键位收编（PB-0）改动面大引发回归 | 高 | 分目录小步提交；每步全键盘走查；keymap-audit 基线先行 |
| R2 | 共享文件（dictionaries.ts / ipc.ts / lib.rs）并发回滚 | 高 | 0.2 守则；改后立即 rg 复验；错峰提交 |
| R3 | Segoe Fluent Icons 在精简系统缺失 | 中 | 缺失回退 lucide 映射表（Z-01 内建） |
| R4 | 兼容实测依赖真机/外设（RDP、双屏、输入法、旧软件） | 中 | 未实测项按既有口径不勾 ✅，登记待验清单 |
| R5 | SQLite migration 与其他路冲突 | 中 | 0003 编号占用前 rg 核对；只增不删 |
| R6 | 事件流/状态 API 引入安全面 | 中 | 默认关闭 + 只读 + 回环绑定 + 文档明示 |
| R7 | 降级阶梯（Z-21/Z-60）误降档伤观感 | 中 | 只降不升 + 手动覆盖入口 + 档位透明呈现 |
| R8 | cargo test 被其他 AI 未完成组件阻塞 | 高 | 沿用 `--no-run` 口径先行收口，全量测试留待合并窗口 |

## 13. 完成定义（DoD，每批次通用）

1. 功能全景对应条目的"验收"点全部实测通过并留档（未实测 = 未完成，不虚报）。
2. 0.1 验证基线全绿。
3. 新增键位 100% 经注册表、keymap-audit 零 error。
4. 新增视觉元素 100% 走令牌、无裸值回潮。
5. `src/apps/`（四大内置应用）零改动。
6. 每个新开关默认值 = 最保守档；关闭后零残留（回归对照）。
7. CHANGELOG.md 增加对应条目；涉 i18n 键的 zh/en 同步且 rg 复验。

---

## 第四部分：SUMMIT-90（极顶计划）实施步骤 · 原文全量

# SUMMIT-90 — 「极顶计划」实施步骤

> 系列：NEXT-40 → ASCENT-60 → APEX-70 → **SUMMIT-90**
> 配套文档：《SUMMIT-90-功能全景.md》（功能定义）、《SUMMIT-90-AI分工图.md》（责任归属）
> 施工总则见第 0 章；三波次交付节奏见第 1 章；逐项步骤见第 2~11 章；验收与收口见第 12 章。

---

## 0. 施工总则（每个 AI 每一项都必须遵守）

### 0.1 五条铁律的工程化落点

| 铁律 | 工程化检查点 |
|---|---|
| 零退化 | 每项功能默认关闭或默认值=现状；改动前后 `tools/visual-audit.cjs` 基线 diff ≤0.5%；新设置项默认值写在 DEFAULT_SETTINGS 并过 sanitize 测试 |
| 零键位冲突 | 新键位只允许 `ctrl+alt+` 空闲槽（用 `rg "ctrl\+alt\+" src/lib/shortcuts.ts` 先查占用）；Z-08 注册表落地后一律改走 `keymap/register()` |
| 四应用隔离 | 改动前 `git status` 确认不触碰 `src/apps/write|mind|code|fate`、`src/features/editor|mindmap|project|fate*`；PR 检查清单勾选「四应用零修改」 |
| 不弄巧成拙 | 新动效必须走既有 `--dur-*`/`--ease-*` 令牌与 reduce-motion 降级；新增 UI 文案三语齐全（audit.cjs 门禁） |
| 如实降级 | 任何系统 API 失败路径都有诚实文案与日志，禁止静默吞错 |

### 0.2 共享文件协议（沿用项目既有纪律，冲突重灾区）

- **共享文件清单**：`src/i18n/dictionaries.ts`、`src/lib/ipc.ts`、`src-tauri/src/lib.rs`、`src/system/windows/vwm.ts`、`src/system/desktop/StartMenu.tsx`、`desktop.css`/`tools.css`
- **改前必核**：`rg` 验证当前状态（他人可能已改），最小化 diff，只加不改他人段落
- **改后必验**：`npm run typecheck` + `node tools/audit.cjs` + 涉及后端时 `cargo check`
- **i18n 特别纪律**：zh 块曾被并发回滚——每次提交前 `rg "新键名" src/i18n/dictionaries.ts` 双侧（zh/en）复核
- **PS1 脚本纪律**：Edit 工具会剥 UTF-8 BOM，每次编辑 .ps1 后必须重写 BOM

### 0.3 每项功能的通用 DoD（完成定义）

1. 代码落地 + 单测（vitest / cargo test）覆盖核心路径与失败路径
2. `npm run typecheck` 0 错、`node tools/audit.cjs` AUDIT PASSED、`cargo check`（涉后端）0 错
3. 新设置项：DEFAULT_SETTINGS + sanitize + 设置页 UI + i18n 三语
4. 新键位：注册表/快捷键表登记 + 冲突检测通过 + 帮助文案
5. 默认行为与改动前一致（或默认关闭），验收清单打勾（未实测不勾，诚实口径）
6. docs/acceptance/ 下对应验收记录更新

### 0.4 测试命令速查

```powershell
npm run typecheck                # 前端类型
node tools/audit.cjs             # i18n/IPC 命令面审计
npx vitest run                   # 前端单测
cargo check                      # 后端编译
cargo test --no-run              # 测试可编译性（cargo check 绿 ≠ 测试可编译）
cargo test                       # 后端单测
node tools/visual-audit.cjs      # 视觉回归（puppeteer 可选，缺省诚实退出 2）
node tools/bench --no-gui        # 性能基准（先看 docs/bench 当日归档防覆盖）
```

---

## 1. 三波次交付节奏

| 波次 | 主题 | 项数 | 前置 |
|---|---|---|---|
| 第一波（P0） | 立刻可做、无依赖、痛点直击 | 12 项 | 无 |
| 第二波（P1） | 体验增量明显、依赖既有基础设施 | 36 项 | P0 完成 + APEX-70 基础项（Z-08 注册表建议优先） |
| 第三波（P2） | 依赖前序 N/Z/U 系列交付的生态与基建 | 42 项 | 各自前置（见全景附 A） |

> 波次内各域可并行（分属不同 AI 路）；波次间只有软依赖——P1 里不依赖 Z 系列的项可与 P0 并行。

---

## 2. 域 W 施工步骤（M-01…M-09，AI-2 窗口路）

### M-01 摇一摇最小化
1. `src/system/windows/VirtualWindowFrame.tsx` 拖拽路径：pointermove 采样横坐标，计算换向次数与速度峰值（窗口 ≥2 次换向且幅度 >60px 触发）
2. 触发调用 vwm 状态机已有的 minimizeAll 动作（复用 Ctrl+Alt+M 路径）
3. `settings.winShake`（默认 false）+ sanitize + 设置页开关 + i18n `winShakeLabel` 三语
4. 单测：合成拖拽事件序列（正常拖动 100 次 / 摇动 10 次）断言触发率
5. 验收：贴靠拖拽路径零误触（贴靠手势本身有横移，需在「已进入贴靠预览」状态禁用检测）

### M-02 窗口卷帘
1. vwm.ts 窗口状态新增 `rolledUp: boolean`；VirtualWindowFrame 高度动画到 `--titlebar-h`（复用 snapping 动效令牌）
2. 内容容器 `display:none`（与最小化同一保活策略，不卸载 React 树）
3. 标题栏右键菜单加「卷起/展开」；双击热区可选项（默认关）
4. 任务栏图标角标组件复用 IM 徽标样式；贴靠/最大化时自动展开
5. 嵌入窗口（isEmbed）菜单项置灰 + `embedNotSupported` 文案；单测卷起→展开内容状态不丢

### M-03 最小化抽屉
1. 新组件 `src/system/taskbar/MinimizedDrawer.tsx`；数据源 = vwm 状态中 `minimizedAt` 时间戳（vwm.ts 窗口对象加字段）
2. 任务栏抽屉图标（徽标 = 计数；空时 `return null` 不占位）
3. 键位 `ctrl+alt+\``（反引号，查 shortcuts.ts 确认空闲）注册进 SHORTCUT_ACTIONS + winman.rs default_binds 同步
4. 键盘导航（↑↓/Enter/Esc）复用 WintabSwitcher 交互模式；i18n `drawerEmpty/drawerTitle`
5. 单测：20 窗口最小化排序、还原后计数一致性

### M-04 窗口体检
1. `winman.rs` 新增 `health_check` 轮询（3s，仅已登记 hwnd，`IsHungAppWindow`）；结果经事件 `health://window` 推前端
2. VirtualWindowFrame 标题栏琥珀徽标（`--warn` 令牌）+ 悬停说明文案（诚实口径：应用自身问题）
3. 「结束进程」走 ConfirmHost 模态（明确数据丢失警告）→ 既有 taskkill 通道
4. 事件频率走 M-50 削峰（本项可与 M-50 并行，先用简单节流）
5. cargo test：状态机 hung→normal→hung 转移用例

### M-05 跨屏摆渡
1. 拖拽悬停边缘 600ms 计时器 → 目标屏 SnapPreviewHost 半透明预览（复用贴靠预览组件，换锚定屏）
2. 松手：目标屏坐标换算（QueryDisplayConfig 的 monitor rect + DPI），260ms 位移动画（transform 过渡，不重排）
3. 键盘版 `ctrl+alt+shift+left/right` 进快捷键表（前后端双侧）
4. 混合 DPI 单测：100%→150% 屏几何换算比例正确（纯函数抽取）
5. 验收：双屏 20 次摆渡零错位、零黑帧

### M-06 窗口挂起
1. `embed.rs` 会话扩展 `suspend()/resume()`（`NtSuspendProcess`/`NtResumeProcess`，feature gate）
2. 环境退出钩子（lib.rs on_exit 附近）：枚举挂起会话全部恢复——**带着挂起退出是红线事故**
3. 前端右键菜单 + 冰蓝徽标（`--accent` 变体）；任务栏角标
4. 只对 embed 登记进程开放；四空间/文件管理器等环境内窗口菜单项不显示
5. cargo test：挂起→恢复→挂起循环句柄不泄漏；退出恢复路径用例

### M-07 对齐参考线
1. 拖拽 move 时收集其他可见窗口 rect + 屏幕中线/三分线；距离 <8px 画 1px 参考线（绝对定位 div，`--accent` 色）
2. 吸附 = 坐标取整到参考线；Alt 按住时 `guidesEnabled=false`（本帧）
3. `settings.snapGuides`（默认 false）；i18n 开关文案
4. 单测：纯函数 `computeGuide(candidates, moving)` 距离/吸附用例
5. 验收：三窗口 1/3 排列几何误差 ≤1px；reduce-motion 不影响（效率功能）

### M-08 精炼 Alt+Tab
1. WintabSwitcher 数据层加过滤谓词（byMonitor / byApp）；过滤器状态进 settings
2. `ctrl+alt+tab` 打开带过滤的切换器；`alt+tab` 默认路径零改动
3. 空集合 → toast「当前过滤器下无其他窗口」+ 回退一次全局轮转（诚实降级）
4. i18n 三语；单测过滤谓词
5. 验收：双屏各 5 窗口集合正确

### M-09 悬停聚焦
1. VirtualWindowManager pointermove 节流（300ms 停留判定）
2. 三档设置：off（默认）/focusOnly/focusAndRaise；仅聚焦档只改焦点态不改 Z 序
3. 暂停条件：全屏应用、拖拽中、模态打开、kbdhook 环境隐藏
4. 设置项带诚实提示文案；单测暂停条件矩阵

---

## 3. 域 T 施工步骤（M-10…M-18，AI-2 / AI-5）

### M-10 跳转列表
1. 数据层 `src/system/startmenu/jumplist.ts`：recent.ts 记录 + launcher 登记表按 appKey 聚合（最近文件 top7）
2. 组件 `JumpList.tsx`：固定图标右键 → 二级浮层（最近文件 / 常用动作 / 最近实例窗口）
3. pin/unpin 持久化 settings KV（`jumpPins.{appKey}`）
4. 嵌入应用列出活会话窗口（点击 → embed focus 通道）
5. 单测聚合逻辑；i18n `jlRecent/jlPinned/jlNewWindow`

### M-11 托盘收纳抽屉
1. `tray.rs` 托盘枚举已有 → 新增溢出阈值事件；前端 `TrayDrawer.tsx`
2. 拼音搜索复用 `pinyin.ts`（`searchTray(q)`）
3. 置顶常驻 + 拖动排序持久化；新图标徽标 +1 点开清零
4. 文档明示「只读取镜像、不修改系统托盘」；i18n 三语
5. 验收：15 测试图标搜索三种输入命中

### M-12 时钟多时区
1. 设置 `clockZones: string[]`（IANA，≤3）+ sanitize
2. 时钟悬停卡增量渲染（多时区行 + ISO 周数 + 今日计数只读）
3. `Intl.DateTimeFormat` 按时区渲染（夏令时自动正确）；周数纯函数 + 单测（年初/年末/闰年边界）
4. i18n 三语；验收三城市夏令时切换日

### M-13 等待态规范
1. launcher 启动事件（pid 已有）→ 任务栏占位条目（.skeleton shimmer 既有样式）
2. 占位点击 = 聚焦/无操作（**绝不重复 ShellExecute**，800ms 防抖锁）
3. 8s 超时状态切换文案 + 点击显示详情（pid/耗时）
4. 单测防抖锁；验收 10GB 级 IDE 冷启动

### M-14 IM 未读聚合
1. imwatch.rs 事件已推 → 前端聚合 store（Σ 计数，99+ 封顶）
2. 任务栏总徽标（复用徽标组件）+ 悬停明细（逐 IM 计数，点击聚焦）
3. IM 进程退出 → 计数清零事件；i18n 三语
4. 验收：双 IM 并发未读一致

### M-15 任务栏空区菜单定制
1. 菜单项注册表 `src/system/desktop/taskbarMenu.ts`（id/i18n/action/默认显隐）
2. 菜单渲染改读注册表 + settings 覆盖（顺序+显隐）
3. 设置页「任务栏」区菜单编辑器（拖动排序复用 M-31 的托架交互或简化为 ↑↓ 按钮）
4. 「恢复默认」一键；默认状态与现状逐像素一致（视觉回归验证）

### M-16 媒体呼吸
1. MediaControl 播放态 → 时钟旁媒体图标 CSS class `breathing`
2. `@keyframes breathe`（scale 1→1.02，4s，ease-in-out infinite）；`[data-reduce-motion="true"]` 下禁用
3. `settings.mediaBreath`（默认 false）；幅度周期写死
4. 视觉回归：开启/关闭/降级三态基线

### M-17 便签速贴
1. `ctrl+alt+s` 快捷键（查空闲）→ 单行输入浮层（桌面 shell 层，PromptHost 变体）
2. 便签组件 `StickyNote.tsx`：四角堆叠、拖动、10 分钟淡隐（500ms 动画）、点击钉住
3. 数据 settings KV（`stickies: {id,text,pinned,corner}[]`，≤20 条上限）；环境重启恢复 pinned 项
4. 「一键清除全部」走确认模态；i18n 三语
5. 单测：淡隐计时、钉住持久化、上限拒绝

### M-18 音量滚轮规范
1. 前端 keydown 捕获 `VK_VOLUME_*`（不 preventDefault，纯监听）
2. 音量浮标（QuickPanel 同款样式复用）1s 淡出；与逐应用音量档位联动显示
3. DND 下照常显示（音量非打扰）；i18n
4. 验收：三种设备（键盘滚轮/多媒体键/耳机线控）走查

---

## 4. 域 F 施工步骤（M-19…M-27，AI-1 / AI-3）

### M-19 右键菜单编辑器
1. `src/components/ContextMenu.tsx` 项抽取注册表（id/i18n/danger 标记/分组）
2. 设置页「文件管理器」区编辑器（显隐勾选 + ↑↓ 排序 + 分组分隔线开关）
3. 持久化 settings `ctxMenuOrder`；「恢复默认」
4. 单测：注册表完整性（全部动作有 i18n 与处理器）

### M-20 同名选择记忆
1. explorer 批量操作的冲突对话框（已有或补齐）加「对剩余全部应用」勾选
2. 会话级传播（批量上下文对象持有选择，不进 settings）
3. 批量结束 toast 汇总（替换/跳过/保留两者计数）；保留两者重命名预览
4. 单测：47 冲突的 200 文件批量弹窗 1 次；Esc 中断不产生半拷（事务路径回归）

### M-21 校验和工具
1. 后端新 command `checksum(path, algo)`：分块流式读取（1MB 块），进度事件 `checksum://progress`，可取消（CancelToken 模式复用 bench 先例）
2. BLAKE3 复用容器依赖；MD5/SHA 由 `md-5`/`sha2` crate
3. 前端浮窗：算法四选、进度条、期望值比对（绿勾/红叉高亮）、复制按钮
4. ipc.ts 登记 + audit.cjs 通过；cargo test 对照 certutil 已知向量
5. 验收：1GB 文件内存峰值 <80MB、取消零残留

### M-22 空格快速预览
1. explorer 列表聚焦态 keydown 空格 → `QuickLook.tsx` 居中浮窗（非模态，Esc/空格关闭）
2. 渲染层复用既有预览组件；格式白名单表（支持/降级两态，降级显示图标+类型说明）
3. ←/→ 相邻文件、↑/↓ 层级；音视频显示控制条
4. 单测白名单矩阵；i18n `qlUnsupported`
5. 验收：20 格式走查 + 键盘闭环

### M-23 压缩包目录浏览
1. 后端 `archive_ls(path)` / `archive_extract_one(path, inner)`：`zip` crate 只读；7z/tar 视依赖体积决定 v1 范围（zip 优先，7z 如实「暂不支持」）
2. explorer 路径解析层：`xxx.zip` 可进入（虚拟路径前缀 `zip://`），列表/排序复用
3. 双击 → 缓存目录解压 → 系统打开 → 关闭后清理（temp 管理复用 sysmaint 临时区）
4. 包内文件名搜索（前端过滤即可）；损坏包错误走 error.rs 归一化文案
5. cargo test：zip 构造/读取/单文件解出/损坏路径

### M-24 目录置顶书签条
1. settings `placePins: string[]`（≤12）+ 访问计数 `placeStats`
2. 地址栏旁书签条组件（图标+名称，LRU 排序，满 12 淘汰提示）
3. 拖拽文件夹到书签条（drop 目标）即置顶；右键菜单（新标签/系统打开/取消）
4. 单测 LRU 淘汰；i18n 三语

### M-25 文件锁定侦探
1. 后端 `who_locks(path)`：Restart Manager API（RmStartSession/RmRegisterResources/RmGetList）
2. 删除/移动失败错误码映射 → 前端失败 toast 加「查看占用」
3. 侦探浮窗：进程名/pid/窗口标题列表 + 「在任务管理器中查看」（跳转 TaskManApp 并定位 pid）+ 刷新
4. 查不到 → 诚实文案「系统未披露占用者」；**无强拆按钮**（红线）
5. cargo test：本进程打开文件的自锁用例（锁定者=自己，可断言）

### M-26 环境回收站安全网
1. 设置 `deleteMode: system|env|ask`（默认 system = 现状）
2. env 模式：删除走环境回收站目录（便携模式跟随数据卷，usb.rs 拔出保护联动）
3. 清空前确认模态显示最近 10 项可恢复清单
4. 单测三档行为；验收便携拔盘场景

### M-27 目录监控哨兵
1. 后端 `sentinel.rs`：ReadDirectoryChangesW 每哨兵一线程（上限 5），事件节流合并 1s 窗口
2. settings `sentinels: {path,enabled,quietHours}[]`；通知中心推送（复用 notifyStore，含声音开关）
3. 环境隐藏时照常记录（通知入中心不弹横幅）
4. SMB/网络路径：入口校验拒绝并提示「暂不支持网络目录」
5. cargo test：临时目录注入增删改名事件合并用例

---

## 5. 域 K 施工步骤（M-28…M-36，AI-2）

### M-28 键位使用统计
1. winman.rs dispatch_action 处计数（内存 HashMap + 每 5 分钟落盘 settings KV `keyStats`）
2. 快捷键中心（N-17 落地后）列表加使用次数列；未落地则先出设置页只读榜
3. 注册失败（降级）单独计数成「占用榜」+ 空闲槽换绑建议（复用 M-36 的建议函数）
4. 隐私口径注释：只记 action id + 次数，无内容；单测计数与落盘

### M-29 长按加速曲线
1. VWM 键盘移动循环处：keydown 持续时间 → 重复间隔查表（500ms 前 1x，之后 1.5x/2.5x/4x 三段）
2. 只挂接「窗口移动/网格导航」两类；曲线常数 const 化
3. 单测：合成长按 2s 的移动步数 ≈ 12 次单击 ±15%

### M-30 侧键可编程
1. kbdhook 轮询路径识别 XBUTTON1/2（GetAsyncKeyState 扩展键位）
2. 映射表 settings `xBinds: {xbutton1,xbutton2}` 默认 = back/forward（零退化）
3. 全屏应用检测（复用既有避让逻辑）→ 让位回系统语义
4. 映射动作复用 SHORTCUT_ACTIONS 分发；单测默认语义

### M-31 启动槽可视化分配
1. 设置页快捷键区「启动槽托架」：9 槽组件（虚线空槽 + 图标实槽 + 键位标签）
2. 从开始菜单/桌面拖拽（HTML5 DnD，payload 携带 appKey）落槽即写 `shortcutBinds.launchN` 的应用映射（注：launch 槽的应用归属存 settings `launchApps`）
3. 应用被删 → 槽位清空 + toast；同应用多槽允许但提示
4. 单测绑定写回 effectiveBinds 一致性

### M-32 每窗口 IME 状态
1. 后端 `ime_state.rs`：ImmGetDefaultIMEWnd/ImmGetConversionStatus 查询 + WM_IME 设置（不可用 API → 事件「unsupported」，前端零动作）
2. VWM 焦点切换钩子：切换前记录旧窗口状态、切换后恢复新窗口记忆态
3. 托盘状态条小图标（中/英）；四空间窗口同样适用（只读系统状态，不改其代码）
4. 单测：状态机记录/恢复；降级路径（API 不可用）零副作用
5. 验收：终端↔聊天 50 次正确率 ≥95%

### M-33 按键回显
1. winman.rs dispatch 处事件 `keycast://show`（仅 `settings.keycast` 开启时发）
2. 前端角落浮层（修饰键组合 + i18n 功能名），2s 淡出，四角可选
3. **只回显功能组合**（dispatch 表内的），打字内容永不出现（红线注释）
4. 单测：表外按键零事件

### M-34 Esc 层级规范
1. uiStore 加「浮层栈」`overlayStack: OverlayId[]`（push/pop API）；12 类浮层（模态/右键菜单/各面板/搜索框）逐一走查入栈
2. 全局 Esc handler：栈顶消费一层；kbdhook 双击 Esc 判定保持独立（30ms 轮询，不受影响——需在 kbdhook 层加「环境内有浮层时双击 Esc 只关浮层」的白名单逻辑，谨慎回归）
3. 层级表文档进帮助中心（Z-70 落地后）；单测栈序
4. 回归重点：双击 Esc 切环境的手感不变（验收专列）

### M-35 滚轮语义规范
1. 盘点壳层 15 个滚动容器（explorer/开始菜单/通知/设置/任务栏…）逐一核对 Shift=横滚、Ctrl=缩放（支持处）
2. 任务栏滚轮音量（`settings.wheelVolume` 默认 false）+ M-18 浮标联动
3. 行为表写入 docs/INTERACTION.md（新文件，10 行级小文档）
4. 四大空间内部不在范围（隔离红线，文档注明）

### M-36 键位变更预览
1. 快捷键设置页保存前 diff：`effectiveBinds(newOverrides)` → 找被挤占 action + 系统保留键库命中标注
2. 预览卡（冲突列表 + 风险高/低）+ 「查看占用者」跳转 + 「自动挑空闲槽」（扫描 ctrl+alt+* 未占用槽建议）
3. 系统保留键库（40+，与 Z-09 清单同源共用常量）
4. 单测：diff、建议函数；i18n 三语

---

## 6. 域 C 施工步骤（M-37…M-45，AI-3 / AI-1 / AI-2）

### M-37 图标缓存自愈
1. launcher.rs 缓存条目结构加 `src_mtime + src_hash`；启动快扫失效 → 后台重建队列（并发 ≤2）
2. 重建完成事件 `icon://refresh` → 前端替换占位（占位用 .skeleton shimmer，替换走零闪策略）
3. 「全量重建」按钮进设置→维护区
4. cargo test：失效判定函数

### M-38 UWP 识别
1. launcher.rs 枚举补 GetStartApps + PackageManager（windows crate winrt feature）→ UWP 条目（图标/显示名/发布者）
2. 与 .lnk 扫描去重合并（AUMID 键）；compat 矩阵 tier=L4（让位，不可嵌入）
3. 前端嵌入按钮置灰 + 说明 tooltip
4. cargo test：合成 UWP 条目合并去重

### M-39 提权提示
1. 启动路径复用安全工作台 PE 解析（requireAdministrator manifest 标记）
2. 说明卡组件（每应用一次，settings `elevatedAck.{appKey}` 记忆）；「不再提示」勾选
3. ShellExecute runas 失败（用户拒绝 UAC）→ toast 如实回执
4. 单测 manifest 解析

### M-40 高刷自适应
1. 后端启动时 QueryDisplayConfig 主屏刷新率 → 事件推前端
2. 前端 root 元素 `--dur-scale: 0.85`（≥120Hz）写入（与 M-75 缩放乘法叠加：`calc(var(--dur-3) * var(--dur-scale) * var(--motion-scale))`）
3. `settings.hfrTiming` 默认 true；混合多屏按主屏
4. 单测：系数计算；验收 144Hz 无跳帧

### M-41 驱动共存协议
1. 内置已知驱动进程名清单（G Hub/Synapse/Armour Crate 等 10 个，const 可更新）
2. 启动进程快照匹配 → 一次性提示卡（settings `driverAck` 记忆）
3. 帮助中心收录「与驱动软件共存」条目（Z-70 落地后挂链）
4. 单测清单匹配

### M-42 实例角标
1. 任务栏聚合键 exe path；计数 → 复用徽标组件（≤3 数字 / >3 `3+`）
2. 悬停 tooltip 显示区分性窗口标题（同组窗口标题列表）
3. 单测聚合计数收敛

### M-43 便携路径自愈
1. launcher 登记结构加卷 GUID（FindFirstVolume/GetVolumeInformationByHandleW）；壁纸池/数据卷引用同步
2. 启动定位失败 → 枚举卷 GUID 匹配 → 命中批量改写登记 + toast 告知
3. 未命中走既有 `--export-rescue` 应急提示
4. cargo test：模拟盘符漂移的改写用例；usb.rs 拔出保护不误触发回归

### M-44 嵌入崩溃善后
1. embed.rs 会话状态机加 `crashed` 态（进程退出且非用户关闭）；事件 `embed://crashed`
2. 前端虚拟窗口 300ms 优雅摘除（复用关闭仪式样式，非黑框 display:none）
3. 「应用 X 已停止运行」卡片（重开按钮带原启动参数；60s 内同应用合并）
4. 取证日志（时间/pid/退出码）本地记录；cargo test 状态机转移

### M-45 热插拔稳定
1. 后端 WM_DEVICECHANGE（窗口消息钩子）→ 快捷键重注册（指数退避 ≤5 次）+ kbdhook 重挂
2. 重挂期动作队列缓存 ≤1s 后回放；toast「输入设备已重连，快捷键已恢复」（每事件一次）
3. cargo test：重注册退避；验收蓝牙键鼠休眠唤醒 30 次 100% 恢复

---

## 7. 域 S 施工步骤（M-46…M-54，AI-3 / AI-1）

### M-46 日志轮转
1. 日志 append 路径统一走 `log.rs`（若分散先收拢）：按天 + 5MB 双阈值轮转，旧份 gzip，保留 7 份/总 50MB
2. 设置→维护区显示占用 + 「立即清理」
3. cargo test：轮转触发、配额删除最旧

### M-47 迁移预检
1. settings 迁移器加 dry-run 模式（不落库返回 diff 报告：未知键/损坏值清单）
2. 自更新流程（sysmaint）升级前调用预检；不过 → 自动备份先行 + 「保守升级」建议文案
3. fixtures：5 种损坏样本单测全拦截

### M-48 DB 紧凑会话
1. 一次性 `PRAGMA auto_vacuum=incremental` 迁移（版本号升级触发）
2. sysmaint 计划任务新增 compact 任务（触发四要素：空闲 ≥15min/接电/非备份/距上次 ≥7 天）；执行前强制快照
3. incremental_vacuum 分批 + WAL checkpoint；进度走维护面板既有事件
4. cargo test：触发条件矩阵；验收 10 万条删除回收 ≥80%

### M-49 图标缓存 LRU
1. 前端图标缓存 Map → LRU 包装（last_used 更新，上限 256MB 可调 64~512）
2. 淘汰分批（rAF 每批 ≤32 张）；设置页显示占用/命中率
3. 单测：淘汰顺序、分批不阻塞主线程 >8ms

### M-50 事件削峰
1. 新 `src/lib/eventShed.ts`：三档策略（merge/latest/queue）声明式频道注册
2. 五个风暴源迁移：winman 轮询、imwatch、设置广播、硬件面板、VWM 状态
3. 单测：1000 事件/秒合成风暴，最新必达 100%、回调次数收敛
4. 迁移回归：各面板行为不变（人工走查清单）

### M-51 浸泡测试
1. bench 工具加 `--soak --hours 8` 模式：合成负载循环（开/关窗口、启停应用、设置读写、事件风暴）
2. 每小时采样内存/句柄/线程数 → 报告曲线（归档 docs/bench，先查当日归档防覆盖）
3. 泄漏阈值：8h 内存 +100MB / 句柄 +500；nightly 计划任务
4. 先观察一个月再决定是否升 CI 门禁（诚实口径写文档）

### M-52 冷启动对照
1. boot 事件流分阶段时间戳 → 快照 JSON 落盘 `docs/bench/boot-<version>.json`
2. 对照工具 `tools/boot-diff.cjs`：与上一版 diff，回归 >10% 阶段标红 + 归因建议（阶段名）
3. 发版流程（M-87）挂接；固定口径：同机空载 5 次取中位
4. 验收：注入 200ms 延迟定位到阶段

### M-53 崩溃转储
1. lib.rs 加 SetUnhandledExceptionFilter + MiniDumpWriteDump（`crashes/` 目录，保留 10 份）
2. 启动检测新 dump → 提示「可导出诊断包」（联动 Z-42 导出）
3. `tools/symbolize.ps1`（发版符号表解析主栈；注意 BOM 纪律）
4. cargo test：dump 写入路径；验收注入崩溃 5 次 100% 落盘

### M-54 资源公平调度
1. embed.rs 每应用 Job Object（JOB_OBJECT_CPU_RATE_CONTROL_INFORMATION，三档 100/50/25%）
2. 登记信息加 `cpuQuota`；前端右键菜单 + 硬件面板标黄
3. 不做内存限额（注释红线）；单测 Job 创建/取消

---

## 8. 域 O 施工步骤（M-55…M-63，AI-4）

### M-55 路由注册公开表
1. 路由声明集中化注解（id/params schema/owner/since）；生成器 `tools/gen-routes.cjs` → `docs/ROUTES.md`
2. audit.cjs 加检查：文档路由集与代码注册集 diff 为空
3. 示例链接（variable://xxx）可复制格式

### M-56 设置自动文档
1. settings 定义处元数据集中（key/type/default/range/since/i18n 描述引用）
2. `tools/gen-settings.cjs` → `docs/SETTINGS.md`；发版流程挂接
3. audit.cjs 校验默认值与 DEFAULT_SETTINGS 一致

### M-57 本地出站桥
1. 事件白名单子集（嵌入成功/下载完成/哨兵触发等 8 个）→ 规则表 settings `webhooks`
2. 出站 POST（reqwest，超时 3s，重试 1 次）；**逐次走 netconsent 或会话白名单**
3. URL 校验：环回/内网段直接放行（仍记日志），公网段需二次确认
4. cargo test：白名单事件、URL 校验、失败重试；验收 echo server 10/10、未确认 0 出站（抓包）

### M-58 插件热重载
1. 插件运行时（N-26 落地后）加 dev 通道：目录 watch（notify crate）→ 沙箱重建
2. dev 模式插件 UI 带 DEV 角标；结构化日志到控制台
3. 坏插件隔离失败不影响环境（运行时既有隔离兜底回归）

### M-59 嵌入声明协议
1. `variable-embed.json` schema（titleMatch/minSize/multiInstance/detectWaitMs）+ 校验器
2. launcher 扫描读取，用户登记优先级高于声明（文档明示优先级规则）
3. 样例与 schema 说明进 docs/ROUTES.md 同级 `docs/EMBED-MANIFEST.md`
4. cargo test：合法/非法声明用例

### M-60 测试钩子规范
1. `docs/TEST-HOOKS.md` 命名规范（`{组件}-{语义}`）；15 条主路径组件补 data-testid
2. audit.cjs 查重复 testid；boot 完成事件作为 e2e 就绪哨兵
3. 既有 e2e 迁移到钩子选择器（迁移后零修改通过 = 验收）

### M-61 变更日志自动化
1. `tools/gen-changelog.cjs`：conventional commits 解析 → 中文模板草稿（feat/fix/docs/perf 分节）
2. 发版流程（M-87）串接；输出到 CHANGELOG.draft.md 供维护者审阅

### M-62 社区翻译格式
1. i18n 词典导出/导入 CSV（键/zh/zh-Hant/en 四列）；导入仅更新既有键 + 占位符（{n}）校验
2. CLI：`node tools/i18n-csv.cjs export|import <file>`；坏行定位行号报错
3. audit.cjs 保持一致性门禁；单测导入回环（导出→改→导入→词典断言）

### M-63 资源包安全扫描
1. `pack_scan.rs`：类型白名单（图片/CSS/JSON/字体）、zip-slip 路径穿越检查、体积阈值、可执行文件拦截
2. 导入确认模态展示扫描报告（文件数/类型分布/可疑项列表）
3. 恶意包 fixtures（exe/`../`/超大假图）cargo test 全拦截；正常包 10 个零误报

---

## 9. 域 A 施工步骤（M-64…M-72，AI-5）

### M-64 壁纸主色采样
1. 后端 `wallpaper_accent(path)`：缩至 64px → RGB→OKLCH → 聚类取前 3 主色 → 候选返回
2. 前端预览卡：三候选色对关键界面迷你预览（任务栏/按钮/开关三元素着色）
3. 应用 = 生成主题存档（走主题系统既有存档结构），绝不自动生效
4. 单测：聚类纯函数；验收 10 壁纸人工评价；HC 模式不受影响（HC 覆盖 accent）

### M-65 昼夜壁纸组
1. 壁纸池结构加「时段组」维度（morning/day/dusk/night 四组）；设置页组编辑
2. 切换调度：系统时间监听（含 M-90 日界统一事件），边界默认 6/12/18/21 可调
3. 切换动效复用 Z-68 交叉淡入技术（300ms）；视频壁纸直接替换（不强交叉，注释说明）
4. 单测边界判定；验收跨 4 边界无黑帧

### M-66 音量淡变
1. 后端音量设置路径（QuickPanel/逐应用音量动作）包 30ms 淡变（IAudioEndpointVolume SetMasterVolumeLevelScalar 分步）
2. 系统侧调节（音量键直达系统）不拦截不改写（注释红线）
3. 单测淡变步进；人工听测 20 次零爆音

### M-67 桌面纯净模式
1. `ctrl+alt+p`（查空闲）→ 三层（图标/任务栏/横幅）淡出 200ms；状态记忆进 uiStore
2. 通知中心角标移屏幕边缘小点（可感知不丢）
3. 再按恢复原状（各层独立还原）；单测状态记忆/还原
4. 验收：30 分钟视频零侵入、退出精确还原

### M-68 屏保时钟
1. 空闲检测（复用打字降级输入监听，N 分钟无输入，默认 10）
2. `ScreensaverClock.tsx`：大字号 HH:mm + 日期，暗色低亮度层；分钟级更新；任意输入 ≤100ms 退出
3. 视频壁纸播放中不触发（播放态即「活跃」）；`settings.screensaver: off|clock|black`
4. 单测空闲判定；验收 2h CPU <1%

### M-69 今日简报卡
1. 每日首启判定（与 M-90 日界事件联动，`lastBriefDate` settings）
2. 卡片：日期周数 + 昨日摘要（Z-62 统计只读，未落地则显示「本地就绪」诚实降级）+ 贴士池 30 条轮换（`briefTip{n}` i18n）
3. 8s 自动收起 + 「今日不再显示」；单测首启判定/轮换不重复

### M-70 壁纸快捷操作
1. 桌面空白右键菜单壁纸区：收藏/下一张/详细信息（分辨率/路径可复制）
2. 壁纸池 favorite 标记；壁纸工坊收藏组（N-07 落地后）或设置页分组显示
3. 菜单项走 M-19 注册表；i18n 三语

### M-71 悬停延迟面板
1. tokens.css 加 `--hover-delay-fast/std/slow`（200/400/600ms）；6 处浮层统一引用
2. 设置项 `hoverLatency: fast|std|slow` 默认 std（= 现状）
3. 视觉回归默认档与改动前一致

### M-72 氛围会话恢复
1. 退出前快照：壁纸状态/音量/DND/窗口集合（登记应用+几何；环境内窗口记路由参数）→ settings `ambientSnapshot`（可关）
2. 下次进入提示「恢复上次的样子？」（绝不自动恢复）
3. 恢复动作：逐项应用（窗口批量创建走既有 launch 通道 + 几何设置）
4. 单测快照/恢复回环；快照关闭后 `rg` 验证零残留

---

## 10. 域 U 施工步骤（M-73…M-78，AI-5）

### M-73 辅助功能桥
1. 后端读 SystemParametersInfo（粘滞/滤波键）+ 讲述人进程检测 → 启动事件/变更事件
2. 键位解析层（shortcuts.ts 消费端）支持分步修饰键序列（粘滞语义：单键序列拼装）
3. 讲述人运行 → 自动 `data-reduce-motion=true`（可覆盖）
4. 单测序列拼装；验收粘滞键逐键触发 Ctrl→Alt→E

### M-74 HC 系统跟随
1. 后端 WM_THEMECHANGED 监听 → HighContrast 状态事件
2. 前端高对比档新增「跟随系统」：系统 HC 开 → 环境 HC 主题 300ms 内切换并广播
3. 联动日志；单测联动状态机；验收三档系统 HC 来回

### M-75 动效时长缩放
1. `--motion-scale` 令牌（0.5/1）乘入全部 `--dur-*`（calc 乘法，与 M-40 hfr 系数叠加）
2. 设置项三档（0.5x/1x/交给 reduce-motion 说明）；reduce-motion 优先级最高（CSS 顺序保证）
3. 全局 20 处动效时长断言单测（编译期提取或走查清单）

### M-76 ARIA 审计
1. 15 条主路径组件补 aria-label/role/aria-keyshortcuts；状态变化 aria-live=polite
2. audit.cjs 无头检查：图标按钮（无文本子节点）必须有 aria-label（静态可查项）
3. 覆盖率报告进发版清单；NVDA 人工走查 3 条主路径
4. 注意：四大空间内部不在本轮范围（隔离），报告如实标注范围

### M-77 简繁用户词表
1. settings `s2tLexicon: Record<string,string>`（≤500 条）；s2t.ts 管线最后一级应用
2. 设置页词表编辑器（增删改查 + CSV 导入导出复用 M-62 格式）
3. 冲突词（同简异繁）保存时提示选择；单测转换增量 <5% 性能

### M-78 区域格式跟随
1. 工具函数 `src/lib/format.ts`：Intl 按系统 locale（日期/时间/数字/容量）；30 处显示点走查替换
2. 容量口径设置（二进制 MiB / 十进制 MB，默认系统口径）
3. 单测 20 边界值；验收三区域（en-US/zh-CN/de-DE）走查

---

## 11. 域 Q 施工步骤（M-79…M-87，AI-5/AI-4/AI-3/AI-1 轮值）

### M-79 错误聚合看板
1. ErrorBoundary 上报到 `errBoard.ts` 环形缓冲（500 条：组件/消息/次数/首末时间），退出落盘 JSON
2. PII 清洗（剥离用户输入片段：只保留错误消息与栈）；设置→诊断区看板 Top10 + 一键复制 markdown
3. 单测聚合；验收注入 3 类错误 100 次聚合正确

### M-80 IPC 追踪
1. ipc.ts 埋点（`import.meta.env.DEV` 编译期条件）：命令/耗时/成败 → 环形缓冲
2. `ctrl+alt+f12`（仅 dev 生效：release 下该键位不注册）打开瀑布面板（过滤/标红 >100ms）
3. release 剔除验证：构建产物 `rg "ipcTrace"` 零命中

### M-81 迁移测试基建
1. `fixtures/settings/v{n}.json` 每版一份匿名化样例；矩阵测试逐级迁移断言
2. vitest（前端 settings 迁移）+ cargo test（后端 KV 迁移）双侧
3. 流程纪律：改迁移函数必须同 PR 补「上一版→新版」用例（CONTRIBUTING 注明）

### M-82 视觉回归矩阵
1. visual-audit.cjs 基线结构升级：`{route}/{theme}/{scale}.png` 两维（dark/light/HC × 100/125/150）
2. 基线更新流程文档（PR 必须含 diff 截图）；矩阵跑批 15 路由 = 135 张
3. 阈值按主题标定（HC 对比强，阈值收紧）

### M-83 键位 CI 门禁
1. keymap-audit.cjs（Z-08 交付物）接 pre-commit hook（husky 或手动安装脚本）+ CI job
2. error 级（功能冲突）阻断、warn 级（系统键重叠）要求 PR 说明标签
3. 注入冲突提交被拦截的验收记录

### M-84 性能影响声明
1. `.github/PULL_REQUEST_TEMPLATE.md` 加必填段（三选一：无影响 / <1ms / >1ms+bench 数据）
2. bench 工具 `--snippet` 输出标准化 markdown 片段（可直贴 PR）

### M-85 依赖审计自动化
1. `tools/dep-audit.cjs`：npm audit --json + cargo audit 解析聚合 → `docs/selfcheck/dep-audit-<date>.md`
2. sysmaint 计划任务每周触发；不自动升级（红线注释：升级必须人审）
3. 注入漏洞测试依赖验证可报出（验证后移除）

### M-86 文档链接检查
1. `tools/link-check.cjs`：解析 docs/ 与 README markdown 相对链接与锚点 → 断链清单退出码 1
2. CI job；当前全库断链清零（修复属附带小改动，允许跨域提交）

### M-87 发版演练
1. `tools/release.ps1`（注意 BOM 纪律）：版本号一致性 → 全量测试 → tauri build → NSIS 产物校验 → CHANGELOG 草稿（M-61）→ ROUTES/SETTINGS 再生成（M-55/56）→ 检查清单输出
2. 空跑演练一次输出完整清单归档 docs/acceptance/
3. 版本号不一致注入被拦截的验收

---

## 12. 验收与收口

### 12.1 分波验收门

| 波次 | 验收门（全部通过才进下一波主体） |
|---|---|
| P0 完成 | 12 项全落地 + 全量测试绿 + 视觉基线零退化 + 键位零冲突（脚本证明）|
| P1 完成 | 36 项全落地 + 同上 + 三语 audit 全绿 + soak 首周报告 ≥6/7 |
| P2 完成 | 42 项全落地 + 同上 + 生态文档（ROUTES/SETTINGS/EMBED-MANIFEST）再生成一致 |

### 12.2 收口清单（全部项完成后）

1. `docs/acceptance/summit90-验收.md`：90 项逐条状态（未实测不打 ✅，沿用项目诚实口径）
2. README「二十六、已知边界与路线图」追加 SUMMIT-90 完成度总览
3. CHANGELOG 收口条目
4. 发版演练（M-87）跑通 + NSIS 产物校验
5. 键位审计、i18n 审计、视觉矩阵、soak 四份报告归档
6. 五路进度总览文档同步

### 12.3 回退预案

- 每项功能默认关闭/默认=现状 → 回退 = 关开关，不需要代码回滚
- 涉及数据结构升级（图标缓存、壁纸池、settings schema）的项：迁移函数必须带降级注释与备份先行（M-47 预检联动）
- 涉及共享文件的冲突：以 git 历史最近有效版本为准，`rg` 复核后最小化重放

### 12.4 明确不做清单（重申）

- 不修改写作/思维导图/项目分析/命运推演四应用任何代码
- 不做焚毁/覆写/诱饵类隐私功能
- 不做需要在线服务的功能（天气源、云查杀、在线字体等）
- 不做键位系统自定义外的全局键鼠监听扩张（侧键之外不碰）
- 不做「演示惊艳日常干扰」的动效（媒体呼吸是唯一例外且默认关）

---

## 第五部分：Version-1.2sein298h7mx.00wix（化境计划）实施步骤 · 原文全量

# Version 1.2sein298h7mx.00wix — 「化境计划」实施步骤

> 系列：NEXT-40 → ASCENT-60 → APEX-70 → SUMMIT-90 → **Version 1.2sein298h7mx.00wix（化境计划）**
> 配套文档：《Version-1.2sein298h7mx.00wix-功能全景.md》（100 项功能定义与验收）、《Version-1.2sein298h7mx.00wix-AI分工图.md》（五路分工与文件边界）
> 施工总纲：**八波次推进，每波次「进入条件 → 施工 → 验证门禁 → 回滚预案」四段闭环**；与 SUMMIT-90 / APEX-70 并行在建项完全兼容，互不阻塞、互不回滚。

---

## 0. 全局工程纪律（每一波次、每一项都必须遵守）

### 0.1 开工前置核查（每项 V 功能动第一行代码前）

1. **去重复核**：`rg` 检索本项关键词在四份既有《功能全景》（NEXT-40 / ASCENT-60 / APEX-70 / SUMMIT-90）中的立项情况，确认无重叠；发现重叠即提请立项仲裁，宁可缓做不可重做。
2. **共享文件核对**：涉及 `src/i18n/dictionaries.ts`、`src/lib/ipc.ts`、`src-tauri/src/lib.rs`、`src/system/windows/vwm.ts`、`src/system/StartMenu.tsx`、`desktop.css`、`tools.css` 的，改前 `rg` 核对当前状态（并行 AI 可能刚改过），只增不改他人段落，改后立即跑 `node tools/audit.cjs`。
3. **键位槽位冻结**：本轮所有新键位（V-25/44/46/51/65 等）一律先查 Z-08 键位注册表；注册表未落地期间只允许 `ctrl+alt+` 空闲槽与裸功能键，且必须实现 Z-09 让位检测（系统组合与第三方全局热键优先）。
4. **四大内置应用零修改**：任何 diff 若触碰 `src/apps/write|mind|code|fate/**`、`src/features/editor|mindmap|project|fate*/**`、`src-tauri/src/library|mindmap|project_scan/**`，一律打回。

### 0.2 每项功能的统一交付流程（DoD）

```
实现（默认关闭或默认=现状）
  → 单测/组件测试（vitest / cargo test 对应模块）
  → i18n zh+en 键补全（audit.cjs 过）
  → tsc 零错误、npm run build 通过
  → cargo check 通过；涉及后端逻辑的项跑 cargo test --no-run（教训：lib check 绿 ≠ test 可编译）
  → 共享文件 diff 复核（只增不删他人段落）
  → 验收条目逐项自测（全景文档「验收」小节）
  → 未实测项不打 ✅（B-30 口径）
```

### 0.3 已知地雷清单（历史教训，逐条规避）

| 地雷 | 规避动作 |
|---|---|
| zh i18n 块被并行进程反复回滚 | 每批修改前后 `rg` 复核新增键；每波次收尾再全量复核一次 |
| Edit 工具剥掉 .ps1 的 UTF-8 BOM | 任何 .ps1 编辑后立即写回 BOM |
| 结构体重构后 `#[cfg(test)]` 字面量漂移 | 后端结构体改动必跑 `cargo test --no-run` |
| match 枚举兜底臂吞语义（L1 嵌入 vs L4 让位教训） | 语义分叉处禁用 `_ =>`，显式分臂 |
| 并行编辑共享文件导致回滚冲突 | 共享文件「一次一批、最小 diff、rg 前后核」 |
| Invalid Cargo feature 阻断 cargo check | 不动 Cargo.toml features；新增依赖走评审 |
| 嵌入窗口（L1）的宿主约束 | 窗口功能对 L1 窗口的不可支持项如实置灰（不硬做） |

### 0.4 并行兼容协议（与 SUMMIT-90 / APEX-70 并行施工）

- 本轮不修改 SUMMIT-90 / APEX-70 已立项项的任何文件领地；需要消费其交付物（如 M-21 校验和、M-24 书签条、N-31 使用洞察）时以**软依赖**接入：未交付时功能如实降级或暂缓，不阻塞本项其他部分。
- 文件领地图以《AI分工图》为准；跨路依赖只允许「上游交付物 → 下游消费」，禁止跨路改文件。
- 波次节奏错开：本轮 P2 波（桌面/文件）与 AI-1 SUMMIT 批次不同周开工，降低 explorer 目录并发。

---

## 1. 波次总览

| 波次 | 名称 | 内容 | 主责路 | 预估项数 |
|---|---|---|---|---|
| P0 | 立项核查波 | 去重仲裁、键位槽位冻结、基线快照 | 五路共同 | 0（纯核查） |
| P1 | 地基波 | 令牌档位、共享引擎、键位仲裁依赖 | AI-5 / AI-2 | 8 |
| P2 | 桌面与文件波 | 域 A + 域 D + V-96 | AI-1（+AI-5 协作） | 21 |
| P3 | 窗口与任务栏波 | 域 C + 域 B 任务栏部分 + G 域 AI-2 部分 + V-94 | AI-2 | 22 |
| P4 | 个性化与氛围波 | 域 H + 域 B 开始菜单部分 + G 域 AI-5 部分 | AI-5 | 19 |
| P5 | 效率与开放波 | 域 E + 域 I | AI-4（+AI-1 V-85、AI-3 V-86/87/89） | 16 |
| P6 | 系统健康波 | 域 F + V-91/92/95 | AI-3 | 13 |
| P7 | 收官波 | V-93/97/98/99/100 + 全量验收发版 | AI-1 / AI-5 / 全路 | 5 + 验收 |

> 项数合计 100；波次间存在硬依赖的项目在分工图中标注前置，其余可并行。

---

## 2. P0 立项核查波（进入施工前的一次性动作）

**进入条件**：本文档与功能全景、AI 分工图三件套合入 main。

**施工步骤**：

1. 五路各自对分配项跑 `rg` 去重复核（对照四份既有《功能全景》全部 `###` 标题与既有代码），产出《化境-去重核查表》（每项一行：无重叠 / 软依赖某项 / 冲突提请仲裁）。
2. 冻结本轮键位槽位表：`ctrl+alt+[ ]`（V-25）、`ctrl+alt+n`（V-46）、`ctrl+alt+滚轮`（V-51）、`alt+shift+c`（V-33）等逐一到 Z-08 注册表登记；注册表未落地项在槽位表中标注「暂缓」。
3. 基线快照：记录当前 `npm run build` 产物大小、`cargo check` 时长、bench 归档目录 mtime、audit 输出，作为本轮「零退化」的对照基线。
4. 共享文件基线 commit 打 tag（`wix-baseline`），供波次收尾 diff 对照。

**验证门禁**：去重核查表覆盖率 100%；键位槽位表无与系统组合/第三方热键的已知冲突；基线快照三项数据落档。

**回滚预案**：无代码改动，无需回滚。

---

## 3. P1 地基波（8 项：其他波次的地基，最先做）

**进入条件**：P0 完成。

**项目与顺序**：

1. **V-74 界面密度档位**（AI-5）：扩展 `src/design/tokens.css` 间距缩放层（`[data-density="compact"]`）；全部组件走既有 sp-* 令牌，禁组件级 hack。→ 它是 V-75/76/78 的施工范式样板。
2. **V-75 界面几何风格**（AI-5）：r-* 令牌档位化；与 V-74 同批验证视觉矩阵。
3. **V-78 全局 UI 图标尺寸档位**（AI-5）：SVG 图标令牌化尺寸；与 V-74 正交组合回归。
4. **V-76 界面字体偏好**（AI-5）：font-family 令牌注入 + CJK 回退链测试。
5. **V-22 失联窗口救援**（AI-2）：窗口几何体检函数（后续 V-21/23/24/26/30 的窗口工具函数依赖它的几何计算模块）。
6. **V-31 文件夹视图记忆**（AI-1）：记忆存储模块（后续 D 域多项消费排序/视图状态）。
7. **V-32 即时过滤与命中高亮**（AI-1）：模糊匹配引擎（V-07 桌面敲字、V-39 地址栏跳转复用；V-07 在 P2 接入）。
8. **V-50 键位速查表导出**（AI-4）：注册表读取与导出渲染（V-94 体检医生复用其读取层）。

**施工要点**：令牌三件（74/75/78）必须同一批做完——分批做会导致 desktop.css 三次冲突窗口；每个档位默认值必须逐像素等于现状（对照 P0 基线截图）。

**验证门禁**：tsc 零错误；vitest 含密度/几何/图标尺寸的布局快照用例；`npm run build` 产物大小与基线偏差 <2%；audit PASSED；三档位 × 浅/深/HC 主题九宫格视觉走查零布局破损。

**回滚预案**：令牌档位均为新增 CSS 属性选择器，回滚 = 删除属性分支，零残留。

---

## 4. P2 桌面与文件波（21 项：域 A 全部 + 域 D 全部 + V-96）

**进入条件**：P1 的 V-31/V-32 已合入。

**施工顺序（按触点分批，降低冲突窗口）**：

1. **桌面右键批**（AI-1）：V-01 排列系统 → V-02 系统图标 → V-03 图标锁定 → V-04 双击动作（四个同触点：桌面右键菜单与桌面设置；与 M-15 空区菜单定制的注册机制对接处 rg 先核）。
2. **图标体验批**（AI-1 + AI-5 协作渲染层）：V-05 标签可读性 → V-06 密度档位 → V-07 敲字定位（接入 V-32 引擎）→ V-08 回收站徽标 → V-10 让位 FLIP。
3. **模板与归档批**（AI-1）：V-09 新建模板中心 → V-96 桌面归档建议。
4. **文件列表批**（AI-1）：V-33 复制路径 → V-37 智能选取 → V-38 预览锁定 → V-34 拖拽计数（与 V-36 落点确认协同）。
5. **文件健壮批**（AI-1）：V-36 特殊名防呆 → V-35 属性对比（依赖 M-21 校验和软依赖）→ V-40 提取向导。
6. **导航批**（AI-1）：V-39 地址栏模糊跳转（复用 V-32 引擎 + M-24 书签软依赖）。

**施工要点**：域 A 的桌面图标数据层放 `src-tauri/src/shell/` 新模块（如 `desktop_icons.rs`），不改他人后端；桌面渲染层若需进 desktop.css，只增独立段落；V-08 只读消费 U-27 数据源，绝不写回收站库。

**验证门禁**：桌面 100 图标压力操作全程 60fps；文件批 500 文件目录操作 keystroke 级响应；`cargo test` 新增桌面/文件模块用例全绿；`cargo test --no-run` 全量可编译；audit PASSED；zh/en 全键。

**回滚预案**：每批一个 commit；桌面批次回滚不影响文件批次（不同模块）；共享文件段落回滚按「只删自己段落」执行。

---

## 5. P3 窗口与任务栏波（22 项：域 C 全部 + V-15…V-20 + V-61/65/66/67/70 + V-94）

**进入条件**：P1 的 V-22 已合入；Z-08 键位注册表落地状态确认（未落地项键位暂缓、功能先行）。

**施工顺序（vwm.ts 最小 diff 原则，一批一核）**：

1. **系统菜单与几何批**（AI-2）：V-21 系统菜单 → V-23 几何提示 → V-30 拖拽取消（三者同触点 VirtualWindowFrame 标题栏与拖拽路径）。
2. **编组批**（AI-2）：V-24 多选编组 → V-26 位置互换（V-26 依赖 V-24 编组态）。
3. **导航批**（AI-2）：V-25 焦点回溯（键位走注册表）→ V-28 窗口分布小地图（依赖 capture 基础设施软依赖，未就绪则暂缓截图缩略、先用图标占位如实降级）。
4. **视觉联动批**（AI-2）：V-27 嵌入焦点联动 → V-29 色带标记（默认关，逐像素基线对照）。
5. **任务栏批**（AI-2）：V-15 中键新实例 → V-16 拖放打开 → V-17 溢出折叠 → V-18 运行指示样式 → V-19 关机会话清单 → V-20 电源菜单（V-19/20 依赖 V-52 数据源软依赖，未就绪则首版不带异常关机标记）。
6. **输入手感批**（AI-2）：V-65 大写提示 → V-66 按键重映射（严格走 Z-08/Z-09）→ V-67 触控板方向 → V-70 拖拽阈值 → V-61 鼠标面板。
7. **V-94 键位体检医生**（AI-2）：全量键位收编完成后做（依赖本轮所有键位登记完毕）。

**施工要点**：vwm.ts 每批 diff 前后 `rg` 复核（回滚冲突重灾区）；V-27 绝不改系统焦点语义（仅视觉态）；V-66 重映射对四大内置应用全局生效但不改其任何代码；批次间穿插既有「双击 Esc 切环境」契约回归。

**验证门禁**：窗口批拖拽路径 100 次零误触（摇一摇 M-01 回归同跑）；任务栏批 50 窗口压力测试；V-66 映射矩阵测试（10 组映射 × 系统冲突样本）；`cargo test --no-run` + vitest 全绿；audit PASSED。

**回滚预案**：编组态/色带/提示浮层均为独立状态分支，回滚独立；键位类回滚必须同步注销注册表条目（不留幽灵键位）。

---

## 6. P4 个性化与氛围波（19 项：域 H 全部 + V-11…V-14 + V-62/63/64/68/69）

**进入条件**：P1 令牌三件已合入；开始菜单四项需 StartMenu.tsx 共享文件当前状态核验。

**施工顺序**：

1. **壁纸批**（AI-5）：V-71 本地精选（图片资产入包，体积预算 <15MB）→ V-72 饱和度明度。
2. **时钟批**（AI-5）：V-73 农历节气（本地万年历算法 + 2020–2040 数据表，与 M-12 时钟卡片同族）。
3. **个性化面板批**（AI-5）：V-77 焦点环样式 → V-79 深浅分离（含单开关设置无损迁移）→ V-80 对比度守护。
4. **开始菜单批**（AI-5）：V-11 字母索引条 → V-12 最近/高频分组 → V-13 固定文件夹 → V-14 右键高级操作（StartMenu.tsx 增量，只增段落；拼音归组复用既有索引基础设施）。
5. **指针与反馈批**（AI-5）：V-62 指针方案 → V-63 指针轨迹 → V-64 点击涟漪 → V-69 精确模式 → V-68 打字音效（与 A-4 声音引擎联动，不另开通道）。

**施工要点**：本波全部默认关闭或默认=现状，收尾必须跑「默认态逐像素基线对照」（P0 快照）；V-73 农历数据表用代码生成再校验（含无中气置闰月边界年份）；V-71 图片资产统一 WebP、命名入审计清单。

**验证门禁**：九宫格（3 档位 × 3 主题）视觉矩阵回归；V-73 与标准万年历 60 天抽样一致；默认态基线对照零偏差；audit PASSED（V-73 双语文案量大，重点核查）。

**回滚预案**：每项独立设置键，回滚 = 默认值化；图片资产独立目录，可整目录移除。

---

## 7. P5 效率与开放波（16 项：域 E 全部 + V-81/82/83/84/85/88/90 + V-89）

**进入条件**：P1 的 V-50 导出层已合入；命令面板/运行框触点当前状态核验。

**施工顺序**：

1. **计算与时间批**（AI-4）：V-41 内联计算 → V-49 时间戳速插（同触点：结果卡组件复用）。
2. **取字与传物批**（AI-4）：V-42 屏幕取字 OCR（Windows.Media.Ocr 绑定 + 语言包检测）→ V-43 二维码速递（本地生成库选型走依赖评审）。
3. **粘贴与历史批**（AI-4）：V-44 净化粘贴（全局钩子 + 让位检测）→ V-47 搜索历史三态 → V-48 运行框补全。
4. **面板与速记批**（AI-4）：V-45 宏收藏（N-18 软依赖）→ V-46 全局速记（键位注册表就绪为前置；未就绪暂缓，其余先做）。
5. **开放工具批**（AI-4 + AI-3 + AI-1 协作）：V-81 winget 前端 → V-82 环境变量编辑器 → V-83 计划任务工坊 → V-84 关联快照（AI-4）；V-86 启动延迟 + V-87 服务依赖图（AI-3，taskman 触点，与 AI-1 的 TaskManApp 骨架现状 rg 先核）；V-85 卸载善后（AI-1，扫描白名单制）；V-89 配置对比（AI-3，与 V-84 共用 diff 引擎）。
6. **沙盒试用**（AI-4）：V-90（N-35 沙盒基建软依赖，未就绪则本项整体暂缓，不硬做半成品）。

**施工要点**：V-81 的所有网络行为都发生在 winget CLI 自身，环境侧只做进程调用与输出解析，且每个动作显式确认（零自动出站红线）；V-83 动作白名单在代码层硬编码（非配置可扩展，防任意命令执行）；V-85 与 V-95 的删除语义统一「回收站式可反悔，绝不覆写」。

**验证门禁**：V-42 OCR 30 张抽样准确率 >95%；V-81 全链路 20 例 + 无 winget 降级；V-44 让位矩阵 20 应用；白名单外动作配置测试全部拒绝；`cargo test --no-run` + vitest 全绿；audit PASSED。

**回滚预案**：winget/OCR 均为独立 command，可单独 feature-off；V-83 任务库格式版本化，回滚兼容。

---

## 8. P6 系统健康波（13 项：域 F 全部 + V-91/92/95）

**进入条件**：P3 任务栏批合入（V-19/20 消费 V-52 数据源）。

**施工顺序**：

1. **只读感知批**（AI-3）：V-52 可靠性时间线 → V-53 端口侦探 → V-55 大文件雷达（三者同触点：系统只读查询后端，新 shell 模块 `sysprobe.rs`）。
2. **电源批**（AI-3）：V-54 临时唤醒 → V-51 微步进 → V-91 演示模式（组合 V-54/63/64，退出钩子恢复是红线测试）。
3. **进程与启动批**（AI-3）：V-57 优先级预设 → V-60 启动归因 → V-86 启动延迟（若 P5 未完成则此处收口）→ V-87 服务依赖图。
4. **稳定批**（AI-3）：V-58 更新闲时下载 → V-59 断电自检 → V-56 运行时长。
5. **卸载抉择**（AI-3）：V-95（NSIS 卸载脚本升级，走独立安装器分支验证）。

**施工要点**：sysprobe 全部只读（任何写系统状态的提案一律打回）；V-54 用 SetThreadExecutionState 会话级实现，环境退出自动失效是验收红线；V-95 卸载脚本为 .nsi，修改后必须本地完整构建安装包验证。

**验证门禁**：只读声明 UI 明示；V-52/53 与系统工具输出一致性抽检；V-59 脏关机模拟 10 次；V-91 进出 20 次状态零残留（含进程被杀场景的钩子恢复测试）；安装器构建产物走查。

**回滚预案**：感知类回滚 = 移除面板入口（查询后端保留无害）；V-95 回滚走安装器版本回退。

---

## 9. P7 收官波（5 项 + 全量验收发版）

**进入条件**：P2–P6 全部项合入或如实标记暂缓。

**施工顺序**：

1. **V-93 偏好搬家向导**（AI-1）：系统偏好只读读取 + 向导 UI（首启触发与设置页触发双入口）。
2. **V-97/98 打印双件**（AI-1）：右键打印 → 队列查看器（无打印机环境全程可测的降级路径）。
3. **V-99 依赖诚实声明页 v2**（AI-5）：在 D-5 声明下增外部依赖节，逐项「检测当前状态」按钮。
4. **V-100 收官毕业页**（AI-5）：完成度墙数据从验收矩阵自动生成。
5. **全量验收**：
   - 跑全量门禁：`tsc` 零错误 → `npm run build` → `vitest` 全绿 → `node tools/audit.cjs` PASSED → `cargo check` → `cargo test`（或 `--no-run` + 模块级）→ 键位 CI 门禁（M-83 若已落地）→ 视觉矩阵回归（M-82 若已落地）。
   - 性能对照 P0 基线：产物大小偏差 <5%、冷启动时长偏差 <10%、bench 归档不覆盖当日既有归档（先查 mtime）。
   - 真机验收矩阵：新建《docs/acceptance/wix-化境验收矩阵.md》，沿用 B-30 口径——**未实测不打 ✅**；桌面/窗口/任务栏/文件四条主路径真机走查。
   - CHANGELOG 顶部新增「化境计划」条目（全量 100 项完成/暂缓清单）。
   - 版本号推进：`1.2sein298h7mx.00wix`，tag `v1.2wix`，发版演练走 M-87 流程（若已落地）。

**验证门禁**：上述全量门禁 + 三份文档（全景/步骤/分工）与实际交付一致性复核（完成度墙自动生成即为此设计）。

**回滚预案**：发版问题走增量更新通道回滚（Z-61）；文档完成度墙数据自动刷新，无需手工改。

---

## 10. 里程碑与完成度口径

| 里程碑 | 判定 | 口径 |
|---|---|---|
| M-Wix-1 | P0+P1 完成 | 地基八项全绿 + 去重表 100% |
| M-Wix-2 | P2+P3 完成 | 桌面/文件/窗口/任务栏 43 项 |
| M-Wix-3 | P4+P5 完成 | 个性化/效率/开放 35 项 |
| M-Wix-4 | P6 完成 | 系统健康 13 项 |
| M-Wix-5 | P7 发版 | 100 项闭环（含如实暂缓项），验收矩阵归档 |

- **暂缓不是失败**：依赖未就绪（Z-08 注册表、N-18 宏、N-35 沙盒、capture 基建）的项如实标记「暂缓 + 前置条件」，在毕业页完成度墙灰色展示——诚实优先于好看。
- **每波次收尾**：更新本文档对应章节的状态行（✅ 已完成 / 🔄 进行中 / ⏸ 暂缓+前置），并同步《AI分工图》的协作点状态。

---

## 自检清单

> 本节为整合自检记录，由整合脚本程序化核验。

| 检查项 | 口径 | 结果 |
|---|---|---|
| 原文完整性 | 五份实施步骤原文逐字包含（5/5） | ✅ 程序核验通过 |
| 批次体系 | PB-0…PB-10 / AB-0…AB-12 / Wave 0…5 / P0…P2 / P0…P7 全部保留 | ✅ 程序核验通过 |
| 施工顺序 | 五阶段排布符合五计划递进关系；化境并行声明原文保留 | ✅ 程序核验通过 |
| 纪律有效性 | 各计划验证基线 / 共享文件守则 / DoD / 回滚策略原文有效 | ✅ 程序核验通过 |

**复核命令**：

```bash
rg -n "^## 第.部分：.*原文全量" "docs/ENGINE-Version-01XHI9DN.1.5xw-实施步骤.md"   # 五个部分标签
```

