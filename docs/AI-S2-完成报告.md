# AI-S2 完成报告 · Varix STAR I 安全加固域后段（F186-F200）

> 分工包：《Varix STAR I start.md》G-1 深化设计报告（G-G-16 ~ G-G-30）判据实装层。
> 分工边界：AI-S2 严格限定 F186-F200 十五项；未触碰 AI-S1（F171-F185，`secstar/` 目录）与其他 AI 的任务面。
> 收口状态：**域内全绿**（主仓 `cargo test --lib secstar2::` 96/96 PASS；隔离口径 no_std `--features kernel-image` 零错误）。
> 落位提交：`secstar2/` 15 模块 + `mod.rs` 聚合 + `lib.rs`/`robust.rs` 接线 + `tools/vx-walkcheck-all.py`。

---

## 1. 交付物台账

| 位置 | 内容 | 规模 |
| --- | --- | --- |
| `kernel/varix/src/secstar2/` | 15 项判据实装 + `mod.rs` 域聚合（单聚合注册——288 容量余量约束下的选择） | 16 文件 / 6,838 行 |
| `kernel/varix/src/lib.rs` | `pub mod secstar2;` 挂接 | +2 行 |
| `kernel/varix/src/robust.rs` | `secstar2::run_secstar2_checks` 注册进内核全域体检表（274→275） | +8 行 |
| `tools/vx-walkcheck-all.py` | F200 主册钦定的走查族总集成脚本（解析→配证据→红绿一页纸→季检归档） | 新增 ~280 行 |
| `CHANGELOG.md` | S2 批次条目 | 修改 |
| `docs/AI-S2-完成报告.md` | 本报告 | 新增 |

行数分布（wc -l）：clockguard 567 · logring 564 · resquota 531 · batguard 490 · thermgov 482 · bootaudit 475 · syspart 459 · selfheal2 436 · paramwl 424 · auditchain 416 · walkall 402 · slotview 387 · safemode 385 · recenv 364 · lineage 351 · mod.rs 105。

## 2. 判据对账（15 项 × 主册判据锚 × 验收标准第一句）

| 项 | 判据锚 | 模块 | 行数 | 自检 | 单测 | 实装说明 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F186 系统分区不可见 | G-G-16 | syspart.rs | 459 | 20 | 6 | 枚举过滤（挂载照常、呈现缺席——`enumerate_user_volumes` 零枚举）；管理页全卷表灰显+用途说明；操作按钮=移除不是禁用；越权写拒+审计留痕；哈希坏红显+修复指引（F193/F198 链）；红线不设开关（无解除隐藏 API） | ✅ |
| F187 时区与时钟守护 | G-G-17 | clockguard.rs | 567 | 31 | 8 | SNTP 客户端自研（RFC 4330 构造/解析/kiss-of-death 拒绝）；漂移三档（<2s 静默 / 2-300s 静默校正+记录 / >300s 显式）；无网络用上次好值插值+「估算」标注；RTC 失效三连零→推断+黄条；时区向导三步状态机；mtime 历史真相不动（只换显示层）；NTP 源池 3+1 槽满零静默 | ✅ |
| F188 日志三环 | G-G-18 | logring.rs | 564 | 19 | 6 | 内核环 256KB 预算（满覆盖+头部丢弃洞标记）；系统轮转 10×2MB（封档入账，对拍恒等式 total=Σseg+archived 全程守恒——轮转边界零丢）；应用沙盒配额拒写留账；统一 QPC 时钟（u128 中间量防溢出）；三泳道 O(n) 归并（3.3 万×3 实测有序）；±50ms 对齐校验；洞检测；debug 默认关；导出 manifest 诚实标注缺段 | ✅ |
| F189 静默自愈集 | G-G-19 | selfheal2.rs | 436 | 16 | 7 | 三类损坏（图标缓存/主题令牌/缩略图）注入 5×3 全愈成功率 1000‰；通知报备 100%（每条自愈必有通知）；「连续自愈同项 ≥3 次/周→升级工单」——同项反复自愈即慢性病信号（成功也立票，每 kind×周一次）；失败降级默认态+通知显目；重建策略三分（即时/原子热替换/后台分时）；快照协同（令牌类留、缓存类不留） | ✅ |
| F190 更新双槽可视 | G-G-20 | slotview.rs | 387 | 20 | 7 | 槽卡并排（当前槽强调——公平感）；条款卡主册逐字文案；更新前置空间拦截（计数留痕）；回滚点+保留期 7 天旋钮（1-30 钳制）；到期灰置+「回滚点已按策略清理」（诚实不藏）；回滚全链（执行+校验失败红显）；备用槽损坏不切槽+红显「下次更新将重建」；B-1304 外部对拍接口 | ✅ |
| F191 安全启动链自查 | G-G-21 | bootaudit.rs | 475 | 22 | 6 | 三查依赖序（门表→公钥→W^X，上游坏下游免查且显式标「免查」不冒充绿）；三注入全拦+拦截画面（「已保护」成功防御语义——与 panic 故障语义区分）；预算 60/5/5ms 分配对账；基准损坏→F198 重建指引+事件计数；无基准诚实降级（不伪装通过）；差异详情（基准 vs 实际并排+首分歧字节定位）；哈希原语复用 ksha256（FIPS 180-4 向量锁定） | ✅ |
| F192 内核参数白名单 | G-G-22 | paramwl.rs | 424 | 18 | 7 | 三族 13 参编译期常量（调试 6/降级 3/兼容 4——增补走 ADR）；20 非法样本（拼错 6/注入符 6/超长 1/空名 1/坏值 1/未知 2）全拒且建议准确（Levenshtein ≤2 两行滚动零堆实现）；越界值钳制+警告分账（rejections=17 / clamps=3 两条账各归各）；单参 128 字符/总长 1KB 双防线；字符集注入防线；safe-mode 即降级族成员（一处一事实） | ✅ |
| F193 安全模式 | G-G-23 | safemode.rs | 385 | 27 | 6 | 进入唯一源=内核参数（无参拒绝——一处一事实）；黄条常驻+原因随来源（菜单手选/异常关机/参数直进三路各有文案）；12 项最小集白名单（清单公开，救援四要件必在）；白名单外灰置且可解释；更新禁用（防半态更新）；低分辨率 1024×768 GOP 保底（F056 旁路）；连续两次异常关机→询问+接受给参/拒绝清态；退出=正常重启无残留 | ✅ |
| F194 审计日志完整性 | G-G-24 | auditchain.rs | 416 | 15 | 6 | SHA-256 序号链（节点=序号/时间戳/事件体/前哈希四字段，哈希=SHA-256(全字段拼接)）；篡改注入矩阵：改一条/删一条/插一条（删插后重排序号的真实篡改者行为）全检出且定位；跨日续链（`continued(prev_head)`——昨日链头为 genesis）+锚不符即断；导出包三方独立校验（~20 行可读校验器随包）；超长事件拒绝不截断（拆条重试语义）；磁盘满账本（安全日志 P0 优先） | ✅ |
| F195 资源配额执行 | G-G-25 | resquota.rs | 531 | 25 | 6 | 三维配额（内存/IO 权重/进程数）；四级阶梯顺序对账（缓存回收→IO 降权→增长冻结→硬顶拒绝，跳级不可能、顶格不复燃）；后台应用触发线 ×0.8 优先降（硬顶不偏置——安全边界不松动）；豁免清单 6 项零误伤；掐增量不掐存量（fork 拒绝）；动作流水全记+通知按应用 5min 窗合并（两条账分开）；仪表条绿黄红三段（60%/85%/100%）；放宽需 F038 式确认 | ✅ |
| F196 电池保护策略 | G-G-26 | batguard.rs | 490 | 23 | 7 | 两级阈值（15%/5%，界内 10-20/3-10 可调+交叉防呆）；30s 滑动平均滤波（跳变 >8‰ 判噪丢弃——老化电池跳变永不误触发关机）；60s 倒计时状态机（Normal→Warned→Countdown→Flushing→Handoff）；取消路径即时（倒计时与冲刷段均可逆）；关机账目完整（原因=低电/强制，取消也入账——B-2902 对拍源）；冲刷卡死 30s→强制下电+修复族；保护清单 4 项+「估算」标注+琥珀警示卡语汇（与 F173 panic 族区分） | ✅ |
| F197 温度感知降档 | G-G-27 | thermgov.rs | 482 | 19 | 7 | 三档阈值（75/85/95℃）+迟滞 5℃（触发-回退差——84/86 交替 20 轮零震荡实测）；升档即时降档逐级（渐升温 86℃ 必达 Notify——含 Throttle→Notify 升级路径的回归测试）；读数异常滤波（负值/超界/秒间跳变 >20℃ 丢弃）+**连续 3 次拒绝重定基线**（基线卡死会让真实过热永远读不到——那比噪声更危险）；中途失效优雅退出+一次通知；graceful 跳过（不可读机型整体旁路）；温度强制优先于 F048 手动档（拒绝+计数）；60s 窗+24h 聚合双视图；95℃ 冲刷复用 F196 管线（一套机制两处用） | ✅ |
| F198 恢复环境 | G-G-28 | recenv.rs | 364 | 23 | 6 | 三卡（修复引导/回滚配置/导出日志，480×140 主册尺寸）；引导修复=闸门三条件重检+成败两路诚实；还原点损坏跳过+标注（不赌）；**零写问题盘**（目标盘检查拒绝+尝试计数审计——5 次尝试 0 实写）；无第二 U 盘→二维码降级（F173 同款）；两级降级（三卡→纯文字菜单→最简修复单命令 `fixboot`）+降级到底不伪装成功；16px 最小字号；快照策略（导出卡只读不快照） | ✅ |
| F199 版本与谱系页 | G-G-29 | lineage.rs | 351 | 16 | 5 | 版本树 seq 排序+当前节点「运行中」标；6 节点可见+「+N」折叠；构建指纹 hex12（复制钮数据源，二次生成一致性=保真判据）；构建记录对拍（semver+指纹逐字段——R8 腐化网）；双槽态并显（F190 联动）；本地构建诚实标注（`localBuild:true`）；开放 JSON 手写序列化（F128 同语言，键序稳定可复现，F130 组件清单内嵌） | ✅ |
| F200 全域总检 | G-G-30 | walkall.rs + vx-walkcheck-all.py | 402 | 20 | 5 | 锚点登记（F 编号唯一，重复拒绝）；覆盖率 1000‰（199 项全注册实测）；红绿判定=覆盖+证据在+未过期（TTL 90 天——过期绿按红，附录 D 纪律全域化）；红绿一页纸按七域分组+冻结五项排除考核（冻结≠失败）；季检三栏（新增/废止/冻结）+「现实契机」四问门槛（四问全过才立项）；候删五项冻结名单在册（F101/F104/F112/F145/F154）；开放 JSON 数据契约；Python 脚本解析分工书 199 项表格→配证据→一页纸渲染→季检归档（reports/walkcheck/） | ✅ |
| **合计** | | **16 文件** | **6,838** | **315** | **96** | | **全绿** |

## 3. 行数对账（诚实口径）

实测 6,838 行 vs 主册上限合计 35,880 行（19%）。对齐团队既有口径：AI-K1 交付 8,514/87,425 ≈ 10%，AI-K2 交付 17,733/72,085 ≈ 25%。本域按「每项判据全覆盖、零占位、零死代码」收口，行数随判据复杂度自然分布（复杂判据项如 clockguard/logring/resquota 500+ 行，轻判据项 350+ 行），不注水、不为凑行数拆分包装。

## 4. 自检与测试规模

- **域自检**：`run_secstar2_checks()` 聚合 15 模块子集，每模块 CheckSet 15-31 条（总 315 条），全部实算（非声明）。
- **单元测试**：96 个 `#[test]`（宿主 `cargo test --lib secstar2::` 直跑），覆盖判据正向路径+异常路径+边界（溢出/环回/钳制/降级到底/篡改矩阵）。
- **双口径编译**：宿主测试口径（std）与内核镜像口径（`--features kernel-image` no_std）双双零错误——`vec!/format!` 一律 `alloc::` 显式导入，镜像目标不破。
- **F200 脚本自检**：`python tools/vx-walkcheck-all.py --selftest` PASS；实跑产出 199/199（100% 覆盖）红绿一页纸，148 红项如实暴露（各分队证据未挂——这正是季检一页纸的职责）。

## 5. 验收十二查对照（通用查，每项必过）

| # | 查什么 | 本域执行 |
| --- | --- | --- |
| 1 | 功能完整 | 主册【功能定义】逐条落位（对账表「实装说明」列） |
| 2 | 无感标准 | 判据数值全部常量化（一处一事实：迟滞 5℃/阈值 15-5%/窗口 30s/60s 倒计时/16px/480×140…） |
| 3 | 性能线 | <100ms 自查预算逐项对账；三环对齐 ±50ms；10 万条归并 O(n) 实测；QPC 换算 u128 防溢出 |
| 4 | 4K 走查 | 本域为策略/数据层，视觉锚点（卡尺寸/字号/配色语汇）常量化交 UI 层验收 |
| 5 | 可调三通则 | 旋钮全部带界（阈值/保留期/升级阈值/合并窗）+钳制+越界审计 |
| 6 | 三落位登记 | 策略层常量即登记面（调优走旋钮、红线不设开关——F186 显式声明） |
| 7 | 导航路径链 | 进入路径三处（F198）/向导三步（F187）/询问-接受-带参（F193）链路实测 |
| 8 | 说明句 | 通知/提示全部三要素文案常量（主册逐字优先） |
| 9 | 回归零破坏 | 主仓 `secstar2::` 96/96 绿；未动其他域任何代码（lib.rs +2 行/robust.rs +8 行） |
| 10 | 台账与证据 | 本报告+walkcheck 脚本产出（日期在 `generated` 字段） |
| 11 | 最丑角落 | 见 §6 缺陷账本——施工中 6 处自抓算术/语义错全部修正；诚实列出 |
| 12 | 收工五勾 | F200 walkcheck 脚本逐项登记（覆盖/判据/证据/TTL/冻结） |

## 6. 缺陷账本（施工中自抓自修——诚实入账）

| # | 现象 | 位置 | 严重度 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | `clockguard` 测试只填 SNTP transmit 未填 receive，p2 饱和到 u64::MAX 后 ×1000 溢出 panic | clockguard.rs | 🟡 | 补 receive 字段+移除双重缩放 |
| 2 | `logring` 轮转封档未清段账，对拍恒等式被环回复用打破 | logring.rs | 🔴 | 封档即清账（archived 语义修正） |
| 3 | `logring` QPC 换算把 ns 当 ticks 用，非 1GHz 频率全错 | logring.rs | 🔴 | 改 ticks 语义+u128 中间量 |
| 4 | `syspart`/`paramwl`/`clockguard` 检查算术错（审计计数/池容量/时区静默/DST 语义）共 6 处 | 多处 | 🟡 | 全部修正并对账注释 |
| 5 | `resquota` 通知合并错把「动作」当「通知」合并（升级通知被吞） | resquota.rs | 🟡 | 动作流水与通知流分离 |
| 6 | `thermgov` 基线卡死（跳变拒绝后 last_temp 不更新，真实高温永远读不到）+ Throttle→Notify 渐升温升级路径缺失 | thermgov.rs | 🔴 | 连续 3 次拒绝重定基线+升档直达最高触发档 |
| 7 | `batguard` recover() 不处理 Warned 相态，toast 一次性标志永久卡死 | batguard.rs | 🟡 | Warned 纳入可恢复态 |
| 8 | 并行会话在建代码（deskstar/h1star/genstar2）间歇挡主仓编译 | （他人任务面） | ⚪ | 建隔离校验 crate（`_attic/ais2-scratch/`，真实文件拷贝+no_std 口径镜像）保验证循环不停——收口以主仓全绿为准 |

🔴=判据语义级（会产出错误行为）；🟡=测试/账本级；⚪=环境事项。

## 7. 边界声明

- **未触碰**：`secstar/`（AI-S1 的 F171-F178 在建文件）、`deskstar/`、`h1star/`、`genstar2/`（并行会话在建）、以及一切既有模块的实现逻辑。
- **接入点**：`lib.rs` +2 行（模块声明）、`robust.rs` +8 行（域注册 274→275 及注释同步）——最小侵入。
- **依赖复用登记**（跨分队借力，分工书对账纪律）：`crate::checks::CheckSet`（自检框架）、`crate::ksha256::sha256`（F194 哈希链/F199 指纹）、`crate::star::sbase::{KnobReg 语义, RingLog, MinuteBook}`（K2 共享底盘——旋钮/环账/分钟账本）。
- **非功能产物**：隔离校验 crate 收于 `_attic/ais2-scratch/`（含临时 Cargo 工程）——按规程不入交付面。

## 8. 复现口令

```bash
# 域内全量（96 测试）
cd kernel && cargo test --lib secstar2::
# 内核镜像口径（no_std 编译验证）
cd kernel && cargo build --features kernel-image --lib   # 或 cargo check --features kernel-image
# F200 总检脚本（红绿一页纸 / JSON / 季检归档 / 自检）
python tools/vx-walkcheck-all.py
python tools/vx-walkcheck-all.py --json --archive
python tools/vx-walkcheck-all.py --selftest
```

> 附注：收口时点主仓存在并行分队在建代码（deskstar/h1star/genstar2 等），全量套件的偶发红项以各分队收口为准；本域判据以 `secstar2::` 过滤面与本报告对账表为准。

---

# v2 深化批次（2026-09-26 第二轮收口）

> 分工包：同 v1（F186-F200）；本轮依据分工图铁律 #1「收工实测功能代码低于
> 目标 90% 即回炉补深化」，对齐 AI-J1/AI-H4 深化批次先例，把主册 G-G-16~
> G-G-30 的【交互设计】【数据与存储】【状态与异常】【设计细节】细节条款
> 全展开为真实功能面。分两段施工：F186-F192（会话一）+ F193-F200（会话二）。
> 收口状态：**域内全绿**（隔离舱 195/195 单测 PASS；宿主 std 与 kernel-image
> no_std 双口径零错误零警告）。

## v2-1. 交付物台账（本轮新增）

| 位置 | 内容 | 规模（文件总行，v1→v2） |
| --- | --- | --- |
| `secstar2/syspart.rs` | 深化一：MountTable 挂载通路 / 帮助篇 / 布局对拍器 / 体验日志 / RepairFlow / 渲染数据 | 459→1,056 |
| `secstar2/clockguard.rs` | 深化一：同步引擎（失败连击/退避/跳过账）等 | 567→988 |
| `secstar2/logring.rs` | 深化一：细节条款面 | 564→1,006 |
| `secstar2/selfheal2.rs` | 深化一：细节条款面 | 436→880 |
| `secstar2/slotview.rs` | 深化一：细节条款面 | 387→844 |
| `secstar2/bootaudit.rs` | 深化一：细节条款面 | 475→882 |
| `secstar2/paramwl.rs` | 深化一：FuzzGen/ADR/帮助文档/审计行/族语义执行器 + **三缺陷修复** | 424→925 |
| `secstar2/safemode.rs` | 深化二：选单 Shift 门 / RepairFlow / F126 公开面 / 参数回读 / StrikeBook / 会话账 | 385→813 |
| `secstar2/auditchain.rs` | 深化二：脱敏前移 / 报告渲染 / 隔离保全 / AppendOnlySeal / 每日锚定行 | 416→884 |
| `secstar2/resquota.rs` | 深化二：机型档推导 / 通知四段 / OOM 交接 / 新鲜度 / 仪表契约 / 放宽门卫 | 531→882 |
| `secstar2/batguard.rs` | 深化二：打勾状态机 / AC 时延对账 / toast 载荷 / 诊断行 / 可逆地图 | 490→832 |
| `secstar2/thermgov.rs` | 深化二：托盘角标 / 节奏对账 / 页面模型 / 回落归因 / 冲刷交接 | 482→856 |
| `secstar2/recenv.rs` | 深化二：进入路径三处 / 脱网硬门 / 快照账 / 导出 manifest / 列表渲染 | 364→670 |
| `secstar2/lineage.rs` | 深化二：时间线模型 / 指纹完整行 / 组件表 / 保留期联动 / 清单对拍 | 351→659 |
| `secstar2/walkall.rs` | 深化二：归档版本化 / 决议执法 / 腐化警报 / 证据查询 / 覆盖缺口 | 402→696 |
| `secstar2/mod.rs` | 聚合器挂接 15 深检（基检+深检同表，30 行聚合） | 105→106 |
| `_attic/ais2-scratch/` | 隔离校验 crate（真实文件镜像；双口径验证环境） | 非功能产物，收于 _attic |
| `docs/AI-S2-完成报告.md` | 本报告 v2 章节 | 本节 |

## v2-2. 深化对账（15 项 × 深化面 × 自检/单测）

| 项 | 模块 | 深化自检 | 深化单测 | 状态 |
| --- | --- | --- | --- | --- |
| F186 | syspart | 30 条 | 6 | ✅ |
| F187 | clockguard | （会话一收口） | — | ✅ |
| F188 | logring | （会话一收口） | — | ✅ |
| F189 | selfheal2 | （会话一收口） | — | ✅ |
| F190 | slotview | （会话一收口） | — | ✅ |
| F191 | bootaudit | （会话一收口） | — | ✅ |
| F192 | paramwl | 26 条 | 7 | ✅ |
| F193 | safemode | 32 条 | 6 | ✅ |
| F194 | auditchain | 34 条 | 6 | ✅ |
| F195 | resquota | 30 条 | 5 | ✅ |
| F196 | batguard | 29 条 | 4 | ✅ |
| F197 | thermgov | 24 条 | 4 | ✅ |
| F198 | recenv | 24 条 | 4 | ✅ |
| F199 | lineage | 24 条 | 4 | ✅ |
| F200 | walkall | 22 条 | 4 | ✅ |
| **合计** | 16 文件 | 15 基检块 + 15 深检块 | **195 单测** | **全绿** |

## v2-3. 行数对账（诚实口径）

实测 **12,979 行**（16 文件合计）vs v1 的 6,838 行——本轮净增 **6,141 行**。
对主册上限合计 35,880 行为 **36.2%**（v1 为 19%）。深化方向对齐团队先例
（AI-J1 v2 达成率 25.3% 如实呈报、AI-H4 v2 同口径）：每一行都是主册细节
条款的直接实现，零注水零占位零死代码（双口径零警告背书）。剩余差距的
主要构成是主册【工程量】区间含 UI 呈现层工程量（本域为策略/数据层落位，
UI 面随 H 域控件件接入时再摊销），如实登记不掩盖。

## v2-4. 缺陷账本 v2（两段施工自抓自修——诚实入账）

| # | 现象 | 位置 | 严重度 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | paramwl 注入样本查表对无 `=` 条目缺分支，注入串回退成 BadValue 而非 Illegal（fuzz 注入专项测试红） | paramwl.rs | 🔴 | 补 `strip_prefix` 形态分支 |
| 2 | ADR 批准参数不进解析器（AdrLedger.overlay 与 ParamWhitelist 断路——`apply` 后 `check_token` 仍 NoSuchParam） | paramwl.rs | 🔴 | 新增 `attach_adr` 生效面+覆盖层查找序（覆盖层→编译期表） |
| 3 | `overlay_static` 名单外冒名返回 earlyprintk（静默错配） | paramwl.rs | 🟡 | 改 Option 语义，名单外诚实 None |
| 4 | `propose` 不查待批条目重名（同参数可并列立案） | paramwl.rs | 🟡 | 重名防线补第三层（entries 查重） |
| 5 | `value_range` 缺 `=`：`0..=5` 渲染成 `0..5`（文档契约违约） | paramwl.rs | 🟡 | 补 `..=` 三字符版式+越界防呆 |
| 6 | 归因器单槽模型配不了多级连升（渐升温三档连续 up 后逐级 down 配对错乱） | thermgov.rs | 🟡 | 改按「离开档位=进入档位」配对的挂起栈 |
| 7 | 指纹完整行对 hex12 缓冲二次编码（输出 ASCII 后再 hex 一遍→24 字符乱码） | lineage.rs | 🟡 | 直推字符不重编码 |
| 8 | 测试曲线步幅 >20℃ 被本域跳变滤波拦截（角标/页面/归因四处自相矛盾——自己忘了自己写的滤波） | thermgov.rs | 🟡 | 渐升温曲线重排（≤20℃ 步幅） |
| 9 | 深化测试三处 `unused_mut`/`useless_cmp` + 上一会话遗留死方法 `SyncEngine::streak` + 五处死导入（零死代码纪律清账） | 多处 | 🟢 | 全部清除；双口径零警告 |
| 10 | 主仓并行分队在建代码（lib.rs 重复 ustar3 声明 / genstar2 / deskstar）间歇挡 `cargo test` | （他人任务面） | ⚪ | 沿用隔离校验 crate 口径验证——收口以主仓全绿为准（既有先例） |

## v2-5. 复现口令

```bash
# 域内全量（195 单测，隔离舱=真实文件镜像）
cd _attic/ais2-scratch && cargo test --lib
# 内核镜像口径（no_std 编译验证，零警告）
cd _attic/ais2-scratch && cargo build --features kernel-image
# F200 总检脚本（红绿一页纸 / JSON / 季检归档 / 自检）
python tools/vx-walkcheck-all.py
python tools/vx-walkcheck-all.py --json --archive
python tools/vx-walkcheck-all.py --selftest
```

> 附注：主仓当前存在并行分队在建代码（ustar3 重复声明 / genstar2 /
> deskstar 三处编译红，均非本域文件），按 v1 先例以隔离舱双口径为验证
> 基准；主仓侧全量认证待并行分队收口后统一进行。

---

# v3 批次（2026-09-26 第三轮收口 · 大量深化 + 检查项对账）

> 分工包：同 v1/v2。本轮 = **15 模块全部第三轮深化**（45 个新功能面，全部
> 取自主册 G-G-16~G-G-30 尚未展开的细节条款）+ **检查项全量对账机器钉数**。
> 收口状态：**域内全绿**（隔离舱 242/242 单测；宿主 std + kernel-image
> no_std 双口径零错误零警告）。

## v3-1. 新增功能面（15 模块 × 3 面）

| 项 | 模块 | v3 新增面（主册条款源） |
| --- | --- | --- |
| F186 | syspart | LetterAssigner 盘符分配隔离（隐藏卷从源头不进池——24 位耗尽诚实报错）/ VolumeAuditTrail 全事件审计（注册/剥符/拒配/越权/对拍五类可回放）/ CapacityReport 容量水位（<70/90 两段位） |
| F187 | clockguard | HistoryRows 校时历史行（时刻/来源/偏移/性质四字段——估算优先标注）/ PoolHealth 源池健康度（全灭诚实、空池≠全灭）/ TzSwitchChecklist 时区切换四项联动对账（重复回报拒——假勾进不来）/ RtcInferBanner 推断黄条 |
| F188 | logring | LevelCensus 级别五档统计（与 LogQuery 过滤互证）/ ExportNaming 导出命名契约（zip 四件固定名）/ HoleTooltip 洞标记查询（真灌满 256KB 环触发覆盖取证） |
| F189 | selfheal2 | FallbackMap 降级默认态映射（三类兜底+人话——穷尽匹配）/ SnapshotAudit 快照协同对账（令牌留/缓存豁免带因）/ NoticeEscalation 通知升级显目（成功低档/失败抢眼） |
| F190 | slotview | TermsCard 条款卡（主册逐字+动态天数行）/ ExpiryCountdown 到期预告（0 天临界——灰置有预告）/ ExternalDiffReport B-1304 差异定位（版本差/校验差逐槽） |
| F191 | bootaudit | BudgetReport 预算对账（三查逐行+总账+超支定位）/ SkipSemantics 免查标记（显式标记不冒充绿）/ InterceptVerdict 拦截语义机检（panic 词表出现即红） |
| F192 | paramwl | SuggestCoverage 建议覆盖矩阵（距离 1 全命中+距离 2 统计——确定性样本）/ FamilyDocPage 三族文档页（6/3/4 定 census）/ audit_stream_line 审计流整行（四段格式+F188 解析契约） |
| F193 | safemode | BannerRender 黄条渲染（右下角锚+不可关机检字段）/ HELP_ARTICLE 帮助篇三段（在什么模式/能做什么/怎么出去）/ GateMatrix 功能门矩阵（100 功能压测守恒） |
| F194 | auditchain | SegmentExport 隔离段导出（前后段各自成包各自校验）/ P0Ticket 工单契约（三要素+定位+隔离算术守恒）/ RedactStats 脱敏统计账（三规则累计） |
| F195 | resquota | QuotaConfigStore 配置持久化（encode/decode 往返等值+倒置钳正）/ LadderDocPage 阶梯文档（四级定序动作互异）/ RelaxAuditLog 放宽审计（未确认拒收双防线） |
| F196 | batguard | DrainEstimator 放电预估（斜率外推+零斜率诚实拒绝）/ CardRender 提示卡三区（倒计时环/取消钮/清单勾态零自持）/ DiagnosticExport 账目导出（行数守恒式） |
| F197 | thermgov | DailyStats 24h 统计（峰值/均值/超阈时长）/ EventMarker 事件-曲线对齐（abs_diff 容差=采样纪律，容差外不造点）/ SensorDiag 诊断页（四账+200‰ 红线） |
| F198 | recenv | RepairReport 三条件报告（逐行红绿定位）/ ReturnStack 返回栈（根恒在+8 层上限+乱点压测）/ EntryAudit 入口审计（三路径可溯） |
| F199 | lineage | JsonShapeCheck 形状校验器（自家 JSON 过自家校验——四查闭环）/ TIMELINE_LEGEND 图例（三态语义）/ copy_sim 复制保真模拟（三源一致） |
| F200 | walkall | quarterly_section 季报第五节（环比三态文案不撒谎）/ batch_import 脚本批注册（幂等握手 199 项端到端）/ VERDICT_LEGEND 四态图例（与 verdict 语义一致） |

## v3-2. 检查项对账（机器钉数）

对账由 `secstar2::check_ledger()` + 测试 `secstar2_check_ledger_reconciled`
机检（数字改动必须同步断言——报告数字与代码永不脱节）：

| 模块 | 基检 | 深检(v2) | 深检(v3) | 小计 | | 模块 | 基检 | 深检(v2) | 深检(v3) | 小计 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F186 | 20 | 27 | 16 | 63 | | F194 | 24 | 23 | 12 | 59 |
| F187 | 31 | 27 | 18 | 76 | | F195 | 25 | 23 | 9 | 57 |
| F188 | 19 | 20 | 11 | 50 | | F196 | 23 | 24 | 12 | 59 |
| F189 | 30 | 25 | 12 | 67 | | F197 | 19 | 17 | 11 | 47 |
| F190 | 20 | 22 | 9 | 51 | | F198 | 23 | 15 | 11 | 49 |
| F191 | 22 | 21 | 14 | 57 | | F199 | 16 | 16 | 9 | 41 |
| F192 | 18 | 25 | 9 | 52 | | F200 | 20 | 19 | 11 | 50 |
| F193 | 27 | 29 | 12 | 68 | | **合计** | | | | **846** |

- **总检查项 846**（机器实测：基检 337 + 深检(v2) 333 + 深检(v3) 176；
  v1 报告曾记 315 为基检数——本轮机器对账发现并如实更正，以 `check_ledger()`
  实测为准），全部实算非声明。
- 聚合器恒 15 行（每模块一行，行内三表合并判定），`secstar2_aggregate_rows_15_within_capacity` 机检。
- 每块 CheckSet ≤64 容量逐块机检（`check_ledger` 断言），截断即红。
- 单元测试 **242 项**全绿（v1 96 → v2 195 → v3 242）。

## v3-3. 行数对账（诚实口径）

实测 **16,084 行**（16 文件）vs v2 的 12,979——本轮净增 **3,105 行**。
对主册上限 35,880 行为 **44.8%**（v1 19% → v2 36.2% → v3 44.8%）。
策略/数据层判据已三轮全覆盖；与上限的剩余差距主要是【工程量】区间中的
UI 呈现层工程量（按分工书属 H 域控件件接入时摊销的面），如实登记。

## v3-4. 缺陷账本 v3（施工中自抓自修）

| # | 现象 | 位置 | 严重度 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | 校时历史标签边界：恰好 300s 被我标成显式（主册是 2-300s 静默闭区间） | clockguard.rs | 🟡 | 边界对齐主册+补闭区间机检 |
| 2 | 洞标记检查伪造状态（推 2 条就想要洞——洞只在环满覆盖时产生） | logring.rs | 🟡 | 真灌满 256KB 环取证 |
| 3 | 聚合器注释与机检口径不符（说 45 行实为 15 行——每模块一行合三表） | mod.rs | 🟡 | 注释对齐+行数机检钉死 |
| 4 | 对齐容差用 saturating_sub 导致恒真匹配（995s 远点被错配） | thermgov.rs | 🔴 | 改 abs_diff 单条件 |
| 5 | 统计阈值计数手滑（5 点序列数成 3 点越线） | thermgov.rs | 🟡 | 重新对账+注释点位 |
| 6 | no_std 双口径破功（v3 七文件缺 alloc 导入/vec! 宏） | 多处 | 🟡 | 全部补齐——双口径零警告恢复 |
| 7 | 复制模拟返回值未用（is_some 裸语句） | lineage.rs | 🟢 | let _ 显式丢弃 |
| 8 | 批注册测试类型链错（Vec<&str> 收集元组） | walkall.rs | 🟡 | 类型对齐 |
| 9 | 主仓并行在建代码仍挡编译（h1star 非 ASCII 字节串——他人任务面） | （他人） | ⚪ | 沿用隔离舱口径，收口以主仓全绿为准 |

## v3-5. 复现口令

```bash
cd _attic/ais2-scratch && cargo test --lib           # 242/242 全绿
cd _attic/ais2-scratch && cargo build --features kernel-image   # no_std 零警告
cd _attic/ais2-scratch && cargo test dump_ledger -- --nocapture # 45 块逐块计数
python tools/vx-walkcheck-all.py --selftest          # F200 脚本自检 PASS
```
