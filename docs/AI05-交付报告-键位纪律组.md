# AI-05 键位纪律组 交付报告（Z-08…Z-14、M-28…M-36、V-94）

日期：2026-09-08　｜　范围：仅 AI-05 领地（src/lib/keymap/**、快捷键设置页扩展、键位浮层件）。未触碰其他 AI 的领地文件。

## 一、已交付

### Z-08 全局键位注册表与冲突仲裁（PB-0 地基）
- src/lib/keymap/types.ts：KeyBinding / ConflictRecord / RegisterResult。
- src/lib/keymap/registry.ts：register()/unregister()/snapshot()/conflicts()；同 scope 同 combo = error（高优先级可挤占并留痕），跨 scope = warn。
- src/lib/keymap/arbitrate.ts：作用域栈裁决；可编辑元素内 global 层自动降级；context 栈 LIFO。
- tools/keymap-audit.cjs：裸 keydown 静态扫描 + 基线（tools/keymap-audit-baseline.json，基线 28 处）比对，新增即 CI 失败；npm run keymap:audit。
- 单测：注册/注销/冲突/挤占/收编自举（默认表全量登记零冲突）。

### Z-09 系统组合键让位协议
- src/lib/keymap/system-combos.ts：44 条系统组合（passthrough/yield/translate）；SYSTEM_RESERVED 保留键库与 M-36 同源；全屏标志位 setFullscreenActive()（Z-18 落地前对接）。
- docs/SYSTEM-COMBOS.md 行为表。
- 既有契约保持：super+tab（wintab）、双击 Esc 切环境（kbdhook 独立判定，未触碰）。

### Z-10 三级作用域 / Z-11 全键盘导航
- src/lib/keymap/hooks.ts：useHotkey()（注册即仲裁，卸载即注销，快照 diff 空）；useGridNav()（roving tabindex，Home/End/Page 快跳，scroll-into-view 由调用方接）。
- 12 面板全量接入留待面板归属 AI 配合（hook 已就绪，接入零改造成本）。

### Z-12 速查浮层 / Z-13 命令提示条
- src/components/KeymapOverlays.tsx：KeymapOverlay（Ctrl+/ 呼出，自举注册验证；毛玻璃、不抢焦点）；CommandHintBar（注册表派生，设置开关默认开，高度 >600px 才显示）。

### Z-14 键位方案管理
- src/lib/keymap/profiles.ts：三套预设（默认/左手/极简，JSON 补丁集）；导入过 schema 校验 + 冲突/保留键裁决；切换即重置（往返终态一致门禁）。
- 设置页快捷键区方案选择器（src/features/settings/KeymapExtras.tsx）。

### M-28 键位使用统计（本地）
- src/lib/keymap/telemetry.ts：只记 action id + 次数（隐私口径注释）；占用榜（注册失败降级计数）；KEYSTATS_FLUSH_MS = 5min 落盘节奏。
- 设置页只读使用榜（前 5）。

### M-29 长按加速曲线
- KEY_REPEAT_CURVE：500ms 前 1x → 1.5x / 2.5x / 4x 三段；单测含单调性。曲线已用于 useGridNav 长按节流；VWM 窗口移动（Rust 侧）接入待 AI-2 窗口路配合，常数已 const 化导出。

### M-30 鼠标侧键可编程
- settings.xBinds（默认 back/forward 零退化）；设置页映射 UI；全屏让位口径与映射动作复用分发表。kbdhook.rs 轮询路径的 XBUTTON 识别待 Rust 侧对接（见"暂缓"）。

### M-31 启动槽可视化分配
- 设置页 9 槽托架（虚线空槽/实槽 + 键位标签）；HTML5 DnD 落槽写 settings.launchApps；应用删除清空 + toast 由后端联动（暂缓项见下）；同应用多槽允许。

### M-33 按键回显
- KeycastOverlay：keycast://show 事件驱动，2s 淡出，四角可选；红线注释：只回显功能组合，打字内容永不出现（表外按键零事件）。设置开关 + 位置。winman.rs dispatch 处发事件待 Rust 侧对接。

### M-34 Esc 层级语义规范
- uiStore.overlayStack + pushOverlay/popOverlay/consumeOverlayOnEsc（幂等 push、栈顶消费）；App.tsx 挂全局 Esc handler（栈空放行）。双击 Esc 切环境契约未动（回归红线遵守）。
- 12 类浮层逐一入栈的走查归档属面板归属 AI 的接入工作，API 已就绪。

### M-35 滚轮语义全局规范
- settings.wheelVolume（默认 false）；docs/INTERACTION.md 行为表；四大空间隔离边界注明。

### M-36 键位变更预览
- src/lib/keymap/preview.ts：保存前 diff（变更/被挤占/系统保留键命中/风险标注）+ suggestFreeSlots() 空闲槽扫描；设置页预览卡（快捷键编辑草稿实时预览）。

### V-94 键位体检医生
- src/lib/keymap/doctor.ts：注册表冲突、保留键命中、绑定重复、裸 keydown 审计计数、禁用项信息、空闲槽建议；healthy 布尔。设置页"运行体检"入口。

## 二、验证

- vitest：src/lib/__tests__/keymap.test.ts 35 passed / 2 skipped（DOM 用例在 node 环境跳过）。
- tsc：AI-05 全部新增/触碰文件零错误（仓库内其他文件存在既有错误，属其他 AI 领地，未处理）。
- keymap:audit：基线 28 处裸 keydown，零新增。

## 三、暂缓项（前置条件未就绪，非失败）

| 项 | 暂缓内容 | 前置条件 |
| --- | --- | --- |
| M-30 | kbdhook.rs 轮询路径识别 XBUTTON1/2 | kbdhook 归属路（AI-2 键盘与窗口路）Rust 侧窗口期；settings.xBinds 与前端映射已就绪 |
| M-32 | 每窗口 IME 状态（ime_state.rs：ImmGetConversionStatus 等） | 需 Rust 侧新模块 + VWM 焦点钩子；涉及跨 AI 共建 |
| M-33 | winman.rs dispatch 处发 keycast://show 事件 | winman.rs 为共享文件，等键位链路统一收编后一并接入 |
| M-28 | winman.rs dispatch 计数侧 | 同上（前端榜/落盘 KV 已就绪） |
| Z-10 | 12 面板逐个接入 useGridNav/useHotkey | 面板代码分属各 AI，接入零改造成本，走查记录随后续批次归档 |

## 四、红线遵守声明

- 未新增任何未经注册表仲裁的键位；新 UI 键位（Ctrl+/）走 register()。
- 未触碰 kbdhook.rs / winman.rs（共享/归属受限文件）；双击 Esc 切环境契约未动。
- 统计数据仅本地、仅 action id + 次数，零出站。
