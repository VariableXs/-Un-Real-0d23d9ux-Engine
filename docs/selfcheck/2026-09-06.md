# 程序与功能自检报告 — 2026-09-06（M1 隔离执行档与凭据封存收口）

**验证环境**：Windows 11 x64（本机）· Node 24 · Rust stable-msvc 1.98
**范围**：批次 B-1/B-2（bench 基线 + container 骨架）与 **B-3…B-6（M1 全部四批）**
**结论**：✅ 自动验证栈全绿；M1 机制层有端到端测试证明；GUI 实机点验项如实列于第 4 节。

## 1. 自动验证栈结果（全部通过）

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| TypeScript 严格类型检查 | `npm run typecheck` | ✅ 0 错误 |
| 前端单元测试 | `npm test`（Vitest） | ✅ 218/218 |
| 后端测试（container + 主 crate） | `cargo test --workspace` | ✅ 8 + 42（含 exec 6 项新增） |
| IPC 命令面审计 | `node tools/audit.cjs` | ✅ 前端调用 ↔ Rust 命令一一对应（新增 profile_* ×4 + residue_scan_cmd） |
| i18n 键完整性审计 | `node tools/audit.cjs` | ✅ zh / en 全覆盖（新增 pf* 22 键；zh-TW 经 convertDict 自动继承） |
| 生产构建 | `npm run build` | ✅ 10.7s（chunk 提示为既有项） |
| 性能基准回归 | `node tools/bench.cjs --check` | ✅ fileIndex +5%（限内）；GUI 项本轮回避 |

## 2. 本批次（M1）功能级验证

- **端到端注入证明（新增测试 `exec::tests::spawn_profiled_injects_environment`）**：
  经 `spawn_profiled` 启动真实 `cmd.exe` 子进程，断言其环境变量
  `VARIABLE_TEST_HOME` 已展开为容器内路径 `{home}`——凭据零落宿主的机制证明；
- **apps.json v1→v2 平滑迁移**（`v1_registry_json_upgrades_with_default_profile`）：
  旧 JSON（无 profile 字段）反序列化得到空执行档，可读可写可回写；
- **模板库完整性**：5 模板（claude-code/codex/zcode/git/node）键名合法、id 唯一、
  占位符约定一致；HOME 与 USERPROFILE 镜像策略符合蓝图 14.2；
- **bench.cjs**：四项基准首跑（coldStart 571ms / fileIndex 1591ms / memory 32MB），
  `--check` 回归门禁可用；container crate 8 测试桩全绿。

## 3. 本轮自检发现并修复的问题

1. **`spawn_profiled` 空 cwd bug（端到端测试抓到的真实缺陷）**：裸程序名
   （如 `cmd.exe`）的 `program.parent()` 返回空路径，`current_dir("")` 导致
   CreateProcess 报 InvalidFilename——已改为仅在有目录前缀时设置工作目录。
   这验证了「新通道必须有真实子进程级测试」的纪律价值。
2. bench.cjs 初版两处口径问题：报告日期用 UTC（比本地慢一天）；memory 只统计
   主进程 WorkingSet（不含 WebView2 渲染子进程，严重低估）——均已修正并写入口径说明。

## 4. 已知边界（如实声明，待实机点验）

- **真机注册 + 启动真实 Claude Code CLI → `%USERPROFILE%\.claude` 不出现**：
  需要在桌面环境里手工登记/启动（GUI 路径），本轮以 cmd.exe 端到端注入测试替代
  机制验证；实机点验按三宿主矩阵在里程碑验收时执行；
- **.lnk 通道无环境注入**（ShellExecute 不可注入，蓝图 14.2 如实边界）：UI 已标注
  「pfLnkNote」提示，依赖残留扫描兜底；
- **残留扫描观测面**：仅 %USERPROFILE% 顶层文件 + Recent 目录（不递归、不读内容），
  目录级误报高故不观测——已知取舍；
- **出站白名单（net_allow）仅登记不执行**：执行点在 M8 环回代理落地；
- **设置页「执行档」标签的 GUI 交互**（模板套用/表格编辑/干跑展示）未实机点验，
  typecheck + audit 已验证接线正确性。

## 5. 里程碑状态

M0 收口（B-1/B-2 ✅）；**M1 收口（B-3/B-4/B-5/B-6 ✅，本报告）**。
关键路径下一站：M3 终端与云 AI 矩阵（B-7 ConPTY 起步）。

---

## 追加：M3 终端与云 AI 矩阵收口（同日第二批，B-7…B-11）

| 检查 | 结果 |
| --- | --- |
| typecheck / vitest 218 / audit（新增 term_* ×2 + ai_* ×8 命令面）/ build | ✅ 全绿 |
| cargo test --workspace | ✅ 55（新增 ai ×3、terminal ×2、exec e2e ×1） |

### M3 新增的如实边界
1. **真机矩阵未跑**（B-11 🟡）：claude/codex/zcode 真实登录→会话落容器→残留=0
   需真实账号与 GUI 操作，按三宿主矩阵点验；ai_verify 断言工具已可一键核验；
2. **npm 包名待真机确认**：codex（@openai/codex）与 zcode（@z-ai/zcode）的包名
   以 registry 实际解析为准，错误时安装可见失败并可修正注册表；
3. **WT 单实例**：切换账号身份需先关闭既有终端窗口（新进程才拿到新环境注入）；
4. **登录态为推断**：容器配置标记文件存在 = 已登录·推断；不读凭据本体；
5. **终端 V1 依赖便携 WT 部署**：runtime/wt/WindowsTerminal.exe 由用户放入
  （或后续接 B-8 同款授权下载通道）——缺失时 term_open 如实报错。

---

## 追加：B-11 部分点验结果（同日第三批，npm 包名真机确认）

**验证方式**：本机直连 npm registry 实际解析（`npm view`），2026-09-06。

| 工具 | 注册表当前包名 | registry 实际解析 | 结论 |
| --- | --- | --- | --- |
| claude-code | `@anthropic-ai/claude-code` | ✅ 存在（v2.1.263） | 正确，无需改动 |
| codex | `@openai/codex` | ✅ 存在（v0.153.4） | 正确，无需改动 |
| zcode | `@z-ai/zcode` | ❌ 404 Not Found | **注册表需修正（见下）** |

### zcode 包名调查结论

- `@z-ai/zcode`、`@z-ai/cli`、`@zai-org/*`、`zai-code`、`zcode`（现名已被 2020 年
  无关项目占用）在 npm 上均不可用或非目标包；
- npm 生态现状：Z.ai 官方**没有独立发布的 coding CLI npm 包**——官方 GLM Coding
  Plan 路径是在 Claude Code / Codex 等既有工具内配置 ZAI 端点
  （`https://api.z.ai/api/coding/paas/...`），而非独立 CLI；
- 存在 `zcode-app-cli`（kingsword09）——"Unofficial terminal client for the ZCode
  agent runtime"，说明 ZCode agent runtime 本体可能经由非 npm 渠道分发
  （官方安装器/脚本），本机无法在 npm 层面确证。

### 处置（M4 修正登记，编码批次执行）

1. `src-tauri/src/shell/ai.rs` 中 zcode 条目的 `npm_package: "@z-ai/zcode"`
   暂不可安装（安装时会如实报 404，行为符合"错误可见"设计）；
2. 修正方向二选一：a) 改为通过 ZAI 端点复用 claude-code/codex（与官方路径一致）；
   b) 确认 ZCode 官方分发渠道后替换包名。倾向 a)；
3. M4 动工前无需阻塞——ai_verify 对该工具的核验路径不受包名影响。

### B-11 剩余点验项（需 GUI + 真实账号，登记在案）

- 保险箱初始化 → 装入 Node / Claude Code → 真实登录 → 会话落容器 →
  `ai_verify` 残留 = 0：**仍未执行**（需用户真实凭据与桌面 GUI 操作），
  维持 🟡 状态，按三宿主矩阵在里程碑验收时点验。


---

## 追加：M2 容器线收口（同日第四批，B-12…B-17 + B-31）

| 检查 | 结果 |
| --- | --- |
| typecheck / vitest 218 / audit / build | ✅ 全绿 |
| cargo test --workspace | ✅ 49(container) + 47(主 crate) + 7 + 7 |
| 五层合并门禁 + bench | ✅（bench 本轮未改 GUI 路径，无回归面） |

### M2 功能级验证

- **B-12**：B+ 树 oracle 差分 4000 步；Uxv 单文件容器 4MiB chunk + BLAKE3 去重 + refcount；read_range/stream；Footer 双副本 seal/reopen；热 chunk 随机读（200 次 @16MiB）远低于 20ms 口径；
- **B-13**：掉电注入 100 轮（随机写/覆盖/删/改名 + 未 seal drop）oracle 全量比对 0 丢失；撕裂尾事务丢弃；SuperBlock 指针原子发布；
- **B-14**：压缩分级（LZ4→Zstd-19 比选→RAW 回退）+ Argon2id/XChaCha20 全容器加密（错口令拒绝/篡改认证失败/随机 nonce）；冷读 LRU 支撑 20ms 口径；
- **B-15**：4 卷条带拓扑 + 卷表持久化 + GC 标记/走查/单卷压实（回收 >0、活数据不膨胀、暂停 <100ms）；
- **B-16**：VHDX probe 降级链 + 迁移向导逐文件 BLAKE3（600KB 缩比验证，10GB 实数据属 H1 实机项）；
- **B-17**：freeze/unfreeze 冷层重编码 + stats() 水位线/写放大口径（2TB 声明容量入表）；
- **B-31**：schema v1→v2 实走迁移协议（快照/升版/校验/回滚）。

### 已知边界（如实声明）

1. **GUI 接线点未含**：水位线任务栏 UI、迁移向导 UI、OOBE——内核数据源/协议已就绪，属 B-32/前端批次；
2. **v1 索引重写迁移器未做**（v1 从未发布；当前 v1→v2 为版本戳迁移），真实重写随 B-33 恢复模式；
3. **B-11 真机矩阵维持 🟡**：需真实账号与 GUI（见上文同日第三批结论）；
4. **B-32/B-33 未开工**（GUI 批次）；
5. 直连逃逸观测、net_allow 执行点仍按计划属 M8（B-28）。

---

## 追加：B-32 + B-33 收口（同日第五批）

| 检查 | 结果 |
| --- | --- |
| tsc 0 错 / vitest 218 / audit（IPC 178↔182 OK + i18n zh/en 全覆盖）/ build | ✅ 全绿 |
| cargo test（container 52 + 主 crate 47） | ✅ 全绿 |

- B-33 M2 半包：两级救援（文件级 → chunk 级裸走查，只读容器只写输出）+ diag/repair/rescue_export/init/stats/vhdx_probe 六命令 + 设置页「存储与恢复」标签；
- B-32：OOBE 四步向导（介质体检/口令建卷/三模板/导览），OobeGate 首启门控（settings.oobeDone）；
- 顺手修复：residue_scan 前后端命令名不匹配（原为运行时必炸的遗留缺陷，audit 反向冗余不报红的工具盲区已登记 M4 顺手项）；
- 如实边界：吊销清单 UI、--export-rescue CLI 参数、软件渲染开关属 B-33 M3 半包，未含；OOBE 向导实机走查属 H1 实机项。
