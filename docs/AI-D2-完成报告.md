# AI-D2 · 完成报告（Varix STAR I · 桌面体验域·后段 F093-F110）

| | |
| --- | --- |
| 分队 | AI-D2（泳道三 · C 桌面体验域·后段） |
| 交付落位 | `kernel/varix/src/stard/`（17 文件，域聚合器 `run_stard_checks` blocks=17 全绿） |
| 判据唯一源 | `docs/Varix STAR I start.md` G-C-23～G-C-40 |
| 验证基线 | **129 通过 / 0 失败 / 0 警告**（隔离舱 `D:\2\aid2-scratch`，`cargo test` 宿主直跑） |
| 自检规模 | CheckSet **425 条** + 域内单元测试 **115 项** |
| 源码规模 | 总 10,240 行 · 纯功能 **5,933 行** · 测试块 1,661 行 |
| 冻结登记 | F101 天气件、F104 录音件——主册 F200 存量冻结候删，不落码（结构面守护） |
| 对账与缺陷账 | `_attic/aid2-f093-f110/行数对账与缺陷账本.md` |
| 交付提交 | 见 git log（本报告同批提交并 push origin main） |

---

## 一、交付范围（16 实现项 + 2 冻结登记）

| 项 | 模块 | 判据锚（主册） | 模型面核心机制 |
| --- | --- | --- | --- |
| F093 | thumbeng | 万张滚动帧率不掉；二次命中率 >95%；2GB LRU | 三级优先生成队列（可视>邻近>背景，挤兑/拒绝如实记账）、FNV-1a 哈希键四档缓存、损坏即弃自愈、EXIF APP1 内嵌缩略直抽（对抗样本全拒）、渐进占位（低清先行） |
| F094 | mediainfo | 五容器解析全对；缓存命中 <100ms | 手写 MP4 box 走查（mvhd/tkhd 旋转矩阵/mdhd/stsz 轨字节实算）、EBML varint（Duration float）、FLAC STREAMINFO 位拆、MP3 帧头+Xing VBR、媒体缓存 LRU+负缓存、悬浮 800ms/<100ms 双路状态机 |
| F095 | term2 | 10 万行回看 80fps；CJK 对齐；分屏重排 80fps | CJK 宽度表（组合字符零宽——混排列不错位算法本体）、环形回看双上限（行数+字节账）+截断提示行钉头、虚拟滚动切片+渲染帧账（2000 格恰在 12.5ms 预算）、分屏二叉树（拖拽重排面积守恒）、会话导出（时间戳+退出码） |
| F096 | termpalette | 内置 12 条全可达；收藏-搜索-执行全链；弹出 <100ms | 内置 12 枚举唯一源、子序列+首字母双路模糊打分、收藏 frecency+定容逐出、面板冻结/恢复计数对账、模板变量 {host} 提取回填、危险字符原样传递 |
| F097 | notepad | 300MB <2s；原子保存百次断电零损坏；草稿恢复 | BOM/行尾混合检测、分块扫描打开成本账、MemStore 原子写协议（临时文件+换名，断电百次注入原文件零损坏）、大文件只读门+显式启用、极简正则（`.`/`x*` 回溯）、高亮 1000 上限诚实溢出、草稿防抖 500ms+崩溃恢复、外部修改哈希对拍 |
| F098 | snipshot | 三模式全流程；取色对拍；标注导出无损 | 像素级取色 RGB+HEX、放大镜 15×15 八倍边缘钳制循回、马赛克 24/48px 块均色两档、标注撤销 20 步+序号删中自动重排、历史 20 条环、选区任意方向归一、通知静默联动、保存失败三要素 |
| F099 | calcx | 标准/科学各 30 例全对；历史回填 20 轮零错位 | **128 位定点（SCALE=10^12）+ 256 位中间积**（学校簿乘法+移位减法长除+四舍五入）——十进制补偿 0.1+0.2=0.3 精确；调度场（多层括号/前缀函数/一元负号/右结合幂/后缀阶乘）；Windows 百分号三语境改写（差比/乘除/独立）；60 例判据 + 错误定位 Parse(pos) |
| F100 | clocksuite | 世界时差 12 城全对；倒计时到点全链；闹钟唤醒触发 | civil 算法（Hinnant 口径）round-trip 零漂移、12 城偏移表+日界标注（昨天/明天）、秒表圈计最快最慢自动标记、倒计时圆环千分比+音效/通知/全屏到点三件全链+自动重启、闹钟重复规则（单次/每日/工作日/自选位图）全周矩阵、贪睡 5 分钟跨时进位、F065 唤醒源白名单+F077 免打扰照弹 |
| F102 | sticknote | 10 张开机全恢复；自动保存断电零丢失；置顶跨全屏 | `#VARIX-NOTE v1` 序列化 round-trip 全字段（位置/颜色/置顶/字号）、防抖 500ms 草稿快照、z 序置顶组恒前、20 张上限诚实拒绝、六色 E1 令牌、回收站语义零真删、开放格式 .txt 导出、外部修改重载提示 |
| F103 | sketchpad | 50 步撤销零错位；4K 跟手 <33ms；导出逐像素一致 | Catmull-Rom 笔迹平滑（过控制点、共线不偏离）、速度映射笔压（慢粗快细单调）、8 方向 45° 吸附（免 atan2——no_std 无 libm 纪律）、撤销栈 50 步+64MB 内存账+末位匹配防同内容错删、重做清零语义、30s 自动草稿、1x/2x 导出账 |
| F105 | photolib | 万张滚动 80fps；放映翻页 <200ms；旋转 EXIF 写回 | 月分组降序时间流（civil 同源）、8 档缩放+光标焦点几何（看哪哪居中）、放映状态机（250ms 交叉淡入/自动 5s 可关/环绕翻页）、旋转 90° 步进+EXIF orientation 1/3/6/8 对拍+显示分辨率互换、损坏图占位不炸流、目录增量刷新、批量旋转/删除 |
| F106 | keyhud | 三键三态全对；淡出 1s±50ms；全屏降级 | Showing(120ms)→Holding(1s)→Fading(300ms)→Hidden 计时链、风暴合并（200ms 窗换内容不重展不闪屏）、色弱形状冗余（圆/方/三角互异）、角标/横条双几何、F107 输入法切换复用、设置页可关 |
| F107 | imefloat | 三态跟随 <16ms；重定位不抖；避让 20 例全对 | 三态 8 组合标签互异、16ms 节流（首移必应用）、避让几何唯一源+终极钳制（四角四边 20 例+多分辨率矩阵全过）、密码框隐私隐藏、任务栏小标收起、全屏角标降级（F106 对端）、F027 灰显路径 |
| F108 | phrasebk | 100 条全链 <100ms；变量三族 20 例；round-trip 无损 | 变量白名单 9 格式符（date/time/weekday 三族，族名-格式符双重校验，白名单外拒绝不静默展开）、缩写 32/内容 500 边界、候选混排徽标+短语优先开关、导入 Overwrite/Skip/KeepBoth 三策略、使用计数冷沉底、1000 上限、三列搜索+分类树 |
| F109 | cliphist | 20 条循环驱逐（钉选除外）；密码框排除；搜索高亮 | 顶端插入+同内容去重置顶（钉选属性随并）、驱逐跳过钉选（全钉选诚实停手）、B-3901 隐私总闸+敏感复制拒入负账、三型条目预览（文本 3 行/图片/文件引用）、>16MB 引用化、字节位高亮段、落盘可关、总闸关面板灰置说明 |
| F110 | osk | 全键位点击全对；半透明可读；学习模式同步 | 104 键等效布局（功能排 13/主区 61/导航区 13/小键盘 17，码位唯一、44px 触屏热区）、符号层 Shift⊕Caps+单发弹起、锁定键三态灯、修饰键组合粘滞、学习模式物理键同步高亮、透明度 30% 可读下限、多点并发 held 集、屏边吸附、点击穿透 |
| 冻结 | —（无模块） | F200 存量冻结候删 | F101/F104 登记守护：`FROZEN_ITEMS` 在册 + 16 实现项 id 清单互斥断言 |

---

## 二、按人格纪律交付的三个要点

**1. 判据载体优先，行数不注水。** 本批纯功能 5,933 行 / 上限口径 17,485 行
= 33.9%，与 K2（同口径约 60%）、D1 批次同口径如实登记。每模块的 CheckSet
把主册判线逐条钉死（425 条），模型面可复现；回炉路径明确——各模块的
渲染/窗口/资产/平台桥接面属真机 UI 接线批（F094 demux 流、F098 截屏桥、
F110 键位渲染等），已在账本中逐项列明。

**2. 冻结候删按令执行。** F101 天气件、F104 录音件在主册 F200 存量冻结候删
名单内（Variable 已拍板「冻结不删除、编号不复用、不再投入工时」）——本批
**未实现、未投入工时**，只在域聚合器登记结构面守护（实现模块清单与冻结
名单互斥断言）。**正式删除待 Variable 逐项确认**后走冻结流程。

**3. 十六处真缺陷全部测试拦截后修复，无一静默。** 最有价值的三处：
256 位乘法的 OR/进位混淆（D1）、div256 位序交错（D2）——计算器引擎的
数值正确性地基；from_f64 复用 round64 的 i64 中转饱和（D3）——揭示了
galaxy::math 的设计域边界（其 doc 注释已声明「输入量级有限」，本次按
纪律在本模块就地实现大数域舍入，未改动共享底盘）。全账见
`_attic/aid2-f093-f110/行数对账与缺陷账本.md` 第三节。

---

## 三、工程纪律执行情况

- **判据唯一源**：全部判据摘自主册 G-C-23～40，与本分工书冲突时以主册为准。
- **先底盘后界面**：依赖锚点（F093 缓存键、F072 frecency、F106/F107 降级
  对端、F079 音效、F085 回收站语义）全部以显式调用/参数注入承接，未制造
  对未落地面的编译依赖。
- **一处一事实**：复用 `star::sbase`（sat_sub）与 `galaxy::math`（no_std
  浮点），未复制第二份；模块间共享语义（哈希键/徽标/图标族）在 doc 注释
  中声明对端。
- **时间注入**：一切时钟由调用方注入（unix ms / 注入钟），宿主测试确定
  复现，内核侧由上层供给真值。
- **零静默**：所有可失败路径显式三要素或诚实降级（`None`/`Err` + 下一步
  文案），无被吞异常。
- **回归零破坏**：隔离舱 129/129 全绿零警告；lib.rs 接线一行
  （`pub mod stard;`），未触碰其他分队落位。

## 四、遗留与风险（如实）

1. **UI 接线批未做**：本批是判据实装层（模型面），16 项的窗口/渲染/资产
   面待真机批——每模块已在账本标注具体缺口。
2. **MP4 fps 字段未给**：视频帧率需 stts+mdhd 联算，本版未实装（字段省略
   ——诚实留白），已在 mediainfo doc 注释登记扩展点。
3. **F101/F104 的正式删除**待 Variable 确认；确认前编号不复用。
4. **隔离舱 `D:\2\aid2-scratch`** 不进 Git（与 K2 舱同纪律），交付后可删。
