# M3 复盘（步骤 0749 / 0754）

## 缺陷回溯（W3 期间发现并修复）

| 缺陷 | 根因 | 修复 | 预防 |
|---|---|---|---|
| `metrics.rs` M002 render 自检借用悬垂（E0597） | CheckSet detail 要求 `&'static str`，却传了局部缓冲区的借用电 `s` | 先把断言算进 `bool`，detail 改静态字面量 | 自检框架约定：detail 一律静态字面量 |
| `tests/fuzz.rs` make_layer 缺参（E0061） | compositor 扩签名后测试未同步 | 按当前签名补 color/alpha/blend | 域签名变更必须 grep 全部调用点 |
| fileman search 死循环（W3 前期，历史会话修复） | 哨兵值歧义 | 根节点自指出栈 + ordered_siblings 排除 | 树遍历统一走 PathStack，禁裸循环 |
| CheckSet 截断 | MAX_CHECKS=64 早期能力不足（历史修复，容量已 32→64） | 扩容 | 域自检项 ≤ 容量，`truncated()` 断言 |
| rustup 并发安装组件冲突（环境） | 多会话同时触发 `rustup component add` | 重试即可恢复 | 收口会话避免并发动工具链 |

## 做得好的

- 域参数表先行（预算/观测/降级/fuzz/自检五要素），实现时目标清晰。
- 闭环七步模板统一，w3gate 集成门禁把跨域联调固化成可回归资产。

## 改进点（带入 W4）

1. 多会话并行改同一 lib.rs/robust.rs 时仍会互相覆盖，注册类改动建议「各自追加自己段落 + 提交前重读」。
2. 真机点验长期「待环境具备」，建议 W4 优先打通 QEMU 冒烟，使 0737 类步骤可自动验收。
3. fuzz 轮数目前受单测时长约束（2000 轮/域），里程碑前可加一次 10 分钟离线大跑脚本。
