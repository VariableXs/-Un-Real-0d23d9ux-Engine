# M1 复盘（步骤 0269 / 0270 / 0277）

## 慢在哪
1. **共享工作区多会话并行**：同一仓库多个 AI 会话同时施工，`lib.rs`、`aurora/mod.rs`、
   `robust.rs` 注册表被多方追加；出现过同文件并行编辑互相覆盖（需先 git log / 重读再改）。
2. **测试反馈周期**：ktest 全量编译 ~1~4 分钟；靠重定向到文件 + 过滤行的方式看结果。
3. **环境缺失**：QEMU / 真机不可用，所有启动类验收只能标注「待环境具备」。

## 下波改什么（进入 W2/W3 前）
- 域注册表（robust.rs）按域分组并加注释段，降低并行冲突面。
- 集成测试继续放 `aurora/integration.rs`（W2 域用 `w2_integration.rs`，已由后续批次建立惯例）。
- 文档数字一律由 `scripts/gen-numbers.py` 生成，人手不再改数字，防漂移。

## 缺陷回溯（步骤 0269）
| 缺陷 | 根因 | 修复 | 预防 |
|---|---|---|---|
| `MAX_DOMAINS=32` 注册表静默截断 | 早期注册域不足 32，扩容被遗忘 | 扩到 96（本次） | checkup 增加 len 断言（`>= 10` 已有） |
| 集成测试用错 API 名（SnapSide::Left 等笔误） | 未读域源码先写调用 | 编译期暴露，改为 LeftHalf | 写跨域调用前先 grep 域 pub API |
| 并行会话同文件编辑互相覆盖 | Edit 工具并行写同文件 | 顺序化编辑 | 同文件多处修改必须串行 |

## 质量度量（步骤 0270）
- 覆盖：AURORA 15 域 × 25 项 + W1 集成 7 项 + W3 门禁，全部纳入内核 checkup 强制全 PASS。
- 性能：见 `docs/perf/基线.json`。
- 缺陷：0 未修复。
