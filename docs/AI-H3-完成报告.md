# AI-H3 · 完成报告（F301-F350 · H 基础通用域·三分队）

| 批次 | Varix STAR I · 泳道三 H 基础通用域·三分队（AI-H3，50 项 + hbase 共享底盘） |
| --- | --- |
| 落位 | `kernel/varix/src/h3star/`（37 文件，域聚合器 `run_h3star_checks` blocks=51） |
| 判据唯一源 | `docs/Varix STAR I start.md` 主册 G-H 区段（F301-F350）+《Varix STAR I start · AI分工完成图》 |
| 验证基线 | **185 通过 / 0 失败 / 0 警告**（域内单测实数，深化补批后收尾复核口径；隔离舱 `_attic/aih3-f301-f350/isolation/` 经 `#[path]` 直挂本目录实测。原记 189 系把 4 个舱内探针测试计入——口径已修正，见 §六） |
| 源码规模 | 总 15,663 行；纯功能 **9,584 行**（口径：总行 − `#[cfg(test)]` 块 − 注释行 − 空行；深化补批 +1,629） |
| 非功能产物 | 本报告 + `_attic/aih3-f301-f350/行数对账与缺陷账本.md`（K2 同款逐项对账 + 合并缺陷账本 18 项） |

---

## 一、交付物与自检规模一览

| 维度 | 数值 | 说明 |
| --- | --- | --- |
| 模块 | 37 文件（hbase + 50 项逐项落位，同族项合模块） | 域聚合器 blocks=51 全绿 |
| 域内单元测试 | **185 个**（原记 189 含 4 个舱内探针测试——收尾复核修正，见 §六） | 宿主 `cargo test` 直跑（注入钟确定复现） |
| CheckSet 自检 | 51 块聚合、逐模块 9-13 项判据检查 | 每项判据载体，逐条钉死主册判线 |
| 警告 | **0** | `cargo test` 0 warning 纪律 |

### 模块地图（F 项 → 落位文件）

| F 项 | 落位 | F 项 | 落位 | F 项 | 落位 |
| --- | --- | --- | --- | --- | --- |
| F301 设置中心搜索 | `setsearch` | F318 电源按钮行为 | `pwbtn` | F335 指针直通平面 | `loadresp` |
| F302 页层级规范 | `pagehier` | F319 唤醒即回 | `wakeresume` | F336 复制文件地址 | `copypath` |
| F303 即时生效哲学 | `instantfx` | F320 飞行模式 | `wakeresume` | F337 命令行互通 | `copypath` |
| F304 单页还原默认 | `pagedflt` | F321 就近共享 | `nearshare` | F338 多选操作条 | `multibar` |
| F305 设置导入导出 | `themepack` | F322 麦克风指示 | `micind` | F339 空格即看 | `multibar` |
| F306 全文搜索 | `fulltext` | F323 摄像头指示 | `micind` | F340 预览形制统一 | `multibar` |
| F307 搜索历史无痕 | `srchhist` | F324 权限中心 | `micind` | F341 静音四档 | `sndmode` |
| F308 运行框 | `runbox` | F325 文件历史版本 | `filevers` | F342 磁盘检查 | `sysgov` |
| F309 窗口键位族 | `winkeys` | F330 通知分组 | `ntfgrp` | F343 内存出路 | `sysgov` |
| F310 保存三问 | `saveask` | F331 动画降级 | `animdegrade` | F344 卸载三步 | `sysgov` |
| F311 会话自动恢复 | `sesrestore` | F332 帧率自适应 | `animdegrade` | F345 默认应用矩阵 | `sysgov` |
| F313 符号表情面板 | `sympanel` | F333 低电量视觉 | `animdegrade` | F346 自启动管理 | `sysgov` |
| F314 截图取字 | `ocrtake` | F334 高负载保响应 | `loadresp` | F347 键盘重复参数 | `keyrep` |
| F315 拖拽悬停前置 | `draghover` | F348 光标指针加粗 | `caretbold` | F349 焦点模式进阶 | `sndmode` |
| F316 闲置锁屏 | `idlelock` | F350 触感反馈谱 | `haptic` | F312 拼音容错搜索 | `pyfault` |
| F317 氛围模式 | `idlelock` | F326-F329 输入面 | `imecore`/`cnensw`/`shuangpin`/`clipbig` | | |

## 二、验收判据达成方式（内核侧核查面）

每项主册判据在对应模块 `run_*_checks() -> CheckSet` 内有具名检查（检查名即判据摘文），
并有单元测试覆盖。代表性锚点：

- **F301**：同义词覆盖率 1000‰ 审计、Top3 就地操作（即时项可就地反馈）、混输
  （CJK+ASCII 分字段合账）、空态三建议（编辑距离最近优先）、<100ms 注入钟直读。
- **F304/F325**：还原只碰已改项、确认流状态机（预览→确认/取消/终态拒绝）、版本
  快照 <1s、还原前自动备份（可撤销）、30 天/500 上限清理。
- **F310/F311**：默认焦点=保存、批量勾选一次问清、未保存判定准确性（打开没动
  不问）、30s 恢复点双写、崩溃窗口期 ≤35s、拒绝路径同样清理。
- **F326**：切分器 20 组歧义音节全展开（`xian` → [xian]/[xi'an]）、三因子定点排
  序（同输入同结果）、造词三次入库、离线结构性成立。
- **F332**：三级阈值（50/45/40）×2s 确认窗、逐级回升（30s 观察期）、建议条只出
  一次、fps 实测记录在账。
- **F335**：1000Hz 采样不丢（1000 注入全入账）、独立平面审计（指针移动合成调度
  计数为 0 + 窗口移动对照组计数 1）。
- **F350**：触感参数表五项常量钉死（80ms/1px/1.03/400ms/8%）、私设触感=0 注册制
  扫描、性能模式降为瞬时、F124 弹性过冲曲线关键点核账。

## 三、行数对账（诚实偏差登记）

目标行数（主册上限口径）合计 **60,710 行**；实际纯功能 **9,584 行（15.8%）**——首批 7,955 + 深化补批 1,629（深化层：F331-333 三级动作参数表/恢复快照账/事件日志、F344 vxapp 清单与残留扫描规则表、F343 决策条候选账、F342 修复日志、F326 候选窗分页/二元权重/词库持久化、F312 节表全量/模糊多候选、F321 发现账/分块校验/速度账、F319/320 采样统计账/无线状态持久化/逐台重连账、F336/337 转换规则表/派发器/历史、F338 选择三键语义/媒体预览态/标注旁文件/缓存 LRU、F340 缓存复用、F306 分词器/倒排索引/增量更新、F301 防抖账/同义词冲突检测、F341/349 日程窗口/拦截明细账）。
**偏差原因如实登记**：本域 50 项的判据本体是状态机/账本/审计器逻辑核（判据载体），
主册行数上限口径含界面渲染与交互层（界面类项依赖 F151/F216/F436 控件件，未就绪
不得开工自绘——铁律 5）。本队交付「判据实装层」：全部逻辑核、全量自检与单测，
界面层待控件件就绪后由后续批次在同一模块上叠加。**禁注水纪律**：未以复制粘贴、
空转包装凑行数；逐模块行数见 `_attic/aih3-f301-f350/行数对账与缺陷账本.md`（K2 同款逐项对账 + 合并缺陷账本 18 项）。

## 四、缺陷账本（施工中发现并修复）

| # | 现象 | 位置 | 严重度 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | 混输匹配要求 CJK/ASCII 同字段，真实场景「亮度bright」跨字段命中 | `setsearch::score_item` | 🟡 | 条目级合账（两片段分字段命中即中英混输） |
| 2 | 切分器裸声母段（`["xia","n"]`）产生垃圾切分 | `imecore::segmentations` | 🟡 | `valid_segment` 要求有韵母；裸声母仅整串放行 |
| 3 | 候选同词多切分重复刷屏（先 899999 后 900000） | `imecore::candidates` | 🟡 | `dedup_by` 按词取最优分 |
| 4 | `theme_tokens` 键被凭据启发式误伤 | `themepack::credential_like_key` | 🟢 | 凭据词表收窄（auth/secret/password/credential/api_key/session_key） |
| 5 | Win+R 单会话二次 execute 被状态机拒绝且历史不跨会话 | `runbox::open` | 🟢 | open 可重入（重呼出历史保留——常用词两击直达） |
| 6 | 唤醒 reboot 无条件置脏干净标志 | `sesrestore::reboot` | 🟡 | reboot 只读持久化标志；崩溃场景从未置位 |
| 7 | 双拼 `q+un` 与 `q+iu` 同键歧义直接取表序首个 | `shuangpin::decode` | 🟢 | 文档化（声韵搭配歧义面留待真引擎）；判据测试选无歧义键 |
| 8 | 剪贴板大对象引用条内存增量判据空洞断言 | `clipbig::memory_budget_ok` | 🟡 | 重写为「引用零增量 + 流式峰值 ≤ 单块」真账 |
| 9 | 权限申请对未登记应用报「未说明用途」（误导） | `micind::PermissionCenter::request` | 🟡 | 先查登记位——未登记报「应用未登记」 |
| 10 | F334 过载自愈以末条延迟为峰值基准（自比较恒假） | `loadresp::recovered` | 🟡 | 与饱和段最大值比较 |
| 11 | Save 裁决关窗移除文档导致后续 Discard 用例空转 | `saveask` checks | 🟢 | 用例重新开窗（测试面修正，非实现缺陷） |
| 12 | 预览 diff 字节区间期望按字符口径（多字节错位） | `filevers` checks | 🟢 | 明确字节面口径并文档化（渲染面做字符对齐） |

## 五、边界与后续（随闸门登记）

- **实机面判据**（录屏/示波/长跑/4K 走查）属装机验证阶段——本泳道交付判据载体，
  实机项按分工图「随闸门补测」口径登记。
- **界面层**（主册工程量的界面与交互 60%）依赖 F151 令牌与 F216/F436 控件件，
  未就绪不开工自绘（铁律 5）；逻辑核已预留注入口（识别器注入口、光标面注入
  口、F244 键位注册表、F238 锁屏衔接点等）。
- **共享树并行施工**：本报告提交时主 crate 存在他队（compatstar/h1star 等）在
  途编译错误，本队落位以隔离舱 189 绿验证；主 crate 恢复绿后 `run_kernel_checkup`
  将自动吃到 `h3star::run_h3star_checks`（robust.rs 表 347→348）。

## 六、落位收尾补记（2026-09-26 · 合并伤修复与重新入册）

**发生了什么**：首批 bb6de292 本已完整提交（37 模块文件 + lib.rs/robust.rs
注册 hunk + 本报告 + 行数对账），但其后多会话 `pull --rebase` 冲突解决中，
974a8d2a（他队 CHANGELOG 合并伤修复提交）把 `kernel/varix/src/h3star/` 全部
37 文件与 CHANGELOG H3 段一并误删——提交信息只写了修 D1 条目，实际 diff 带
走了整个 h3star 目录。AI-U4 基线随后按「模块文件缺失」把 lib.rs 挂载行与
robust.rs 注册行临时屏蔽（注释在案）。深化补批（+1,629 行）只落在工作区、
未及提交。**仓库 HEAD 从此没有 h3star，工作区未跟踪文件成了唯一事实副本。**

**收尾动作（本批）**：

1. **事实核对**：逐文件 diff bb6de292 版 vs 工作区版——13 文件为深化后版本
   （恰为深化补批清单：animdegrade/copypath/fulltext/hbase/imecore/mod/
   multibar/nearshare/pyfault/setsearch/sndmode/sysgov/wakeresume），总行数
   15,663 与本报告 §三 完全一致，工作区即最终事实。
2. **重新验证**：隔离舱迁入仓库 `_attic/aih3-f301-f350/isolation/`（#[path]
   直挂真实文件，零拷贝零分叉），深化后代码全量重跑——**185 通过 / 0 失败 /
   0 警告**。旧值 189 经查含 4 个旧舱 lib.rs 内的探针测试（dump_red_checks
   等舱内诊断件，非交付代码），口径修正并在 §一 挂账。
3. **注释冗余修复**：h3star/mod.rs 域内模块地图表存在整段重复行（F309 起
   26 行抄两遍）——删除重复段并补「模块次序说明」，零冗余纪律。
4. **基线接线**：lib.rs 挂载行解除屏蔽（`pub mod h3star;`）；robust.rs 注册
   行恢复 + 域表计数 349→350（数组条目数已程序化核对 = 350，恰合）。AI-U4
   屏蔽注释替换为收尾留痕注释（经手可查）。
5. **他队在途状态零携带**：lib.rs/CHANGELOG 经私有索引（HEAD blob + 单 hunk）
   暂存——工作区里他队（AI-U3/AI-H1/AI-J2/AI-D1/AI-J1 等）的在途改动原样
   留存不碰；主 crate 当前 201 项编译错全部位于他队模块，0 项在 h3star。
6. **缺陷账本补登**（见 `_attic/aih3-f301-f350/行数对账与缺陷账本.md`）：
   D-19 合并伤丢失与修复留痕、D-20 测试口径修正、D-21 基线观察（MAX_DOMAINS=320
   小于域表 350 项，register 超容静默丢弃——基线设计面问题，属 robust.rs
   属主分队裁决，H3 不越权改动，仅显性化登记）。

**一句话**：代码没丢过一天——丢的是 git 里的注册表；本批把账本、代码、注册
三条线重新对齐，隔离舱证据随仓归档。

**补记（收尾批内自省）**：收尾提交 76b82935 自身复刻了一次同类事故——其
CHANGELOG blob 基于旧 HEAD 快照构造，落在并发提交 12d116ad（AI-V2）之后，
覆盖丢失了其补登的深化批次段；6e7b9ea1 已字节级取回并交叉审计全段无缺
（D-22 认账入账本）。多队共写文件的并发纪律由此定稿：**提交前重取最新
HEAD 重放本队段落，再构造 blob**。

——AI-H3 · 2026-09-26

## 七、深化批次三（2026-09-26 · 统一调速器核与识别管线本体）

**目标口径重申**：50 项纯功能目标上限 60,710 行。本批收口时程序化实数：
总 21,017 行 / 纯功能 **13,036**（50 项 12,402 = 20.4% + hbase 531 + 域聚合器 103），
域内单测 185 → **265**，隔离舱 cargo test 全绿 0 警告，域聚合 blocks 51 → 52。

**本批深化件**（判据载体，禁注水口径——每件都是状态机/引擎/账本逻辑核）：

1. **统一调速器核 `FrameGovernor`**（animdegrade，F331-333）：三机制优先级
   仲裁（低电量 > 预算降级 > 帧率分级）+ 帧成本模型 `FrameCost`（降级动作
   对每帧成本的数值直证——「降级期间响应延迟不增」从结构面升级为数值面）
   + 全周期延迟守卫乱序采样测试 + 模式切换环形账。
2. **vxapp 卸载全链彩排**（sysgov，F344-346/F342）：注入测试应用端到端
   （清单→三步→关联回收→残留兜底→五项对账单，含执行漏项防呆用例）+
   声明权仲裁（安装不抢默认的执行面）+ 磁盘检查忙时排队 + 人话映射表 +
   冷启动重放与估算 ±20% 校准。
3. **路径互通引擎**（copypath，F336-337）：路径归一化核（混合分隔符/点段/
   越界钳制/盘符规范/UNC/POSIX/WSL）+ 逐味转义总表 + 互通事件账（十三章
   体验日志语义）+ `open .` 目录同步语义核。初版 unsafe 垫片按内核纪律
   重写为纯安全实现（D-25④）。
4. **OCR 识别管线本体**（ocrtake，F314）：截取状态机（误触防呆/Esc 全路）
   + Otsu 二值化 + 栈式连通域 + 8×8 密度特征 + 模板最近邻匹配（置信度‰）
   + 行序组装端到端——离线判据从「注入绕过」升级为「管线每个阶段都真
   离线」。
5. **断点续传引擎**（nearshare，F321）：分块位图 + fnv1a 块校验（坏块视为
   缺失）+ 恢复只补缺失 + 零缺失才完成 + 拒绝清场幂等。
6. **全量模糊规则表**（pyfault，F312）：7 规则（n↔l/平翘舌×3/前后鼻音×3）
   逐条开关 + 查询变体展开 + 「规则展开使命中层 Tolerant→PinyinFull」的
   端到端层序证明（D-26 恒真式事件后重写的真断言）。

**对账与虚账修正**：上一会话把逐项合计登记为 20,057（大于总行实数）——
D-23 认账，本版对账表全部改由脚本实数产出（四分法），并固化「对账数字
必须来自脚本输出」纪律。逐项实数见
`_attic/aih3-f301-f350/行数对账与缺陷账本.md` §〇·1 与 §一。

**剩余差距的诚实说明**：距 60,710 上限仍差约 4.8 万行。缺口主体是主册
工程量口径中的界面渲染与交互层（约 60%），依赖 F151 令牌与 F216/F436
控件件——未就绪不开工自绘（铁律 5），逻辑核注入口已全部预留。F332（6,175）
/F344（3,640）/F337（2,860）等大目标项的达成率仍低（4%/7%/10%），已登记
为下一深化批次的优先施工面。

——AI-H3 · 深化批次三 · 2026-09-26

## 八、深化批次四（2026-09-26 · 帧模拟器 / 干跑卸载 / 粘贴清洗 / 组句引擎）

**程序化实数**：总 23,117 行 / 纯功能 **14,355**（50 项 13,721 = 22.6% +
hbase 531 + 域聚合器 103）；域内单测 265 → **296**；隔离舱 cargo test 全绿
0 警告；域聚合 blocks 52 全绿。

**十件功能核**（详表见账本 §四·六）：

1. **帧步进模拟器 `FrameSim`**（F332）：工作负载×降级折算→帧时序账，
   p95/掉帧率——「降级改善 fps」从账面记录升级为机制本体；
   `SurfaceBudgetBook` 逐表面预算分配（指针 100‰ 保额——F335 联动，
   总账不漏 μs）；`TierPolicyTable` 策略参数总表（折算单调自证）。
2. **卸载深化四件**（F344-345）：文件类型注册表 DB（摘除回退出厂默认、
   无悬空）+ 释放空间逐项账（声明对账 5% 线）+ 卸载前还原点闸门
   （未就绪拒绝且计数可见）+ **干跑面**（执行越出干跑承诺必须被拦
   ——硬件与数据安全红线「先干跑列清单」的机器面）。
3. **命令行互通深化**（F337）：环境变量展开双味规则（未登记留痕不猜）
   + 终端形制能力档案 + **粘贴清洗器**（控制字符剔除/多行拒绝防夹带
   命令/4KB 上限——粘贴进终端的内容不可信）+ 多文件拖入 argv 序列。
4. **音节系矩阵**（F312）：声母×韵母合法性判定核（正字法规则显式成文，
   912 组合程序化统计）+ 容错命中统计账（词库缺口事件直出）。
5. **电源域深化**（F318）：三触发源×双供电面动作矩阵（配置缺口显性化）
   + 后果预演面 + 长按 4s 进度账（逐拍进度/松手清零/只触发一次）。
6. **组句引擎**（F326）：句子级贪心最长匹配（全确定/死胡同诚实失败）
   + 用户词频自学习账（**历史重放收敛证明**——自学习不是黑盒）。
7-10. **托底层**（F303 徽标覆盖审计+pending 反悔队列 / F348 参数协调器
   越权点名 / F334-335 负载分级矩阵+指针延迟账 / F319 分级启动时序账+
   全链 <2s 采样判定）。

**施工即时修复（D-27）**：单级超时判线拍脑袋 800ms 误杀合法 900ms 后台
级（探针实证后改 1.5×最宽正字级）；p95 样本容量口径误设（1 劣化恰落
p95 位）；组句器「最短切分方案」初版设计对词级词库不成立（重写贪心最
长匹配）。全部探针实证后修复并有单测钉死。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.7 万行——主册口径中的界面
渲染与交互层依赖 F151/F216/F436 控件件（铁律 5 未就绪不开工自绘）。
F332（5%）/F344（8%）/F337（14%）三大目标项已连续两批深化，达成率
4%→5%、7%→8%、10%→14%；下一批继续按缺口序推进。

——AI-H3 · 深化批次四 · 2026-09-26

## 九、深化批次五（2026-09-26 · 排队与保护 / 握手与自适应 / 版面与重试）

**程序化实数**：总 24,578 行 / 纯功能 **15,149**（50 项 14,515 = 23.9% +
hbase 531 + 域聚合器 103）；域内单测 296 → **317**；隔离舱 cargo test 全绿
0 警告；域聚合 blocks 52 全绿。

**八组功能核**（详表见账本 §四·七）：

1. **卸载域三件**（F344）：系统组件保护白名单（出厂六件冻结审计——
   越权卸载拒绝留痕）+ 卸载排队中心（FIFO/并发互斥/排队位可取消）+
   关联清理复盘审计（自启动/权限/默认应用让位四面全绿才算干净）。
2. **互通历史与别名**（F337）：历史面板凭据模式脱敏（**D-28 红线级
   缺陷当轮修复**——初版只换前缀不换值段，秘密仍在账里；重写为模式
   +值段整体替换）+ shell 别名三闸登记（可逆）。
3. **就近共享三件**（F321）：配对确认码握手（确定性六位码/三次错码
   封禁防暴力）+ 自适应分块（速度掉→块加大）+ 多接收方独立进度账。
4. **OCR 三件**（F314）：水平投影版面切行（覆盖完整性自证）+ 低置信
   阈值扫描重试（质量单调承诺）+ 批量多区域账（短板语义）。
5-8. **托底四件**（F315 触发质量账 / F307 去重双档+无痕双粒度 /
   F316-317 媒体豁免心跳续窗+演示 2h 倒计时 / F329 流式进度+预估校准）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.6 万行。逻辑核面已连续
五批深化，剩余达成率提升最陡的路径仍是 F332/F344/F337 三大目标项的
持续追加 + 界面层（等 F151/F216/F436 控件件）。下一批继续。

——AI-H3 · 深化批次五 · 2026-09-26

## 十、深化批次六（2026-09-26 · 负载发生器 / 事务回滚 / 通配与参数化）

**程序化实数**：总 25,625 行 / 纯功能 **15,661**（50 项 15,027 = 24.8%）；
域内单测 317 → **332**；隔离舱 cargo test 全绿 0 警告；域聚合 blocks 52 全绿。

**六组功能核**（详表见账本 §四·八）：

1. **合成负载发生器**（F332）：常载/正弦/突发三型确定性曲线——实机
   判据「Y7000 注入负载」的宿主侧替身；配套升降级振荡审计（最小驻留
   时间，抖动=策略缺陷事件显性化）与用户手选档位让位账（手动期机制
   动作压制计数——用户意志优先的结构面）。
2. **卸载域三件**（F344）：人话报告（三要素+残留分支文案分岔+技术
   折叠）+ 卸载事务性（undo 日志逆序回滚——中断不留半截卸载，提交后
   回滚拒绝的不可逆点语义）+ 蜂巢回收验证（残留蜂巢点名直出）。
3. **互通三件**（F337）：通配符匹配核（* 与 ? 递归+字典序确定性展开，
   不猜 [] 扩展语义）+ %1 脚本参数化（越位缺参显式错误）+ 粘贴回读
   保真闭环（归一化形比对 + 引号歧义诚实判红——POSIX 单引号方案对
   内嵌引号路径不可逆的边界显性化）。
4-6. **托底三件**（F309 连按轮转状态机账 + 80ms 去抖 / F304 改动标记
   点数据面 + 账实一致性审计 / F350 场景强度矩阵 + 私设强度点名审计）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.57 万行。逻辑核已连续六
批深化、测试 185→332；界面层（约 60% 工程量）仍等 F151/F216/F436 控
件件就绪。下一对话进入批次七，继续按缺口序推进。

——AI-H3 · 深化批次六 · 2026-09-26

## 十一、深化批次七（2026-09-26 · 全链端到端 / 事务确认门 / 路径边界）+ D-30 计数器 v2

**程序化实数（计数器 v2）**：总 26,566 行 / 纯功能 **16,453**（50 项 15,819
= 26.1% + hbase 531 + 域聚合器 103）；域内单测 332 → **352**；隔离舱
cargo test 全绿 0 警告；域聚合 blocks 52 全绿。

**六组功能核**（详表见账本 §四·九）：

1. **调速器整车试验**（F332）：`EndToEndVerifier` 把三型负载曲线逐点
   喂 FrameGovernor——突发必响应、健康负载（40% 占用）零误伤、延迟
   守卫全链不变式三断言；配套采样密度自适应（负载变化率→加密采样，
   平稳疏采省测量开销）。
2. **卸载三件**（F344）：确认输入校验（手打应用名才放行——破坏性二
   次确认数据面，错误提示说怎么改对）+ 阶段 ETA 账（剩余时间预估 +
   超预估 50% 漂移显性化——诚实进度）+ 多盘清理账（单盘失败不阻塞
   他盘 + 重试翻转 + 失败显性）。
3. **路径三件**（F337/F336）：MAX_PATH 260 边界审计（不截断——
   `\?\` 长路径前缀合法出路）+ 相对路径越界检测（`..` 逃逸 + 前缀
   校验）+ 批量转换账（失败留痕 + 成功率‰）。
4. **恢复条生命周期**（F311）：30s TTL 自动消退 / 一键消费 / 超时拒
   绝三态留痕 + 暂存区清理完整性对账（残留项直出）。
5. **通知分组规则表**（F330）：同应用才合组 + 跨组合法性审计 +
   59/60/61 分钟时间边界人话分岔钉死。
6. **双拼键位表校验**（F328）：声母映射无冲突、26 键无死键集合证明、
   四方案隔离、20 字抽样覆盖。

**D-30 计数器 v2**（本批最重要认账）：v1 四分法解析器按整行数花括号，
被 format! 模板里的 `{}` 扰乱深度追踪——copypath 深化层 338 行纯功能
被误判进测试区。重写 v2（剥离字符串/注释后数括号），随仓归档为唯一
口径；全量重算后 50 项达成率 24.8% → **26.1%**（历史值被系统性低估）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.5 万行。下一对话进入批次
八，继续按缺口序推进（F332/F344/F337 深水区 + 剩余托底 + 界面层等
F151/F216/F436 控件件）。

——AI-H3 · 深化批次七 · 2026-09-26

## 十二、深化批次八（2026-09-26 · 配置快照 / 冲突依赖 / 书签缓存）

**程序化实数（计数器 v2）**：总 27,526 行 / 纯功能 **17,039**（50 项 16,405
= 27.0%）；域内单测 352 → **364**；隔离舱 cargo test 全绿 0 警告；域聚合
blocks 52 全绿。

**六组功能核**（详表见账本 §四·十）：

1. **配置快照导出**（F332）：调速器六参数键值快照——人话可读导出、
   键白名单导入防呆、round-trip 可迁移机器证明（十四章开放性落地）。
2. **卸载三件**（F344）：冲突检测（运行中拒绝 + 人话提示三要素）+
   依赖断裂警告（影响面直出——知情卸载不阻断）+ 批量卸载账（逐件
   裁决独立推进，失败不阻塞批量）。
3. **互通三件**（F337/F336）：路径书签（两闸登记/形制化解析/可逆/
   导出）+ 环境快照合并导入（只增不覆盖——现值神圣，冲突留痕）+
   转换缓存（同键命中免重算、异味不误命中）。
4-6. **托底三件**（F338-340 多选批量撤销链——整链粒度 + redo 对称 /
   F325 版本保留策略表——30 天/500 上限机制面 + 备份豁免 / F308
   建议排序核——三源合并去重确定性排序）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.4 万行。下一对话进入批次
九，继续按缺口序推进。

——AI-H3 · 深化批次八 · 2026-09-26

## 十三、深化批次九（2026-09-26 · 健康探针 / 后悔期 / 行尾守卫）

**程序化实数（计数器 v2）**：总 28,320 行 / 纯功能 **17,482**（50 项 16,848
= 27.8%）；域内单测 364 → **379**；隔离舱 cargo test 全绿 0 警告；域聚合
blocks 52 全绿。

**六组功能核**（详表见账本 §四·十一）：

1. **调速器健康探针**（F332）：参数引用一致/账本容量/折算单调三项
   心跳自检（十三章·补自检语义落地）+ 模式驻留占比聚合（「降级占了
   多少时间」直接出数——性能感知运营面）。
2. **卸载三件**（F344）：5s 后悔期（窗内一键恢复/窗外拒绝计数/倒计时
   钳底——可撤销语义不装永远可逆的样子）+ 用户自定义保护件（导入
   导出 round-trip，出厂六件冻结只增不减）+ 卸载历史账（倒序可查 +
   累计释放）。
3. **互通三件**（F337/F336）：转义规则表导出（与 EscapeMatrix 同源
   投影——规则可查可备份）+ 目录清单批量引号（空串跳过留痕）+ 行尾
   格式守卫（CRLF/LF 保真、混排显性不保真不静默归一）。
4. **「您是不是要找」闭环审计**（F301）：零命中触发建议、建议可达
   （死路建议=缺陷）、三上限、有命中不出建议。
5. **未保存计数徽标**（F310）：脏账派生、9+ 折叠、保存关闭即时回落、
   两账同步审计。
6. **分类翻页核**（F313）：9 宫格、边界不回卷、短尾页如实、页码人话。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.4 万行。下一对话进入批次
十，继续按缺口序推进。

——AI-H3 · 深化批次九 · 2026-09-26

## 十四、深化批次十（2026-09-26 · 面板聚合视图 / 自动重试 / 批量缓存闭环）

**程序化实数（计数器 v2）**：总 29,145 行 / 纯功能 **17,972**（50 项 17,338
= 28.6%）；域内单测 379 → **393**；隔离舱 cargo test 全绿 0 警告；域聚合
blocks 52 全绿。

**六组功能核**（详表见账本 §四·十二）：

1. **降级面板聚合视图 + 事件流**（F332）：四处事实一处聚合（渲染层
   不自算）+ 模式切换事件流（同模式重入不记、环形封顶、人话导出可
   回放——十三章日志语义落地）。
2. **卸载三件**（F344）：冲突自动重试（退出续跑、3 次上限转放弃显性）
   + 卸载前后空间实测对比（±5% 同口径、差异人话指引残留扫描）+
   卸载清单导出（人话行可备份）。
3. **互通三件**（F337/F336）：历史去重容量联动（信息量优先淘汰）+
   书签分组（全路径消歧 + 歧义审计）+ 批量转换缓存闭环（命中率可测）。
4-6. **托底三件**（F307 高频词置顶权重 + 幽灵权重防线 / F306 增量索
   引账——指纹未变跳过 / F327 中英切换边界词账——三策略登记 + 丢弃
   字节诚实统计）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.3 万行。下一对话进入批次
十一，继续按缺口序推进。

——AI-H3 · 深化批次十 · 2026-09-26
