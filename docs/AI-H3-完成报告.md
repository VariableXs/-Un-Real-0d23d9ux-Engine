
## 十五、深化批次十一（2026-09-26 · 内存审计 / 哈希锚定 / 规则对拍）

**程序化实数（计数器 v2）**：总 29,936 行 / 纯功能 **18,424**（50 项 17,790
= 29.3%）；域内单测 393 → **408**；隔离舱 cargo test 全绿 0 警告；域聚合
blocks 52 全绿。

**六组功能核**（详表见账本 §四·十三）：

1. **内存占用审计**（F332）：调速器三类账本字节数上界估算 + 预算判定
   + 80% 贴线预警带（内存不许随运行时间无界增长）；配套面板空账
   降级（骨架→完整视图边界显性——零数据不虚报满效）。
2. **卸载三件**（F344）：**干跑清单哈希锚定**（确认的是 A 执行的必须
   是 A——清单与执行间任何改动被拦，顺序敏感）+ 服务残留自检（第
   五关联面——隐形尾巴直出可清）+ 同厂商批量模板（展开复用逐件裁
   决）。
3. **互通三件**（F337/F336）：转换历史审计（脱敏红线沿用）+ **引号规
   则对拍审计**——抓出 D-31 真实缺陷（needs_quotes 白名单缺 $/`/"
   三字符，含 $ 路径 POSIX 会被命令替换）并封闭集补齐 + 长度分级
   提示（安全/临近/危险渐进披露）。
4. **延迟项恢复时长账**（F303）：p95≤2s 与唤醒同口径、样本不足不虚
   报、反悔条目不参与统计。
5. **重复率统计**（F347）：键卡键指纹直出、小样本不虚报。
6. **主题包导出迁移**（F305）：键白名单 + round-trip 可迁移证明。

**里程碑**：F336 复制文件地址达成率 **108%**（775/715——第二个超额
完成项，与 F336 并列双超额）。

**剩余差距诚实说明**：距 60,710 上限仍差约 4.3 万行。下一对话进入批次
十二，继续按缺口序推进。

——AI-H3 · 深化批次十一 · 2026-09-26
