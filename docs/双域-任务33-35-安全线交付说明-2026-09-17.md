# 双域 · AI-S 安全线交付说明（任务 31/33/34/35 + 负向矩阵，2026-09-17）

> 需要完成什么——本文件只列交付物与验收对照；过程证据见 ktest 用例名。

## 任务 33 · 消息通道原语（`kernel/varix/src/security/msgchan.rs`）
- [x] 订阅/广播/单发三语义 + 通道名注册表（重名拒绝、16 通道定容）
- [x] 每订阅者定容队列 64 + 慢消费者丢最旧 + dropped 计数（inputsvc 同策略）
- [x] 同通道 FIFO ×1000 用例；容量边界/未注册/未订阅/载荷超限负向用例
- [x] ktest 6 用例（msgchan::tests）

## 任务 34 · 剪贴板/拖放白名单化（`kernel/varix/src/security/clip.rs`）
- [x] 路径裁决/审计复用 vfsguard（任务30），不另造权限模型
- [x] pid 级剪贴板授权位图（对象维度，与路径维度正交，任务36 UI 接管增删）
- [x] 未授权进程读剪贴板得空 + 审计（Ok(None)+deny 记录，总案验收原句）
- [x] 格式白名单三类：文本/图片引用/文件引用；线上未知格式码拒绝
- [x] 伪装拒绝：Text 非 UTF-8、ImageRef 无魔数、FileRef 白名单外路径；拒绝零副作用
- [x] 拖放 = 同一裁决通道（dragdrop→adjudicate）+ 审计
- [x] ktest 5 用例（clipsrv::tests）

### 与 Windows 侧剪贴板行为差异清单（完善性验收，如实公示）
- [x] Windows 剪贴板全局共享；VARIX 剪贴板按 pid 授权位图收敛（未授权=得空）
- [x] Windows 支持 N 种格式并行；VARIX 起步仅文本/图片引用/文件引用三类
- [x] Windows 无格式真实性校验；VARIX 读写两端做魔数/编码校验（防伪装）
- [x] Windows 拖放走 shell COM 通道；VARIX 拖放等价文件读写走 VFS 白名单
- [x] 图片引用起步只认 png/jpeg/gif/bmp 魔数，不认识 = 拒绝（诚实口径，不猜）

## 任务 35 · Uxv 交换格式接入（`kernel/varix/src/security/uxv_ingest.rs` + `security/blake3.rs`）
- [x] 内核侧 = 只读校验门（写侧/解包复用 `src-tauri/crates/container`，不复制实现）
- [x] 校验语义对齐引擎 open()：SuperBlock magic/未 seal 拒绝/Footer 双副本 B 优先 A 兜底/schema=2/索引越界/BLAKE3 index_hash
- [x] BLAKE3 内核自实现（security.rs 同款真加密纪律），与官方 crate 全长度段交叉验证 + 官方向量 abc/空串
- [x] 校验失败 = 整包拒绝 + 原因码表（8 类），绝不产生半文件
- [x] 掉电注入 ×10（确定性截断+位翻转）全被拒收
- [x] 单文件大小上限声明：MAX_INGEST_BYTES = 256MiB（ktest 断言固定）
- [x] 内核镜像零依赖不变（blake3 仅 dev-dependency，测试链路专用）
- [x] ktest 4 用例（uxvingest::tests）

## 任务 31 · 路径逃逸负向用例（并入 stage4 矩阵 + 复用任务30 vfsguard）
- [x] 16 变体全拦截（`..` 上跳×4/混合分隔符/全角反斜杠/U+2215/U+2044/全角点/NUL 控制字符/盘符/UNC/保留设备名×2/结尾空格/结尾点/段尾点）
- [x] 逃逸拒绝理由 = BadPath（逃逸是安全事件非参数错）
- [x] 符号链接：exFAT 无符号链接对象，如实声明无此攻击面

## 负向演练矩阵（总案阶段4·步骤10，`security/stage4_matrix.rs`）
- [x] 4 进程 × 4 路径形态 × {读,写} × {文件/拖放/剪贴板FileRef/共享内存} + 通道未放行 = 116 组合自动化
- [x] 88 组拒绝数算术唯一可复核；零崩溃（大栈线程执行，遵守 >64KB 禁栈上戒律）
- [x] 审计全覆盖断言（拖放+剪贴板拒绝落 journal ≥32 条）
- [x] 未覆盖组合如实声明：通道×pid 全组合待垫片（任务26）携带身份；实机矩阵待任务26/27 接线后补挂

## 门禁
- [x] cargo ktest --lib：2915 passed / 0 failed（含本批 15 新用例 + 并行 AI-S 任务30/32 既有 17 用例）
- [x] cargo kcheck：0 warning / 0 error
- [x] cargo kbuild：成功
