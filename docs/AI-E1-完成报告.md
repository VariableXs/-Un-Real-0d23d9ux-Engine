# AI-E1 完成报告 · Varix STAR I 个性化全域（F151-F170）

> 分工包：主册《Varix STAR I start.md》E-5 深化设计报告（G-E-01 ~ G-E-20）判据实装层。
> 分工边界：AI-E1 严格限定 F151-F170；未触碰 AI-H1~H4、AI-J1/J2 及其他任何分队任务面。
> **v4 状态（本报告）**：深化批次四（引擎群+实验室接线）已收口——
> E 域单测 **278/278 PASS**、typecheck（`--incremental false` 全新口径）本域零错误、
> walkcheck 自检 PASS、tally 检查项对账二十桶全非零。隔离验证与检查项对账见
> `_attic/aie1-f151-f170/隔离验证与检查项对账.md`。
> 实现层级：主册【工程量】标注本域为「界面组件与交互层」——落点为
> `src/system/persona/`（TS/React），与 AI-K1/K2 的 Rust 内核泳道互不相交。

---

## 1. 交付物台账（v2 全景）

| 批次 | 位置 | 内容 | 规模 |
| --- | --- | --- | --- |
| v1（已提交 `8b4203d0`） | `src/system/persona/` | 20 项判据实装：19 纯逻辑模块 + PersonaStudio + 底座总线 | 26 文件 / 5,620 行 |
| v2 引擎层 | `src/system/persona/*-engine.ts` ×13 | 深化引擎：主题过渡编排/壁纸下载管线/图标缓存与失效总线/指针渲染规划/音频合成/布局几何/动效监控/档案签名链/锁屏状态机与粒子/字体批量扫描/IME 候选布局/快捷键解析与证据/小组件摆放 | 13 文件 / 2,320 行 |
| v2 接线层 | `pages-*.tsx` + `PersonaStudio.tsx` | 二十页全量接线：引擎能力全部变成可操作界面（见 §3 逐页清单） | 增量 ≈1,750 行 |
| 测试 | `__tests__/` ×13 | **176 用例**（v1 135 + 引擎批 41），含 2 个引擎批测试文件 | 1,910 行 |
| 工具 | `tools/vx-walkcheck-e.py` | F170 走查脚本（v1 交付，v2 复验） | 200 行 |
| 接线点 | `PersonaTab.tsx` / `SettingsModal.tsx` / `bus.ts` | 双入口注册（v1 已提交，v2 零改动） | — |

**纯功能合计 ≈9,687 行（wc -l，不含测试/空行口径见 §4）；测试 1,910 行。**

## 2. 判据对账（20 项 × 主册验收标准第一句）

### 2.1 对账总表

| 项 | 判据锚 | 逻辑层（v1） | 深化引擎（v2） | 界面接线（v2） | 单测 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| F151 主题令牌全集 | G-E-01 | tokens.ts (487) | theme-engine：diff/对比度自修/硬编码扫描/vtheme 双向桥 (350) | 令牌表页 + AA 审计采纳 + 硬编码扫描器 | 13+9 | ✅ |
| F152 实时预览编辑器 | G-E-02 | preview.ts (138) | theme-engine：TransitionDriver 300ms 双层交叉 | diff 摘要行 + 过渡帧演示 | 5+3 | ✅ |
| F153 主题深浅自动切换 | G-E-03 | autodark.ts (291) | theme-engine：中段原子替换编排 | 手动暂停/今晚跳过（v1）+ 过渡可视化 | 9+2 | ✅ |
| F154 壁纸每日一换 ¹ | G-E-04 | dailywall.ts (217) | wallpaper-engine：下载队列/预载/多屏池/EXIF (237) | 下载管线面板 + 排队演示 | 8+6 | ✅ |
| F155 图标包热更换 | G-E-05 | iconswap.ts (153) | icon-engine：LRU 缓存/失效总线/SVG 审计/迁移 (190) | 广播事件流 + 安全审计面板 | 5+6 | ✅ |
| F156 指针编辑器 | G-E-06 | pointer.ts (179) | pointer-engine：播放头/渲染规划/vxpointer/.cur+.ani (222) | DPR 渲染规划 + 互通评估面板 | 5+5 | ✅ |
| F157 声音混合器 | G-E-07 | soundmix.ts (88) | audio-engine：合成试听/解析链/音量 ramp (150) | 解析链与合成特征面板 | 4+4 | ✅ |
| F158 开始菜单布局预设 | G-E-08 | startpresets.ts (187) | layout-engine：几何计算器/缩略规格 (204) | 布局几何实算卡 | 6+3 | ✅ |
| F159 字体安全档 | G-E-09 | fontguard.ts (214) | font-engine：name 表/回退链/批量扫描 (202) | 回退链卡 + 批量扫描协议 | 7+3 | ✅ |
| F160 动效强度预演 | G-E-10 | motiontier.ts (78) | motion-monitor：帧时采样/档位建议/WP-207 (151) | 性能监控面板（注入演示） | 4+5 | ✅ |
| F161 个性化档案导出 | G-E-11 | archive.ts (266) | archive-engine：签名链/分节校验/迁移 (145) | 导出签名 + 导入验签 + V0 迁移 | 6+3 | ✅ |
| F162 每应用主题例外 | G-E-12 | appexcept.ts (139) | archive-engine：锁定令牌派生 | 例外行锁定表预览 | 6+2 | ✅ |
| F163 桌面小组件开关集 | G-E-13 | widgets.ts (119) | widgets-engine：吸附/碰撞/回收/调度 (137) | 摆放引擎面板 | 5+5 | ✅ |
| F164 锁屏定制 | G-E-14 | lockcustom.ts (91) | boot-engine：状态机/节流/让位/摘要 | 状态机演示 + 让位预览 | 4+4 | ✅ |
| F165 开机动画个性化 | G-E-15 | bootskin.ts (209) | boot-engine：确定性粒子/烘帧清单 | canvas 四幕重播（预览即真播） | 5+3 | ✅ |
| F166 输入法皮肤 | G-E-16 | imeskin.ts (141) | ime-menu-engine：候选布局/分页提示 (139) | 布局引擎实算试打框 | 5+3 | ✅ |
| F167 右键菜单自定义 | G-E-17 | ctxmenu.ts (172) | ime-menu-engine：菜单总装器（二级收纳） | 总装预览（真实渲染模型） | 6+4 | ✅ |
| F168 任务栏个性化 | G-E-18 | taskbarprefs.ts (71) | layout-engine：任务栏几何/自动隐藏五态 | 几何实算 + 状态机演示 | 5+2 | ✅ |
| F169 快捷键查看器 | G-E-19 | shortcuts.ts (277) | shortcut-engine：事件语法/冲突分级/键位迁移 (169) | 解析器重录 + 冲突审计 + 导出导入 | 8+5 | ✅ |
| F170 个性化域总判据 | G-E-20 | verdict.ts (495) | shortcut-engine：证据归档器 | 证据包导出（完整性校验） | 7+3 | ✅ |
| UI 界面层（二十页） | 通用十二查 | PersonaStudio + pages ×5 + ui | — | 全量接线（v2 核心） | — | ✅ |
| **合计** | | **27 文件** | **13 引擎** | **二十页全量** | **176** | **全绿** |

¹ F154 在 F200 候删五项冻结名单内（冻结后不再投入工时）——本域按「19/19 判成」口径执行，
F154 的既有实装与 v2 引擎保持可用但不追加工时，去留待 Variable 季度审视拍板。

### 2.2 v2 深化批的新增判据落点（主册【设计细节】逐条）

- **F151**「导出含未覆盖令牌清单」+ B-1104「30 界面零硬编码」→ 硬编码扫描器
  （确定性命中：与令牌默认物理值完全相等才算违例——门禁要确定性不猜测）；
  对比度审计采纳建议色（保持色相只调明度轴——不改作者意图）。
- **F152**「应用过程 300ms 交叉淡入」→ TransitionDriver：双层亮度守恒
  （旧+新恒 100%）、中段原子替换恰好一次（跳帧安全）——diff 摘要在应用前可见。
- **F154**「预加载次日壁纸（空闲时段——换的时刻零等待）」→ planPreload
  空闲窗口交集；「多屏前瞻接口」→ pickForMonitors；下载只写白名单缓存、
  校验失败即弃（数据安全红线）。
- **F155**「广播失效（订阅制不轮询）」→ iconInvalidationBus 接通三个动作，
  页面留事件流；SVG 资产安全审计（脚本注入向量全拒）。
- **F156**「sprite 生成含 1x/2x 双套（乙节基线）」→ renderPlan 按 DPR 选档
  （≥物理尺寸的最小者、热点物理坐标精确）；「导入兼容 .cur/.ani 评估」→
  互通评估报告面板。
- **F157**「试听走独立一次性流」→ PreviewChannel；「音量曲线感知对数」
  落地成解析链展示 + 80ms 防爆音 ramp。
- **F158**「预设卡缩略图实时渲染（真缩小版非贴图）」→ presetThumbSpec 确定性几何。
- **F159**「扫描超时 → 分批后台扫+先给部分结论」→ BatchFontScanner
  （500 字/批、部分缺字率先行）；「回退路径可用」→ buildFallbackChain 永不落空。
- **F160**「预演自身掉帧 → 提示性能受限（诚实）」→ previewHonestyNote；
  WP-207「进度环转显性进度数字」→ progressNumberText 双格式。
- **F161**「包签名（F127）」→ signArchive/verifySignature 全链（导出即签、
  导入先验、篡改拒绝）；「跨版本导入 → 兼容矩阵」→ migrateArchiveV0toV1 自动迁移。
- **F162**「深浅例外实现=应用收到锁定令牌表（全局广播时跳过）」→
  lockedTableForException 派生并可视。
- **F163**「组件渲染走桌面层独立脏区」+「8px 吸附网格（F084 同族手感）」→
  snapToGrid/resolvePlacement/reclaimOffscreen 全接线（越界回收不静默丢弃）。
- **F164**「唤醒路径三段预算」+「密码聚焦让位」+「通知只计数」→
  focusRetreatTransform 真变换参数入预览、lockMachineStep 节流（5 次/15s）、
  buildNotificationDigest 摘要。
- **F165**「预览复用动画本体（预览即真话）」→ 确定性种子粒子模拟
  （mulberry32 同种子同画面）canvas 重播四幕 + bakeManifestAll 烘帧清单对拍。
- **F166**「候选窗每次弹出重读皮肤参数」+「9 档翻页键提示」→ layoutCandidates
  实算（分页/几何/提示），试打框由引擎驱动。
- **F167**「隐藏项在二级完整保留（功能不丢只收纳）」→ assembleContextMenu
  总装预览（系统组保序 + 应用组 F072 排序 + 二级收纳 + 100ms 预算对账）。
- **F168**「大图标档增高 56px」「左对齐锚点切换」「托盘折叠联动」→
  taskbarGeometry 实算可视；自动隐藏五态状态机（含焦点钉住、全屏强制）。
- **F169**「重录捕获层独立于输入系统」→ parseComboFromEvent（Esc 取消、
  单键拒绝、规范序归一）；「冲突检测即录即查」→ classifyConflict 全表审计；
  键位迁移走 vxkeymap v1 契约格式（导出/导入双向）。
- **F170**「三步验收记录归档验收目录」→ buildEvidenceDoc + evidenceDocComplete
  证据包导出（完整性不足时按钮诚实拒绝）。

### 2.3 F170 三步执法实测

- `tools/vx-walkcheck-e.py --selftest`：**SELFTEST PASS**（checklist 19 项 +
  verify 合法样本 PASS + 注入失败样本被检出）。
- verdict 单测 7/7：runDomainVerdict 19/19 全绿、执行 <0.1 分钟（预算 30）、
  证据链完整率 100%、域级零残留（令牌表哈希执法前后一致）。

## 3. v2 界面接线清单（二十页全量）

| 页 | 接线内容 |
| --- | --- |
| 令牌表 | AA 对比度审计（一键采纳建议色）· 硬编码扫描器（确定性命中清单） |
| 实时预览 | 应用前 diff 摘要（「N 处差异」）· 300ms 过渡帧演示（中段原子替换可视） |
| 深浅自动切换 | v1 三态配置（定时/日落日出/主题对）+ 60s 预告 toast 二钮 |
| 每应用例外 | 例外行锁定令牌派生预览（锁定画布底/强调色看得见） |
| 壁纸每日一换 | 下载管线面板：空闲窗口状态/预载计划/排队调度演示/多屏独立池 |
| 图标包热更换 | 失效广播事件流（换装/回退/卸载全接总线）· SVG 安全审计演示 |
| 指针编辑器 | DPR 1/2 渲染规划（sprite 选档+热点物理坐标）· .cur/.ani 互通评估 |
| 声音混合器 | 六事件解析链（合成参数+路径+响度）· 80ms 音量过渡演示 |
| 开始菜单预设 | 布局几何实算（格子/列数/长辈达标）· 各预设缩略规格 |
| 字体安全档 | 回退链（永不落空字体栈）· 批量扫描协议（部分结论先行） |
| 动效强度预演 | 帧时采样注入演示 → 降级执法判定 → WP-207 进度数字 |
| 我的档案 | 导出签名信封 · 导入验签（篡改拒绝）· 分节校验 · V0→V1 迁移 |
| 桌面小组件 | 摆放引擎（吸附/碰撞推开/越界回收）· 更新调度表（15s 抖动） |
| 锁屏定制 | 让位布局真变换参数 · 锁屏状态机（输错节流演示）· 通知摘要 |
| 开机动画 | 确定性粒子 canvas 四幕重播（预览即真播）· 烘帧清单 |
| 输入法皮肤 | 候选窗布局引擎实算（分页/几何/翻页提示）· 16ms 预算标注 |
| 右键菜单 | 菜单总装预览（一级/二级收纳/锁定项/上下文过滤/预算对账） |
| 任务栏 | 几何实算（图标位/折叠线/托盘起点）· 自动隐藏五态状态机演示 |
| 快捷键 | 事件语法解析重录（Esc 取消/单键拒绝）· 全表冲突审计 · vxkeymap 导出导入 |
| 域总检 | 三步执法执行 · checklist 导出 · 证据包导出（完整性门禁） |

## 4. 行数对账（诚实呈报 · v3）

**实际纯功能代码 ≈10,348 行；主册上限口径目标 20,800 行；达成率 ≈49.8%**
（v1 5,620 → v2 9,687 → v3 10,348；测试 2,136 行 / 192 用例，零注水——
每行都是可运行的功能代码，无空转循环、无复制粘贴重复、无无意义包装层）。

缺口归因（如实登记，与 v1 归因一致并推进）：

1. **素材资产管线**（F156 双倍率 sprite 位图生产、F164 模拟钟面 4K 资产、
   F165 全密度帧序列烘焙产物）——需要素材生产环境，逻辑引擎已就绪等待资产灌注。
2. **真机走查证据**（四档 DPI 截图、录屏帧检、示波法 16ms 实测）——随闸门补测
   清单见 §6，实机/QEMU 判据按产线纪律登记排队，不阻塞产线。
3. **二十页空态/错误态双语全量铺开**（labels 词典扩容）——下一批次优先项。

**深化批次三建议拆单**（对齐 AI-J1 v2→v3 轨迹）：labels 双语扩容 →
集成点实接（F116 日落注入/F072 计数直连/F127 打包直通）→ 素材管线批次。

## 5. 与其他分队的接口纪律

- 依赖消费：F124 曲线、F141 对比度公式（复用 theme-studio 同函数——一处一事实）；
  F072/F116/F121/F126/F127/F133/F147 等为接口预留位（各模块头注释显式标注）。
- 未申报加塞：零（附录 F 纪律）。本批改动全部位于 `src/system/persona/` 域内文件。
- 非功能产物：本批施工期未产生探针残留与临时脚本（测试全走 vitest 常规通道），
  无需 `_attic` 归档；`tools/update-varix-usb.ps1` 等工作区未提交文件属其他会话，
  本批次不触碰。

## 6. 随闸门补测清单（开发期零 QEMU 纪律 · 产线双轨）

| # | 补测项 | 环境 | 触发闸门 |
| --- | --- | --- | --- |
| 1 | F151 热替换真机 4K 四档 DPI 截图（1:1/125%/150%/200%） | 宿主浏览器/桌面壳 | 里程碑收官 |
| 2 | F152 改-预览延迟真机计时（含渲染段）+ 300ms 过渡录屏帧检 | 桌面壳 | 里程碑收官 |
| 3 | F153 定时三连测 ±5s + 交叉淡入无白屏帧录屏 | 桌面壳 | 里程碑收官 |
| 4 | F156 双倍率 sprite 4K 放大验证 + 热点 1px 取放对拍 | 桌面壳 | 里程碑收官 |
| 5 | F163/F164 组件与锁屏 80fps 账本对账（F041） | 实机 | 随闸门 |
| 6 | F165 开机动画 8.0s±0.2s 实机录屏（U 盘引导） | 实机 | 随闸门 |
| 7 | F166 16ms 红线组合期实测（示波法/环回法） | 实机 | 随闸门 |
| 8 | 二十页无障碍全键盘走查 + 焦点环抽查 | 宿主 | 里程碑收官 |

## 7. 复现口令

```bash
# E 域全量单测（176 用例）
npx vitest run src/system/persona
# 本域类型检查（应零输出）
npx tsc --noEmit 2>&1 | grep persona
# 生产构建
npx vite build
# F170 walkcheck 脚本三模式
python tools/vx-walkcheck-e.py --selftest
python tools/vx-walkcheck-e.py --checklist
# 域总检（PersonaStudio → 系统面 → 域总检 → 执行 19 项三步验收 + 导出证据包）
```

—— AI-E1 · 2026-09-26 · v2 收口 · Not a corporate drone. Just... good.

## 8. 深化批次三（v3 · 集成点层 · 本节为 v3 增补）

**新增模块**：
- `compat-matrix.ts`（F151/F161）：令牌版本兼容矩阵三态判定（native/migratable/
  unsupported）+ v0→v1 迁移引擎（11 桥全接、非法值逐项指出、迁移前原始输入随行
  ——随时可退）+ 降级清单前置可见。
- `integrations.ts`（跨域集成点层）：F116 日落注入点（SunProvider 注册/注入/
  ±5min 判定——Schema 先行，F116 就绪即插即用）；F072 计数直连（批量灌入+
  外部输入清洗+两源对账差异如实报告）；F121 双路保险（直接回退 vs 还原点路径
  复测——分叉即回炉）；F127 打包直通声明（vxappPackDescriptor）；F187 时钟
  强一致判定（1s 容差）。

**既有引擎增补**：archive-engine 包体积预估（导入前字节数+人话刻度+资产引用
计数）；widgets-engine 断网降级「数据截至」+ 性能保护执法判定（超预算降透明
×0.6/关闭诚实不干预）；imeskin F126 增节 JSON Schema + 皮肤包双向校验；
font-engine 3500 字 <100ms 扫描预算判定（归一化口径）。

**接线增量（六处）**：令牌表页兼容矩阵卡（版本判定/v0 迁移演示/应用迁移表）；
深浅切换页 F116 注入点状态卡（provider 注册/今日注入/±5min 对拍）；档案页
体积预估并入差异预览；小组件页断网降级与性能执法演示行；输入法页 schema 导出；
右键菜单页 F072 灌入演示；域总检页双路保险实跑行。

**验证**：批次三 16 新用例（engines-batch3）全绿；域累计 **192/192**；
typecheck `--incremental false` 全新口径本域零错误；tally 检查项对账二十桶
全非零（`_attic/aie1-f151-f170/tally.mjs` 可复跑）。

**缺陷账（批次三捕获 · 全即时修）**：①v2 收口时 tsc 增量缓存假阴性掩盖了
2 处真实错误（unused import + autoHideNext 单参数误调）——已修，验证规程
升级为强制 `--incremental false`；②heredoc 笔误一处（Edit 重写修正）；
③Edit 前未回读导致一次插入点不匹配（回读后修正）。全记录见
`_attic/aie1-f151-f170/隔离验证与检查项对账.md` §4。

**全树 build 说明**：v3 时点 vite build 被 U3Tab 在途 WIP（未提交的
named-export 缺失）阻塞——非 E1 任务面不代修；E1 最近一次全树 build 证据在
v2 收口时点通过；隔离口径（vitest 单目录 + tsc 域内过滤）不依赖全树健康。

—— AI-E1 · 2026-09-26 · v3 收口 · Not a corporate drone. Just... good.

## 9. 深化批次四（v4 · 引擎群+实验室接线 · 本节为 v4 增补）

**新增引擎（12 件 · 全部纯逻辑零 DOM 依赖 · 零注水）**：

| 引擎 | F 项 | 能力 |
| --- | --- | --- |
| `palette-engine.ts` | F151/F154 | 中位切分量化（体积最大箱优先变体）· 壁纸亮度判类与和谐判定 · img→tokens 九键派生底稿（全灰壁纸诚实拒绝造强调色）· 色彩命名 |
| `icon-atlas.ts` | F155 | shelf-next-fit 图集装箱（缩档尊重 4K 底线、overflow 显性化）· O(1) 命中测试 · 尺寸阶梯/mip 链 · 缓存键前缀失效契约 · <2s 换包预算拆账 |
| `cursor-physics.ts` | F156 | Unity 规范 SmoothDamp（临界阻尼无过冲、帧率无关、大 dt 稳定）· 拖尾环形缓冲 · 点击状态机（抖动容忍/拖拽/移出取消/长按一次/连点去抖）· 热点半上取整精确变换 · 动画指针跳帧不追帧播放头 |
| `sound-synthesis.ts` | F157 | 五种振荡器（含确定性噪声）· ADSR 包络 · 恒功率摆位多层合成 · soft clip 防削波 · RMS 归一 · 等功率淡化 · 线性重采样 · 16-bit WAV 编码 · 六事件合成谱 |
| `widget-data.ts` | F163 | 月历时钟面（闰年正确）· 天气归一（华氏转制/脏值逐项拒绝/体感回退）· 系统采样清洗 · 调度抖动+指数退避（封顶 8×）· 新鲜度四级 · 开销滑窗 P95 |
| `lockscreen-composer.ts` | F164 | 两拍渐进层栈（第一拍 <100ms）· 唤醒 ≤2s 预算拆账 · 壁纸亮度→虚化参数 · 让位 easeOutCubic 插值 · 通知摘要前 4 组折叠 · 解锁状态机（冷却按轮次翻倍封顶 5min） |
| `ime-composition.ts` | F166 | 拼音音节切分（最长匹配+回溯、歧义串多解）· 真词库候选生成（全匹配+前缀）· 组合串纯编辑器（B-904 只收字母）· 9/页分页与数字键直选 · 组合期旗标 |
| `ctxnav.ts` | F167/F168 | 菜单扁平化与键盘导航（回绕/跳过禁用/锁定项 Enter 不激活/type-ahead 循环）· F215 层级审计 · 托盘溢出布局（钉选挤出=缺陷级信号）· 热区数学 |
| `chord-engine.ts` | F169 | 运行期唯一匹配面：两段序列缓冲（1s 超时/Esc 清空/失败不串段）· 冲突序列谁也不触发 · IME 组合期让位 · 全表一致性机械对拍 |
| `verdict-report.ts` | F170 | DomainVerdict→Markdown 报告 · 跨轮回归四分法 · 抖动检测（红绿交替≠稳定通过）· 证据完整性深度门禁（含 7 天新鲜度）· FNV-1a 证据包验签 |
| `tokens-schema.ts` | F151 | Draft-07 JSON Schema 生成（第三方作者预检）· CSS 变量双向桥（45 变量契约、键名对齐 tokens.css 惯例、派生变量回收跳过）· 无障碍字号 1.25× 等比钳制 |
| `wallpaper-solar.ts` | F153/F154 | NOAA 高精度日出日落（中心差方程+均时差+经度修正，较 Cooper 兜底精度翻倍）· 极昼极夜显性策略 · 预载窗规划（跨天双摆位求交）· 多屏并行度 2 封顶 |

**接线增量**：`pages-lab.tsx` 十一个实验室面板（调色板/天文历/图集装箱/指针物理/
合成实验室/小组件数据/锁屏合成/拼音组合/菜单导航/序列匹配/报告与证据包/
Schema 桥——预览即真话，跑的是引擎本体）；五页组全量挂载（令牌表页+
SchemaLab/PaletteLab、深浅切换页+SolarLab、图标页+AtlasLab、指针页+
CursorPhysicsCard、声音页+SynthLabCard、小组件页+WidgetDataCard、锁屏页+
LockComposerCard、输入法页+ImeLabCard、右键菜单页+CtxNavCard、快捷键页+
ChordLabCard、域总检页+ReportLabCard）；labels 双语扩容约 70 键。

**验证**：批次四 86 新用例（engines-batch4/5/6）全绿；域累计 **278/278**；
tsc `--incremental false` 本域零错误（严格模式 18 处首过全部清零）；
walkcheck SELFTEST PASS；tally 二十桶全非零、未登记桶归零（FILE_TO_F 已登记
三个新文件）。行数对账：**14,072 / 20,800 = 67.7%**（批次四净增 3,724 功能行）。

**缺陷账（批次四 · 全即时修，10 条全录）**：extractPalette 行寻址被采样步进
污染（🟡）、planPreload 跨午夜窗整窗丢失（🟡）、UnlockMachine 冷却按累计失败
而非轮次（🟡）、smoothDamp 私改公式 NaN（🟡）——四处逻辑缺陷全部被单测兜住
后修正；verdict-report 引用臆造类型（写前未回读，重写对齐）；toCssVariables
键名双前缀、fromCssVariables 只读变量误拒、pages-lab 死代码残留、JSX 单父、
tsc 严格模式 18 处。全记录见 `_attic/aie1-f151-f170/隔离验证与检查项对账.md` §4。

**批次五候选**（对齐 67.7% → 目标线的缺口 ≈6,700 行）：素材资产管线
（F156 双倍率 sprite 位图、F165 烘帧产物）→ 双语空态/错误态全量铺开 →
真机走查证据随闸门补测。

—— AI-E1 · 2026-09-26 · v4 收口 · Not a corporate drone. Just... good.
