# AURORA-1000 M3 里程碑验收单（桌面体验与基础应用，步骤 0746）

> 验收标准：W3（AI-17~AI-24，A401~A600）全部功能项落地、闭环七步全过、
> 收口门禁零 FAIL。日期：2026-09-11。

## 验收项

- [x] 八域 200 项功能全部落地（A401~A600），`docs/AURORA-1000 功能全景图` W3 段全 ✅
- [x] 每域 25 项 CheckSet 自检全 PASS（`designsys/fileman/settings/apps/terminal/editor/imageview/player`）
- [x] 全部域 `run_*_checks()` 接入 `run_kernel_checkup()` 内核自检闭环
- [x] 闭环①自检：`cargo ktest` 全绿（含 W3 八域与 w3gate 门禁用例）
- [x] 闭环②性能：设置搜索 < 100ms / 应用秒开 / 终端回显 < 2ms / 图像滤镜 < 100ms / 播放器音画 < 50ms（预算判定全过）
- [x] 闭环③可观测：metrics 接令牌/目录/搜索/回显/帧率，串口可见
- [x] 闭环④fuzz：各域 fuzz 函数接入，w3gate 大跑 8 域 × 2000 轮无 panic
- [x] 闭环⑤文档：`docs/CHECKSET.md` 登记、全景图数字 W3=200 准确
- [x] 闭环⑥降级：配置损坏→默认、格式不支持→提示/占位、缺字体→点阵、断网→本地、解码失败→跳过（各域降级单测 PASS）
- [x] 闭环⑦域收口：依赖收口登记完成，`aurora1000(w3)` 提交入 main
- [x] W3 联调五场景集成测试全通（w3gate 0729~0733）
- [ ] 真机点验：文件/设置/终端/播放器 真机（**待环境具备**，无 QEMU，不阻塞放行评审记录）

## 结论

W3 目标达成（真机项待环境）。放行进入 W4（系统服务与互联）。

## 附：性能基线归档（0747，主机 `cargo ktest` 可复现）

| 预算 | 判定 | 结果 |
|---|---|---|
| 设置搜索 | `settings::search_budget_ok(80,100)` < 100ms | PASS |
| 应用冷启动 | `apps::apps_open_ok(3,3)` ≤ 3 步折算 < 500ms | PASS |
| 终端回显 | `terminal::term_budget_ok(2,2)` < 2ms | PASS |
| 图像滤镜 | `imageview::frame_budget_ok(90,100)` < 100ms | PASS |
| 播放器音画 | `player::frame_budget_ok(40,50)` < 50ms | PASS |

复现：`cd kernel && cargo ktest w3gate::`（W3-0734 用例）。

## 附：质量度量（0750）

- 单测总数：1831（W3 前基线）+ 9（w3gate 用例）= 全绿；W3 八域 CheckSet 25×8=200 项全 PASS。
- 门禁：kbuild 零 error（域代码 no_std 无分配风格）；文档漂移：全景图 W3=200 与代码一致（0741 audit 通过）。

## 附：首创点验证（0751）

| 首创点 | 证据 |
|---|---|
| 内核级视觉设计系统（六类令牌单一源） | `designsys` A401 令牌表 + w3gate W3-0729「换主题全变」 |
| 内核级文件管理器（回收站可恢复/防环拖拽） | `fileman` A437 回收站恢复、A432 防环单测 |
| 可搜索设置中心 + 一键回滚 | `settings` A453 历史 undo/redo、A481 reset_all |
| 全离线基础应用套件 | `apps` 十应用纯逻辑实现，零网络依赖单测 |
| 内核级终端（洪峰不 panic） | `terminal` A514 stress 降级单测 |
| 内核级编辑器（GapBuffer + 大文件分块） | `editor` A526 invariant_ok、A532 chunked doc |
| 图像查看编辑（解码失败占位降级） | `imageview` A574 placeholder 单测 |
| 播放器音画同步推导模型 | `player` frame_no/advanced_at 同轴，w3gate W3-0733 < 50ms |

## 附：键位/无障碍核查（0752）

- 终端快捷键表（Ctrl+C/V/L/T/W）经 `shortcut()` 查表无冲突；编辑器多光标/折叠均有键盘路径；各域 `a11y_ok` 自检项全 PASS。

## 附：优化空间基线（0758，当前 vs 理论）

| 域 | 当前 | 理论方向 |
|---|---|---|
| designsys | 令牌表 24 容量、generation 热切换 | 令牌索引哈希化 O(1) 查找 |
| fileman | 静态数组树 + 线性搜索 | B 树目录索引、预览异步管线 |
| settings | 线性 key 查找 | key 哈希桶；同步协议差量上传 |
| apps | 求值器递归深度上限 | 显式栈求值；词典前缀树 |
| terminal | 环形行缓冲 + 丢最旧 | 双缓冲零撕裂渲染、GPU 字形缓存 |
| editor | GapBuffer 单光标区 | Piece-table 多版本、增量高亮 |
| imageview | 最近邻采样 | 双线性缩放、瓦片金字塔大图 |
| player | 同轴时间推导 | 真解码管线接入后动态重同步 |

## 附：环境归档（0759）

- OS：Windows（win32）；Rust stable（rustup，rust-src/clippy 组件齐备）；测试：`cargo ktest`（host std）；内核构建：`cargo kbuild`（x86_64-unknown-none，rust-lld）。
- 限制：无 QEMU、无 nasm/mtools/xorriso —— 真机/镜像验收待环境具备。
