# W4 → W5 交接清单（步骤 0998）

## 交给 W5 的东西

1. **代码基线**：main 上 W4 八域（netweb/notify/search/sysmon/pkgstore/printing/a11y/apower）+ 集成域 `aurora/w4_integration.rs` 全绿；冻结 tag `aurora-m4`。
2. **自检闭环**：`robust::run_kernel_checkup()` 已接入 AI-31~AI-40 全部 10 个域 CheckSet（MAX_DOMAINS=96），W5 的 domain-gate 直接复用。
3. **性能基线**：见 `docs/BASELINE.md` M4 段（帧 16.7ms 预算、启动 < 3s、内存 < 1GB、唤醒 ≤ 900ms）。W5 步骤 1212 性能终测以此对照。
4. **fuzz 语料**：netweb/search/notify/sysmon/pkgstore/printing 各有确定性 fuzz（seed 固定），W5 大跑（1211）合并调度即可。
5. **文档**：CHECKSET.md W4 段、M4-验收.md、知识/M4-系统服务.md、知识/M4-复盘.md。

## W5 注意事项（接 M4-复盘 教训）

- perf/stability/asecurity/testing/help 模块挂载名：`perf`/`stability`/`asecurity`/`testing`/`help`（lib.rs #[path] 注记为准，勿另起别名）。
- 真机点验欠账：W4 步骤 0980 与真机矩阵（第 7 章）需环境具备后统一补测。
- rustup 工具链损坏时按 `kernel/rust-toolchain.toml`（stable → 1.97.1）重装，勿改 CI 钉板。

## 未尽事项

- [ ] 0980 W4 真机点验（待环境）
- [ ] 真机矩阵 1241~1290（W5 后真机章执行）
