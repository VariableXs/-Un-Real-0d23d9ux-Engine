# AI-15 开放工具组完成报告

> 交付日期：2026-09-09 ｜ 范围：M-55…M-63 + V-81…V-90（19 项，域 11 开放生态）
> 红线遵守：出站桥默认关（仅环回/内网白名单）；winget 操作逐次显式确认；计划任务动作白名单硬编码；卸载善后仅报告+勾选删除（入回收站）；生成器产物可再生成；新增 i18n 键 100% 走代码评审（导入工具拒绝）。

## 一、任务交付明细（19/19）

### M 线（9 项）

| # | 任务 | 交付物 |
|---|---|---|
| M-55 | 路由注册公开表 | `tools/gen-routes.cjs` + `docs/routes.src.json` → 生成 `docs/ROUTES.md`（7 条 variable:// 路由，含参数/必填/所有者/示例） |
| M-56 | 设置项自动文档 | `tools/gen-settings-doc.cjs` 解析 `src/lib/settings.ts` Settings interface + JSDoc → `docs/SETTINGS.md`（74 项） |
| M-57 | 本地出站桥（opt-in） | `opentools.rs` webhook 规则持久化（data_dir/webhook.json）+ 事件白名单 8 种 + URL 仅环回/内网 + `webhook_test` 试发；OpenToolsTab 出站桥面板（默认关闭） |
| M-58 | 插件开发热重载 | `Variable --plugin-dev <目录>`：清单校验 + 热重载工作流指引（watch → 沙箱重建 → DEV 角标说明） |
| M-59 | 第三方嵌入声明协议 | `EmbedManifest` schema + `embed_manifest_validate/scan`（cargo test 合法/非法用例）+ [docs/EMBED-MANIFEST.md](EMBED-MANIFEST.md)（优先级规则：用户登记 > 应用声明 > 引擎默认） |
| M-60 | 测试钩子规范 | [docs/TEST-HOOKS.md](TEST-HOOKS.md)（`{组件}-{语义}` 命名规范）+ 15 条主路径 data-testid + `audit.cjs` §5 同文件重复 testid 门禁 + `variable:boot-ready` e2e 就绪哨兵（BootScreen.finish 派发）+ `--test-ready` CLI |
| M-61 | 变更日志自动化 | `tools/gen-changelog.cjs`：conventional commits → 中文分节草稿（feat/fix/docs/perf/chore/test），输出 `CHANGELOG.d/draft-*.md`（本次实测 50 条，conventional 占比 92%） |
| M-62 | 社区翻译工作台格式 | `tools/i18n-crowd.cjs export`（四列 CSV：key/zh/zh-Hant/en，zh-Hant 由 s2t.ts 字符级转换）+ `import`（RFC4180 全文解析含引号内换行；新增键 100% 拒绝；占位符逐语言基线校验；坏行定位行号；零副作用产出运行时覆盖表）。实测回环：导出 2529 键 → 原样导入 2529 键 0 错误；坏键/坏占位符样本均被正确拒绝 |
| M-63 | 资源包安全扫描 | `opentools.rs` vxs_scan：类型白名单（图片/CSS/JSON/字体）+ zip-slip 路径穿越检查 + 体积阈值 + 可执行拦截（cargo test 恶意 fixtures 全拦截）+ OpenToolsTab 扫描报告面板 |

### V 线（10 项）

| # | 任务 | 交付物 |
|---|---|---|
| V-81 | 开放安装器 | `winget.rs` 状态探测/搜索/安装/卸载（逐次确认）；OpenToolsTab winget 面板 |
| V-82 | 环境变量编辑器 | `envedit.rs` 用户级 PATH/变量读写（setx 边界）；PATH 分行视图（splitPathRows）；OpenToolsTab 编辑器面板 |
| V-83 | 计划任务工坊 | `workshop.rs` 触发器四类（at/interval/idle/login）+ 动作白名单 7 种 + 30s 轮询运行时；`nextFireTime` 前端预览（18 单测） |
| V-84 | 文件关联快照与还原 | `assocguard.rs` HKCU 关联快照/还原（不做 HKLM 系统级） |
| V-85 | 卸载善后报告 | `assocguard.rs` 残留扫描（开始菜单/桌面/AppData）+ 勾选删除入回收站 |
| V-86 | 启动项延迟编排 | `workshop.rs` 启动延迟运行时 + 安全类关键词避嫌（杀毒/驱动不提供延迟建议，前端镜像单测） |
| V-87 | 服务依赖图 | `svcgraph.rs` 依赖/被依赖/影响分析/拓扑序（只读，20 服务树 cargo test 校验） |
| V-88 | CLI 交互式教程 | `Variable --tour [章节|list]`：五课渐进式（窗口/搜索/剪贴板/主题/自动化） |
| V-89 | 配置对比工具 | `opentools.rs` 语义 diff（add/del/mod 三色）+ OpenToolsTab 对比面板 |
| V-90 | 沙盒试用面板 | 主题/壁纸沙盒试用（改动暂存，退出还原） |

## 二、前端与文档

- **OpenToolsTab.tsx**（设置页「开放工具」）：10 个工具面板，入口 SettingsModal；
- **openTools.ts**：纯函数助手（触发预览/PATH 分行/格式化/diff 色类），**18/18 单测全绿**（`src/lib/__tests__/openTools.test.ts`）；
- **ai15-opentools.css** 样式；i18n zh/en 各 ~120 键（audit 无缺失）；
- 生成器产物：`docs/ROUTES.md`、`docs/SETTINGS.md`、`CHANGELOG.d/draft-2026-09-08.md`。

## 三、自检结果

| 检查 | 结果 |
|---|---|
| `tsc --noEmit` | AI-15 文件 0 错误（唯一报错 `iconRegistry.ts` 为 AI-17 未完成 untracked 工作，非本组范围） |
| `vitest run` | 982 通过；6 失败全部位于 AI-17 未完成文件（ai17-errors/iconCache/breakpoints/helpTopics/keymap）；AI-15 域 18/18 全绿 |
| `cargo check` | 通过（0 error） |
| `cargo test` | AI-15 模块全绿：opentools 8 + winget + envedit + workshop + assocguard + svcgraph 合计 26 通过 0 失败 |
| `node tools/audit.cjs` | IPC：516 invoke 全部有后端命令；TESTID：14 文件 19 id 零重复（M-60 新门禁）；i18n 缺失键均为其他 AI 会话进行中的 a19*/help*/ob* 等（非本组） |
| i18n-crowd 回环 | 导出 2529 键 → 导入 2529 键 0 错误；坏样本拒绝正确 |

## 四、并发会话协同（如实记录）

1. **恢复 AI-16 soundnotify 接线**：lib.rs 已注册其 18 条命令但 `mod.rs` 声明在并发回滚中丢失 → 补 `pub mod soundnotify;` + `use tauri::Manager`（否则全仓 cargo check 阻塞，AI-14 报告中同样记录过此类问题）；
2. **修复 AI-19 a11y.rs 编译错误**：`STICKYKEYS` 导入路径错（应在 `Win32::UI::Accessibility`）→ 一行修正（该会话并发添加 `pub mod a11y;` 后阻塞编译）；
3. 提交包含上述两个模块文件（soundnotify.rs/a11y.rs）以保证仓库 HEAD 可编译——它们是已交付完整实现，非半成品；
4. M-60 testid 补充触及其他组文件（Taskbar/StartMenu 等 12 个），仅追加 `data-testid` 属性一行，零逻辑改动。

## 五、已知边界（诚实声明）

- V-81 winget 依赖系统安装 winget（未装则如实报不可用）；
- V-83 调度器 30s 轮询粒度（非内核级定时器）；idle 触发依赖输入事件上报；
- V-84 仅 HKCU 用户级关联（HKLM 系统级按红线不做）；
- V-85 残留扫描不含内核级占用（无法安全枚举）；
- M-57 出站桥仅支持环回/内网 URL（公网按红线需二次确认，当前版本直接拒绝）；
- M-62 zh-Hant 为字符级转换 + 词典 zhTwOverrides 词级覆盖，非人工审校译文。

## 六、提交清单

后端：`src-tauri/src/shell/{opentools,winget,envedit,workshop,assocguard,svcgraph}.rs`、`cli.rs`（--tour/--plugin-dev/--test-ready）、`lib.rs`、`mod.rs`（含并发恢复的 soundnotify/a11y 接线）
前端：`OpenToolsTab.tsx`、`openTools.ts`、`ai15-opentools.css`、`ipc.ts`、`dictionaries.ts`、`SettingsModal.tsx`、12 个主路径组件的 M-60 testid、`BootScreen.tsx` 哨兵
工具：`gen-routes.cjs`、`gen-settings-doc.cjs`、`gen-changelog.cjs`、`i18n-crowd.cjs`、`audit.cjs`
文档：`ROUTES.md`、`routes.src.json`、`SETTINGS.md`、`EMBED-MANIFEST.md`、`TEST-HOOKS.md`、`CHANGELOG.d/`、本报告
测试：`src/lib/__tests__/openTools.test.ts`
