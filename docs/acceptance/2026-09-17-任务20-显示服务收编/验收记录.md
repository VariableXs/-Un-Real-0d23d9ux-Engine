# 任务20 验收记录 — 显示服务收编：Surface 归口 + 双缓冲 + 脏矩形提交（AI-K · 2026-09-17）

## 需要完成什么（总案施工步骤 13 口径）

- [x] 阶段 0 绘制原语收编为唯一内核显示服务（main.rs 构造服务并 install；绘制面唯一来源=fb.rs Surface + font.rs，服务自身零绘制原语、只有行搬运）
- [x] 双缓冲（后备=PMM order-10 4MiB 连续块 + HHDM 线性映射；attach 时前台快照到后备）
- [x] 脏矩形提交（commit 列表通道 + mark_dirty 登记环 32 槽，满转全屏合并绝不丢脏）
- [x] 视觉：高速滚动抽帧无撕裂（360 帧滚动条带、宿主连拍 10 帧逐帧分析全 clean）
- [x] 逻辑：双缓冲交换竞态用例（宿主 std::thread mark/commit hammer ×1000）
- [x] 代码：bootui 与显示服务合并后 grep 无重复实现（启动链路径；登记体域边界如实声明）
- [x] 完善性：分辨率切换能力声明

## 交付文件

- [x] `kernel/varix/src/displaysrv.rs`（新建 ~620 行）：DisplayService（Direct/DoubleBuffered 双模式）+ Rect 裁剪 + 32 槽脏登记 + 行级 blit + Drop 归还 PMM 块 + target::display_probe + 宿主用例 ×9
- [x] `kernel/varix/src/fb.rs`：Surface::base_ptr() 访问器（后备→前台行拷贝）
- [x] `kernel/varix/src/lib.rs`：`pub mod displaysrv;`
- [x] `kernel/varix/src/main.rs`：显示服务构造 + install（收编挂点）+ display_probe 探针
- [x] `kernel/varix/src/inputsvc.rs`/`fs/exfat_ro.rs`/`fs/fs23_disk.rs`：清理 5 处测试档警告（ktest 构建零警告）

## 架构决策（如实声明）

- [x] **boot 期直写=双缓冲的退化形式**：boot 链单核、无并发消费者、无遮挡，直写前台无撕裂且零提交开销（任务1/3 实机视觉证据零漂移）；main 用 `new_direct` 显式直写模式收编，bootselect/HUD/domain 绘制零改动
- [x] **双缓冲真实消费场景**=future shell（多线程）：服务接口已就绪（draw_surface 借用强制"先画完再提交"），宿主竞态用例示范 Mutex 外层防护；实机撕裂验证由探针独立 Double 实例完成
- [x] **grep 无重复实现边界**：启动链绘制路径 fill_rect/draw_text 定义唯一（fb.rs/font.rs）；gfx/aurora/gperiph 域的同名函数属 15000 项登记体、与启动链零调用，由后续任务接入时收编——不在本任务范围（不越权改登记体）
- [x] **分辨率切换能力声明**：Limine 协议下分辨率由引导参数决定（GOP 直通），服务与分辨率无关（几何全部来自前台 Surface；任务3 已归档 800×600/1280×720/1920×1080）；运行期热切换需按新引导参数重建服务

## 宿主测试（ktest 2855 全绿 = 2846 + 9 新增）

- [x] direct_mode_writes_front_immediately：直写立即生效（收编前语义保持）
- [x] double_mode_commit_moves_pixels：commit 前前台不动、commit 后同步
- [x] dirty_rect_isolation：脏区外前台保持（局部提交正确性）
- [x] clip_oob_and_partial：出屏/零尺寸忽略、部分出屏裁剪
- [x] mark_dirty_registry_and_overflow_merge：32 槽满转全屏合并、旗标复位
- [x] mark_full_equivalent_to_whole
- [x] attach_twice_rejected：二次注入拒绝
- [x] resolutions_800_720_1080：三分辨率前后台一致
- [x] concurrent_mark_and_commit_no_loss：双线程 hammer ×1000 后全屏提交前台≡后备（无状态损坏）

## 实机（QEMU 11.1 q35 双盘环境）

- [x] mode=double 1280×800（4MiB 后备分配成功，HHDM 映射）
- [x] mark_full 清屏路径：overflow_merges=1 精确一次
- [x] 滚动条带 360 帧：frames=361 rects_flushed=361 逐帧脏矩形提交
- [x] 静态收尾帧：彩带+四角色块 5 矩形一次提交（多矩形脏提交/边界裁剪）
- [x] **撕裂抽帧：10/10 帧 clean**——每帧恰好一条连续 40 行纯蓝带、无混色行、无双带、带外全黑（连拍全部落在 3.6s 滚动窗口内）
- [x] verdict=ok frames=362 rects=366 merges=1
- [x] fs23/共享分区/输入探针零回归（探针序：NVMe→fs23→shared→input→display）

## 实机排查记录

- [x] 首轮抓帧全 "torn-partial-rows"：根因=**console 镜像**（main.rs:148 enable_mirror）把每条 kinfo 画到前台字符格——"scroll begin" 日志在清屏后打印，字形（LightGray 0xAAAAAA）重新上屏。修复：清屏 commit 移到全部打印之后
- [x] 二轮 shot5-9 误报：根因=抓帧节奏 0.85s/帧超出 3.6s 滚动窗口，尾部帧拍到静态帧四角色块+内核 HUD（合法内容）。修复：持久连接连拍 0.2s/帧全部落入窗口

## 门禁

- [x] kcheck 0 告警；ktest 2855 passed / 0 failed；ktest 测试档构建 0 警告；kbuild release 成功 + make-iso-qemu 12253184 字节

## 凭证

- [x] `docs/acceptance/2026-09-17-任务20-显示服务收编/实机串口-最终验收.log`（probe 全链 INFO 行）
- [x] `docs/acceptance/2026-09-17-任务20-显示服务收编/display-shot-{0,5}.ppm`（抽帧样本：滚动中段+后段）
- [x] `docs/acceptance/2026-09-17-任务20-显示服务收编/display-probe-drive.py`（连拍+撕裂分析脚本）
