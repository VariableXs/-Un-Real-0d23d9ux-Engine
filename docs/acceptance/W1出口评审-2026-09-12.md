# W1 出口评审报告（VARIX-M400 · 2026-09-12）

## 结论：**通过**（附 1 项登记欠账）

## 评审范围

M400 W1 波次收口评审，核心两项：CI 门禁真实化 + QEMU 引导复验。

## 1. CI 门禁真实化 ✅（本次修复后全绿）

### 历史：run 全部 startup_failure（0 jobs）

根因：`.github/workflows/ci.yml` 四处 step name 含**裸冒号**（`M-83:`/`M-86:`/`F181:`/`F182:`），
YAML 解析失败 → 每个 push 只产生一个 0-job 的 failure run，形同虚设。

### 修复链（4 个提交，全部经 Git Data API 推送）

| 提交 | 内容 |
|------|------|
| 95205ad | YAML 四处 name 加引号 → CI 首次真正调度出 4 个 job |
| 84dc7de | ① audit.cjs 正则适配 `#[tauri::command(async)]`（修复 516 个命令的全量误报）②补 zh 词条 `wfHiddenRestoreAll` ③K2 改 `cargo ktest --lib`（msvc bin unwinding 已知坑）④B6 改字节级 GPT 校验（sgdisk 输出格式不稳）⑤README 目录锚点 6 处修正 |
| 950c35a | 恢复被误归档的 7 个 MPA 入口 html（desktop/app-*、explorer、datavault——上次清理误伤，vite 多页构建依赖它们） |
| 5819643 | 两个环境敏感测试加固：msr 统计断言加测试间互斥锁；spin 争用断言仅在 ≥2 并行度时强制 |

### 终局

**run 34692645288 @ 4a6916e1：success** —— frontend（typecheck/vitest/audit/keymap/link-check/build）、
backend（cargo test）、kernel（kcheck/ktest --lib/kfuzz/kbuild + ELF 产物）、
boot（xorriso → initrd → ISO → USB 字节校验 → **云端 QEMU 冒烟引导至 boot complete**）四路全绿。

## 2. QEMU 引导复验 ✅（M400 后内核）

本机链路：kbuild（零告警）→ make-iso.sh（xorriso，GPT/MBR 校验 OK）→ QEMU q35/512M →
串口：`self-test: 13/13 pass` → `interrupts enabled` → **`boot complete — halting`**，无 fatal/panic。

## 登记欠账（不阻塞出口）

- **Z-08 裸 keydown 迁移**：13 个新增裸监听（并行会话引入，nova/windows 系模块），keymap 基线已更新至 66；
  应迁移到 `src/lib/keymap/registry.ts` 统一登记（下次 W2 入口前完成）。

## 其他记录

- 推送通道：github.com:443 间歇性被重置，全程走 api.github.com（Git Data API blob→tree→commit→ref）。
  教训沉淀：API 推送脚本必须 `core.quotepath=false` 解析 diff-tree，否则中文路径会被转义成乱码文件（本日第 3 次踩坑）。
- 清理纪律修订：根目录的 html 可能是构建入口，归档前必须先对照 vite.config / 构建配置确认。
