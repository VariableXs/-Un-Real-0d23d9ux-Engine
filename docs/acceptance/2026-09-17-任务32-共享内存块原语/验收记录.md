# 任务32 验收记录 — 共享内存块原语＋授权模型（AI-S）

- 日期：2026-09-17
- 交付：`kernel/varix/src/shmsrv.rs`（526 行，commit 6142b62 入库；并行 AI-S 会话三检复核后随其提交入库，本记录补齐实机证据归档）
- 前置：任务 15（ring3 用户态）✅

## 1. 总案验收对照（阶段4 步骤4 + 阶段4 逻辑门）

| 总案验收项 | 证据 |
| --- | --- |
| create/map/revoke 三语义（+destroy/unmap/access 完整闭环） | shmsrv.rs 公开面七操作全实现；宿主 `create_map_roundtrip_owner_rw` |
| 授权模型：句柄不可传递默认 | 实机 `shm-probe: no-pass default-deny verdict=ok`；宿主 `map_without_grant_denied`（句柄号泄露 ≠ 有权） |
| 创建者显式授权（禁转授） | 实机 `grant-and-no-regrant verdict=ok`（A 转授 B 被拒）；宿主 `grant_enables_map_and_forbids_regrant` |
| 块销毁后映射失效 | 实机 `destroy bumps gen (handle never reused same-gen) verdict=ok`；宿主 `destroy_invalidates_all_and_handles_never_reused`（升代防旧句柄重放，gen 0→1） |
| 句柄传递攻击用例被拒 | 实机 `handle-passing-attack denied verdict=ok`（句柄号+令牌值双泄露路径均拒）；宿主 `handle_passing_attack_denied` |
| revoke 即时失效（授权位+既有映射） | 实机 `revoke invalidates grant+mapping verdict=ok`；宿主 `revoke_invalidates_grant_and_mapping` |
| 槽位/容量明确报错 | 宿主 `grant_and_map_slots_exhausted_then_reused`（第 5 授权/第 9 映射拒绝+复用）、`size_limits_and_capacity`（17 块拒绝） |

## 2. 实机证据（QEMU q35 + NVMe，`_attic/shmvfs-demo.py`，日志 `shmvfs-session{1,2}.log`）

会话1（全新盘面）：

```
[ INFO] shm-probe: owner roundtrip verdict=ok
[ INFO] shm-probe: no-pass default-deny verdict=ok
[ INFO] shm-probe: grant-and-no-regrant verdict=ok
[ INFO] shm-probe: handle-passing-attack denied verdict=ok
[ INFO] shm-probe: revoke invalidates grant+mapping verdict=ok
[ INFO] shm-probe: destroy bumps gen (handle never reused same-gen) verdict=ok
[ INFO] shm-probe: stats after drill used=1 g=0 m=0 verdict=ok
[ INFO] shm-probe: verdict=true matrix=[roundtrip,default-deny,grant,revoke,attack,destroy-gen]
```

会话2（跨 kill QEMU 断电，同盘面重启）幂等复跑全绿（七步同上全 verdict=ok）。两会话 PANIC=0（驱动脚本逐行扫描失败行负向断言）。

## 3. 宿主门禁

- ktest：8/8 用例全绿（全库终态 **2915 passed / 0 failed**，含并行会话 31/33/34/35 用例）
- kcheck：0 告警；kbuild release 成功；ISO 构建成功
- 实现形态声明：内核侧注册表模型（"映射"=登记+令牌 pid/seq 复核；无 MMU 用户态前 access 返回块可见区切片；ring3/vmm 落地后换真实页映射，语义面不变）——与任务38 报告 §2.1 内存段行（已标 ✅ 已落地）一致

## 4. 排障记录（3 枚）

1. **ktest 栈溢出（STATUS_STACK_OVERFLOW）**：`Box::new(ShmStore::new())` 先在测试线程栈（Windows 默认 1 MiB）物化 1 MiB 临时值 → 改 `Box::<ShmStore>::new_uninit()` + 整块原地清零（全零位模式 ≡ `ShmStore::new()`，逐字段等价）。探针侧同步改为 `static mut STORE: ShmStore = ShmStore::new();` 静态初始化器位置 const 求值（全零落 .bss，运行时零栈物化）——任务56「>64KB struct 禁栈上/Box 中转物化」戒律的第二次应用。
2. **宿主用例抓获首组件双斜杠**：normalize 类比（vfsguard）中 `/a` 被拼成 `//a`（根已有斜杠仍补分隔符）→ 修为仅非首组件补分隔符，宿主用例 `normalize_collapses_dot_and_slashes` 锁定。
3. **探针期望错 1 处（实现正确）**：初次探针把 `/a/../secrets` 当作逃逸期望——实际它是带内合法回溯（收敛 `/secrets`，应 NoRule 默认拒绝）；修正探针期望并增补「带内收敛拒绝/放行」两条证据项，真逃逸（`/../secrets`）保持 Escape 断言。

## 5. 凭证

- 驱动脚本：`docs/acceptance/2026-09-17-任务32-共享内存块原语/shmvfs-demo.py`（与任务30 共用双会话驱动）
- 串口日志（本地留存，gitignore 口径）：`_attic/shmvfs-session{1,2}.log`，关键行已内嵌本记录
- 源码：`kernel/varix/src/shmsrv.rs`
