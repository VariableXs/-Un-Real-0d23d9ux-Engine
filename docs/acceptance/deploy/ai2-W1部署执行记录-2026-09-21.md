# AI-2 · W1 部署执行记录（2026-09-21 23:32 → 09-22 00:24）

> 三体《实施总步骤图》S1.2 执行记录 · 路线三（干净装 + WE 壁纸库搬家 + 数据走 SHARED）。
> 结果先行：**W1-DEPLOY-DONE —— Win11 25H2 专业版已部署至 X:（WIN_ENGINE，300GB），完工自检 PASS，ready 标记在位，ESP 零触碰。**

## 1. 执行过程（含四轮启动的三次自纠）

| 轮 | 时间 | 模式 | 结果 | 根因与修复 |
|---|---|---|---|---|
| 1 | 23:32 | ShellExecuteW 提权，可见窗口 | 死于 G2→G3，无 FAIL 标记 | `Get-Volume` 的 `FreeSpace` 属性缺失 + `Set-StrictMode` → PropertyNotFoundException（当时无全局 catch，窗口随脚本退出被误认为误关） |
| 2 | 23:35 | 同上 | 死于同点 | 同根因（当时无诊断手段） |
| 3 | 23:39 | 隐藏窗口 + 全局 catch + G3a/G3b 步进标记 | EXC 精确落日志：**PropertyNotFoundException: FreeSpace** | 修复：容量/空置口径全部换 `Win32_LogicalDisk`（工作记忆"Get-Volume FreeSpace 不可信"的凶版实证） |
| 4 | 23:43 | 隐藏窗口 | 死于 dism 启动瞬间，静默，事件日志/WER/Defender 全空 | 原因未定（外部静默终止）；**工程决策：改计划任务模式**（会话 0 独立执行，免疫父进程/窗口问题） |
| 5 | 00:14 | **计划任务 VarixW1Deploy（RL HIGHEST，会话 0）** | **✅ W1-DEPLOY-DONE（00:23:58，dism 全程 40分42秒）** | —— |

## 2. 部署闸门记录（全部 PASS 后才动盘）

- G1 卷=X: label=WIN_ENGINE @ Lenovo thinkplus 1TB（BusType=USB）✓
- G2 容量 300GB（±30 判定）✓
- G3 空置（已用 0.11GB < 5GB）✓
- G4 ISO 实证（D:\VarixDeploy\Win11_25H2_Chinese_Simplified_x64_v2.iso，SHA-256=7408581e…02e4）✓
- WIM 选版：**index=4 = Windows 11 专业版**（与内置盘同版本；消费数字许可证可激活）
- 格式化：X: NTFS 快速，**卷标签 WIN_ENGINE 保留**（EngineConfig::discover / 预检口径不受影响），复核 PASS

## 3. 完工核验（独立只读探针）

| 项 | 结果 |
|---|---|
| X:\Windows\System32 / ntoskrnl.exe / explorer.exe | 全在位 ✓ |
| 卷标签 | WIN_ENGINE ✓（保留） |
| 已用 / 剩余 | 14.32 GB / 285.7 GB ✓ |
| ready 标记 | X:\varix-w1-ready.txt ✓（route=W1-route3-clean, index=4, espTouched=false） |
| ESP 引导区 | **零触碰**（S1.3 辖区，见 §5） |

## 4. 已知瑕疵（如实记录）

1. 部署脚本选版日志/marker 的 `edition` 名记录为残留变量"专业工作站版"（选版逻辑本身正确——index=4 实际应用的就是专业版）；marker 修正写入被沙箱对可移动卷的写限制拒绝（见 §5 注），index 字段为权威口径，待下次提权会话顺手修正；
2. run-4 的静默死亡原因未定（事件日志/WER/Defender 全空）——计划任务模式绕开，已按工程决策收束；
3. 计划任务 `VarixW1Deploy` 保留在任务计划程序中（SC ONCE 仅手动触发，无自启风险），下次提权会话可 `schtasks /Delete /TN VarixW1Deploy /F` 清理。

## 5. 下一步（明确边界）

1. **S1.3（AI-1 辖区）**：U 盘 ESP 补 bootmgr+BCD 指向 X:\Windows（handoff_target 参数化）——本部署刻意不碰 ESP；接线完成后 X: 的 Windows 才能从 F12/bootselect 引导；
2. **首次引导（步骤 4）**：S1.3 后由用户在场执行（F12/引导菜单 → WIN_ENGINE Windows → OOBE → 激活确认）；
3. **步骤 5/5b/5c**：驱动、Variable 便携包自启、快速启动关、Steam+WE 接 W:\SteamLibrary、LTSC 化——首次引导后执行；
4. **WE 壁纸库搬家（§0.3）**：可在首次引导前的任意时间由用户在内置盘执行；
5. 下机后 4 项核验（SOP 1.8）在首次引导后收口。
