# 任务 39 · PE 装载器＋静态链接 exe 加载运行 — 验收记录

- **归属**：AI-B（桌面后端线）
- **日期**：2026-09-17
- **前置**：任务 38 ✅（评估报告同日）；任务 15 ✅（装载引擎，commit 668e8a0）
- **边界（诚实声明）**：不含导入表解析（任务 40）。导入目录 RVA/Size 已解析暴露在 `PeImage::import_dir` 供任务 40 直接消费；带导入映像在实机链路如实拒绝（绝不带着未解析 thunk 进 ring3）。

## 交付物

| 文件 | 内容 |
|---|---|
| `kernel/varix/src/proc/pe.rs` | PE32+（AMD64）解析器：DOS/PE/COFF/可选头/节表逐字段校验，14 种具名拒绝（BadDosMagic→EntryNotExecutable）；`PeSource` 适配器接入任务 15 装载引擎（SegmentView 翻译、W^X 可写节强制 NX）；`MAX_IMAGE_SIZE` 256MiB 上限；ImageBase 用户半区双端校验 |
| `kernel/varix/src/proc/hello.pe` | 静态 PE64 样例（1024B）：.text 单节 R|X，代码 = `write(1,"hello from PE\n",14)` → `exit(0)`，与 hello.elf 同一 syscall ABI；无导入表、无重定位（静态按首选基址 0x140000000 装载） |
| `tools/make-pe.py` | 样例生成器（纯 Python 手工构造 PE32+，无第三方依赖；含独立结构自校验） |
| `kernel/varix/src/proc/ring3.rs` | 实机接线：监护链在 hello 两轮生命周期后装载并运行一轮 PE 实例（`spawn_pe`，与 spawn_hello 同链：PCB 入表 → 共用装载引擎 → 页账本登记 → iretq → exit → wait 收割 → 压力探针） |

## 验收结果

### 宿主测试（ktest 口径，RUST_MIN_STACK=16MB）
- `proc::pe` 5 用例全绿：真实样例解析校验 / 任务15 引擎端到端装载（入口机器码逐字节核对 `48 C7 C0 02`=mov rax,2、消息串精确落位 0x140001040、R|X 页 W=0 NX=0、栈页 W=1 NX=1、3 帧账本、全量回收一页不漏）/ 导入目录解析但不解析转发（任务40 分流）/ 敌意头 13 变体逐名拒绝 / W^X 可写节强制 NX。
- 全量 ktest：**2889 passed / 0 failed**（基线 2876 + 13 新增）。

### 实机（QEMU q35，串口证据）
QEMU q35（`varix-qemu.iso`，130s 运行含 input-probe 90s 窗口）串口证据 `build/pe-run-serial.log`：

```
[ INFO] ring3: PE spawned pid=2 entry=0x140001000 pages=17 imports=false — 任务39 静态 PE 装载
[ INFO] ring3: iretq → PE user (rip=0x140001000 rsp=0x7ffffffff000 rflags=0x3200)
hello from PE                                   ← PE .text 的 syscall write 真实输出
[ INFO] ring3: exit(0) pid=2 → zombie=true — 控制权交还监护者，wait 收割
[ INFO] ring3: supervisor wait → pid=2 code=0 pages_released=17/17 — 空间随进程消亡，一页不留
```

- PE 实例与 hello.elf 同一监护链：spawn → iretq → ring3 write → exit → wait 收割，零 PANIC；
- 既有链路零回归：hello.elf 两轮（int 0x80 + syscall 双入口 raxw=0x11/0xfffffffffffffff8）、64 槽压力探针 fill/drain、task15 lifecycle complete 全部照常；
- 页账本 17/17 全额回收，表清空无泄漏。

### 门禁
- kcheck 0 告警 0 error；kbuild 成功；make-iso-qemu 产出 varix-qemu.iso 实机引导。
