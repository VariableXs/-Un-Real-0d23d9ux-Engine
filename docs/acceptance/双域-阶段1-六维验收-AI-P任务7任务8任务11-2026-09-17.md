# AI-P 任务 7 / 8 / 11 六维验收（2026-09-17）

## 交付物

- [ ] -> [x] `portable/AI-P/Build-ESP.ps1`：ESP 双链组装（任务 7）
  - VARIX 链：EFI\BOOT\BOOTX64.EFI（Limine UEFI）/ limine-bios.sys / limine.conf / kernel\varix / initrd.img（存在才收）
  - Windows 链：EFI\Microsoft\Boot\{bootmgfw.efi,BCD}（来自 -WindowsBootDir；目录与 VARIX 链互斥）
  - ESP-MANIFEST.json：双链文件 + SHA-256；-VerifyOnly 按清单校验；重入不破坏对方链
- [ ] -> [x] `portable/AI-P/Deploy-Varix-USB.ps1`：五分区总编排（任务 8）
  - Preflight（盘检测/系统盘拒绝）→ Partition（复用 Create-Partitions.ps1）→ ESP（复用 Build-ESP.ps1）→ Shared（复用 Init-Shared.ps1）→ SysFiles（robocopy，缺省如实跳过登记）→ Verify（双校验复用）→ Report（分区图+容量表归档）
  - 断点续作：deploy-state.json 逐阶段落账，重跑跳过已完成阶段
- [ ] -> [x] `portable/AI-P/Manage-Versions.ps1`：多 U 盘版本管理（任务 11）
  - Snapshot：vMAJOR.MINOR 单调递增（非法降版拒绝）；MANIFEST.json 哈希清单
  - New-Diff：对「当前版本之前」最近历史做 added/changed/removed 差集 -> _versions\diff-X-Y.json（阶段 9 升级包输入）
  - Compare：双盘 MANIFEST 哈希逐一比对 -> Base 相同性结论（差异 exit 1）
  - Retire：退役文件移入 _trash\retired-<时间戳>\（只进不自动删，零残留）

## 六维验收

- [x] 视觉：部署报告 ASCII 分区表 + 容量核对表（Report 段归档 docs/acceptance/deploy/）；本线无 UI
- [x] 逻辑：篡改被 VerifyOnly 检出；重入不破坏 Windows 链；版本降版拒绝；差集/同盘一致/差异盘拒绝/回收站留档，负向用例全覆盖（36 项自测）
- [x] 代码：复用 Create-Partitions/Build-ESP/Init-Shared 不重写（编排只做段调度）；StrictMode Latest；错误全 throw 不静默；UTF-8 BOM
- [x] 开放性：链源路径（内核 ELF/Limine/limine.conf/WindowsBootDir）全部参数化；MANIFEST/version 字段可升版；第六分区/新链不改主逻辑
- [x] 简洁性：单文件各司其职；无同语义重复实现；grep 复核无 $rel/$Rel 类大小写碰撞残留
- [x] 完善性：❌ 登记两条——VM 首启引导验证（任务 7 验收尾项）与产盘端到端 ×2（任务 8 验收尾项）需 Hyper-V 实机，移交任务 88 终局联验；差分升级包实体打包移交任务 66

## 测试证据

- [x] Test-AIP2.ps1：36/36 通过（输出：双域-阶段1-AIP2自测输出-2026-09-17.txt）
- [x] Test-AIP.ps1 回归：25/25 通过
- [x] portable/tests/Run-PortableTests.ps1 -SkipExec：70 项 PASS（含 4 个新脚本语法解析）

## 遗留（非静默缺失）

- [ ] VM 首启 + 产盘端到端：需管理员/Hyper-V 实机环境，脚本链路已全部就绪（-> 任务 88）
- [ ] 推送 GitHub：上一轮网络阻断，本轮 commit 后重试
