# 双域-任务88/89-终局联验与文档收口-2026-09-18

> 任务 88（终局联验）与任务 89（文档收口）的验收归档。收口会话内全部门禁复核记录、
> 「U 盘随插随用」完整旅程走查证据、残项三色登记表均在此文件。
> 用户红线遵守：本文件是任务表 88/89 自身规定的归档物（证据簿），不是交付报告替代品。

## 一、收口门禁复核（2026-09-18 本会话实跑）

| 门禁 | 结果 | 证据 |
|---|---|---|
| V 线 tsc | 0 错（修复 9 处 `noUncheckedIndexedAccess` 索引访问后归零） | `npx tsc --noEmit` |
| V 线 vitest | 2787 passed / 4 skipped（含本批新增 skeleton/dataMigration/journeys 23 例） | 全库 `vitest run` |
| K 线 ktest | 3026 passed / 0 failed（首轮 3025/1 为宿主时序敏感用例偶发，复跑零失败） | `cargo ktest` |
| K 线 kcheck | 0 告警 | `cargo kcheck` |
| 后端 | cargo test -p variable --lib 280 绿 | 单跑必绿口径 |
| 任务 58 拔出全链 | **10/10 PASS** | `_attic/p58-drill-report.json`（verdict=PASS）+ `_attic/p58-serial-{s1,s2}.log` |
| 任务 60 机型矩阵 | **3/3 PASS** | `_attic/p60-matrix/p60-matrix-report.json` + 逐配置串口 |
| 性能基线 | boot 896ms / first_frame 896ms / 交互 P95 51.7ms 全 PASS | `_attic/bench/perf-baseline-usb.json` + `scripts/perf-gate-usb.py` |
| 嵌入层走查 | 20/20 PASS（8 屏截图归档） | `_attic/acceptance-p27/` + `_attic/p27-28-serial.log` |

### 58 演练连带修复（内核侧真实缺陷，非脚本绕过）
- ushell 设置页 `setting_sel` 重进不归零（违「页面重进=顶部」既有约定）→ `user/ushell/src/main.rs` 进入设置页归零；
- `SHIM_VFS_SOURCE` 内核漏接线 + `mount::healthcheck`（PCI vendor 探测）/`mount::uninstall_io_failed`（IO 失效卸载）→ 数据源诚实降级链；
- tsc 9 错修复：`src/lib/skeleton.ts` / `dataMigration.ts` / `journeys.ts` / `skeleton.test.ts`。

### 60 矩阵脚本修复（复用 58 演练约定）
- boot-replay 后必须发任意键才进桌面（否则双向死等 600s/配置）；
- no-panic 异常词快照改为全链走完后取全量日志。

## 二、「U 盘随插随用」完整旅程走查

### QEMU 代理轮（×3 配置 = 机型差异代理，2026-09-18 实跑全 PASS）

| 配置 | 代理口径 | 断言 | 结果 |
|---|---|---|---|
| A · q35 单核 1GiB | 全功能基线 | boot + desktop-ready + SHARED exFAT 挂载（source=shared-exfat）+ 零 panic | PASS |
| B · q35 双核 2GiB | 多核/大内存调度代理 | 同上四项 | PASS |
| C · pc(i440FX) 单核 | 无 ECAM/MCFG 老机降级代理 | boot + `blk: no MCFG table - block stack skipped` 如实打点 + 零 panic（块栈降级不致瘫） | PASS |

### 拔出→中断→恢复全链（任务 58，随插随用的核心语义）
会话1 真数据源写入 → 运行中强拔 → 内核 IO 失效卸载 → 数据源如实降级 demo-tree（零冒充）→ RAM KV 独立存活 → 零 panic → 断电 → 重插 → SHARED 恢复挂载 + KV 干净重建。10/10 PASS。

### 实机轮（登记为待用户协作项，与任务 10/63 同批协作通道）
- ≥3 台不同厂商物理机型「随插随用」走查；
- 真机 U 盘五分区引导（任务 7/8 的产盘端到端 ×2 亦在此轮）；
- 真机引擎 VHDX 在线时的 4K 随机读写阈值与无缝窗口 ≤50ms 延迟（阶段 6 验收口径）。

## 三、残项三色登记表（任务 88 指定产出）

**🟢 绿＝已闭环（代码+验收+证据齐全）**
- 任务 1-87 全部（除下行黄/红项所属的实机复核部分）；三处文档状态同步完成
  （任务表 1-89、总施工清单阶段 2/4/5/6/7/8/9/10+专项 A-T、施工总案进度实况注记）。
- 门禁现状：tsc 0 / vitest 2787 / ktest 3026 / kcheck 0 / 后端 280。

**🟡 黄＝QEMU 代理已过、待实机复核（能力已交付，等硬件在环）**
| 残项 | QEMU 侧证据 | 实机缺口 |
|---|---|---|
| 多机型矩阵（任务 60） | A/B/C 三配置 3/3 PASS | ≥3 台不同厂商实机 |
| 强拔演练物理层（任务 10/58） | HMP drive_del ×9+全链 10/10；真机卷设备直通拔盘已过 | 整盘 PhysicalDrive 直通（需管理员 UAC）与控制器错误层级差异 |
| U 盘产盘端到端（任务 8/88） | Deploy-Varix-USB 五分区编排+断点续作 | 实盘 ×2 走查 |
| 引导选择页真机（任务 1-5） | QEMU 三路径+OVMF UEFI BootNext 实证 | 真机 5 秒倒计时进 VARIX |
| 引擎通道阈值（阶段 6 验收） | 五态机/画面流/注入/冷启动叙事逻辑核+探针全过 | 真机 VHDX 的 4K 随机读写与 ≤50ms 延迟实测 |
| 硬件七项（任务 87） | groups A-E 逻辑核全绿 | 实机多屏/合盖/唤醒复核 |

**🔴 红＝需用户协作/外部条件，本轮不冒充**
| 残项 | 原因 | 解卡路径 |
|---|---|---|
| ≥3 台物理机型走查 | 物理硬件不在会话内 | 任务 88 实机轮（用户协作通道） |
| Secure Boot 开启态引导 | 需关闭 Secure Boot 或签名链，涉及用户固件设置 | 实机轮按 docs 内引导文档操作 |
| Servo 完整渲染 Variable React 桌面 | 当前以 ushell 原生绘制承载同等交互语义（任务 27 如实登记）；Servo 集成=后续通道增强 | 任务 25 风险表排序逐项落地 |
| 阶段 0/1/3 部分清单项（VHDX 三级差分链部署脚本等） | 属下一批次工作面，非本批任务表范围 | 总施工清单保持未勾并注明 |
| 体验人工轮（专项 A/M 验收：3 名新手 ≤15 分钟等） | 需真人参与 | 任务 88 人工终验 |

## 四、文档收口（任务 89）完成清单

1. **三处状态同步**：任务表（1-87 勾+58/60/88/89 收口勾+完成注记）、总施工清单（阶段/专项逐项按任务表证据勾选注记，未完成项注明原因）、施工总案（顶部进度实况注记，逻辑与验收标准不变）。
2. **CHANGELOG**：`[Unreleased]` 批次收官段（内核/前端/文档与工具三节+门禁数字）。
3. **验收报告归档 docs/acceptance/**：即本文件；此前批次凭证目录（任务 1-67 各 acceptance 目录）保持原位。

## 五、复核抽查记录（任务 88「对照 1-87 逐项复核」抽样证据）

- 任务表 `grep "^- \[ \]"` 收口前仅 58/60/88/89 四行未勾 → 58/60 以本会话实跑证据补勾，88/89 随本文件归档勾选；
- 新增模块测试实跑：skeleton/dataMigration/journeys 23/23 PASS（tsc 修复后复跑）；
- ushell 重建链：`cargo build --release`（3.41s）→ 嵌入 `kernel/varix/src/proc/ushell.elf` → `cargo kbuild`（33.96s）→ `scripts/make-iso-qemu.py`（8,892,416 字节）→ 演练/矩阵双绿；
- 内核 ELF 4.32MiB（4,526,000 字节）< 7.69MiB Limine 阈值；ISO 8,892,416 字节（8.48MiB，含引导器与 initfs）。
