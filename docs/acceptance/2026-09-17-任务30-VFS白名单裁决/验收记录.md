# 任务30 验收记录 — VFS 白名单裁决层＋越权审计日志（AI-S）

- 日期：2026-09-17
- 交付：`kernel/varix/src/vfsguard.rs`（commit 6142b62 入库；并行 AI-S 会话三检复核后随其提交入库，本记录补齐实机证据归档）
- 前置：任务 18（共享分区只读挂载）✅；任务 31（路径逃逸 15 变体，由并行会话基于本模块 normalize 完成）✅

## 1. 总案验收对照（阶段4 步骤1 + 阶段4 逻辑门）

| 总案验收项 | 证据 |
| --- | --- |
| 路径规范化：防 `../` 逃逸 | 越根**拒绝不钳制**（`/..`、`/../secrets`、`/a/../../b`、深层越根 → `Escape`）；宿主 3 用例锁定 |
| 路径规范化：符号链接绕过 | exFAT 无符号链接对象；反斜杠判非法（非分隔符）+ 控制字符/DEL 拒绝——逃逸无构造路径（模块头声明） |
| 路径规范化：大小写变体 | ASCII 小写折叠（exFAT 不分大小写口径）：实机 `/APPS.JSON` 命中 `/apps.json` 规则；完整 Unicode 折叠超出范围如实声明 |
| 规则匹配：目录级前缀 + 通配 | 组件边界匹配（`/app` 不命中 `/apps`、`/apps.json.bak` 不命中）；尾部 `/*` 通配 ≡ 目录前缀；规则文本解析（`allow r|w|rw <path>`，坏行计数） |
| 默认拒绝 | 白名单外一律 `NoRule` 拒绝（实机 `/etc/passwd`）；命中但操作位不含 → `OpMask` 拒绝（实机 `/apps.json` 写） |
| 挂 SHARED 挂载点唯一入口 | `GuardedVolume.adjudicate` = 裁决+留痕一体面（裁决 fail-closed，审计通道尽力而为） |
| 越权审计日志走 journal 断电不丢 | `AuditJournal` = DiskJournal tier3 + `append_slot` 载荷同块原子 + fnv 载荷校验；实机双会话跨 kill QEMU 断电续记（见 §2） |
| 审计腐坏安全路径 | fnv 复核失败按 torn 口径「该点及之后全部丢弃」；宿主 `audit_payload_corruption_is_torn_tail`（直接翻转盘面载荷字节 → 前缀 2 条完好） |

## 2. 实机证据（QEMU q35 + NVMe，`_attic/shmvfs-demo.py`，日志 `shmvfs-session{1,2}.log`）

会话1（全新盘面）——裁决矩阵十项 + 审计账本首记：

```
[ INFO] vfs-probe: decisions matrix=[allow,opmask,dir-deep,default-deny,case-fold,boundary,escape,inband-deny,inband-allow,wild] verdict=ok
[ INFO] vfs-probe: audit-session=1 entries=6 verdict=ok
[ INFO] ring3: task15 lifecycle complete
```

十项矩阵：①白名单内读放行 ②命中未授写拒绝(OpMask) ③目录 rw 深层放行 ④白名单外默认拒 ⑤大小写折叠 ⑥组件边界双向 ⑦越根 Escape ⑦b带内收敛到白名单外拒 ⑧带内收敛到白名单内放行 ⑨通配前缀。

会话2（跨 kill QEMU 断电，同盘面重启）：

```
[ INFO] vfs-probe: audit-recovered verdict=ok recovered=6
[ INFO] vfs-probe: audit-session=2 entries=12 verdict=ok
```

前会话 6 条审计（3 准 3 拒，含 1 条逃逸拒绝、1 条 OpMask 拒绝）逐条精确核对零丢失；entries 6→12 严格递增；两会话 PANIC=0。

## 3. 宿主门禁

- ktest：9/9 用例全绿（规范化×3 + 规则匹配×2 + 逃逸×1 + 审计×3；全库终态 **2915 passed / 0 failed**）
- kcheck：0 告警；kbuild release 成功；ISO 构建成功
- 盘区登记：审计账本 LBA 80 000..80 193（与 loopback≈16007 / fs23 20000..20193 / milestone 40000 / kv 60000/70000 无交集）

## 4. 排障记录

- **首组件双斜杠**：`/a` 归一化成 `//a`（根斜杠后仍补分隔符）→ 仅非首组件补分隔符；宿主用例抓获（详见任务32 记录 §4.2，同一次修复）。
- **探针期望错 1 处（实现正确）**：`/a/../secrets` 是带内合法回溯（→ `/secrets`，NoRule 默认拒绝）而非越根逃逸；修正探针期望并增补带内收敛两条证据项（详见任务32 记录 §4.3）。

## 5. 凭证与后续

- 驱动脚本：`docs/acceptance/2026-09-17-任务30-VFS白名单裁决/shmvfs-demo.py`
- 串口日志（本地留存）：`_attic/shmvfs-session{1,2}.log`，关键行已内嵌本记录
- 任务 31（15 变体全拦截）由并行 AI-S 会话基于本模块完成并归档（commit 6142b62：BLAKE3 自实现交叉验证 + 阶段4 负向矩阵 116 组合 88 拒绝）
- 下游解锁：任务 36（设置页白名单管理 UI＋审计查看页）
