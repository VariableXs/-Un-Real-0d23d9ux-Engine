# 双域 · 阶段 1 · AI-P 任务 6 / 任务 9 六维验收（2026-09-16）

> 交付物：`portable/AI-P/`（Create-Partitions.ps1、Init-Shared.ps1、partition-plan.json、Test-AIP.ps1、README.md）
> 自测证据：`docs/acceptance/双域-阶段1-AIP自测输出-2026-09-16.txt`（25/25 全绿）
> 套件门禁：`portable/tests/Run-PortableTests.ps1` 语法段 66 项 PASS（含 3 个新 .ps1）

## 任务 6：五分区 GPT 脚本

- [x] 五分区布局（ESP 1GB FAT32 / VARIX_SYS 64GB / WIN_ENGINE 300GB / SHARED 600GB exFAT / SNAPSHOT 余量 NTFS）
- [x] 幂等重跑：已有分区先做布局一致性校验，一致即跳过；不一致拒绝并要求 -Recreate + 二次确认
- [x] 4K 对齐校验：计划阶段逐分区断言 Offset % 4096 == 0；落盘后 New-Partition 回读复验
- [x] 容量全部参数化：partition-plan.json 配置驱动（version 字段；SNAPSHOT=0 表示吃余量，minSnapshotGB 下限）
- [x] 小盘拒绝：低于 minDiskGB 明确报错；SNAPSHOT 余量不足明确报错
- [x] 清盘二次确认交互（大写 YES；-Yes 仅供自动化）；启动/系统盘直接拒绝
- [x] ASCII 分区表可视化输出（-ReportPath 可归档部署报告）
- [x] -PlanOnly 纯计算模式：不碰磁盘、无管理员要求，供测试与部署预览
- [x] 双目标支持：物理盘（-DiskNumber）与 VHD（-VhdPath，挂载/卸载 finally 兜底）
- [x] 负向自测：1024GB 正常布局 / 50GB 小盘拒绝 / 975GB SNAPSHOT 余量不足拒绝
- [ ] VM 首启引导验证（属任务 7 前置链路，待任务 7 一并验收）

## 任务 9：SHARED 目录契约初始化 + apps.json schema 定版

- [x] 契约四件套：apps.json / boot-select.json / whitelist\whitelist.json / handoff\
- [x] apps.json schema 定版：version=1，条目字段 id/name/icon/channel(wine|engine|native-only)/tier(ok|partial|blocked)/updated
- [x] 版本迁移占位：Move-AppsJsonVersion（version>1 明确报错要求升级工具，不静默降级）
- [x] 幂等：契约文件已存在且可解析 → 不动；重跑哈希比对用例通过
- [x] 损坏走默认：损坏文件改名 *.corrupt-<时间戳> 留证后重建（负向用例通过）
- [x] -ValidateOnly 校验模式（部署 Verify 段复用；损坏/缺字段/枚举非法逐一拦截）
- [x] boot-select.json 字段与总案一致（default_entry/timeout_sec=5/show_menu/last_boot/windows_bootnext）
- [x] 契约三方表（写者/读者）入档 portable/AI-P/README.md

## 六维核对

- [x] 视觉：分区表 ASCII 可视化（Name/FS/Size/Offset 表格）输出与归档（替代界面项）
- [x] 逻辑：幂等连跑、小盘拒绝、余量不足拒绝、损坏 JSON 重建、负向 schema 拒绝——均有自动化用例（25/25）
- [x] 代码：纯 ASCII 语法门禁过套件解析；UTF-8 BOM；危险操作强制确认；管理员门禁仅限动盘路径
- [x] 开放性：容量/对齐/最低余量全部配置驱动；apps.json version 迁移占位；未知字段前向兼容约定入档
- [x] 简洁性：复用 AI1 既有风格（Write-Step/Ok/Note、Get-FreeLetter 思路、4K 校验），未改 AI1 文件
- [x] 完善性：VM 首启验证明确移交任务 7（非静默缺失）；「SNAPSHOT 余量最小值」显式声明

## 遗留与移交

- [ ] 任务 7（ESP 双引导链 + VM 首启验证）：依赖真实/VHD 引导环境
- [ ] 任务 8（Deploy-To-USB 五分区总编排）：依赖任务 7
- [ ] 任务 10/11（强拔演练 / 多盘版本管理）：依赖任务 8/9，需实机 U 盘
