# 双域·任务22+23 六维验收记录（AI-B · 2026-09-16）

## 任务 22：垫片协议定版（AI-V＋AI-B）
- [x] 协议规范文档：`docs/双域-垫片协议规范-v1.md`（AI-V 侧，本任务沿用为唯一规范）
- [x] 单一事实源：`tools/shim-protocol.source.json`（AI-B）
- [x] 同源生成器：`tools/gen-shim-protocol.cjs` → `src/lib/shim/protocol.ts` + `src-tauri/src/shim_protocol.rs`（改 JSON 即再生成，双侧防漂移）
- [x] 前端运行时：`src/lib/shim/shimInvoke.ts`（AI-V；传输层注入式，同份前端两后端）
- [x] 逻辑·错误三段式各路径用例：TS 8 例（`src/lib/shim/__tests__/shim-protocol.spec.ts`：OK/MAPPED/MISSING/版本协商/通道/未安装传输层）+ Rust 4 例（shim_protocol 内嵌：ok/mapped roundtrip/missing/hello）
- [x] 开放性·版本协商：`shim_hello` + `ShimHello`/`ShimCapability` 双侧同构
- [x] 完善性·协议变更流程：规范文档 + source.json `$comment` 注明
- 门禁：tsc 0 错；vitest shim 8/8；cargo test -p variable --lib shim_protocol 4/4

## 任务 23：三色审计（AI-B）
- [x] 产出：`docs/shim-mapping.json`（数据驱动；新命令登记归属后进 generate_handler，加命令不改垫片代码）
- [x] 生成器/门禁：`tools/shim-mapping.cjs`（未分类即非零退出；实测 549 方法 / 546 唯一命令全归属）
- [x] 三色占比：✅ 314（57.2%）/ 🔶 222（40.4%，均写明组合服务）/ ❌ 13（2.4%，均写明 missingService 与原因）
- [x] 逻辑·人工抽查 20 条归属正确（含修正 a11yProbe 误归属 → input+sysinfo）
- [x] ❌ 清单复核（13 条全部为宿主面/远程面，与物理隔离不变量一致）：
  ex_drives、who_locks（fsHost：宿主盘面按设计不提供）；fw_* ×4（netFirewall 需立项）；winget_* ×7（pkgRemote：走共享分区登记+引擎通道替代）

## 遗留与如实说明
- AI-K 用户态（任务 12/14/15）未完成 → AI-B 名下任务 38-45、65 仍被前置锁定，未开工、不臆造。
- 本会话曾与 AI-V 会话并发改动 `src/lib/shim/`，已收敛到同一协议（AI-V 运行时 + AI-B 单源/生成器/Rust 侧），无残留双协议。
