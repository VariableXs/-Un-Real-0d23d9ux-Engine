# AI-4 · 真机走查步骤卡（S2.06 对照 / S2.07 闭环 / S2.11 基线 / S2.12 M3 ×5）

> **性质**：实机会话预置件——《实施总步骤图》1.8 SOP 第 6 项「步骤卡就绪」的 AI-4 域落实。用户实机窗口出现时**按卡逐行执行**，全程 60-90 分钟。
> **前置依赖**：S4.1 真机 USB 键鼠（AI-5，已落地 xHCI 最小栈）待真机验证通过；U 盘内核刷至含本卡对应交付的版本（c4b1d41+）。
> **安全宪法**：1.7 三红线/五重保障全程生效；本卡**零内置盘接触、零固件写入**（唯一固件动作=读引导序，一次性 BootNext 不在本卡范围）。

---

## 上机前 7 项（缺一不上机）

1. ☐ SecureBoot=Disabled 权威判定（`Confirm-SecureBootUEFI`）；=True 则本次会话**只做非引导类步骤**（S2.11 基线采集可做）
2. ☐ 快速启动已关（HiberbootEnabled=0 回读确认，AI-1 S0.4 已置则复核）
3. ☐ BitLocker 状态记录；本卡无反复重启设计，无需 Suspend
4. ☐ BootOrder 截图归档
5. ☐ 内置盘 Windows 引导四件套 hash 基线（`_attic/readonly-*` 范式）
6. ☐ 本卡在手（即本文件）
7. ☐ 用户知情三句：做什么（内核桌面全功能走查+基线采集）、最坏情况（U 盘引导异常=拔盘回 Windows 毫发无损）、怎么回来（Novo 菜单/拔盘）

## 走查步骤卡（动作/验证/回滚 三栏）

### 卡 A · S2.07 三件套真机闭环（=M2 关卡，约 25 分钟）

| # | 动作 | 验证 | 回滚 |
|---|---|---|---|
| A1 | 插盘 → Novo 菜单选 UEFI:U 盘 → VARIX 引导 | 引导菜单出现；加载动画直落桌面（零按键） | 拔盘重试；菜单卡死=复位重来 |
| A2 | 桌面静止观察 | 壁纸/任务栏/图标/光标（屏幕中心）完整无残留；串口（如接）见 desktop-ready | — |
| A3 | 鼠标点 START → 逐项悬停 → 点 Files | 菜单削角/图标/悬停高亮正常；Files 打开且列表=SHARED 真数据 | Esc；菜单异常=复位 |
| A4 | 双击/再点打开一个文件 → 预览 → Esc ×2 | 预览内容正确（bytes= 与 SHARED 文件一致）；返回无残留 | — |
| A5 | START → Settings → 三参数各调一次 | 三条写入即时生效（值即时刷新）；重启 ushell 后值仍在（KV 落盘语义） | — |
| A6 | START → About | kv-keys 指标 ≥ 前述写入数 | — |
| A7 | 纯键盘走一遍 A3-A6 全路径 | 键盘可达与鼠标对等；焦点环可见 | — |
| A8 | 体验五子项评分 | 首用理解/反馈即时/错误可解/一致性/记忆点 逐项记录 | — |
| A9 | 拔盘 → 冷启动进原生 Windows ×1 | Windows 毫发无损（回归验证常态化） | 物理兜底 |

### 卡 B · S2.11 三项基线采集（约 15 分钟，可与卡 A 同会话）

| # | 动作 | 验证 | 回滚 |
|---|---|---|---|
| B1 | 串口抓 `SHELL: first-frame ms=`（三轮冷启动） | 三次方差 ≤5% 记入 docs/bench | 超差=环境因素排查（外接设备/供电） |
| B2 | 打字 60s（记事本节奏）→ 内核探针读 latency_ticks（enable_tsc 已置） | last/max/samples 落盘；换算 μs <30ms 门 | 超门=如实登记，开单查管线 |
| B3 | SYS_WIN 窗口开关 20 次（若 Servo 已接入；未接入则记 ushell 页面切换耗时） | 无卡顿无泄漏视觉；统计单调 | — |
| B4 | 数据写入 docs/bench/desktop-S2.11（同机三次方差 ≤5% 才入册） | AI-6 复核 | — |

### 卡 C · S2.06 逐屏对照（约 15 分钟，**需 Servo 接入后**；未接入则跳过登记"待 Servo"）

| # | 动作 | 验证 | 回滚 |
|---|---|---|---|
| C1 | 同屏内容两侧截图：Windows 侧 Variable 桌面 vs 内核侧（screendump） | `_attic/ai4-png-diff.py <win.png> <kernel.png>` 报告相似度与差异热区 | — |
| C2 | 125% DPI 与 2560×1440 双分辨率各一轮 | 双分辨率下布局成立（无破版/截断） | — |
| C3 | 帧率掉帧观察（动画页） | 如实记录（GPU 缺位=软件渲染兜底声明在案） | — |

### 卡 D · S2.12 M3 整合验收（约 20 分钟；卡 A/B/C 全过后的收口）

| # | 动作 | 验证 | 回滚 |
|---|---|---|---|
| D1 | 全流程（引导→桌面→三件套全功能）×5 稳定复现 | 5/5 无一失败（参照 `_attic/qemu-s207-suite-walkthrough.py` 清单人工执行） | 任一失败=停下记录，不开 M3 |
| D2 | 三线门禁复核（ktest/kcheck/tsc+vitest/后端） | 全绿快照归档 | — |
| D3 | 三色占比复核（audit.cjs 输出） | 与 62b571f 公示一致 | — |
| D4 | 验收报告写入 docs/acceptance/，AI-6 签发栏留位 | — | — |

## 下机后 4 项

1. ☐ 拔盘冷启动进原生 Windows ×1（卡 A9 已含则复核）
2. ☐ 内置盘四件套 hash 与上机前基线一致
3. ☐ BootOrder 与基线一致（本卡无 BootNext 写入，应零变化）
4. ☐ 会话记录（做什么/异常/下一步）写入 docs/acceptance/desktop-*

## 已知边界（如实）

- 触控板（I2C）内核侧不支持——外接 USB 鼠标（S4.1）或 PS/2
- 视频壁纸需 S4.3 GPU 前后评估；网页壁纸 Servo 原生（未接入前 ushell 无此页）
- 内核侧 SHARED 只读——文件写入类走查项在 S4.2 MSC 后开放
