# Varix STAR I start · 界面与体验设计案 v0.2

**定版**：2026-09-25 ｜ **作者**：AI01 ｜ **状态**：设计讨论稿（Variable 逐条确认后升级 v1.0 冻结）
**设计宪法**：丰盛的界面，极简的工程——高密度、深层级、质感、顶格完成度、4K 精度、风格化创新、顺手易用。界面拒绝极简风；工程侧零死代码零冗余。
**本版说明**：v0.2 按四域重排——每域一个部分，域内「提案表在前、设计内容随后」，域内自带落点与验收；四域共用的总纲与验收收在卷首与卷尾。

---

## 全册导航

| 区域 | 内容 | 对应功能项 |
| --- | --- | --- |
| 卷首·总纲 | 与 Windows 关系第一原则、执行纪律 | — |
| 卷首·甲 | 防误解写作规范（七条） | — |
| 卷首·乙 | 尺寸与规格基线（四张测量表） | — |
| 卷首·丙 | 深化设计报告模板（七段式） | — |
| 第 1 部分 · A 兼容域 | 提案 A1-A5 / 下载安装管线 / 诚实边界 / **A-5 深化报告** | F001-F040 |
| 第 2 部分 · B 性能域 | 提案 B1-B4 / 性能红线 / 四层优化 / B-3 报告（批次推进中） | F041-F070 |
| 第 3 部分 · C 体验域 | 提案 C1-C5 / 开机动画 / 桌面窗口 / 开始菜单与独立电源 / 资源管理器 / 鼠标 / 4K 管线 / 词典 / C-9 报告（批次推进中） | F071-F125 |
| 第 4 部分 · D 生态开放域 | 三原则 / D-2 报告（批次推进中） | F126-F150 |
| 第 5 部分 · E 个性化域 | 三铁律 / E1-E8 / vxtheme 定制包 / E-5 报告（批次推进中） | F151-F170 |
| 增补 · F 清单 | 200 项功能一行账 + G-1 底盘安全报告（批次推进中） | F171-F200 |
| 卷尾 | 全域验收 / 五域首发序 / 待拍板事项 | — |

**阅读路线**：只看方向 → 读各域提案表（约 10 分钟）；要看做到什么算做到 → 读 F 清单对应域（约 1 小时）；要施工 → 读对应域的深化报告（每项七段，AI 施工者可直接照做）。

---

## 卷首 · 总纲（四域共同的第一原则）

**交互词典对齐 Windows，视觉表达完全自研。**

- 用户从 Windows 过来：**零学习成本**——按钮在哪、右键菜单有什么、拖拽怎么行为、快捷键是什么，全部与 Windows 一致（先照抄交互布局，等我们有自己的按钮形状语汇后再统一替换，届时一次全局换装、不动动线）。
- 用户看到的一切：**没有一处像 Windows 克隆**——配色、材质、光影、动画曲线、图标风格、字体层级全部是 VARIX 自己的设计语汇，按 3A 大作级质感做。
- 一句话：**手感和 Windows 一样熟，眼睛看到的是另一个作品。**

**执行纪律**：无软件商店是宪法级决定（MD1 第 20.2 章），一切「获取软件」路径走浏览器下载 + S: 侧载 + vxapp 校验；每项开工前先落 WP 编号与绑定判据。本册与《MD3 第 6 章》互为镜像（此处从体验视角记，MD3 从任务视角记，两处同账不同面）。

---

## 卷首·甲 防误解写作规范（本册条文如何写成 AI 不会理解错的句子）

本册的读者包括 AI 施工者——AI 不理解失误的前提是**每条条文只有一种解读**。本册全部条文的写作规范七条，施工时发现歧义条文按此修订：

1. **五要素齐备**：每条设计条文必须含「对象 / 数值 / 单位 / 状态 / 验收」——例：「任务栏高度 48px（100% 缩放，1920×1080 基准），偏差 ≤2px，拖动窗口贴底时任务栏不被遮挡（验收）」。「任务栏要好看」不是条文，是愿望。
2. **禁用模糊词表**：适当、美观、尽量、差不多、合理、优化一下、大概——这些词出现在条文中即文档缺陷。想说「适当留白」必须写成「卡片间距 16px」；说不准数值的，写成「随 E1 主题令牌 `--space-md` 取值」，让数值有唯一出处。
3. **状态穷举**：凡控件必有状态清单（常驻/悬停/按下/禁用/选中/焦点），凡流程必有异常分支（取消/失败/断电/中断恢复）——只写正常路径的条文视为未完成。
4. **视觉词配参照物**：颜色用色板令牌（`--accent`）不用「蓝色」；质感用材质样张文件（`design/assets/material-*.png`）不用「毛玻璃感」；动画用时长+曲线（200ms ease-out）不用「流畅地」——AI 拿到条文时，参照物必须已存在于仓库。
5. **对齐 Windows 的条文必须给「对齐到什么」**：写「对齐 Windows」必须同时给出测量基准（本册卷首·乙的规格基线表），基线表是对齐条文的唯一裁判——条文与基线冲突时，以基线表为准并回改条文。
6. **一处一事实**：同一数值只在一处定义、他处引用（引用格式「见 C-9 表第 N 行」）——两处各写一份 48px，将来一处改一处不改就是事故。
7. **判据绑定**：每节末尾列验收判据，格式沿用 B 判据纪律——实测数据 + 复现命令 + 日期三件齐才算绿。

## 卷首·乙 尺寸与规格基线（Windows 11 对齐测量值）

**基准环境**：1920×1080、100% 缩放、简体中文。基线值取自 Windows 11 实机测量，允许偏差 ±2px；高 DPI 按缩放系数等比放大（150% = ×1.5），资产按 4K 管线供应（C-7 节）。**VARIX 视觉（配色/材质/圆角风格）自研，几何尺寸对齐下表**——尺寸是动线的一部分，属于「手感对齐」范围。

### 乙-1 全局骨架

| 对象 | 数值 | 备注 |
| --- | --- | --- |
| 任务栏高度 | 48px | 底部固定，图标按钮 40×40px、居中图标 24px |
| 窗口标题栏高度 | 32px | 标题文字 12px 字号；关闭等系统按钮区 46×32px 每枚 |
| 系统圆角 | 窗口 8px / 弹层 8px / 控件 4px | VARIX 可按主题令牌微调（E1），基准 8px |
| 系统字体字号 | 正文 14px / 次要 12px / 标题 20px / 大标题 28px | 微软雅黑语位对齐，VARIX 字体自选（E7） |
| 基础间距档位 | 4 / 8 / 12 / 16 / 24 / 32px | 六档，任何间距必须落档 |

### 乙-2 开始菜单（对齐 Win11 开始）

| 对象 | 数值 | 备注 |
| --- | --- | --- |
| 整体面板 | 640 × 700px | 底部居中、距任务栏 12px、圆角 8px |
| 顶部搜索框 | 高 40px、左右边距 58px、圆角 20px | 占位文字「搜索应用、设置和文档」 |
| 已固定网格 | 6 列 × 3 行、单元 84 × 84px | 图标 32px + 名称 12px 单行截断；「显示简略信息」展开时行高 100px |
| 网格图标间距 | 单元间隙 12px | — |
| 最近使用列表 | 行高 44px、图标 32px、最多 6 行 | 右侧时间标注 12px 次要色 |
| 底部账户区 | 高 64px、头像 32px、左右边距 56px | 右端电源按钮 40×40px |
| 电源菜单弹层 | 宽 190px、行高 36px | 睡眠/关机/重启三项 |
| 全屏模式 | 铺满工作区、网格 8 列、单元 96×96px | 与窗口模式同一数据源 |
| 展开动画 | 200ms ease-out、自下而上 12px 位移 + 淡入 | 随 E8 动效三档 |

### 乙-3 设置中心（对齐 Win11 设置）

| 对象 | 数值 | 备注 |
| --- | --- | --- |
| 窗口默认尺寸 | 1000 × 640px，最小 500 × 500px | 可最大化，记忆上次尺寸 |
| 左侧导航宽 | 300px（最小窗口时 250px） | 顶部搜索框高 32px；导航项高 36px、选中态左侧 3px 强调条 |
| 首页设备卡 | 高 120px、缩略图 176 × 96px、圆角 8px | 设备名 20px 标题；右侧快捷卡宽 180px |
| 卡片式设置行 | 高 65px、图标 20px、圆角 4px、行间距 4px | 左图标+名称 14px+副标题 12px+右箭头；卡片分组间距 8px |
| 页面内边距 | 左 40px / 右 40px / 上 24px | 二级页同构 |
| 二级页标题 | 28px 大标题、标题区高 72px | — |
| 开关（toggle） | 40 × 20px、圆角全弧 | 开=强调色，关=描边灰 |
| 下拉/输入框 | 高 32px、圆角 4px | — |

### 乙-4 桌面与文件资源管理器

| 对象 | 数值 | 备注 |
| --- | --- | --- |
| 桌面图标 | 48px 档基准（32/48/96 三档可切）、网格单元 76 × 100px | 名称 12px、选中半透明矩形 20% |
| 右键菜单 | 宽 220-280px、行高 30px、圆角 8px | 一级最多 11 行，超出出「显示更多选项」 |
| 文件管理器窗口 | 默认 1100 × 680px、最小 600 × 400px | — |
| 导航树窗格 | 宽 200px、行高 28px | 可折叠至 0 |
| 详细信息表格 | 行高 30px、列头高 32px、列宽（名称弹性 240px 起/修改日期 140px/类型 120px/大小 90px） | 斑马纹悬停行高不变 |
| 状态栏 | 高 24px、文字 12px | 选中数/合计大小/数据源标注 |
| 通知 toast | 宽 360px、行距 8px、右下角距边 16px、停留 5s | — |
| 对话框按钮 | 高 32px、最小宽 120px、按钮间距 8px | 主按钮强调色实心 |

**基线表的修订纪律**：基线表是本册唯一尺寸裁判——新界面元素先查此表，表里没有的先入表再引用（一处一事实）；对 Windows 新版测量更新基线值走 ADR，改值即全局同步。

---

## 卷首·丙 深化设计报告模板（各域报告卷的统一写法）

**卷定位**：F 清单是「做到什么算做到」的一行账，本卷把每一项展开成**完整的设计报告**——按统一七段模板：【功能定义】（这是什么、边界在哪）【用户故事】（谁在什么场景怎么用它）【交互设计】（入口/动线/每个状态的样子，尺寸引用卷首·乙基线表）【数据与存储】（数据长什么样、放哪、怎么保命）【状态与异常】（正常路径之外的全部分支）【验收判据】（实测什么、证据形态）【开源复用】（接哪个现成件、接缝在哪）。**报告式纪律**：每段必须可执行——AI 施工者读完【交互设计】就能画出界面，读完【状态与异常】就知道每个分支怎么弹，读完【验收判据】就知道跑什么命令算绿；凡是写不出的段落，标注「设计缺口」排队补齐，不许用形容词糊过去。

---

# 第 1 部分 · A 兼容域（目标②——杠杆最高）

> 域目标：让 VARIX 能真正打开、运行 Windows 软件。

## A-1 提案表

| 编号 | 提案 | 说明 | 量级 |
| --- | --- | --- | --- |
| A1 | PE 直载通道扩面 | M-A→M-G 链：从 SHARED 打开 .exe → peblock 门 → ring3；先静态后导入绑定 | 中 |
| A2 | Win32 API 判例工厂 | 每个常用 API 一条判例 + 实现 + 星卡，按「用户实际装什么」排优先级——兼容面由真实使用数据驱动而非闭门造 | 长期但可并行 |
| A3 | Wine 路线 | Wine 入镜像 + lxrun 实机接线——兼容面的「外包加速器」 | 大 |
| A4 | vxapp 侧载闭环 | S: 放 .vxapp 包 → 校验 → 四段安装 → 桌面出现图标——不用等应用商店就有「装软件」的体验 | 小，杠杆极高 |
| A5 | 兼容性大量拓展深化（Variable 2026-09-25 指定） | 目标：**下载的软件在 VARIX 里完全无感地独立使用**——.exe 双击即开、VS Code 及其各语言生态（扩展/编译器/解释器链）原样可用；**大力复用现有开源项目**（Wine、Mesa、musl/relibc、开源语言运行时等），不自研重复轮子，把开源件接进 VARIX 的判例与垫片体系 | 大，持续 |

## A-1·补 A5 的落地口径

- **无感标准（验收判据）**：下载 → 双击 → 窗口出现，中间零配置、零终端命令、零「切换域」——用户全程不知道兼容层的存在；错误路径（程序不兼容）用三要素文案诚实提示，不闪退不静默；
- **多语言软件链**：VS Code + 各语言运行时（Python/Node/Java/Go/Rust 等）按 A2 判例工厂逐语言过判例——语言链的兼容目标不是「能装」，是「能装扩展、能跑构建、能调试」（以 VS Code 全绿为标志性判据，承接 MD1 S304）；
- **开源复用纪律**：优先列清单评估现有开源件（借力纪律同 MD1 第 18 章），只接不改、版本锁定；自研只做开源件与 VARIX 内核之间的接缝（垫片/判例/差异表）——兼容面的工程预算花在接缝上，不花在重造上。

## A-2 下载与安装（无应用商店，浏览器为门）

1. **浏览器下载（Variable 2026-09-25 已拍板：Edge 级浏览器下载，不建应用商店——MD1 第 20.2 章同步入宪）**：用户用浏览器正常下载——**不建应用商店**，星图退回其本职（兼容性评级目录），不做商店前端。
2. **下载落点**：SHARED 卷（S:）下载目录，VARIX 域可读。
3. **安装链**：下载物 → vxapp 校验（清单/签名/哈希）→ 四段安装 → **桌面出现图标，尺寸网格对齐 Windows 规范**、图标按本设计案视觉自研三态套装 → 双击即用。
4. **安全**：所有下载物过 peblock 门与签名校验，未签名如实标注（不静默拦、也不静默放）。

## A-3 诚实边界

.exe 真正「下载即可双击运行」依赖 PE 直载通道扩面（M-A→M-G 链）+ Win32 API 覆盖面——本设计案定义的是**体验管线**（下载→出现→双击），管线尽头能跑多少软件由 A1-A3 的兼容里程碑决定，两线并行互不阻塞。

## A-4 域内落点

A1 的打开动作落在第 3 部分文件资源管理器右键「打开方式」动线；A4 的安装完成反馈落在第 3 部分桌面图标三态套装。**下载→出现→双击**的体验管线即本部分 A-2 节。

---

> 以下 40 份报告为 A 域全部功能项的报告式展开——七段模板见卷首·丙；每项末尾【设计细节】段含参数级、像素级、判据级补深。

## A-5 深化设计报告（F001-F040 报告式全展开）

### G-A-01 F001 无感双击运行 · 完整设计

**【功能定义】**用户在文件资源管理器（或桌面）双击一个 .exe 文件，系统自动完成「识别格式 → peblock 门校验 → 装载进内存 → 建进程 → 显示窗口」全链路，用户全程只做「双击」一个动作。边界：本项只承诺 64 位 PE（32 位见 F004），只承诺 peblock 门规则内的程序（越权见 F037），只承诺窗口级 GUI 与控制台两类子系统。
**【用户故事】**Variable 从浏览器下载了一个开源记事本替代品（Notepad3.exe）到 S:\Downloads，打开资源管理器双击它——3 秒后窗口出现，标题栏是 VARIX 统一装饰，内容区是程序自己的界面；全程没有终端、没有命令行、没有「切到 Windows 域」的提示。Variable 的朋友问「这是 Windows 吗」，Variable 说「不是，这是 VARIX」——这就是无感的定义。
**【交互设计】**双击后 100ms 内指针变忙碌态（Windows 语义保留，C-6 红线）；首帧窗口出现前若超过 500ms，屏幕中央出现占位窗（尺寸按程序 manifest 声明的窗口大小，标题显示程序名，中央三颗点呼吸动画 1.2s 周期）；窗口就位后占位窗无缝替换（无闪烁）。双击过程任何时候 Esc 取消装载（进程回收，资源零残留）。入口共三处：资源管理器双击/桌面双击/开始菜单搜索后 Enter——三条路走同一装载服务，不许有第二条实现。
**【数据与存储】**装载器不落盘任何中间物；peblock 校验结果（哈希+规则命中）写入会话日志；程序自身的写入按 F009/F010 沙盒规则重定向。
**【状态与异常】**①格式不是 PE → 弹「这不是 Windows 可执行文件」三要素对话框；②peblock 拒绝 → 按 F037 给完整解释与越权入口；③装载中崩溃 → 占位窗转为错误卡（「程序未能启动」，展开显示归因五分类 F035），桌面不受影响（F175 隔离）；④双击后立刻断电 → 无任何持久状态损坏（装载零落盘）。
**【验收判据】**「常用 50 件」（F040）中 GUI 类逐件实测：双击到首帧 ≤3s（U 盘实机、二次启动）、占位窗出现率 100%（超过 500ms 的样本）、Esc 取消后进程表零残留（kinfo process self-test 11/11 实测口径）。证据三件套：录屏 + 进程表快照 + 计时数据。
**【开源复用】**装载核心自研（ring3 既有雏形 spawn_pe 族扩面），PE 格式参考文档用 Microsoft PE Spec + 开源 pe-parse 库做格式测试集（只用作对拍样本，不链接进内核）。

【设计细节】装载服务进程独立于合成器与资源管理器（崩溃互不传染）；双击识别走文件签名嗅探（MZ 头加 PE 标记双验证，扩展名不作准）；占位窗动画参数：三颗点 480ms 周期呼吸、透明度 0.4 到 1.0；同一文件 60 秒内重复双击合并为一次装载（防手滑双开）；装载全程 CPU 限额单核 80%——后台装载不抢前台帧率。

### G-A-02 F002 静态 PE 全量支持 · 完整设计

**【功能定义】**无导入表（或仅导入 kernel32 基础面）的静态链接 PE 完整装载：GUI/CUI/Windows CE 三类子系统、任意节数（上限 96 节，Windows 规范值）、非常规 SectionAlignment（含 4KB 以下）、BASE 重定位全类型（HIGHLOW/DIR64）。
**【用户故事】**一位用 Go 写工具的开发者 `go build` 出单文件 exe（静态链接、20MB），拷进 S: 双击——控制台窗口弹出、输出正常、退出码正确。他不知道这背后是 VARIX 自己的装载器，他只知道「能跑」。
**【交互设计】**CUI 子系统自动挂接终端应用（F012）：若从资源管理器双击控制台程序，自动新开一个终端标签页运行（关闭窗口即退出进程，动线对齐 Windows）；GUI 子系统按 F001 流程。装载期无用户可见等待项（20MB 装载目标 ≤800ms，实测线）。
**【数据与存储】**节映射零拷贝（文件页缓存直接映射，写时复制保护）；重定位表不常驻内存（处理完即弃）。
**【状态与异常】**畸形头（e_lfanew 越界/节表溢出）→ 拒绝并归因「文件损坏或非 PE」；地址空间不足 → 提示「内存不足」而非闪退；TLS 回调执行按 Windows 语义先于入口点（顺序差异表登记）。
**【验收判据】**开源静态样本集 20 枚（Go/Rust/Nim 各类）装载成功率 20/20；对抗样本集 30 枚（自造畸形）拒绝率 30/30——两组数字同页呈现，缺一不可。
**【开源复用】**测试集复用 pe-parse 与 Wine 的 conformance 测试样本；装载实现自研（内核零堆纪律不兼容第三方 C 库直入）。

【设计细节】节数上限按 Windows 规范 96 节硬顶；SectionAlignment 小于 4096 时按文件对齐映射（FILE_ALIGN 模式），重定位 HIGHLOW 占 95% 场景优先快路径；BASE 重定位块处理上限 64MB（超限归因「镜像异常」拒绝）；Go/Rust 静态样本的 .text 平均 8-20MB，装载内存按节惰性提交（触碰才分配物理页），20MB 程序实际驻留首启小于 6MB。

### G-A-03 F003 导入绑定加速 · 完整设计

**【功能定义】**IAT 解析的两级缓存体系：进程族缓存（同主程序的 DLL 符号地址表）与会话缓存（系统 DLL 符号表全局只读共享）；delay-load 按首次调用时绑定。目标：二次启动同程序导入解析 ≤ 一次启动的 50%。
**【用户故事】**Variable 每天开机第一件事打开同一个 .exe 记账工具——第一次打开 2.8s，第二次起稳定 1.4s 以内，他感受不到区别也不需要知道，账本里两次数字都在。
**【交互设计】**无直接交互面；数据面在任务管理器（若 C-5 理解 A 拍板）进程详情页显示「导入解析：缓存命中/全量」，给排查者看。
**【数据与存储】**缓存键 = (模块基址哈希 + 符号名哈希表)；缓存文件存 DATA 分区 `cache/pebind.bin`，损坏即弃建（自愈，F189 同族）；上限 64MB，LRU 驱逐。
**【状态与异常】**DLL 版本变化 → 键失配自动全量解析（不报错）；缓存文件损坏 → 静默重建 + 通知中心报备；内存压力下缓存先于文件页被回收。
**【验收判据】**vxbench 装载类基准新增「导入解析耗时」子项：同程序三次启动取后两次均值 ≤ 首次 50%；缓存命中率 >80%（常用 50 件口径）。
**【开源复用】**符号解析参考 Wine 的 PE loader 设计（接缝：仅借鉴算法，代码自研）；缓存格式参考 Microsoft IAT snapshot 思路。

【设计细节】缓存键加系统 DLL 版本戳（内核更新后键全失效重建，防陈旧地址）；delay-load 桩生成按 thunk 表原位改写（Windows 同语义）；会话缓存只读共享以全局映射实现（零拷贝）；命中率统计入诊断面板，低于 60% 自动清缓存重建（自愈）；缓存文件写盘走 WAL 与应用蜂巢同设施。

### G-A-04 F004 Wow64 门（32 位 .exe 前瞻）· 完整设计

**【功能定义】**32 位 PE 的三层处理：探测（机器类型字段识别）→ 拒绝（当前版本无 32 位子系统）→ 诚实提示 + 计划入口。拒绝不是终点，是分流：把「不支持」变成「明确知道为什么不行」。
**【用户故事】**Variable 双击一个 2015 年的老工具（32 位），弹出卡片：「此程序为 32 位应用。VARIX 当前运行 64 位程序。32 位兼容在路线图中（预计 STAR I start 后程）。」附「查看 64 位替代品」按钮直跳星图搜索——**拒绝也给出路**。
**【交互设计】**拒绝卡片样式对齐 F035 兼容性向导（同一卡片体系）：图标+标题+一句归因+两个动作（关闭/查替代）。卡片自动记忆该文件，同文件第二次双击不再重复解释（本地记录，可清）。
**【数据与存储】**拒绝记录存 `cache/wow64-refusals.json`（文件哈希 → 已提示标记），上限 1000 条。
**【状态与异常】**带 32 位安装器的混合包（32 位安装器装 64 位主程序）→ 安装器本身被拒时提示完整归因；伪装 32 位的恶意样本照走 peblock 门，先过门再谈位数。
**【验收判据】**32 位样本集 10 枚 100% 触发诚实卡片（零静默失败/零崩溃）；卡片文案三要素齐。
**【开源复用】**机器类型判定逻辑参照 pe-parse 字段表；无运行时依赖。

【设计细节】位数判定读 PE 机器字段 0x014c（I386）与 0x8664（AMD64），ARM64 声明也识别并如实告知；拒绝记录同时上报星卡草稿（F036 联动，匿名）；「查替代品」按钮跳星图搜索并预填程序名关键词；卡片文案经过三要素审计（发生了什么/为什么/下一步），禁用「不支持」裸句。

### G-A-05 F005 Win32 窗口管理兼容层 · 完整设计

**【功能定义】**winsrv 消息泵扩面：常用窗口类七族（按钮/编辑框/列表框/组合框/滚动条/静态文本/通用对话框容器）的 CreateWindowExW/消息路由/绘制请求全通；目标程序是「纯 Win32 写的完整应用」而非 demo 片段。
**【用户故事】**一位只学过 Win32 API 的开发者，把他的开源记事本（3,000 行 C，纯 CreateWindow/DefWindowProc）拷进 VARIX——菜单栏能点、编辑区能打中文（F027 输入法面）、文件对话框能开（F008）、最小化最大化正常——他说「我像在给 Windows 写程序」。
**【交互设计】**窗口装饰由 VARIX 合成器统一供给（标题栏/边框/系统按钮按 C-3 规范），程序自身画的客户区原样呈现；程序自绘标题栏（WM_NCCALCSIZE 处理）则尊重程序——**程序对自己的窗口有主权，VARIX 只管没主权的地方**。非客户区命中测试（WM_NCHITTEST）语义逐消息对齐。
**【数据与存储】**窗口类注册表在进程内存内，不落盘；每窗口的 DPI 感知声明（F014 manifest）决定缩放路径。
**【状态与异常】**消息死循环检测（同消息 1s 内 10 万次）→ 判定应用卡死，转 F175 隔离处理；未处理消息返回 DefWindowProc 语义（不吞不崩）；窗口类名冲突按 Windows 规则（后注册失败）。
**【验收判据】**标志件：Notepad2 级开源编辑器全功能走查（新建/打开/保存/查找/替换/字体对话框/状态栏）；消息面补全按「常用 50 件」应用的 API 采样频率排序（A2 数据驱动）。
**【开源复用】**消息语义参照 Wine user32 与 ReactOS（语义对拍库），实现自研（winsrv 既有消息泵扩面，非移植）。

【设计细节】七族控件各配消息语义表（WM_ 前缀 40 个高频消息优先），每表附「VARIX 渲染替代」说明——按钮按下态由 winsrv 画还是程序自绘按程序风格位裁定；窗口类原子表进程内上限 16384（Windows 同值）；DefWindowProc 缺省行为逐消息文档化（对拍 ReactOS 用例）；消息泵每窗口独立队列，一窗卡死不堵同进程他窗。

### G-A-06 F006 GDI 绘图面 · 完整设计

**【功能定义】**GDI 最小集：GetDC/ReleaseDC/BeginPaint/EndPaint/FillRect/DrawText/BitBlt/StretchBlt/PatBlt/LineTo/Rectangle/Ellipse/TextOut/ExtTextOut/SelectObject/DeleteObject/SetBkMode/SetTextColor/GetTextMetrics——覆盖纯 Win32 应用的典型绘制路径，全部直通合成器提交面（B-801 唯一出口红线在 GDI 面同样生效）。
**【用户故事】**程序在 WM_PAINT 里画一段文字加一个红色矩形——VARIX 合成器收到的是标准脏区提交，画面清晰、字体走 VARIX 字形管线（4K 不糊），程序以为自己在调 GDI。
**【交互设计】**无独立 UI；绘制质量验收看三点：文字 4K 渲染走字形图集（C-7）、抗锯齿与 VARIX 原生窗口一致、双缓冲程序（先画内存位图再 BitBlt）无撕裂。
**【数据与存储】**GDI 对象表每进程限额 10,000 个（Windows 同值），泄漏超限告警（应用隔离档联动 F038）。
**【状态与异常】**无效 HDC 使用 → 返回失败不崩进程（Windows 语义返回值如实）；BitBlt 跨设备格式转换按源格式如实转换（不静默降色深）；对象句柄双删除返回失败。
**【验收判据】**F005 标志件（Notepad2 级）的绘制路径零黑屏零乱码；GDI 对象压力脚本（创建-删除 10 万循环）句柄表稳定不增长。
**【开源复用】**API 语义对照 Wine gdi32 文档与 ReactOS 测试用例；光栅实现自研直通合成器（不复刻 GDI 的 DIB 引擎）。

【设计细节】DC 概念映射为「合成器上下文句柄」，屏上 DC 直接绘即所见（省一次拷贝）；BitBlt 光栅操作码实现 16 个高频码，冷门码如实返回不支持（差异表）；文本绘制走字形图集热路径（F055 缓存）；GDI 对象泄漏检测：进程退出时未删对象列表进 dump（F020）供开发者定位。

### G-A-07 F007 GDI+ 与双缓冲 · 完整设计

**【功能定义】**GDI+ 基本面：Graphics/ Pen/Brush(实心/渐变)/Path/抗锯齿文本/图像绘制(Bitmap/ ImageAttributes 裁剪)；双缓冲管线（内存位图 → 一次性提交）一等公民支持。
**【用户故事】**一款开源图片水印工具（C# + GDI+，经 .NET 运行时 F032 面或纯 Win32 GDI+）在 VARIX 上正常打开图片、加水印、导出——抗锯齿文字与渐变边框和 Windows 上看不出差别。
**【交互设计】**同 F006，无独立 UI；验收看渲染质量对拍：同一输入文件在 Windows 与 VARIX 输出的 PNG 逐像素对比，容差仅限字体 hinting 差异（标注差异源）。
**【数据与存储】**位图对象内存池走进程配额（F195）；大位图（>64MP）流式处理防内存尖峰。
**【状态与异常】**损坏图片（PNG/CRC 错）→ 绘制失败返回错误码，应用决定 UI；内存不足 → OOM 错误码（OutOfMemory 语义）而非杀进程。
**【验收判据】**开源 GDI+ 小工具（ImageMagick GUI 前端类或选取的独立作品）全流程绿；对拍报告入册。
**【开源复用】**图像编解码复用既有解码面（F054 SIMD 路径）；GDI+ API 语义参照 Microsoft 文档与 Wine gdiplus。

【设计细节】抗锯齿统一走 VARIX 文本管线灰度 AA（ClearType 亚像素不承诺，差异表）；渐变画刷支持线性/路径两种；路径对象顶点上限 16k；对拍容差规则文档化：非文本区逐像素容差 1/255、文本区按 SSIM 大于 0.95——容差也是判据不是感觉。

### G-A-08 F008 通用对话框族 · 完整设计

**【功能定义】**四枚通用对话框的自研实现：打开/保存（GetOpenFileName/GetSaveFileNameW + IFileDialog 双接口）、选择颜色（CHOOSECOLOR）、选择字体（CHOOSEFONT）。任何兼容层程序调用都得到**VARIX 风格**的对话框——兼容面视觉不破碎的关键件。
**【用户故事】**在 Wine 记事本里按 Ctrl+S——弹出的保存对话框是 VARIX 的文件选择器（左树导航/地址栏/文件列表全按 C-4 视觉），但程序完全认识它的返回值（路径字符串）。用户看到的是 VARIX，程序以为自己调了 comdlg32。
**【交互设计】**文件对话框按 C-4 资源管理器组件复用（树窗格 200px/列表/文件名输入框高 32px/过滤器下拉），过滤器解析程序传入的 filter 字符串如实显示；颜色对话框：基础色 48 格网格 + 自定义色板（HSV 区域 + RGB/HSL 输入）；字体对话框：字体列表/字型/字号/预览区（实时渲染所选字体）。键盘全可达，Enter 确定 Esc 取消。
**【数据与存储】**「最近位置」记忆（每进程族独立），对话框尺寸记忆（上限 1280×800）；颜色自定义色板持久化。
**【状态与异常】**程序传入非法 filter → 忽略坏段不崩；路径无权限 → 三要素错误 + 定位按钮；对话框期间程序崩溃 → 对话框随之回收（F175 隔离）。
**【验收判据】**F005 标志件四种对话框全走查；对话框打开延迟 ≤200ms（冷首次 ≤500ms）；视觉走查按 20 维度第 5/6 条。
**【开源复用】**交互参照 Windows 11 对话框实测截图（甲节纪律：对齐动线不像素级抄视觉）；实现全自研（资源管理器组件复用是本体）。

【设计细节】文件对话框复用 C-4 组件树（同源同视觉，禁两套实现）；过滤器字符串解析按分号拆分（多模式全显）；保存对话框「已存在」冲突处理复用 F087 面板（保留两者/覆盖/取消三选）；颜色对话框 HSV 区拖动 60fps 实时联动十六进制输入框；对话框模态语义（模态期父窗不收输入）与 Windows 一致。

### G-A-09 F009 注册表虚拟化 · 完整设计

**【功能定义】**每应用独立注册表蜂巢（per-app hive）：应用视角的 HKCU/HKLM 读写全部落在自己的蜂巢文件；读操作先查自有蜂巢、未命中回读系统模板（只读）；写操作一律进自有蜂巢。卸载 = 蜂巢文件进回收站（零真删红线）。
**【用户故事】**两款老软件都想往 HKCU\Software 写自己的设置且都写了同名键——在 VARIX 上互不干扰；用户卸载其中一款，另一款的设置纹丝不动——「注册表泥潭」在结构上不存在。
**【交互设计】**设置中心「应用-高级」页显示蜂巢大小与「查看注册表内容」入口（只读十六进制/键树视图）；诊断中心导出蜂巢快照。
**【数据与存储】**蜂巢文件存沙盒目录（F010），格式自定（键值对 B-树，写入走 WAL 保断电一致）；系统模板蜂巢只读内嵌镜像。
**【状态与异常】**蜂巢损坏 → 重建空蜂巢 + 通知（应用设置丢失如实告知，不静默）；蜂巢超 256MB → 告警（正常应用设置 <1MB，超限必有异常写入）；并发写冲突 WAL 串行化。
**【验收判据】**三族安装器（F030）安装-使用-卸载全周期后：全局模板零写入（哈希不变）、蜂巢完整回收；断电注入百次后蜂巢可打开率 100%（WAL 恢复）。
**【开源复用】**蜂巢格式思路参考 Windows regf 格式文档（只读解析用于「从 Windows 迁移设置」前瞻），自有格式自研；WAL 复用存储栈既有设施。

【设计细节】蜂巢 B 树节点 4KB、写放大控制（脏节点合并落盘）；系统模板蜂巢启动时 mmap 只读共享（全进程共用一份）；蜂巢查看器树视图支持键值复制与十六进制值查看；导出快照为 JSON（开放格式 F126）；恢复点（F121）自动纳入蜂巢快照——注册表可回滚。

### G-A-10 F010 文件系统重定向 · 完整设计

**【功能定义】**兼容层程序的文件写入按三级重定向：Program Files 类写入 → 沙盒 Program 目录；AppData/ProgramData → 沙盒 AppData；用户文档/下载目录 → 真实直通。读操作合并视图：沙盒优先，系统镜像次之，用户目录直通。
**【用户故事】**一款 2010 年的老软件硬往 C:\ProgramData 写配置——VARIX 把写入落进它的沙盒，程序读回正常；用户在文件管理器里能看到沙盒目录（透明可见、可手动清理），一切有据可查。
**【交互设计】**资源管理器显示沙盒目录为普通文件夹（路径 `~\AppSandbox\<应用名>\`），属性页标注「属于 XX 应用的隔离数据」；卸载时（F031）弹清扫清单勾选框。
**【数据与存储】**重定向表 = 静态规则表（路径前缀匹配）+ 应用清单声明可覆盖；重定向发生时记录审计条目（应用/原路径/实际路径）供诊断页查。
**【状态与异常】**程序试图写系统镜像区 → 拒绝 + 归因日志（不给「写成功」假象）；路径穿越（..\）逃逸尝试 → 规范化后重判，穿越失败如实报错；跨沙盒共享需求 → 显式声明「共享数据区」白名单。
**【验收判据】**50 件清单逐件审计：真实系统区零写入（镜像哈希前后一致）；重定向审计条目抽查 20 条全部可解释。
**【开源复用】**规则语义参照 Windows UAC 虚拟化（File and Registry Virtualization）文档；实现自研挂接在兼容层路径解析点。

【设计细节】规则表按路径前缀 trie 匹配（复杂度随路径深度）；合并视图的「系统镜像次之」指镜像内只读文件可见可读不可写；审计条目环形保留 1000 条；用户文档直通白名单含「我的文档/下载/桌面/图片」四目录；跨沙盒共享区位于 SharedData（显式声明应用才可写，写操作过审计）。

### G-A-11 F011 环境变量会话面 · 完整设计

**【功能定义】**会话级环境变量表：系统级（PATH/TEMP/TMP/USERNAME/COMPUTERNAME 等 Windows 标准名）+ 用户级（设置中心配置）+ 会话注入（代理等，判例 14）；兼容层程序与原生程序同表语义（大小写不敏感语义按 Windows）。
**【用户故事】**开发者在设置中心把 `GOPATH` 加进用户变量——下一个打开的终端与 VS Code 都能看到；改完正在运行的程序不受影响（会话边界语义，与 Windows 一致）。
**【交互设计】**设置中心「系统-关于-高级系统设置-环境变量」页：双列表（用户/系统）、新建/编辑/删除、变量名与值行内编辑（值长度上限 32KB）；搜索框过滤变量名。
**【数据与存储】**变量表存配置层（`config/env.json`），还原点（F121）覆盖；注入时展开自引用（%PATH% 引用链上限 10 层防环）。
**【状态与异常】**变量名含非法字符 → 行内红框即时校验；引用环 → 截断 + 警告；超大 PATH（>8KB）警告性能影响。
**【验收判据】**标准名 20 项在兼容层程序内 `GetEnvironmentVariable` 全取到；判例 14 代理场景回归绿。
**【开源复用】**标准名清单照 Microsoft 环境变量文档；无外部依赖。

【设计细节】大小写不敏感语义在表内实现为「首个写入的大小写为准」（Windows 同语义）；PATH 按分号切分展示（编辑器内每行一条防漏项）；COMPUTERNAME 取设备名（F123「关于本机」同源）；变量值内百分号展开仅注入时执行一次；TEMP 指向沙盒内 tmp（每应用独立，防交叉污染）。

### G-A-12 F012 控制台子系统（ConDrv 级）· 完整设计

**【功能定义】**控制台类 .exe 的宿主：VARIX 终端应用提供 ConHost 等价层——控制台 API（WriteConsole/ReadConsole/AllocConsole/SetConsoleTitle/光标控制 VT 序列）翻译到终端的渲染面；stdout/stderr/stdin 三流全保真，exit code 原样返回。
**【用户故事】**开发者双击一个控制台构建脚本（build.cmd 调用的 .exe），VARIX 新开终端标签页：输出彩色、进度条原地刷新（\r 语义）、跑完 exit code 0——和他习惯的 Windows 终端行为一致。
**【交互设计】**终端呈现全走 F095 终端 2.0（标签页/主题/回看）；控制台程序窗口关闭 = 终止进程前先发 CTRL_CLOSE_EVENT 给清理机会（5s 宽限）；Ctrl+C 中断语义逐一对齐。
**【数据与存储】**控制台缓冲区（回看 10 万行）在终端进程内存，不落盘；会话导出功能（F096）可存文本。
**【状态与异常】**程序写 UTF-16 宽字符 → 转码渲染（F034 编码纪律）；程序死循环输出 → 渲染节流（输出丢弃策略显式：保最新），不拖垮终端；程序读 stdin 阻塞 → 窗口关闭时强制终止（Windows 同语义）。
**【验收判据】**开源命令行工具（如 7-Zip 命令行版）压缩-解压-列出全流程 exit code 保真；VT 序列测试集（含颜色/光标/清屏 40 例）渲染全对。
**【开源复用】**VT 序列语义参照 Microsoft 终端 VT 文档与 cmatrix 类测试集；ConHost API 语义参照 Wine kernell32 控制台面。

【设计细节】ConDrv 语义：控制台程序可 AllocConsole 自带窗口（独立终端标签页）也可附着父终端；VT 序列支持含 256 色（SGR 38/48）与真彩（SGR 2）；窗口标题随 SetConsoleTitle 实时更新；CTRL_CLOSE_EVENT 宽限 5 秒后强杀（倒计时显示）；exit code 16 位全保真回传。

### G-A-13 F013 .lnk 与快捷方式 · 完整设计

**【功能定义】**Windows 快捷方式（.lnk Shell Link 二进制格式）解析器：目标路径/参数/工作目录/图标位置/热键/窗口风格六字段全解析；VARIX 自身快捷方式沿用同格式（生态互通），安装器（F030）开始菜单项即 .lnk。
**【用户故事】**Variable 把 Windows 里拷来的一个 .lnk 拖进 VARIX 桌面——图标正常显示（目标程序图标经 PE 资源提取 F014）、双击启动目标程序；「这快捷方式还能直接用？」。
**【交互设计】**右键菜单对齐 Windows：打开/固定到任务栏/属性（属性页显示全部六字段可编辑）；创建快捷方式动线在 C-4 右键全集内。悬停 tooltip 显示目标完整路径。
**【数据与存储】**.lnk 文件本身即存储格式（二进制按 MS-SHLLINK 规范解析），图标缓存按目标哈希入缩略图库（F093 同构）。
**【状态与异常】**目标不存在 → 图标灰显 + 双击弹「目标已丢失，查找/删除快捷方式」二选（对齐 Windows 断链处理）；网络路径 → 诚实标注不支持；循环快捷方式（指向自身）→ 解析深度上限 5 层后报错。
**【验收判据】**构造样本集 15 枚（含 Unicode 路径/带参数/带图标索引/环境变量目标）解析全对；断链处理三分支各录屏。
**【开源复用】**格式解析参照 MS-SHLLINK 开放规范与 liblnk（评估只读接入或自研小解析器，格式复杂度中等）。

【设计细节】解析器覆盖 LinkFlags 全部 18 个已知标志位（未知位跳过不报错，向前兼容）；IconLocation 支持独立文件路径加索引；相对路径目标按快捷方式所在目录解析；图标缓存键含目标 mtime（程序更新图标自动刷新）；「固定到开始菜单」动线与 F072 最近使用联动。

### G-A-14 F014 PE 资源全解析 · 完整设计

**【功能定义】**PE 资源节（.rsrc）三件套：图标组（多尺寸 RT_ICON → 选最优档给桌面/任务栏/对话框）、版本信息（VS_VERSIONINFO → 属性「详细信息」页：产品名/版本/公司/版权）、清单（manifest → DPI 感知三态 F028 + 兼容性声明 + 主题声明）。
**【用户故事】**Variable 右键一个 .exe 看属性——「详细信息」页列出产品名、版本号 2.3.1、公司名、版权声明，和 Windows 上看到的一样全；桌面图标用的是程序内嵌的 256px 图标，4K 屏上清清楚楚。
**【交互设计】**属性对话框「详细信息」页字段布局对齐乙-4 表对话框规范；图标提取优先级：256>64>48>32 逐级降档（4K 重采样走 C-7 管线）；manifest 声明 DPI-aware 的程序进属性页标注「已适配高分辨率」。
**【数据与存储】**图标缓存按 (文件哈希+尺寸) 入缩略图库；版本信息不缓存（属性页现取现显）。
**【状态与异常】**无资源节 → 默认 VARIX 通用图标 + 属性页「无版本信息」；图标资源损坏 → 逐尺寸回退，全坏用默认图标；manifest 非法 XML → 按 unaware 处理 + 诊断日志。
**【验收判据】**50 件清单图标提取成功率 100%（有图标的样本）；版本信息字段抽 10 件与 Windows 显示对照全对。
**【开源复用】**格式解析参照 pe-parse 资源遍历逻辑（自研实现对拍）；图标重采样用既有 4K 管线缩放器。

【设计细节】资源目录树按 ID-语言-尺寸三级遍历；图标选择策略：目标尺寸有精确档用精确档，无则就近放大禁止缩小（小图标放大等于糊）；256px PNG 压缩格式图标支持；manifest 解析容忍 BOM 与命名空间前缀；版本信息字符串表全字段展示（Comments 等冷门字段不藏）。

### G-A-15 F015 多语言 .exe 资源 · 完整设计

**【功能定义】**RC 字符串/对话框/菜单资源按语言 ID 选择：系统 UI 语言（简中）优先、英文回退、其他语言再回退；代码页转换表（GBK/Big5/Shift-JIS/西欧各码页 → UTF-8）全量内嵌。
**【用户故事】**一款双语开源工具（内置简中与英文资源）在 VARIX 上界面自动是简体中文；一款只有日文的工具正常显示日文——用户看到的语言永远有序可循，不出现「半中半英乱码」。
**【交互设计】**无独立 UI；效果在 F005/F008 各对话框与程序菜单中体现；语言选择规则在设置中心「时间和语言」页可查（展示当前生效顺序）。
**【数据与存储】**码页转换表编译进镜像（只读，约 300KB）；语言选择结果按会话生效。
**【状态与异常】**无简中资源 → 回退链逐级降，最终若只有中立语言（LANG_NEUTRAL）则用之；资源内字符串坏码页 → 按声明的码页强转 + 替换字符（U+FFFD）显式可见（乱码可见而非隐藏）。
**【验收判据】**双语样本 5 枚自动出中文；码页转换测试集 8 码页 × 100 常用字 round-trip 全对。
**【开源复用】**码页表评估复用 ICU 子集（裁剪）或 iconv 表（只链接数据不链接代码，许可证核验入 F130 登记册）。

【设计细节】语言选择序：zh-CN 到 zh 到 en-US 到 en 到中立逐级回退；资源语言匹配支持子语言降级（zh-TW 请求在无 zh-TW 资源时降 zh 全系）；东亚码页表含未定义区处理（映射 U+FFFD 并计数）；对话框模板资源的语言标记同样参与选择——菜单对话框全链一致。

### G-A-16 F016 字体链兼容 · 完整设计

**【功能定义】**Windows 字体名到 VARIX 字体族映射：宋体→衬线族、微软雅黑→无衬线族、Consolas/Courier→等宽族、Segoe UI→无衬线族；请求字号 pt→px 换算按 96DPI 基准；缺字回退链对齐 MD2 篇 34。
**【用户故事】**程序请求「微软雅黑 9pt」——VARIX 用自己的无衬线族 12px 渲染，字重和排布视觉同级；请求「Consolas 10pt」得到 VARIX 等宽族——程序不炸，用户的眼睛不累。
**【交互设计】**无独立 UI（E7 字体设置页展示映射表只读）；渲染质量验收与原生窗口并排截图对比（同字号同文本，视觉层级一致）。
**【数据与存储】**映射表编译进镜像（配置可覆盖，E7 令牌）；字体度量缓存入字形图集管线。
**【状态与异常】**请求不存在字体 → 回退族 + 日志；pt 超界（>200pt）钳制；字体缺失字符 → 回退链逐级（最终 tofu 显式可见不静默空白）。
**【验收判据】**F005 标志件字体对话框选「微软雅黑」渲染正常；映射覆盖 20 个最高频 Windows 字体名（按采样频率排序）。
**【开源复用】**度量语义参照 Wine 字体子系统文档；字体本体用开源族（思源黑体/更纱等宽候选，F130 登记许可证）。

【设计细节】pt 到 px 换算 1pt 等于 1.333px（96DPI 基准），偏差不超 1px；映射表 20 个高频字体名覆盖宋体/黑体/楷体/雅黑/Segoe/Consolas/Arial/Times/Verdana/Tahoma 等；加粗映射到族内 Bold 字重（无 Bold 时合成加粗并标注）；斜体同策略；E7 页可整体换族（映射跟随用户选择）。

### G-A-17 F017 剪贴板格式族 · 完整设计

**【功能定义】**剪贴板格式翻译中枢：Windows 侧格式（CF_TEXT/CF_UNICODETEXT/CF_BITMAP/CF_DIB/CF_HDROP/CF_ENHMETAFILE/注册格式）与 VARIX 剪贴板协议（B-3901 所有权模型）双向映射；多格式共存（同份数据多格式挂载，按消费者能力选优）。
**【用户故事】**在 Wine 程序里复制一段文字，粘到 VARIX 原生编辑器——格式、字体语义（纯文本保真）、换行风格全部正确；反过来从原生复制到 Wine 程序同样通——双向无感。
**【交互设计】**剪贴板历史（F109）统一展示三型内容；格式详情在诊断工具可见（给排查者）；所有权指示器（谁在持有）进状态栏可选。
**【数据与存储】**剪贴板数据驻内存（所有权协议），历史（F109）持久化上限 20 条可清空；大对象（>16MB 位图）落临时文件引用。
**【状态与异常】**源应用退出后数据存活（剪贴板接管所有权，Windows 同语义）；HDROP 文件被移动后粘贴 → 提示文件已失效；格式协商失败 → 降级纯文本并如实提示。
**【验收判据】**跨表面（Wine↔原生）×五格式矩阵 10 场景粘贴保真；所有权协议红线（后台读取零成功 B-3901）在兼容面回归绿。
**【开源复用】**格式语义对照 Wine clipboard 与 MS 剪贴板格式文档；翻译层自研（协议接缝在 VARIX 剪贴板服务）。

【设计细节】格式优先级表：消费方声明能力后取最高保真交集（文字类 UNICODE 优先于 TEXT，图像类 DIB 优先于 BMP）；CF_HDROP 文件列表支持多选拖粘；私有格式按名称注册会话内有效；延迟渲染（句柄占位按需供数）支持防大对象驻留；历史条目带来源应用标注。

### G-A-18 F018 拖放协议互通 · 完整设计

**【功能定义】**OLE 拖放（DoDragDrop/IDropSource/IDropTarget 语义）与 VXWM 拖放协议（B-3902 三取消）双向翻译：Wine 程序拖出文件到资源管理器、资源管理器拖文件进 Wine 程序、两个 Wine 程序间互拖，三条路全通。
**【用户故事】**Variable 从一个 Wine 版图片管理器拖一张照片到 VARIX 资源管理器——拖拽跟手、落点半透明预览、松手复制完成，Esc 中途取消三态（悬停区取消/落点非法取消/按键取消）全有效——和原生拖放手感零差别。
**【交互设计】**动线全按 C-6/C-4 红线（跟手/落点提示/Esc）；跨表面拖拽时数据预览（文件名+图标）由发起方供给，翻译层保证预览不缺席。
**【数据与存储】**拖拽中数据驻内存（延迟渲染语义：目标请求才真取）；跨盘拖放落 F086 复制管线（进度/冲突面板复用）。
**【状态与异常】**目标不接受该格式 → 光标禁止态（不静默松手失败）；源应用拖拽中崩溃 → 拖放会话回收 + 目标复位；同源同目标死拖 → 超时 30s 自动取消。
**【验收判据】**三方向 × 文件/文本两类数据 = 6 场景全绿录屏；B-3902 三取消逐条在兼容面复测。
**【开源复用】**OLE 拖放语义对照 Wine ole32 拖放面；协议接缝在 VXWM 显式转发（MD1 17.1 既有设计）。

【设计细节】拖拽会话全生命周期状态机（就绪/悬停/落入/取消/完成五态），状态跃迁走 VXWM 报文（可观测可录屏归因）；数据获取延迟渲染在松手时才执行（拖 100 个文件不预读内容）；落点提示矩形 2px 强调色描边加 8% 填充；拖拽中源窗口加 20% 降透明（视觉上「正在被拖出」）。

### G-A-19 F019 COM 本地接口最小集 · 完整设计

**【功能定义】**COM 承诺面裁剪：进程内 COM（CoCreateInstance 类 ID 注册表 = F009 蜂巢的 CLSID 子树）+ 本地对象四族（Shell Link F013/拖放源 F018/剪贴板 F017/应用激活）。进程外 COM/DCOM 明确不承诺（差异表 F132 公开）。
**【用户故事】**程序调用 COM 创建一个 Shell Link 来生成快捷方式——成功；调用某个专业 COM 组件（如语音识别 SAPI）——得到结构化错误「该组件不在 VARIX 兼容面」，引导用户提判例请求——**每个边界都有说法**。
**【交互设计】**无独立 UI；错误经 F035 向导呈现；CLSID 注册内容可在蜂巢查看器（F009 诊断入口）查。
**【数据与存储】**CLSID 注册进应用蜂巢；COM 对象生命周期随进程（引用计数语义按 Windows：AddRef/Release 对拍）。
**【状态与异常】**未注册 CLSID → REGDB_E_CLASSNOTREG 如实返回（不假装成功）；聚合请求不支持 → 明确错误码；引用计数泄漏检测 → 进程退出时告警日志。
**【验收判据】**四族本地对象各一个开源消费者场景全绿；未注册类错误路径文案三要素。
**【开源复用】**语义对照 Wine ole32 本地对象族；实现自研接缝。

【设计细节】CLSID 蜂巢子树路径 Classes 下 CLSID 节点；类对象注册会话级；四族对象接口方法覆盖以「50 件清单实际调用面」为准（A2 数据驱动扩面）；QueryInterface 对未知 IID 返回 E_NOINTERFACE（不假装支持）；聚合按不支持处理（差异表登记）。

### G-A-20 F020 异常与调试面 · 完整设计

**【功能定义】**SEH/VEH 结构化异常翻译：try/except 语义（异常过滤/展开/最终处理）映射到 VARIX 信号与 unwind 层；未处理异常 → minidump（线程栈/寄存器/模块表）落诊断中心，应用按 F175 隔离回收。
**【用户故事】**一个程序段错误崩了——VARIX 弹「程序已停止工作」卡片（可重启/可关闭/可查看详情），详情页就是 minidump 摘要（崩溃地址/模块/异常类型）；开发者拿到 dump 能定位到自己的源码行。崩溃的用户感受是「一个窗口没了」，不是「整个系统没了」。
**【交互设计】**崩溃卡片样式对齐 F035 体系；「重新启动应用」保留原参数重拉；「查看详情」内嵌 dump 摘要只读页。
**【数据与存储】**minidump 存 `diagnostics/dumps/` 上限 20 个 LRU；dump 格式用自定轻量格式（可导出转换 CDB 可读文本）。
**【状态与异常】**异常处理器自身再异常 → 双重故障直接回收进程（不递归）；栈溢出 → 专用栈保护页捕获按栈溢出归因；dump 写入失败 → 静默降级为日志摘要。
**【验收判据】**注入异常样本 6 类（除零/非法访问/栈溢出/未处理 C++ 异常/SEH 吞噬/VEH 链）行为全对；崩溃后桌面帧率实测不跌（F175 联动）。
**【开源复用】**SEH 语义参照 Microsoft 异常文档与 Wine exception 面；minidump 格式参考 MDMP 规范裁剪自定。

【设计细节】异常翻译层内核零堆（定长异常帧栈深 32）；minidump 含：异常码/崩溃地址/线程栈回溯 64 帧/已加载模块表（含校验和）/系统版本指纹；dump 压缩存储（zstd 级评估）；「重新启动应用」保留命令行与工作目录（启动时快照）；同一文件 24 小时内崩溃三次以上自动建议提交 F036 草稿。

### G-A-21 F021 内存语义对齐 · 完整设计

**【功能定义】**VirtualAlloc 族（RESERVE/COMMIT/PROTECT 六保护位/写时复制）、MapViewOfFile 文件映射、VirtualQuery 查询、大页请求（自适应降级）全语义对齐；伙伴分配器供给（WP-104 既有）加大页优先策略。
**【用户故事】**一款内存敏感的开源压缩工具（自管内存池）在 VARIX 上跑基准——吞吐与 Windows 相差 <5%，它的 VirtualAlloc 大块请求拿到对齐良好的物理页。
**【交互设计】**任务管理器（若拍板 A）进程内存三列：工作集/提交/映射——三列语义与 Windows 同名同义；诊断中心内存面板显示整系统分配分布。
**【数据与存储】**提交记账入 MD2 附录 K 内存配额账（F195 配额执行）；文件映射页缓存与存储栈共享（同一份页缓存）。
**【状态与异常】**内存不足 → COMMIT 失败返回 NULL（Windows 语义），程序自处理；保护位违规 → SIGSEGV 类异常进 F020 流程；释放后再访问 → 守卫页捕获（F176）。
**【验收判据】**7-Zip 基准实测无降级（F021 验收原文）；VirtualAlloc 压力脚本（分配-提交-触碰-释放 1 万轮）无泄漏无碎片失控。
**【开源复用】**语义对照 Wine virtual.c 与 MS 文档；实现自研（伙伴/slab 既有面扩展）。

【设计细节】RESERVE 地址空间按 64KB 粒度（Windows 同粒度）；PROTECT 六位全支持（含写时复制位）；WRITECOPY 即写时复制（改页触发复制）；文件映射大小按文件当前大小（扩展后需重映射，语义对齐）；大页请求 2MB 对齐不足自动降 4KB（降级日志）。

### G-A-22 F022 时间与定时器族 · 完整设计

**【功能定义】**时间 API 族：GetTickCount(64)（毫秒 uptime）/QueryPerformanceCounter（TSC 2500MHz 直通，实测 isa-none 已就绪）/GetSystemTime（UTC+时区换算）/timeBeginPeriod（定时器精度请求，尊重但记账）/SetTimer/WaitableTimer。
**【用户故事】**一款开源音乐练习软件依赖高精度计时打拍子——在 VARIX 上节拍稳定（QPC 直通 TSC，无分层纠偏抖动）；长开 24 小时的工具 GetTickCount 不回绕（64 位化）。
**【交互设计】**无独立 UI；定时器精度请求的「谁在要求高精度」进功耗面板（F060 归因数据源之一）。
**【数据与存储】**时区/夏令时规则数据内嵌（tzdata 裁剪版，F130 登记）；uptime 派生自内核打点。
**【状态与异常】**timeBeginPeriod 滥用（多个 1ms 请求）→ 合并执行 + 功耗归因标注；系统休眠唤醒后 QPC 连续性保证（计数不跳变）；时钟被改（F187 NTC 校时）→ GetTickCount 不受影响（uptime 与墙钟分离）。
**【验收判据】**QPC 与外部秒表 24 小时漂移 <50ms；游戏类样本（开 1ms 定时器）帧节奏实测稳定； tzdata 换算 10 时区 round-trip 全对。
**【开源复用】**tzdata 直接复用（IANA 公共数据）；语义对照 Wine kernel32 时间面。

【设计细节】QPC 频率常量等于 TSC 实测频率（启动时校准，2500MHz 基线）；GetTickCount 64 位化且 32 位接口保留 49.7 天回绕语义（对齐 Windows 让程序按预期走）；SetTimer 最小 10ms（请求更短钳制并记账）；timeBeginPeriod 全局最小值生效（多请求取最急）；休眠唤醒后 tick 补齐墙钟差值。

### G-A-23 F023 网络栈 Winsock 面 · 完整设计

**【功能定义】**Winsock2 常用面：socket/connect/bind/listen/accept/send/recv/sendto/recvfrom/select/ioctlsocket/getaddrinfo（DNS 走系统解析器）/WSAAsyncSelect（消息泵模型适配）；阻塞/非阻塞双模式；smoltcp socket 面承接（B-60x 既有）。
**【用户故事】**一款开源 FTP 客户端（Win32 写的）在 VARIX 里连上服务器、浏览目录、传文件——主动/被动模式都通；用户不知道底下是 smoltcp 在替 BSD socket 打工。
**【交互设计】**无独立 UI；网络错误（超时/拒连/DNS 失败）错误码如实翻译（WSAETIMEDOUT 等），程序自己的错误提示自然正确。
**【数据与存储】**socket 缓冲配额按进程（F195）；DNS 缓存 60s TTL（系统级共享）。
**【状态与异常】**句柄泄漏检测（每进程 socket 上限 256）→ 超限告警；网络断开 → 进行中 send/recv 返回 WSAECONNRESET（不悬挂）；WSAAsyncSelect 消息窗口失效 → 静默回收 + 日志。
**【验收判据】**FTP 客户端全流程绿；SSH 交互延迟 ≤30ms（F059 线）；并发 50 socket 压测无饥饿。
**【开源复用】**API 语义对照 Wine ws2_32 与 MS Winsock 文档；传输层全复用 smoltcp（F130 借力件）。

【设计细节】socket 句柄表每进程 256 上限；WSAAsyncSelect 消息映射到窗口消息保留段（与程序自定义消息隔离）；getaddrinfo 支持域名/点分地址/服务名端口；阻塞调用走异步内核等待（不占线程自旋）；UDP 广播/组播基础支持；每 socket 双向缓冲 64KB 默认可调（F195 配额内）。

### G-A-24 F024 TLS 与证书库 · 完整设计

**【功能定义】**rustls 承载 TLS1.2/1.3（F130 借力件），Winsock 面 SChannel 语义适配层：证书校验链走 VARIX 证书管理器（系统根证书包 + 用户导入证书）；Windows 证书库语义（CertOpenStore 族常用四函数）映射。
**【用户故事】**一款开源下载工具走 HTTPS 拉文件——证书链校验正常、被撤销证书如实拒绝；用户导入了自己公司的根证书后内网 HTTPS 也能通。
**【交互设计】**设置中心「隐私和安全性-证书」页：根证书列表/导入（PEM/DER 选择器复用 F008）/指纹展示/删除（回收站语义）；导入时大字提示风险确认。
**【数据与存储】**证书库存配置层（还原点覆盖 F121）；私钥文件权限 0600 等价强制。
**【状态与异常】**证书过期/域名不匹配 → 程序收到校验失败码（不给绕过后门，程序自己决定 UI）；用户证书损坏 → 导入时即拒并归因；CRL/OCSP 检查网络不可用 → 按软失败策略并日志（策略公开在差异表）。
**【验收判据】**HTTPS 下载场景 10 站点全通；自签/过期/域名错三负样本全拒；rustls 版本号入 F130 登记册。
**【开源复用】**rustls（借力件）+ webpki 根证书包（Mozilla 集合，来源标注）；SChannel 语义对照 Wine secur32。

【设计细节】TLS 会话票据缓存（连接复用提速）；SNI 如实透传；证书页指纹显示 SHA-256 全串可复制；导入证书自动分类（根/中间/个人三区）；系统根包随借力件升级窗更新（F138 日历）；软失败策略：OCSP 不可达时放行加诊断面板橙灯（策略可关）。

### G-A-25 F025 打印虚拟面 · 完整设计

**【功能定义】**「VARIX PDF 打印机」虚拟打印设备：GDI 打印路径（StartDoc/TextOut/BitBlt 到打印 DC）光栅化 → 开源 PDF 生成库封装成 PDF 文件 → 保存对话框（F008）落用户文档目录。物理打印机列后程（差异表公开）。
**【用户故事】**用户在一个只有「打印」按钮的老程序里想留住一张票据——选「VARIX PDF 打印机」打印，保存对话框弹出，得到一份可分享的 PDF——**没有物理打印机也不耽误事**。
**【交互设计】**打印对话框动线对齐 Windows（打印机选择/页码范围/份数——虚拟打印机仅页范围生效）；打印完成 toast 带文件路径直达按钮。
**【数据与存储】**光栅中间物驻内存流式进 PDF（A4 300dpi ≈ 8MB 上限告警）；产出 PDF 元数据写源程序名与时间。
**【状态与异常】**打印任务取消（对话框关闭/任务取消按钮）→ 资源回收零残留；字体缺字 → PDF 内嵌 VARIX 回退族并日志；超大页码范围 → 确认对话框防手滑。
**【验收判据】**三款开源程序打印到 PDF 全流程绿；产物 PDF 在外部阅读器打开正常（互操作抽查 3 款阅读器）。
**【开源复用】**PDF 生成评估 libharu 或 pdf-writer 级小库（只链接不修改，版本锁定 F130）；光栅走 GDI 面（F006）。

【设计细节】PDF 产出 300dpi A4 基准（可切 150dpi 省体积）；文字以矢量内嵌（VARIX 字体子集化，文件更小可选位图化保兼容）；打印队列面板（多任务排队/取消/重打）；任务超 5 分钟自动告警；产物自动命名「程序名-日期时间.pdf」可在对话框改。

### G-A-26 F026 音频 WinMM/DirectSound 面 · 完整设计

**【功能定义】**waveOut 族 + DirectSound 常用路径（主缓冲/次缓冲/音量/pan）翻译到 HDA 混音器（B-806/807）：每应用独立流、独立音量、逐流监视；WASAPI 独占模式不承诺（差异表）。
**【用户故事】**用户一边听音乐（Wine 播放器）一边玩游戏提示音（原生）——混音器面板两条流各自音量条实时跳动，把音乐拉低游戏声音突出——**谁在出声、多大声，一眼可知一杆可调**。
**【交互设计】**快速设置面板（F076）音量条旁「应用混音器」入口：逐应用音量滑杆 + 静音钮 + 流活动指示（60fps 电平条）；默认设备切换即时生效。
**【数据与存储】**混音配置（每应用记忆音量）存配置层；无音频落盘（录音走 F104 独立面）。
**【状态与异常】**设备拔出（HDA 异常）→ 各流切默认设备并通知；缓冲不足（程序供数慢）→ 静音欠载（underrun 计数入诊断）不爆音；独占请求 → 按共享降级 + 日志。
**【验收判据】**双流混音实测无互扰；欠载率 <0.1%（正常负载）；WinMM 语义测试集（音量/pan/重置）全对。
**【开源复用】**语义对照 Wine winmm/mmdevapi；混音核心自研（B-807 既有面）。

【设计细节】混音器电平条走合成器独立图层（不触发窗口重绘）；每流默认音量在应用退出时保存；DirectSound 3D 不承诺（差异表）；采样率不匹配实时重采样（多相滤波，延迟小于 2ms）；主音量闸优先于应用音量（总闸语义）；静音态电平条保留但灰显（可观测不误判）。

### G-A-27 F027 输入法 IMM32 面 · 完整设计

**【功能定义】**Wine/兼容程序经 IMM32 消费 VARIX IME：组合串/候选窗/提交文本三事件桥接；候选窗渲染统一走 VARIX 风格（F166 皮肤），程序窗口只收最终提交与组合中串（GCS_* 语义）。
**【用户故事】**在 Wine 记事本里打拼音——候选窗出现在光标下方（VARIX 风格、字体随主题），选词上屏、组合中串有下划线——与原生程序里打字体验无差别；程序以为自己面对的是 Windows 输入法。
**【交互设计】**候选窗定位：程序提供 EXFORMINFO 则尊重其位置，否则光标锚定 8px 偏移（F107 同构）；组合中串样式（下划线/背景）按 VARIX 主题令牌。
**【数据与存储】**无新增存储（词库/短语复用 C5/F108 既有面）。
**【状态与异常】**程序不支持 IME 消息 → 降级直通提交（无组合预览，如实体验并差异表标注）；候选窗程序崩溃 → 输入服务自恢复；焦点切换竞态 → 组合串自动提交或回退（显式策略）。
**【验收判据】**B-904 组合期零误触在兼容面复测绿；三款 Wine 程序打中文全流程录屏；候选窗延迟 ≤16ms 实测。
**【开源复用】**IMM32 语义对照 Wine imm32；桥接自研（VARIX IME 既有四层引擎供数）。

【设计细节】组合串传输每键即时（程序端实时下划线渲染）；候选窗跟随光标重定位节流 16ms；程序窗口失焦自动收起候选；数字键选词/空格首选/回车裸串三提交语义照 Windows；表情符号候选支持；与 F108 短语库共享候选窗渲染管线。

### G-A-28 F028 高 DPI 感知三态 · 完整设计

**【功能定义】**按程序 manifest（F014）声明执行三态：unaware（合成器位图放大，模糊但比例正确）/ system-aware（按主屏 DPI 虚拟化坐标）/ per-monitor aware（拿真实像素）。VARIX 不越权替程序「变清晰」——尊重声明。
**【用户故事】**用户把缩放调到 150%：老工具（unaware）界面等比放大（略糊可接受，属性页有说明）；新程序（per-monitor）文字锐利如初——两类程序同屏各得其所，用户不困惑。
**【交互设计】**属性「兼容性」页显示程序 DPI 声明 + 用户级覆盖下拉（对齐 Windows 兼容性设置动线）；缩放变更时 aware 程序收 WM_DPICHANGED 实时重排。
**【数据与存储】**用户覆盖存应用蜂巢（F009）；缩放档位全局（E7 联动）。
**【状态与异常】**无 manifest → 按 unaware 处理（保守正确）；声明与实际行为矛盾（声明 aware 却糊）→ 如实按声明执行 + 差异表登记样本；多屏未支持期 per-monitor 按 system 处理（F029 前瞻联动）。
**【验收判据】**三态样本各 2 枚在 100%/150% 缩放下行为全对；WM_DPICHANGED 重排无闪烁录屏。
**【开源复用】**语义对照 MS High DPI 文档；放大滤波评估 Scalar/Anime4K 类开源算法（可选增强，默认双线性）。

【设计细节】unaware 放大走合成器离屏位图（应用不感知，成本为一次重采样）；150% 下 unaware 文本可读性实测（SSIM 大于 0.85 达标线）；覆盖下拉含「程序默认/强制 aware/强制 unaware」三选（高级排查用）；DPICHANGED 重排宽限 500ms（超时按旧尺寸保持不撕裂）；缩放档 100/125/150/175/200 五档（对齐 Windows）。

### G-A-29 F029 多显示器前瞻接口 · 完整设计

**【功能定义】**显示枚举/模式 API 以单屏实现返回（EnumDisplayMonitors 返回一枚、分辨率即当前），接口形状与 Windows 多屏语义同构（不锁死），未来多屏替换实现层不动 API 契约。
**【用户故事】**开发者写的程序调用枚举显示器的代码——拿到一块屏的数据正常运行；两年后 VARIX 支持多屏，同一程序零改动看到两块屏——**今天的单屏不给明天的多屏埋雷**。
**【交互设计】**设置中心「系统-屏幕」页预留「检测其他显示器」按钮（灰置 + 「即将支持」标注）——诚实展示路线图位置。
**【数据与存储】**无新增存储。
**【状态与异常】**程序请求切模式（ChangeDisplaySettings）→ 单屏内分辨率档位切换支持，越界请求如实失败码。
**【验收判据】**枚举类 API 返回结构与 MS 文档字段级对拍；接口冻结登记（变更走 ADR）。
**【开源复用】**接口形状对照 MS 文档；无运行时依赖。

【设计细节】枚举结构预留显示器数组（长度 1）；虚拟桌面与多屏正交（虚拟桌面先上）；分辨率档位切换即显即生效（无重启）；「即将支持」标注文案入词条库（F140 字符串表）；接口冻结登记编号 ADR-PR-001（变更须 ADR）。

### G-A-30 F030 安装器兼容模式 · 完整设计

**【功能定义】**NSIS/Inno Setup/MSI 三族安装器在沙盒内完成安装：临时解压/进度页/完成页如实呈现；安装产物登记（F031 卸载清单）+ 开始菜单 .lnk 生成（F013）+ 桌面图标（C-3 尺寸网格）。
**【用户故事】**用户下载一款开源软件的 Windows 安装包双击——安装向导正常走完（下一步/下一步/安装/完成），开始菜单出现入口、桌面出现图标、软件能用、控制面板里能卸载——**「装软件」这件事在 VARIX 上是完整体验不是走钢丝**。
**【交互设计】**安装向导窗口走 VARIX 窗口装饰（客户区程序自绘）；UAC 类提权语义按 F038 隔离档替代（弹 VARIX 权限卡说明将写入哪些沙盒区）；安装完成页若程序自动勾选「立即运行」则如实执行。
**【数据与存储】**全产物落 F009/F010 沙盒；卸载清单（文件清单+蜂巢+快捷方式三表）存沙盒元数据。
**【状态与异常】**安装器要求重启 → 诚实告知「VARIX 无需重启即可用，已跳过重启步骤」+ 日志；安装中途取消 → 已写文件进回收站式回滚（零真删）；MSI 自修复请求 → 重新执行修复流。
**【验收判据】**三族各一款开源软件安装-使用-卸载全周期绿；沙盒外镜像哈希不变（与 F010 联合验证）。
**【开源复用】**无运行时复用（沙盒与重定向是本体）；安装器行为参照各自开源实现文档（NSIS/Inno 开源、MSI 参照规范）。

【设计细节】NSIS 解包监听：临时目录写入映射沙盒 temp；Inno 的任务选择页与安装目录页全可交互（目录默认沙盒 Program，用户可改到共享区）；MSI 序列号与组件选择按标准 UI 流；安装进度条程序自绘（如实透传）；「立即运行」勾选执行时若触发 F035 失败则向导接管。

### G-A-31 F031 卸载与残留清扫 · 完整设计

**【功能定义】**设置中心「应用-已安装」页：列表（图标/名称/大小/安装日期）+ 卸载按钮；卸载 = 沙盒目录+蜂巢+快捷方式三清（清单驱动），清扫项给用户勾选（默认全选，保留项可勾出），清扫物进回收站（F085 动画语义）。
**【用户故事】**Variable 卸载试装过的工具——勾选页显示「将移除：程序文件 86MB / 设置数据 2MB / 开始菜单 3 项」，他勾掉「保留设置数据」，卸载后重装设置还在——**卸载是可控的告别，不是抽奖**。
**【交互设计】**列表排序（名称/大小/日期）、搜索过滤、卸载进度条（大沙盒不卡 UI，F086 管线复用）；卸载完成 toast + 「撤销」按钮（回收站还原语义，10 分钟内有效）。
**【数据与存储】**清扫记录审计条目；「撤销」依赖回收站未清空（零真删红线）。
**【状态与异常】**卸载器是程序自带（ uninstall.exe）→ 沙盒内执行后补清扫清单差额；清单缺失（手工安装）→ 按目录树整沙盒清除 + 勾选页如实显示；卸载中程序正在运行 → 先请求关闭（F175 宽限），拒关则列明进程让用户选。
**【验收判据】**三族样本卸载后残留扫描 = 勾选外零残留；撤销路径实测还原成功。
**【开源复用】**无外部依赖；清单格式自定（开放进 F126 规范面）。

【设计细节】清扫清单按三类分组展示（文件/注册表/快捷方式）逐类可勾；「撤销」窗口 10 分钟倒计时显示在 toast 内；清单缺失时目录树清扫排除「用户明确放入」文件（时间戳早于安装时刻的文件列外并说明）；卸载统计入生态季报（F149：平均残留率指标）。

### G-A-32 F032 开源运行时直装族 · 完整设计

**【功能定义】**五语言工具链 vxapp 包（Python/Node/Java JDK/Go/Rust）：包内含运行时+包管理器（pip/npm/maven go mod cargo），装后 PATH 注入（F011）、解释器解析按会话；VSCode（Wine 面或后续原生）扩展调用链判例齐。
**【用户故事】**学生用户装上 Python 包，打开终端敲 `python --version` 出版本号，`pip install requests` 走网络栈（F023/F024）装成功，VS Code 里写脚本按 F5 直接跑——**在 VARIX 上学编程，和在 Windows 上一样顺**。
**【交互设计】**各语言包安装完成页显示「验证安装」按钮（一键跑 version/hello-world/包安装三验）；多版本并存提示（激活机制显式，不搞隐式切换）。
**【数据与存储】**运行时落沙盒化共享区（`~\Toolchains\<lang>\<ver>\`），包缓存全局共享防重复下载；PATH 注入会话级（F011 语义）。
**【状态与异常】**包管理器下载失败 → 三要素错误 + 重试；磁盘空间不足 → 预检拦截（装前算大小）；运行时损坏 → 「验证安装」检出后一键重装。
**【验收判据】**每语言「装-写-跑-调」四步判例（调 = VSCode 断点命中一次）；五语言全绿录屏在册。
**【开源复用】**本体全是开源官方发行版（python.org/nodejs.org/Adoptium/go.dev/rustup 直包重打包 vxapp），零修改零重编译——**语言链兼容的真相是不折腾语言链**。

【设计细节】包缓存全局共享（哈希去重）；版本并存激活机制：会话内显式切换或设置页点选默认（显式无魔法）；pip/npm 走 F023 网络面加 F024 TLS；「验证安装」三验脚本随包发布（判例即产品）；磁盘预检含包缓存膨胀预估。

### G-A-33 F033 构建工具链判例 · 完整设计

**【功能定义】**git/cmake/ninja/make 四件 vxapp 化 + 判例集：clone（HTTPS 走 F024）/子模块/分支操作/cmake 配置生成/ninja 增量构建/make 目标——从 clone 到 run 全程 VARIX 内闭环。
**【用户故事】**开源贡献者把一个小项目 clone 到 VARIX 文档目录，cmake 配置、ninja 构建、跑测试全绿，git 提交推送——他在 VARIX 里完成了完整的一次开源贡献，截图发社区。
**【交互设计】**无独立 UI（终端面 F095 承载）；判例脚本包进社区自测工具（F129 形态），任何人可复跑。
**【数据与存储】**工具链同 F032 布局；git 仓库文件属性（可执行位/符号链接）在 VARIX 文件系统如实保真。
**【状态与异常】**网络中断 → 工具自身重试语义不破坏（透明传输）；大仓库 clone 内存峰值受 F195 配额护住；git 的 fsync 依赖走存储栈硬承诺（B-7xx）。
**【验收判据】**选一开源小项目（<50 文件）全流程录制在册为标志判据；四工具 version/help 基线绿。
**【开源复用】**git/cmake/ninja/make 官方 Windows 版直包（零修改），接缝只在文件系统与网络语义——构建链是「直通类」的旗舰样本。

【设计细节】git 的换行符按仓库 attributes 如实（不全局改写）；符号链接支持目录链接；cmake 生成器用 ninja（判例锁定）；增量构建判据：二次构建仅重编变更文件（时间戳比对实测）；构建产物默认落项目目录（无沙盒劫持）；判例脚本公开（F129 自测包）。

### G-A-34 F034 字符编码终局 · 完整设计

**【功能定义】**UTF-8 为 VARIX 系统编码；兼容层内 ANSI API（A 后缀）按声明码页翻译；每个「乱码场景」一条判例兜底（GBK 文本打开/Zip 内文件名 CP437 历史/ini 文件 ANSI 读写），乱码显式可见（U+FFFD）不静默吞。
**【用户故事】**用户打开一个 GBK 编码的老 txt——记事本类编辑器（F097）检测到非 UTF-8，弹「此文件使用 GBK 编码，已按 GBK 打开（另存为 UTF-8）」提示条——**乱码被系统接住，而不是丢给用户**。
**【交互设计】**编码检测提示条（编辑器顶部）：显示检出编码 + 「重新按 XX 编码打开」下拉 + 「另存为 UTF-8」按钮；无 BOM 时检测顺序 UTF-8 严格校验 → UTF-16 启发式 → GBK 启发式 → Latin-1 兜底（顺序文档化）。
**【数据与存储】**编辑器打开原文件字节不动（显示层转换），保存时按用户选择编码写出（显式选择，不偷换）。
**【状态与异常】**检测置信度低 → 提示条列出候选编码让用户点；二进制文件误开 → 拒绝渲染提示用十六进制视图；转换不可逆字符 → U+FFFD + 日志。
**【验收判据】**乱码判例集 10 场景全绿（每场景一条判例文件）；round-trip 测试 8 码页全对（F015 联动）。
**【开源复用】**检测算法评估 uchardet（只链接或移植启发式表）；码页表复用 F015 面。

【设计细节】检测顺序文档化并给出置信度（大于 0.9 自动选，0.5 到 0.9 列候选，小于 0.5 按二进制拒绝）；GBK 检测启发式高频字表 3000 字内嵌；保存编码选择记忆每文件（元数据伴生文件）；「始终以此编码打开此类扩展」选项；替换字符计数超全文 1% 时警告「可能选错编码」。

### G-A-35 F035 兼容性自检向导 · 完整设计

**【功能定义】**程序启动失败时自动进入的引导流：预检（位数/架构/格式）→ 归因五分类（缺 API/缺运行时/架构不符/权限/未知）→ 分支行动（缺运行时给 F032 下载指引；权限给 F038 提权卡；未知给 F036 星卡草稿+社区提报入口）。
**【用户故事】**双击一个需要 .NET 的程序——向导弹出：「此程序需要 .NET 运行时。去浏览器获取 .NET 桌面运行时（免费，约 55MB）」——按钮直接开浏览器到官方下载页（无商店原则：我们只指路，不代办货架）；装完回来双击，程序打开。
**【交互设计】**向导卡片体系统一（F001 占位窗/F004 拒绝卡/本向导同一视觉族）：图标+标题+一句归因+主按钮+「查看技术详情」折叠（面向开发者给完整日志）。
**【数据与存储】**归因记录按文件哈希记忆（下次同程序失败直接给结论）；提报记录进 F139 反馈通道。
**【状态与异常】**归因置信度低 → 如实「未知原因」+ 日志导出按钮（不硬编原因）；向导自身崩溃 → 降级纯文本错误框；断网时给离线指引（本地已装运行时清单检查）。
**【验收判据】**五分类各构造一个真实失败样本，向导分支全对；「下载→装运行时→程序成功」全流程录屏为标志判据。
**【开源复用】**运行时检测签名表参照各运行时官方识别方式（.NET/VC++ Redist/JRE）；向导本身自研。

【设计细节】归因决策树：位数检查到格式检查到缺运行时签名表到 API 缺失日志分析到权限日志到未知；主按钮动作映射（下载/提权/替代/提报）；「查看技术详情」折叠含完整调用栈与归因链（开发者模式）；向导出现频率统计（某程序反复失败自动建议「移除并反馈」）。

### G-A-36 F036 星卡自动草稿 · 完整设计

**【功能定义】**程序成功运行后静默生成星卡草稿：API 使用采样（兼容面 hook 计数）/启动耗时（F043 画像）/内存峰值/崩溃史；用户在通知中心看到「已为 XX 生成兼容性报告草稿」一键提交（匿名）进星图（D 域 F129 通道）。
**【用户故事】**Variable 装了个冷门开源工具跑了一周——系统提示「XX 的兼容性报告已就绪」，他点提交，三周后星图上出现了这张星卡（他的匿名提交署名在贡献列表）——**每个用户都是兼容性地图的测绘员**。
**【交互设计】**草稿通知合批（一周一次汇总）；提交前可展开预览内容（确认无隐私敏感项：路径已脱敏/无文件名清单默认）；提交状态可查（F139）。
**【数据与存储】**草稿存本地 `diagnostics/stardrafts/` 上限 50 份；提交走 HTTPS（F024）。
**【状态与异常】**API 采样开销超阈值（>1% 性能）→ 自动降采样并标注；用户关闭遥测（隐私设置总闸）→ 草稿功能整体停用（尊重优先）；提交失败 → 本地留存重试。
**【验收判据】**草稿生成对应用启动耗时影响 <1%（vxbench 对账）；脱敏审计（路径/用户名/序列号三类全查无）。
**【开源复用】**无外部依赖；星卡格式沿用星图 JSON 规范（F126）。

【设计细节】API 采样 hook 开销预算 1%——超预算自动切低频采样（每 N 次调用采 1 次）；脱敏三规则硬编码（路径用户段改 user/文件名改哈希前 8 位/序列号类全删）；提交预览页红绿双色标注敏感项；草稿合并：同程序多次会话合成一张完整星卡。

### G-A-37 F037 peblock 门用户可见化 · 完整设计

**【功能定义】**门拦截时给完整解释卡：命中规则名/文件哈希/规则来源；提供「我了解风险，仍要运行」显式越权（二次确认 + 风险清单 + 越权全量审计日志 + 该文件角标永久标注）。
**【用户故事】**开发者自己编译的小工具被哈希规则误伤——他打开解释卡看到「拒绝原因：未签名 + 新哈希」，选择越权运行，程序正常用；窗口角标小盾牌提示未签名——**规则的透明换来的是理解，理解换来的是信任**。
**【交互设计】**解释卡对齐 F035 体系；越权按钮置灰 3 秒防手滑；角标样式 4K 管线、悬停出说明；「信任此发布者」选项（同签名后续免提示，可撤销）。
**【数据与存储】**越权审计 append-only（F194 序号链）；信任列表存配置层（还原点覆盖）。
**【状态与异常】**规则更新后已信任发布者复核 → 静默通过；越权程序后续崩溃 → 审计链自动关联进 dump（F020）；用户想撤销信任 → 设置页一键撤。
**【验收判据】**拦截→解释→越权→审计四步全链录屏；审计不可篡改验证（改一条序号断链即检出）。
**【开源复用】**无外部依赖；规则格式开放（F126）。

【设计细节】规则名展示带规则来源分类（内置/社区/自建）；3 秒置灰用进度环可视化；越权后 72 小时内该文件再拦截直接放行（信任窗口）但审计照记；角标悬停说明含越权时间与操作者（本机账户）；「信任发布者」验证签名证书链（F024 证书库）而非仅哈希。

### G-A-38 F038 应用隔离档位 · 完整设计

**【功能定义】**每应用三档隔离：宽松（全直通+审计）/标准（F009/F010 沙盒+网络允许）/严格（沙盒+禁网络+文档目录只读）；默认标准档；档位变更弹权限卡说明差异；配额（F195）随档联动。
**【用户故事】**用户给一个来路不明的下载工具设严格档——它跑起来后想联网被拒，弹一次说明卡；用户看着它本地干活，放心——**权限心智对齐手机，学习成本为零**。
**【交互设计】**设置中心「应用-权限」页：每应用档位选择器 + 能力开关明细（网络/文档写/剪贴板写/后台运行）；变更即时生效（进程已运行则提示重启应用生效）。
**【数据与存储】**档位存应用蜂巢外层元数据（卸载随清）；权限卡确认记录审计。
**【状态与异常】**严格档应用请求联网 → 拒绝 + 通知（可临时放行 10 分钟，倒计时自动收回）；宽松档滥用（写系统区）→ 审计告警建议升档；档位冲突的子进程继承父档。
**【验收判据】**三档行为矩阵 9 格实测全对；临时放行倒计时自动收回实测；档位变更不崩溃正在运行的系统其他部分（F175 联动）。
**【开源复用】**心智模型参照移动端权限系统（iOS/Android 文档为交互参照，非代码）；实现自研（能力执法 B-15xx 既有面）。

【设计细节】档位差异明细页用九宫格矩阵展示（3 档乘 3 能力组一目了然）；临时放行 10 分钟倒计时在通知卡内实时显示；档位变更审计进 F194 序号链日志；严格档应用的网络请求静默丢弃率计入诊断面板（排查「为什么连不上」第一入口）。

### G-A-39 F039 游戏兼容前瞻面 · 完整设计

**【功能定义】**DirectX 探测层：程序请求 D3D 设备时探测版本需求（D3D9/11/12），当前不支持 → 「一键切 Windows 域」引导卡（复用交接流程 C4，预检 F181 自动带上游戏参数）；wineserver 面（M-D/M-E）就绪后 → DXVK 评估立项（开源转译层，需 GPU 面 B3 先行）。
**【用户故事】**Variable 双击一款老游戏——探测到 D3D11，VARIX 给出：「此游戏需要 DirectX 11 渲染。VARIX 兼容面正在扩展。现在切到 Windows 域玩？你的工作已自动保存。」点确认，25 秒内已在 Windows 游戏里——**不装能跑，就诚实分流，且分流本身丝滑**。
**【交互设计】**引导卡在 F035 体系内；切换复用交接四步画面（会话保全/冲刷/闸门/重启）；「下次直接切」记忆选项。
**【数据与存储】**游戏识别记录（哈希→D3D 版本）缓存；交接参数（游戏全屏参数）进快照扩展字段。
**【状态与异常】**探测到 D3D12 → 同卡不同文案；交接失败（R1 固件脾气）→ 按风险册流程降级回 VARIX 并说明；游戏是 32 位 → F004 与本面合并提示。
**【验收判据】**D3D9/11/12 探测样本各 2 枚分支全对；「双击游戏到 Windows 游戏画面」全流程 ≤30s 实测录屏。
**【开源复用】**D3D 版本探测参照 DXVK/DXVK-dxgi 的设备枚举面（评估阶段）；转译本体（DXVK）留待 GPU 面就绪后立项（F130 预登记）。

【设计细节】D3D 版本探测读程序 import 表 d3d 动态库引用（零开销静态判定）；「下次直接切」写入交接快照偏好字段；切域前自动暂停游戏外的下载与同步任务（省带宽降风险）；回来路径对称（Windows 侧助手一键切回 VARIX）；切换全程电量低于 15% 时劝阻提示（F196 联动）。

### G-A-40 F040 兼容域总判据 · 完整设计

**【功能定义】**「常用 50 件」兼容账本制度：五类（文本编辑/压缩/图片/终端工具/开发工具）各 10 件开源代表，逐件实测建档（安装/启动/核心功能三关），通过率季度刷新进星图与生态季报（F149）。
**【用户故事】**任何人对「VARIX 兼容性怎么样」好奇——星图一页看 50 件清单逐件状态（绿/黄/红+日期+证据链），不用听任何人吹——**兼容性从宣传语变成了可审计的公共账本**。
**【交互设计】**星图「兼容账本」分栏：筛选器（类别/状态/季度）+ 每件展开看三关详情与证据（录屏哈希/日期/版本）；账本数据全量 JSON 开放（F128）。
**【数据与存储】**账本数据文件版本化（季度快照不可变，历史可溯）；构建机器自动汇总。
**【状态与异常】**样本升级后回退 → 状态降黄并归因（上游问题/VARIX 回归二分）；连续两季红的件 → 移入「攻坚名单」公示；样本淘汰（上游停维）→ 归档不删除。
**【验收判据】**首版账本 50/50 建档完成即本域总判据达成；季报如期发布两期。
**【开源复用】**样本全部选活跃开源项目（F130 登记）；账本框架自研（JSON 格式开放 F126）。

---

# 第 2 部分 · B 性能域（目标③）

> 域目标：让系统更快更顺，且所有快都可实测、可复现。

## B-1 提案表

| 编号 | 提案 | 说明 | 量级 |
| --- | --- | --- | --- |
| B1 | vxbench 实机首跑 + 基线账本 | 一切优化的前置——没有基线就没有「极大优化」 | 小 |
| B2 | 启动链优化 | 预读窗口对账 + 二次启动 8 秒判据实机化——用户每天感知最强的性能 | 中 |
| B3 | GPU 加速合成器（R3 摸底后决策） | Ivy Bridge 核显有基本 2D/3D 加速能力（Intel GPU 驱动路线），不一定要换硬件 | 大，需摸底 |
| B4 | U 盘 I/O 深化 | 写合并/预读/缓存策略在真 U 盘上调优——B707 族判据实机化 | 中 |

**铁律**：B1 不绿，B2-B4 不开工——没有基线的优化是凭感觉的优化。

## B-2 性能红线（体验域的性能约束，全域通用）

- 开机动画总时长 ≤8 秒——这是 B2 的验收线，也是第 3 部分开机动画的时长上限，实测几秒就几秒，进度条不骗人；
- **任何点击 100ms 内必有可见反馈**；动画稳定 **80fps**（Variable 2026-09-25 拍板：目标 80 帧，60fps 为最低达标线）、打断不跳变——B3 决定这条 KPI 的天花板（软渲染达标后再谈 GPU 加成）；
- **任意软件打开体验**：窗口出现前的等待全程有反馈（指针忙碌态 → 首帧占位窗 → 内容就位），不允许「点了没反应」——打开耗时随应用如实波动，但**帧率与反馈红线对任意软件一视同仁**（打开慢是诚实标注的事，卡死冻结是事故）；
- 丰盛不以卡顿为代价——P95 与帧率实测是验收硬项（见卷尾验收）。

## B-2·补 性能深度优化方向（Variable 2026-09-25 指定）

80fps 打开任意软件的支撑面按四层推进：① 合成器主循环削减空转与冗余合成（脏区策略深化）；② 应用冷启动优化——ELF/PE 装载预取指纹、BOT 预读窗口对账、二次启动减半；③ 渲染路径 GPU 化（B3 摸底后立项，Intel 核显 2D/3D 加速）；④ U 盘 I/O 深化（B4 写合并/预读调优）。每层先过 B1 基线，逐层实测帧率归因——**80fps 是整链的总验收，不是单层指标**。

---


## B-3 深化设计报告（F041-F070 报告式全展开）

> 以下 30 份报告为 B 域全部功能项的报告式展开——七段模板见卷首·丙；每项末尾【设计细节】段含参数级、像素级、判据级补深。性能域报告的共同前提：B1 基线账本先行（铁律），一切数字与监视器同源。

### G-B-01 F041 帧率账本 · 完整设计

**【功能定义】**合成器逐帧记账：每帧记录合成耗时、脏区面积、提交耗时、等待时间四项，写入性能账本（与监视器同源，B-2001 纪律）；账本按滚动窗口保留 60 秒逐帧 + 24 小时分钟聚合。
**【用户故事】**用户感觉拖动窗口「偶尔顿了一下」——打开任务管理器性能页，拖动时刻的逐帧图上一眼看到那 300ms 里有五帧超了 12.5ms 红线，归因列写着「输入风暴」——性能问题从玄学变成可回放的录像。
**【交互设计】**监视器性能页帧率图：纵轴帧耗时 0-25ms、横轴最近 60 秒、80fps 线（12.5ms）与 60fps 线（16.6ms）双参考线；悬停任一帧弹出该帧四项数据；「归因」列红黄标注。
**【数据与存储】**逐帧环形缓冲内存 64KB（60s×1000fps 上限）；分钟聚合落账本文件；账本格式对齐 vxbench（F061 回归门直接消费）。
**【状态与异常】**账本写入自身超预算（>0.1% CPU）→ 自动降采样（每 2 帧记 1 帧）并标注；合成器崩溃重启 → 账本标注断点（不伪造连续）。
**【验收判据】**账本打点自身开销 <0.1% CPU 实测；帧率图与实测录屏逐帧对得上（抽 10 帧核对）。
**【开源复用】**聚合口径参照 Travis/Timeline 类性能面板设计；实现自研（合成器既有打点面扩展）。
**【设计细节】**帧耗时测量点三处（输入处理/合成/提交）分段计时——归因的最小粒度是「哪一段慢」；四项数据每项独立阈值（合成 6ms/提交 3ms/输入 1.5ms/等待不做红线）；窗口拖动场景打专项标签（压力测试复现用）；80fps 红线用强调色实线、60fps 用灰虚线——目标线与底线视觉分级。

### G-B-02 F042 帧率归因器 · 完整设计

**【功能定义】**掉帧自动归因：帧超预算时按四类嫌疑（输入风暴/大脏区/IO 阻塞/调度抢占）采集证据并给出排名——「上一秒谁偷了帧」的自动侦探。
**【用户故事】**用户报告「复制大文件时窗口会卡」——开发者打开归因器看历史：卡顿时刻 IO 阻塞类占比 80%，证据链里挂着对应的存储打点——一条可复现的性能缺陷工单 5 分钟建好，以前要靠猜一晚上。
**【交互设计】**监视器「掉帧历史」页：列表（时间/掉帧数/头号归因/证据链接），点开单条看四类嫌疑占比条形图 + 每类的原始证据（哪次 IO/哪个窗口多大脏区）。
**【数据与存储】**归因事件落账本分钟聚合；证据引用账本原始条目（不复制，一处一事实）；保留 7 天。
**【状态与异常】**证据不足（多因并发）→ 如实「混合归因」并按占比排序（不硬编主因）；归因器自身异常 → 静默停用 + 诊断报备（不拖累合成器）。
**【验收判据】**构造四类掉帧样本各一，归器全部命中正确主因；误报率 <10%（正常拖动 100 秒样本零误报）。
**【开源复用】**归因思路参照 Chrome Tracing / ETW 的事件分类法；实现自研（账本数据面直接消费）。
**【设计细节】**四类判定阈值：输入事件 >200/秒 = 输入风暴；单帧脏区 >屏幕 60% = 大脏区；帧内 IO 等待 >2ms = IO 阻塞；帧被高优先线程抢占 >3 次 = 调度抢占；排名算法按「扣除该因素后帧耗时是否回线」的虚拟重放验证——归因要自证不是贴标签。

### G-B-03 F043 冷启动画像 · 完整设计

**【功能定义】**应用启动五段刻度：装载（PE/ELF 解析与映射）/重定位（导入绑定 F003）/首帧（首次合成提交）/可交互（输入事件响应）/稳定（5 秒无超预算帧）；每段耗时建档进星卡与账本。
**【用户故事】**「VS Code 打开慢」到底慢在哪——画像说：装载 1.2s/重定位 0.4s/首帧 6.1s/可交互 8.3s——一眼看出大头在首帧（渲染初始化），优化目标从「让 VS Code 变快」聚焦到「砍首帧前的那段」。
**【交互设计】**星卡详情页「启动画像」五段横向条形图（各段颜色区分、段宽按耗时比例）；对比视图：「首次 vs 二次」并排——预取（F044）的效果一眼可见。
**【数据与存储】**画像按 (应用版本, 启动序号) 记录；保留最近 20 次启动；聚合进星卡取「二次启动中位数」。
**【状态与异常】**应用启动中途崩溃 → 画像标记「中止于第 X 段」（本身就是极有价值的归因数据）；用户取消启动 → 同理记录。
**【验收判据】**五段边界与内核打点一一对应（无重叠无遗漏）；同应用 10 次启动画像方差 <15%（测量自身稳定）。
**【开源复用】**分段口径参照 Android Metrics 的 App Startup 分段；实现自研（装载器/合成器打点扩展）。
**【设计细节】**段边界定义写死在打点常量（一处一事实）：装载止于入口点前、重定位止于 IAT 解析完、首帧止于首次 Present、可交互止于首个窗口过程返回、稳定止于 5 秒观察窗结束；启动序号 1 = 冷（缓存清）、2 起算热；画像收集受隐私总闸（F036 同开关）控制。

### G-B-04 F044 预取指纹 v2 · 完整设计

**【功能定义】**按历史启动记录「哪些文件页真正被摸过」生成指纹，下次启动按指纹预读调序：热页先读、冷页不读；与 BOT 预读窗口对账（存储栈旋钮联动）。
**【用户故事】**VS Code 首次启动读 40MB、二次启动只预读 9MB 热页——用户体感「第二次快一倍」，U 盘也少磨——**预取的聪明在于只预取真的**。
**【交互设计】**无独立 UI；效果在星卡画像对比视图（F043）可见；诊断面板显示当前指纹命中率。
**【数据与存储】**指纹文件按应用存 `cache/prefetch/<hash>.pf`（页位图+访问序），上限 8MB/应用，LRU 驱逐；格式自定开放（F126）。
**【状态与异常】**程序更新（版本变化）→ 指纹失效重建；指纹损坏 → 弃用走无预取路径（正确但慢，不报错）；U 盘换机 → 指纹随 DATA 分区走（天然随身）。
**【验收判据】**「常用 50 件」二次启动均值 ≤ 首次 50%（与 F003/F043 联合对账）；指纹命中率 >70% 实测。
**【开源复用】**思路参照 Windows SuperFetch 与 eBPF prefetch 的页级追踪；实现自研（存储栈预读钩子）。
**【设计细节】**页访问记录在软缺页中断点（内核零堆：定长环形位图）；预读 IO 按指纹序批量 8 页一组（对齐 BOT 顺序读优势）；预读优先级永远低于前台 IO（F057 分级）；指纹生成在应用退出后 30 秒后台完成（不抢退出体验）。

### G-B-05 F045 页缓存水位策略 · 完整设计

**【功能定义】**4GB 内存边界下的页缓存治理：全局水位线三档（高 3.2GB/中 2.4GB/低 1.6GB），到线按 LRU 回收文件页；脏页占比上限 20%（超限强制冲刷，断电窗口受控）。
**【用户故事】**用户连续打开几十张 4K 图后系统依然流畅——水位线在后台悄悄回收了最早的图页；他不会知道有这条策略，只会觉得「这系统不卡也不崩」。
**【交互设计】**监视器内存页三区图（应用/缓存/空闲）实时面积图；水位线档位在诊断面板只读展示（自动策略不暴露旋钮给普通用户——防乱调）。
**【数据与存储】**无持久化（纯运行时策略）；策略决策日志入诊断快照（F174）。
**【状态与异常】**应用内存配额挤压缓存 → 缓存先让（交互优先）；回收导致二次读盘激增 → 记录「回收后悔」指标调参依据；内存耗尽前 200MB 警戒 → 全局冲刷 + 通知后台应用释放（F195 联动）。
**【验收判据】**断电百次中脏页丢失窗口 ≤ 水位规则承诺值（B-703 数据交叉验证）；压测（连续开关大图）无 OOM。
**【开源复用】**水位思路参照 Linux min_free_kbytes 与 dirty_ratio；实现自研（伙伴分配器面扩展）。
**【设计细节】**LRU 按「文件页/匿名页」分列（文件页可弃可重读优先弃）；脏页 20% 上限按分区分别计（SHARED 只读卷不计）；水位判定每 500ms 一次（不逐页检查的功耗账）；三档切换条件文档化（>1GB 空闲=高档……全表在旋钮清单）。

### G-B-06 F046 写合并窗口自适应 · 完整设计

**【功能定义】**写合并窗口按负载动态伸缩：空闲 1s / 正常 5s / 重载 8s 三档自动切换；fsync 硬承诺（应用显式要求时立即冲刷）永不因窗口调整而拖延——自适应只调「惰性写」部分。
**【用户故事】**用户写完文档立刻拔 U 盘走人——空闲档 1 秒内已落盘，数据无损；批量解压时切重载档 8 秒合并，写入量减少 40%，U 盘安静寿命长。
**【交互诊断】**监视器存储页显示当前档位与最近 60 秒实际写入量曲线；档位切换事件入诊断快照。
**【数据与存储】**档位判定依据：每秒写请求数 + 脏页占比（F045 数据源共享）；无持久化配置。
**【状态与异常】**应用 fsync 请求在重载档也立即执行（硬承诺优先级最高）；档位抖动（1s 内反复切换）→ 迟滞保护（切档后至少驻留 5s）；断电一致性判据在任意档位都成立（B-703 分档重跑）。
**【验收判据】**断电百次按三档各跑一遍全绿；写入合并率（合并写/总写）重载档 >40% 实测；fsync 延迟 P99 <10ms 不受档位影响。
**【开源复用】**自适应思路参照 Linux dirty_expire_centisecs 动态化讨论；实现走存储栈旋钮（MD2 附录 K）。
**【设计细节】**三档阈值：请求 <10/s 且脏页 <5% = 空闲；>200/s 或脏页 >15% = 重载；迟滞驻留防抖；档位切换写一行审计；窗口上限 8s 的依据=断电丢失窗口承诺（用户可理解的边界）；B-702 判据在自适应模式下重测重录。

### G-B-07 F047 调度器延迟预算深化 · 完整设计

**【功能定义】**交互线程 p99 唤醒延迟 2000μs 总预算拆为逐类子预算：输入 500μs / 合成 800μs / 音频 300μs / 普通 400μs；每类独立记账，超标逐类归因——预算不是平均值是分布。
**【用户故事】**「系统偶尔卡一下」的追责有了精确到类别的答案：音频类从未超标、合成类每周三次超标且都指向同一场景——性能债有了具体的债务人。
**【交互设计】**监视器调度页四类泳道图（最近 60 秒各类 p99 曲线 + 超标点红标）；点击超标点看当时线程与优先级快照。
**【数据与存储】**延迟采样每类环形 10,000 条（内存 ~160KB）；聚合入账本；调度器打点零堆（定长结构）。
**【状态与异常】**某类长期零样本 → 观察窗说明（不代表无风险）；超预算且归因到自身策略（如优先级饥饿）→ 调度器自修正（优先级老化）并记录修正事件。
**【验收判据】**四类 p99 各自达标（2000μs 总预算内）；压力混载（音频+拖动+构建并发）下输入类 p99 仍达标——交互优先不是口号是曲线。
**【开源复用】**分级思路参照 Linux SCHED_DEADLINE 预算模型；实现自研（既有 rr 调度器扩展）。
**【设计细节】**优先级映射：输入=最高带抢占、音频=高带 deadline、合成=高、普通=rr 老化；energy=balanced 策略（既有实测）在大核可用时保持；预算超标定义 = p99 而非 max（尾部管理）；预算表编译期常量（变更走 ADR）。

### G-B-08 F048 CPU 频率联动 · 完整设计

**【功能定义】**负载感知 P-state 升降档：交互突发秒升最高档、持续负载 30s 后按类型选档（构建=高频/下载=低频）、空闲 5s 降最低档；Y7000 实机频率表驱动（ACPI _PSS 读取，graceful 不可读则固定档）。
**【用户故事】**用户按一下搜索框——CPU 瞬间拉满 0.5 秒完成索引查询又安静降回——「该快的时候快，该静的时候静」，风扇和续航都感知不到中间态。
**【交互设计】**快速设置面板（F076）性能三档（F069）与本策略叠加：手动档位 = 给自动策略设边界（静音档封顶低频）；监视器显示当前频率与档位切换时间线。
**【数据与存储】**频率表启动时从 ACPI 读入（一次）；策略决策日志入诊断快照。
**【状态与异常】**_PSS 不可读（部分固件）→ 固定中档 + 诊断标注（graceful 已实测同纪律）；频率切换失败 → 重试一次后锁定安全档；温度超限（F197 联动）→ 强制降档优先于本策略。
**【验收判据】**交互突发响应（点按到满频）<50ms；续航对比：自动策略 vs 固定高频，视频播放场景续航提升 >15% 实测。
**【开源复用】**策略参照 Linux intel_pstate powersave governor 思路；实现自研（ACPI 面既有）。
**【设计细节】**升档触发：任何输入事件或音频 deadline 线程就绪；降档迟滞 5s（防抖）；30s 类型判定按线程名签名（构建工具名单内=计算型）；频率切换自身耗时 <10ms（MSR 写）；全策略参数进旋钮清单（无隐藏魔法数）。

### G-B-09 F049 空转清零工程 · 完整设计

**【功能定义】**合成器空闲态零唤醒：无脏区、无动画、无光标移动时主循环深度休眠（等事件而非轮询）；B-501「空闲 5%」判据升级为「空闲 <0.5%，目标 0」。
**【用户故事】**用户开着 VARIX 挂机下载——CPU 占用表上合成器一栏几乎是零：桌面没有动画时它真的睡了——**安静是一种性能美德**。
**【交互设计】**无直接 UI；监视器 CPU 页合成器曲线在空闲段的平坦度就是本项的活验收。
**【数据与存储】**空闲判定与唤醒计数入账本（唤醒原因分类：输入/定时/脏区）。
**【状态与异常】**应用挂常驻动画（进度条）→ 合成器按需唤醒（不算空转违规）；唤醒风暴（每秒 >60 次无意义唤醒）→ 归因报告给 F042。
**【验收判据】**纯桌面静置 60 秒：合成器 CPU <0.5%、内核唤醒次数 <10 次；两数字同录在案。
**【开源复用】**思路参照 Wayland 合成器的 idle 事件驱动实践；实现自研（既有主循环改造）。
**【设计细节】**事件源统一 fence（输入/VSync 定时器仅在动画注册时 armed/IPC）；光标闪烁类周期事件只在相关窗口可见时 armed；VSync 空闲时关闭定时器源；深睡用 MONITOR/MWAIT 指令（实测 Y7000 支持性记档）。

### G-B-10 F050 中断合并 · 完整设计

**【功能定义】**输入与网络中断按突发窗口合并：2000ns 预算内批量入队处理；input-probe 的 flood_dropped（实测 70）转为「合并处理零丢弃」——把丢弃变成延迟。
**【用户故事】**高速滚动长网页每秒几百个滚轮事件——VARIX 不是丢掉一些（跳变），而是合并成平滑的位置更新（跟手不跳）——用户说不出原理，只觉得「滑起来真顺」。
**【交互设计】**无直接 UI；效果 = 滚动/拖动流畅度（F041 账本中输入风暴类掉帧归零）。
**【数据与存储】**合并队列定长环形（零堆）；合并统计（每秒合并率/最大批）入账本。
**【状态与异常】**合并导致延迟 >8ms（体感阈值）→ 动态收缩窗口（流畅优先于省电）；中断风暴（恶意/故障）→ 熔断降频 + 诊断告警。
**【验收判据】**flood 测试（flood-publishes 70 级）零丢弃；滚动场景输入类 p99 延迟 ≤8ms；两数字对账 F041 掉帧归零。
**【开源复用】**合并思路参照 Linux NAPI 与 input coalescing 实践；实现自研（input-probe 既有面扩展）。
**【设计细节】**合并键：同设备同类型事件才合并（键盘不与滚轮混批）；批处理预算 2000ns 超则留队下轮（预算硬，不超卖）；鼠标位置类只保最新（绝对量事件可覆盖）、按键类全保（离散事件不可丢）——「可覆盖与不可丢」的二分是合并的正确性根基。

### G-B-11 F051 大页策略 · 完整设计

**【功能定义】**4MB 大页用于三类静态区：内核代码段、字形图集、合成器帧缓冲池；TLB miss 实测对比入账；R3 GPU 摸底同步评估核显侧大页可行性。
**【用户故事】**长文档连续滚动一小时无 TLB 抖动卡顿——大页让地址翻译的「路牌」少了 512 倍，滚动只烧 CPU 不烧 TLB。
**【交互设计】**无直接 UI；诊断面板内存页显示大页占用与命中率。
**【数据与存储】**大页池启动时预留（物理连续内存分配一次锁定）。
**【状态与异常】**物理连续内存不足 → 部分降级 4KB（按区独立降级并标注）；大页池碎片（长期运行）→ 只增不减策略防碎片。
**【验收判据】**字形图集场景 TLB miss 率实测下降 >50%；大页池启动预留成功率 >95%。
**【开源复用】**策略参照 Linux hugetlbfs 预留池模式；实现自研（WP-104 分页面扩展）。
**【设计细节】**三类区尺寸预算：内核 4MB/图集 8MB/帧缓冲按分辨率（4K 双缓冲 32MB）；预留失败的降级顺序：帧缓冲先降（影响最小）内核最后；TLB miss 测量用性能计数器（Ivy Bridge 支持 mem_load_retired 类事件评估）。

### G-B-12 F052 堆碎片治理 · 完整设计

**【功能定义】**内核堆（56KB 分块实测既有）改尺寸分级池：8/16/32/64/128/256B 六档 + 大块直通；碎片率（空闲块/总块）指标入账本；长期运行碎片率 <15% 达标。
**【用户故事】**系统连续运行一周后内核内存分配依然秒级响应——分级池让「住过很多人的旅馆」依然整洁，不出现「明明有空房却开不出去」的碎片窘境。
**【交互设计】**诊断面板内核堆健康页：六档占用条形图 + 碎片率曲线（7 天）。
**【数据与存储】**池结构定长（零堆纪律自洽）；指标每分钟采样入账本聚合。
**【状态与异常】**某档耗尽 → 相邻档切分（带切分开销标注）；碎片率 >25% → 告警 + 归因（哪类分配模式导致）；分配失败路径全测（panic 演练 B-2903 场景含）。
**【验收判据】**7 天烤机碎片率 <15%；六档分配延迟 P99 <1μs；零堆纪律 grep 自证（无动态分配进内核）。
**【开源复用】**分级池参照 jemalloc size-class 思想（内核零堆约束下的极简版）；实现自研。
**【设计细节】**档位边界按内核实际分配谱定（先采集 1 周分配尺寸直方图再定档——数据先行）；大块直通阈值 512B；每档空闲链用位图（O(1) 分配）；碎片率 = 空闲块数/总块数的分档加权。

### G-B-13 F053 启动并行度 · 完整设计

**【功能定义】**init 里程碑依赖图显式化：ACPI/PCI 枚举/USB/存储四链并行推进，串行等待点逐个消除；开机 8 秒线（B2）的分解攻坚图挂账本——每 100ms 都有名有姓。
**【用户故事】**用户按电源到看到桌面 7.4 秒——账本里每一段（固件 4s 不可改/Limine 0.3s/内核四链并行 2.1s/动画 1s）各自认领自己的时间——**快是设计出来的，不是许愿出来的**。
**【交互设计】**诊断中心「启动时间线」页：横向甘特图逐链展示（每链每阶段起止）；与 8 秒目标差值标红。
**【数据与存储】**时间线数据来自既有 11 打点（MD2 篇 1.6）扩展为依赖图节点；每次启动记录在账本。
**【状态与异常】**某链失败 → 后续依赖该链的阶段跳过并标注（存储链失败仍可进安全模式 F193）；并行竞争（两链抢同一资源）→ 锁等待归因入时间线。
**【验收判据】**四链并行后内核段总耗时 ≤ 串行版 60%；8 秒线分解后每段预算在甘特图可见且实测偏差 <10%。
**【开源复用】**依赖图思路参照 systemd 启动关键路径分析；实现自研（init 既有打点面）。
**【设计细节】**四链依赖矩阵文档化（谁真依赖谁——多数「依赖」是历史串行的惯性）；USB 链的 xHC 枚举与存储链解耦（键盘慢不让盘慢）；动画启动点提前到内核段 80% 处（画面先动起来，感知时间再减 0.5s）；每链超时阈值独立（USB 3s 宽限/存储 1s 严格）。

### G-B-14 F054 图像解码 SIMD · 完整设计

**【功能定义】**PPM/PNG/JPEG 三解码器 SIMD 化：SSE4.2 基线全机可用（实测面），AVX2 运行时探测启用；解码耗时入 vxbench 图像类基准。
**【用户故事】**4K 壁纸换上的瞬间就已经渲染完成（≤150ms）——用户感受是「即换即见」，背后是 SIMD 把逐像素循环压进了 SIMD 通道。
**【交互设计】**无直接 UI；诊断面板显示当前启用的指令集档（SSE4.2/AVX2）。
**【数据与存储】**解码器无状态（流式）；vxbench 新增图像子类基准（4K PNG/JPEG 解码各一条）。
**【状态与异常】**损坏文件 → 解码错误如实（不产出半图）；超大图（>64MP）→ 流式分块解码（内存峰值受控）；AVX2 探测失败 → 无缝回退 SSE4.2（同结果）。
**【验收判据】**4K PNG 解码 ≤150ms、JPEG ≤120ms（实测线）；SIMD 与标量结果逐像素一致（正确性优先于速度）。
**【开源复用】**评估 libpng-turbo/libjpeg-turbo 的 SIMD 内核（许可证 zlib/IJG 宽松，F130 登记）；接口自研封装。
**【设计细节】**IDCT/反滤波/行过滤三个热点先行（占解码 80% 耗时）；对齐分配走大页池（F051）尾部；基准样本固定三张（4K 照片/截图/插画）入 vxbench 资产；正确性对拍 = SIMD 输出与标量参考逐位比较（CI 夜跑）。

### G-B-15 F055 字形光栅缓存 · 完整设计

**【功能定义】**字形图集 LRU 热度驻留：热字形（高频字符）常驻、冷字形驱逐腾位；图集分页管理（每页 512×512px）；滚动长文档光栅耗时实测不抖动。
**【用户故事】**用户在 500 页文档里连续滚动 10 分钟——每屏的文字渲染都和第一屏一样快（热字形早已在图集里），没有「越滚越卡」的缓存失效率曲线。
**【交互设计】**无直接 UI；F041 帧率账本在滚动场景的平稳曲线是活验收；诊断面板显示图集命中率与占用。
**【数据与存储】**图集显存（或锁页内存）配额 16MB（4K 管线）；持久化不做（启动重建快，F053 并行）。
**【状态与异常】**图集满 → LRU 驱逐（驱逐粒度=页，避免碎片）；命中率 <80% → 自动扩一页（上限 32MB）；字形光栅自身耗时 >2ms → 后台线程光栅化（前台用旧版占位，到位后原子换）。
**【验收判据】**中文长文档滚动 10 分钟帧耗时方差 <20%；图集命中率 >90%（常用字场景）。
**【开源复用】**LRU 策略参照字体光栅化文献（FreeType cache 子系统思路）；实现自研（C-7 字形管线扩展）。
**【设计细节】**常用字集预热：启动后台预光栅常用 3500 汉字 + ASCII（用户打字前图集已热）；驱逐保护：当前帧用到的字形禁止驱逐（双缓冲页交换）；灰度 AA 与 hinting 参数冻结进资产版本（换字体版本=图集全重建，显式日志）。

### G-B-16 F056 合成器脏区深化 · 完整设计

**【功能定义】**光标独立层（光标移动不触发窗口重合成）；弹窗/菜单动画只合成自身矩形；全屏重绘从词典删除——所有动画都在「最小矩形集」内发生。
**【用户故事】**用户在文档里打字、光标闪烁、任务栏时钟走秒——三件事同时发生，合成器每帧只碰三小块矩形，CPU 占用依然近乎零——丰盛的界面与安静的系统并存。
**【交互设计】**无直接 UI；效果验收 = F041 账本打字场景的脏区面积曲线（应维持在屏幕 5% 以下）。
**【数据与存储】**脏区跟踪结构定长（每窗口脏矩形 8 个上限，溢出合并为整窗）。
**【状态与异常】**脏区爆炸（程序疯狂自刷）→ 合并限频 30fps + F042 归因；层重叠动画 → 层间脏区求交裁剪；光标层与内容层 z 序冲突 → 光标永远最上（独立层语义保证）。
**【验收判据】**打字场景脏区面积 P95 <5% 屏；光标移动 60fps 恒定且零内容重绘（账本验证）；弹窗动画帧内合成矩形 = 弹窗矩形 + 阴影环带。
**【开源复用】**脏区策略参照 Wayland damage tracking 与游戏引擎 dirty rect 文献；实现自研（既有四步主循环深化）。
**【设计细节】**光标层实现为硬件兼容路径（Ivy Bridge cursor plane 评估）或独立覆盖面（软路径兜底）；阴影按 16px 环带预渲染（不逐帧重算）；动画矩形集在动画注册时声明（合成器预先知道要碰哪里）；脏区合并算法 = 区间树相交合并（O(n log n)）。

### G-B-17 F057 IO 调度分级 · 完整设计

**【功能定义】**IO 队列三级：前台交互（菜单打开/搜索/缩略图）> 后台任务（下载/索引/构建）> 批量（备份/日志轮转）；调度按截止期+权重的混合策略；io-pending 指标（实测 9/9 pass 既有）细化为分级队列视图。
**【用户故事】**后台正在下载 2GB 文件的同时用户打开文件管理器——目录秒开（前台插队），下载匀速继续（后台饿不死）——互不拖累的感觉就是分级在干活。
**【交互设计】**监视器 IO 页三队列实时深度条 + 各队列吞吐曲线；下载类任务自动降级的提示出现在通知中心（透明可查）。
**【数据与存储】**队列结构定长环形；分级事件审计（谁被降级/为什么）入诊断快照。
**【状态与异常】**前台持续满载 60s → 后台完全暂停（防饿死反向保护：前台让出窗口计时）；批量任务超 30 分钟 → 分段让路（每 5 分钟让 1s）；配额（F195）与分级叠加生效。
**【验收判据】**「下载中打开目录」场景目录延迟 ≤ 无下载时的 1.2 倍；后台任务零饥饿（24h 混载测试全部完成）。
**【开源复用】**分级参照 Linux CFQ/BFQ 的 class 思想（极简三 class 版）；实现自研（存储栈既有队列扩展）。
**【设计细节】**class 判定：按发起进程标签（交互面进程=前台）+ 请求特征（读<64KB=交互倾向）；截止期参数：前台 50ms/后台 2s/批量无截止期；分级是调度顺序不改变 fsync 硬承诺（B-7xx 优先于一切）。

### G-B-18 F058 内存压缩前瞻 · 完整设计

**【功能定义】**冷页压缩池评估项：真实工作集超 3.2GB（水位高档 F045 持续一周触发）才立项启用；LZ4 级算法评估；先测后做——不提前上压缩。
**【用户故事】**（立项后）重度用户同时开大程序群——4GB 边界被压缩池悄悄扩到等效 5GB，慢一点但没崩没杀进程——**压缩是边界扩展器，不是常规武器**。
**【交互设计】**触发立项前：设置中心「系统-内存」页显示工作集趋势图（让用户看见「为什么我们建议关掉一个程序」）。
**【数据与存储】**工作集监测数据入账本；压缩池启用后结构：压缩页槽 4KB 框（定长零堆）。
**【状态与异常】**解压延迟超标（P99 >200μs）→ 池缩小；压缩比 <1.5 的页 → 不压缩（白费 CPU）；池满 → 回退 OOM 流程（如实告知用户）。
**【验收判据】**立项判据本身可验证：工作集数据采集脚本先跑一周出报告；启用后（若立项）解压延迟与压缩比双达标。
**【开源复用】**算法评估 zstd/LZ4（zlib 许可核验 F130）；思路参照 zswap/Windows MemCompression 文档。
**【设计细节】**「冷」定义：64 秒未触碰；压缩在空闲 CPU 时段批量做（F048 低频档窗口）；池大小动态 0-512MB；优先压缩文件备份页（重读成本高者）；本项整体是 R3 级评估件——立项文档先行，代码后行（纪律同 S406）。

### G-B-19 F059 网络小包优化 · 完整设计

**【功能定义】**smoltcp 批量收包路径：中断聚合（F050 同窗口）+ 批量轮询处理；每包处理耗时入账；SSH 交互打字延迟 ≤30ms 为体验线。
**【用户故事】**开发者在 VARIX 终端里 SSH 到远程服务器写代码——每个击键回显干脆利落，没有「小包处理不过来」的粘滞感——网络栈对小包的尊重就是交互的尊重。
**【交互设计】**无直接 UI；诊断面板网络页：包处理耗时分布 + 批处理率。
**【数据与存储】**统计入账本分钟聚合；无持久化。
**【状态与异常】**包风暴（>10k pps）→ 熔断限流 + 告警（保交互流优先）；批量路径 bug 兜底 → 单包路径热切换（运行时可切，诊断用）。
**【验收判据】**SSH 打字延迟 P99 ≤30ms 实测；10k pps 小包风暴下交互流延迟劣化 <20%。
**【开源复用】**批处理参照 smoltcp 官方示例与 NAPI 模式；实现自研（F023 面的性能层）。
**【设计细节】**批量窗口 = F050 中断合并窗口（一个窗口两处受益）；轮询 budget 每轮 64 包（超则让 CPU）；TCP ACK 延迟确认 40ms（交互流 5ms——分流参数）；小包定义 ≤512B；延迟测量打点在协议栈入口出口两端。

### G-B-20 F060 电量账本 · 完整设计

**【功能定义】**按应用能耗画像：CPU 时间 × 频率档 + 唤醒次数 + IO 量折算为相对能耗单位；设置中心电池页显示「今天谁在耗电」排行。
**【用户故事】**用户发现笔记本掉电快——电池页排行第一名是某个后台同步工具（CPU 唤醒每秒 20 次）——关掉它，续航回来了。**看不懂的耗电才是焦虑，看得见的耗电是选择**。
**【交互设计】**设置中心「系统-电源和电池」页：应用排行条形图（相对单位，不伪精确瓦特数——诚实原则）+ 每应用明细（唤醒/IO/CPU 三分项）；时间范围切换（1h/今天/7 天）。
**【数据与存储】**画像每分钟聚合入账本；保留 30 天；排行计算在设置页打开时现算（账本数据轻量）。
**【状态与异常】**电量计不可读（部分机器）→ 页面显示「耗电相对值」并说明口径（诚实降级）；应用已卸载 → 画像归档不删除。
**【验收判据】**折算值与实测电量计曲线相关性 >0.8（同窗口对比）；排行前三与人工判断一致（抽查场景）。
**【开源复用】**折算模型参照 Android Battery Historian 权重思路（简化版）；实现自研（账本数据面消费）。
**【设计细节】**权重初值：CPU 每毫秒核秒=1.0/唤醒每次=0.05/IO 每 MB=0.02（参数进旋钮清单）；唤醒合并（F050）效果在画像里自动体现；屏幕亮度不计入应用（归系统项单列）；数据受隐私总闸（F036 开关）管理。

### G-B-21 F061 基准回归门 · 完整设计

**【功能定义】**vxbench 六类基准接 CI 夜跑（QEMU 替身）：装载/合成/存储/网络/调度/图像每类 2-4 条基准，基线倒退 >5% 即门禁红——性能资产像代码一样有回归保护。
**【用户故事】**某次提交不小心让合成器多了一次全屏拷贝——第二晚 CI 红灯亮起标出「合成基准 -18%」，修复在扩散前完成——**没有回归门的优化会悄悄漏光**。
**【交互设计】**CI 报告页：六类基准趋势图（90 天）+ 本次 vs 基线差值表；红灯必附 top3 嫌疑提交列表。
**【数据与存储】**基线存档不可变（换基线=改分数，ADR 纪律）；历史结果全保留。
**【状态与异常】**QEMU 噪声（虚拟化抖动）→ 三跑取中位数 + 噪声带标注；机器差异 → 基线绑定 runner 标签；偶发红 → 连续两晚红才告警（防狼来了）。
**【验收判据】**回归门捕获率：人为注入 5 处性能回退全部检出；误报率 <5%（30 天零噪声误报）。
**【开源复用】**门禁思路参照性能 CI 通用实践（coz/benchkit 类）；vxbench 本体自研（F053/F054 基准共用）。
**【设计细节】**夜跑窗口固定且独占（无其他任务污染）；基准间隔离：每基准独立 QEMU 实例；5% 阈值分基准可调（存储类噪声大放宽到 8%）；趋势图异常点可标注（固件更新/借力件升级等外因）。

### G-B-22 F062 性能自检报告 · 完整设计

**【功能定义】**开机 10 分钟静默采样生成健康快照：帧率 P95/IO 延迟 P99/调度超标次数/唤醒统计四项，超阈值自动转诊断中心工单——性能问题在用户感知之前先被系统自己看见。
**【用户故事】**用户还没抱怨卡，通知中心已经有一条：「今晨系统启动后有一段加载导致 12 帧超预算，已记录详情，无需操作」——用户感觉「这个系统自己有体检」。
**【交互设计】**通知合批：同日多条快照异常合并一条；诊断中心「健康快照」页逐日列表，异常项可展开看证据链（F042 归因数据直接引用）。
**【数据与存储】**快照每日一份保留 30 天；工单引用快照（不复制数据）。
**【状态与异常】**开机后用户立即高强度使用 → 采样避开（首个空闲 10 分钟窗口才采）；采样自身影响（<0.1% CPU 预算）超限 → 降频采样。
**【验收判据】**阈值设定经实机标定（正常机器 30 天零误报）；异常检出率：注入 3 类慢故障全部捕获。
**【开源复用】**无外部依赖；阈值表进旋钮清单。
**【设计细节】**四项阈值初值：帧 P95 >17ms/IO P99 >50ms/调度超标 >5 次/唤醒 >200 次/分钟——每项可关（用户不想要体检就安静）；快照生成在 F049 空转窗口内（零感知）；工单不弹窗只进中心（不打扰纪律）。

### G-B-23 F063 触控板手势前瞻 · 完整设计

**【功能定义】**Y7000 触控板多指手势评估项：HID 报文采集（双指滚动/三指切换/捏合缩放的原始数据面先行），定级后决定实现批次——数据面不锁死，承诺面按实测定级。
**【用户故事】**（若定级通过）用户双指在触控板上滑——页面平滑滚动（惯性曲线 200ms ease-out）；三指上滑进任务视图（F081）——笔记本用户的肌肉记忆完整迁移。
**【交互设计】**定级通过后：设置中心「蓝牙和其他设备-触控板」页：手势开关集 + 灵敏度滑杆 + 手势演示动画；不通过则页面显示「本机触控板评估中」诚实标注。
**【数据与存储】**采集数据仅本机诊断用（不上传）；手势配置存配置层。
**【状态与异常】**报文解析失败 → 降级为单点触摸（基础可用）；手势与输入法组合冲突 → 手势让位（打字优先）。
**【验收判据】**评估报告产出（报文格式/多指识别可行性/延迟实测三节）即本项达标——评估件以报告为交付。
**【开源复用】**手势识别思路参照 libinput gesture 状态机；采集自研（input-probe 扩展）。
**【设计细节】**采集维度：触点数/坐标轨迹/压力（若报文有）/时间戳；惯性滚动模型评估（速度衰减指数 0.95 档）；手势判定窗口 80ms；定级标准：延迟 <40ms 且误触率 <1% 才可承诺。

### G-B-24 F064 音频低延迟链 · 完整设计

**【功能定义】**混音器单流端到端延迟 ≤20ms：应用写缓冲 → 混音 → HDA CORB/RIRB → 出声，全链分段计时；音乐类应用可用性门槛。
**【用户故事】**音乐人用户接 MIDI 键盘（前瞻）或用节拍器应用——按下到出声的延迟低于人耳感知的「不同步」阈值——VARIX 上做音乐不是将就。
**【交互设计】**诊断面板音频页：全链延迟分布图 + 当前缓冲水位；混音器（F026）面板显示每流延迟标注。
**【数据与存储】**延迟采样入账本；无持久化配置（参数进旋钮清单）。
**【状态与异常】**延迟超标归因分四段（应用供数/混音/传输/DAC）逐段显示——「慢在哪一段」不猜；缓冲欠载（underrun）→ F026 既有静音策略 + 计数告警。
**【验收判据】**单流 20ms 线实测达标（正弦测试信号示波法或环回法）；双流混音延迟增量 <2ms。
**【开源复用】**分段计时参照 Linux PW 低延迟实践文档；实现自研（B-806/807 既有链深化）。
**【设计细节】**缓冲三档：交互 5ms/默认 10ms/省电 20ms（按流声明选）；混音器 tick 与 HDA DMA 对齐（不做两次缓冲拷贝）；延迟测量工具内置（环回自测一键跑）；deadline 线程类（F047）音频 300μs 预算保障供数。

### G-B-25 F065 唤醒源治理 · 完整设计

**【功能定义】**睡眠态唤醒源白名单：默认仅电源键/盖开合/USB 键鼠；每分钟唤醒次数 ≤1 达标；唤醒原因全记录。
**【用户故事】**用户合盖放包里两小时，拿出来还有 95% 电——没有某个后台服务每 30 秒偷偷醒来——**待机耗电从查不到变成有账可查**。
**【交互设计】**设置中心「电源和电池-唤醒源」页：白名单管理 + 最近 10 次唤醒原因列表（时间/来源/耗时）。
**【数据与存储】**唤醒事件日志入账本（保留 30 天）；白名单存配置层。
**【状态与异常】**未知唤醒源（固件行为）→ 记录「未知」并建议排查；唤醒后 60 秒无操作 → 自动回睡（可关）；闹钟类（F100）按用户预约显式唤醒。
**【验收判据】**合盖 2 小时实测唤醒 ≤2 次、掉电 ≤2%；唤醒原因记录 100% 可解释。
**【开源复用】**白名单思路参照 Windows Modern Standby 与 Linux wakeup sources；实现自研（电源面 F WP-106 扩展）。
**【设计细节】**白名单分级：硬源（电源键不可移）/软源（USB 设备可逐个关）；唤醒抖动保护：醒后 5s 内再睡视为抖动计数（>3 次/小时告警）；RTC 唤醒仅限用户闹钟；包内唤醒（WoL）默认关。

### G-B-26 F066 文件系统日志策略 · 完整设计

**【功能定义】**ext4 journal 模式选型与参数固化：data=ordered 基线；commit 间隔与写合并窗口（F046）联动；断电百次与性能双达标为选型裁决。
**【用户故事】**用户经历一次突然断电——来电开机后文件系统自动恢复，最近 1 秒内的工作丢了（诚实承诺范围）、其余完好——**日志策略的使命是把意外变成可预期**。
**【交互设计】**无直接 UI（策略是工程裁决）；诊断面板存储页显示 journal 模式与最近恢复记录。
**【数据与存储】**journal 参数进旋钮清单；恢复事件入诊断日志。
**【状态与异常】**journal 恢复失败（极端损坏）→ fsck 路径 + 恢复环境（F198）兜底；journal 空间满 → 强制冲刷（延迟尖峰如实标注）。
**【验收判据】**断电百次（三档窗口 F046 各跑）全部自动恢复零 fsck；顺序写吞吐在 ordered 模式损失 <10%。
**【开源复用】**ext4 与 Google ext4 crate（F130 既有借力件）参数化使用；裁决文档参照 ext4 官方文档。
**【设计细节】**commit 间隔 = F046 窗口（一处一事实，不双参数）；journal 大小 128MB（4GB 内存机型均衡值，旋钮）；元数据 checksum 开启（v1 校验）；恢复后自动触发一次 B-703 子集复测（自证健康）。

### G-B-27 F067 启动 IO 冷热分离 · 完整设计

**【功能定义】**镜像分区布局分离只读区（内核/字体/资产）与可写区：只读区零 journal、零写放大；BOT 顺序读优势最大化（预读全开）。
**【用户故事】**系统更新后首次开机依然快——只读区的内容稳定（不变的文件不重写），U 盘对它们的读取永远走最优顺序路径。
**【交互设计】**无直接 UI；诊断中心启动时间线（F053）中「只读区加载」段耗时应显著优于可写段。
**【数据与存储】**分区布局设计文档化（偏移/对齐/大小三表）；只读区哈希清单用于完整性自查（F191 联动）。
**【状态与异常】**只读区哈希不符 → 启动链自查（F191）拦截进恢复环境；可写区满 → 提前预警（<500MB）。
**【验收判据】**只读区随机读延迟 P99 <2ms（U 盘实测）；启动段 IO 走并行（F053）后只读段耗时占比 <25%。
**【开源复用】**布局思路参照只读 rootfs（squashfs 类）设计经验；实现自研（镜像构建工具链）。
**【设计细节】**只读区对齐 4MB（大页 F051 联动）；资产按访问序物理排布（启动甘特图顺序=盘上顺序）；版本更新 = 只读区整体换槽（双槽 F190 语义）；可写区 journal 只为可写数据服务（职责不混）。

### G-B-28 F068 渲染资产按需装载 · 完整设计

**【功能定义】**壁纸/图标集/音效按当前主题（E1）惰性装载：未选中的主题资产不驻内存；内存占用差值入账本。
**【用户故事】**系统装了 10 套官方主题——用户只用一套时，内存里只有那套的资产（省下 9 套的空间给应用）；切换主题时旧资产卸载新资产流入（300ms 交叉淡入完成换装）。
**【交互设计】**无直接 UI；E1 切换的流畅度即体验面；诊断面板资产页显示当前驻留清单。
**【数据与存储】**资产文件按主题目录组织（4K 管线 C-7 产出）；驻留清单运行时维护。
**【状态与异常】**资产文件损坏 → 回退默认主题 + 诊断报备；切换时新资产解码失败 → 保持旧主题（不白屏）。
**【验收判据】**单主题驻留内存 ≤80MB（4K 全套实测）；主题切换全程无白屏闪烁（录屏帧检）。
**【开源复用】**无外部依赖；装载器复用图像解码面（F054）。
**【设计细节】**引用计数管理资产生命周期（最后一个引用卸载）；解码在后缓冲完成后原子换入（无中间态上屏）；音效文件小（<200KB 全量驻留不按需）；图标集按需粒度=单图标（常用 100 个预驻）。

### G-B-29 F069 性能模式三档 · 完整设计

**【功能定义】**静音/均衡/性能三档一键切换：联动 CPU 频率边界（F048）+ 写合并窗口（F046）+ 音频缓冲档（F064）+ 温度阈值；档位定义全走旋钮清单。
**【用户故事】**会议室里一键切静音档——风扇安静、频率封顶、续航优先；回家插电切性能档——全速释放——**一台机器两种性格，切换只要一秒**。
**【交互设计】**快速设置面板（F076）三档开关（当前档高亮）；切换 toast 显示生效项清单；每档的参数摘要悬浮可见。
**【数据与存储】**当前档持久化（重启保持）；切换事件入账本。
**【状态与异常】**温度强制降档（F197）覆盖手动性能档（安全优先）+ 通知说明；档位参数缺失（旋钮表版本不齐）→ 拒绝切换并诊断报备。
**【验收判据】**三档参数差异实测可辨（频率/写入量/续航三指标分档明显）；切换全程无音频爆音、无 UI 卡顿。
**【开源复用】**档位思路参照 Windows 电源计划；实现全走旋钮清单联动（无新机制）。
**【设计细节】**三档参数表：静音（频率≤中档/窗口 8s/缓冲 20ms）/均衡（自动 F048/窗口 5s/缓冲 10ms）/性能（全频/窗口 5s/缓冲 5ms）；切换动画 200ms（F124 标准曲线）；托盘图标三态区分；自动模式（F048）与手动档的叠加规则文档化（手动=边界，自动=边界内调节）。

### G-B-30 F070 性能域总判据 · 完整设计

**【功能定义】**80fps 整链验收制度：B 域四层（合成/启动/IO/调度）每层独立判据全绿后，整链在「常用 50 件」（F040）真实负载下复测 80fps——单层绿不算数，整链绿才交付。
**【用户故事】**收官那天，账本上 30 项 B 域判据逐条打绿，最后一条整链实测：50 件应用依次打开、拖动、切换——帧率曲线稳稳压在 12.5ms 线下——**这一条曲线是整个性能域的毕业证**。
**【交互设计】**星图/生态季报（F149）公布整链成绩单；诊断中心「性能域状态」页 30 项判据红绿一览（每项可点进证据）。
**【数据与存储】**成绩单版本化（季度快照）；证据链引用各单项判据记录（不复制）。
**【状态与异常】**整链复测中有应用拖垮帧率 → 该应用入「攻坚名单」单独优化（不降低总验收标准）；借力件升级后整链复测重跑（版本漂移防护）。
**【验收判据】**整链场景定义文档化（50 件的打开/操作/退出脚本化）；80fps 达标定义 = P95 帧耗时 ≤12.5ms 且 P99 ≤16.6ms；成绩单两份证据（录屏+账本导出）齐备。
**【开源复用】**负载脚本框架复用 F033 构建链工具；测量本体全自研（F041 账本）。
**【设计细节】**整链测试机锁定 Y7000（可比性）；测试环境温度记录（热节流干扰排除）；每次系统大版本（借力件升级窗）后整链必复测；达标线与红线分离：80fps 目标/60fps 底线——底线破即回炉，目标破即优化不停。


---

# 第 3 部分 · C 体验域（宪章手感条款的兑现）

> 域目标：界面从「能显示」到「像样」再到「想住进来」。设计案给图纸，C1-C5 按节落包，验收走卷尾加权条款。

## C-1 提案表

| 编号 | 提案 | 说明 |
| --- | --- | --- |
| C1 | 桌面 UI 成型 | taskbar/desktop 已有雏形——从「能显示」到「像样」 |
| C2 | 文件管理器接 SHARED 真数据 | 用户第一次「在 VARIX 里看到自己的文件」 |
| C3 | 设置中心 + 壁纸/主题 | 个性化 = 归属感 |
| C4 | 自动交接体验化 | 交接动画/进度条（四帧画面已有设计）——m1 体验峰值 |
| C5 | 候选词个性 | 输入法记住用户用词习惯、支持自定义短语（打「yx」出常用句子）——落点 WP-202 输入与输入法面的体验扩展 |

**获取口径（Variable 2026-09-25 拍板）**：轻量小游戏与媒体三件套（看图/看视频/听音乐）**不做内置开发——下载第三方软件即可**（走 A4 的 vxapp 侧载管线或直接 .exe，由兼容域覆盖）。系统只保证「下载→安装→能跑→体验合规」的管线与手感底座，内容交给开放生态。

## C-2 开机进入动画（Boot Sequence Experience）

1. **序列设计**（四幕，总时长 ≤8 秒，可跳过——上限即 B 域 B2 验收线）：
   - 幕一「点火」：Limine 选单后的黑场中，一枚 VARIX 星徽从暗到亮（0.8s，缓出）；
   - 幕二「聚合」：星徽周围粒子/光线向中心汇聚，同步真实加载进度（进度条不许骗人——进度与真实 init 里程碑绑定，实测几秒就几秒）；
   - 幕三「点亮」：屏幕整体亮度渐升过渡到桌面（0.6s，桌面先骨架后内容）；
   - 幕四「呼吸」：桌面元素错落淡入（任务栏→图标→组件，错峰 80ms），落定后动画即止——**常驻动画只属于交互反馈，不属于待机**。
2. **技术约束**：动画帧率独立于桌面合成器（不互相拖累）；4K 帧资产预生成、运行时零实时模糊/光追类昂贵效果；跳过键（Esc/回车）第一帧即可用。
3. **断电安全**：动画中断任意帧 → 直接落桌面（动画是装饰不是关卡）。
4. **零代码开机（Variable 2026-09-25 拍板）**：开机全程用户**看不到任何内核自检文字**——现有 kinfo 自检输出（self-test PASS、MMIO、ACPI、xHCI 之类逐行日志）从用户视野移除，转入后台日志文件（开机后可在诊断中心查看）；Limine 选单阶段也换成图形化渲染（星徽背景 + 两枚图形条目 + 倒计时环），不再黑屏白字。开发场景保留快捷通道（按 D 键展开原始日志，仅限开发态镜像，用户镜像无此键）——**用户的开机是动画，工程的开机是日志，两者永不混在同一个屏幕上**。

## C-3 桌面与窗口体系

1. **图标与网格**：桌面图标尺寸、网格间距、双击间隔**对齐 Windows 规范**（32/48/96px 档位、缩放联动）；图标**视觉**全自研（4K 多分辨率 sprite 套装，风格：光影写实+玻璃质感，单图标含常驻/悬停/按下三态）。
2. **任务栏**：位置/行为对齐 Windows（底部、固定+活动区、右键跳转清单、托盘折叠）；视觉自研——毛玻璃底、悬停浮起、按下下沉。
3. **窗口**：最小化/最大化/关闭/贴边分屏/多窗口层叠全部对齐 Windows 动线；窗口阴影按深度层级分层（活动窗深、非活动浅）；边缘吸附有落点预览。
4. **右键菜单**：结构对齐（新建/排序/刷新/属性的位置一致），菜单分层、悬停子菜单、键盘可导航；视觉自研。

## C-3·补 开始菜单、设置中心与独立电源（Variable 2026-09-25 指定，参照 Windows 11 实机截图）

**① 开始菜单（对齐 Windows 11 结构，视觉全自研）**：
- **顶部搜索框**：「搜索应用、设置和文档」——全局搜索入口（应用/设置/文件三类一体，随输入即出结果）；
- **「已固定」图标网格**：6 列网格、图标+名称、显示简略信息折叠开关——用户可固定/取消固定/拖拽排序（与 E6 任务栏与开始菜单布局定制联动）；
- **「最近使用」列表**：最近打开的应用与文件（含时间标注「9 小时前」式），点击即开；
- **底部账户区**：左侧用户头像+用户名，点击出账户菜单（锁定/注销/账户设置）；
- **底部电源按钮（独立开关机——本节核心）**：右下角电源图标，点击弹出「睡眠/关机/重启」三选；
- **全屏模式**：开始菜单支持窗口模式与**全屏模式**两种形态切换（对齐 Windows 全屏开始界面），全屏下网格放大、最近使用铺满——两种形态同一数据源，随时切换；
- 视觉：布局与动线照抄 Windows 11，面板材质/配色/动效全自研（毛玻璃面板、悬停浮起、展开动画按 60fps 红线）。

**② 独立开关机（VARIX 自己的电源链，不借道 Windows）**：
- 开始菜单电源按钮 → 关机/重启直接走 **VARIX 电源固件链**（WP-106 ACPI 最小集）——关机前走存储冲刷五步 + 关机账目（B-2902），有进度画面无黑箱；
- 重启支持「回 VARIX」与「切 Windows 域」双目标（后者即交接流程，与 C4 自动交接同链）——**电源按钮是 VARIX 自己的，不是 Windows 通道的入口**；
- 睡眠/合盖按 WP-106 边界执行（能响应合盖），后续 STAR I start 扩展 Modern Standby 评估；
- 电源菜单与 E 域联动：关机前音效、关机动画淡出（可关）。

**③ 设置中心（对齐 Windows 设置结构，视觉全自研）**：
- **左侧分类导航**：主页/系统/网络和 Internet/个性化/应用/账户/时间和语言/辅助功能/隐私和安全性/更新——一级分类对齐 Windows 设置的信息架构，用户零学习成本；
- **首页顶部设备卡**：设备名/机型/壁纸缩略图/重命名入口 + 右侧快捷卡（更新状态/备份状态）；
- **首页卡片区**：屏幕/声音/通知/专注/电源和电池/存储/共享/多任务处理——卡片式行（图标+名称+副标题+右箭头），二级页进入即详情；
- **查找设置搜索框**：顶部全局设置搜索，直达具体设置项；
- 与 E 域关系：设置中心的「个性化」分类就是 E 域 E1-E8 的执行界面——**E 域定义可定制什么，设置中心定义在哪改**；
- 视觉：信息架构对齐 Windows，卡片样式/配色/图标全自研（4K 资产管线覆盖）。

## C-4 文件资源管理器（功能克隆 Windows，视觉全新）

**功能对齐清单**（一项不缺）：
- 树形导航窗格（快速访问/此电脑/盘符/目录树展开）
- 地址栏（面包屑 + 可编辑路径 + 下拉历史）
- 搜索框（当前目录递归，搜索中可取消）
- 视图切换：超大/大/中/小图标、列表、**详细信息（表格视图——列：名称/修改日期/类型/大小）**、内容视图
- 排序与分组（点击列头、右键排序方式、分组依据）
- 右键全集：打开/打开方式/剪切/复制/粘贴/删除/重命名/属性/新建（文件夹与常见类型）
- 回收站（删除入站、还原、清空——零真删红线不动摇）
- 剪贴板操作全链（复制/剪切/粘贴保真、大文件异步不卡 UI）
- 拖放（同盘移动/跨盘复制、Esc 取消、合法落点提示）
- 状态栏（选中数/合计大小/数据源如实标注）
- 键盘全可达（方向键导航、F2 重命名、Del 删除、Ctrl+A/C/V/X/Z、Enter 打开、Alt+D 焦点地址栏）

**视觉**：与 Windows 资源管理器**没有任何像素级相似**——自研导航树样式、自研表格样式（斑马纹/悬停行/选中态按 VARIX 语汇）、自研图标体系。数据源页脚如实标注（exFAT SHARED 真挂载 vs 内置演示树，绝不冒充）。

## C-5 会话/任务表格（语义待 Variable 确认）

按两种理解都立项，确认后取舍：
- **理解 A（任务管理器）**：进程/会话表格视图——名称/状态/CPU/内存/磁盘，红绿健康灯（账本订阅面），结束任务（二次确认）。系统监视器的表格化升级。
- **理解 B（文件管理器详细信息视图）**：即 C-4 节表格视图——若此意，A 并入系统监视器原计划。

## C-6 鼠标操作深化（手感是本域唯一 KPI）

按人格体验标准第 5/8 条全量执行：悬停必有反馈且不可点不装可点；按下有按压态、移出取消、松开才触发；拖拽元素跟手、有落点提示、Esc 放弃；滚轮方向自然、到底有边界感；双击/右键/长按分工明确；防抖全量（连点不触发双份）；**任何点击 100ms 内必有可见反馈**（B 域红线）；动画稳定 60fps、打断不跳变。指针方案自研（4K 双倍率 sprite，Windows 指针语义：箭头/手型/文本/忙碌/缩放全保留）。

## C-7 4K 资产管线（硬标准，不是口号）

- **全部视觉素材按 4K（3840×2160）原生制作**：图标（多档位 sprite）、壁纸、纹理、动效关键帧、指针——高分屏放大三倍不糊不锯齿；
- 渲染链路按 4K 验收：字形图集、动画插值在 4K 分辨率下逐帧检查；
- 资产管线进 CI：素材尺寸/格式/命名/三态齐全性自动校验；
- 低分屏（1280×800）按比例降采样，不按低分重画——一个源头，处处清晰。

## C-8 交互词典 v1（对齐映射表·摘要）

| 交互 | 对齐 Windows | VARIX 视觉 |
| --- | --- | --- |
| 关闭窗口 | Alt+F4 / 右上叉位 | 叉位同位，样式自研 |
| 开始菜单 | Win 键 / 左下角 | 同位同动线，面板全自研 |
| 重命名 | F2 | 同 |
| 删除 | Del（入回收站）| 同 |
| 任务切换 | Alt+Tab | 同动线，切换器视觉自研 |
| 属性 | 右键末位 / Alt+Enter | 同 |
| 全选 | Ctrl+A | 同 |
| 刷新 | F5 | 同 |

（完整词典随实现逐条入册；改词典必须全局同步。）

---


## C-9 深化设计报告（F071-F125 报告式全展开）

> 以下 55 份报告为 C 域全部功能项的报告式展开——七段模板见卷首·丙；每项末尾【设计细节】段含参数级、像素级、判据级补深。尺寸一律引用卷首·乙基线表。

### G-C-01 F071 开始菜单搜索直达 · 完整设计

**【功能定义】**开始菜单顶部搜索框（乙-2 表：高 40px、圆角 20px）输入即搜三类目标：应用（已装/可从 vxapp 侧载的本地包）/设置项/文件（文档+下载+桌面三目录索引）；结果三类分区展示，键盘上下直达、Enter 开首项。
**【用户故事】**Variable 想改音量——按 Win 键打「音量」两个字母，设置项直接出现在结果第一位，Enter 直达声音设置页——三秒完成过去五步的操作。
**【交互设计】**聚焦即展开结果面板（面板宽 640px 与菜单同宽）；三类分区横向标签切换 + 混排模式（最相关优先）；选中项右侧预览卡（应用显图标+版本，文件显缩略图，设置显所在页路径）；无结果态给「去浏览器搜索」外链（D 域原则）。
**【数据与存储】**应用索引来自已装清单（实时）；设置索引静态表（页面注册制，新增设置页必须登记——一处一事实）；文件索引懒建（三目录首次打开后台扫描，F093 缩略图库共享）。
**【状态与异常】**索引未就绪 → 文件区显示「正在建立索引」进度条（不空转）；输入防抖 50ms（比 F088 的 150ms 更急——搜索框场景不同）；输入含路径分隔符 → 自动切文件模式（直搜路径）。
**【验收判据】**「音量/记事本/报告.docx」三类查询各实测：首结果正确率 10/10；按键到首结果 ≤50ms；键盘全程可达（无鼠标走完全流程）。
**【开源复用】**交互参照 Windows Search 与 macOS Spotlight 分区逻辑；索引自研（轻量倒排，量级千条级无需重器）。
**【设计细节】**打分公式：设置项名完全匹配=100/应用名前缀=80/文件名包含=60+最近使用加权（F072 引擎共用）；拼音首字母匹配支持（yx→应用）；结果面板动画复用 F124 进入曲线 200ms；搜索历史本地保存 20 条可清空（隐私总闸联动）。

### G-C-02 F072 最近使用引擎 · 完整设计

**【功能定义】**双维度使用记录：应用（启动/前台时长）与文件（打开/保存/拖拽）分别计频次与时间衰减分；同一引擎供三处消费：开始菜单「最近使用」/文件管理器「快速访问」/任务栏跳转清单（F074）。
**【用户故事】**Variable 每天早上打开同三个文件——它们的快捷方式自动浮在快速访问顶部；上周用过的临时文件沉底直至消失——系统默默记住习惯，不需要用户动手整理。
**【交互设计】**「最近使用」列表行高 44px（乙-2 表）、右端相对时间标注（「9 小时前」格式，>7 天显日期）；「固定」图钉按钮（悬停出现）——固定的永不衰减；清除入口在列表尾部「清除历史」（二次确认）。
**【数据与存储】**记录表：文件路径哈希+频次+最近时间戳，上限 500 条 LRU；存储配置层（还原点 F121 覆盖）；隐私总闸一键清空。
**【状态与异常】**文件被移走/删除 → 列表显示灰条「文件已不存在」点击给定位/移除二选；路径脱敏规则与 F036 共用（用户目录段替换）；同文件多路径（快捷方式打开）归并到真身。
**【验收判据】**三处消费面数据一致（同一时刻三处列表同序同内容）；时间衰减实测：7 天不用的文件排名持续下沉。
**【开源复用】**衰减公式参照 frecency（Firefox urlline 模型：频次×时间对数）；实现自研。
**【设计细节】**frecency 公式：score = freq × 1/(1+days/3)（三天半衰期）；频次上限封顶 20（防单一文件霸榜）；固定项单独区置顶不参与排序；记录写入异步批量（防 IO 抖动）；三消费面的渲染各自实现但数据 API 唯一（一处一事实）。

### G-C-03 F073 任务栏预览缩略图 · 完整设计

**【功能定义】**悬停任务栏图标 400ms 后弹出该应用窗口实时缩略图卡：单窗一卡、多窗纵向卡片列；缩略图来自合成器离屏渲染管道（每秒刷新 2 次，悬停期间）。
**【用户故事】**用户开了五个浏览器窗口找那篇资料——悬停浏览器图标，五张缩略图一列排开，一眼认出目标点击直达——不用逐个 Alt+Tab 摸索。
**【交互设计】**缩略图卡宽 200px（16:9 比例高 112px）、标题条 24px（窗口标题单行截断）、圆角 8px（乙-1 表）；多窗时纵向堆叠最多 5 卡（超出出滚动）；悬停卡时该窗口在屏幕上高亮描边（定位辅助）；移出 200ms 淡出。
**【数据与存储】**缩略图内存缓冲每窗一份（悬停期生命周期）；不落盘。
**【状态与异常】**最小化窗口 → 缩略图显示最后帧+「已最小化」角标，点击即还原；窗口销毁 → 卡片实时移除；合成器忙（帧超预算）→ 刷新降为 1 次/秒（性能优先）。
**【验收判据】**缩略图与窗口实际内容一致性抽查（10 窗样本全对）；悬停到出图 ≤400ms；刷新期间前台帧率不掉（F041 账本验证）。
**【开源复用】**实现思路参照 GNOME Shell 窗口缩略与 Windows DWM 缩略 API；离屏渲染走合成器既有管道（自研）。
**【设计细节】**离屏渲染走合成器空闲窗口（F049 深睡间隙）；缩略图降采样 0.5 倍再缩放（省一半带宽）；标题条文字走 12px 次要色（乙-1 表）；卡片阴影用预渲染环带（F056 同款）；多显示器前瞻（F029）时缩略图跟随任务栏所在屏。

### G-C-04 F074 跳转清单 · 完整设计

**【功能定义】**右键任务栏图标出「跳转清单」：最近文件（F072 引擎，最多 8 条）/固定文件区/应用声明的自定义任务组/「关闭所有窗口」尾项；数据源与开始菜单最近使用同引擎（一处一事实）。
**【用户故事】**右键记事本图标——最近编辑的三个文档直接列着，点开就继续昨天的工作；再右键终端——「新标签页/分屏/以管理员运行」三个自定义任务一步直达。
**【交互设计】**清单宽 260px、行高 36px、分组标题 12px 灰字；固定文件区带图钉移除钮；任务项带小图标（应用声明）；清单展开动画 150ms（F124 强调曲线）。
**【数据与存储】**固定项存应用蜂巢外元数据；自定义任务由应用在注册窗口时声明（静态清单，不做动态插件——防滥用）。
**【状态与异常】**应用无窗口时右键 → 仍显清单（最近+任务），无「关闭所有窗口」项；声明任务执行失败 → 三要素 toast；清单项过多 → 尾部「更多」进应用主界面。
**【验收判据】**最近区与 F072 数据一致（抽 5 例）；自定义任务三应用样本执行全对；清单键盘可达（方向+Enter）。
**【开源复用】**结构对照 Windows Jump List 文档；实现自研（F072 引擎 + 任务栏面）。
**【设计细节】**清单定位贴任务栏上缘 8px；超出屏幕高自动改为向上展开模式；最近区空态给「此应用还没有最近文件」轻文案；固定文件同样进 F109 剪贴板历史旁的「快速槽」概念（不复制数据只引用）。

### G-C-05 F075 托盘系统 · 完整设计

**【功能定义】**任务栏右端托盘：常驻三件（网络/音量/电池）+ 应用托盘折叠区（隐藏图标上拉面板）；三件套点击各弹控制条（F076 聚合前的轻量版）；托盘图标 16px 基准（2x 档 32px 4K 资产）。
**【用户故事】**Variable 点音量图标——竖滑杆直接弹出拖到 60%——没有中间页；十个后台应用的图标全收进折叠区（^ 钮），任务栏永远干净。
**【交互设计】**控制条面板：音量竖滑杆高 160px 实时 60fps、网络弹已存 Wi-Fi 列表、电池弹百分比+预估续航；折叠面板宽 280px 网格 4 列排应用图标；图标悬停 tooltip 显示应用名（1s 延迟）。
**【数据与存储】**折叠/展开状态与「哪些图标允许常驻」白名单存配置层。
**【状态与异常】**应用崩溃后托盘图标残留 → 30s 无心跳自动清除（幽灵图标清道夫）；电池计不可读 → 电池图标灰显+悬停说明（诚实降级）；图标缺失 → 默认占位图+应用名。
**【验收判据】**滑杆拖动全程 80fps（F041 账本）；幽灵图标清除实测；三件套点击响应 <100ms（C-6 红线）。
**【开源复用】**交互对照 Windows 11 托盘实机；实现自研（任务栏组件族）。
**【设计细节】**滑杆步进 2%（100 格），键盘上下键 5% 大步；托盘区总宽上限 200px（超出强制折叠）；折叠面板图标支持拖拽调序（E6 联动）；电池图标四态（充电/高/低/极低）颜色走主题令牌（E1）。

### G-C-06 F076 快速设置面板 · 完整设计

**【功能定义】**点击网络/音量/电池任意一枚出聚合面板：Wi-Fi 选择/蓝牙开关（前瞻灰置）/音量滑杆/亮度滑杆/专注模式开关/性能三档（F069）——一块面板管全系统开关。
**【用户故事】**要开会了——Variable 点开快速设置：专注模式一键开（F115 联动压暗+静音）、Wi-Fi 切到会议室热点、音量拉零——十秒完成会前准备。
**【交互设计】**面板 360×420px 右下弹出（距边 16px，乙-4 表 toast 同位）；开关卡两列网格（卡 164×56px，图标+名称+开关态色）；滑杆区横向全宽；面板外点击即关（轻模态）。
**【数据与存储】**面板布局（哪些卡显示/顺序）用户可编辑（长按拖拽，E6 联动）；状态即时生效无存储（开关态本身在各自子系统）。
**【状态与异常】**Wi-Fi 列表为空 → 「未找到网络」+ 重新扫描钮；亮度滑杆在夜间模式（F116）联动色温提示；性能档切换（F069）toast 确认；飞行模式一键开关（聚合所有射频）。
**【验收判据】**六类开关全部实测生效；面板弹出 ≤100ms；盲操作友好（全键盘可达：Tab 循环+Space 切换）。
**【开源复用】**布局对照 Windows 11 快速设置实机（甲节纪律）；实现自研（托盘 F075 同组件族）。
**【设计细节】**开关态色用强调色令牌（E1）非硬编码绿；滑杆双端数值标签（拖动时放大显示当前值）；Wi-Fi 连接中态转圈动画在卡内（不弹窗）；面板圆角 8px+毛玻璃材质（C-3 任务栏同材质族）；所有开关翻转动画 150ms 弹性曲线。

### G-C-07 F077 通知中心 · 完整设计

**【功能定义】**右侧滑出通知面板：toast 停留 5s 后沉入历史；历史按应用分组（组头=应用名+图标+「全部清除」）；顶部免打扰总开关；逐应用通知权限管理入口。
**【用户故事】**Variable 去开了个会，回来屏幕右下没有堆积的弹窗——全在通知中心里按应用排好，一眼扫完，一键清空——通知是有秩序的邮件箱，不是弹窗雨。
**【交互设计】**面板宽 360px、右侧全高滑入（250ms F124 曲线）；单条通知卡：应用图标+标题+正文两行截断+时间；卡片操作钮（应用自定义最多 2 枚）；组折叠/展开；清空走确认（防手滑，可撤销 5s）。
**【数据与存储】**历史保留 50 条/应用上限 20 条，落盘配置层（重启不丢）；免打扰计划（时段）可设。
**【状态与异常】**应用通知风暴（>10 条/分钟）→ 自动折叠该组+提示；toast 期间同组新到 → 合并为计数角标（不叠罗汉）；免打扰期间 → 静默入历史（不弹）；通知点击直达应用对应内容（深链协议，应用注册）。
**【验收判据】**toast→历史→点击直达全链录屏；风暴合并实测；免打扰时段自动生效抽查。
**【开源复用】**交互对照 Windows 11 通知中心与 Android 分组；实现自研（F075/F076 同面板族）。
**【设计细节】**优先级三档（应用声明）：高=免打扰也弹（闹钟类）/正常/低=只进历史；时间标注相对格式与 F072 同函数（一处一事实）；通知卡操作按钮走应用回执协议（点击带 action id 回应用）；深链失败 → 降级打开应用主界面。

### G-C-08 F078 日历飞出 · 完整设计

**【功能定义】**点击任务栏时钟弹出月历卡：当月网格/今日高亮（强调色圆底）/前后月翻页/今日日程位（数据面留口，STAR I start 后程接日历应用）。
**【用户故事】**「今天几号来着」——点一下时钟，月历弹出、今天亮着，顺手翻了下月确认截止日——两秒钟的事，不打断手头工作。
**【交互设计】**面板 320×340px 时钟上方弹出；月网格 7 列单元 42px、今日格强调色圆底白字；左右箭头翻月（键盘 PgUp/PgDn 同效）；标题「2026 年 9 月」可点回今日。
**【数据与存储】**月历纯计算无存储；日程槽位预留 API（空实现，差异表注明）。
**【状态与异常】**系统时间异常（F187 校时未完成）→ 顶部黄条提示；翻页无边界（无限前后）。
**【验收判据】**月历渲染与万年历对照 12 个月全对（含闰年 2028 二月）；弹出 ≤100ms。
**【开源复用】**历法计算自研（公历无库依赖）；交互对照 Windows 11 日历飞出。
**【设计细节】**周起始日跟随区域设置（中国周一）；节假日标注槽位预留（数据面后程）；农历显示评估项（开源 lunar 库接入评估，F130）——中国用户高频需求，进评估不进承诺；面板动画 150ms 自上而下 8px 位移。

### G-C-09 F079 全局音效体系 · 完整设计

**【功能定义】**六事件音效：开机/关机/通知/USB 插拔/回收站操作/错误；音效文件 48kHz FLAC 进 4K 资产管线（E5 声音方案承载可换）；静音总闸一键全停。
**【用户故事】**深夜 Variable 把系统切静音——所有音效全停包括「看似无害」的插入提示；白天换上「星海」主题音效包——开机那声粒子汇聚的泛音成为每天的小仪式。
**【交互设计】**E5 声音方案页：六事件列表各带试听钮/音量独立滑杆/更换按钮；总闸在快速设置（F076）音量区旁小喇叭；试听走当前输出设备。
**【数据与存储】**官方方案 3 套（星海/晨光/无声）内嵌镜像按需装载（F068）；用户方案 vxtheme 承载；音量记忆配置层。
**【状态与异常】**音频设备缺失 → 音效静默跳过（不报错骚扰）；方案文件损坏 → 回退无声+诊断报备；多事件同时 → 排队播（间隔 80ms 不叠音）。
**【验收判据】**六事件全链触发实测（含真实插拔 U 盘）；静音总闸 100% 生效（含系统级提示音）；FLAC 解码延迟 <20ms（F064 链路）。
**【开源复用】**音频播放走 F026 输出面；FLAC 解码评估 libFLAC（F130 登记）；音效素材按 4K 资产管线同级标准（48kHz/24bit）自制或授权采样。
**【设计细节】**事件→音效映射表公开（F126 文档）；音效响度统一归一 -18LUFS（防某个音效炸耳）；开机音在动画幕二「聚合」点触发（C-2 编排同步）；静音总闸状态持久化；错误音效与三要素对话框（F035）绑定出现。

### G-C-10 F080 窗口吸附动画 · 完整设计

**【功能定义】**贴边分屏交互：拖窗到屏幕边缘出落点预览（半透明矩形 2px 强调色描边+8% 填充，F018 同视觉族），松手弹性动画 150ms ease-out 就位；Win+方向键同动线（键盘路径）；四分屏（四角）支持。
**【用户故事】**Variable 写报告要对照资料——拖文档到左缘、资料到右缘，两窗各半一秒完成；再 Win+上让报告最大化——分屏像磁铁一样「啪」地吸上，手感干脆。
**【交互设计】**预览矩形实时跟随（拖动期间 60fps）；预览阶段源窗口降透明 20%（F018 同款语义）；就位动画含轻微过冲（弹性 105%→100%）；Esc 中途放弃回原位（F018 三取消对齐）；分屏后按 Win+方向微调尺寸档（半/三分/四分循环）。
**【数据与存储】**分屏布局记忆：应用重开恢复上次分屏位（每应用蜂巢外元数据）。
**【状态与异常】**贴边触发带 8px 进入判定带（防误触）；程序声明固定尺寸 → 分屏禁用+灰置说明；多桌面前瞻（F081）分屏位每桌面独立。
**【验收判据】**四向+四角八种落点全实测录屏；键盘路径与鼠标路径结果一致；就位动画期间帧率不掉（F041）。
**【开源复用】**动线对照 Windows Snap Assist；实现自研（VXWM 窗口管理面）。
**【设计细节】**预览矩形尺寸计算含任务栏遮挡（工作区而非全屏）；三分屏布局（左半+右上下两半）在预览时按拖拽停留位置智能推荐；过冲曲线参数（105% 回弹 80ms）进 F124 总谱；Snap 后窗口间缝隙 0px（视觉一体）。

### G-C-11 F081 任务视图 · 完整设计

**【功能定义】**Win+Tab 多窗口缩略墙（复用 F073 管道）+ 底部虚拟桌面条：桌面 1/2 增删、窗口跨桌拖移；每桌面独立最近使用栈（F072）与分屏布局（F080）。
**【用户故事】**工作桌面开着报告和资料，娱乐桌面开着音乐和相册——Win+Tab 一拉，两块天地一目了然，拖个窗口换个桌就像换张桌子——**多任务是房间，不是叠罗汉**。
**【交互设计】**进入动画 250ms：所有窗口缩略飞入网格（保持相对位置感）；顶部当前桌面名可点击重命名；虚拟桌面条卡 160×90px（桌面缩略）；拖窗口到桌面卡上=移动；桌面条「+新建」尾部常驻；Esc 退出回原桌面。
**【数据与存储】**桌面列表与窗口归属存会话状态（重启恢复 F 会话恢复面）；桌面名自定义存配置层。
**【状态与异常】**关闭含窗口的桌面 → 窗口并入前一桌（Windows 同策略）+ toast 可撤销；缩略墙窗口过多 → 分页（每页 12 格）；Alt+Tab（F082）仅限当前桌面（跨桌用任务视图——边界明确）。
**【验收判据】**四桌面×三窗口压测全流程录屏；拖移窗口跨桌后焦点正确；进入/退出动画帧率达标。
**【开源复用】**交互对照 Windows 任务视图与 GNOME 动态工作区；实现自研（合成器离屏管道+F073 复用）。
**【设计细节】**缩略墙网格自适应（3-4 列按窗口数）；窗口缩略比例统一缩放（保持相对大小感）；当前桌面卡描边强调色；键盘路径：Win+Tab 后方向键+Enter 全可达；新建桌面动画从「+」钮展开（250ms）。

### G-C-12 F082 Alt+Tab 现代化 · 完整设计

**【功能定义】**Alt+Tab 切换器：缩略图+图标双排卡片、按最近使用排序、长按 Alt 停留展开完整面板（松开即切、加 Tab 或方向键选择）。
**【用户故事】**两个窗口间快速来回——按一下 Alt+Tab 即切（最快路径无需看画面）；找不常用的窗口——按住 Alt 停留，完整卡片墙出现，方向键选中松手——快慢两种节奏一套交互。
**【交互设计】**快速态：双窗直接切换（<200ms 无 UI）；停留态：卡片墙居中弹出（卡 240×150px、图标 24px 左上、标题底部）、最近使用序从左上蛇形排列；选择高亮放大 5%（弹性曲线）；Alt+Shift+Tab 反向。
**【数据与存储】**排序即 F072 应用维度最近序（同引擎）；无额外存储。
**【状态与异常】**单窗口 → 不弹（直接忽略）；应用假死 → 卡片灰显「无响应」仍可选（切过去看它死透没——诚实）；UAC 类系统窗不进墙。
**【验收判据】**双窗切换延迟 <100ms 实测；十二窗压力下卡片墙布局不乱；假死窗口标注实测。
**【开源复用】**动线对照 Windows Alt+Tab；实现自研（F073 缩略管道复用）。
**【设计细节】**停留判定：Alt 按住 >350ms 出墙（低于则维持快速态）；墙出现后 Alt 可松开（选择态独立）；卡片缩略图 2 次/秒刷新（同 F073）；卡片数上限 24（超出滚动）；墙背景毛玻璃+背景窗口降透明 40%（聚焦感）。

### G-C-13 F083 桌面刷新语义 · 完整设计

**【功能定义】**F5/右键刷新做诚实版重载：真重载图标缓存与桌面枚举（含可见反馈 80ms 图标微闪），不装样子——如果无事可刷则反馈极轻（不演戏）。
**【用户故事】**老习惯用户按 F5——桌面轻闪一下、图标位置校准；其实 VARIX 的桌面本就实时——但那一闪是真实的重载确认，不是安慰剂——**诚实到连肌肉记忆都照顾**。
**【交互设计】**刷新反馈：图标 80ms 透明度 1→0.85→1（单一曲线全局统一）；右键菜单「刷新」位对齐 Windows（乙-4 表右键结构）；刷新期间桌面不接受新枚举请求（防抖）。
**【数据与存储】**无存储；刷新计数入诊断（好奇用户可查「这个月刷新了 214 次，其实 0 次是必要的」——彩蛋式诚实）。
**【状态与异常】**图标缓存损坏 → 刷新触发重建（自愈 F189 联动）；刷新中弹出新文件 → 完成后补枚举。
**【验收判据】**刷新前后图标位一致（位移零）；80ms 反馈实测；缓存重建路径触发实测（手动损坏缓存）。
**【开源复用】**无外部依赖；实现自研（桌面组件）。
**【设计细节】**微闪只对图标层（壁纸任务栏不动——最小惊扰）；刷新语义文档化（词条库 F140）；连续 F5 合并（300ms 内单次）；桌面枚举增量化（对比快照只刷差异项——刷新成本 <5ms 常态）。

### G-C-14 F084 图标拖拽网格 · 完整设计

**【功能定义】**桌面图标两种模式：对齐网格（拖动吸附 76×100px 单元，乙-4 表）/自由摆放（像素级）；拖拽半透明跟手+目标位高亮+碰撞自动换位动画；框选（橡皮筋）多选拖。
**【用户故事】**整理派用户把图标按项目排成列阵（对齐网格吸附得整整齐齐）；随性派用户随手一丢（自由模式想放哪放哪）——两种洁癖与随性都被尊重。
**【交互设计】**拖拽中图标 80% 透明跟手（F018 视觉族）；落点合法→目标单元高亮 2px 描边；碰撞→被占位图标滑动让位动画（150ms 逐个连锁，波浪感）；框选矩形虚线描边+8% 填充；多选拖动整体平移保持相对位。
**【数据与存储】**图标位存桌面布局文件（每图标网格坐标或自由坐标）；布局随主题（E1）可分套保存。
**【状态与异常】**拖出屏幕 → 回弹动画+归位（不丢失）；同名冲突 → F087 面板；布局文件损坏 → 重建默认网格（自愈+报备）；「自动排列」一键重排（按名称/类型/日期）。
**【验收判据】**碰撞换位波浪动画全对录屏；框选 20 图标整体拖动无散架；双模式切换即时生效。
**【开源复用】**无外部依赖；实现自研（桌面组件族）。
**【设计细节】**网格吸附阈值 24px（松手距最近格心内才吸附，否则回弹）；让位动画连锁延迟每级 20ms（波浪节拍）；自由模式吸附关闭但轻微磁吸 8px（对齐强迫症友好）；Ctrl+拖=复制语义（对齐 Windows）；布局文件版本化（主题切换恢复不丢用户手工位）。

### G-C-15 F085 回收站体验化 · 完整设计

**【功能定义】**删除动画（图标飞向回收站 200ms 弧线+缩淡）、还原反向、容量水位环（回收站图标角标）；零真删红线（B-1701）是全部动画的前提——动画演的是真实的数据流。
**【用户故事】**Variable 删错了一份合同——打开回收站，文件还在，右键还原，桌面合同回到原位原名字——**「回收站」三个字在 VARIX 是承诺不是比喻**。
**【交互设计】**删除动画多选批量（N 个图标依次 30ms 错峰飞入）；回收站图标角标显示件数；容量水位环 >80% 转黄色+toast 提醒清理；回收站窗口复用资源管理器（C-4）加「还原/清空」工具条；清空二次确认+可撤销 5s（F031 同语义）。
**【数据与存储】**回收站目录结构对齐 Windows（$Recycle.Bin 语义）：原路径元数据伴生（还原保真）；容量上限可设（默认分区 10%）。
**【状态与异常】**空间不足删除 → 弹「回收站已满」三选（清空旧件/直接永久删/取消——永久删需二次确认）；跨盘删除 → 各盘独立回收站（还原回原盘）；还原目标已被占 → 重命名还原（F087 语义）。
**【验收判据】**删-还原-再删 100 轮零数据损失（哈希对拍）；动画全程 80fps；容量环与实际占用一致。
**【开源复用】**目录语义对照 Windows 回收站规范（跨生态互通：VARIX 删的 Windows 能看见）；动画自研。
**【设计细节】**飞入弧线贝塞尔控制点=回收站方向 45° 抬升；动画期间真删除已异步完成（动画不阻塞数据流——表演与事实解耦）；元数据含删除时间（排序用）+原路径（>260 字符截断策略文档化）；清空撤销依赖回收站内暂存区（真清空在撤销窗后）。

### G-C-16 F086 复制/移动进度对话框 · 完整设计

**【功能定义】**文件操作进度对话框：总进度+当前文件双进度条、速度曲线、剩余时间、暂停/取消；冲突合并处理（F087）；「后台化」按钮——关对话框任务继续，托盘小图标续命。
**【用户故事】**Variable 拷 40GB 素材到 SHARED 卷——先看着速度曲线心里有数，点「后台化」去干别的，托盘小图标进度隐约可见，完成时 toast 报告（含冲突处理摘要）。
**【交互设计】**对话框 480×280px：文件名行（当前文件）/双进度条（总 8px+当前 4px）/速度+剩余时间行/三钮（暂停/取消/后台化）；取消=确认（已拷部分保留+说明，不静默丢）；冲突弹 F087 面板（应用到此文件，全选应用）。
**【数据与存储】**任务队列持久化（重启后未完成任务询问续传）；速度采样 1s 粒度。
**【状态与异常】**源文件中途消失 → 该文件跳过+汇总报告；目标盘满 → 暂停+三选（清理/换目标/取消）；U 盘拔出 → 任务失败如实+已拷清单；多任务并发排队（上限 3 并行，其余队列）。
**【验收判据】**40GB 拷贝全程对话框不卡（UI 线程隔离 B-18xx 判据）；暂停-恢复-续传实测；冲突面板三选全对。
**【开源复用】**无外部依赖；异步管线自研（存储栈 F057 分级队列消费端）。
**【设计细节】**剩余时间用滑动窗口均速（防抖动数字跳）；速度曲线图 120px 宽迷你图；后台化后托盘图标带迷你进度环；对话框位置记忆；多冲突批量面板单屏最多 20 条（超出分页）；完成音效（F079 回收站族旁「任务完成」事件）。

### G-C-17 F087 冲突智能提示 · 完整设计

**【功能定义】**同名冲突面板：两侧缩略图+大小+修改时间并排对比；三选（保留两者=自动加后缀/覆盖/跳过）；批量冲突合并单面板（逐条决策或「应用到全部」）。
**【用户故事】**拷贝 30 个文件 5 个重名——一个面板列出 5 组对比，Variable 逐组选完（或「新的全保留」一键）——冲突处理从 30 次弹窗地狱变成一张决策表。
**【交互设计】**面板 520×420px：左右对比卡（各 200px 缩略图+元数据行），中间三动作钮竖排；批量列表左侧 24px 每行（勾选决策）；「应用到全部」需二次点确认（防误伤）；后缀规则「名称 (2).ext」对齐 Windows。
**【数据与存储】**决策记忆（同类冲突默认策略可选记住）；无持久化必需。
**【状态与异常】**缩略图生成失败 → 类型图标兜底；时间相同+大小相同 → 提示「看起来是同一文件」建议跳过；覆盖只读目标 → 权限说明+强制/跳过二选。
**【验收判据】**30 冲突批量场景单面板完成全流程录屏；三选后缀规则 100 例全对；「应用到全部」误伤撤销路径（操作级 undo）。
**【开源复用】**对比布局对照 Windows 复制冲突对话框；实现自研。
**【设计细节】**缩略图复用 F093 引擎；时间显示精确到秒+相对时长；后缀序号查找从 (2) 起跳过已占位；面板 Esc=全部跳过（保守默认）；决策表在任务完成摘要（F086 toast）中可回看。

### G-C-18 F088 搜索即输即显 · 完整设计

**【功能定义】**资源管理器搜索框：输入防抖 150ms 即出结果、命中词高亮、范围条显式（当前目录/含子目录切换）、Esc 三击内清空回位；结果即时列表替换（不打断输入焦点）。
**【用户故事】**Variable 在 5000 文件的项目目录打「报告」——0.2 秒内列表只剩 8 个命中文件，「报告」二字高亮——找文件像打字一样自然。
**【交互设计】**搜索框高 32px（乙-1 表）带放大镜图标；范围切换胶囊钮（当前/递归）；结果列表保留原视图模式（详细信息列照排）；高亮用强调色底 40% 透明；清空动画 120ms 列表淡出回原目录。
**【数据与存储】**目录级索引懒建（首次搜索该目录时扫描建缓存）；缓存随目录变更失效（F093 缩略库同监管）。
**【状态与异常】**递归大目录（>10k 文件）→ 进度条+可取消（首屏结果先行渐进显示）；搜索中目录被改 → 结果标注「目录已变化」；无结果 → 空态给「试试包含子目录」建议。
**【验收判据】**5000 文件目录首结果 <300ms；防抖期间零多余扫描（打点验证）；高亮正确率 100%（含中英混排）。
**【开源复用】**无外部依赖（量级无需重索引器）；交互对照 Windows 资源管理器搜索。
**【设计细节】**匹配策略：文件名子串+拼音首字母（F071 同引擎分档）；递归深度上限 8 层（可配）；结果上限 2000 条（超出提示细化关键词）；搜索态地址栏显示虚拟路径「XX > 搜索结果」可点回；F3 重复上次搜索。

### G-C-19 F089 标签页式资源管理器 · 完整设计

**【功能定义】**多标签打开不同目录：Ctrl+T 新开（复制当前目录）/Ctrl+W 关闭/拖标签重排/拖出成独立窗；每标签独立记忆视图模式、滚动位置、搜索态。
**【用户故事】**Variable 对比两个目录的文件——左标签源目录右标签目标目录来回切换核对，Ctrl+Tab 一按即达——**窗口管理的粒度从「窗」细化到「页」**。
**【交互设计】**标签栏高 36px（标题栏下方，乙-1 表窗框内）；标签宽自适应（最小 120px 收窄为图标+悬停全名）；活动标签强调色底条 2px；中键点击=关闭；标签右键（关闭其他/复制标签路径）；Ctrl+Tab 循环切换。
**【数据与存储】**会话标签集持久化（重启恢复全部标签）；每标签状态（视图/滚动/历史栈 20 步）随存。
**【状态与异常】**标签目录被删 → 标签灰显「目录不存在」+定位上级按钮；单标签不可全关（关最后一个=关窗口，确认）；拖出成窗动画 200ms（标签飞出新窗）。
**【验收判据】**10 标签压测切换流畅；重启恢复 10 标签全状态（含滚动位）实测；拖出成窗路径录屏。
**【开源复用】**交互对照 Windows 11 资源管理器标签页；实现自研。
**【设计细节】**标签内存共享同一文件系统会话（枚举缓存跨标签复用）；标签溢出出左右滚动箭头（折叠菜单兜底）；拖入文件到标签头=移动到该目录（F018 管线）；历史栈前进后退与地址栏面包屑协同（一处一事实：同一历史引擎）。

### G-C-20 F090 面包屑增强 · 完整设计

**【功能定义】**地址栏面包屑两级增强：点击任一级出该层兄弟目录下拉；拖文件到面包屑级=移动到该层；可编辑模式（点击空白区转输入框，乙-1 表高 32px）。
**【用户故事】**Variable 在深层目录想挪到隔壁项目——直接把文件拖到面包屑的「项目」级上松手——路径层级变成了可投放的目标，不是只读的字符串。
**【交互设计】**面包屑项高 28px 悬停底色；下拉面板宽 240px 列兄弟目录（图标+名+子目录数）；编辑模式全路径选中（Ctrl+A 等价）、输入即时补全（存在路径前缀）；「回到当前」钮在编辑态旁（放弃编辑）。
**【数据与存储】**无新增存储（历史栈 F089 引擎共用）。
**【状态与异常】**拖拽到只读层 → 禁止光标+原因 tooltip；下拉目录 50+ → 滚动+搜索框；编辑输入不存在路径 → Enter 时如实报错不跳。
**【验收判据】**拖放移动 20 例全对（含跨盘语义 F018）；补全响应 <50ms；编辑-取消-回显零状态残留。
**【开源复用】**交互对照 Windows 面包屑；实现自研（地址栏组件族）。
**【设计细节】**下拉展开 120ms 淡入；面包屑过长省略中间层为「…」（点击可展开中间层列表）；拖放目标高亮同 F084 视觉族；键盘路径 Alt+D 直焦地址栏（乙-4 表既有）——编辑与面包屑两态全键盘切换。

### G-C-21 F091 详情窗格 · 完整设计

**【功能定义】**资源管理器右侧详情窗格（240px 可折叠）：选中文件显示元数据——类型/大小/修改时间/创建时间/图片加尺寸与 EXIF（相机/光圈/快门可选展开）；多选显示统计（数/总大小）。
**【用户故事】**Variable 选一张照片——右侧窗格立刻显示 4032×3024、拍摄于上周六、f/1.8 1/120s——不用打开看属性，扫一眼就够。
**【交互设计】**窗格背景浅一层（材质令牌）；字段行高 28px 标签灰 12px 值 14px；EXIF 默认收起「更多属性」展开；无选中时显示目录摘要（子目录数/文件数/总大小）；窗格开关钮在「查看」菜单+右键。
**【数据与存储】**元数据解析即时（EXIF 缓存入 F093 库）；无独立存储。
**【状态与异常】**文件被外部修改 → 窗格实时刷新（文件监视事件）；EXIF 缺失 → 字段省略不显「未知」占位（诚实留白）；大文件夹统计 >5000 项 → 后台算+渐进更新。
**【验收判据】**EXIF 五机型样本解析全对；窗格开合动画 150ms 不跳内容；多选统计与状态栏（C-4）数字一致（一处一事实）。
**【开源复用】**EXIF 解析评估 libexif（F130 登记）；布局对照 Windows 详情窗格。
**【设计细节】**窗格宽拖拽调（200-320px 范围记忆）；日期格式全局设置（F187 时区页联动）；图片字段显示原始+有效分辨率（旋转 EXIF 修正后）；窗格在窄窗口（<800px）自动隐藏并记忆。

### G-C-22 F092 压缩/解压内置 · 完整设计

**【功能定义】**zip 读写内置：右键「解压到当前文件夹/解压到 XX\」与「压缩为 zip」；加密 zip 只读支持（密码弹窗）；rar/7z 走第三方下载（C 域获取口径拍板）。
**【用户故事】**朋友发来 zip 相册——右键解压，进度条走完文件夹出现；回发时右键三张照片「压缩为 zip」——**最常见的打包场景零依赖完成**。
**【交互设计】**解压进度复用 F086 对话框；压缩前可选档（存储/均衡/最快三档）；加密 zip 双击弹密码卡（三次错误后建议「确认密码或换工具」）；压缩产物命名=选中项名.zip。
**【数据与存储】**解压产物直接落目标目录（无沙盒劫持）；压缩临时文件用后即删（回收站语义不适用临时物）。
**【状态与异常】**zip 损坏 → 归因「中央目录损坏，已恢复 N/M 个文件」（能救则救）；磁盘满 → F086 暂停流；路径穿越（zip 内 ../）→ 拦截+归因日志（F176 安全纪律）；非 UTF-8 文件名 → F034 码页检测接住。
**【验收判据】**标准 zip 样本 20 枚（含加密/分卷不含/中文文件名）解压全对；压缩-解压 round-trip 哈希一致 50 例。
**【开源复用】**minizip 级库评估（zlib 许可 F130）；实现接口自研封装。
**【设计细节】**压缩档映射：存储=0/均衡=6/最快=1（deflate level）；解压流式（大 zip 不全量驻内存）；中文文件名优先按 UTF-8 标志位、无标志走 F034 检测；分卷 zip 不承诺（差异表）。

### G-C-23 F093 图片缩略图引擎 · 完整设计

**【功能定义】**EXIF 缩略图缓存库：按文件哈希缓存缩略图（16 档尺寸按视图档位生成）；万张目录滚动实测不卡；缓存库持久化（`cache/thumbs.db` 结构化）。
**【用户故事】**相册目录万张照片快速滚动——缩略图如流水线般现成（上次浏览已缓存），新图实时解码（F054 SIMD 支撑）——滚动丝滑是媒体体验的地基。
**【交互设计】**缩略图生成优先级：可视区>邻近区>全目录（按滚动预测）；生成中占位=模糊微缩图（低清先行渐进替换）；悬停大预览 480px（F091 窗格联动）。
**【数据与存储】**缓存条目：哈希+尺寸+时间戳+位图块；库上限 2GB LRU；损坏条目即弃（自愈）。
**【状态与异常】**原图变更 → 哈希失配重生成；库文件损坏 → 重建（进度通知）；只读卷（SHARED）→ 缓存写本地（原图位置无关）。
**【验收判据】**万张目录滚动帧率不掉（F041 账本）；二次浏览命中率 >95%；缓存库 2GB 上限生效实测。
**【开源复用】**EXIF 缩略图直接抽取（原图内嵌缩略免解码——Linux EXIF 标准做法）；库结构自研。
**【设计细节】**尺寸档对齐乙-4 表图标档（32/48/96/256 四档够用）；生成走后台低优先队列（F057 分级）；同图多档共存（视图切换零等待）；库碎片整理在空闲时段（F049 窗口）。

### G-C-24 F094 媒体信息悬浮 · 完整设计

**【功能定义】**悬停视频/音频文件 800ms 出信息提示条：时长/分辨率/码率/帧率（视频）或时长/码率/采样率（音频）；解析轻量（容器头信息，不解码全文件）。
**【用户故事】**整理视频素材时悬停一扫——「00:03:24 / 1920×1080 / 12Mbps / 30fps」——不用打开播放器就知道哪段是哪段。
**【交互设计】**提示条深色底白字（4K 资产管线样式）圆角 4px；在缩略图下方 8px；字段用间隔点分隔单行；F091 窗格同步展示全量元数据。
**【数据与存储】**解析结果入 F093 缓存库（哈希键共享）；无独立存储。
**【状态与异常】**容器损坏 → 悬停显示「无法读取媒体信息」（不给错误弹窗——轻量场景轻量处理）；超大文件（>8GB）只读头部（毫秒级）；编码信息缺失 → 字段省略。
**【验收判据】**MP4/MKV/WebM/FLAC/MP3 五容器样本解析全对；悬停到显示 <100ms（缓存命中时）。
**【开源复用】**容器解析评估 mp4box/ffprobe 裁剪（F130；只读头部无解码依赖）；UI 自研。
**【设计细节】**解析在后台队列（F057）悬停等待期完成（800ms 够冷启动解析）；时长格式 hh:mm:ss 自动缩位；分辨率含旋转修正（手机竖拍视频显示 1080×1920 如实）；码率显示总码率+视频码率双值（专业用户友好）。

### G-C-25 F095 终端应用 2.0 · 完整设计

**【功能定义】**VARIX 终端升级：标签页（F089 同组件）/分屏（左右上下四分）/主题（E1 令牌联动）/Unicode 全渲染（含 CJK 宽字符与组合字符）/回看 10 万行不卡（虚拟滚动）。
**【用户故事】**开发者左侧跑构建右侧看日志——分屏各自滚动互不干扰；回滚到昨晚的输出秒搜关键词——终端是主力工具不是附属品。
**【交互设计】**标签栏复用 F089 组件；分屏分隔条拖拽（6px 热区）；字号 Ctrl+滚轮 8 档；搜索 Ctrl+Shift+F 面板（高亮全部命中+上下跳）；滚轮回看零卡顿（虚拟化只渲染可视行）；主题即 E1 切换（前景/背景/光标色全令牌）。
**【数据与存储】**回看缓冲环形 10 万行（内存 ~80MB 上限自适应行宽）；会话导出文本（F096）；标签布局持久化。
**【状态与异常】**程序狂刷输出 → 渲染节流保最新（F012 同策略）；缓冲满 → 最早行丢弃（头部截断提示一行）；CJK 组合字符回退链走 F016。
**【验收判据】**10 万行 cat 大文件回看滚动 80fps；CJK 混排对齐抽查（中英混排列不错位）；分屏拖拽实时重排 80fps。
**【开源复用】**渲染层参照 Alacritty/WezTerm 的网格渲染思路（自研实现，内核零堆约束下的终端面）；VT 语义复用 F012 面。
**【设计细节】**网格渲染：字形实例化绘制（一次 draw call 渲染整屏字符——GPU 路径 B3 就绪后启用，软路径先达标）；回看缓冲按行块分页；光标三种样式（块/下划线/竖线）+闪烁频率设置；选区复制自动转纯文本（富文本可选）；滚动条 60px 宽触区（触屏友好前瞻）。

### G-C-26 F096 终端命令面板 · 完整设计

**【功能定义】**Ctrl+Shift+P 命令面板：内置命令（复制模式/清屏/分屏/导出会话/字号）模糊搜索直执行；应用可注册自定义命令（终端内运行的长命令收藏）。
**【用户故事】**每次都要敲的长命令（ssh 网关跳板）收藏进面板——Ctrl+Shift+P 打「跳板」Enter 直接跑——**终端的效率是把重复变一键**。
**【交互设计】**面板居中弹出 560×56px 搜索框态、输入后下拉结果（高 320px 上限）；高亮匹配段；Enter 执行/Esc 关；命令执行反馈区分「即时类」（清屏即见）与「运行类」（新标签执行）。
**【数据与存储】**收藏命令存应用蜂巢（导入导出随 vxtheme 面）；使用频次影响排序（F072 引擎分档复用）。
**【状态与异常】**命令执行失败 → 输出在当前终端如实显示（面板不吞输出）；面板输入含危险字符 → 原样传递（终端是成年人的工具，不代管）。
**【验收判据】**内置命令 12 条全可达；收藏-搜索-执行全链录屏；面板弹出 <100ms。
**【开源复用】**交互对照 VS Code 命令面板（动线对齐）；实现自研。
**【设计细节】**模糊匹配 = 子序列+首字母双路（F071 引擎复用）；面板出现时终端输出冻结显示（不被执行结果冲走——执行完恢复跟随）；命令模板变量（{host} 占位执行时问）评估项；导出会话含时间戳与退出码元数据。

### G-C-27 F097 记事本类编辑器 · 完整设计

**【功能定义】**轻量文本编辑器：无 BOM 感知（F034 检测条）/行尾统一提示（CRLF/LF 混合检测）/大文件流式打开（百 MB 不卡）/最近文件列表/查找替换（正则可选）/字数统计。
**【用户故事】**Variable 双击一个 300MB 日志文件——编辑器秒开（流式虚拟滚动），搜索「ERROR」0.5 秒定位全部 1,204 处——「轻量编辑器」不等于「只能编辑小文件」。
**【交互设计】**编辑区字体走等宽令牌（E7）；行号栏灰显当前行高亮；查找条顶部滑出（实时计数 n/m）；状态栏（24px 乙-4 表）：行:列/编码/行尾/字数；大文件态（>10MB）自动只读模式+「启用编辑（内存许可时）」显式开关——诚实边界。
**【数据与存储】**流式缓冲分块 4MB 窗口；保存走原子写（B-1801 判据：临时文件+换名）；未保存关闭 → 三选（保存/不保存/取消）+自动草稿（崩溃恢复 S209 面消费）。
**【状态与异常】**文件被外部修改 → 重载提示（对比视图前瞻评估）；编码保存不兼容字符 → 显式警告清单；磁盘满 → 保存失败三要素+草稿已保全。
**【验收判据】**300MB 日志打开 <2s、搜索 <1s、滚动 80fps；原子保存断电百次零损坏（B-1801 复测）；崩溃草稿恢复实测。
**【开源复用】**无外部依赖（流式文本面自研）；交互对照 Notepad++/VS Code 精华子集。
**【设计细节】**行高亮当前行背景 4%（不喧宾）；括号匹配高亮对；Tab/空格显示开关（空格点/制表箭头）；自动换行开关记忆每文件；查找高亮上限 1000 处（超出计数显示）；字数统计含字符/词/行三值（CJK 计词按字）。

### G-C-28 F098 截图工具 · 完整设计

**【功能定义】**三模式截图：区域（十字准星+放大镜像素取色）/窗口（悬停高亮整窗）/全屏；截图后直接进标注（画笔/箭头/马赛克/文字/序号圆标）与保存/复制；PrtSc 全键直达。
**【用户故事】**Variable 要给朋友说明一个设置——PrtSc 框选那个按钮，画红箭头，序号①标步骤，Ctrl+C 复制贴进聊天——**从按键到发出 20 秒，中间不借任何外部工具**。
**【交互设计】**区域模式：全屏压暗 30%+选区原亮、十字线全屏+放大镜 120px 圆（8x 放大+像素坐标+色值）、尺寸标注随选区实时显示；标注工具条浮动选区下方（画笔三粗细/箭头/马赛克涂抹/文字框/序号自动递增）；保存对话框（F008）+Ctrl+C 直复制。
**【数据与存储】**截图历史 20 条（`pictures/screenshots/`+库索引）；标注撤销 20 步。
**【状态与异常】**多屏前瞻（F029 单屏）——接口不锁死；截屏期间通知静默（防 toast 入镜）；放大镜边缘循回（到边不消失）；保存失败（盘满）三要素+重试。
**【验收判据】**三模式全流程各录屏；取色色值与实际像素一致（对拍）；标注后导出 PNG 无损。
**【开源复用】**交互参照 Snipaste/Flameshot 精华（放大镜/序号标注是公认好设计）；截屏走合成器离屏面（F073 管道复用）。
**【设计细节】**放大镜取色显示 RGB+HEX 双值可复制；序号圆标自动编号（删中间重排）；马赛克块 24px 网格（强度两档）；历史条支持重标注（原图保留分层）；截图全局热键可改（F169 快捷键查看器登记）；4K 截图文件 ≤15MB（PNG 压缩档自动）。

### G-C-29 F099 计算器 · 完整设计

**【功能定义】**标准/科学双模式计算器：标准（四则+百分比+平方根）、科学（三角/对数/幂/阶乘/括号多层）；历史记录侧栏；键盘全可达（数字区直输）。
**【用户故事】**Variable 算一段材料预算——数字小键盘噼啪输入、括号嵌套、回头看历史栏核对每一步——算完 Esc 清零继续下一笔，全程手不离键盘。
**【交互设计】**窗口 360×520px（科学模式 480px 宽）；显示区大字 32px 右对齐、表达式行 14px 灰显上一步；按键网格 4 列（科学 6 列）、按压态 100ms 红线（C-6）；历史侧栏 200px 可折叠、任一条目点击回填；Esc=清零/退格=Backspace。
**【数据与存储】**历史 50 条会话内持久（重启保留可清）；无其他存储。
**【状态与异常】**除零 → 显示「不能除以零」不崩溃；超长数（>16 位）→ 科学计数显示+全值可复制；表达式解析错误 → 即时红框定位非法字符；浮点精度按十进制补偿（0.1+0.2=0.3——计算器不该教用户浮点教训）。
**【验收判据】**标准/科学各 30 例运算全对（含优先级与括号）；历史回填 20 轮零错位；键盘走查（F169 登记热键）。
**【开源复用】**无外部依赖（表达式解析自研，量级极小）；交互对照 Windows 计算器。
**【设计细节】**十进制补偿算法（定点缩放）杜绝浮点尾差；百分号语义按 Windows（加法上下文为差比）；角度/弧度切换在科学模式显式可见；按键音走 F079（可关）；深浅主题随 E1 令牌；结果复制 Ctrl+C 带全精度。

### G-C-30 F100 时钟套件 · 完整设计

**【功能定义】**四合一件：世界时钟（多城市卡）/秒表（圈计）/倒计时（预设+自定义）/闹钟（多组+重复规则）；闹钟到点走通知中心（F077）+ 音效方案（F079）+ 全屏提醒（可选）。
**【用户故事】**跨时区协作——世界时钟卡钉着两座城市；泡面计时——倒计时三分钟一键；每天 standup 前 5 分钟闹钟——通知中心弹出，点一下迟到保险。
**【交互设计】**窗口 480×560px 顶部四标签切换；世界时钟卡：城市名+时差+模拟钟面（4K 渲染）；秒表大数字 40px 等宽字体（E7）+ 圈列表；倒计时圆环进度（80fps）；闹钟列表行编辑（时间选择器+重复星期胶囊钮）。
**【数据与存储】**闹钟与倒计时持久化（配置层，还原点覆盖）；世界时钟城市列表记忆；秒表会话内。
**【状态与异常】**系统睡眠中闹钟 → 唤醒源（F065 白名单显式唤醒）；闹钟触发时免打扰（F077）→ 高优先级通道照弹（F077 优先级设计）；城市库无 → 搜索框+时区手选兜底。
**【验收判据】**闹钟睡眠唤醒触发实测（合盖 2h 场景）；世界时差与标准时区库 12 城全对；倒计时到点全链（音效+通知）录屏。
**【开源复用】**时区数据复用 tzdata（F022 同源）；实现自研。
**【设计细节】**闹钟重复规则支持单次/每日/工作日/自选星期；贪睡 5 分钟（按钮大目标 64px——睁眼误触友好）；倒计时完成后自动可重启；秒表圈次自动标记最快最慢色；钟面刻度 4K 抗锯齿；时差显示含「明天/昨天」跨界标注。

### G-C-31 F101 天气件 · 完整设计

**【功能定义】**天气显示两形态：任务栏托盘行内简报（图标+温度）/点击面板详情（未来 5 天+逐时曲线）；数据走公开免费 API（无键方案优先），数据源诚实标注；无网络优雅降级。
**【用户故事】**出门前瞟一眼任务栏「18° 多云」——点开看下午有无雨——五秒的决策支撑，不需要打开浏览器查天气站。
**【交互设计】**面板 320×420px：今日大卡（温度 40px+图标 96px+体感/风/湿度三行）、逐时温度曲线（SVG 路径 80fps 悬停显值）、五天列表行；城市切换（多城市管理）；数据源页脚小字（「数据：XX 气象 API · 30 分钟前」）。
**【数据与存储】**缓存 30 分钟刷新一次（省流量省电）；城市列表配置层；缓存天气回包（离线可看上次数据+时间标注）。
**【状态与异常】**API 不可达 → 显示缓存+「数据截至 XX:XX」黄条；API 变更/失效 → 诊断报备+设置页提示更新源；无定位权限概念（城市手选为主，IP 定位显式询问）。
**【验收判据】**刷新-解析-渲染全链绿；离线降级路径实测；曲线悬停交互 80fps。
**【开源复用】**API 候选评估 wttr.in/Open-Meteo（无键免费，F130 登记）；面板自研。
**【设计细节】**温度单位 ℃/℉ 全局设置；图标集走 4K 资产（晴天/多云/雨/雪/雾五族×日夜）；曲线悬停十字线+数值气泡；刷新策略对齐 F060 电量账本（后台低频）；城市搜索面板复用 F071 搜索组件。

### G-C-32 F102 便签 · 完整设计

**【功能定义】**桌面粘性便签：多张独立小窗（默认 240×240px）、六色主题（E1 令牌联动）、置顶开关、开机自动恢复；内容即点即编。
**【用户故事】**Variable 的待办哲学是「贴在眼前」——屏幕右上角永远有张黄色便签写着本周三件事，重启后还在原位——**桌面就是他的大脑皮层**。
**【交互设计】**便签窗无标题栏（拖拽区=顶部 24px 隐形带，悬停显三钮：置顶/换色/菜单）；文字区直接编辑（自动保存防抖 500ms）；色板六色（黄/绿/蓝/粉/紫/灰）；右键菜单（复制全文/导出文本/删除——删除走回收站语义）。
**【数据与存储】**每张便签独立文件（`notes/` 目录，纯文本+元数据头）；零真删；开机恢复位置与置顶态。
**【状态与异常】**便签文件被外部改 → 重载提示；数量上限 20 张（超出提示整理）；磁盘异常自动草稿保命。
**【验收判据】**10 张便签开机全恢复（位置+颜色+置顶）；自动保存断电零丢失（草稿机制）；置顶跨全屏应用生效。
**【开源复用】**无外部依赖；交互参照 Sticky Notes 精华。
**【设计细节】**文字区滚动条自动显隐；字号两档（14/16px）；拖拽到屏幕边缘半屏吸附可选；导出为 .txt 纯文本（开放格式）；多显示器前瞻（F029）位置接口不锁死。

### G-C-33 F103 画图件 · 完整设计

**【功能定义】**轻量画板：画笔（三粗细+取色器）/形状（线/矩形/椭圆）/橡皮/文字框/撤销重做 50 步/导出 PNG；画布 4K 精度；定位「随手画」不做专业绘图（专业走第三方——C 域获取口径）。
**【用户故事】**给同事画个流程草图——框几个矩形拉几条线加两行字，导出发出——五分钟的活不需要打开专业软件。
**【交互设计】**工具条左侧竖排（图标 32px 悬停出名称）；画布底色白/透明双选；形状拖拽预览虚线；文字框双击编辑；Ctrl+Z/Y 撤销重做（50 步上限）；取色器=屏上吸管（F098 放大镜复用）。
**【数据与存储】**工程临时保存（崩溃草稿）；导出 PNG 走 F008 保存对话框；自动保存 30s 间隔。
**【状态与异常】**超大画布（>8K）→ 建议档位限制；内存压力 → 撤销栈自动收缩（提示）；放大镜取色与 F098 同源。
**【验收判据】**50 步撤销零错位；4K 画布画笔跟手延迟 <33ms（一帧内）；导出 PNG 与画布逐像素一致。
**【开源复用】**无外部依赖；笔迹平滑评估（Catmull-Rom 插值——开源算法文献）。
**【设计细节】**画笔压力模拟（速度映射粗细——鼠标可用）；形状 Shift 约束正方/正圆/45° 线；网格吸附开关（对齐 8px）；撤销栈内存上限 64MB（超则丢最早）；导出可选 1x/2x。

### G-C-34 F104 录音件 · 完整设计

**【功能定义】**单键麦克风录音：波形实时渲染（60fps）、录音列表管理（播放/重命名/删除走回收站）、音量电平监视；走 HDA 输入面（WP-208）。
**【用户故事】**开会前录个语音备忘——按一下大红钮说话，停——列表里出现带时间名的录音，试听、留着、发给同事。
**【交互设计】**主界面 420×520px：大录音钮 72px（按压态+呼吸环）、电平表双声道竖条、波形实时画布（滚动瀑布式）；列表行：名/时长/日期+播放钮；播放进度条拖拽。
**【数据与存储】**录音存 `music/recordings/` WAV（48kHz/16bit）可转出；列表元数据 JSON；录音中断电 → 已录部分保全（流式落盘）。
**【状态与异常】**无麦克风 → 空态指引（设置中心设备页直跳）；录音中设备拔出 → 自动保存已录部分+说明；存储满 → 提前预警（<100MB）。
**【验收判据】**1 小时长录音零丢帧（文件完整性对拍）；波形渲染 60fps 全程；电平表与实际输入一致（对拍）。
**【开源复用】**WAV 封装自研（格式极简）；电平计算自研；无压缩依赖（转码评估 F130 后程）。
**【设计细节】**电平峰值保持（peak hold 1s 衰减）；录音命名「录音 2026-09-25 15-42」；波形纵轴自动增益；播放走 F026 输出面（混音器可见流）；录音时系统静音策略可选（防提示音入录）。

### G-C-35 F105 相册应用 · 完整设计

**【功能定义】**图片库双视图浏览：按时间流（月分组）/按文件夹树；滑动缩放（Ctrl+滚轮 8 档 12%-800%）、全屏放映（过渡动画+缩放平移）、基础旋转；解码全走 F054 SIMD 面。
**【用户故事】**翻三个月的旅行照片——时间流按月分组滚起来，看到一张好的双击全屏、滚轮放大看细节、Esc 退回原位——**看照片的乐趣不该被工具打断**。
**【交互设计】**时间流：月头粘性分组条（滚动钉住）；缩略网格按档位（F093 四档）自适应列数；全屏放映：黑底居中、缩放平移（拖拽跟手）、左右键翻页、过渡 250ms 交叉淡入；旋转写回 EXIF 或另存（显式二选）。
**【数据与存储】**库索引=F093 缩略库+目录监视增量；放映书签不持久（轻量定位）。
**【状态与异常】**损坏图 → 占位卡「无法显示」不炸流；超大图（>64MP）→ 流式分块（F054）；目录变更 → 增量刷新不闪全屏。
**【验收判据】**万张库滚动 80fps；全屏放映翻页 <200ms；旋转保存 EXIF 正确写回（对拍）。
**【开源复用】**解码复用 F054；EXIF 面 F014/F093 同源；UI 自研。
**【设计细节】**月分组条毛玻璃材质；缩放焦点=光标位置（放大看哪哪居中）；放映幻灯片模式自动 5s（可调可关）；删除走回收站（F085）；多选批量旋转/删除；视频文件在此只显缩略+时长（F094 元数据），播放走第三方（C 域口径）。

### G-C-36 F106 键盘提示 HUD · 完整设计

**【功能定义】**CapsLock/NumLock/大写锁定态变化时屏幕下中出 HUD 提示（图标+状态字，1s 淡出）；输入法中英切换同显示（F107 联动）；位置避开任务栏与通知区。
**【用户故事】**打字打了一半发现全在「大写」——想起刚才碰了 CapsLock——HUD 早在切换那一刻就告诉你了（只是你没抬头）——**状态变化永远可见可溯**。
**【交互设计】**HUD 640px 宽横条居下中（距底 80px）：锁图标+「大写锁定 开」14px；出现动画 120ms 上浮淡入、1s 后 300ms 淡出；连续切换重置计时；设置页可关可移位。
**【数据与存储】**开关与位置配置层；无历史存储（瞬态提示）。
**【状态与异常】**切换风暴（键盘故障连发）→ HUD 合并不闪屏；全屏应用（放映/游戏前瞻）→ HUD 转角标模式（右下小图标）。
**【验收判据】**三键切换 HUD 三态全对；淡出时机 1s±50ms 实测；全屏降级路径实测。
**【开源复用】**交互参照 macOS 大写提示灯语义；实现自研（合成器独立层 F056）。
**【设计细节】**HUD 走合成器通知层（不抢焦点零打扰）；图标 24px+文字 14px 组合 4K 资产；三个锁定键图标差异设计（色弱形状冗余——无障碍 B-39xx 联动）；输入法切换 HUD 复用同组件（图标换「中/英」字样）。

### G-C-37 F107 输入法状态浮窗 · 完整设计

**【功能定义】**输入法三态浮窗：中/英文、全/半角、中/英标点——悬浮于光标下方 8px（乙-2 表搜索框偏移同族）、三态即时跟随切换；可收起为任务栏小标。
**【用户故事】**写中英混排文档——每次切态光标旁的浮窗即刻标明「中 · 全角 · 中文标点」——再也不会出现「打了半句才发现是英文标点」的返工。
**【交互设计】**浮窗 160×32px 圆角 8px 毛玻璃（F076 同材质族）；三态用分隔点连接显示（当前态强调色）；点击任态即切换（浮窗即开关）；设置页可调：显示/隐藏/仅显中英态。
**【数据与存储】**显示偏好配置层；无历史。
**【状态与异常】**浮窗避让屏幕右缘（自动左移）；全屏应用降级为 F106 角标模式；程序不支持 IME 组合（F027 降级路径）→ 浮窗灰显说明。
**【验收判据】**三态切换跟随 <16ms（一帧内）；光标跟随重定位不抖（节流 16ms F027 同源）；避让边界 20 例全对。
**【开源复用】**交互参照搜狗/微软状态栏精华；实现自研（IME 渲染层 F027 既有面）。
**【设计细节】**浮窗字体 13px（介于正文与次要间——专用档）；态切换微动画 80ms（字重渐变非位移——安静切换）；光标锚定取文本插入点（含 RTL 前瞻偏移接口）；密码框聚焦时浮窗自动隐藏（隐私纪律）。

### G-C-38 F108 自定义短语库 · 完整设计

**【功能定义】**输入法短语系统：缩写映射（打 yx 出整句）、日期时间变量（打 rq 出今天日期）、多行短语、分类管理、导入导出（vxtheme 管线）；词条上限 1000 条本地存储。
**【用户故事】**客服 Variable 打「kh」出完整问候语模板，打「rq」出「2026年9月25日」——每天省下上百次重复输入，短语库是他私人的效率武器。
**【交互设计】**设置中心「时间和语言-自定义短语」页：列表（缩写/内容/分类三列）+ 搜索 + 新建编辑器（缩写 32 字符/内容 500 字/多行支持）；变量插入菜单（日期/时间/星期三族格式选择）；导入导出按钮（vxtheme 包内 phrases.json）。
**【数据与存储】**短语库 JSON 本地（还原点覆盖 F121）；候选窗（F027/F107）内短语候选与普通候选混排（短语带徽标）。
**【状态与异常】**缩写冲突（与词库词重）→ 短语优先并可配置；内容超长 → 候选窗截断显示全量上屏；导入冲突 → 逐条选（覆盖/跳过/都留）。
**【验收判据】**100 条短语库实测：输入-候选-上屏全链 <100ms；变量三族格式 20 例全对；导入导出 round-trip 无损。
**【开源复用】**变量语法参照主流输入法自定义短语惯例（自定格式开放 F126）；实现自研（IME 引擎 C5 落点）。
**【设计细节】**变量语法 {date:yyyy-MM-dd} 系（解析器白名单格式符）；候选窗短语项加「短语」角标（与普通候选区分）；缩写触发即展即选（数字键选）；分类目录树左栏（复用 C-4 树组件）；使用计数排序（冷短语沉底）。

### G-C-39 F109 剪贴板历史 · 完整设计

**【功能定义】**Win+V 剪贴板历史：最近 20 条（文本/图片/文件引用三型）、固定钉选（钉选不驱逐）、搜索、逐条清除；隐私红线（B-3901 后台零读取）与隐私总闸（可整体停用）双保险。
**【用户故事】**Variable 复制了新内容才发现刚才那份还要用——Win+V 一拉历史还在；常用回复钉在第一位永远不跑——**剪贴板从单槽变成了信箱**。
**【交互设计】**面板 360×480px 光标附近或固定右下（设置选）；条目卡：内容预览（文本 3 行/图片缩略/文件链图标列表）+来源应用图标+时间；钉选图钉钮；搜索框顶部；清空全部（二次确认）。
**【数据与存储】**历史落盘配置层（重启保留，可设不保留）；图片条目缩略落库（F093 共享）；敏感窗口（密码框 F107 同检测）复制不入历史（隐私纪律）。
**【状态与异常】**大对象（>16MB）→ 历史存引用+文件（F017 临时文件同源）；来源应用已卸载 → 条目保留（数据本身无主）；粘贴失败（目标格式不支持）→ 原条目仍可复制。
**【验收判据】**三型条目 20 条循环驱逐正确（钉选除外）；密码框排除实测；搜索命中高亮。
**【开源复用】**交互对照 Windows 剪贴板历史；实现自研（F017 剪贴板服务扩展）。
**【设计细节】**粘贴回历史顶端（使用序）；条目点击=粘贴到当前焦点（原快捷键语义保留 Ctrl+V 备选）；文本 diff 提示（相邻相似条目标注「与前条仅差 3 字符」——评估项）；总闸关闭时面板入口灰置说明（诚实不藏功能）。

### G-C-40 F110 屏幕键盘 · 完整设计

**【功能定义】**屏幕键盘：全布局（104 键等效）/紧凑布局（九宫联想前瞻）两模式；按键回显走音效方案（F079）；触屏与应急场景（物理键盘故障）双用途。
**【用户故事】**键盘认为卡键的深夜——鼠标点开屏幕键盘，把紧急邮件敲完——**系统给你留的备用手**。
**【交互设计】**窗口 900×320px 可半透明（透明度滑杆）；按键尺寸 ≥44px（触屏标准热区）；按压高亮+音效反馈；布局切换钮；置顶常驻开关；物理键按下时对应虚拟键同步高亮（学习模式）。
**【数据与存储】**模式与透明度配置层；无其他。
**【状态与异常】**物理键盘恢复 → 提示可关（不自动关——用户自己决定）；触屏多点并发按键支持（前瞻 F063 联动）；无法输入的窗口（密码框安全键盘前瞻）→ 诚实标注限制。
**【验收判据】**全键位点击输入全对；半透明态下层内容可读；学习模式同步高亮实测。
**【开源复用】**布局数据参照标准 104 键定义；实现自研。
**【设计细节】**按键字体 16px+符号层（Shift 态翻转显示）；锁定键三态灯（F106 HUD 同图标族）；拖拽移动屏边吸附；点击穿透开关（半透明观察模式）；启动途径三个（设置/快捷键/F169 登记）。

### G-C-41 F111 放大镜 · 完整设计

**【功能定义】**Win+加号全屏放大（2x-16x，全屏模式=整个桌面放大移动）与镜头模式（240px 圆形镜头随光标）；随光标/随焦点/随键盘三跟随策略；无障碍五判据（WP-207）放大镜条目落地。
**【用户故事】**低视力用户把全屏放到 4x——光标走到哪内容放大到哪，文字锐利（4K 管线原生放大不糊）——**放大不是模糊的放大，是清晰的放大**。
**【交互设计】**工具条迷你悬浮（放大倍率 +/-/模式切换/退出）；镜头模式圆形边缘柔化 8px；倍率快捷 Ctrl+滚轮；全屏模式平移=光标顶边触发（对齐 Windows 动线）。
**【数据与存储】**倍率与模式记忆（会话）；无其他存储。
**【状态与异常】**16x 极限 → 像素网格显示辅助（帮助定位）；放大状态下截图（F098）→ 截取放大后所见（所见即所得）；性能保护 → 放大区域外渲染降载（F056 脏区联动）。
**【验收判据】**2x-16x 全档文字锐利（4K 放大三倍不糊条款验证）；镜头模式 80fps 跟手；三跟随策略切换实测。
**【开源复用】**无外部依赖；实现自研（合成器缩放层）。
**【设计细节】**放大重采样用最近邻（低倍保清晰锐利）+8x 以上双线性（像素画观察）；全屏模式边缘平移速度三档；镜头内坐标尺可选（像素定位用）；快捷键全套登记 F169；与夜间模式（F116）叠加顺序文档化。

### G-C-42 F112 讲述人雏形 · 完整设计

**【功能定义】**焦点元素朗读（TTS）：引擎评估 eSpeak-NG（开源轻量）接入；先覆盖两场景——设置中心与资源管理器（控件类型/文本/状态朗读）；快捷键 Ctrl+Win+Enter 开关。
**【用户故事】**视障用户 Tab 到「亮度」滑杆——讲述人读「亮度，滑块，百分之六十，左右方向键调整」——基础可用性先立起来，覆盖面按季度扩（诚实路线图）。
**【交互设计】**朗读高亮：被读元素描边（与焦点环区分的双视觉）；语速/音调设置页；「朗读所_FOCUS」与「全文连读」两模式；Esc 静音即停。
**【数据与存储】**设置页配置层；eSpeak 数据文件镜像内嵌（约 20MB，F130 登记）。
**【状态与异常】**TTS 引擎异常 → 静默停用+诊断报备（不弹错骚扰）；无声音设备 → 开关灰置说明；朗读与音效（F079）通道混音（语音优先压低音效）。
**【验收判据】**两场景全控件遍历朗读测试（100% 控件有可读名——无障碍门禁 B-39xx 联动）；开关快捷键全流程实测。
**【开源复用】**eSpeak-NG（GPL 许可——进程隔离评估：独立进程+IPC 避免许可证传染，F130 法律面登记）；实现接缝自研。
**【设计细节】**朗读文本模板化：「{控件类型}，{文本}，{状态}」（模板公开 F126）；数字朗读读法（「60」读「百分之六十」——滑杆语境化）；速率默认 1.2x（可用性调优）；朗读队列（新焦点打断旧句——响应优先）；与讲述人联动的焦点环加粗 2px。

### G-C-43 F113 高对比度主题 · 完整设计

**【功能定义】**纯黑/纯白两套官方高对比主题：纯色零渐变、边框强制 2px、对比度全表 ≥7:1（WCAG AAA 线）；E1 令牌体系承载、一键切换即时生效。
**【用户故事】**强光下用笔记本的用户切纯白主题——所有文字黑压白清晰锐利；暗房用户切纯黑——零刺眼灰阶——**高对比不是风格是刚需**。
**【交互设计】**E1 主题页两套卡置顶（「辅助」分组）；切换预览缩略图（甲节禁模糊——缩略图即真实渲染缩放）；应用后全局校验门禁（对比度不达标的第三方应用标黄提示其令牌缺失）。
**【数据与存储】**主题包内嵌镜像（F068 按需装载）；切换记忆配置层。
**【状态与异常】**第三方应用硬编码色（B-1104 违例）→ 高对比下降级为系统描边兜底（可读性保命）+ 星卡记录；图片类内容不做对比干预（内容自由）。
**【验收判据】**全系统界面逐页对比度实测 ≥7:1（工具扫描报告）；两主题 20 维度走查子集通过。
**【开源复用】**对比度公式 WCAG 2.1 标准公开算法；无运行时依赖。
**【设计细节】**纯黑 #000000 背景/#FFFFFF 文字/#FFD700 焦点环；边框色独立令牌（非文字色复用）；焦点环加粗至 3px；滚动条加宽 12px；两主题各含图标高对比变体（4K 管线双套产出）。

### G-C-44 F114 色弱辅助滤镜 · 完整设计

**【功能定义】**三向色觉滤镜：红色弱（protanopia）/绿色弱（deuteranopia）/蓝色弱（tritanopia）全屏色域变换（合成器像素后处理）；强度两档；E8 动效三档同级可关。
**【用户故事】**绿色弱用户开启滤镜后——红绿状态灯一眼可辨（红更橙绿更蓝的变换映射）——**世界不需要变色，需要的是翻译**。
**【交互设计】**设置中心「辅助功能-色彩滤镜」页：三模式卡（各带模拟预览图——用户看不到色差所以预览用对比图例）+ 强度滑杆 + 快捷开关（F076 快速设置卡）；滤镜图标常驻托盘（开启时）。
**【数据与存储】**模式与强度配置层；快捷键登记（F169）。
**【状态与异常】**合成器负载超预算（滤镜后处理 >0.5ms/帧）→ 自动降强度档+说明；截图（F098）默认截原色（滤镜为观看辅助不污染产出——显式说明）。
**【验收判据】**三滤镜色域变换与标准模拟矩阵对拍（Ishihara 测试图通过率提升实测）；帧耗时增量 ≤0.5ms。
**【开源复用】**变换矩阵用公开色觉模拟研究（Machado 2009 矩阵——学术公共成果标注）；实现自研（合成器后处理管线）。
**【设计细节】**矩阵应用于合成输出末级（滤镜之下所有内容统一）；强度两档=矩阵插值 50%/100%；玻璃色板预览（设置页色轮展示变换前后）；与夜间模式（F116）叠加顺序：色温先、滤镜后（文档化）；快捷键 Win+Ctrl+C 循环开关。

### G-C-45 F115 专注模式 · 完整设计

**【功能定义】**一键沉浸：非活动窗口压暗 20% 降透、通知静默（高优先除外 F077）、托盘气泡停发、计时器可选（专注 25 分钟 Pomodoro 档）到点恢复；快速设置（F076）与快捷键双入口。
**【用户故事】**要写两小时报告——点专注模式：屏幕安静下来（只有手头窗口亮着）、通知闭嘴、25 分钟一轮回提醒起身——**沉浸不只是加东西，是帮你屏蔽全世界**。
**【交互设计】**激活态：任务栏专注图标强调色+倒计时环（计时档）；F076 面板专注卡显示状态与剩余；结束 toast 汇总（「专注 25 分钟，替你挡了 7 条通知」——诚实数据）；设置页自定义压暗强度/时长档。
**【数据与存储】**专注会话统计（时长/拦截数）入账本 30 天；偏好配置层。
**【状态与异常】**高优先通知（闹钟 F100）照弹（安全优先）；用户手动关 → 立即恢复+统计保留；系统重启 → 专注态不跨重启（显式重开）。
**【验收判据】**四效果（压暗/静默/停泡/计时）全链实测；拦截统计准确性对拍；快捷键一次开关不误触（长按 500ms 防手滑）。
**【开源复用】**Pomodoro 计时通用法（无依赖）；压暗走合成器层（F056 独立面）。
**【设计细节】**压暗实现=非活动窗后景暗幕（不改窗口自身透明——应用无感知）；活动窗口判定跟焦点走（实时 16ms 切换）；倒计时环 60fps；专注期间 F098 截图不受影响；白名单应用（音乐播放器）通知可豁免（用户配置）。

### G-C-46 F116 夜间模式 · 完整设计

**【功能定义】**色温随时间曲线：1900K-6500K 映射表可调，日落日出自动切换（按位置或手动时间）或定时档；与 E1 深浅主题联动（夜间自动换深色可选）；强度三档。
**【用户故事】**晚上十点屏幕自动变暖——眼睛舒服了，睡眠不受蓝光刺激——**系统感知你的作息，替你把光调柔**。
**【交互设计】**设置中心「系统-屏幕-夜间模式」页：色温滑杆+曲线图（24h 温度曲线可拖锚点）；日落日出自动需城市（F101 天气城市共用）；F076 快速卡三档（关/暖/暖+深色联动）。
**【数据与存储】**曲线锚点配置层；城市引用 F101 数据；状态持久。
**【状态与异常】**色温实现失败（驱动不支持伽马表写）→ 诚实降级提示（差异表）；曲线锚点拖乱（交叉）→ 自动排序修正；全屏游戏前瞻场景可豁免（用户配置）。
**【验收判据】**色温档位实测（色度计对拍 ±200K）；日落触发时刻准确（±5 分钟）；深色联动切换无闪烁（300ms 交叉）。
**【开源复用】**色温-伽马映射参照 f.lux/Redshift 公开算法（F130 标注）；实现自研（显示面伽马表写）。
**【设计细节】**映射表锚点默认：正午 6500K/日落 3400K/深夜 1900K；过渡斜率 30 分钟渐变（无跳变）；伽马表每通道独立（RGB 分量调）；与 F114 滤镜叠加顺序已文档化；日出日落数据来自 F101 API 无网络时用手动时间兜底。

### G-C-47 F117 首次开机向导 · 完整设计

**【功能定义】**五步引导：区域与语言/网络连接/输入法配置/主题选择（E1 实时预览）/完成页（系统概览）；每步可跳过、可随时重进（设置中心「系统-首次体验」）；空态引导（宪章十一章）图形化实现。
**【用户故事】**新用户插上 U 盘开机——五步走完已经联网、输入法就位、选好喜欢的主题——第一印象是「这系统懂我」，不是「这系统要考我」。
**【交互设计】**全屏引导层（桌面毛玻璃虚化底）：步骤进度点五枚顶部居中；每步卡片 560×420px 居中；上一步/下一步/跳过三动线恒在；主题步实时换桌面预览（改了马上看到）；完成页五秒后自动进桌面（不拖沓）。
**【数据与存储】**完成标记持久（重进入口显式）；各步选择即写配置（中途断电已完成步保留——步进式提交）。
**【状态与异常】**网络步失败 → 跳过引导（B-607 手机热点文档指引）；断电重启 → 从未完成步续走（不重头）； skipped 步保留默认值并在完成页汇总「你跳过了 X，可稍后在设置里配」。
**【验收判据】**五步全流程+全跳过流程双路径实测；断电续走实测；完成页汇总准确。
**【开源复用】**无外部依赖；动线对照 Windows OOBE 精华（去糟粕：不强制联网不强制账号——VARIX 无账号概念）。
**【设计细节】**步骤卡背景 4K 星徽微动画（安静不喧宾）；输入法步含试打框（当场体验候选）；主题步四官方主题卡（点击即全局换——所见即所得）；向导期间通知静默；Esc=跳过当前步（可寻性）。

### G-C-48 F118 欢迎中心 · 完整设计

**【功能定义】**开机首日弹「了解 VARIX」交互卡五张：双域概念/软件下载（无商店原则宣导）/域切换/个性化入口/帮助中心入口；看完即走、可找回（设置+帮助中心）；不再自动弹第二次。
**【用户故事】**新用户第一次进桌面——五张卡片每张十秒讲清一件事——他没有被教程绑架，但关键心智（这是双域系统、软件去浏览器下）已经就位。
**【交互设计】**卡片流：全屏层内横向卡轮播（560×420px）+进度点+「跳过全部」恒在；每卡一个核心图示（4K 插画资产）+标题+两行文案+行动钮（直跳对应功能：下载卡开浏览器/个性化卡开 E1 页）；完成态卡「开始使用」收尾。
**【数据与存储】**完成标记持久；「再次查看」入口在帮助中心（F119）。
**【状态与异常】**中途退出 → 记录进度下次不再自动弹（只可手动看）；插画资产缺失 → 纯文字版降级。
**【验收判据】**五卡内容过审（概念准确：双域/无商店/交接三大心智零歧义）；行动钮直跳路径全对；不二弹实测。
**【开源复用】**无外部依赖；文案走 F140 词条库（可本地化）。
**【设计细节】**每卡文案 ≤60 字（一眼读完）；图示风格=壁纸引擎同族（视觉统一）；卡片切换 250ms 横滑；「跳过全部」首卡即显（不绑架）；完成标记可重置（演示/教学场景）。

### G-C-49 F119 帮助中心 · 完整设计

**【功能定义】**离线帮助手册：MD 文档渲染器承载（三册图纸精选+操作指南）；每页「去设置里做」直跳对应设置页；全文搜索；在线更新检查（可选）。
**【用户故事】**用户想改壁纸忘了在哪——帮助中心搜「壁纸」，第一篇「个性化你的桌面」文末按钮「打开个性化设置」一键直达——**帮助不是文档坟场是导航台**。
**【交互设计】**窗口 960×640px：左目录树（章节导航，C-4 树组件复用）/右内容区（MD 渲染：标题层级/表格/图片/代码块全支持）/顶部搜索框；直跳按钮内嵌语法（渲染器识别 `[[设置:个性化]]` 标记渲染为按钮）；字号调节（E7 联动）。
**【数据与存储】**手册内容镜像内嵌（约 5MB MD 源）；搜索索引启动后台建（F071 引擎复用）；更新包走双槽（F190）。
**【状态与异常】**索引未就绪 → 搜索降级为逐页标题搜（诚实标注「快速模式」）；直跳目标页已改名 → 跳设置中心首页+定位提示（死链可追）；渲染失败页面 → 源码视图兜底。
**【验收判据】**50 篇手册页渲染全对（表格/图/直跳钮抽查 20 处）；搜索首结果准确率 10/10；离线全程可用（断网实测）。
**【开源复用】**MD 渲染评估 markdown-it 级库裁剪（F130）或自研轻量渲染（内核风格零堆不适用此用户态——正常实现）；手册内容源自三册文档自动裁剪管线（构建期生成）。
**【设计细节】**直跳标记语法文档化（F126 开放格式——第三方应用文档可嵌同款按钮）；深链协议与 F077 通知直跳共用注册表；手册版本与系统版本绑定显示（「本手册对应 vX」）；打印友好样式（导出 PDF 走 F025 打印面）。

### G-C-50 F120 诊断中心 · 完整设计

**【功能定义】**系统健康统一入口：健康快照（F062）展示/日志导出（脱敏选项）/一键修复项（引导菜单修复/图标缓存重建/缩略图库重建）/启动时间线（F053）查看。
**【用户故事】**系统哪里不对劲——诊断中心一眼看四项体检灯，红一项点进去看证据链，导出日志（脱敏勾选默认开）发给社区——**求助成本从「我会看日志吗」降到「我会点按钮吗」**。
**【交互设计】**窗口 880×600px：顶四体检灯卡（帧率/存储/调度/唤醒——F062 数据）；中部修复项列表（每项：名称/说明/风险级/执行钮——执行前说明做什么可回滚）；页签：快照/日志/时间线/修复。
**【数据与存储】**快照与日志数据源复用（F062/F188 三环日志）；导出包 zip 含 manifest（脱敏声明）。
**【状态与异常】**修复项执行失败 → 三要素+回滚自动（修复项全部设计为可回滚）；导出脱敏规则固定三查（路径用户段/序列号/密钥类）；日志量大（>100MB）→ 分卷导出。
**【验收判据】**四体检灯与 F062 数据一致；三修复项执行-回滚全链实测；脱敏导出三查通过（注入敏感样本验证）。
**【开源复用】**无外部依赖；日志格式开放（F126——社区工具可解析）。
**【设计细节】**修复项注册制（新增修复必须登记风险级与回滚方案——门禁）；体检灯三色语义文档化（绿=正常/黄=关注/红=建议处理+直链帮助 F119）；日志查看器内嵌（过滤/搜索/时间窗）；时间线页复用 F053 甘特组件。

### G-C-51 F121 系统还原点 · 完整设计

**【功能定义】**关键变更前自动快照配置层：装应用/改主题/更新/环境变量变更四触发；快照含：配置层文件+应用蜂巢+环境变量表+已装清单；设置中心一键回滚任意点；数据层不动（零真删）。
**【用户故事】**Variable 装了个改系统的工具把主题搞乱了——还原点列表选「装 XX 之前」，一键回滚——主题回来了，文件一张不少——**配置可以反悔，数据永远安全**。
**【交互设计】**设置中心「系统-恢复」页：还原点时间线列表（触发原因标签/时间/占用大小）；「创建还原点」手动钮；回滚前对比摘要（「将回滚：主题配置、3 个应用设置；不动：你的文件」）；回滚进度+完成确认。
**【数据与存储】**快照增量式（变更块存储），上限 10 个滚动（最旧自动清，可锁定 1 个）；快照存储独立区（自身损坏不影响系统）。
**【状态与异常】**快照期间断电 → 快照作废（系统本体无损）；回滚失败 → 自动重试一次后完整回滚本次回滚（二次回滚到回滚前——安全网套安全网）；空间不足 → 提示清理旧点。
**【验收判据】**四触发自动快照实测；回滚后配置层哈希与快照点一致；断电注入快照完整性百次。
**【开源复用】**增量快照思路参照 Btrfs/Btrfs 快照概念（ext4 上自研块级增量）；实现自研（存储栈上层）。
**【设计细节】**变更监测钩子清单（四触发点埋点位置文档化）；快照压缩存储（zstd 评估 F130）；锁定点防滚动清理（用户锁的永远在）；回滚不重启（配置层热加载——E1 即时生效机制复用）；快照占用面板显示（<2GB 预算）。

### G-C-52 F122 更新体验面 · 完整设计

**【功能定义】**系统更新的全体验流程：提示 toast（延后三档 1h/今天/手动）→ 下载进度（后台 F057 低优先）→ 安装预约（立即/关机时）→ 全屏更新画面（复用开机四幕语汇）→ 失败自动回滚+说明页。
**【用户故事】**更新 toast 来了——Variable 选「今天」，晚上关机时自动装；屏幕显示四幕动画+真实进度环；万一失败，开机后说明页讲清发生了什么、回滚到了哪、下一步——**更新全程无黑箱无惊吓**。
**【交互设计】**toast 含更新内容摘要（前 3 条+「详情」）；预约设置页（F190 双槽状态可视）；全屏画面：进度环+当前阶段文案（备份/写入/校验）+禁止断电提示（图形化）；失败页：原因归因+已回滚确认+重试钮。
**【数据与存储】**更新包缓存双槽区（F067 布局）；预约与偏好配置层。
**【状态与异常】**下载中断网 → 断点续传；电量 <20% → 推迟建议（F196 联动）；校验失败 → 包弃用重下；回滚后自动报备（F120 体检灯）。
**【验收判据】**正常更新全流程（含预约关机装）实测录屏；断电注入更新中 → 回滚成功且系统可用（B-1304 复测）；失败页文案三要素。
**【开源复用】**双槽机制本体 WP-404 既有（A/B 槽）；本项纯体验面。
**【设计细节】**进度环与真实安装阶段绑定（不骗人条款——四幕同纪律）；「关机时安装」在电源菜单（C-3 补节）加角标提示；更新中全程可取消（校验前）；完成后首启弹「更新了什么」卡（3 条内）；静默时段（免打扰）不弹 toast。

### G-C-53 F123 关于本机页 · 完整设计

**【功能定义】**设备诚实档案：设备名（可改）/系统版本与谱系（F199 联动）/硬件清单（CPU 型号/核数/频率/内存容量/盘容量与 health F183）/显示分辨率与缩放/每项可复制。
**【用户故事】**社区求助要报配置——关于本机页每行后面都有复制钮，或一键「复制全部」粘到帖子——**报配置 10 秒，不用拆机**。
**【交互设计】**设置中心「系统-关于」页：设备卡（壁纸缩略+名称+重命名行内编辑）；规格分组列表（处理器/内存/存储/显示四组，行高 65px 乙-3 表卡片行）；每行尾复制图标；「复制全部报告」尾部大钮（生成文本报告含格式）。
**【数据与存储】**只读数据即时采集（ACPI/PCI 表已有 F053 面）；设备名配置层。
**【状态与异常】**某项不可读（温度计类 graceful 既有纪律）→ 该行显「本机不可读」；规格采集异常 → 缺行不编造（诚实留白）；重命名即时生效（F011 COMPUTERNAME 同源）。
**【验收判据】**Y7000 实机规格全对（对照官方规格表）；复制-粘贴保真；全部行可读性走查。
**【开源复用】**无外部依赖；采集复用内核枚举面。
**【设计细节】**CPU 频率显示实时档（F048 联动小字「当前 2.5GHz」）；内存显示已装/可用双值；盘行含读写量累计（F183 寿命参考）；报告文本格式固定模板（社区互助标准格式 F139 联动）；页脚小字「数据每 30 秒自动刷新」。

### G-C-54 F124 动画曲线总谱 · 完整设计

**【功能定义】**全 UI 动画收敛为五条标准曲线（进入 ease-out/退出 ease-in/强调 ease-in-out/弹性 overshoot/线性）+ 三时长档（120/200/320ms）；总谱文档 `design/assets/motion-curves.md` 供全应用引用——动画是乐谱不是即兴。
**【用户故事】**十种界面动画在同一秒发生——它们的节奏像同一首曲子（进出有对仗、强调有呼吸）——用户说不出为什么，只觉得「这个系统很顺」。
**【交互设计】**无用户直接 UI（工程资产）；E8 动效强度档可整体缩放时长（完整 100%/减弱 60%/关闭 0）；开发者文档站（F135）收录总谱全文与曲线函数。
**【数据与存储】**曲线函数表编译进合成器+控件库（一处一事实：常量唯一源）；总谱 MD 版进资产库。
**【状态与异常】**未登记动画（新代码私加曲线）→ code review 门禁（grep 曲线常量表外值）；减弱档下必达动画（进度环）转显性进度数字（可感知冗余）。
**【验收判据】**全系统动画抽查 30 处曲线/时长全部落在总谱表内；三强度档切换全局生效实测。
**【开源复用】**曲线参数参照 Material Design easing 与 Apple HIG 时长指南（公开设计系统，语义对齐不抄值——VARIX 自定五曲线）；实现自研。
**【设计细节】**五曲线定义：进入 cubic-bezier(0.16,1,0.3,1)/退出 (0.7,0,0.84,0)/强调 (0.65,0,0.35,1)/弹性 105% 过冲 80ms 回弹/线性仅进度条；三档用途表（进入=200/微反馈=120/大面板=320——文档化）；曲线插值器统一实现（一处）；60fps 底线 80fps 目标下超预算动画自动降级直线（保帧率不保花活）。

### G-C-55 F125 体验域总判据 · 完整设计

**【功能定义】**宪章手感条款逐条映射到 F071-F124 验收锚点的总对账制度：20 维度走查脚本以本清单为母本更新；每功能验收写在功能旁边（本卷体例）——验收不集中各处，杜绝「验收是另一本册子」的脱节。
**【用户故事】**收官走查那天，走查人拿着本卷逐节打勾——每项的【验收判据】段就是打勾标准，证据链直接引用——**设计、实现、验收三本账合一本**。
**【交互设计】**走查脚本工具化：验收锚点编号提取脚本生成 checklist（`tools/vx-walkcheck-c.py` 形态，走查材料族既有惯例）；不达标项自动回流加塞审批（MD3 附录 J 纪律）。
**【数据与存储】**走查结果归档验收目录（`docs/acceptance/` 惯例）；版本随设计案版本绑定。
**【状态与异常】**验收锚点与实现脱节（功能改了判据没改）→ 三册腐化 R8 流程（周对账抽查）；锚点不可测（写不出命令）→ 设计缺口回炉（卷首·丙纪律）。
**【验收判据】**映射覆盖率 100%（55 项每项至少一条可执行判据）；走查脚本生成即跑通（冒烟）。
**【开源复用】**无外部依赖；checklist 生成脚本自研（Python 工具族惯例）。
**【设计细节】**映射表三列（F 编号/宪章条款/判据编号）自动生成+人工复核双轨；20 维度特别加权（第九章）引用本卷编号而非复述（一处一事实）；季度走查随生态季报（F149）节奏；C 域报告卷随本判据一并冻结（v1.0 里程碑件）。


---

# 第 4 部分 · D 生态与开放域（目标④）

> 域目标：开放性逼近大型作品，分发是开放网络的事。

原则三条：**不建软件商店**（可以从 Edge 或任何浏览器下载，管线见第 1 部分 A-2）；星图退回本职（兼容性评级目录，给实测数字不做货架）；判例、差异表、vxapp 格式、星图目录全部开放文档化。

---


## D-2 深化设计报告（F126-F150 报告式全展开）

> 以下 25 份报告为 D 域全部功能项的报告式展开——七段模板见卷首·丙；每项末尾【设计细节】段含参数级、像素级、判据级补深。D 域的共同底色：**开放是可测量的无障碍程度，不是口号**（F150 总判据）。

### G-D-01 F126 开放格式宪法页 · 完整设计

**【功能定义】**四份核心格式规范的公开文档页：vxapp 包格式/vxtheme 定制包/星图 JSON 目录/判例提交格式——每份含：版本号、完整 schema、示例包、变更历史；版本化承诺（破格必双读过渡）写进规范正文。
**【用户故事】**第三方开发者深夜想给 VARIX 做工具——打开格式页，schema + 可下载示例包 + 五分钟上手文，不用问人不用猜——**格式的门槛就是生态的门槛**。
**【交互设计】**帮助中心（F119）内设「开发者-格式规范」分区；每规范页结构统一：概览 200 字/Schema 表格/示例下载钮/变更历史表/「讨论入口」外链；页内 schema 字段表支持锚点跳转。
**【数据与存储】**规范 MD 源随代码库版本化（F135 文档站同源）；示例包 CI 自动重打（永不腐烂）。
**【状态与异常】**schema 与实现脱节 → CI 校验（示例包过实现校验即文档正确）；规范更新 → 变更历史强制记条目（无记录不合入）。
**【验收判据】**四规范页齐全可访问；示例包全部通过当前实现校验；双读过渡承诺条款在册。
**【开源复用】**schema 表达采用 JSON Schema 标准（公共规范）；页面渲染复用 F119 MD 管线。
**【设计细节】**版本号语义：主版本=破格/次版本=加字段/修订=改文档；破格流程 ADR + 迁移窗一个季度；示例包四份各 ≤100KB（分钟级上手）；规范页打印样式友好（导出 PDF 走 F025）；四格式互相引用关系图（一张图讲清数据流）。

### G-D-02 F127 vxapp 打包工具 · 完整设计

**【功能定义】**一条命令打包：`vxapp pack <目录>` 自动生成清单骨架（补默认值）/计算哈希/签名（用户密钥）/输出 .vxapp；工具开源（F126 格式的参考实现）；十分钟心智成本承诺。
**【用户故事】**开发者写完一个小工具——`vxapp pack` 回车，60 秒后 .vxapp 出现在目录，直接发给朋友或传社区——**打包不该比写程序还难**。
**【交互设计】**CLI 为主（终端 F095 场景）：进度/警告/产物路径三输出；`vxapp validate` 独立校验命令（打包前后自查）；`--interactive` 向导模式（问答式补清单，新手路径）；错误输出带修复建议（三要素纪律的 CLI 版）。
**【数据与存储】**清单骨架模板内嵌；签名密钥对生成与保管指引（`vxapp keygen`）；打包缓存（重复打包跳过未变文件）。
**【状态与异常】**目录缺必需文件（主程序）→ 指出缺什么+示例；哈希计算中断 → 断点续算；签名失败 → 密钥排查指引；产物校验失败 → 工具 bug 报告通道（F139）。
**【验收判据】**五个不同形态目录（单 exe/带资源/带运行时/带图标/最简）打包全绿；产物在系统侧安装-运行-卸载全通（与 F030 闭环）；分钟级承诺实测（五例均 <10 分钟含学习）。
**【开源复用】**签名评估 minisign 算法思路（ed25519 公共密码学）；工具本身自研开源（MIT 建议许可，F130 登记）。
**【设计细节】**清单字段必填三项（id/版本/主程序）其余默认值全文档化；哈希 SHA-256 全文件+分块双计；签名覆盖清单+内容树；产物格式版本戳（F126 双读依据）；CLI 退出码规范（0 成功/1 用户错/2 内部错——脚本友好）。

### G-D-03 F128 星图开放数据面 · 完整设计

**【功能定义】**兼容性评级目录全量 JSON 可下载：星卡（评级/判例通过率/启动画像 F043）/「常用 50 件」账本（F040）/更新时间戳；无账号无门槛；社区镜像自建合法（数据可自由再分发）。
**【用户故事】**一个独立开发者把星图数据做成自己的第三方查询站——每周同步一次 JSON，加自己的 UI——VARIX 不拦不收钱不加锁，生态数据是公共品。
**【交互设计】**数据入口三处：帮助中心页下载钮/星图应用「导出数据」/固定 URL（版本化路径）；JSON 结构与 F126 规范页 schema 一致；分页拉取（增量按时间戳）。
**【数据与存储】**服务端（或镜像）JSON 快照按日生成；文件签名（F127 同算法）防篡改；历史快照保留 90 天。
**【状态与异常】**下载中断 → 断点续传；签名校验失败 → 客户端弃用+告警；数据量增长 → 按类别分文件（单文件 <50MB 保可用）。
**【验收判据】**全量下载-导入-查询闭环实测；签名校验双向（篡改样本必拒）；第三方镜像指南文档化（本报告即指南）。
**【开源复用】**无外部依赖；JSON 结构复用 F126 schema；签名复用 F127 面。
**【设计细节】**快照生成脚本进 CI 日任务；增量协议：`?since=<ts>` 返回变更集；每星卡带数据来源标注（AI01 实测/社区提交/自动草稿 F036——可信度分级）；许可声明 CC-BY（标注来源可自由再分发，F130 法律面）。

### G-D-04 F129 社区判例提交线 · 完整设计

**【功能定义】**社区提交判例的完整通道：自测脚本包（只读形态，B-430x 既有纪律）/提交模板/状态全链可查（B-4303）/验收从单 AI 裁决变为社区共证（两人复核制）。
**【用户故事】**社区成员跑通一款新软件——用自测脚本包一键采证，模板填好提交，两周后在星图看到自己的判例（署名在列）——**生态的正门永远开着**。
**【交互设计】**提交页（星图应用内）：模板表单（程序名/版本/判例集/证据哈希）/进度查看页（五态：已收/形式审/技术审/共证/收录，各态时间戳）；驳回必附原因（三要素）。
**【数据与存储】**提交走 HTTPS（F024）；状态数据公开（F128 面引用）；证据包哈希链（F194 同款防篡改）。
**【状态与异常】**证据不全 → 形式审驳回+清单化缺项；复核分歧 → 第三人仲裁（流程文档化 F148）；刷量防护 → 同 IP 限频+质量分。
**【验收判据】**端到端演练：外部视角提交一判例到收录全流程计时（目标 <30 天）；五态查询实时准确。
**【开源复用】**自测脚本包复用 F040 账本框架；状态机自研（B-4303 既有承诺兑现）。
**【设计细节】**两人复核制：AI01 一票 + 社区轮值一票（轮值池规则入 F148 治理文档）；证据哈希上链式串联（提交即锁定不可改）；收录即进 F040 账本候选池；每季「判例之星」致谢（F142 致谢页面）。

### G-D-05 F130 开源项目登记册 · 完整设计

**【功能定义】**全部借力件的公开账本：组件名/版本/许可证/改动面/升级窗日期/上游地址/用途——十三件借力纪律（MD1 第 18 章）的执行账本，公开可查。
**【用户故事】**社区贡献者想确认「VARIX 里的 rustls 是什么版本、有没有改过」——登记册一行查清，许可证合规一目了然——**用开源的诚意是让所有人看得见账**。
**【交互设计】**帮助中心「开源与许可」分区：登记册表格页（可筛选/搜索）+ 各组件详情页（版本历史/改动 diff 摘要/许可证全文链接）；系统内「关于本机」（F123）尾部入口。
**【数据与存储】**登记册数据文件随仓库版本化（YAML 惯例）；许可证全文快照存档（防上游删文）。
**【状态与异常】**新借力件未登记 → CI 门禁（依赖清单与登记册 diff 为零才合入——门禁不是良心的连续版）；许可证变更（上游换许可）→ 升级窗评估法律面+ADR。
**【验收判据】**登记册与镜像实际组件清单一致性 CI 校验零 diff；许可证兼容性法律面过审记录在册。
**【开源复用】**登记格式参照 SBOM（SPDX 惯例——公共标准）；实现即文档。
**【设计细节】**登记字段 12 项固定 schema（F126 格式面）；许可证族分类标注（MIT/Apache/LGPL/GPL-隔离……）；GPL 件必标「进程隔离」策略（F112 eSpeak 先例）；季度升级窗日历与登记册联动（F138）。

### G-D-06 F131 上游回馈通道 · 完整设计

**【功能定义】**对借力件（smoltcp/ext4 crate 等）的修复按开源礼仪回馈：PR 模板/CLA 说明/回馈记录册——**用开源也养开源**，回馈是义务不是施舍。
**【用户故事】**AI01 修了 smoltcp 一个边界 bug——按模板向上游提 PR，两周后合入——回馈记录册添一行，下季度季报（F149）讲这个故事——VARIX 是开源社区的好公民。
**【交互设计】**无用户 UI；回馈记录册公开页（F130 分区）；PR 模板与流程文档在仓库 `docs/upstream/`。
**【数据与存储】**回馈记录：组件/PR 链接/状态/合入版本；随仓库版本化。
**【状态与异常】**PR 被拒 → 记录原因+本地补丁维护（fork 责任文档化）；上游无响应 90 天 → 维护分支公告；回馈与锁版本冲突 → 升级窗统一处理。
**【验收判据】**首批回馈 ≥2 件实际 PR 在册；流程文档完整（模板/CLA/礼节三件）。
**【开源复用】**流程参照主流项目 contribution guide 惯例；无运行时依赖。
**【设计细节】**PR 拆分纪律（一个逻辑一个 PR——上游友好）；commit 规范对齐上游惯例（入乡随俗）；回馈优先级：安全修复>正确性>性能——安全件走 F142 披露通道协调（同步披露窗口）。

### G-D-07 F132 差异表公开 · 完整设计

**【功能定义】**Win32 兼容层「不承诺清单」全量公开：未覆盖 API 族/语义差异点/已知行为差异——每条附：影响场景/替代方案/计划状态；与星卡（诚实评级）构成双面诚实体系。
**【用户故事】**开发者评估「我的程序能不能移植」——差异表一查：我用的 API 在/不在、语义差异影响我吗——**五分钟得出靠谱结论，不用装完才知道**。
**【交互设计】**帮助中心「兼容性-差异表」分区：按 API 族分页（窗口/GDI/网络/存储……）；每条：API 名/差异描述/影响级别（致命/降级/无感）/追踪编号；星卡页反向链接（「此程序受差异 X 影响」）。
**【数据与存储】**差异表数据随判例库版本化；来源双轨（实现中登记+用户报障转判例 F035→F036 管线）。
**【状态与异常】**差异被修复 → 条目转「已解决」保留（历史可溯）；新差异发现 → 判例流程进（不是悄悄改表）；影响级别判定标准文档化。
**【验收判据】**差异表条目与 F040 账本实测结果零矛盾（CI 交叉校验）；每条有追踪编号可查。
**【开源复用】**无外部依赖；表格式开放（F126）。
**【设计细节】**影响级别定义：致命=功能不可用/降级=可用但体验降/无感=理论差异；条目模板五字段（API/差异/影响/替代/状态）；季度刷新随账本；「已解决」条目保留计数进季报（兼容面成长曲线）。

### G-D-08 F133 第三方图标包规范 · 完整设计

**【功能定义】**图标包目录结构与格式规范：清单（名称/作者/版本/适用档位）/尺寸档位（32/48/96/256，乙-4 表对齐）/三态要求（常驻/悬停/按下）/命名规则；E3 导入即用（过格式校验）。
**【用户故事】**图标设计师做完一套 200 枚图标——按规范排目录、填一行清单、打 vxtheme 包——别人导入即换——**设计师的作品十分钟即可上架 VARIX 桌面**。
**【交互设计】**规范页（F126 面）+ E3 导入向导（校验失败逐条指出：缺哪个尺寸/哪个态/命名错在哪——修图指南式报错）。
**【数据与存储】**包结构：`icons/<名称>/<尺寸>/<状态>/<类别>.png` + `manifest.json`；校验器开源（F127 工具族）。
**【状态与异常】**缺档位 → 缺失档自动用邻近档重采样（标注降级）；缺状态态 → 用常驻态代替+标注；三态不齐提示作者补——**宽容导入，诚实标注**。
**【验收判据】**官方示例包 2 套过校验；社区模拟包 5 套（含故意错误）报错逐条准确。
**【开源复用】**PNG 标准；命名惯例自定（开放 F126）；校验器复用 vxapp validate 面。
**【设计细节】**类别清单固定（系统/文件夹/文件类型/托盘四族清单表）；位深要求 32bit（含 alpha）；4K 管线要求的 256px 档为必填（其余档可由校验器建议重采样输出）；版权字段必填（作者+许可——E3 导入时展示）。

### G-D-09 F134 主题分享页（非商店） · 完整设计

**【功能定义】**星图内「社区主题」分栏：纯目录（预览图+作者+说明+外部下载链接或 S: 侧载指引）——**发现好东西与不做商店兼容**：我们不托管不抽成不排序收钱，只做目录页。
**【用户故事】**用户想换套新主题——星图社区主题页翻到喜欢的，点作者的外链下载 vxtheme（浏览器 F126 管线）或邻居 S: 卷拷贝——导入即用；目录页没有评分没有推广位。
**【交互设计】**分栏列表卡（预览 176×96px + 名称 + 作者 + 版本）；筛选（风格标签）；条目详情：多图预览+说明+「获取方式」区（外链按钮/S: 路径指引二形态）；「我也想上架」指引链接（F133+F127 文档）。
**【数据与存储】**目录数据 JSON（F128 开放面一部分）；收录标准：格式校验过+作者申请+无恶意哈希——三条件文档化。
**【状态与异常】**外链失效 → 条目标「链接待更新」+举报钮；主题与系统版本不兼容 → 校验拦截+说明；发现恶意包 → 下架流程（F148 治理）。
**【验收判据】**首批收录 10 套（官方 3+社区 7 目标）；「非商店」三不承诺（不托管不抽成不卖位）写进页脚宪法句。
**【开源复用】**无外部依赖；页面复用星图既有前端面。
**【设计细节】**预览图规格统一 352×192（4K 出图缩放）；标签词表固定（极简/玻璃/暗色/暖色…12 词防标签膨胀）；收录审核 SLA 7 天；条目举报入口直连 F142 安全通道（恶意与审美分开处理）。

### G-D-10 F135 开发者文档站 · 完整设计

**【功能定义】**vx-SDK 文档/控件宪章/主题令牌表/交互词典/格式规范五区合一的开发者门户：每页带可复制示例、与代码同库同版本（文档漂移即 CI 红）。
**【用户故事】**第三方开发者从零起步——门户五区按序读完（约两小时）——装 SDK、跑示例（F136）、查词典（C-8 表）——**两小时从旁观者变成建设者**。
**【交互设计】**复用 F119 帮助中心渲染器（同引擎不同分区——一套工具两拨读者）；代码块一键复制；示例可下载（对应 F136 仓库链）；搜索跨五区（F071 引擎）。
**【数据与存储】**文档源随代码库（docs/ 惯例）；API 表从代码注释自动提取（可执行文档——CI 校验）。
**【状态与异常】**注释与文档冲突 → CI 红（文档即测试的纪律）；示例失效 → F136 夜跑兜底。
**【验收判据】**五区内容完整度核对清单全绿；API 提取覆盖率 >90%（公共接口口径）。
**【开源复用】**API 文档提取评估 rustdoc 思路（工具链适配）；渲染复用 F119。
**【设计细节】**五区固定结构：快速开始/API 参考/概念指南/规范/词典；每 API 页必含：签名/示例/错误码/版本史四段（模板门禁）；代码示例语法高亮（轻量着色器）；「本页对应源码」直链（透明度）。

### G-D-11 F136 示例应用仓库 · 完整设计

**【功能定义】**五个官方示例从 hello 到完整应用：①hello 窗口 ②按钮与布局 ③文件读写 ④多窗口与菜单 ⑤完整小应用（记事本级）；每步可跑、CI 夜跑保活（示例永不腐烂——烂示例比没示例更毒）。
**【用户故事】**新手照着示例②改十行——自己的第一个 VARIX 程序跑起来了——**学习曲线的每一格都有扶手**。
**【交互设计】**仓库 README 即索引（五示例各：一句话/截图/构建命令/预计学习时长）；每示例含注释版源码（教学注释密度 30%）；F135 门户直链。
**【数据与存储】**随主仓库子目录（`examples/`）；CI 夜跑构建+运行冒烟（QEMU 截图比对——示例窗口出现即绿）。
**【状态与异常】**示例因 API 变更坏 → CI 红即修（门禁）；平台差异（软渲染 vs 未来 GPU）→ 示例双验证。
**【验收判据】**五示例 CI 连续 30 天绿；新手实测（非作者）照 README 跑通 5/5。
**【开源复用】**无外部依赖；CI 面复用 F061 基准夜跑设施。
**【设计细节】**示例间递进关系文档化（②用①的知识+一点新——坡度设计）；每个示例 ≤200 行（超了拆）；构建命令统一 `vxapp run examples/02`（工具链 F127 复用）；截图基线进 CI 比对（UI 回归附带保护）。

### G-D-12 F137 API 稳定性承诺 · 完整设计

**【功能定义】**系统 API 两级标注：稳定级（破面需 ADR+迁移窗一季）/实验级（可变需注明）；标注三处可见：F135 文档页/SDK 头文件注记/运行时日志（实验 API 使用提示）。
**【用户故事】**第三方把应用建立在稳定 API 上——一年后系统升级应用照跑（迁移窗给足）；另一个用实验 API 的——升级提示「该 API 已变，迁移指南在此」——**承诺清晰，双方都睡得着**。
**【交互设计】**文档页 API 名旁徽标（稳定=实心盾/实验=虚线盾）；实验 API 首次调用日志一行提示（开发者可见不扰用户）；迁移指南随破面 ADR 发布。
**【数据与存储】**标注数据源=代码属性注记（提取进 F135 面）；破面历史册。
**【状态与异常】**稳定 API 紧急破面（安全）→ 走加速 ADR+双读过渡仍强制；误标（稳定级 API 悄悄变了）→ CI 签名比对门禁拦截。
**【验收判据】**公共 API 100% 有级别标注；CI 签名门禁生效（注入假变更被拦）；迁移窗条款在 ADR 中兑现一例。
**【开源复用】**分级思路参照 Android API level 与 Rust stability 惯例；标注实现自研。
**【设计细节】**API 签名指纹（参数类型序列哈希）CI 比对；稳定级最低存活一季才可授予（考察期）；实验级默认两年内转正或删除（防僵尸实验品）；破面通知三渠道（文档/日志/星图公告）。

### G-D-13 F138 版本发布节奏公开 · 完整设计

**【功能定义】**发布日历公开页：系统版本节奏（季度功能/月度修复）/借力件升级窗（季度，F130 联动）/重大变更提前一个窗公告——生态参与者可预期，不搞突然袭击。
**【用户故事】**第三方开发者看着日历排自己的发版——「下个升级窗 Wine 会动，我的测试计划提前排」——**可预期本身就是生产力**。
**【交互设计】**日历页（帮助中心分区）：时间轴视图（未来两个窗的内容预告+过去窗的归档）；公告条目带订阅（F139 反馈通道同邮件列表机制）。
**【数据与存储】**日历数据文件版本化；归档不可改（历史诚实）。
**【状态与异常】**紧急安全更新打破节奏 → 公告说明+补偿性预告；日期滑动 → 归档如实记录（不粉饰）。
**【验收判据】**连续两个发布窗按日历兑现或如实归档偏差；公告提前量 ≥1 窗实测。
**【开源复用】**节奏参照主流发行版发布火车模型；实现即文档。
**【设计细节】**窗口定义：升级窗=季度第 2 周（固定周次防漂移）；公告最小提前量 30 天；日历订阅格式 iCal 导出（通用日历可订）；每窗内容三栏（系统/借力件/破坏性变更预告）。

### G-D-14 F139 反馈闭环通道 · 完整设计

**【功能定义】**诊断报告（F120 导出）一键提交进社区 tracker：匿名化默认、处理状态回查（五态与 F129 同引擎）——反馈有去有回，才有人继续反馈。
**【用户故事】**用户遇到怪问题导出日志一键提交——三天后收到状态更新「已复现，正在查」——两个月后修复说明@了他的报告编号——**他的十分钟帮了所有人**。
**【交互设计】**F120 导出页尾「提交到社区」钮（预览脱敏内容→确认）；查询页：报告编号查状态；状态变更通知可选（回查式为主，不堆通知）。
**【数据与存储】**提交走 HTTPS；编号全局唯一（日期+序号）；状态数据公开（F128 面）。
**【状态与异常】**脱敏失败 → 提交拦截（敏感模式硬查 F120 三查）；重复报告 → 自动关联合并（相似哈希聚类提示）；恶意刷量 → F129 同限频。
**【验收判据】**端到端：导出-提交-状态更新-修复回链全流程演练；脱敏三查在提交路径强制。
**【开源复用】**tracker 后端评估轻量开源 issue 系统（或 F128 JSON 面自建——轻起步）；脱敏复用 F120。
**【设计细节】**报告模板结构化（现象/复现/环境三段必填）；状态五态与 F129 复用同引擎（一处一事实）；修复回链自动匹配（提交带版本指纹，修复版含指纹即关联）；季度「最佳报告」致谢（F142 面）。

### G-D-15 F140 本地化开放 · 完整设计

**【功能定义】**全 UI 文案抽离字符串表（JSON）：中文/英文首发内嵌；社区翻译包 vxtheme 承载（安装即换语言）；词条库规则（禁模糊词表联动——翻译也要过三要素审查）。
**【用户故事】**社区日语爱好者把系统翻成日文——打包 vxtheme 分享——日本用户导入后全界面日文——**多语言不是墙内私活是社区的礼物**。
**【交互设计】**设置中心「时间和语言-语言」页：已装语言列表/切换（即时生效热替换）/缺失词条回退标注（翻译覆盖度百分比诚实显示）；词条贡献指引链（F135 门户）。
**【数据与存储】**字符串表结构化（key/值/注释上下文——译者需要语境）；翻译包走 vxtheme phrases.json（F108 同管线）；覆盖度 <90% 标「测试版语言包」。
**【状态与异常】**缺词条 → 回退英文+控制台日志清单（社区可按单补翻）；占位符错乱（翻译丢 {n}）→ 运行时校验兜底回退；RTL 语言前瞻接口（布局镜像评估项）。
**【验收判据】**双语切换全界面走查（30 关键页零漏翻零硬编码——B-1104 纪律的语言面）；翻译包导入导出 round-trip 无损。
**【开源复用】**无外部依赖；词条 schema 开放（F126）。
**【设计细节】**词条 key 命名规范（域.页面.用途三级）；上下文注释强制（「按钮」「标题」用途标注）；文案审查双关：中文源过禁模糊词表（卷首·甲）、译文过等义审查（模板在 F135）；热替换无重启（E1 同机制复用）；数字/日期格式随区域（F187 联动）。

### G-D-16 F141 无障碍开放标准 · 完整设计

**【功能定义】**无障碍五判据实现指南公开：焦点环登记协议/还焦点规则/对比度门禁公式/色弱形状冗余表/减弱动效接口——第三方应用照做即合规（F144 徽标前提）。
**【用户故事】**第三方开发者想让应用拿徽标——指南页五节照做，自测脚本一跑全绿——**合规不靠感觉靠 checklist**。
**【交互设计】**F135 门户「无障碍」区：五节指南 + 自测脚本下载（`vxapp a11y-check`，F127 工具族）+ 常见错例集（图解对错对比）。
**【数据与存储】**指南随文档版本化；脚本与 CI 门禁同源（WP-207 既有面提取）。
**【状态与异常】**指南与门禁实现漂移 → CI 交叉校验（指南引用的阈值即代码常量）；新判据加入 → 指南先行评审。
**【验收判据】**按指南做的新应用一次过门禁（实测 3 例）；自测脚本与系统门禁判定一致率 100%。
**【开源复用】**公式引用 WCAG 2.1（对比度）/WAI-ARIA（焦点语义）公共标准；脚本自研。
**【设计细节】**焦点环登记协议：控件注册时声明环样式令牌（E1 主题联动高对比加粗 F113）；还焦点规则三场景文档化（对话框关闭/窗口切换/应用恢复）；对比度公式直接引用 4.5:1 正文 3:1 大字双阈值；每节末「一分钟自测」代码片段。

### G-D-17 F142 安全披露通道 · 完整设计

**【功能定义】**安全漏洞报告三件：security.txt（RFC 9116 标准文件）/报告邮箱（PGP 可用）/致谢页（贡献者名单+处理时限承诺：48h 确认/90 天披露窗）。
**【用户故事】**安全研究者发现一个 peblock 门绕过——按 security.txt 联系，48 小时内收到确认，90 天协调披露后致谢页添名——**专业的报告值得专业的接待**。
**【交互设计】**security.txt 部署在文档站标准路径；致谢页公开（名字+贡献类型+日期，匿名选项尊重）；处理流程页公开（接收/确认/修复/披露四步时限）。
**【数据与存储】**报告走加密邮件；流程状态内部册（不公开细节只公布时间线）。
**【状态与异常】**报告无效 → 礼貌说明+归类；重复报告 → 合并致谢首报者；超时未修（90 天）→ 按承诺披露（对自家施压的宪法条款）。
**【验收判据】**security.txt 格式校验过；演练全流程（内部红队样本）时限达标；致谢页更新机制实测。
**【开源复用】**RFC 9116 直接采用（公共标准）；PGP 基础设施评估 GnuPG（F130 登记）。
**【设计细节】**披露窗 90 天可协商延长（复杂修复）但需说明；安全修复随月度版发（不等季窗——安全例外 F138 节奏条款）；致谢页排序按首个报告时间；协调披露文档模板公开。

### G-D-18 F143 星徽与品牌资产包 · 完整设计

**【功能定义】**官方品牌资产包开放下载：LOGO（多分辨率+单色变体）/星徽图标/官方壁纸精选/使用条款（可/不可用清单）——社区创作有官方素材可用，品牌不被滥用。
**【用户故事】**社区UP主做 VARIX 介绍视频——资产包里拿 LOGO 与壁纸，条款页确认「允许非商业展示」——**好用、好看、不越界**。
**【交互设计】**帮助中心「品牌资产」页：资产预览网格 + 单项下载 + 条款区（允许：非商业展示/评测引用；禁止：篡改主色/暗示官方背书——清单式条款）。
**【数据与存储】**资产包 vxtheme 兼容容器（复用管线）；版本随系统品牌更新。
**【状态与异常】**条款争议 → F148 治理流程裁决；滥用发现 → 沟通下架流程（先礼后兵文档化）。
**【验收判据】**资产包全分辨率齐（含 4K 原生）；条款页法律面过审。
**【开源复用】**无外部依赖；资产本体 4K 管线产出（C-7）。
**【设计细节】**LOGO 五尺寸（16/32/48/128/512+矢量源）；单色变体三色（白/黑/强调色）；壁纸 10 张精选（4K）；条款页含「图标正确用法示例」图解（间距/底色规范——品牌一致性也是作品集）。

### G-D-19 F144 「Crafted for VARIX」徽标计划 · 完整设计

**【功能定义】**质量徽标：通过控件宪章默认合规（B-1103）+ 无障碍五判据（F141）+ 三要素文案审查的应用授予徽标展示权；徽标三处可用（应用关于页/下载页/星卡）；撤销机制（质量回退即撤）。
**【用户故事】**认真打磨 UI 的第三方应用获得徽标——用户在下载页看到「Crafted for VARIX」知道这应用遵守手感宪法——**质量被看见，投入就有回报，生态飞轮转起来**。
**【交互设计】**申请页（F135 门户）：自测脚本预跑→提交→审核 SLA 14 天→徽标发放（文件+嵌入指引）；徽标展示规范（尺寸/底色/间距图解 F143 同款）。
**【数据与存储】**授予册公开（F128 面）；徽标验证 = 签名文件（可离线验真防伪造）。
**【状态与异常】**后续版本质量回退 → 撤销+通知+申诉通道；徽标伪造 → 签名验真拦截+F142 报告；标准升级 → 已授徽标宽限一季复检。
**【验收判据】**首批授予演练 3 应用全流程；签名验真双向通过；撤销路径实测。
**【开源复用】**无外部依赖；签名复用 F127；自测复用 F141 脚本。
**【设计细节】**徽标资产三形态（横版/方版/单色）；申请免费（质量导向不收费——与商店模式的根本区别写进条款）；审核内容四查（合规/无障碍/文案/隐私声明）；徽标有效期随系统大版本（复检制保鲜活）；授予册数据进季报（F149 生态健康指标）。

### G-D-20 F145 教育/作品集友好 · 完整设计

**【功能定义】**VARIX 自身作为公开作品：三册图纸+本设计案脱敏版公开读物化——架构决策/判据纪律/设计演化为可学习材料；「跟 VARIX 学造系统」栏目（帮助中心分区）。
**【用户故事】**操作系统课的学生读到 VARIX 的防自锁闸门设计与断电百次判据——课本上的概念第一次有了真实的工程血肉——**VARIX 不仅是作品，还是教材**。
**【交互设计】**帮助中心「作品集」分区：脱敏文档列表（按主题：引导/存储/兼容/体验）/设计演化时间线（ADR 精选导读）；每篇带「为什么这样设计」决策注解。
**【数据与存储】**脱敏管线构建期自动（剥离实机序列号/路径敏感段——三查复用）；源文档与公开版 diff 可溯。
**【状态与异常】**脱敏漏检 → 抽查流程（每版人工复核 20 处）；文档更新 → 公开版随版本窗同步（F138 节奏）。
**【验收判据】**首批公开 20 篇（四主题各 5）；脱敏三查通过率 100%（注入敏感样本测试）。
**【开源复用】**无外部依赖；内容即三册资产复用。
**【设计细节】**脱敏规则：实机编号/账号/路径用户段/内部密钥四类硬删；导读由 AI01 撰写决策注解（保留原 ADR 编号可溯）；文章分级（入门/进阶/深水区）；学生反馈通道进 F139。

### G-D-21 F146 插件化星图后端 · 完整设计

**【功能定义】**星图目录数据源可替换：本地文件/官方源/社区镜像三源可配（格式不变 F126）；优先级与校验链（签名）不变——**中心化不是架构假设**。
**【用户故事】**某个地区的用户访问官方源慢——社区镜像源切上，星卡照查——数据源的自主权在用户手里。
**【交互设计】**星图应用设置页「数据源」：三源单选+自定义 URL；切换即时生效；每源显示签名校验态与最后同步时间；失败自动回退下一源（顺序可调）。
**【数据与存储】**源配置配置层；本地缓存各源独立（不混写——防污染）。
**【状态与异常】**源签名失败 → 弃用+告警（不静默降级到未校验数据）；全部源不可达 → 本地缓存只读可用（降级诚实标注）；自定义源警告确认（自担风险显式化）。
**【验收判据】**三源切换查询结果一致（同版本数据）；回退链实测；签名拦截恶意源样本。
**【开源复用】**多源思路参照 apt 源机制；实现自研。
**【设计细节】**源健康检查每 6h（后台低优先）；镜像搭建指南（F128 联动）；缓存按源隔离目录；官方源默认首位（用户可改——尊重但不误导）；同步差量协议复用 F128 since 参数。

### G-D-22 F147 跨设备主题同步 · 完整设计

**【功能定义】**vxtheme 包+U 盘整机的天然移动性产品化：个性化档案（F161）随盘走——插任何机器，壁纸/主题/图标/短语全套就位，「你的桌面已就位」欢迎提示显性化。
**【用户故事】**Variable 把 U 盘插到机房电脑——登录 VARIX 十秒后：自己的壁纸、自己的深色主题、自己的输入法短语全在——**换硬件不换系统，这是 U 盘整机的灵魂**。
**【交互设计】**首次插入检测 → 欢迎卡（桌面已就位+个性化项清单勾选态）；「同步到本机」与「仅本次」二选（机房场景尊重公共机器）；设置中心「个性化-随身同步」页管理。
**【数据与存储】**档案随 DATA 分区（本就随身——本项是把既有事实做成显性体验）；「仅本次」不写宿主残留（退出清理临时态）。
**【状态与异常】**宿主分辨率不同 → 壁纸自适应裁切（构图保护）；资产缺失 → 本地重建默认+标注；多 U 盘冲突 → 最后插入优先+提示。
**【验收判据】**跨机器插拔全链录屏（含欢迎卡/就位/退出清理）；壁纸自适应 20 档分辨率实测；「仅本次」零残留验证。
**【开源复用】**无外部依赖；档案即 F161 导出格式。
**【设计细节】**就位动画：桌面元素错落淡入复用 C-2 幕四「呼吸」（一套语汇两处用）；欢迎卡出现条件=检测到档案且本机无个性化（三思不扰常客）；临时态清单（写过的缓存位置）退出逐清（审计日志对拍）；同步耗时目标 <10s（资产按需流式 F068）。

### G-D-23 F148 社区规则与治理 · 完整设计

**【功能定义】**三份治理文档公开：星图收录标准（什么能进目录）/徽标授予标准（F144 细则）/争议仲裁流程（谁说了算、怎么申诉）——规则先于规模，治理前置防社区腐化。
**【用户故事】**社区成员对某个条目收录有异议——仲裁流程写得明明白白：提出→复核→三人组裁决→公示——**有规则的社区才走得远**。
**【交互设计】**帮助中心「社区」分区三文档 + 治理公告栏（裁决记录公开）；申诉入口在每条目页脚。
**【数据与存储】**文档版本化（修订留痕）；裁决记录册 append-only（F194 序号链同款）。
**【状态与异常】**规则漏洞（未覆盖情形）→ 临时裁定+规则修订提案（规则也走 ADR）；裁决分歧 → 升级 Variable 终裁（宪法级兜底）。
**【验收判据】**三文档齐全过审；一例完整仲裁演练（模拟争议全流程）；裁决记录公开可查。
**【开源复用】**治理结构参照 RFC 与主流开源社区仲裁惯例；无运行时依赖。
**【设计细节】**收录标准量化（格式过验+无恶意+功能真实三硬条件，审美不设限）；仲裁组构成：AI01+轮值社区 2 人（与 F129 复核池同池）；裁决时限 14 天；申诉一级终级两级（不无限递归）；规则修订频率上限（季一次——防规则抖动）。

### G-D-24 F149 季度生态报告 · 完整设计

**【功能定义】**季报四指标公开：兼容件数（F040 账本）/社区提交数（F129）/借力件版本变动（F130）/徽标授予数（F144）——生态健康度自己先晒，数字说话。
**【用户故事】**关心 VARIX 生态的人每季读一页报告——四条曲线、三个故事、一份下季预告——**生态的体温计自己量给别人看**。
**【交互设计】**帮助中心「生态报告」分区：本期报告页（四指标卡片+趋势图+两三条季度故事）+ 历史归档；PDF 导出（F025 打印面）。
**【数据与存储】**数据自动汇总自各账本（F040/F129/F130/F144 引用不复制）；归档版本化。
**【状态与异常】**指标下滑 → 如实呈现+归因（不挑好看的说——诚实纪律的生态版）；数据源异常 → 指标标「数据待修」。
**【验收判据】**连续两季按期发布；四指标与源账本一致（交叉校验）；故事内容真实可溯（编号引用）。
**【开源复用】**无外部依赖；图表渲染复用监视器组件族（B-2001 同源）。
**【设计细节】**发布日：每季首月第一周（F138 日历挂账）；报告模板固定（四指标+故事+预告三段）；趋势图 8 季度窗口；故事配图用 F143 资产（视觉统一）；报告自身进 F128 开放数据（JSON 版同步）。

### G-D-25 F150 生态域总判据 · 完整设计

**【功能定义】**开放性总验收：外部人 30 分钟内走完「下载规范→打包应用→提交判例→查到状态」全流程计时，超时即修流程——**开放不是口号是无障碍程度的实测值**。
**【用户故事】**每季度找一位真·外部志愿者（没碰过 VARIX 的人）按文档走全流程并秒表计时——哪一步卡壳就修哪一步的文档或工具——生态的门槛用秒表量。
**【交互设计】**无用户 UI（验收制度）；结果进季报（F149 第五指标：外部人全流程耗时曲线——逐季应下降）。
**【数据与存储】**计时记录归档；卡壳点清单回流 F135/F127 改进。
**【状态与异常】**志愿者缺席 → AI01 自测降级并标注（不可比口径说明）；流程大改 → 基线重置（标注断点）。
**【验收判据】**首轮 30 分钟线达成；逐季不回退（新流程重新计时）。
**【开源复用】**无外部依赖；计时即人工秒表+流程检查单（模板公开）。
**【设计细节】**四步定义细化：规范页找到 ≤5min/打包成功 ≤10min/判例提交 ≤10min/状态可查 ≤5min；志愿者画像记录（开发经验年限——可比性）；卡壳点分级（文档缺/工具错/流程绕三类）；每季至少 1 名外部志愿者（招募启事进 F139 通道）。

---

# 第 5 部分 · E 个性化与定制域（Variable 2026-09-25 指定新增）

> 域目标：系统「长成用户自己的样子」——一切个性化**随时可改、改完即生效、随时可退回**。定制不是设置一次就锁死的开关，是活的。

## E-1 总原则（本域铁律）

1. **随时可改**：所有个性化项不改系统文件、不动镜像——配置独立存放，改了立刻生效，无需重启、无需重装；
2. **随时可退**：每一项都有一键「恢复默认」；用户自定义前系统自动保留上一版，改坏了随时回退；
3. **预设 + 微调双层**：每项先给一套官方预设（按设计宪法 3A 质感做），用户可在预设基础上逐项微调——不从零开始，也不被预设锁死。

## E-2 功能清单

| 编号 | 功能 | 定制内容 | 生效方式 |
| --- | --- | --- | --- |
| E1 | 主题换装 | 整套配色/窗口材质/毛玻璃强度/圆角半径/强调色，多套官方主题，支持逐项微调 | 即时生效，全局换装不重启 |
| E2 | 壁纸系统 | 静态壁纸（4K 原生）/纯色/幻灯片轮换/按时间换（晨昏自动切），支持用户自己的图 | 即时生效 |
| E3 | 图标包 | 系统图标包可整包更换，支持用户导入第三方图标包；单图标可单独换 | 即时生效，逐项可回退 |
| E4 | 指针方案 | 指针配色/大小/动效方案多套，支持导入 | 即时生效 |
| E5 | 声音方案 | 开机音/提示音/通知音/错误音整套可换可静音，单项可自定义 | 即时生效 |
| E6 | 任务栏与开始菜单布局 | 任务栏位置/图标固定区/开始菜单磁贴排布，支持布局预设一键切换 | 即时生效 |
| E7 | 字体与字号 | 界面字体选择/全局字号缩放（对齐 Windows 缩放档位）/等宽字体指定（终端用） | 即时生效，实时预览 |
| E8 | 动效强度 | 全局动效三档（完整/减弱/关闭，对齐无障碍减弱动效条款）+ 单类动效开关 | 即时生效 |

## E-3 定制包（个性化的分享形态）

- 用户的一整套个性化（主题+壁纸+图标包+指针+声音）可打包为 **vxtheme 定制包**——格式走 vxapp 扩展区，天然兼容 A4 侧载管线；
- 定制包可从浏览器下载、可导出分享（D 域开放性的体验面）——**别人的一键导入，我的随时导出**；
- 导入前预览、导入后可整包回退——分享与安全同规则（过 peblock 门与签名校验，未签名如实标注）。

## E-4 域内落点与验收

- E1-E8 的施工面全部落在第 3 部分 C 域各节之上（C-3 桌面/任务栏、C-7 4K 资产管线）——个性化改的是「皮」，C 域定的是「骨」，骨不改皮常换；
- 主题令牌纪律（B-1104 零硬编码）是本域的技术前提：所有个性化项通过主题令牌体系实现，令牌体系就是「随时可改」的工程保障；
- 验收附加条款：每一项个性化必须实测「改→生效→回退」三步全通过，任何一项改完要重启才能生效的，按不达标处理。

---


## E-5 深化设计报告（F151-F170 报告式全展开）

> 以下 20 份报告为 E 域全部功能项的报告式展开——七段模板见卷首·丙；每项末尾【设计细节】段含参数级、像素级、判据级补深。E 域共同底座：主题令牌体系（F151）与三铁律（随时可改/随时可退/预设+微调）——**每项验收必过「改→生效→回退」三步**。

### G-E-01 F151 主题令牌全集 · 完整设计

**【功能定义】**五类令牌全表：颜色（语义色 24 枚：背景三层/前景两层/强调/成功/警告/危险/边框三档/禁用…）/间距（乙-1 表六档）/圆角（三档）/字号（四档乙-1 表）/动效（F124 五曲线三时长）；JSON Schema 发布（F126）；E1 换装=令牌表热替换。
**【用户故事】**第三方主题作者拿到令牌表——改 24 个颜色值就能做出完整协调的主题，不用碰任何代码——**令牌是设计系统的原子，也是开放的接口**。
**【交互设计】**E1 主题页「高级-令牌表」：五类分组树 + 每令牌行（名/当前值/默认值/色板块或数值输入）；改值即时全局预览；「恢复此令牌默认」单项回退。
**【数据与存储】**令牌表 JSON（schema F126 发布）；主题包=令牌表+资产引用；热替换走 E1 机制（无重启）。
**【状态与异常】**非法值（色值格式错/间距不在档）→ 校验拦截+指出；令牌缺失（旧版主题）→ 默认值兜底+覆盖度标注；循环引用 → 拒绝保存。
**【验收判据】**24 色令牌全系统覆盖抽查（30 界面零硬编码——B-1104 联动）；热替换全程无重启无闪烁；JSON schema 校验双向通过。
**【开源复用】**令牌命名参照 Design Tokens Community Group 公共提案（语义层命名惯例）；实现自研。
**【设计细节】**语义色不允许直接引用物理色（主题作者只写语义层——换主题才可能全局一致）；间距令牌即乙-1 表六档（一处一事实）；动效令牌含曲线函数与时长双值；令牌表带版本戳（旧主题兼容矩阵文档化）；导出含「未覆盖令牌清单」（作者自查）。

### G-E-02 F152 实时预览编辑器 · 完整设计

**【功能定义】**设置中心「个性化」页的所见即所得编辑：改令牌/换壁纸/调圆角——右侧预览区即时重演（预览区=迷你桌面：任务栏+窗口+图标三件套）；「应用/放弃」双钮确认制。
**【用户故事】**用户把圆角从 8 拖到 16——预览里窗口立刻变圆润——满意点应用全桌生效，不满意点放弃原样复原——**改错了也毫无成本，这才是敢玩的心理基础**。
**【交互设计】**编辑页左右分栏（左控件右预览，预览宽 400px 固定）；预览区渲染走真实渲染管线缩小版（非贴图——所见即所得是真话）；未应用态顶栏黄条「有未应用的更改」；放弃=逐令牌回滚（快照于进入编辑时）。
**【数据与存储】**编辑态快照内存；应用写令牌表+还原点触发（F121 联动）。
**【状态与异常】**改后崩溃 → 未应用更改自然丢弃（确认制保证系统态干净）；预览自身异常 → 提示+主界面不受染；并发改（多设置窗）→ 后写覆盖+提示。
**【验收判据】**改-预览延迟 ≤100ms；应用-全局生效无重启；放弃-零残留（令牌表哈希还原验证）。
**【开源复用】**无外部依赖；预览复用合成器离屏面（F073 管道）。
**【设计细节】**预览缩放 0.5 倍实时（渲染成本减 75%）；应用过程动画 300ms 交叉淡入（F124 强调曲线）；编辑器打开即自动快照（退出未应用自动还原——双保险）；预览区可切换三场景（桌面/资源管理器/设置自身——三类典型界面看效果）。

### G-E-03 F153 主题深浅自动切换 · 完整设计

**【功能定义】**深浅主题对绑定切换：按时间（用户设）或日落日出（F116 联动）自动在深/浅主题间切换；切换动画 300ms 交叉淡入（跨零点无闪烁）；每主题对各自配壁纸组。
**【用户故事】**白天清爽浅色、晚上安静深色——日落那刻系统自己换了装，300ms 柔和过渡，Variable 抬头时已是夜间模式——**系统比他更记得天黑了**。
**【交互设计】**E1 页「自动切换」区：开关+模式二选（定时/日落日出）+主题对绑定器（深色主题下拉×浅色主题下拉）；切换前 60s 预告 toast（可取消本次）。
**【数据与存储】**绑定对与模式配置层；日落时间引用 F116 城市数据。
**【状态与异常】**主题对中某主题被卸载 → 切换跳过+提示重绑；跨零点（23:59 浅→00:01 深）无重复触发（状态机去抖）；手动切主题 → 自动切换暂停当日（尊重手动——次日恢复）。
**【验收判据】**定时切换三连测准时（±5s）；交叉淡入无白屏帧（录屏帧检）；手动优先逻辑实测。
**【开源复用】**无外部依赖；切换即 E1 热替换（F151 机制）。
**【设计细节】**淡入实现=双层壁纸交叉（任务栏/窗口令牌同步替换在动画中段原子完成）；预告 toast 60s 含「立即切换/今晚跳过」二钮；壁纸组跟随主题对（深浅各一张默认）；状态机文档化（四态：浅/深/切换中/暂停）。

### G-E-04 F154 壁纸每日一换 · 完整设计

**【功能定义】**壁纸轮换：本地图片池（用户选文件夹）/官方精选池（可选下载，无商店原则走 F134 目录）/每日定时换（默认 00:00）；切换动画 500ms 交叉淡入；构图保护（自适应裁切 F147 同算法）。
**【用户故事】**每天开机壁纸都不一样——全部来自自己选的相册文件夹——桌面每天都有一点新鲜感，而素材永远是自己喜欢的。
**【交互设计】**E2 壁纸页「每日一换」区：池选择（本地文件夹多选/官方池勾选）/更换时刻（00:00 默认可改）/「现在换一张」手动钮；当日壁纸缩略+「固定今天这张」钮（当天不换）。
**【数据与存储】**池配置与历史（最近 30 天壁纸记录防短期重复）；当日已换标记持久。
**【状态与异常】**池空（文件夹被清）→ 回退默认壁纸+提示；官方池下载失败 → 静默用本地池；重复抽取 → 近 7 天排除（记录驱动）。
**【验收判据】**定时切换三连测；交叉淡入 500ms 无撕裂；构图保护 20 档分辨率实测（主体不裁头）。
**【开源复用】**无外部依赖；下载走 F126 管线；裁切算法 F147 共用。
**【设计细节】**抽取算法：池内均匀随机+近 7 天排除表；4K 图即换即显依赖 F054 解码（≤150ms 线）；预加载次日壁纸（空闲时段 F049 窗口解码缓存——换的时刻零等待）；「固定今天」次日自动解除；多屏前瞻接口预留（各屏独立池）。

### G-E-05 F155 图标包热更换 · 完整设计

**【功能定义】**E3 承载：图标包整包热更换——不重启、失败回退上一包（三铁律）；系统图标+文件类型图标两类覆盖；与第三方包规范（F133）对接。
**【用户故事】**换上社区图标包的瞬间——桌面、任务栏、资源管理器所有图标一次换装（300ms 交叉淡入）；不喜欢？设置页「回退上一包」一点复原——**换装的自由建立在回退的自由上**。
**【交互设计】**E3 页包列表（已装包缩略预览条）；切换即时；失败场景全处理（缺图标回退官方包对应项+计数标注「本包覆盖 187/200」——诚实覆盖度）。
**【数据与存储】**当前包与上一包指针配置层；包资产按需装载（F068）；缓存键含包版本（F013 图标缓存联动）。
**【状态与异常】**包损坏半途 → 原子切换失败即整体回退（不留半套）；运行中应用窗口图标 → 下次刷新生效（显式说明不骗「全部即时」）；缓存重建进度通知。
**【验收判据】**热更换全程 <2s 无重启；回退路径一键复原；覆盖度标注准确（抽样 20 图标）。
**【开源复用】**校验复用 F133 校验器；实现自研（图标服务面）。
**【设计细节】**切换实现=图标服务原子换指针+广播失效（各面监听重取——订阅制不轮询）；覆盖度=包内有效图标/规范类别清单数；「回退上一包」栈深 3 层（可连退三次）；同类目多包优先级文档化（当前包>官方>默认）。

### G-E-06 F156 指针编辑器 · 完整设计

**【功能定义】**指针方案定制编辑器：热点校准（像素级）/尺寸缩放（0.5x-2x）/动效开关（沙漏旋转等动画指针）/导入第三方指针包（F133 规范子集）；4K 双倍率 sprite 自动生成。
**【用户故事】**指针设计师把自己画的箭头导进编辑器——点一下箭头尖端设为热点、拖滑杆调到 1.25x、导出方案——**指针这个最小的 UI 件也有完整的创作管线**。
**【交互设计】**编辑器 720×480px：左侧指针列表（15 枚标准指针：箭头/手型/文本/忙碌/缩放…乙语义全保留）、中间画布（热点十字标定+网格）、右侧属性（尺寸/动效/预览）；试玩区（悬停试手感）。
**【数据与存储】**方案存 E4 面（vxtheme 兼容）；热点元数据随方案；导出走 F127 打包。
**【状态与异常】**热点未设 → 默认 (0,0)+黄色提醒；尺寸过大（>128px）→ 警告遮挡；动效帧率 >60 → 降采样提示。
**【验收判据】**15 枚指针全可编辑导出；热点生效精确（1px 级取放对拍）；双倍率 sprite 生成清晰（4K 放大验证）。
**【开源复用】**无外部依赖；画布组件复用 F103 画板面。
**【设计细节】**热点标定十字线放大 8x（F098 放大镜同款精度）；sprite 生成含 1x/2x 双套（乙节基线）；动效指针帧序列上限 16 帧；预览区背景三色切换（深浅中灰——各底色可见性检查）；导入兼容 .cur/.ani 评估（Windows 生态互通）。

### G-E-07 F157 声音混合器 · 完整设计

**【功能定义】**E5 承载：事件音效逐项音量独立调节+试听+「全部静音」总闸；音效方案（F079 三官方+社区）管理；应用混音器（F026 逐流音量）入口聚合。
**【用户故事】**用户觉得通知音太吵、开机音刚好——音效页把「通知」拉到 40%、「开机」留 100%——**每件事的声音都有独立的音量杆**。
**【交互设计】**E5 页六事件列表（F079 映射表）：每行方案选择下拉+音量滑杆+试听钮；顶部总闸大开关；底部「应用混音器」直达卡；方案预览（选中方案六事件连播试听）。
**【数据与存储】**逐事件音量配置层；总闸状态持久（F079 同源）；试听不落盘。
**【状态与异常】**方案缺某事件音 → 该行标「沿用默认」；试听设备缺失 → 按钮灰置说明；总闸开时试听也无声（总闸语义一致——诚实）。
**【验收判据】**逐事件音量独立生效实测（六事件六档位）；总闸 100% 压制；试听延迟 <200ms。
**【开源复用】**播放走 F026 面；无外部依赖。
**【设计细节】**音量曲线用感知对数（ slider 50%=实际半响——线性杆骗耳朵）；试听走独立一次性流（不污染混音器记忆）；滑杆 2% 步进（F075 同族）；总闸关闭时 E5 页整页降透明+「已全局静音」条（状态可见）。

### G-E-08 F158 开始菜单布局预设 · 完整设计

**【功能定义】**E6 承载：开始菜单三官方预设（效率=固定常用+最近多用/简洁=仅固定六枚/全屏=全屏模式默认）+ 用户自定义布局存第四套；一键切换即时生效。
**【用户故事】**给长辈远程配置——切「简洁」预设，开始菜单只剩六个大图标——**预设是把「配置系统」变成「选一种生活」**。
**【交互设计】**E6 页预设卡三枚（各带缩略示意）+「保存当前为自定义」钮；切换确认卡（列出将改变的项——固定区/最近区/全屏态）；切换后开始菜单即时重排（200ms F124 曲线）。
**【数据与存储】**预设定义为声明式清单（固定项集/区开关/全屏态）；自定义套存配置层。
**【状态与异常】**预设引用的应用已卸载 → 该项跳过+清单标注；保存自定义与官方同名 → 自动加「（自定义）」；切换到全屏预设 → 分辨率不适配时警告。
**【验收判据】**三预设切换结果与定义一致（逐项对拍）；自定义保存-切换-还原闭环；长辈模式实测（大图标可点面积达标）。
**【开源复用】**无外部依赖；开始菜单组件（C-3 补节）消费预设声明。
**【设计细节】**预设=「差异集」存储（只记与默认的差异——官方更新不冲掉用户改动）；切换不丢最近使用数据（预设只改布局不改数据）；全屏预设与 F071 搜索共存验证；预设卡缩略图实时渲染（真缩小版非贴图）。

### G-E-09 F159 字体安全档 · 完整设计

**【功能定义】**E7 换字体前自动兼容性预检：缺字率扫描（常用 3500 字+界面词条双集）、字号档位适配检查；缺字率 >5% 黄色警告、>15% 红色劝阻（可强行但显式确认）。
**【用户故事】**用户选了个只有拉丁字符的艺术字体——预检红色警告「该字体缺 87% 中文字符，界面将大量回退」——他改选了含中文字重的字体——**个性化不许破坏可用性，警告就是护栏**。
**【交互设计】**E7 页选择字体即出预检卡：缺字率环形图+三色判定+「预览示例文本」（含中英混排+数字+界面高频字）；强行应用需勾选「我了解缺字影响」。
**【数据与存储】**预检结果缓存（同字体不重扫）；字体包元数据可自带覆盖度声明（信任但验证）。
**【状态与异常】**扫描超时（超大字体文件）→ 分批后台扫+先给部分结论；字体文件损坏 → 拒绝导入三要素；等宽检测（终端用场景）同步显示。
**【验收判据】**三档判定阈值实测（构造 0%/8%/20% 缺字字体样本）；缺字清单前 20 字展示准确；强行应用后回退路径可用。
**【开源复用】**字符集覆盖扫描自研（cmap 解析小工具）；常用字集复用 F055 预热表（一处一事实）。
**【设计细节】**双集定义：通用集 3500 常用汉字+ASCII+高频符号；界面集=当前语言词条去重字符集（F140 联动——更精准）；缺字率=缺字数/界面集（界面优先口径）；扫描走 SIMD（F054 族，3500 字 <100ms）；等宽判定=等宽数字与字母 advance 一致性。

### G-E-10 F160 动效强度预演 · 完整设计

**【功能定义】**E8 三档（完整/减弱/关闭）切换前并排预演：同一段标准动画（窗口打开+菜单展开+通知滑入三联）三档并排播放——选完再应用，所见即所选。
**【用户故事】**拿不准「减弱动效」是什么效果——预演三联画并排一放：完整=全动画/减弱=时长 60% 简化轨迹/关闭=瞬显——**选择建立在看见之后**。
**【交互设计】**E8 页预演区三列并排（各列顶部档位名+当前选择强调框）；预演循环播放（可暂停）；「应用到系统」钮（切档全局生效 F124 缩放）；关闭档附「无障碍关联」说明（减弱动效条款 WP-207）。
**【数据与存储】**档位配置层（E8 既有）；预演无存储。
**【状态与异常】**预演自身掉帧 → 提示性能受限（诚实）；关闭档下预演区显示静帧+「此档下动画将瞬显」。
**【验收判据】**三档差异可辨（时长实测 100%/60%/0%）；应用后全局抽查 10 处动画一致。
**【开源复用】**无外部依赖；预演内容复用真实组件（非录屏——真渲染）。
**【设计细节】**预演三联共用同一时间轴驱动（对齐比较才公平）；减弱档轨迹简化规则文档化（位移减半+淡入替代滑动）；预演区背景中性灰（不受当前主题干扰）；播完停 1s 循环（呼吸间隙）。

### G-E-11 F161 个性化档案导出 · 完整设计

**【功能定义】**E 域全配置打包 vxtheme：主题+壁纸+图标包+指针+声音+短语库+布局预设+动效档——含版本号与回滚链；导入即复原（F147 随身同步的本体格式）。
**【用户故事】**Variable 重装系统——导入自己的档案包，30 秒后整个桌面人格完全复原——**他的系统气质是可以备份的资产**。
**【交互设计】**E1 页「我的档案」区：导出钮（可选包含项清单勾选）/导入钮（预览将应用项）/档案版本信息；导入前校验+与当前差异预览（「将更改：主题/图标/12 条短语」）。
**【数据与存储】**包结构 F126 schema（分节清单+资产）；回滚链=导入前自动快照（F121 还原点联动）；包签名（F127）。
**【状态与异常】**包含第三方资产 → 版权字段展示（F133 要求）；导入中断 → 原子回退；跨版本导入 → 兼容矩阵校验+降级项清单。
**【验收判据】**全项导出-导入 round-trip 逐项等值（令牌表哈希对拍）；差异预览准确；中断原子性实测。
**【开源复用】**打包复用 F127 工具；schema 复用 F126。
**【设计细节】**分节独立勾选（只要短语也行——包是菜单不是套餐）；回滚链与 F121 快照共用存储（不双份占空间）；包体积预估显示（导入前）；档案内短语隐私提示（导出前提醒检查内容——私人句子可能不想分享）。

### G-E-12 F162 每应用主题例外 · 完整设计

**【功能定义】**个别应用可脱离全局主题：例外清单上限 10 项、每项可指定独立深浅/强调色；全局一致优先、例外显式可控——**共性是默认，个性是特权**。
**【用户故事】**Variable 全局浅色但终端永远深色——例外清单里给终端锁深色——切全局主题时终端纹丝不动。
**【交互设计】**E1 页「应用例外」区：清单行（应用图标/名/例外模式：深色/浅色/跟随全局+强调色拾色器）；添加（已装应用选择器 F008 族）/移除；全局切换时例外应用标注小角标。
**【数据与存储】**例外表配置层（还原点覆盖）；上限 10 强制（超出提示精简——防清单腐化）。
**【状态与异常】**例外应用卸载 → 自动清条目；应用不支持主题令牌（旧应用）→ 例外标注「该应用未接入令牌，仅深浅生效」诚实降级；冲突例外（同应用重复）→ 合并提示。
**【验收判据】**全局切换时例外应用保持（20 次切换实测）；上限 10 生效；未接入令牌应用降级路径实测。
**【开源复用】**无外部依赖；令牌体系 F151 承载。
**【设计细节】**例外粒度=应用级（非窗口级——复杂度红线）；深浅例外实现=应用收到锁定令牌表（全局广播时跳过）；强调色例外只影响该应用自身控件；例外生效标识=应用标题栏小圆点（自己可辨）；F162 与 E8 动效档独立（动效全局统一不做例外——简化纪律）。

### G-E-13 F163 桌面小组件开关集 · 完整设计

**【功能定义】**三枚官方桌面小组件：时钟（大字模拟/数字双式）/天气（F101 数据）/系统快照（F062 四体检灯迷你版）；桌面右键「添加小组件」启用；走 4K 管线与 E1 令牌；自由摆放（F084 自由模式同族）。
**【用户故事】**Variable 桌面右上角钉了天气+时钟——抬头即知时间与冷暖——桌面从图标墙变成了他的仪表盘。
**【交互设计】**添加流：右键菜单项→小组件选择卡（三枚预览+尺寸档）→放置（半透明跟手 F084 视觉族）；组件右键菜单（尺寸/透明度/移除）；拖拽重摆；数据更新无感（天气 30min/时钟秒/快照 10min）。
**【数据与存储】**组件实例（类型/位置/尺寸/透明度）配置层；天气引用 F101 缓存（不重复请求）。
**【状态与异常】**天气断网 → 组件显示「数据截至 XX」；时钟与系统时间强一致（F187）；组件与图标重叠 → 置于图标层之上但可穿透点击设置（显式开关）；性能保护 → 全组件渲染超预算自动降透明帧率。
**【验收判据】**三组件增删改位全链录屏；时钟秒级准确；天气数据源一致（与 F101 面板同值）。
**【开源复用】**无外部依赖；渲染走合成器独立层（F056 光标层同机制）。
**【设计细节】**组件渲染走桌面层独立脏区（不拖累图标刷新）；透明度滑杆 20%-100%（20% 下限保可读）；时钟数字式走等宽令牌（E7）；快照组件红黄灯与 F120 同源同色；右键菜单三项固定+扩展位（后续官方组件注册制）。

### G-E-14 F164 锁屏定制 · 完整设计

**【功能定义】**锁屏界面定制：壁纸（独立于桌面可选）/时间样式（三式：大数字/模拟/极简）/状态信息行（电量/日期可选开关）；锁屏动画复用 C-2 四幕语汇静版（淡入即静，无循环动画——省电纪律）。
**【用户故事】**Variable 给锁屏配了张星空壁纸+大数字时钟——合盖再开，两秒内看到时间与电量——**锁屏是系统的门面，也是待机的门神**。
**【交互设计】**设置中心「个性化-锁屏」页：壁纸选择（独立池或跟随桌面）/三式预览卡/状态行开关组；实时预览（F152 编辑器同机制——迷你锁屏即改即见）。
**【数据与存储】**锁屏配置层；锁屏态本身零写入（纯展示）。
**【状态与异常】**锁屏期间通知 → 不显示内容只计数（隐私默认）；壁纸加载失败 → 深色纯色兜底；唤醒延迟预算 2s 内可见时间（省电与体验平衡实测线）。
**【验收判据】**三式切换即时预览；唤醒到可见 ≤2s 实测；零循环动画验证（功耗挂账 F060）。
**【开源复用】**无外部依赖；渲染复用合成器锁屏层。
**【设计细节】**大数字式 96px（乙-1 大标题档×3 场景特例——登记乙节增补）；模拟式钟面 4K 资产（F100 同素材）；状态行图标走 E1 令牌色；唤醒路径：ACPI 唤醒→合成器恢复→锁屏层首帧（F053 时间线登记新节点）；密码输入框聚焦时时间缩小上移（让位布局预演）。

### G-E-15 F165 开机动画个性化 · 完整设计

**【功能定义】**四幕动画（C-2）可定制项：配色（星徽主色/粒子色随 E1 强调色）/粒子密度三档/幕三「点亮」过渡底图（可选用户壁纸暗化版）；结构与时长不可改（8 秒线是性能契约）——**标准不可改，皮肤可以**。
**【用户故事】**用户把强调色改成琥珀金——下次开机整场动画的星徽与粒子都成了金色——熟悉的仪式，自己的颜色。
**【交互设计】**E1 页「开机动画」区：三档密度单选（繁星/标准/极简——性能联动 F068）+过渡底图开关；实时预览（重播完整四幕 8s——预览即真播）；改动下次开机生效（显式说明——引导帧不可热改的诚实边界）。
**【数据与存储】**定制参数配置层；资产按配置预生成（关机前烘帧——F068 按需装载联动）。
**【状态与异常】**资产烘焙失败 → 回退官方默认动画+报备；用户壁纸过暗/过亮 → 自动压暗/提亮至可读区间（说明条）；密度档与低端机自动匹配（F060 电量账本弱机降档建议）。
**【验收判据】**改色后开机录屏与预览一致；三档密度实际帧资产数对拍；结构时长零变化（8.0s±0.2s）。
**【开源复用】**无外部依赖；预览复用动画本体（同一渲染代码——预览即真话）。
**【设计细节】**粒子色=强调色令牌映射表（主色→粒子基色/辅色→高光）；密度三档粒子数（1200/600/200）；过渡底图暗化 65% 固定（保证幕四交接可读）；烘帧在「关机时安装更新」同窗口执行（空闲批次）；跳过键（Esc）语义不受定制影响。

### G-E-16 F166 输入法皮肤 · 完整设计

**【功能定义】**候选窗（F027/F107）外观定制：配色（随 E1 或独立）/字体字号/透明度/候选数显示（5/9 档）；组合期渲染延迟 ≤16ms 红线不变——皮肤自由，速度不商量。
**【用户故事】**用户给候选窗配了半透明深色+自己挑的字体——打字时候选窗优雅地浮在光标下，和主题浑然一体——**每平方厘米的界面都值得一整套设计**。
**【交互设计】**设置中心「时间和语言-输入法外观」页：「跟随主题」总开关（开=令牌联动，关=独立定制组）/字体选择（F159 预检联动）/字号 12-16px 滑杆/透明度 60%-100%/候选数单选；实时试打框（即改即打即见）。
**【数据与存储】**皮肤配置层；试打不落盘；渲染参数热生效（下一键即新肤）。
**【状态与异常】**所选字体缺字 → F159 红黄卡拦截路径；透明度过低可读性差 → 60% 下限（护栏）；渲染延迟超标（自检 F064 同法）→ 自动回退默认皮肤+报备。
**【验收判据】**定制全项生效试打验证；16ms 延迟红线在自定义皮肤下复测；跟随主题切换联动实测。
**【开源复用】**无外部依赖；渲染走 IME 既有管线（F027）。
**【设计细节】**热生效=候选窗每次弹出重读皮肤参数（无缓存陈旧态）；透明度走合成器层透明（不重绘底层——性能）；候选高亮色对比度门禁（4.5:1 自动校验——F141 公式复用）；9 候选档下翻页键提示；皮肤参数 schema 并入 vxtheme（F126 增节）。

### G-E-17 F167 右键菜单自定义 · 完整设计

**【功能定义】**资源管理器右键菜单可定制：隐藏不常用项（用户白名单恢复）/应用注册项按使用频率自动排序（可选）；菜单结构基线仍对齐 Windows（乙-4 表）——定制是修饰不是重构。
**【用户故事】**Variable 右键菜单里从不用「压缩」项——隐藏它，菜单短了三行；常用的「打开方式」自动升到顺手位——**菜单长在你手上**。
**【交互设计】**设置中心「个性化-右键菜单」页：当前菜单项全列表（开关每项+预览小图）+「恢复默认」；自动排序开关；改动即时生效（下次弹出）；菜单内「管理」入口直达此页。
**【数据与存储】**隐藏清单与使用计数配置层；计数环形 90 天。
**【状态与异常】**系统关键项（删除/重命名/属性）→ 锁定不可隐（防自残——红线显式）；应用卸载 → 其菜单项自动清；隐藏项过多（>50%）→ 提示菜单可能不合群（仍允许）。
**【验收判据】**隐藏-生效-恢复闭环；自动排序与 F072 计数一致；关键项锁定实测。
**【开源复用】**无外部依赖；计数引擎复用 F072。
**【设计细节】**使用计数打点在菜单项选择时刻（零额外开销）；自动排序仅对「打开方式」组与应用注册组生效（系统组位置不动——乙-4 对齐承诺）；隐藏项在「显示更多选项」二级完整保留（功能不丢只收纳）；菜单弹出延迟红线 100ms（定制不增负——性能回归项）。

### G-E-18 F168 任务栏个性化 · 完整设计

**【功能定义】**任务栏三选项：图标大小（标准 24px/大 32px 档）/对齐（居中/左对齐——Windows 双惯例）/自动隐藏（贴底滑出）；改后即时生效；与 C-3 基线尺寸联动标注。
**【用户故事】**老 Windows 用户切「左对齐+大图标」——肌肉记忆无缝迁移；极简用户开自动隐藏——桌面一寸不浪费。
**【交互设计】**E6 页任务栏区三控件（大小单选/对齐单选/隐藏开关）+实时预览条（迷你任务栏即改即见）；自动隐藏附触发说明（光标贴底 200ms 唤出）。
**【数据与存储】**三选项配置层；预览无存储。
**【状态与异常】**大图标档托盘区溢出 → 自动折叠阈值联动（F075）；自动隐藏与全屏应用冲突 → 全屏态强制显示（可交互性优先）；切换动画 200ms（F124）。
**【验收判据】**三选项独立生效互不干扰；隐藏-唤出 200ms 触发实测；托盘折叠联动正确。
**【开源复用】**无外部依赖；任务栏组件（C-3/F075）消费选项。
**【设计细节】**大图标档任务栏增高 56px（乙-1 表 48px 的增补档——乙节联动修订登记）；左对齐实现=布局锚点切换（图标区整体平移动画）；自动隐藏唤出热区 6px（比触发带窄——防误唤）；隐藏态下 Win 键仍弹开始菜单（可连性兜底）；三选项即时生效走任务栏布局重排（200ms 内完成——F124 大面板档）。

### G-E-19 F169 快捷键查看器 · 完整设计

**【功能定义】**全局快捷键总表页：按应用/按场景分组（系统/窗口/文本编辑/辅助）、冲突检测显式提示、逐条可改（系统级热键）；快捷键是契约，契约要可查可改。
**【用户故事】**用户想改截图热键（F098）——查看器里搜「截图」，找到条目，点击重录新组合——冲突了立刻红字告诉撞了谁——**再没有「这快捷键被谁占了」的悬案**。
**【交互设计】**设置中心「辅助功能-键盘-快捷键」页：分组树+搜索框（按功能名/按键串双向搜）；每行：功能名/当前组合/修改钮（重录态：按下新组合即录，Esc 取消）；冲突行红底+「冲突对象」链接；「恢复全部默认」。
**【数据与存储】**自定义表配置层；默认表编译期常量（一处一事实）；重录历史撤销栈。
**【状态与异常】**录到单键/纯修饰键 → 拒绝+说明；组合被系统保留（Win 系）→ 拒绝+保留清单链接；应用级热键冲突 → 提示应用内改（边界诚实）。
**【验收判据】**全表条目与实际行为一致性抽测 20 条；重录-冲突-改回全链录屏；恢复默认一键全复原。
**【开源复用】**无外部依赖；搜索复用 F071 引擎。
**【设计细节】**按键串标准化显示（Ctrl+Shift+S 规范序：Win>Ctrl>Alt>Shift>键）；重录捕获层独立于输入系统（录时不触发功能）；冲突检测即录即查（O(1) 查表）；默认表 60+ 条目分 4 组（数量实查登记）；修改持久即时生效（注册表广播同 F155 机制）。

### G-E-20 F170 个性化域总判据 · 完整设计

**【功能定义】**E-1 三铁律总验收：随时可改/随时可退/预设+微调——对 F151-F169 逐项实测「改→生效→回退」三步打勾；任何一项「改完要重启」即整项不达标——本域的宪法用秒表和脚本执法。
**【用户故事】**个性化域收官：自动化脚本对 19 个功能项逐个「改-生效-回退」三连跑，19 项全绿——**这个域的每项自由，都被证明是真实的自由**。
**【交互设计】**无用户 UI（验收制度）；结果进季报（F149）；脚本工具 `tools/vx-walkcheck-e.py`（走查材料族惯例）。
**【数据与存储】**三步验收记录归档验收目录；逐项证据（配置哈希前后+生效抽查截图）。
**【状态与异常】**某项回退不干净（残留态）→ 该项回炉+回归门禁加该 case；新个性化功能合入 → 三步脚本先扩再合（门禁前移）。
**【验收判据】**19/19 三步全绿即本域判成；脚本全量执行 <30 分钟（可进 CI 周跑）；证据链完整率 100%。
**【开源复用】**无外部依赖；脚本自研（A/C 域走查脚本同族）。
**【设计细节】**三步定义：改=写新值/生效=验证点抽查（令牌值或渲染像素）/回退=恢复默认哈希等值；「生效」验证点每项在报告中已定义（本卷各【验收判据】段——直接消费）；脚本失败输出三要素（哪步/差多少/证据路径）；与 F121 还原点联动验证（回退可走还原点路径复测——双路保险）。

---

# 卷尾 · 全域验收与首发序

## 全域验收

20 维度清单照走，本设计案特别加权：第 4 条细节（1px 对齐×3 放大）、第 5/6 条视觉动画（4K 放大检查+节奏）、第 8 条（丰盛不以卡顿为代价——P95 与帧率实测）、第 10/11/12 条（体验完整性 14 章逐条）。**任何一项低于标准不交付。**

## 五域首发序

A4 → B1 → C2 → A1(M-A/M-B) 为首发四项（量小、杠杆高、互不阻塞），其余按域内顺序推进；E 域随 C 域各节成型后接入（主题令牌体系先行，E1-E8 逐项落包）。

---

## 待 Variable 拍板的三件事（进展：③ 已拍板）

1. **「会话表格」语义**：任务管理器（理解 A）还是资源管理器表格视图（理解 B）？还是两个都要？（见第 3 部分 C-5）
2. **进入动画风格方向**：星徽粒子路线（本草案）还是你有其他意象？（光影写实 / 玻璃折射 / 抽象几何 任选，都按 4K 做）（见第 3 部分 C-2）
3. **内置浏览器的现实路径**：✅ **已拍板（2026-09-25）**——从 Edge 或其他浏览器下载，无软件商店（见第 1 部分 A-2、第 4 部分）。

---


## G-1 深化设计报告（F171-F200 报告式全展开）

> 按卷首·丙模板逐项展开中——30 项，批次推进（A 域 40 项已完成，见第 1 部分 A-5 节的成卷样例）。本节报告体随批次写入，写完即删除本占位。

---

# 增补 · F 全域深化清单（200 项超大功能登记，Variable 2026-09-25 指定）

**登记规则**：本清单在五域 + 内核底盘之下做**不重复、可落地、可验收**的功能深化——每项按卷首·甲五要素口径书写（对象/数值或形态/状态/验收锚点/开源复用），编号 F001-F200 全册唯一；与既有条文冲突时以本清单为准并回改原文；每项开工前仍须先落 WP 编号与绑定判据（A4 管线/MD3 第 6 章纪律照走）。分域归属：F-A 兼容 40 项、F-B 性能 30 项、F-C 体验 55 项、F-D 生态开放 25 项、F-E 个性化 20 项、F-G 内核底盘与安全 30 项，合计 200 项。

## F-A 兼容域深化（F001-F040）

**F001 无感双击运行**——.exe 从资源管理器双击到窗口出现的全程零介入：装载链（peblock 门→ring3）对用户完全透明，失败时弹三要素对话框并附「查看日志」入口；验收 = 十个常用 .exe 双击成功率实测在册。
**F002 静态 PE 全量支持**——无导入表的静态链接 PE 全格式装载（GUI/控制台/服务三类子系统），包括非常规节对齐与高熵入口；对抗样本（畸形头/重叠段/越界入口）拒绝率 100%（承接 B-2701 纪律）。
**F003 导入绑定加速**——延迟绑定（delay-load）与标准 IAT 绑定双路径，导入解析结果按进程族缓存；二次启动同程序导入解析耗时实测减半（对账 vxbench 装载类基准）。
**F004 Wow64 门（32 位 .exe 前瞻）**——32 位 PE 的探测、拒绝与诚实提示（「此程序为 32 位，兼容计划中」）先行落地，避免用户撞上不可解的静默失败；64 位子系统就绪后此门转为转换入口。
**F005 Win32 窗口管理兼容层**——winsrv 消息泵扩面至常用窗口类（按钮/编辑框/列表/组合框/滚动条/静态文本），每类一组判例 + 一张星卡；以「跑通一个纯 Win32 开源文本编辑器（Notepad2 级）」为标志性验收。
**F006 GDI 绘图面**——GetDC/BitBlt/TextOut 等 GDI 最小集直通到 VARIX 合成器提交面，绕行即红线（B-801 同构）；开源复用：参照 Wine winex11 的 GDI 映射表裁剪。
**F007 GDI+ 与双缓冲**——GDI+ 基本形状/文本抗锯齿/双缓冲位图支持，覆盖开源小工具（图像查看器类）的典型绘制路径；验收 = 一款开源 GDI+ 小工具全绿。
**F008 通用对话框族**——打开/保存/选择颜色/选择字体四枚通用对话框按 Windows 动线自研实现，任何 Win32 程序调用都得到 VARIX 风格的同一套对话框——**兼容面统一出口，视觉不破碎**。
**F009 注册表虚拟化**——每应用独立注册表蜂巢（per-app hive），写操作进沙盒蜂巢不污染全局；卸载即蜂巢整体删除（回收站语义，零真删红线不动摇）。
**F010 文件系统重定向**——AppData/Program Files/ProgramData 三处写入重定向到每应用沙盒目录；用户文档目录真实直通——「程序乱写 C 盘」在 VARIX 结构上不可能发生。
**F011 环境变量会话面**——PATH/TEMP/HOMEDRIVE 等标准变量按会话注入，承接判例 14（代理环境变量直通）；应用继承顺序与 Windows 语义一致，差异表公开。
**F012 控制台子系统（ConDrv 级）**——控制台类 .exe（命令行工具/构建脚本）在 VARIX 终端应用内运行，stdout/stderr/exit code 全保真；验收 = 一款开源命令行压缩工具在终端内全流程可用。
**F013 .lnk 与快捷方式**——Windows 快捷方式解析器（目标/参数/工作目录/图标提取），资源管理器右键「创建快捷方式」动线对齐；图标提取走 PE 资源解析（4K 重采样入 C-7 管线）。
**F014 PE 资源全解析**——图标/版本信息/清单（manifest）三件套解析：版本信息进属性对话框「详细信息」页，manifest 决定 DPI 感知与兼容性声明——声明了 DPI 感知的程序 VARIX 如实放大，不双倍缩放。
**F015 多语言 .exe 资源**——RC 字符串表按 UI 语言选择（中文优先），代码页转换表全量内嵌；验收 = 中英双语开源程序在中文界面下全程中文。
**F016 字体链兼容**——Windows 字体名（宋体/微软雅黑/Consolas）映射到 VARIX 字体族，缺字回退链对齐 MD2 篇 34；应用请求「微软雅黑 9pt」得到视觉同级渲染，不是缺字方块。
**F017 剪贴板格式族**——CF_TEXT/CF_UNICODETEXT/CF_BITMAP/CF_HDROP/自定义格式注册链全通，VARIX 剪贴板协议（B-3901）做格式翻译中枢；跨 Wine/原生窗口复制粘贴保真为验收判据。
**F018 拖放协议互通**——OLE 拖放（DoDragDrop）与 VARIX VXWM 拖放协议双向翻译；跨表面拖文件（Wine 窗口 → 资源管理器）落点预览与 Esc 三取消照走 C-6 红线。
**F019 COM 本地接口最小集**——CoCreateInstance 本地对象族（Shell 链接/拖放源/剪贴板）按判例补齐，不承诺进程外 COM 全集——承诺面写进星卡，不装懂。
**F020 异常与调试面**——SEH/VEH 结构化异常翻译到 VARIX 信号层，崩溃时生成 minidump 进崩溃恢复全书（S209 面）；应用崩溃不拖垮合成器（AI2 隔离防崩纪律的兼容面落点）。
**F021 内存语义对齐**——VirtualAlloc/MapViewOfFile/文件映射语义对齐（写时复制/保护页），伙伴分配器供给策略按大页优先；验收 = 一款开源内存敏感工具（7-Zip 基准）实测无降级。
**F022 时间与定时器族**——GetTickCount/QueryPerformanceCounter/timeBeginPeriod 语义对齐，TSC 时钟源直通（`tsc 2500MHz isa-none` 已实测）；游戏与音视频应用的时基保真判据入册。
**F023 网络栈 Winsock 面**——Winsock2 常用 API（socket/connect/send/recv/select/WSAAsyncSelect）翻译到 smoltcp socket 面，异步模型以消息泵适配；验收 = 一款开源 FTP 客户端全流程可用。
**F024 TLS 与证书库**——rustls 承载 TLS，Windows 证书库语义映射到 VARIX 证书管理器（F-C 域设置面）；DoH 切换（B-604）对 Winsock 面同样生效。
**F025 打印虚拟面**——「打印到 PDF」虚拟打印机先行（开源 PDF 生成库接入，只链接不修改），物理打印机列 STAR I start 后程；打印对话框动线对齐 Windows。
**F026 音频 WinMM/DirectSound 面**——waveOut/DirectSound 常用路径直通 HDA 混音器（B-806/807），每流独立音量；混音器 UI（C 域托盘面）实时显示各应用流——**谁在出声一眼可见**。
**F027 输入法 IMM32 面**——Wine 应用经 IMM32 消费 VARIX IME 组合事件，候选窗统一走 VARIX 渲染（风格不破碎）；组合期零误触（B-904）在兼容面复测。
**F028 高 DPI 感知三态**——DPI-unaware/aware/system-aware 三态如实执行：unaware 程序由合成器位图放大（模糊可接受并标注），aware 程序拿真实分辨率——**尊重程序的自我声明，不越俎代庖**。
**F029 多显示器前瞻接口**——显示配置 API 以单屏实现、多屏语义预留（枚举返回一块屏），接口形状不锁死——未来多屏不破 API。
**F030 安装器兼容模式**——NSIS/Inno Setup/MSI 三族安装器在沙盒（F009/F010）内正常完成安装流，安装进度条与完成页如实呈现；验收 = 三族各一款开源软件安装-卸载全周期在册。
**F031 卸载与残留清扫**——按 F009/F010 沙盒结构卸载即目录+蜂巢+开始菜单项三清，清扫清单给用户过目（回收站语义）；「安装 5 分钟、残留 5 年」在 VARIX 结构上不可能。
**F032 开源运行时直装族**——Python/Node/Java(JDK)/Go/Rust 工具链按 vxapp 打包上架侧载管线，PATH 与解释器解析按会话注入（F011）；每语言一组「装-写-跑-调」判例，VSCode 集成验收承接 S304。
**F033 构建工具链判例**——git/cmake/ninja/make 四件在控制台子系统（F012）全流程绿，网络操作走 Winsock 面（F023）；验收 = 开源小项目从 clone 到 build 到 run 全程在 VARIX 内完成并录制在册。
**F034 字符编码终局**——UTF-8 为系统编码，ANSI 代码页语义在兼容层内翻译（A 代码页 → UTF-8）；乱码事故归档为判例，每个乱码场景有一条判例兜住。
**F035 兼容性自检向导**——用户双击后若失败，弹「兼容性向导」：自动归因五分类（缺 API/缺运行时/架构不符/权限/未知），给出「需要 XX 运行时，去浏览器下载」的可执行指引（无商店原则照走）——**失败也是一次被引导的体验，不是死胡同**。
**F036 星卡自动草稿**——每个成功运行的未知程序自动生成星卡草稿（API 覆盖面/启动耗时/资源画像），用户一键提交进星图（D 域社区通道）；兼容性数据从真实使用里长出来，正是 A2 的数据驱动纪律。
**F037 peblock 门用户可见化**——被门拦下的程序给出「为什么拦我」的完整解释（哪条规则/哪个哈希），允许「我了解风险，仍要运行」的显式越权（越权行为全量审计日志）——**安全不是黑名单的沉默，是规则的透明**。
**F038 应用隔离档位**——每应用三档隔离（宽松/标准/严格）可调（E 域联动），严格档禁网络+只读文档目录；默认标准档，权限变更时弹窗告知——权限体验对齐手机系统的心智模型。
**F039 游戏兼容前瞻面**——DirectX 探测与拒绝表先行（DX9+ 检测后给「切 Windows 域」一键引导，判例 19 诚实边界照走）；wineserver 会话（M-D/M-E）就绪后此面转 Wine DXVK 评估——不做半吊子承诺，做诚实的分流。
**F040 兼容域总判据**——「常用 50 件」清单（文本编辑/压缩/图片/终端工具/开发工具五类各 10 件开源代表）逐件实测建档，通过率每季度刷新进星图——**兼容性不是口号是账本**。

## F-B 性能域深化（F041-F070）

**F041 帧率账本**——合成器逐帧打点（合成耗时/脏区面积/提交耗时）进账本，监视器直接画 80fps 红线图；帧率不是感觉，是每秒 80 行账。
**F042 帧率归因器**——掉帧时自动归因（输入风暴/大脏区/IO 阻塞/调度抢占），监视器给出「上一秒谁偷了帧」的排名——性能问题先可见再可治。
**F043 冷启动画像**——每个应用首次/二次启动的逐阶段耗时（装载/重定位/首帧/可交互）建档入星卡；「打开任意软件」的快慢从黑盒变成五段刻度尺。
**F044 预取指纹 v2**——文件页预取按历史启动指纹（哪些页真正被摸过）调序，对账 BOT 预读窗口；二次启动目标实测减半（承接 MD2 篇 27）。
**F045 页缓存水位策略**——U 盘整机内存 4GB 边界下的页缓存水位线（回收阈值/脏页占比上限）做成旋钮（MD2 附录 K），断电一致性让位规则显式化。
**F046 写合并窗口自适应**——5 秒窗口按负载动态伸缩（空闲收窄到 1s、重载放宽到 8s，fsync 硬承诺不动）；旋钮联动走 ADR。
**F047 调度器延迟预算深化**——交互线程 p99 唤醒延迟 2000μs 预算拆到逐类（输入/合成/音频/普通），超标即归因报告——预算不是平均值是分布。
**F048 CPU 频率联动**——TSC 2500MHz 实测面之上做负载感知升降档（P-state 表驱动），降档不降交互预算；笔记本电池续航与手感联动的第一块旋钮。
**F049 空转清零工程**——合成器空闲 5% 判据（B-501）细化为「空闲态零定时器唤醒」（帧缓冲无脏区即休眠），待机功耗实测入账本。
**F050 中断合并**——输入/网络中断按突发窗口合并（2000ns 预算内批量处理），input-probe flood 已有 flood_dropped 计数（实测 70）——把丢弃变成合并，计数器归零为目标。
**F051 大页策略**——4MB 页用于内核静态区与字形图集，TLB miss 实测对比入账；R3 GPU 摸底时同步评估核显侧大页。
**F052 堆碎片治理**——内核堆（56KB 分块已实测）增长策略改为尺寸分级池，碎片率指标入账本；零堆纪律之外「堆健康」也要有数。
**F053 启动并行度**——init 里程碑依赖图显式化（ACPI/PCI/USB/存储四链并行），串行等待点逐个消除；开机 8 秒线（B2）的分解攻坚图挂账本。
**F054 图像解码 SIMD**——PPM/PNG/JPEG 解码走 SIMD（SSE4.2 基线，AVX2 探测后启用），解码耗时入 vxbench；壁纸 4K 解码 ≤150ms 为验收线。
**F055 字形光栅缓存**——字形图集按 LRU 热度驻留，冷字形驱逐热字形保位；滚动长文档的光栅耗时实测不抖动。
**F056 合成器脏区深化**——光标移动不触发全屏合成（光标层独立），弹窗动画只合成弹窗矩形——把「全屏重绘」从系统词典里删掉。
**F057 IO 调度分级**——前台交互 IO（打开菜单/搜索）优先级永远高于后台（索引/下载），io-pending 指标（实测 avg 9/9 pass）细化为分优先级队列。
**F058 内存压缩前瞻**——4GB 边界的冷页压缩池（LZ4 级算法评估）立项评估不急于上——先测真实工作集，超 3.2GB 再启用。
**F059 网络小包优化**——smoltcp 描述符混表（B-602）之上的批量收包路径，每包处理耗时入账；终端 SSH 打字延迟 ≤30ms 为体验线。
**F060 电量账本**——按应用的能耗画像（CPU 时间/唤醒次数/IO 量折算），设置中心「电池」页显示「今天谁在耗电」——功耗透明是 U 盘整机的体验面。
**F061 基准回归门**——vxbench 六类基准接入 CI 夜跑（QEMU 替身），基线倒退即门禁红——性能资产和代码资产一样不许悄悄缩水。
**F062 性能自检报告**——开机后 10 分钟静默采样生成「系统健康快照」（帧率 P95/IO 延迟/调度超标次数），异常自动转诊断中心工单——性能问题在用户感知之前先被系统自己看见。
**F063 触控板手势前瞻**——HID 触控板多指手势协议评估（Y7000 触控板实测定级），不承诺但把数据面留好。
**F064 音频低延迟链**——混音器单流延迟 ≤20ms 实测线（音乐类应用的可用性门槛），CORB/RIRB 环形缓冲调优入账。
**F065 唤醒源治理**——USB 键鼠（xHC 已 live）之外的唤醒源白名单化，睡眠态每分钟唤醒次数 ≤1 为达标；待机耗电从查不到变成有账。
**F066 文件系统日志策略**——ext4 journal 模式按分区特性选型（data=ordered 基线），断电百次与性能双达标为准；journal 参数进旋钮清单。
**F067 启动 IO 冷热分离**——镜像内只读区（内核/字体/资产）与可写区物理分离，只读区零 journal 开销；BOT 顺序读优势最大化。
**F068 渲染资产按需装载**——4K 壁纸/图标集按主题加载（当前主题外资产不驻内存），内存占用差值入账本——丰盛与省内存兼得。
**F069 性能模式三档**——静音/均衡/性能三档一键切换（调度预算+频率+写合并窗口联动），任务栏托盘直达；档位定义全走旋钮清单，无隐藏魔法数。
**F070 性能域总判据**——80fps 红线（B-2 补）之下每层都有独立判据与账本页，四层优化逐层绿才整链绿——**总指标是试金石，分层判据是施工图**。

## F-C 体验域深化（F071-F125）

**F071 开始菜单搜索直达**——搜索结果三类分区（应用/设置/文件），键盘上下直达、Enter 即开、首项高亮；索引延迟 ≤50ms 回显（本地索引面）。
**F072 最近使用引擎**——按应用+文件双维度记录使用频次与时间，开始菜单「最近使用」与文件管理器「快速访问」同源；隐私红线：记录只存本地、可一键清空。
**F073 任务栏预览缩略图**——悬停任务栏图标弹出窗口实时缩略图（合成器离屏渲染管道复用），多窗口纵向卡片布局对齐 Win11 动线。
**F074 跳转清单（Jump List）**——右键任务栏图标出「最近文件/常用任务/固定项」，应用可声明自定义任务；数据源与 F072 同引擎。
**F075 托盘系统**——托盘折叠展开动画、隐藏图标上拉面板、电池/音量/网络三件套点击弹出控制条（滑杆 60fps 实时）——托盘不是图标堆是控制中枢。
**F076 快速设置面板**——点击网络/音量/电池聚合弹出（对齐 Win11 快速设置）：Wi-Fi 选择/音量滑杆/亮度滑杆/专注模式开关/性能三档（F069）一板全收。
**F077 通知中心**——右侧滑出面板：历史通知按应用分组、免打扰开关、逐应用通知权限（E 域联动）；toast 停留 5s（乙-4 表）后沉入中心不丢失。
**F078 日历飞出**——任务栏时钟点击弹出月历+今日日程位（日历数据面留口，STAR I start 后程接）；月历 4K 渲染、今日高亮主题色。
**F079 全局音效体系**——开机/关机/通知/USB 插拔/回收站/错误六事件音效（E5 声音方案承载），音效文件 48kHz FLAC 进 4K 资产管线；静音键一键全停。
**F080 窗口吸附动画**——贴边分屏的落点预览半透明矩形 + 松手弹性动画（150ms ease-out），Win+方向键同动线；预览矩形实时跟随不撕裂。
**F081 任务视图**——Win+Tab 多窗口缩略墙 + 虚拟桌面条（桌面 1/2 增删拖移窗口）；缩略墙复用 F073 管道，虚拟桌面各留独立最近使用栈。
**F082 Alt+Tab 现代化**——缩略图 + 图标双排卡片、按最近使用排序、长按停留展开——动线对齐 Windows，视觉全自研。
**F083 桌面右键 refresh 语义**——刷新有可见反馈（图标闪 80ms），不装样子——Windows 的 F5 心理按摩在 VARIX 里做诚实版（真重载图标缓存）。
**F084 图标拖拽网格**——自由/对齐网格双模式，拖拽半透明跟随 + 目标位高亮 + 碰撞换位动画；双击间隔对齐乙-4 表。
**F085 回收站体验化**——删除动画（图标飞向回收站 200ms）、还原动画反向、容量水位环显示；零真删红线（B-1701）是全部动画的前提。
**F086 复制/移动进度对话框**——速度曲线/剩余时间/冲突处理（跳过/覆盖/重命名三选）+ 后台队列（关对话框继续传）；大文件异步不卡 UI 是 B-18xx 既有判据的体验面。
**F087 冲突智能提示**——同名冲突时显示两侧缩略图+大小+时间，一键「保留两者」自动加后缀；批量冲突合并为一个决策面板。
**F088 搜索即输即显**——资源管理器搜索框输入即出结果（防抖 150ms）、结果高亮命中词、搜索范围条（当前目录/递归）显式可见；Esc 三击内清空回位。
**F089 标签页式资源管理器**——多标签打开不同目录（Ctrl+T 开/Ctrl+W 关/拖出成窗），标签记忆每页的视图模式与滚动位置——Windows 11 已验证的效率件。
**F090 面包屑增强**——地址栏每级面包屑点击下拉该层兄弟目录、拖文件到面包屑即移动到该层——地址栏不只是显示路径。
**F091 详情窗格**——选中文件右侧显示元数据（类型/大小/修改时间/图片尺寸/EXIF 相机型号可选展开），窗格宽 240px 可折叠。
**F092 压缩/解压内置**——zip 只读解压 + 打包内置（开源 minizip 级库只链接不修改），右键「解压到当前文件夹」动线对齐；rar/7z 走第三方软件下载（C 域获取口径）。
**F093 图片缩略图引擎**——EXIF 缩略图缓存（缩略图库文件持久化），万张目录滚动实测不卡（缩略图解码 SIMD 复用 F054）。
**F094 媒体信息悬浮**——悬停视频/音频文件出时长/分辨率/码率提示条（元数据解析轻量实现），详情窗格（F091）同步展示。
**F095 终端应用 2.0**——标签页/分屏/主题（E 域联动）/Unicode 全渲染/滚动回看 10 万行不卡；字体走等宽字体令牌（E7），字形渲染走 4K 管线。
**F096 终端 command palette**——Ctrl+Shift+P 命令面板：常用命令/复制模式/清屏/导出会话——终端也是「作品」，不是黑框。
**F097 记事本类编辑器**——无 BOM 感知/行尾统一提示/大文件流式打开（百 MB 不卡）/最近文件列表；宪章手感条款（快捷键/焦点环）全量照走。
**F098 截图工具**——区域/窗口/全屏三模式（区域选择十字准星 + 放大镜像素取色），截图后直接进标注（画笔/马赛克/箭头）与保存——Windows Snipping Tool 的 VARIX 版，动态范围 UX 复用探索面。
**F099 计算器**——标准/科学双模式 + 历史记录 + 键盘全可达；按键按压态 100ms 红线照走。
**F100 时钟套件**——世界时钟/秒表/倒计时/闹钟四合一件，闹钟到点走通知中心（F077）。
**F101 天气件**——任务栏托盘显示 + 面板详情（走公开免费天气 API，无键不承诺），数据源诚实标注。
**F102 便签**——桌面粘性便签（多色/置顶/开机恢复），数据独立存储零真删。
**F103 画图件**——画笔/形状/取色器/撤销 50 步的轻量画板，画布 4K 画布坐标精度；开源参考 Inkscape 极小子集——只做「随手画」不做专业绘图（专业走第三方）。
**F104 录音件**——麦克风单键录音走 HDA 输入面，波形实时渲染 60fps，录音列表管理。
**F105 相册应用**——按时间/文件夹双视图浏览图片库，滑动缩放（Ctrl+滚轮 8 档）、全屏放映模式；图片解码全走 F054 SIMD 路径。
**F106 键盘提示 HUD**——按 CapsLock/NumLock 屏幕角落出状态提示（1s 淡出）；输入法切换同样有 HUD——键盘状态永远可见。
**F107 输入法状态浮窗**——中/英态、全/半角、标点态组合显示（悬浮于光标下方 8px），三态即时跟随——输入法的心智模型显性化。
**F108 自定义短语库**——打「yx」出常用句（C5 落地面），短语管理界面 + 导入导出（vxtheme 同路）；词条上限 1000 条，本地存储。
**F109 剪贴板历史**——Win+V 出最近 20 条（文本/图片/文件引用三型），固定钉选防驱逐；剪贴板隐私红线（B-3901 后台零读取）在历史面同样生效。
**F110 屏幕键盘**——触屏与应急场景的屏幕键盘（布局对齐物理键盘），按键回显走音效方案。
**F111 放大镜**——Win+加号全屏/镜头双模式放大，2x-16x，随光标移动；无障碍五判据（WP-207）的放大镜条目落地。
**F112 讲述人雏形**——焦点元素朗读（TTS 引擎评估开源 eSpeak-NG 接入），先覆盖设置中心与资源管理器两场景——无障碍不是加分项是条款（宪章第六章）。
**F113 高对比度主题**——纯黑/纯白两套高对比官方主题（对比度门禁 B-39xx 照走），一键切换即时生效（E1 承载）。
**F114 色弱辅助滤镜**——红/绿/蓝三向滤镜全屏色变换（合成器像素级后处理），E8 动效三档同级可关。
**F115 专注模式**——一键压暗非活动窗口（20% 降透）、静音通知、隐藏托盘气泡；计时器模式（专注 25 分钟）到点恢复——「沉浸」包括帮用户屏蔽干扰。
**F116 夜间模式**——色温随时间曲线（1900K-6500K 映射表可调），日出日落自动切换或定时；与 E1 主题联动（夜间自动换深色主题可选）。
**F117 首次开机向导**——五步引导（区域/网络/输入法/主题选择/完成页），每步可跳过、可随时重进（设置中心「系统-首次体验」）——空态引导（宪章十一章）的图形化实现。
**F118 欢迎中心**——开机首日弹「了解 VARIX」交互卡（五张卡讲清双域/下载/交接/个性化/帮助），看完即走不纠缠。
**F119 帮助中心**——离线手册（MD 文档渲染器复用），每页「去设置里做」直跳对应设置页——帮助即入口不是文档坟场。
**F120 诊断中心**——系统健康快照（F062）展示 + 日志导出（脱敏选项）+ 一键修复项（引导菜单修复/图标缓存重建）；错误呈现三要素文案宪法级执行。
**F121 系统还原点**——关键变更（装应用/改主题/更新）前自动快照配置层，设置中心一键回滚到任意还原点——数据层不动（零真删），只回滚配置与蜂巢。
**F122 更新体验面**——更新提示 toast（可延后三档：1h/今天/手动）、更新进度全屏画面（复用开机动画四幕语汇）、失败自动回滚 + 说明页——双槽更新（B-1304）的体验面。
**F123 关于本机页**——设备名/系统版本/硬件清单（CPU 型号/内存/盘）/体验版本号，每项可复制——诚实档案，不吹参数。
**F124 动画曲线总谱**——全部 UI 动画收敛为五条标准曲线（进入/退出/强调/弹性/线性）+ 三时长档（120/200/320ms），设计资产文件 `design/assets/motion-curves.md` 供全应用引用——动画系统是乐谱不是即兴。
**F125 体验域总判据**——宪章手感条款逐条映射到 F071-F124 的验收锚点，20 维度走查脚本以本清单为母本更新——**每个功能的验收都写在功能旁边**。

## F-D 生态与开放域深化（F126-F150）

**F126 开放格式宪法页**——vxapp/vxtheme/星图 JSON/判例格式四份规范在线文档化 + 示例包 downloadable；格式版本化承诺（破格必双读过渡）。
**F127 vxapp 打包工具**——开发者一条命令把目录打成 .vxapp（清单生成/签名/校验三步），工具本身开源；「给 VARIX 做包」的心智成本压到十分钟。
**F128 星图开放数据面**——兼容性评级目录全量 JSON 可下载（无账号无门槛），社区镜像自建合法——目录是公共品不是护城河。
**F129 社区判例提交线**——自测脚本包（只读形态）+ 提交模板 + 状态全链可查（B-4303 照走），判例验收从 AI01 独裁变成社区共证。
**F130 开源项目登记册**——全部借力件（Wine/Mesa/smoltcp/rustls/FFmpeg/eSpeak-NG/minizip 等）在册：版本/许可证/改动面/升级窗，十三件借力纪律的执行账本公开。
**F131 上游回馈通道**——对 smoltcp/ext4 crate 等修复的补丁按开源礼仪回馈上游（PR 模板/CLA 说明），回馈记录在册——**用开源也养开源**。
**F132 差异表公开**——Win32 兼容层的「不承诺清单」（API 未覆盖/语义差异）全量公开可查——诚实是生态的货币。
**F133 第三方图标包规范**——图标包目录结构/尺寸档位/三态要求文档化，社区包可在 E3 导入即用（过格式校验）。
**F134 主题市场…不做，主题分享页做**——星图内设「社区主题」分栏（纯目录非商店）：预览图+作者+下载链接（指向外部或 S: 侧载）——D 域「不做商店」与「发现好东西」兼容。
**F135 开发者文档站**——vx-SDK 文档/控件宪章/主题令牌表/交互词典全文在线，每页带可复制示例；文档与代码同库同版本。
**F136 示例应用仓库**——五个从 hello 到完整应用的官方示例（每步可跑），CI 保证示例永不腐烂（夜跑构建）。
**F137 API 稳定性承诺**——控件基类/系统 API 分稳定/实验两级标注，稳定级破面需 ADR+迁移窗——第三方敢投入的前提是承诺。
**F138 版本发布节奏公开**——季度升级窗（Wine/Servo 等借力件）日历公开，重大变更提前一个窗公告——生态参与者可预期。
**F139 反馈闭环通道**——诊断中心（F120）导出的报告一键提交进社区 tracker（匿名化），处理状态回查——反馈有去有回才有人继续反馈。
**F140 本地化开放**——全 UI 文案抽离为字符串表（JSON），社区翻译包 vxtheme 承载；英文/中文首发，格式开放——多语言不是墙内私活。
**F141 无障碍开放标准**——无障碍五判据的实现指南公开（焦点环登记协议/对比度门禁公式），第三方应用照做即合规。
**F142 安全披露通道**——security.txt + 漏洞报告邮箱 + 致谢页（贡献者名单），安全问题的处理时限承诺在册。
**F143 星徽与品牌资产包**——LOGO/壁纸/图标的多分辨率官方资产包开放下载（注明使用条款），社区创作有官方素材可用。
**F144 「Crafted for VARIX」徽标计划**——通过控件宪章默认合规（B-1103）+ 无障碍判据的应用授予徽标展示权——质量激励是生态的正向飞轮。
**F145 教育/作品集友好**——系统架构文档三册 + 本设计案本身作为「作品集级」公开读物（脱敏后）——VARIX 的设计过程本身是作品。
**F146 插件化星图后端**——星图目录数据源可替换（本地文件/社区镜像），格式不变——中心化不是架构假设。
**F147 跨设备主题同步前瞻**——vxtheme 包 + U 盘整机的天然移动性 = 主题跟人走（U 盘插哪台机器外观都一样），显性化为「你的桌面已就位」欢迎提示。
**F148 社区规则与治理**——星图收录标准/徽标授予标准/争议仲裁流程三份文档公开，规则先于规模——治理前置避免社区腐化。
**F149 季度生态报告**——兼容件数/社区提交数/借力件版本变动/徽标授予数四指标季报公开——生态健康度自己先晒。
**F150 生态域总判据**——开放性由「外部人 30 分钟内完成：下载规范→打包应用→提交判例→查到状态」全流程实测计时，超时即修流程——**开放不是口号是无障碍程度**。

## F-E 个性化域深化（F151-F170）

**F151 主题令牌全集**——颜色/间距/圆角/字号/动效五类令牌全表（JSON Schema 发布，F126 面），E1 换装 = 令牌表热替换；零硬编码纪律（B-1104）是技术地基。
**F152 实时预览编辑器**——设置中心「个性化」页：改令牌即时全屏预览（改圆角立即看效果），「应用/放弃」双按钮——所见即所得。
**F153 主题深浅自动切换**——按时间/日落日出自动在深浅主题对之间切换（跨零点无闪烁，动画 300ms）。
**F154 壁纸每日一换**——本地图片池轮换 + 「每日精选」可选（下载面，无商店原则照走）；切换动画交叉淡入 500ms。
**F155 图标包热更换**——E3 承载：换包不重启、失败回退上一包（E-1 铁律三步验收：改→生效→回退）。
**F156 指针编辑器**——指针热点/尺寸/动效在图形编辑器内调整，4K 双倍率 sprite 自动生成。
**F157 声音混合器**——E5 承载：事件音量独立调节 + 试听按钮 + 「全部静音」总闸。
**F158 开始菜单布局预设**——「效率/简洁/全屏」三官方预设一键切换（E6），用户自定义布局保存为第四套个人预设。
**F159 字体安全档**——E7 换字体前自动兼容性预检（缺字率扫描），缺字率 >5% 给黄色警告——个性化不许破坏可用性。
**F160 动效强度预演**——E8 三档切换前出对比演示动画（同一段动画三档并排预演），选完再应用。
**F161 个性化档案导出**——全部 E 域配置打包 vxtheme（F-E 铁律），含版本号与回滚链；换 U 盘/重装导入即复原。
**F162 每应用主题例外**——个别应用可脱离全局主题（「终端用深色其余浅色」），例外清单上限 10 项——全局一致性优先，例外显式可控。
**F163 桌面小组件开关集**——时钟/天气/系统快照（F062 数据）三枚官方小组件，桌面右键「添加小组件」启用；组件走 4K 管线与 E1 令牌。
**F164 锁屏定制**——锁屏壁纸/时间样式/状态信息（电量/日期）可配置，锁屏动画复用开机四幕语汇的静版。
**F165 开机动画个性化**——四幕动画的配色/粒子密度可调（结构不动），用户壁纸可作幕三「点亮」的过渡底——标准不可改，皮肤可以。
**F166 输入法皮肤**——候选窗配色/字体/透明度随 E1 主题或独立定制，组合期渲染延迟 ≤16ms。
**F167 右键菜单自定义**——用户可隐藏不常用菜单项（白名单恢复），应用注册的菜单项按使用频率自动排序可选。
**F168 任务栏个性化**——图标大小/居中或左对齐/自动隐藏三选项（对齐 Windows 档位），改后即时生效。
**F169 快捷键查看器**——全局快捷键总表页（按应用/按场景分组），冲突检测显式提示——快捷键是契约，契约要可查。
**F170 个性化域总判据**——E-1 三铁律（随时可改/随时可退/预设+微调）对 F151-F169 逐项实测打勾，任何一项「改完要重启」即整项不达标。

## F-G 内核底盘与安全深化（F171-F200）

**F171 图形化引导选单**——Limine 黑底文字选单换成星徽图形渲染（C-2 零代码开机条款的引导面），选单数据不变、渲染层重写。
**F172 引导自检可视化压缩**——kinfo 自检（9/9、11/11 全 PASS 实测面）收敛为一枚进度环 + 四个里程碑图标（内存/进程/存储/输入），文字全入后台日志。
**F173 panic 画面设计**——崩溃画面 ≠ 蓝屏复刻：VARIX 语汇的「星陨」画面（错误码+二维码指向帮助 F119+自动重启倒计时），panic 百次演练（B-2903）同步验证新画面。
**F174 诊断快照键**——任意时刻 Ctrl+Alt+D 静默生成系统快照（账本/日志/调用栈）存诊断中心，全程 UI 零打扰——给问题排查留「案发现场」。
**F175 崩溃隔离强化**——应用崩溃只崩应用（AI2 隔离纪律），崩溃后桌面帧率不跌、其他窗口可交互；崩溃应用的窗口标灰 + 「重新启动应用」按钮。
**F176 内存守卫**——内核堆红线越界检测（guard-host 自检已有）扩面到兼容层沙盒堆，越界即崩应用不崩内核——兼容面是重灾区，守卫先行。
**F177 能力执法可视化**——四个执法点（B-15xx）的拦截事件进通知中心（可关），用户看得见安全系统在工作——安全存在感是信任感。
**F178 签名状态角标**——未签名运行的应用窗口角标 + 任务栏悬停提示（不阻断，如实标注）——「知道自己在跑什么」是用户的权利。
**F179 权限审计页**——设置中心「隐私和安全性」：每应用的权限授予史/越权尝试/网络访问统计，一键收回——权限不是设完就忘。
**F180 断电演练自动化**——QEMU 断电百次脚本化夜跑（替身注入 B-4101），结果分布进周台账——数据红线靠机器守不靠人记得。
**F181 交接预检器**——切 Windows 域前自动预检（目标链存在/哈希/空间），预检不过给修复建议而非放行——防自锁闸门（三条件）的用户可见化。
**F182 双域时钟同步**——交接时 UTC 时间戳互写快照，跨域文件时间戳零漂移——小细节大信任。
**F183 存储健康监测**——U 盘 SMART 等价数据（ext4 错误计数/写入量累计）入设置中心存储页，寿命预估诚实区间显示——耗材也要透明。
**F184 热插拔体验**——U 盘/SHARED 卷插拔通知 + 安全弹出按钮（冲刷完成后才允许弹出，进行中写入则禁止并说明）——数据安全的日常形态。
**F185 只读卷保护提示**——NTFS 只读挂载（B-705）在资源管理器显示「只读」徽标 + 原因解释——限制有说明，不给用户「坏了」的错觉。
**F186 系统分区不可见**——引导/交接分区从资源管理器隐藏（防误写），设置中心「磁盘管理」高级页显式可见（只读）——红线让位给防呆。
**F187 时区与时钟守护**——RTC 漂移校正 + 时区切换向导（跨时区使用 U 盘整机的真实场景），NTP 走网络栈（B-60x）可用时自动校。
**F188 日志三环**——内核环形日志/系统日志轮转/应用日志三环分置，各环上限与覆盖策略在册（B-3802/3803 收尾判据承接），诊断中心可导出合并视图。
**F189 静默自愈集**——图标缓存损坏/主题令牌缺失/缩略图库损坏三类常见损坏自动重建（重建过程通知中心报备）——小病自愈不劳用户。
**F190 更新双槽可视**——A/B 槽当前态在设置中心显示（含上个版本回滚点保留期），更新前强制展示「回滚窗口」——B-1304/B-1305 的透明面。
**F191 安全启动链自查**——每次开机 peblock 门/签名链/W^X 三自查结果进诊断快照（F174），自查失败阻断桌面进入并显示图形化原因——启动链健康是第一公民。
**F192 内核参数白名单**——启动参数仅接受白名单（调试/降级/安全模式三族），未知参数拒绝并记录——引导面不接受自由文本。
**F193 安全模式**——禁用第三方驱动/默认主题/最小服务的救援模式（Limine 菜单图形条目），进入路径与退出路径都文档化——系统永远给自己留一条路。
**F194 审计日志完整性**——安全类日志（越权/门拦截/签名失败）append-only + 序号链（篡改可检测），导出带哈希——审计日志自己先不可抵赖。
**F195 资源配额执行**——每应用内存/IO 配额（MD2 附录 K）超限时降级不终止（后台应用优先降），配额面板可查——失控应用掐流量不掐命。
**F196 电池保护策略**——U 盘整机宿主电池（Y7000）电量 <15% 提示、<5% 主动冲刷+安全关机（B-2902 关机账目照走）——没电也要体面地睡。
**F197 温度感知降档**——宿主温度传感器可读时高温降档（联动 F069 性能三档自动切静音），数据不可读时优雅跳过（graceful 纪律同 NOWE 控制器实测）。
**F198 恢复环境**——镜像内嵌最小恢复环境（引导修复/还原点回滚 F121/日志导出三件），从安全模式（F193）或安装介质可达——最后的救命稻草也做成作品。
**F199 版本与谱系页**——「关于本机」展示系统谱系（STAR I → STAR I start 的版本树/本镜像构建指纹/可复现构建哈希）——每台机器知道自己从哪来。
**F200 全域总检**——F001-F199 的验收锚点汇成总检清单进 20 维度验收脚本，季检滚动复查；**200 项不是终点是基线——每季度审视一次，有现实契机才增补，永远不为功能多而加功能**（MD1 第 20.4 章纪律的全域版）。

## F 清单收尾说明

本清单与正文五域的关系：正文定方向与原则，F 清单定「做到什么算做到」。两项同步纪律：其一，正文修订时扫一遍 F 清单对应条目同步改（一处一事实）；其二，F 清单项开工前仍走 MD3 第 6 章「先落 WP 后记账」，不允许绕过判据直接施工。开源复用优先级写死：**有成熟开源件先评估接入，自研只做接缝**——这是 A5 与 F 清单共同的基因。

---


【设计细节】50 件清单来源：GitHub 各类星标榜加社区提名双轨（提名入口在星图页）；三关定义文档化（安装关/启动关/核心功能关各 3 到 5 条子判据）；录屏证据哈希入账（防证据替换）；黄灯强制附「为什么黄」一句归因；账本页支持订阅更新（社区通道 F129 推送）。
