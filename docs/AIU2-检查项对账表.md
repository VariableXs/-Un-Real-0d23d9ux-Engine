# AI-U2 检查项对账表 · genstar2 50 域 CheckSet × 主册判据（F451-F500）

> **对账口径**：本文把 `kernel/varix/src/genstar2/` 全部 **1456 项 CheckSet 检查项**（含 mod.rs 域聚合器 50 行子检查）逐域映射到主册《Varix STAR I start.md》I 通用域批次三/四的**验收判据锚点**。一处一事实：每项检查都能回答「它在验主册的哪句话」。
> **验证状态**：1456/1456 全绿（独立验证舱 `D:/_u2pod`：genstar2 + checks.rs 同源副本，镜像主仓 kernel/.cargo/config.toml 的 16MiB 测试栈口径；`cargo test` **439 passed / 0 failed**，域聚合 `genstar2_domain_aggregate_all_green` 50/50 全绿）。
> **批次沿革**：v1 判据实装 → 深化 v2（44/50）→ 深化 v3（16 模块）→ 深化 v4（7 模块+栈纪律）→ 深化 v5（11 模块）→ **深化 v6（3 模块：coolgov/appdata/termtheme）**。批次覆盖列见下表。
> **行数对账**：域内 51 文件 26,100 行 / 上限 53,690 = **48.6%**（v5+v6 净增 2,532 行）。
> **容量纪律**：单域 CheckSet ≤64 项（MAX_CHECKS），merge 去重 + dropped 如实累加；域聚合器对每子域断言「全绿且未截断」。

| F | 模块 | 批次覆盖 | 检查项数 | 判据锚口径 |
| --- | --- | --- | --- | --- |
| F451 | lnkwizard | v1+v2 | 36 | 四类目标创建用例；校验提示；图标继承；完成落位；断链衔接。 |
| F452 | foldicon | v1+v2 | 20 | 内置库清单（≥12 分类）；PNG 转换；三处同步刷新；恢复默认；持久化与备份联动。 |
| F453 | thumbbadge | v1+v2 | 23 | 五类角标语义表；尺寸/位置规范；实时同步（状态变更 <1s）；渲染走 F300 栅格；角标与缩略图共存（大图标视图不互遮… |
| F454 | foldtmpl | v1+v2 | 19 | 四类模板判定用例（抽样 20 目录准确率记录）；记忆优先级判据；一次性判定（不反复改用户视图）；模板与 F392 大小列… |
| F455 | deskicons | v1+v2 | 17 | 三枚独立开关；功能可达性（关显示后此机页 F456 照常）；位置记忆；开关即时生效；默认配置（回收站+此机开，文档化）。 |
| F456 | pcpage | v1+v2 | 26 | 三区内容清单；容量条与 F268 阈值联动；悬停明细准确性；设备区实时性；页打开 <1.5s（F404 判据复用）。 |
| F457 | searchmath | v1+v3 | 46 | 表达式支持集（四则/括号/百分比/幂用例各 5）；结果卡时序 <200ms；复制功能；与搜索结果共存排序；不支持静默降级… |
| F458 | unitconv | v1+v2+v3 | 31 | 六族×2 用例；区域默认单位；显式目标单位解析；精度来源登记（系数表入册）；复制与降级（非换算当搜索）。 |
| F459 | webfallback | v1+v2+v4 | 33 | 兜底入口常驻与排序；Edge 跳转参数；网址识别用例（域名/IP/带路径）；可关设置；与 F307 历史共存。 |
| F460 | vxdict | v1+v2+v4 | 25 | 导出格式与可读性；导入两模式与冲突计数；合并结果抽查；备份联动；非支持格式诚实提示。 |
| F461 | fgmute | v1+v2+v4 | 25 | 聚焦判定（<200ms 身份切换）；横幅抑制与中心入库；多窗口应用（任一窗聚焦即算前台）；关闭开关；与其他静默档（F34… |
| F462 | setwall | v1+v2+v3 | 24 | 一步应用；所在屏判定；五式就地切换；撤销；引用不驻留；与 F286/F297 压暗联动。 |
| F463 | imgops | v1+v2+v3 | 30 | 四操作用例；方向二选一询问；副本命名规则；格式转换质量档；原图哈希不变判据。 |
| F464 | trashdrag | v1+v2+v4 | 29 | 拖出还原落点准确性；原位还原并存；清单/角标即时性；与 F261/F414 删除语义闭环；冲突处理（目标有同名 F087… |
| F465 | termalias | v1+v2 | 22 | 十别名输出与图形界面对账；覆盖自定义；help 完整性；无参/有参行为；未知名错误与人话建议（「没有 vx——最接近的是… |
| F466 | termclip | v1+v2+v3 | 32 | 两套习惯开关；右键粘贴；多行确认缓冲；去引号适配；与 F338/F339 互通判据。 |
| F467 | termfont | v1+v2+v3 | 32 | 五档边界；提示时序；会话/默认两级记忆；与全局字号不冲突（独立域审计）；滚轮通路（F204 惯性不用于终端——逐档切换非… |
| F468 | cmdhist | v1+v2+v4 | 31 | 回溯/搜索/持久化三用例；敏感行过滤（10 例注入）；1000 条上限淘汰；清空双入口；历史文件权限（用户级只读他者）。 |
| F469 | termcolor | v1+v4 | 19 | 三色语义用例；重定向去色判据；主题令牌联动对比度；第三方输出原样性；关闭开关。 |
| F470 | termdir | v1+v2+v3 | 23 | 三源设置行为；上次目录持久化；F338 路径优先判据；多标签独立；默认值文档化。 |
| F471 | scrollback | v1+v2+v4 | 29 | 缓冲上限与淘汰；跟随/徽标/恢复三态；选择时暂停判据；滚动性能（万行回滚 60fps）；清屏命令语义（cls 清视窗不清… |
| F472 | termtheme | v1+v2+v6 | 29 | 跟随切换即时性；六预设对比度全检（≥4.5:1）；自定义四要素；透明度红线复判；预设色板与令牌派生关系。 |
| F473 | setkbd | v1+v2 | 20 | 五步全键盘链路录屏；就地卡 Enter 切换；面包屑键盘；焦点回归链（F206）；典型 10 设置任务计时记录。 |
| F474 | setdesc | v1+v2+v5 | 28 | 三件套覆盖率审计（全设置项扫描）；文案句式规范抽查；禁用态说明；链接有效性（死锚=0）；术语表一致性（同一概念全系统同名… |
| F475 | idiffreg | v1+v2+v5 | 28 | 差异表完整性（正文差异点全收录）；三栏格式统一；公开同步；复审条款；对齐项计数与正文 200 项对账。 |
| F476 | envedit | v1+v2+v3 | 32 | 双栏编辑全操作；留痕；撤销栈；空 PATH 红色确认；生效时机说明文案。 |
| F477 | bootrepair | v1+v3 | 41 | 两连败触发阈值；三卡功能链路；人话诊断准确性（注入三类故障各测）；选项说明文案审查；复盘通知。 |
| F478 | avatar | v1+v2+v5 | 35 | 十二枚内置清单；裁剪交互；三处同步；默认剪影渲染；图片大小上限与压缩。 |
| F479 | hostname | v1+v2+v5 | 32 | 默认名规则；改名校验与生效；三处同源审计；F321 发现列表联动；非法字符拒绝用例。 |
| F480 | chantest | v1+v2+v3 | 38 | 左右独立测试与屏幕高亮同步；默认 40% 音量；设备路由正确性；无声诊断提示（「左声道无声——检查接口或换设备」）。 |
| F481 | touchpad | v1+v2+v5 | 29 | 双设备独立记忆；自然/传统切换即时；掌压三档用例（打字注入误触测试）；速度档实测；设置持久化。 |
| F482 | swapbtn | v1+v2+v5 | 28 | 交换后全语义镜像用例（框选/拖拽/菜单三链路）；文案镜像抽查；中键不变；交换即时性；持久化。 |
| F483 | wheeldir | v1+v2+v5 | 25 | 双设备独立开关；语义翻转正确性（内容移动方向）；惯性曲线不变判据；即时性；持久化。 |
| F484 | vpnlite | v1+v3 | 42 | 添加/连接/断开链路；归因映射表；流量计数；协议支持清单与诚实提示；配置持久化与自动重连选项。 |
| F485 | sysproxy | v1+v3 | 35 | 手动配置链路；即时生效（新连接验证）；例外列表；诊断联动；与 F242/F295 数据同源。 |
| F486 | recentmgr | v1+v2 | 20 | 列表准确性；逐删/全清/三处同步；固定 5 条上限；隐私模式不落盘判据；清除确认文案。 |
| F487 | privacysweep | v1+v2+v5 | 28 | 四类清除链路；条数预览准确；总开关；关机自动清（重启后验证全空）；不可恢复确认文案。 |
| F488 | appdata | v1+v2+v6 | 24 | 三类计量准确性（F392 服务同源）；清缓存只清缓存判据；文档类只读；路径直达；大小排序。 |
| F489 | shutbadge | v1+v2+v5 | 28 | 两阶段进度显示；>10s 归因显示；正常路径无数字；与 B-2902 账目对账；强断路径（F318）不受影响。 |
| F490 | shutblock | v1+v2+v5 | 26 | 阻止者列表准确性；默认焦点判据（F207 同源）；仍然关机+抢救链路；逐应用等待；列表实时性。 |
| F491 | autolum | v1+v2+v3 | 26 | 传感器缺失隐藏判据；2s 渐变；30% 下限；手动覆盖 2h 计时；开关默认关。 |
| F492 | powplan | v1+v3 | 36 | 四参数×双场景矩阵；即时生效；留痕（F372）；恢复默认；参数与 F069/F196 底层联动一致性。 |
| F493 | coolgov | v1+v2+v6 | 28 | 两策略+自动三选；切换即时；温度读数与传感器对账；自动档触发阈值（与 F197 同源）；策略持久化。 |
| F494 | taskautohide | v1+v2+v5 | 31 | 滑出 <200ms；2px 提示线；全屏抑制判据（注入全屏视频测试）；3s 收回；开关即时与持久化。 |
| F495 | taskoverflow | v1+v2 | 15 | 折叠触发与宽度计算；展开列表行为一致性；顺序稳定性；全名显示；拖拽排序在溢出态的表现（支持拖回）。 |
| F496 | tilelongpress | v1+v2+v5 | 33 | 500ms 与进度环；六代表磁贴直达页对照；与编辑态冲突消解用例；误触率（快速点击 0 误进）；即时性。 |
| F497 | ncactrow | v1+v2+v5 | 29 | 三枚常驻与功能；轮档即时（图标态同步 F341）；毛玻璃白名单合规；与分组清除（F330）层级关系；键盘可达（F206）… |
| F498 | dpifix | v1+v2+v3 | 32 | 模糊检测判据（非 aware 应用识别）；一键应用与重绘；不再提示；与 F028 三态底层联动；提示一次性记账。 |
| F499 | lockwall | v1+v2+v3 | 21 | 解耦与默认共享；四款模式；压暗联动；mini 预览实时性；设置持久化（F396 备份范围含）。 |
| F500 | xmovkey | v1+v2 | 15 | 跨屏搬移相对位置精度；贴靠重排；单屏静默；动画时长；组合键注册（F244）。 |
| mod.rs 域聚合器 | — | v1 | 50 | 每子域一行「全绿且未截断」账 |
| **合计** | 51 文件 | — | **1456** | |
