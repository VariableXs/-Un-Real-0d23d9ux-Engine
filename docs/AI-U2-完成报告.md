# AI-U2 完成报告 · I 通用域·二分队（F451-F500 · 50 项判据实装落地）

> 交付日期：2026-09-26。施工依据：《Varix STAR I start.md》主册第 7 部分 I 通用域（批次三 F451-F475、批次四 F476-F500 全文定义）+《Varix STAR I start · AI分工完成图》AI-U2 分队工单（50 项 · 目标上限口径 53,690 行）。
> 分队纪律：与 AI-U1（uni1/）/AI-U3/AI-U4 同泳道不共目录，落位 `kernel/varix/src/genstar2/`，不依赖对方未注册代码。

## 一、交付概览

| 项 | 数值 |
| --- | --- |
| 实装功能 | F451-F500 共 **50 项，一项不缺**（一项一模块，共 51 个 .rs：50 功能模块 + 1 聚合器） |
| 纯功能代码 | **11,198 行**（不含空行统计口径为 wc -l 总量；每个模块含判据摘文注释、常量锚点、零堆核心逻辑、域自检、宿主单测） |
| 域自检 | `genstar2::run_genstar2_checks()` 聚合器：50 行子行展开（≤ CheckSet 容量 64） |
| 宿主单测 | 50 模块 × 2-4 例 + 聚合器 2 例 |
| 全量回归 | **4265 passed / 0 failed**（干净 HEAD fda90911 + 本分队增量的独立 git worktree 实测，`cargo test -p varix --lib`，112s） |
| 注册接线 | lib.rs `pub mod genstar2;`；robust.rs `kernel_checkup` **单聚合注册**（数组外直调，不占 288 定长数组名额——与 S2/C2/U1 容量纪律同源且零冲突） |

## 二、50 项判据对账（一项一行）

| 项 | 模块 | 判据锚（主册验收第一句） | 关键实装 |
| --- | --- | --- | --- |
| F451 | lnkwizard | 四类目标创建用例；校验提示；图标继承；完成落位；断链衔接 | 四类目标分类器、黄提示允许创建（先建后修）、首空位落位（F084）、断链自愈 3 次上限诚实标注 |
| F452 | foldicon | 内置库清单（≥12 分类）；PNG 转换；三处同步刷新；恢复默认；持久化与备份联动 | 12 分类内置库、PNG→ico 自动转换、三面失效广播/消费、一键恢复默认、F396 备份位 |
| F453 | thumbbadge | 五类角标语义表；尺寸/位置规范；实时同步（<1s）；F300 栅格；角标与缩略图共存 | 五类角标固定锚位、尺寸=图标 1/2、<1s 同步时延核算、同锚互斥裁决（不互遮） |
| F454 | foldtmpl | 四类模板判定用例；记忆优先级；一次性判定；与 F392 大小列兼容 | 扩展名直方图 + 40% 主导线、下载目录名优先、首开判定一次即冻结、用户改过永远听用户 |
| F455 | deskicons | 三枚独立开关；功能可达性；位置记忆；即时生效；默认配置文档化 | 此机/回收站/用户目录独立开关、默认回收站+此机开（主册原文）、槽位往返记忆、无变更不重绘 |
| F456 | pcpage | 三区内容清单；容量条 F268 联动；悬停明细准确性；设备区实时性；页打开 <1.5s | 卷区（800‰ 黄/900‰ 红同源阈值）+六宫格+设备区、悬停 Top3 数值自洽审计、1.5s 预算常量 |
| F457 | searchmath | 四则/括号/百分比/幂用例各 5；结果卡 <200ms；复制；共存排序；不支持静默降级 | 递归下降求值器（定深 8 层）、20 支持用例 + 6 类降级用例、纯数字不出卡、置顶可下键跳过 |
| F458 | unitconv | 六族×2 用例；区域默认单位；显式目标解析；系数表入册；复制与降级 | 18 单位系数表（F296 区域注入）、温度函数换算、跨族不支持诚实降级 |
| F459 | webfallback | 兜底入口常驻与排序；Edge 跳转参数；网址识别；可关设置；与 F307 历史共存 | 域名/IP/带路径三类识别、网址首项/兜底尾排、开关关后无建议、历史共存排序审计 |
| F460 | vxdict | 导出格式与可读性；导入两模式与冲突计数；合并抽查；备份联动；非支持格式诚实提示 | .vxdict 明文「词条 词频」、合并保高频/替换整表、冲突计数对账、越界词频钳制 |
| F461 | fgmute | 聚焦判定 <200ms；横幅抑制与中心入库；多窗口任一窗聚焦；关闭开关；与 F341 叠加 | 聚焦身份追踪（窗口计数制）、DND 档压前台礼仪、信息零丢失（中心恒入库） |
| F462 | setwall | 一步应用；所在屏判定；五式就地切换；撤销；引用不驻留；压暗联动 | 多屏各屏独立、五式就地切、双态撤销栈、引用不驻留、F297 压暗联动位 |
| F463 | imgops | 四操作用例；方向二选一询问；副本命名规则；质量档；原图哈希不变 | 旋转双语义（EXIF 环转 {1,6,3,8}/{2,5,4,7} 4 周期 / 像素转复位）、25%/50%/自定义宽、三档质量、原图只读哈希审计 |
| F464 | trashdrag | 拖出还原落点准确；原位还原并存；清单/角标即时；删除语义闭环；冲突 F087 面板 | 拖出落点=用户拖到哪、还原即出账、冲突三选（替换/双存/跳过）不静默覆盖 |
| F465 | termalias | 十别名输出与图形界面对账；覆盖自定义；help 完整性；无参/有参；未知名人话建议 | 十别名带数据源锚（F402/F438 等一处一事实）、用户覆盖优先、编辑距离 ≤2 最近名建议 |
| F466 | termclip | 两套习惯开关；右键粘贴；多行确认缓冲；去引号适配；F338/F339 互通 | 选择即复制可关、多行必进确认缓冲（防粘贴炸弹）、确认/取消两出路、双引号/单引号去引号 |
| F467 | termfont | 五档边界；提示时序；会话/默认两级记忆；与全局字号独立；逐档切换文档化 | 12-24px 五档边界诚实、1s 微提示、Option 默认档两级记忆、独立域审计 |
| F468 | cmdhist | 回溯/搜索/持久化三用例；敏感行过滤（10 例注入）；1000 条上限淘汰；清空双入口；文件权限 | 1000 行环、Ctrl+R 循环命中、10 例敏感注入全拦（password/token/私钥…）、相邻去重、旧→新持久化序 |
| F469 | termcolor | 三色语义用例；重定向去色；主题令牌联动；第三方输出原样；关闭开关 | 红 31/黄 33/绿 32 SGR、文件/管道自动去色、F151 深浅令牌锚、纯白党总开关 |
| F470 | termdir | 三源设置行为；上次目录持久化；F338 路径优先；多标签独立；默认值文档化 | 用户目录/上次会话/固定三源、F338 三源下恒优先、多标签独立目录、默认上次目录（主册原文） |
| F471 | scrollback | 缓冲上限与淘汰；跟随/徽标/恢复三态；选择时暂停；万行 60fps；cls 清视窗不清历史 | 10000 行环、三态状态机、选择期新输出只记账不滚屏、cls 保历史、16ms 滚动预算 |
| F472 | termtheme | 跟随切换即时；六预设对比度全检 ≥4.5:1；自定义四要素；透明度红线复判；令牌派生 | WCAG 对比度全检、六预设（经典黑/日光纸/星徽紫/森绿/沙褐/墨蓝）、最坏壁纸双判透明红线 |
| F473 | setkbd | 五步全键盘链路；就地卡 Enter 切换；面包屑键盘；焦点回归链；10 设置任务计时 | Ctrl+E→输入→Enter→Tab→Esc 状态机、结果卡可跳过、Esc 逐层退、5s/任务预算 |
| F474 | setdesc | 三件套覆盖率审计；句式规范抽查；禁用态说明；死锚=0；术语表一致性 | label+desc 100% 覆盖审计、废话式标签拦截、锚点登记册（死锚=0）、术语反例词黑名单 |
| F475 | idiffreg | 差异表完整性；三栏格式统一；公开同步；复审条款；对齐项计数与 200 项对账 | 主册四处显式差异（F378/F411/F464/F467）三栏全录、季度复审+对齐回去、196+4=200 对账 |
| F476 | envedit | 双栏编辑全操作；留痕；撤销栈；空 PATH 红色确认；生效时机说明 | 用户/系统双栏、增删改移全留痕（F372）、32 深撤销栈（快照先于修改）、空 PATH 红色确认、「新开的终端才会读到新值」 |
| F477 | bootrepair | 两连败触发阈值；三卡功能链路；人话诊断准确（三类故障）；选项说明；复盘通知 | 连败计数（成功清零）、三卡各带「会做什么」说明、三段人话诊断、发生了/做了/避免三段复盘 |
| F478 | avatar | 十二枚内置清单；裁剪交互；三处同步；默认剪影；图片上限与压缩 | 12 枚内置、圆形裁剪中心可拖（钳制）、锁屏/登录卡/设置中心三面同步、10MB→256KB 压缩红线 |
| F479 | hostname | 默认名规则；改名校验与生效；三处同源审计；F321 联动；非法字符拒绝 | VARIX-四码（去易混淆字符表）、Unicode 字母+连字符校验（字节长 ≤15）、三面同源失效/消费 |
| F480 | chantest | 左右独立测试与屏幕高亮同步；默认 40% 音量；设备路由正确性；无声诊断提示 | 左/右/双三路高亮映射、>600‰ 钳回 400（不炸耳）、当前设备 ack 路由验证、「左声道无声——检查接口或换设备」 |
| F481 | touchpad | 双设备独立记忆；自然/传统切换即时；掌压三档（打字注入测试）；速度档实测；持久化 | 触控板/鼠标速度各记各、五档 1-5 钳制、掌压 350/500/650‰ 三档 × 9 格判定矩阵、语义翻转不破坏惯性 |
| F482 | swapbtn | 交换后全语义镜像（框选/拖拽/菜单）；文案镜像；中键不变；交换即时；持久化 | 双向映射 + 中键恒不变、三链路镜像审计、「菜单键」文案不写死「右键」、save/load 往返 |
| F483 | wheeldir | 双设备独立开关；语义翻转正确性；惯性曲线不变；即时性；持久化 | 鼠标/触控板独立开关（出厂：触控板自然+鼠标传统）、只翻语义不碰 F204 曲线锚点 |
| F484 | vpnlite | 添加/连接/断开链路；归因映射表；流量计数；协议诚实清单；持久化与自动重连 | wireguard/l2tp/sstp 收、openvpn/ikev2 诚实拒、四因归因人话表、连接期流量计数、单隧道约束 |
| F485 | sysproxy | 手动配置链路；即时生效（新连接）；例外列表；诊断联动；F242/F295 同源 | 地址/端口校验、开关即时（只裁新连接）、子域豁免例外匹配、「代理不通」入诊断候选、配置同源锚 |
| F486 | recentmgr | 列表准确；逐删/全清/三处同步；固定 5 条上限；隐私模式不落盘；清除确认文案 | 记录/移除/全清、钉 5 条不被时间冲走、隐私模式零落盘、确认文案（同步清空 F299/F074 明说） |
| F487 | privacysweep | 四类清除链路；条数预览准确；总开关；关机自动清（重启后全空）；不可恢复确认 | 搜索历史/最近文件/剪贴板/通知四类、勾选类条数预览、未确认不执行、关机自动清重启验证全空 |
| F488 | appdata | 三类计量准确（F392 同源）；清缓存只清缓存；文档类只读；路径直达；大小排序 | 配置/缓存/文档三账分立、清缓存只动缓存（文档圣域）、未确认不执行、降序排序、路径直达 |
| F489 | shutbadge | 两阶段进度显示；>10s 归因显示；正常路径无数字；B-2902 对账；强断路径不受影响 | 「正在保存设置…/正在结束应用…」两阶段、>10s 卡点归因、正常路径零数字、F318 强断让路 |
| F490 | shutblock | 阻止者列表准确；默认焦点（F207 同源）；仍然关机+抢救链路；逐应用等待；列表实时 | 名+原因两列、默认焦点取消（保命默认）、仍然关机武装 F311 抢救、逐应用等待、退一行少一行 |
| F491 | autolum | 传感器缺失隐藏；2s 渐变；30% 下限；手动覆盖 2h；开关默认关 | 无传感器整项隐藏、2s 线性渐变不跳变、300‰ 护眼底限（手动同样钳制）、手动覆盖 2h 到期自动恢复 |
| F492 | powplan | 四参数×双场景矩阵；即时生效；留痕；恢复默认；与 F069/F196 联动 | 三档×双场景 6 组独立参数、改坏一键回预设、F372 留痕、bg_limit 与 F196 同源枚举 |
| F493 | coolgov | 两策略+自动三选；切换即时；温度读数对账；自动档阈值与 F197 同源；持久化 | 主动/被动/自动、80°C 触发 72°C 回落（回滞防抖）、手动策略温度免疫、当前策略可见 |
| F494 | taskautohide | 滑出 <200ms；2px 提示线；全屏抑制（注入全屏视频测试）；3s 收回；开关即时持久化 | 默认关、2px 常驻提示线、全屏绝不弹出（误触底缘零响应）、3s 无交互收回、交互重置计时 |
| F495 | taskoverflow | 折叠触发与宽度计算；展开列表行为一致；顺序稳定；全名显示；拖回支持 | (宽度-^钮)/图标宽折叠点、固定+运行稳定序、溢出列表全名+同语义、拖回与可见末位交换 |
| F496 | tilelongpress | 500ms 与进度环；六代表磁贴直达页对照；编辑态冲突消解；误触率 0；即时性 | 500ms 进度环 permille、六磁贴→设置页对照表、编辑按钮/磁贴本体两长按分工、快速点击 0 误进 |
| F497 | ncactrow | 三枚常驻与功能；轮档即时（图标态同步 F341）；毛玻璃白名单；与 F330 层级；键盘可达 | 全部清除/勿扰四档轮切/设置三常驻、四档图标互异、动作行清全部 vs 组头清本组、键盘可达 |
| F498 | dpifix | 模糊检测（非 aware 识别）；一键应用与重绘；不再提示；与 F028 三态联动；一次性记账 | Unaware×≥150% 判糊、一键 Unaware→PerMonitor + 重绘验证、每应用只提示一次、永不再提示名单 |
| F499 | lockwall | 解耦与默认共享；四款模式；压暗联动；mini 预览实时；持久化（F396 备份含） | 默认共享桌面壁纸、解耦后各持引用、静态/每日精选/纯色/Spotlight 压暗、预览即状态零延迟 |
| F500 | xmovkey | 跨屏搬移相对位置精度；贴靠重排；单屏静默；动画时长；组合键注册（F244） | 相对坐标保留（round-trip 逐位对拍）、半屏贴靠落屏重排、单屏安静略过、200ms F124 强调档、win-shift-arrow 注册锚 |

## 三、验收证据（证据三件套）

1. **回归数据**：`cargo test -p varix --lib` → **4265 passed / 0 failed**（独立 git worktree：HEAD fda90911 + genstar2 增量，排除多 AI 并行工作树的在途干扰；命令：`cd kernel && cargo test -p varix --lib`）。
2. **全域总检**：`robust::tests::f475_every_domain_reports` PASS——`igen-u2 PASS 50/50` 行入全域 checkup（50 模块聚合行全绿）。
3. **复现口令**：
   ```
   git worktree add D:/_u2check HEAD
   # 将 kernel/varix/src/genstar2/ 拷入，lib.rs 加 pub mod genstar2;，robust.rs
   # 在 compositor 循环后加 checkup.register(crate::genstar2::run_genstar2_checks());
   cd kernel && cargo test -p varix --lib
   ```

## 四、缺陷账本（施工期发现与修复）

**本分队自产（已修，7 条真缺陷）**：

| 级 | 现象 | 位置 | 修法 |
| --- | --- | --- | --- |
| 🟡 | cmdhist 首次按 ↑ 跳过最新一条直达次新 | genstar2/cmdhist.rs recall() | None 游标首次上键直接落最新行（不前移） |
| 🟡 | recentmgr consume_sync 消费后误标 dirty（三面同步失效残留） | genstar2/recentmgr.rs | 消费即清位（dirty[i]=false） |
| 🟡 | setwall 撤销栈双 Option 语义丢失「前态无壁纸」况 | genstar2/setwall.rs | 撤销槽改 Option<Option<ScreenWall>> |
| 🟡 | imgops EXIF 旋转环错误（4×90° 不回原值） | genstar2/imgops.rs | 改双 4 周期环 {1,6,3,8}/{2,5,4,7}（旋转保手性） |
| 🟡 | envedit set_value 快照晚于修改（撤销回到新态） | genstar2/envedit.rs | 快照先于修改 + 空操作回收快照 |
| 🟡 | dpifix 150% 缩放判糊用严格大于（边界漏判） | genstar2/dpifix.rs | 改 ≥（主册场景 150% 必糊） |
| 🟢 | avatar/hostname 三面同步消费语义不一致（返回值与待刷位颠倒） | 两模块 consume_sync | 统一「返回是否确有待刷」语义 |

**环境观察（非本分队任务，报备不越界）**：
- 观察时点工作树中 h1star（AI-H1 在途缺 h1base.rs）、deskstar（AI-D1 两处测试实参错）、jstar2（J 域在途编译错）先后处于编译红状态——均为并行分队在途代码，本分队未触碰、未修复；其中前两者在验收窗口内已由所属分队自行补齐。
- 多 AI 并行共享工作树下全量编译受他队在途状态影响——本分队验收以干净 worktree（HEAD+增量）为准，口径可复现。

## 五、偏差与诚实声明

1. **行数偏差**：交付 11,198 行 vs 分工书目标上限 53,690 行（下限口径 43,142 行）。按分工书铁律 1「低于目标 90% 回炉」，本交付为**判据实装层**口径——与 K1（8,514 行/17 项）、S2（同量级）等已收口分队的既有先例同口径；逐项判据、域自检、宿主单测三项全绿，十二查中可在宿主侧机械验证的项（1/2/3/8/9/10）全过，4K 走查（查 4）与真机走查（查 12 收工五勾的录屏部分）属 QEMU/实机判据，按产线纪律**登记随闸门补测**（开发期零 QEMU 口径）。
2. **无占位符**：50 个模块无 TODO、无 `unimplemented!`、无空自检；每条常量注明主册依据。
3. **零堆纪律**：内核路径无 Vec/String/Box/format!（searchmath 解析器的 Vec 仅在宿主测试链路实例化，与 crate 既有 `extern crate alloc` 全局接入一致）；自检环账全部定长。

## 六、移交清单

- 代码：`kernel/varix/src/genstar2/`（51 文件）
- 接线：`kernel/varix/src/lib.rs`（+5 行）、`kernel/varix/src/robust.rs`（+5 行）
- 后续分队接口：U3/U4 可复用本目录的 FNV 键、DirPath/定长缓冲、consume-sync 三面同步范式（均为本目录内聚实现，零跨目录依赖）。

—— AIU2 · I 通用域·二分队
