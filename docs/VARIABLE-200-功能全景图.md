# VARIABLE-200 功能全景图（完整版 · 内核并入 Variable 系统）

> F001~F200 全部 200 项功能，全部属于"Varix 内核并入 Variable 系统"桥接工程，8 域各 25 项。
> 与《VARIX-500-功能全景图》（内核本体 F001~F500）互补、零重复：
> 500 项造发动机，200 项把整车装上轮子开走。
> 每项标注归属 AI 与航程（W2/W3/W5）。状态：⬜ 未开始 / 🔶 进行中 / ✅ 完成

---

## AI-01 ✅ 用户态进程域（F001~F025，W2）

- ✅ F001 每进程地址空间 — 独立页表、内核半区共享、切换时 CR3 原子换装
- ✅ F002 ring3 切换原语 — sysret/iret 双路进入用户态，寄存器现场零损
- ✅ F003 用户栈建设 — 按进程分配守卫页栈、栈溢出即时致命而非踩内存
- ✅ F004 用户堆 — 用户态 brk 式增长，内核按需供页
- ✅ F005 TSS/IST 复核 — 每核 TSS、双故障专用栈在进程切换下依然正确
- ✅ F006 进程控制块 — PID、状态、地址空间、句柄表、记账信息一体
- ✅ F007 进程表与上限 — 固定容量进程表、分配/回收闭环、耗尽可诊断
- ✅ F008 上下文切换 — 完整保存恢复通用/浮点/调试寄存器，切换路径可测
- ✅ F009 spawn 原语 — 从镜像路径创建新进程，参数与环境传递
- ✅ F010 等待与回收 — wait 式语义、zombie 状态、孤儿进程由 init 收养
- ✅ F011 进程退出 — 优雅退出路径、资源全量回收（页表/句柄/内存）零泄漏
- ✅ F012 信号框架 — kill/term/segv 递送、默认行为表、用户态处理注册
- ✅ F013 崩溃隔离 — 用户态页错误/非法指令只杀本进程，内核稳如泰山
- ✅ F014 句柄表 — 进程私有句柄空间、类型化校验、进程消亡自动回收
- ✅ F015 能力模型 — 进程能力位（io/fs/proc/exec），最小授权
- ✅ F016 优先级与亲和 — 32 级优先级、CPU 亲和掩码、与调度器（AI-04 既有）对接
- ✅ F017 进程预算 — CPU 时间片记账、超配额降级，防单进程饿死全系统
- ✅ F018 进程命名空间 — 命名查询、父子树快照，供任务栏/监控消费
- ✅ F019 CoW fork（可选增强）— 写时复制派生，克隆即用
- ✅ F020 用户态页错误处理 — 缺页供页、真错误精确杀进程并带 RIP 报告
- ✅ F021 core dump — 崩溃现场（寄存器/栈/映射）落盘，可回读分析
- ✅ F022 调试接口 — 读写目标进程内存、单步挂接（供开发者生态 AI-20 对接）
- ✅ F023 进程记账仪表 — 每进程 CPU/内存水位环形统计，零成本采样
- ✅ F024 线程模型 — 同地址空间多执行流、每线程独立内核栈
- ✅ F025 进程域自检 — 25 项 CheckSet 全绿：隔离性、切换正确性、回收无泄漏

## AI-02 ✅ ELF 加载与 ABI 域（F026~F050，W2）

- ✅ F026 VXELF 格式定稿 — Varix 可执行格式规范：magic、版本、ABI 版本、入口约定
- ✅ F027 格式规范文档 — 一页纸规范 + golden 样例镜像，开发与验收共用
- ✅ F028 段加载器 — PT_LOAD 逐段映射，文件偏移/虚地址/权限三元组精确落位
- ✅ F029 W^X 执行纪律 — 页面不可同时写+执行，加载完成即锁权限
- ✅ F030 用户态 ASLR — 镜像基址/栈/堆随机化，熵足够且可复现调试
- ✅ F031 入口栈布局 — argc/argv/envp/auxv 按规范摆栈，与 varix-std 对齐
- ✅ F032 auxv 完整性 — AT_ENTRY/PHDR/_PAGESZ 等关键项齐全
- ✅ F033 TLS 支持 — ELF TLS 段解析、fs 基址配置、线程局部模型
- ✅ F034 静态链接优先 — 首版只支持静态链接，动态留接口（降级纪律）
- ✅ F035 重定位引擎 — RELA 处理、符号解析骨架（动态启用时用）
- ✅ F036 镜像校验 — 头部魔数/版本/越界检查，非法镜像明确拒绝而非半加载
- ✅ F037 镜像签名校验 — 可选签名验证开关，交付链完整性
- ✅ F038 只读段保护 — .rodata/.text 映射只读，写访问即崩并报告
- ✅ F039 加载失败恢复 — 任何一步失败：进程不创建、内存全量回收、错误码明确
- ✅ F040 ABI 版本协商 — 镜像声明所需 ABI，内核不匹配时拒绝并说明
- ✅ F041 共享库骨架 — 动态链接预留接口（路径/缓存/预链接设计占位）
- ✅ F042 调试信息保留 — symtab/strtab 不剥离策略，崩溃符号化可用
- ✅ F043 加载性能 — 加载耗时仪表、大镜像流式加载不卡引导
- ✅ F044 加载器 fuzz — 恶意/畸形镜像 10⁵ 次零内核态污染
- ✅ F045 多镜像并发加载 — 并发 spawn 下加载器线程安全
- ✅ F046 资产文件识别 — 可执行/数据/脚本类型判定与分流
- ✅ F047 shebang 式解释器 — 脚本首行转解释器执行（生态友好）
- ✅ F048 环境注入 — 内核级 env 注入通道（调试旗标/日志级别）
- ✅ F049 加载自检 — 加载器 25 项 CheckSet：合法性、W^X、失败回收
- ✅ F050 首个用户程序 — `hello`（rustc 裸 target 静态编译）ring3 运行并 syscall 打印

## AI-03 ✅ 系统调用域（F051~F075，W2）

- ✅ F051 syscall 入口 — syscall/sysret 通道搭建，MSR（LSTAR/STAR/FMASK）配置
- ✅ F052 int 0x80 备路 — 兼容/调试备路，可切换验证（降级纪律）
- ✅ F053 号表治理 — 系统调用号集中注册、区间分配、未实现号明确 ENOSYS
- ✅ F054 参数安全层 — copyin/copyout：用户指针逐一校验，杜绝内核解引用用户态垃圾
- ✅ F055 错误码 ABI — 错误码全表定稿，用户态/内核态语义一致
- ✅ F056 write/read — 控制台与文件描述符读写，日志直达串口/帧缓冲
- ✅ F057 exit/wait — 进程退出与回收的 syscall 面
- ✅ F058 时间调用 — 墙钟/单调钟/时钟精度查询，与 HPET/TSC 源对接
- ✅ F059 mmap 式内存调用 — 匿名/文件映射、共享映射、mprotect 权限调整
- ✅ F060 事件端口 — 内核事件总线到用户态的通道（fd 式订阅）
- ✅ F061 管道调用 — pipe 创建与读写，进程间字节流
- ✅ F062 句柄复制/传递 — dup 与跨进程句柄传递（SCM_RIGHTS 式）
- ✅ F063 能力检查 — 每个敏感调用按能力位放行，拒绝带明确错误
- ✅ F064 调用审计 — 敏感调用环形审计日志（谁/何时/何参），崩溃后可回读
- ✅ F065 调用配额 — 每进程调用频率上限，防用户态打爆内核
- ✅ F066 fast path 优化 — 高频调用（时间/事件轮询）免锁快路
- ✅ F067 seccomp 式过滤 — 进程可自我限制可用调用集（沙箱 AI-07 复用）
- ✅ F068 vDSO — 时间等无副作用调用用户态直取，零陷入
- ✅ F069 ABI 版本查询 — 用户态运行时探测内核 ABI 能力位
- ✅ F070 向后兼容层 — ABI 演进时的旧号保留与转接策略
- ✅ F071 varix-std 雏形 — 无 libc 的 Rust 用户态基础库（print/时间/内存）
- ✅ F072 syscall fuzz — 非法指针/越界号/并发轰击 10⁶ 次零内核崩溃
- ✅ F073 调用仪表 — 每号调用计数/耗时环形统计，p50/p99 可查
- ✅ F074 调用自检 — 25 项 CheckSet：安全性、正确性、配额、审计
- ✅ F075 ABI 规范文档 — 调用面一页纸总表（号/签名/错误/能力要求）

## AI-04 ✅ 内核服务与 initfs 域（F076~F100，W2）

- ✅ F076 initramfs — 引导 ISO 内打包用户程序与资产的只读根文件系统
- ✅ F077 init 进程（pid 1）— 内核起来自动拉起，负责收养孤儿与拉服务
- ✅ F078 VFS 用户态暴露 — open/read/write/stat/close 全套 syscall 面
- ✅ F079 devfs — /dev 式设备节点（console/framebuffer/input）
- ✅ F080 服务管理器 — 声明式服务清单、依赖排序拉起、状态机（stopped/running/failed）
- ✅ F081 看门狗 — 服务心跳缺失即重启，重启风暴熔断
- ✅ F082 崩溃重启策略 — 每服务独立策略（重启/降级/停用），崩溃不扩散红线
- ✅ F083 事件总线用户态端 — 内核事件（设备/电源/进程）订阅与分发
- ✅ F084 管道与 FIFO — 匿名管道 + 命名 FIFO，行缓冲策略
- ✅ F085 共享内存 — 具名共享区创建/映射/权限，零拷贝通道地基
- ✅ F086 事件通道 — 共享内存 + 事件通知组合的高性能 ping-pong 通道
- ✅ F087 io_uring 式批量接口 — 提交/完成环，减少陷入次数
- ✅ F088 命名服务 — 服务名→端口解析（AURORA 内总线对接点）
- ✅ F089 配置读取服务 — 只读配置查询 syscall（主题/键位/性能档）
- ✅ F090 日志服务 — 用户态程序日志汇聚，分级过滤，环形缓冲可回读
- ✅ F091 持久存储桥接 — initfs（只读）→ 真实文件系统（可写）的挂载切换
- ✅ F092 持久化状态目录 — 用户数据目录约定（主题/剪贴板/应用状态）
- ✅ F093 电源事件面 — 关机/重启请求 syscall，优雅收尾全进程
- ✅ F094 时间同步面 — RTC 读取与系统时钟设定（受限能力）
- ✅ F095 随机数服务 — 内核熵池用户态安全取数
- ✅ F096 资源水位查询 — 全局内存/句柄/进程数水位仪表 syscall
- ✅ F097 服务依赖图 — 启动顺序 DAG 校验，环依赖即拒绝
- ✅ F098 服务域 fuzz — 恶意服务行为（不释放句柄/疯狂申请）下系统不倒
- ✅ F099 服务自检 — 25 项 CheckSet：initfs、init、VFS 面、看门狗闭环
- ✅ F100 三演示程序 — hello / 事件回显 / 崩溃演示，作为 W2 收口验收物

## AI-05 ✅ 图形显示服务域（F101~F125，W3）

- ✅ F101 帧缓冲服务 — 内核态帧缓冲独占管理，用户态经服务申请画布
- ✅ F102 双缓冲管线 — 后备缓冲 + 页翻转，杜绝撕裂
- ✅ F103 脏区跟踪 — 只重绘变化区域，静止画面零拷贝
- ✅ F104 VSync 同步 — 垂直同步等待，翻转时机精确
- ✅ F105 合成器内核服务 — 多画布分层合成，Z 序管理
- ✅ F106 分层合成优化 — 不透明层直通、透明层逐像素，性能与画质兼得
- ✅ F107 软合成保底 — 无 GPU 时纯 CPU 合成全路径可用（降级纪律）
- ✅ F108 GPU 探测 — PCI 扫描显示适配器、BOCHS/QEMU 显卡驱动优先
- ✅ F109 硬件加速接口预留 — 未来 GPU 加速的接口占位（不阻塞主线）
- ✅ F110 字体渲染管线 — TTF 解析、亚像素抗锯齿、字重族管理
- ✅ F111 字形缓存 — 光栅结果缓存，命中率高到渲染不进热点
- ✅ F112 令牌颜色管线 — AURORA 主题令牌逐字节复用，暗/亮主题即时切换
- ✅ F113 128px 图标管线 — 高分辨率图标解码与缩放质量达标
- ✅ F114 DPI 缩放 — 整数/分数倍缩放，DPI 变更全 UI 生效
- ✅ F115 分辨率热切换 — 运行中改分辨率，画布全量重建不崩
- ✅ F116 动画时钟源 — 合成器统一动画时钟，弹簧曲线采样稳定
- ✅ F117 帧预算仪表 — 每帧合成耗时环形统计，p99 < 8ms 红线
- ✅ F118 丢帧统计与降级 — 连续丢帧自动降低特效档位，恢复后回升
- ✅ F119 色彩管理 — sRGB 输出基准，色彩空间标记预留
- ✅ F120 截屏服务 — 全屏/窗口截取，供验收与作品集
- ✅ F121 录屏服务（可选）— 帧序列录制，作品集素材直接产出
- ✅ F122 多显示器预留 — 接口与数据结构占位，单屏优先交付
- ✅ F123 显示功耗钩子 — 空闲降帧、唤醒即时满帧
- ✅ F124 画质走查流程 — 与 AURORA Windows 版同屏像素对比的验收流程
- ✅ F125 显示域自检 — 25 项 CheckSet：双缓冲、脏区、字体、令牌、仪表

## AI-06 ✅ 输入与事件域（F126~F150，W3）

- ✅ F126 PS/2 键盘驱动 — 真实 8042 中断、扫描码集 2 解码
- ✅ F127 PS/2 鼠标驱动 — 数据包解析、按钮/滚轮
- ✅ F128 USB 输入预留 — xHCI 骨架占位，无硬件时明确降级 PS/2
- ✅ F129 内核事件队列 — 输入事件统一结构（时间戳/类型/数据）入队
- ✅ F130 事件时间戳 — 高精度单调钟打点，延迟测量地基
- ✅ F131 用户态事件通道 — 输入事件以 fd 订阅方式送达 UI 进程
- ✅ F132 键盘状态机 — 按下/重复/释放、修饰键状态、组合键判定
- ✅ F133 键位纪律对接 — AURORA 键位纪律组（AI05 既有规范）在内核层逐条落实
- ✅ F134 全局快捷键 — 系统级拦截层，应用不可独占
- ✅ F135 焦点系统 — 窗口焦点归属、点击/Tab 切换、焦点丢失事件
- ✅ F136 鼠标加速曲线 — 可配置曲线，默认与 AURORA Windows 版手感一致
- ✅ F137 滚轮平滑 — 行/像素模式、惯性参数令牌化
- ✅ F138 触控预留 — 多点触控事件结构占位
- ✅ F139 手柄预留 — HID 游戏手柄事件类型占位
- ✅ F140 输入延迟仪表 — 事件到上屏全链路打点，< 33ms 硬红线
- ✅ F141 输入无障碍 — 粘滞键、按键重复速率设置
- ✅ F142 输入安全 — 特权键（如 Ctrl+Alt+Del 等价）防应用注入
- ✅ F143 事件回放 — 事件序列录制与回放，回归测试利器
- ✅ F144 输入洪泛防护 — 单源事件速率限制，防止 UI 线程饿死
- ✅ F145 键盘布局 — 布局表机制，中文输入法接口预留
- ✅ F146 输入仪表盘 — 实时事件率/延迟可视化（开发者模式）
- ✅ F147 触控板手势预留 — 双指滚动语义占位
- ✅ F148 事件域 fuzz — 恶意事件序列（超频/畸形包）下内核与 UI 不倒
- ✅ F149 输入域自检 — 25 项 CheckSet：驱动、队列、焦点、纪律、仪表
- ✅ F150 手感验收流程 — 与 Windows 版 AURORA 盲测对比的手感验收流程

## AI-07 ✅ AURORA-1000 移植与兼容域（F151~F175，W3）

- ✅ F151 Tauri API 映射清单 — 每个 Tauri 调用 → Varix syscall/服务 的映射总表
- ✅ F152 映射层实现 — 兼容层 crate：Tauri 语义薄壳，底层 Varix 实现
- ✅ F153 显式降级表 — 无对应实现的调用显式返回降级，禁止静默缺失
- ✅ F154 渲染后端抽象 — AURORA 渲染层抽出后端 trait，Windows/Varix 双实现
- ✅ F155 Varix 渲染后端 — 组件树光栅化到帧缓冲服务画布
- ✅ F156 像素级走查 — 同组件双后端渲染对比，差异清单清零
- ✅ F157 窗口系统语义平移 — AURORA W2 窗口核心（层级/Z序/拖拽）语义保持
- ✅ F158 任务栏/开始菜单进程拆分 — 桌面组件各自进程化
- ✅ F159 应用生命周期 — 启动/挂起/恢复/退出与窗口状态同步
- ✅ F160 资产管线 — 图标/主题/字体/文案进 initfs，路径约定统一
- ✅ F161 状态持久化 — 剪贴板历史/布局/主题落盘，重启恢复
- ✅ F162 剪贴板服务 — 跨进程复制粘贴，多格式（文本/富文本预留）
- ✅ F163 拖放协议 — 窗口间拖放事件通道
- ✅ F164 通知中心对接 — 应用通知 → 桌面通知面板
- ✅ F165 AURORA 内总线对接 — 组件间消息 → Varix 端口/命名服务
- ✅ F166 主题令牌运行时 — 令牌加载/热切换，与设计令牌开放规范（Z50）对齐
- ✅ F167 动效系统复用 — 弹簧曲线参数逐字节一致，动画不掉帧
- ✅ F168 应用进程沙箱 — 每应用独立进程 + 最小能力 + 调用过滤
- ✅ F169 崩溃不扩散验收 — 强杀/注入崩溃，桌面/任务栏/其他应用零影响
- ✅ F170 应用启动器 — 开始菜单点击 → spawn 应用进程，启动耗时仪表
- ✅ F171 设置应用对接 — 系统设置读写 → 配置服务 + 权限面
- ✅ F172 文件管理器对接 — 基于 VFS 用户态面的基础文件浏览
- ✅ F173 兼容性测试矩阵 — Top200 应用行为清单在 Varix 上逐项过
- ✅ F174 双后端 CI — Windows 后端与 Varix 后端同仓双线构建
- ✅ F175 移植域自检 — 25 项 CheckSet：映射完整性、渲染一致、崩溃红线

## AI-08 ✅ 引导链与交付域（F176~F200，W5）

- ✅ F176 Limine 默认项 — Variable 设为默认菜单项，倒计时可选
- ✅ F177 引导品质线 — 开机动画/横幅艺术化升级（承接 F013），桌面接管前无缝
- ✅ F178 引导时间线 — 上电→内核→init→桌面各阶段耗时真实可视化
- ✅ F179 快速启动优化 — 内核/资产加载并行化，上电→桌面 ≤ 8s（QEMU）
- ✅ F180 initrd 打包流水线 — 用户程序/资产自动打包进 ISO 的一键脚本
- ✅ F181 ISO 产物 — BIOS/UEFI 双引导 ISO（xorriso），一条命令产出
- ✅ F182 USB 产物 — DD 可写 U 盘镜像，Ventoy/Rufus 兼容
- ✅ F183 三系统并存引导 — 与 Windows 共存互不破坏，切换验证 20 次
- ✅ F184 A/B 分区布局 — 双系统分区、当前/备用版本指针
- ✅ F185 A/B 更新器 — 新版本写入备用区、验证后切换指针
- ✅ F186 更新回滚 — 更新失败自动回上一版，菜单保留旧版入口
- ✅ F187 引导链完整性 — 镜像哈希校验扩展到用户态镜像与资产
- ✅ F188 镜像签名 — 交付链签名验证，防篡改
- ✅ F189 安全启动预留 — Secure Boot 路线占位（不阻塞主线）
- ✅ F190 崩溃恢复模式 — 桌面起不来时的最小诊断 shell（串口/文字模式）
- ✅ F191 安全模式 — 禁用非核心服务/降级渲染的启动档
- ✅ F192 最小镜像 — < 64MB 引导至桌面空壳，作品集演示用
- ✅ F193 完整镜像 — 全功能版本，含全部应用与资产
- ✅ F194 首次运行向导 — 桌面首次启动的引导体验（主题/键位选择）
- ✅ F195 配置迁移工具 — 从 Windows 版 AURORA 导出配置导入 Varix 版
- ✅ F196 CI boot job — CI 增加内核+镜像构建产物与冒烟引导测试
- ✅ F197 真机验收清单 — 真机从引导到桌面的完整验收步骤与仪表
- ✅ F198 版本发布纪律 — 版本号、变更清单、回滚点管理规范
- ✅ F199 作品集素材 — 引导→桌面全流程录屏、仪表面板截图、架构图
- ✅ F200 交付域自检 — 25 项 CheckSet：双产物、并存、A/B、恢复、发布

---

> 更新纪律：完成一项 → 状态 ⬜→✅ → 更新《AI分工完成图》 → commit（`var200(ai-XX): Fxxx <功能名>`） → 推送 GitHub。
> 立项前查重：VARIX-500 全景图 500 项 + 本图 200 项，零重复。
