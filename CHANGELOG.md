# Changelog

本文件记录面向用户与协作者的显著变更。批次级细节见 `project_memory.md`；
架构与计划见 `docs/BLUEPRINT-1.0sno9u.vxe.md` 与 `docs/MASTER-PLAN-1.0sno9u.vxe.md`。

## [Unreleased] — Varix STAR I · AI-U4 I 通用域·四分队（F551-F600 深化批次三 · 深化层全量收口+隔离验证+检查项对账）

**kernel/varix/src/istar/**（AI-U4，2026-09-26 深夜；批次三 28 项补深，深化层 50/50 全量在位）：

- **28 个深化文件**（deep/f553d~f600d）：F553 拖影徽标几何引擎+刷新节拍器、F559 扫场节奏账+灰阶生成器+网格定位换算、F561 分级校验状态机+参数转义引擎、F564 表盘参数引擎+微步进时序账、F566 三处一致性引擎+过滤状态机+图标仲裁、F568 两层命中判定+三问队列状态机、F570 Peek 生命周期账+热区几何+三路同效对账、F573 翻页联动+降档建议器、F574 条数约束+人话转译分级+展示时机状态机、F575 锚点轮转抽查+三处对账器、F577 分桶过滤器+键盘导航、F578 .lnk 解链+断链诊断、F580 中段三分几何+可打断飞掠、F581 全选生命周期状态机、F582 处理态状态机+豁免清单、F583 焦点路径栈+多区块锚、F584 分段保头截断+混排宽度计量、F585 滞回门+浮层豁免、F587 胶囊 round-trip+LRU 淘汰、F588 命名引擎+PNG 两段提交、F589 落点分流+越界审计、F591 循环覆盖账+防爆音三步、F592 形态保真映射+三模式快照、F593 逐帧进度账+Esc 零残留、F594 涟漪时间线+隔离审计、F595 区域账本+组合状态机、F597 映射引擎+横移动画账、F600 四段基线账+摘要对账+冻结状态机；
- **规模**：纯功能 11,256 → **16,242 行**（对上限口径 32.2%）；CheckSet 检查项 387 → **≈748**（基础 379 + 深化 360 + 聚合 9）；域内单测 155 → **277 全绿**；深化覆盖 22/50 → **50/50**；
- **隔离验证**：worktree（HEAD=4bfd25a6+批次三）本域 277/277 全绿；全量 7,419 测试 7,327 ok / 84 FAILED——红项全部他队领地（82 h1star 未提交在制品+2 stareco），istar 零红零新增警告；施工期闭环缺陷 #14/#15（检查期望错）+#16（他队 HEAD 破损阻塞台账，worktree 最小修复，主树未动）；
- 对账：docs/AI-U4-检查项对账表.md（逐项行数+CheckSet+验证记录）+ _attic/aiu4-f551-f600/ 账本 §五。

## [Unreleased] — Varix STAR I · AI-U3 I 通用域·三分队（F501-F550 深化批次五 · 装配层：桌面实绘+资源管理器+锁屏横幅挂接）

- 三装配引擎（src/features/u3/engines/）：despaint 桌面实绘（橡皮筋几何/键盘网格导航+type-ahead/拖拽幽灵与网格吸附/瞥桌面/布局锁定联动/双击防抖）、expui 资源管理器装配（万节点树平铺 16ms 预算/视口窗口器越界夹取/F528 双向同步计划/键盘树导航/面包屑收缩）、lockmount 锁屏横幅挂接（锁屏窗口状态机五次冷却翻倍+密码回退+焦点归还/F508 黑块合成裁剪/F516 真窗口几何+任务栏避让/U3 Z 序总表五层）；
- 两个活体 UI 装配件挂入 U3Tab：DeskPaint（桌面图标面全输入路径可操作）、ExplorerPane（树窗格+列表+面包屑+24px 状态栏三段）+ U3Lab 锁屏挂接实况区 + labels 16 引擎双语；
- F550 对账增强：anchor 域表 12 域（v5-engines 纳入聚合排除）、labels 断言同步；
- 验证：U3 单测 208/208 全绿（v5 新增 30）、tsc 本域 0 错、worktree 隔离验证（HEAD=268b8d17+仅本批 16 文件）208/208+tsc 0 错、缺陷账 D-37~D-41 全闭合；
- 行数对账：累计 25,619/46,540 = 55.0%（tally 现算口径，_attic/aiu3-f501-f550/）。

## [Unreleased] — Varix STAR I · AI-V1 服务守护域·前段（F111-F130 深化批次八 · 隔离验证/检查项对账）

**kernel/varix/src/svstar/**（AI-V1，2026-09-26；批次八，零返工零缺陷）：

- **规模**：纯功能 11,307 → **11,471 行（50.1%，突破半程线）**；自检
  372 → **377 条 × 21 块全绿**；隔离舱 cargo test 316 → **324 passed /
  0 failed**。
- **F118**：行动钮文案对位/进度点三态（矛盾入参拒绝）/完成态收尾卡
  「开始使用」（仅末卡可进，置完成标记即不二弹出口）。
- **F120**：修复执行前简报（做什么/风险/回滚三段与注册件同源，缺段
  不出确认页）/导出 zip 装配清单（manifest 恒末位+总量守恒+诚实空包）。

## [Unreleased] — Varix STAR I · AI-V1 服务守护域·前段（F111-F130 深化批次七 · 隔离验证/检查项对账）

**kernel/varix/src/svstar/**（AI-V1，2026-09-26；批次七）：

- **规模**：纯功能 11,005 → **11,307 行（49.4%）**；自检 366 →
  **372 条 × 21 块全绿**；隔离舱 cargo test 304 → **316 passed /
  0 failed**。
- **F125**：宪章条款登记册（16 章 46 条款，五查自洽门与 20 维度代表
  锚双侧零漂移对账——施工中实证抓到 D 域锚误挂）；走查脚本工具化
  产出面（vx-walkcheck-c.py 全文生成，MD/JSON 双形态）。
- **F112/F127/F126**：朗读高亮双视觉（撞色拒绝）；keygen 保管指引
  随指纹输出；规范页打印友好（外链转脚注）。

## [Unreleased] — Varix STAR I · AI-V1 服务守护域·前段（F111-F130 深化批次六 · 隔离验证/检查项对账）

**kernel/varix/src/svstar/**（AI-V1 泳道四前段，2026-09-26；Variable
指令「继续大量深化 + 隔离验证 + 检查项对账」批次六）：

- **规模**：纯功能 10,224 → **11,005 行（48.1%，主册上限 22,880 口径）**；
  wc 全文件 16,888 行；CheckSet 自检 346 → **366 条 × 21 块全绿**；
  隔离舱 cargo test 266 → **304 passed / 0 failed**（含双挂载与域聚合）。
- **F125**：归档路径生成器（路径逃逸五路拒绝）/并行会话收口合并（
  Green>Red>Pending 确定裁决）/主册卷判据段自动提取器（三列映射自动
  半轨真实现）/弱维度 Top-N 改进清单/R8 指纹登记导出-抽回 round-trip。
- **F126**：宪法页合集（双读+semver 条款写进正文）/$schema 元键/锚点
  唯一性双对拍/破格公告稿生成器/apply_update 变更历史门禁落地版
  （破格强制 ADR；版本标签-元组双写一致门）。
- **F128**：`?since=` 解析器/导入侧轻量反解重建（导出→导入→查询 50 卡
  round-trip 闭环+逐卡边界校验）/按来源分片/快照流水线 compose/镜像
  对账 diff（停滞含倒退/骤缩/缺签三信号）。
- **F119/F129/F130**：版本绑定横幅+打印友好样式；质量分三因子+快审
  60 分线；关于本机尾部入口机器面。
- 施工缺陷 5 条全记录（最重：extract_json_num 末字段终止符误判缺字段
  ——探针取证定位修复），见 `_attic/aiv1-f111-f130/` 账本 v6 §4。

## [Unreleased] — Varix STAR I · AI-U3 I 通用域·三分队（F501-F550 深化批次四 · 引擎群+实验室+隔离验证+检查项对账）

- 十三新引擎（src/features/u3/engines/）：lumapick 壁纸亮度矩阵采样（5×5 截尾均值+滞后带防抖+4K DPR 精度）、gridlab 网格规划器（换档计划+Spearman 保持度+F084/F401 仲裁）、pinvault（PIN 冷却翻倍表状态机+蓝牙动态锁 RSSI 时间线）、guestbox 访客沙盒四轴记账、capguard 三路截图统一裁决+Z 序黑块合成、shredplan（粉碎计划/SSD 诚实分支/截图历史淘汰）、vxcrypt2 容器头序列化+临时视图无痕账本、findrepl（替换会话原子回滚+状态栏三处同源）、bannerpack（横幅堆叠布局+三义分流矩阵）、hotkeymap（Caps 600ms 分界+双格式同源速查卡+冲突审计）、sysdiag（诊断三要素+电量三处同源+接入三态）、explog 十三章体验日志（挫败指纹/脱敏闸门/环形缓冲）、walkcheck 十二查对账引擎；
- 实验室三区挂入 U3Tab（U3Lab.tsx 引擎群总自检+十二查对账实况+体验日志实况）+ labels 双语词典（B-1104 零缺键）；
- F550 对账增强：anchor 域表 11 域、聚合域排除编号分派、F550 显式声明；walkcheck F501-F550 覆盖不重不漏钉死；
- 验证：U3 单测 178/178 全绿（v4 新增 82）、tsc 本域 0 错、worktree 隔离验证 178/178+tsc 0 错、缺陷账 D-31~D-36 全闭合；
- 行数对账：累计 24,349/46,540 = 52.3%（tally 现算口径，_attic/aiu3-f501-f550/）。

## [Unreleased] — Varix STAR I · AI-V2 生态开放域·后段（F131-F150 深化批次五 · 装配与跨域接口件 + 域台账机检）

**kernel/varix/src/stareco/deep/**（AI-V2 泳道四后段，2026-09-27）：

- **规模**：27,092 → **30,965 行**（批次五 h 层 20 文件 + f150i/f150j
  收尾件 +3,873，聚合器扩容 80→102 blocks）；CheckSet 检查项 1,479 →
  **1,644**（+165）；达成率 78.1% → **89.2%**（90% 线差 274 行，f150j
  机检如实登记）；std 隔离舱 195 → **232/232 全绿零警告**。
- **装配与跨域接口件（20 项 × 18 个消费域）**：贡献者页装配+判例线接
  口（F129）；星卡馈送（F036）+迁移横幅三要素；桌面图标源缺图显性回退
  （F084）；令牌导入适配整体拒绝（F151）；帮助锚点映射死锚检出（F119）；
  欢迎五卡+先修锁启动器（F118）；版本史行+三态徽标；关于页行+更新预约
  （F122/F123）；诊断体检灯四灯+深链（F120）；热切语言清单+两级降级
  （F398）；无障碍设置行+联动引擎指名+健康分；安全面板 embargo 拦截+
  安全模式提示（F193）；关于资产行 4K 标注+申请页拒绝理由上页面；徽标
  角标三处一致+争议深链（F148）；学习路径唯一推荐+待译角标；数据源选
  择行+插件管理页；同步页行+冲突入口分派+关闭诚实行；移交门无证据不立
  案+公示脱敏+指标卡；订阅行缺季「数据待修」+区块门；域总表三色+回归
  看板+挂接总检。
- **收尾件**：f150i 域快照（20 项 wc 实数硬登记）+90% 闸门+跨域接口总
  账+闸门审计链（append-only）；f150j 批次台账机检（六批行数/检查项/缺
  陷数实数编译进内核——账本被改坏时直接红）+缺口工单生成器。
- **缺陷 8 组即时修**（账本 I1-I8）：真缺陷复核 1 处（LinkGraph 死链
  自锚消费链确认）+ 编译错 6 处 + in-check 口径 7 处——全部零遗留。
- **推送恢复**：批次三/四/五全部提交已推 origin/main。报告随共享树迁
  移 docs/START/（E1 迁移）。明细见 `docs/START/AI-V2-完成报告.md` 与
  `_attic/aiv2-f131-f150/行数对账与缺陷账本.md` 〇·五节。

## [Unreleased] — Varix STAR I · AI-V2 生态开放域·后段（F131-F150 深化批次四 · 内容件与后端自建件 + 跨会话对账审计）

**kernel/varix/src/stareco/deep/**（AI-V2 泳道四后段，2026-09-27）：

- **规模**：22,035 → **27,092 行**（批次四 g 层 20 文件 +5,018，聚合器
  扩容 60→80 blocks）；CheckSet 检查项 1,236 → **1,478**（+241，并经跨
  会话对账审计修正基础层计数 306→307）；达成率 63.5% → **78.1%**；
  std 隔离舱 154 → **195/195 全绿零警告**。
- **两大主件**：F136 五示例源码本体（hello/clipboard/theme-tokens/
  mini-editor/notepad-lite 五份教学源码 + 行数/密度/先修链/README 四类
  门禁复用 f136f 机器面——内容件落地）；F139 tracker 后端自建件（月分
  区存储 512 护栏 + 查询词法解析 + 批量迁移审计 + JSONL/CSV 同源导出 +
  周报聚合）。
- **其余 18 项深化**：贡献者分级与上游健康雷达；迁移映射表与分批计划
  器；安装器状态机与哈希分桶；兼容矩阵发布门与作者认证；链接图死链/
  孤页检测与倒排搜索；迁移工作台与冻结表；发布说明渲染与紧急窗审计；
  翻译记忆（有界 Levenshtein）与字符集缺字检出；导航图/读屏标注/动效
  替代通路/仅色传意/44px 触达五类无障碍审计；影响版本区间与修复优先
  级矩阵；打包清单与审批流；徽标批量审计与争端流程；发布流水线与引用
  完整性；插件加载检疫与版本协商与降级决策树；块 MAC 通道与星型拓扑；
  案例时限与选举核；多季矩阵与归档链；评分卡与任务库 churn 线。
- **缺陷 13 组即时修**（账本 H1-H13）：真缺陷 2 处（LinkGraph 死链判
  定自锚永远检不出 / orphan 语义漏判入口页）+ 编译错 8 处 + in-check
  口径 9 处——全部零遗留。
- **跨会话对账审计**：发现并修正账本基础层计数偏差（306→307）；实录
  隔离舱 lib.rs 并发回滚事故一次（改幂等接线+断言自证）。
- **随闸门补测**：committed 树复证再次被他人分队在途状态挡住（perfstar
  裸 vec!，与批次一教训同型，非 V2 面）。明细见 `docs/AI-V2-完成报告.md`
  与 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md` 〇·四节。

## [Unreleased] — Varix STAR I · AI-V2 生态开放域·后段（F131-F150 深化批次二补登 + 批次三 · 隔离验证/检查项对账）

**kernel/varix/src/stareco/deep/**（AI-V2 泳道四后段，2026-09-26；Variable
指令「继续大量深化 + 隔离验证 + 检查项对账」。批次二段在多会话并发中再
次遗失，本条与批次三合并补登——条目存在性复核已入收口清单）：

- **规模**：10,642 → **22,035 行**（批次二 e 层 +6,615、批次三 f 层
  +4,736，聚合器扩容至 164 行）；CheckSet 检查项 482 → **1,236**
  （批次二 +418、批次三 +336）；达成率 30.7% → **63.5%**（主册上限
  34,710 行口径）；std 隔离舱 54 → **154/154 全绿零警告**。
- **批次二 e 层（持久化与错误边界）**：DCO 签名链 + CLA 三态 + 回馈日志
  链 + 补丁维护册；族分页 + 判例管线 + 序列化 round-trip；四族清单表 +
  导入向导状态机；SLA 审计钟 + 版本兼容闸；API 提取器 + 多词 AND 搜索；
  源码构成审计 + 断绿归因四类；指纹漂移检测 + 三渠道通知；排程台账链 +
  iCal VALARM；tracker 存储模型 + 聚类桶；域注册表 + RTL 前瞻；错例集 +
  自测报告；四步时限钟 + PGP 环；条款裁决引擎；四查流水线 + 申诉终级；
  脱敏 diff 引擎 + 注入测试；源配置持久化 + 缓存 LRU；决策树 + 冲突协商；
  文档版本链 + 轮值回避；环比引擎 + JSON 指纹；分段计时 + 基线断点册。
- **批次三 f 层（工程化工具面）**：PR 拆分规划器 + 评审轮次模型 + stale
  检测；差异表查询引擎 + 消费端兼容裁决（Clean/Warn/Block）+ 重复 Open
  矛盾检测；图标包 diff + 重采样三分类 + 覆盖矩阵（按图标名分组 0xFF 哨
  兵）；标签倒排索引 + 审核队列（到期沉底）+ 评分半值向下；签名解析器
  （括号配对扫描）+ TOC 编号树 + 检索权重排序；依赖图三色 DFS 环检测 +
  Kahn 拓扑 + CI 连绿账；调用面扫描 + 破坏分级 + 双读进度线性外推；窗重
  叠检测 + iCal 75 八位组折行（UTF-8 边界回退）+ 季度间距校验；分带相似
  指纹聚类 + SLA 三段时限钟；词条 diff + 复数形数核 + 伪本地化；WCAG f64
  复证 + 安全前景色推荐 + 焦点顺序验证；CVSS 三轴评分 + 披露时间线状态
  机 + 致谢页去重；资产命名校验 + 明暗变体派生 + 4K 硬线 + 场景白名单；
  徽标使用双门 + 伪造指纹册 + 撤销链；脱敏字节扫描偏移定位 + 可读性审
  计；源轮转调度 + TTL 缓存 + 带宽两级降级；三方合并 + 版本向量四态 +
  分块指纹链首坏块定位；裁决员负载分派 + 公示冻结语义；中位数稳健异常
  带 + 归因强制指名；四步分段计时 + 回退 110% 红名单。
- **施工缺陷全即时修**：批次二 9 处零遗留（账本 E1-E9）；批次三 10 处
  （账本 F1-F10，含 4 处真算法缺陷：拓扑入度反向 / rfind 括号误切 /
  CVSS 归一差十倍 / 均值带尖峰自拉偏——均测试网拦截后按真实语义修）。
- **验证与对账**：deep 聚合 blocks 40→60 全绿；隔离舱新增批次三 20 块
  聚合断言 + 20 单测 + 红项逐行诊断口；committed 树复证批次一/二时点
  ktest 3,963/0 绿，批次三后被 C1 在途 `compatstar/clipfmt.rs` 语法错
  挡住（非 V2 面），登记随闸门补测；组织类判据（F131 真实 PR / F150
  外部志愿者）与 kernel-image `powf` 观察项如实登记不粉饰。明细见
  `docs/AI-V2-完成报告.md` 与 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。

## [Unreleased] — Varix STAR I · AI-C2 应用兼容域·后段（F021-F040 深化批次四 · 序列化/账本/容错面 + 隔离验证/检查项对账）

**kernel/varix/src/compatstar2/deep3/ 二十域批次四**（AI-C2 泳道二后段，
2026-09-27；Variable 指令「继续大量深化直至全量 + 每轮隔离验证与检查项
对账 + 未完成自动续作」）：

- **规模**：19,809 → **26,329 行**（+6,520，deep3/ 20 文件 6,410 行 +
  批次聚合器 83 行）；CheckSet 判据 655 → **861**（批次四 +206）；单测
  258 → **321**（+63）；达成率 27.1% → **36.0%**（主册上限 73,125 行口径）。
- **F021-F030**：内存诊断转储（区域快照 round-trip/堆走查三账/守护页陷阱
  环/七桶尺寸分级账）；时区规则表驱动（tzdata 真实样例/年度转换点/春跳
  秋拨两账）；错误注入矩阵（4 操作 × 4 WSA 错误码 16 格判定/重试策略表/
  忙退避封顶）；证书链构建引擎（发行者查表/时间窗校验/缺根过深过期三
  分类/信任判定矩阵）；假脱机与配额（作业头 round-trip/优先级队列/每
  用户配额账/页数估算器）；混音总线（4 路定点饱和求和/PPM 弹道/帧数
  对账/solo 压倒 mute 16 格表）；IME 词典与学习（FNV 哈希 64 槽/评分稳定
  定序/32 条 LRU/自动上屏阈值）；DPI 资源选型（四桶就近舍入/键缓存三账/
  跨桶触发/逐桶预算 LRU）；拓扑变更检测（四类事件/指纹查表/三段回退）；
  安装脚本求值（四指令求值器/变量替换/条件跳转防环/执行日志账）。
- **F031-F040**：卸载顺序依赖（逆拓扑定序/SharedDLLs 引用计数/占用延迟
  清单/重启挂起账）；运行时环境账（10 格版本槽矩阵/venv 影子优先/字节
  LRU）；构建缓存（内容寻址/命中率‰/指纹失效/冷热逐出）；流式转码状态
  （GBK 跨块 lead 续接/round-trip 24 码点零不等/有损无损双模/块边界两
  账）；修复动作执行（四动作四检位图/幂等账/8 步逆序回滚/重试归档）；
  星卡存储格式（schema v1→v2 迁移/记录级校验和定位损坏/4KB 预算账/帧
  round-trip）；策略规则编译（冲突账高优先级胜出/特异性定序/未匹配默认
  记账）；进程树隔离（深度上限/越权检出/后序杀树/孤儿挂根）；帧账归因
  （64 帧环/三级抖动分类/四桶归因/恢复时间账）；账本开放导出（canonical
  两次序列化逐字节一致/round-trip/白名单剔除/校验尾标）。
- **隔离验证与检查项对账**：隔离舱 `cargo test` **321/321 绿、零警告**；
  tally 五向对账 main 237 + deep1 110 + deep2 101 + deep3 207 + deep4 206
  = **861 项 · 0 红项**（tally.rs 升级五向版）。聚合器沿用逐域求值形态
  （缺陷 #26 的修复直接吃下第五层，零新增栈风险）。
- **接力机制**：批次五接力台账落 `_attic/aic2-f021-f040/接力台账.md`；
  未达成全量前由自动化续作下一轮（Variable 明令「没有完成到下一次自动
  进入下一次对话……直到全部完成才算结束」）。
- **缺陷账本**：批次四施工分位自纠 11 项（桶边界语义/跨区规则冲突/深链
  样例主题名重复/中部通配裁决/归因分母/TLV 头长等）均在各自隔离舱内
  修复后才交付，未进主树；主树零新增错误。

## [Unreleased] — Varix STAR I · AI-C2 应用兼容域·后段（F021-F040 深化批次三 · 执行/边界/注入面 + 隔离验证/检查项对账）

**kernel/varix/src/compatstar2/deep2/ 二十域批次三**（AI-C2 泳道二后段，
2026-09-26；Variable 指令「继续大量深化 + 隔离验证 + 检查项对账」）：

- **规模**：13,098 → **19,809 行**（+6,711，其中批次三 deep2/ 20 文件
  6,547 行 + 批次聚合器 83 行）；CheckSet 判据 448 → **655**（批次三
  +207）；单测 193 → **258**（+65）；达成率 17.9% → **27.1%**（主册上限
  73,125 行口径）。
- **F021-F030 执行/边界面**：VirtualAlloc 四态转换矩阵 + PAGE_* 三维判定
  + 64KB 区域扫描器；FILETIME↔SYSTEMTIME civil 历法双向矩阵（闰年边界）
  + Stopwatch 三态 + tick32 回绕；WSABUF 8 段 scatter/gather + select 三
  就绪集位图 + inet_pton/ntop v4/v6；EKU OID 真实值表 + SAN 多名归一 +
  密钥用法六位验证链；ISO 纸张 0.1mm 真实表 + DEVMODE 协商 + 页范围解析
  器；DS 环形缓冲双游标 + DSBPOSITIONNOTIFY 去重 + Q8.8 音量衰减 + lost
  四态机；CFS_* 状态机 + 候选窗边界避让 + WM_IME_* 序链 + ATTR 属性段；
  MDT_* 判定表 + Q16.16 定点缩放 + 传播时序账；模式降序定序 + 24-240Hz
  白名单 + DM_* 掩码变更检测 + 虚拟桌面几何；TLV round-trip + 回滚栈 +
  FNV 校验对拍 + 升级五态机 + 磁盘预算预检。
- **F031-F040 治理/注入面**：残留扫描通配匹配器 + 五类键归属 + 三证据
  置信度评分；pep440 版本序 + 8 镜像回退链 + 锁定账三分类；Kahn 拓扑 +
  mtime 脏传播 + ready 队列 + 命令指纹；GBK/Big5 双轨 lead-byte 状态机 +
  稠密段查表 + BOM 优先序；API 指纹归因表 + facility 聚类 + 12 行决策表；
  2 的幂直方图 + P50/P95/P99 插值 + 采样三账 + 滚动去重；FNV-1a 审计链
  重放 + 改/删/插/重放四攻击检出 + pin 集双因子；六步拦截流水线 + 令牌
  桶三档 + 降档观察窗 + 3×4 审计聚合；DXGI 模式定序 + 独占五态机 + 四步
  交接计时账 + 回滚帧率账；三源表决 + 季度 diff 三分类 + 12 周甘特插值 +
  50 件四态迁移合法性表。
- **隔离验证与检查项对账**：临时 crate 宿主 `cargo test` **258/258 绿、
  零警告**；tally 四向对账 main 237 + deep1 110 + deep2 101 + deep3 207
  = **655 项 · 0 红项**。隔离舱捕获并修复批次三缺陷 #26：域聚合器四层
  展开后 100 个检查调用同帧执行溢出测试线程栈（STATUS_STACK_OVERFLOW）
  ——重构为 fn 指针表 + 逐域求值，栈峰值回到单域水平（修复本身即更正确
  的工程形态，非绕过）。
- **边界纪律**：全树在途唯一编译错位于 `compatstar/clipfmt.rs:685`
  （AI-C1 在途合并伤，括号失配）——按「不碰他人在途文件」纪律登记不代
  改，compatstar2 零归属错。批次三证据归档 `_attic/aic2-f021-f040/`。
- **缺陷账本**：批次三新增 #26 一项（🔴级，见上）闭环；四个施工分位各自
  的编译期自纠（i64/u32 混算、三账闭合公式恒假、桶区间标定错位等）均在
  隔离舱内修复后才交付，未进入主树。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队（F401-F450 v6 深化批次四 · 键位与语义核）

**kernel/varix/src/uni1/ 十七模块 v6 纵深**（AI-U1 泳道六，
2026-09-26；Variable 指令「继续大量深化 + 隔离验证与检查项对账」）：

- **规模**：域累计 12,397 → **13,380 行**（+983 纯功能）；CheckSet
  检查项 692 → **804**（+112）；文件级单测 148 → **161**（+13）；域
  达成率 23.7% → **25.6%**（禁注水纪律下如实累进）。
- **深化对象**：F402/F406/F407/F410/F411/F412/F415/F418/F419/F425/
  F426/F427/F428/F432/F433/F434/F435——单选组隔离修正、空菜单全链
  安全守卫、确认越界诚实回收、剪贴板历史 ring、F087 逐项裁决粘贴、
  N 上打印算式、另存重名链、关窗守卫、历史容量上限、满度档位、
  打字门等。
- **对账口径升级**：ledger.ts 51 行全部程序化机数回填（v6 起弃手改），
  修正一处 v5 历史账差；u1.spec 断言 804/161/51/50。
- **验证**：隔离舱 **170/170 绿**（lib 0 警告）+ tally **51 块 804 项
  零红零截断**；前端 u1.spec **54/54 绿** + tsc u1 面 0 错误。
- **缺陷账**：#26-#31 共 6 条当日全闭合（3🟡3🟢，含 1 崩溃级空菜单
  panic 由自检红项当场暴露当场修）；明细见 `_attic/aiu1-f401-f450/`
  两份账本 v6 轮。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队（F401-F450 v5 深化批次三 · 交互语义核）

**kernel/varix/src/uni1/ 十四模块 v5 纵深**（AI-U1 泳道五，2026-09-26；
Variable 指令「继续大量深化 + 隔离验证与检查项对账」）：

- **规模**：域累计 11,217 → **12,397 行**（+1,180 纯功能）；CheckSet
  检查项 583 → **692**（+109）；文件级单测 125 → **148**（+23）；域
  达成率 21.4% → **23.7%**（禁注水纪律下如实累进）。
- **深化对象**：v4 后最薄的 14 模块（F409/F413/F414/F420/F421/F422/
  F423/F429/F430/F431/F436/F447/F448/F449）——交互语义核：选区会话
  状态机、守恒账原子拒绝、同步失败显性化、菜单生命周期、序热更新、
  静音位、低电量一次性提醒、多显示器全屏、捏合锚点数学、命名校验、
  连发饱和、试听打断、峰值保持、模式热切换、暂停态、帧率实测等。
- **验证**：隔离舱 **157/157 绿**（lib 0 警告）+ tally **51 块 692 项
  零红零截断**；前端 u1.spec **54/54 绿** + tsc u1 面 0 错误；镜像账
  ledger.ts 机数回填同提交同步。
- **缺陷账**：#23-#25 共 3 条当日全闭合（2🟡1🟢，无🔴）；明细见
  `_attic/aiu1-f401-f450/` 两份账本 v5 轮。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队（F401-F450 v4 深化批次二 · 内核纵深）

**kernel/varix/src/uni1/ F437-F446 十模块 v4 纵深**（AI-U1 泳道四，
2026-09-26；Variable 指令「继续大量深化 + 隔离验证与检查项对账」）：

- **规模**：域累计 10,077 → **11,217 行**（+1,140 纯功能）；CheckSet
  检查项 492 → **583**（+91）；宿主单测 114 → **125**（+11）；域达成率
  19.3% → **21.4%**（主册上限口径，禁注水纪律下如实累进）。
- **新功能子系统（34 个，全部机检锚定）**：F441 四型下次触发真实日历
  算术（大小月/闰年）+ 错失策略 Skip/RunLate + 失败重试退避封顶 +
  执行统计；F438 盘符域验证 + 自动分配 + 挂载点冲突拦截 + 断链扫描 +
  事件分类检索；F439 爆破锁定冷却 + 会话自动上锁 + 恢复密钥分组码 +
  加密暂停/恢复 + 退加密全清；F440 ISO9660 PVD 卷标解析 + 目录记录
  遍历 + 重复源拒绝 + 盘符空闲表；F437 簇大小推荐 + 卷标合法性 +
  写后校验两阶段 + ETA 实测速率账 + 中止清理；F442 配额账 + 单在途
  不变量 + 半程中止 + 还原执行语义；F443 连接/断开 + 确认超时回落 +
  忘记设备 + 信号排序；F444 默认打印机互斥 + 移除出队 + 进度分段；
  F445 重叠检测 + 相邻关系 + 锚点保持分辨率变更 + 批量窗口回流；
  F446 多屏管理器 + 仅刷新率通道 + 自定义分辨率诚实门。
- **验证**：隔离舱 **134/134 绿**（lib 0 警告）+ tally **51 块 583 项
  零红零截断**；前端 u1.spec **54/54 绿** + tsc u1 面 0 错误；镜像账
  ledger.ts 机数回填同提交同步。
- **缺陷账**：#17-#22 共 6 条当日全闭合（含 1🔴恒真检查自检有效性
  缺陷），零遗留零静默；明细见 `_attic/aiu1-f401-f450/` 两份账本 v4 轮。

## [Unreleased] — Varix STAR I · AI-K2 内核底盘·后段（F058-F075 深化批次四 · v4 深化 + 隔离验证/检查项对账）

**kernel/varix/src/perfstar2/ 18 域 v4 深化**（AI-K2 泳道一，2026-09-26）：

- **规模**：21,356 → **25,941 行**（+4,585）；CheckSet 判据 752 → **968**
  （deep4 +216）；单测 225 → **269**（+44）；达成率 29.6% → **36.0%**。
- **主题：状态持久化 + 跨域联动 + 自诊断**。亮点：池完整性走查（位翻转
  检出）、确定性 PRNG 迁移模拟器、整数 TCP 拥塞窗、日能耗报告编解码、
  zigzag-varint 差分编解码、采样看门狗、等功率声像律（镜像对称）、采样率
  探测、缓冲自调、睡眠债账、恢复演练驱动器（断电演练自动化）、迭代三色
  DFS 环检测、四源优先级冲突消解、迷你趋势图渲染、图钉回收站、免打扰窗。
- **架构**：聚合器改行级 AND（968 条判据超单 CheckSet 容量）；隔离舱切
  冻结接口 fake（K1 mech_* WIP 阻塞真树，收口后切回复跑）。
- **验证**：隔离舱 **280/280 绿、全舱零警告**；v1-v3 全部判据与单测零改动
  保留。缺陷账本 v4 批 14 项全闭环。
- 对账全文：`docs/AI-K2-检查项对账表.md` v4 章；报告：`docs/AI-K2-完成报告.md` §12。

## [Unreleased] — Varix STAR I · AI-K2 内核底盘·后段（F058-F075 深化批次三 · v3 深化 + 隔离验证/检查项对账）

**kernel/varix/src/perfstar2/ 18 域 v3 深化**（AI-K2 泳道一，2026-09-26）：

- **规模**：15,851 → **21,356 行**（+5,505）；CheckSet 判据 510 → **752**
  （deep3 +242）；单测 170 → **225**（+53）；达成率 22.0% → **29.6%**。
- **亮点子系统**：F059 CoDel 驻留时间队列（间隔指数加密）；F060 CC/CV 充电
  预测 + 内阻阶跃诊断；F062 三类慢故障注入矩阵（主册验收判据可执行化）；
  F064 定点坐标法线性重采样 + 零空转帧闪避包络；F066 FsckLite 三查 + WAF
  写放大账；F068 主题差量装载 + 饥饿防老化；F070 证据包序列化（FNV 防篡改）
  + 二分排查；F071 多词 AND + 一字容错；F073 按字节预算的缩略图 LRU。
- **验证**：隔离舱全量 **464/464 绿**、perfstar2 文件零警告；v1/v2 全部
  判据与单测零改动保留。
- **缺陷账本**：v3 批 8 项（实现侧 2 + 断言侧 6）全闭环。
- 对账全文：`docs/AI-K2-检查项对账表.md` v3 章；报告：`docs/AI-K2-完成报告.md` §11。

## [Unreleased] — Varix STAR I · AI-K2 内核底盘·后段（F058-F075 深化批次二 · v2 深化 + 隔离验证/检查项对账）

**kernel/varix/src/perfstar2/ 18 域 v2 深化**（AI-K2 泳道一，2026-09-26；
Variable 指令「继续大量深化 + 隔离验证 + 检查项对账」）：

- **规模**：8,869 → **15,851 行**（+6,982）；CheckSet 判据 223 → **510**（深化检
  +287）；单测 102 → **170**（+68）；达成率 12.3% → **22.0%**（主册上限口径）。
- **F058-F070 性能域**：LZSS 压缩比预筛闸（哈希链+零页去重+冷龄两维评分+
  200μs 解压预算）；令牌桶整速 + SACK 乱序重排 + RFC6298 整数 RTO + DRR
  公平调度；SOC 库仑计（部分校正收敛）+ 剩余时长预测 + 放电健康双口径；
  MAD 稳健离群（零退化地板）+ 整数 Mann-Kendall 趋势 + Theil–Sen 斜率 +
  误报预算台账；三类慢故障归因 + 阈值实机标定 + 综合健康分；掌压拒绝 +
  点按状态机 + 边缘滚动带 + 惯性积分；音频水位账 + 时钟漂移补偿 + fade
  斜坡 + 饱和混音；睡眠仲裁 + RTC 最小唤醒表 + 掉电预测 + 归因聚合；
  断电重放遍历 + 检查点引擎 + journal 配对账；读合并 + 预读命中 + 并行
  占比折算；解码调度 + 二次机会驱逐 + 白屏前置门；切换编排回滚 + 档位
  迟滞；50 件场景状态机 + 帧统计位次引擎 + 季度环比。
- **F071-F075 壳层五件**：拉丁别名索引（前缀+词首序列）+ 三类结果稳定合并 +
  历史加权；半衰期衰减公式（五锚点单调）+ 多别名归并 + 三消费面同序；
  解码去重队列 + 指纹缓存索引；键盘游标状态机（跳过标题行/循环/不误触）+
  构建序确定性对拍；折叠预算引擎 + Tooltip 三态调度 + 电量三读数滤波。
- **验证**：隔离舱 `D:\2\aik2-scratch` 全量 **404/404 绿**（1.9s）、perfstar2
  文件零警告；v1 既有 223 主检 + 102 单测零改动全保留（回归零破坏）。
- **缺陷账本**：v2 批 13 项（实现侧 5 + 断言侧 8）全闭环——亮点：inertia_step
  溢出、MAD 零退化、中断/超时归因混淆、健康分首日衰减语义、SOC 校正收敛
  残差。跨队：K1 startprof_ext 一处外科代修（已注明 K1 复核）。
- 对账全文：`docs/AI-K2-检查项对账表.md`；批次章：`docs/AI-K2-完成报告.md` §10。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次十 · 事件通道全面唯一化）

**H4 事件架构终态**（AI-H4 泳道三四分队，2026-09-26）：

- **window 双轨全部撤除**：SUMMON×2（拾色器/标尺）、FOCUS×2（专注开始/放弃）、
  READING（阅读模式）全部迁入 h4Bus 契约事件——v9 迁 filter 一条后本批收尾全部。
- **BINDINGS 契约演进**：4 条入向指令事件登记（settings.summon-picker/
  summon-ruler/focus-start/focus-abandon/reading-toggle）+ F359/F360/F363
  surfaces 如实补 settings 面（ledgerDiff 可查演进轨迹）。
- **通道唯一化收尾舱**（crossbatch v10）：全 H4 面 grep `new CustomEvent(vx-h4-*)`
  零命中——「唯一通道」由机检保证。

**对账**：H4 全域 vitest **596/596 全绿**（62 文件）；tsc H4 面 0 错误。纯功能累计
**≈12,908 / 45,305 = 28.5%**（迁移批次架构收益为主；缺陷账本 2 条即时修——含一次
回读复捕的编辑事故，见 `_attic/aih4-f351-f400/` §三十）。十批终点：单总线 + 单日志 +
单契约；引擎 50/50 + 判据缺口补齐 + 两真实表面活挂载 + 十代验证舱。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次九 · 快速设置磁贴 + 滤镜通道唯一化）

**F387 判据缺口补齐**（AI-H4 泳道三四分队，2026-09-26）：

- **快速设置磁贴**（quickTiles.ts + H4QuickTiles.tsx）：F387 判据点名
  「快速设置磁贴+快捷键双入口」——灰度开关磁贴（引擎互斥 toggle 投影）+
  拾色器/标尺呼出钮；复用 QuickPanel 原生 qp-tile 样式（零新增 CSS）；
  灰度态订阅 h4Bus 跟随全系统变更；一行挂载（DemoModeButton 同款先例）。
- **滤镜通道唯一化**：announceFilterChanged 内部改走 h4Bus.emit（H4Tab 零改动）；
  H4Runtime 撤 window 双轨监听——滤镜事件全系统唯一通道（f387.filter-changed）。

**对账**：H4 全域 vitest **595/595 全绿**（62 文件，v9 增 4 用例）；tsc（含
QuickPanel 挂载面）0 错误。纯功能累计 **12,868 / 45,305 = 28.4%**（v9 新增 102 行，
不注水；0 测试红灯，1 处施工自纠，见 `_attic/aih4-f351-f400/` §廿七）。
F387 判据三层全绿：引擎 + 双入口 + 唯一通道。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次八 · 任务栏真实挂载 · 最小侵入）

**H4 功能在真实任务栏窗口上活渲染**（AI-H4 泳道三四分队，2026-09-26）：

- **任务栏芯片纯模型**（taskbarLayer.ts）：专注徽标（F363 mm:ss 秒级走字）/
  任务徽标（F369）/ 布局指示（F373 恒显一键轮切）/ 电池（F366 三档 + 低电黄红闪）——
  顺序词典、**零编造**（无真实读数不出芯片）、点击事件契约预校验。
- **渲染壳**（H4TaskbarLayer.tsx）：专注态走 h4Bus 契约事件、电池走 navigator
  Battery API、秒级拍频、点击 = h4Bus.emit（拒绝即 xlog 留痕）。
- **一行挂载**：任务栏独立 Tauri 顶层窗入口（最小侵入，零他队逻辑改动）+ 芯片样式
  （黄/红/闪烁 + prefers-reduced-motion 降级）。

**对账**：H4 全域 vitest **591/591 全绿**（61 文件，v8 增 8 用例）；tsc（含 taskbar
挂载面）0 错误。纯功能累计 **12,766 / 45,305 = 28.2%**（v8 新增 219 行，不注水；
缺陷账本 1 条断言侧即时修，见 `_attic/aih4-f351-f400/` §廿四）。八批垂直切片闭环：
引擎语义 → 纸面契约 → 活契约总线 → 体验日志 → 面板 → 任务栏真实挂载 → 八代验证舱。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次七 · 契约执法事件总线 + 统一体验日志总线）

**src/features/h4/ 活契约基础设施**（AI-H4 泳道三四分队，2026-09-26）：

- **契约执法事件总线**（bus.ts）：BINDINGS 纸面契约运行时化——emit 未登记事件即
  拒绝 + 显性原因（零静默）；handler 异常逐个隔离 + 错误计数显性；投递账环形 500 +
  通配订阅 + replayTo 回放；h4Bus 单例为桌面壳接线唯一事件通道。
- **统一体验日志总线**（xlog.ts）：十三章域内实装——五级环形 500；元数据隐私红线
  （>200 字符截断 + 显式标记，正文全文进不来）；批量异步 flush（sink 注入、sink 失败
  显性落环）；挫败信号（狂点 1.5s/24px/≥3 次、死点 >100ms）带结论字段自动标记；
  时间轴过滤 + JSON 导出。
- **H4Runtime 接线**：开机徽标/彩蛋/滤镜/阅读四条契约事件走 h4Bus 真投递 + xlog
  埋点 + 卸载兜底冲刷；跨批次清单纳入第七批。

**对账**：H4 全域 vitest **583/583 全绿**（60 文件，v7 增 19 用例）；tsc H4 范围
0 错误。纯功能累计 **12,547 / 45,305 = 27.7%**（v7 新增 306 行，不注水；缺陷账本
1 条断言侧即时修，见 `_attic/aih4-f351-f400/` §廿一）。垂直切片齐装：引擎语义 →
纸面契约 → 活契约 → 体验日志 → 面板 → 七代验证舱。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次六 · 消费面接线契约层 + 跨批次总对账舱）

**src/features/h4/ 接线契约层**（AI-H4 泳道三四分队，2026-09-26）：

- **五十项 × 14 表面接线契约**（consumers.ts）：BINDINGS 全量表——每项挂载面/
  mountId/入向事件/出向事件/人话说明一处一事实；INPUT_SOURCES 入向源词表 37 域；
  四道审计（事件命名双向两域 / 挂载点唯一 / 表面覆盖零空面 / 登记册逐项对齐）；
  接线报告与 bindingFor/bindingsOn 查询面。
- **接线矩阵面板**（H4Wiring.tsx + CSS + H4Tab 注册）：审计灯条 + 表面挂载统计 +
  可筛 50 行高密度矩阵 + 行点开契约详情。
- **跨批次总对账舱**（h4crossbatch.spec.ts）：六批产物清单核对（零幻影交付）+
  六代隔离舱关键项横向复跑（纯逻辑/哈希单点/源码纪律/无环/层级/键域）+
  账册数字交叉核对 + 缺陷闭合 grep 验证（v4/v5 声明修复逐条留痕核实）。

**对账**：H4 全域 vitest **564/564 全绿**（59 文件，v6 增 27 用例）；tsc H4 范围
0 错误。纯功能累计 **12,241 / 45,305 = 27.0%**（v6 新增 513 行，不注水；v6 缺陷
账本 4 条全即时修——含事件域模型设计级补全，见 `_attic/aih4-f351-f400/` §十八）。
剩余口径差的诚实归因与去向（桌面壳接线施工 / 实机走查）见完成报告 §13。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次五 · 剩余 26 模块引擎深化 + 隔离验证再扩展）

**src/system/h4/ 剩余 26 模块引擎深化**（AI-H4 泳道三四分队，2026-09-26；
Variable 指令「继续大量深化……直到全部完成」）：

- **键盘/菜单**（F373/F374/F376）：切换 OSD + 死键模型（组合失败零吞键）+ 热插拔通知；
  键位规范化（修饰键稳定序）+ 卡内搜索 + 分组折叠；菜单键盘导航（循环跳过置灰）+
  双击标题语义 + 加速键（键盘不许绕过置灰状态机）。
- **列表/树/对话框**（F378/F379/F380/F381/F382）：预算内多列自适应 + 手动宽度优先 +
  CJK 计量器；类型感知比较器 + 稳定排序审计 + aria-sort；空白拖拽衔接 + 双击新建位；
  万节点迭代扁平化（显式栈）+ 懒加载 + 移动成环校验；尺寸记忆防抖 + 工作区感知 + 焦屏落位。
- **隔离验证再扩展**：import 图无环检测（DFS 三色标记全扫——零环）+ internal 层
  零出向依赖（层级不变量）。
- **域门禁**（F375）：门禁回归对比（新红/转绿/持续红）+ 豁免登记（过期自动失效）。
- 其余：f352 连点防抖/键盘等价、f353 拓扑签名/记忆 LRU、f354 空闲通道审计、f355
  UA 哨兵、f359 WCAG 对比度/抗噪取样、f360 量测账/物理换算、f363 番茄连击/提醒降级、
  f366 阈值边沿/双电池、f367 分区模板校验、f368 托盘菜单/单实例恢复、f371 慢启动归因、
  f385 朗读三段合成/词表完备、f386 段落重排零侵入审计、f387 强度档位、f388 旋转过渡/
  批量分帧、f399 口碑守卫、f400 覆盖率统计/登记册 diff。

**对账**：H4 全域 vitest **536/536 全绿**（57 文件，v5 增 88 用例）；tsc H4 范围
0 错误。**50/50 模块引擎深化补齐**——隔离验证四层全绿（结构/纪律/架构/死代码）。
纯功能累计 **11,728 / 45,305 = 25.9%**（v5 新增 1,061 行，不注水；v5 缺陷账本
10 条全即时修——4 实现侧 + 6 断言侧，见 `_attic/aih4-f351-f400/` §十五）。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次四 · 引擎深化 + 隔离验证/检查项对账扩展）

**src/system/h4/ 22 模块引擎深化**（AI-H4 泳道三四分队，2026-09-26；
Variable 指令「继续大量深化 + 隔离验证 + 检查项对账」）：

- **快照与恢复**（F351）：快照完整性校验和 + 三道闸导出/导入（可解析/信封/校验和）
  + 恢复 Dry-Run（精确模式零漂移直证 + 越界预点名）。
- **下载收口编排**（F357）：Edge 事件观察态归约器 + 托盘聚合 + 完成通知合并
  （风暴不轰炸）+ 豁免时间线；系统只观察不拥有（宪法边界结构保持）。
- **PiP 布局解算**（F358）：停靠几何（呼吸边距）+ 多窗无重叠碰撞解算 + 回原窗
  计划（时间点不跳的执行面）。
- **录制链**（F361/F362）：帧成本模型（<5fps 判据推演面）+ 码率模型 + 三档诚实
  丢帧；分节账链完整性校验和 + 连续性审计 + 配额驱逐（当前会话保护）。
- **数据管线**（F364/F365/F392）：同名冲突消解（永不静默覆盖）+ 三级扫描管线
  （大小预筛→头尾采样→全量兜底）+ 保护路径 + 保留评分；缓存上限 + 计量队列。
- **调度与不惊扰**（F369/F370）：ETA 指数平滑 + 进度单调守卫 + 饿死看门狗 +
  准入控制；确定性抖动退避 + 每日提醒预算 + 免打扰顺延 + 前台帧率采样环。
- **窗口与浮层**（F377/F383/F384）：边缘吸附 + 步进撤销栈 + 300 步自检走查；
  焦点归还账 + 弹层生命周期日志（30s 反复开闭挫败信号）；焦点环构建防御 +
  双向环游 + 归还审计。
- **存储与备份**（F393/F394/F395/F396/F397）：**完整 Bruls squarified 布局升级**
  （面积 ±2% 对账保持 + 长宽比量化）；不可逆闸门（缺代价说明硬阻断）；磨损模型 +
  寿命外推（TBW 缺失不编数）；备份清单校验 + 链完整性 + 保留策略；演练调度数学 +
  沙盒白名单。
- **拼接/词典/开放性**（F389/F390/F356/F398）：归一化行哈希 + 通配重叠稳健拼接
  （动态行不再一票否决整图）；内置 40 词离线词典 + 词形变体归一 + 分页 LRU；
  manifest 深校验（4K 图标红线）+ 安装 Dry-Run；占位符校验 + 复数规则 + RTL 镜像令牌。
- **FNV-1a 收敛单点**：`internal/hash.ts` 新建（FNV-1a/canonicalJson/结构校验和），
  f372/f386 私抄件收编委托——全域哈希一处一事实。

**对账**：H4 全域 vitest **450/450 全绿**（56 文件：引擎 311 + v2 12 + 隔离 10 +
对账 7 + 浮层 14 + v4 深测 63 + 各模块单测）；tsc H4 范围 0 错误。**隔离验证扩展**
三道新机检门：源码纪律（零 TODO/console/as any）+ 哈希单点（魔数只准在
internal/hash——首航即抓出并收编 f386 存量私抄件）+ internal 通道零越境。
**检查项对账扩展**：22 模块深化导出面逐项可发现性核对全绿（零死代码自证）；
既有对账（标题/判据与主册逐字一致、200 基线、三处同源）零回归。
纯功能累计 **10,667 / 45,305 = 23.5%**（v4 新增 1,633 行，如实登记不注水；
v4 缺陷账本 14 条全即时修——2 实现侧 + 12 断言侧，见 `_attic/aih4-f351-f400/` §十二）。

## [Unreleased] — Varix STAR I · AI-H3 H 基础通用域·三分队（F301-F350 深化批次三 · 隔离验证与检查项对账）

**kernel/varix/src/h3star/ 六件功能核**（AI-H3 泳道三三分队，2026-09-26；
Variable 指令「继续大量深化 + 隔离验证 + 检查项对账」）：

- **统一调速器核 FrameGovernor**（F331-333）：低电量>预算>帧率分级三机制
  优先级仲裁 + FrameCost 帧成本模型（「降级期间响应延迟不增」数值直证，
  全周期乱序采样守卫）+ 模式切换环形账。
- **vxapp 卸载全链彩排**（F344-346/F342）：注入测试应用端到端五项对账单
  （含执行漏项防呆）+ 声明权仲裁（安装不抢默认执行面）+ 磁盘忙时排队与
  人话映射表 + 冷启动重放与估算 ±20% 校准。
- **路径互通引擎**（F336-337）：归一化核（混合分隔符/点段/越界钳制/UNC/
  POSIX/WSL）+ 逐味转义总表 + 互通事件账 + open . 语义核；初版 unsafe
  垫片按内核纪律重写为纯安全实现。
- **OCR 识别管线本体**（F314）：截取状态机 + Otsu 二值化 + 栈式连通域 +
  8×8 密度特征 + 模板最近邻（置信度‰）+ 行序组装——离线判据升级为
  「管线每阶段都真离线」。
- **断点续传引擎**（F321）：分块位图 + fnv1a 块校验 + 只补缺失 + 零缺失
  才完成 + 拒绝清场幂等。
- **全量模糊规则表**（F312）：7 规则逐条开关 + 查询变体展开 + 命中层序
  Tolerant→PinyinFull 提升的端到端证明。

**对账**：隔离舱 cargo test **265 域内通过 / 0 失败 / 0 警告**（域聚合 52 块
全绿双探针证实）；纯功能实数 13,036（50 项 12,402 / 上限 60,710 = 20.4%，
脚本四分法口径）；D-23 虚账修正（上一版 20,057 手填估算作废）+ D-24~D-26
认账修复，明细见 `_attic/aih3-f301-f350/行数对账与缺陷账本.md`。

## [Unreleased] — Varix STAR I · AI-D1 桌面体验·前段（F076-F092 v4 上限收口批）

**kernel/varix/src/deskstar/ 八模块深化 + 域聚合器 51 块**
（AI-D1 泳道三前段分队，2026-09-26 v4 批；Variable 指令「继续大量深化 +
达到所有纯功能代码数量 + 隔离验证与检查项对账」三件全落地）：

- **逐项全部 ≥ 主册上限 100%**：17 项实装 18,309 行 = 域总上限 16,900
  的 108.3%（19 文件总 19,133 行）；v3 后低于上限的 8 项（F076/F079/
  F080/F081/F082/F083/F087/F088）全部推过上限线，判据驱动零注水。
- **判据实体补齐**：F079 开机音 C-2 幕二同步点 + 错误音↔F035 绑定账
  + 队列风暴闸 + 静音/音量持久化；F087 键盘导航 + 决策记忆持久化 +
  「应用到全部」整批撤销；F082 方向键蛇形几何导航 + 鼠标点击双路
  一致；F080 0px 缝隙紧贴对账实体 + 键盘档位双路对账；F081 翻页
  回退/页码指示/切桌状态一致性；F088 深度上限校验 + 命中档位排序。
- **缺陷即时修 6 处**（冲突焦点落点、紧贴判据口径、切桌脏状态等，
  详账本 §v4-3）。
- **验证**：隔离舱 230/230 全绿零警告；311 条 CheckSet 对账表与
  51 块域聚合器同源；真实 crate deskstar 面 0 错 0 警。
  证据：`_attic/aid1-f076-f092/`（账本+对账表+隔离舱）、
  `docs/AI-D1-完成报告.md` §11。

## [Unreleased] — Varix STAR I · AI-U3 I 通用域·三分队（F501-F550 深化批次三 · 接线面+隔离验证+检查项对账）

**src/features/u3/actions|ledger|reconcile|deskiconLayer + U3Runtime/anchor/U3Tab 增强**
（AI-U3 泳道四三分队，2026-09-26 v3 批次；Variable 指令「继续大量深化 + 隔离验证 +
检查项对账」三件全落地）：

- **vx-u3-action 动作路由中心**（actions.ts）：12 动作登记表（F520 中键最小化/
  F535 Win+数字/F539 布局锁定/F542 ClickLock/F524 后悔窗/F511 清空/F505 锁屏…）
  + 应用>全局两级路由 + winnum-N/lockscreen: 别名事件 + 未处理显性化——对齐
  J1 actions.ts 同构哲学，桌面标题栏加 data-u3-titlebar 即接入中键最小化
  （开放扩展点不越权）。
- **F542 ClickLock 真鼠标管线**：mousedown/mouseup/click/Esc 四事件驱动状态机，
  抓起态 html 级光环标记 + 抓起/放下/放弃三出口动作化。
- **F550 三面对账引擎**（reconcile.ts + ledger.ts 数据层 + U3Tab 实时面板）：
  主册 50 判据 × 内核 616 CheckSet/307 单测账册（对源码实测自证——copyops 43
  勘误 v1 报告笔误 42）× 前端十域自检 74 检查点现场重跑，50/50 三面在位，
  缺面显性化 + 破坏注入测试钉住；F400/F575 编号空间合并无冲突断言。
- **桌面接线件**（deskiconLayer.tsx）：IconLabel 渲染件（双层渲染+两行封顶+
  亮度选字）、vx-u3-resnap 网格重排出口、vx-u3-shake 布局锁定拒绝反馈、
  F549 ClockHoverTip 悬停件。
- **隔离验证（worktree 干净快照）**：HEAD=e205239e 快照 + 仅本包 17 文件 →
  vitest 96/96 全绿、tsc U3 领地 0 错；纯 HEAD 对照证实 windowRuntime 6 错
  （AI-J1 既有）与 DesktopD2Panels 2 错（AI-D2 在制品）与本包无关。隔离验证
  即时兑现：揪出并清零 v2 遗留严格模式债约 70 处（含 GridDensity 类型缺
  "custom" 档真 bug）+ 循环依赖消环（ledger.ts）+ 递归自调用栈溢出修复。
- **行数对账**：v1 19,480 + v2 2,566 + v3 ≈540 = 22,586 / 46,540（48.5%，
  如实呈报）；96 前端单测 + 616 内核 CheckSet + 307 内核单测全绿。

## [Unreleased] — Varix STAR I · AI-H2 H 域二分队深化批次五（F251-F300 · 交互生命周期纵深）

**kernel/varix/src/h2star/ 引擎六件新增**（2026-09-27，域 23,385 → 24,858 行，累计 49.2%）：

- **浮层生命周期总管** h2overlay：四路关闭语义（点外/Esc/再点/失焦——确认框不失焦关、面板全开三套标准路由）+同键互斥（重复打开拒收留账）+焦点栈回落+逐层剥离 unwind+关闭生命线账（被拒关闭不污染账）+1000 轮翻搅零泄漏。
- **介质状态机** h2mediastate：十态贯通（Autorun 零执行=转移表无此边的状态级保证）+F293 询问 10s 收起+F294 Busy 弹出拦截计数留因+强拔→Suspect 标注生命周期（重挂保持/验证解除）+弹出三拍位（冲刷/卸载/可拔）+十态×十一事件全遍历封闭性压测。
- **字体安全档** h2fontsafe：魔数校验（ttcf/true/OTTO）+F159 三档判定（0%/8%/20% 缺字样本、80‰ 线、200 档单调扫描）+缺字清单前 20 稳定序+回退映射+系统锁定五件拒卸留因。
- **通知堆叠几何** h2notifstack：右下锚堆叠不遮任务栏+可见上限 3+零丢弃呈现决策（挤出旧横幅入中心、免打扰直入中心——通知本体永不丢）+50 条风暴全部有归宿+进出场滑动镜像对称。
- **快速访问服务** h2pinsvc：固定追加保序+去重+上限 10+取消幂等+拖排序审计+固定/推荐共存去重（同路径至多一次）+持久化 round-trip+注入拒绝。
- **桌面图标网格** h2draggrid：橡皮筋方向归一化+部分相交入选+集合拖动相对偏移不变式+负偏移边缘钳 0+Esc 快照精确复原（乱拖 200 步逐格相等）+碰撞换位（占用者有去处）+自动对齐走 GridSpec 单一定义点。
- **检查项对账**：新增 48 条，域检查总计 **801 条**；聚合器 60 块（64 上限余 4 席位）。
- **验证**：域内 **216/216 绿**（批次四 203 → 216）；全量回归全绿；缺陷 5 条全即时修——unwind 断言抓出「关顶层后焦点不回落」的栈管理缺陷为深层级状态机断言护卫价值证明。

## [Unreleased] — Varix STAR I · AI-H2 H 域二分队深化批次四（F251-F300 · 第三纵深+对账常设设施）

**kernel/varix/src/h2star/ 引擎九件新增（两波）+ 聚合器引擎族合并**（2026-09-27，域 20,988 → 23,385 行，累计 46.3%）：

- **窗口与屏**：h2zorder z 序四层带+置顶授权票根制（无票根拒绝+审计留账，50 次越权压测零漏账）+焦点链（变更才记事件+heir 接管+200ms 预算）；h2switcher Alt+Tab 卡片墙（焦点屏过滤+假死标注可选中+Esc 零副作用+12 卡布局逐对无重叠+确定性）；h2hotsync 分辨率热切换三守恒（不丢窗 moved+stayed=total 机判/相对几何千分率跨 12 档分辨率不漂移/越界钳进屏）+热拔迁移+拓扑指纹 FNV-1a。
- **解码与编辑面**：h2thumbq 可视区优先解码队列（同键去重+并发上限 2+取消不回填+失效重解+万轮翻搅不泄漏）；h2imeflow 组合期快捷键冻结（三编辑面共用唯一闸+组合串只进缓冲不进正文）+提交点校验（非法字符保持组合态可改对）+UTF-8 边界替换+补全暂停判定。
- **文件与回收站**：h2trash 容量水位 LRU 驱逐留账+钉选豁免+同名还原走副本阶梯+清空保钉选+目录聚合+100 轮删还原守恒+5000 轮翻搅零破线；h2tabmgr 宽度三档（160/44px+钉选最低 Name）+打开从活动签右侧长出+关闭焦点转移规则表+拖拽重排审计+恢复 10 签全状态。
- **服务编排**：h2wallsvc 三模式解析归一+轮换默认关（判据原文）+F297 浅色亮度闸 170+引用失效回退人话（闭包注入零 IO）。
- **对账常设设施**：h2dossier 五十项注册表（50,505 目标合计机算钉死）+域健康机查（孤例=零检查项点名/密度下限 4=首批既达密度）+CSV 开放导出——「检查项对账」从一次性脚本升级为域内常设断言。
- **检查项对账**：新增 86 条，域检查总计 **753 条**；聚合器按引擎族合并腾位（58 块，64 上限余 6 席位）。
- **验证**：域内 **203/203 绿**（批次三 183 → 203）；全量回归 **5111/5111 绿**；缺陷 5 条全即时修（含守恒式抓出「主屏被拔=全部迁移」低估边界——断言校准理解一例）。

## [Unreleased] — Varix STAR I · AI-H2 H 域二分队深化批次三（F251-F300 · 宿主胶水层+资产管线+渲染纵深）

**kernel/varix/src/h2star/ 引擎十二件新增（两波+收束件）+ 聚合器容量重构**（2026-09-26，域 16,689 → 20,988 行，累计 41.6%）：

- **宿主胶水层**：h2host 事件总线（六类事件分类唯一源+订阅幂等防抖+优先级稳定序分发计划+无人订阅显式空计划不吞事件）；100ms 反馈红线分发延迟账（超线计数不静默）；会话装配器（恢复族四步固定序+中断幂等续装+损坏降级自首+人话报告）；会话拆除计划（后开先关——浮层最先/遥测最后）。
- **资产管线**：h2asset DPI mip 档位选择（优先 ≥ 屏幕档最小原生档，放大必须打 upscaled 标记——4K 纪律机制化）；F068 主题 80MB 内存预算记账器（账面守恒+LRU 驱逐+常驻豁免+单笔超线拒绝留因+200 笔 4K 全套演练+万轮翻搅零破线）；F300 图标语审计管线（缺态/关键线越界/风格孤例三查——孤例用中位数共识判定）；缩略图三档解码+大源图渐进标记。
- **渲染纵深**：h2render F251 OSD 绘制面（270° 音量环+归属徽标位+50ms 首帧预算）；F254 材质采样核预算（倍频 taps 公式）+3 超线降档/30 回稳升档滞回机（阈值边抖动不横跳压测）；F260 编辑框渲染几何（插入符/选区规范化/IME 候选窗四边翻转/最小平移）；F276 落区呼吸环走 h2curve 总谱（周期对称闭合）。
- **呈现引擎**：h2list 虚拟可视区（上下缓冲+万行窗口恒定压测）+F274 索引跳段（段头滚顶+非段头拒绝）+键盘导航（跳段头/循环/翻页——500 步随机焦点不停段头不变式）+焦点跟随最小平移；h2menu 菜单引擎（构造序=渲染序的 F272 置灰稳定结构保证；F258 扩展注入闸门拦截留账；超限折叠两级封顶；四边翻转+子菜单反向翻+超尺寸钳制）；h2locale F296 单点服务（日期四样式+闰年入口拒收 2000/1900/2400 判例+格式→解析 round-trip+时间 24/12 上下午正午规则+千分位+KB=1024B 四舍五入单位进位+非零不显 0B）；h2power F291 排行聚合+3 倍异常对自身滑动基线（恰 3 倍不旗标——阈值边界诚实）+零历史不误报+归因覆盖率机检+RankRow 无执行字段（越权在类型层不存在）+F287 附加时钟（上限 2 机判+纯偏移换算含半小时时区/跨日+昼夜图标 6-18）。
- **编排引擎**：h2fsops 文件操作计划器（F262 六语义收口——同盘 Relink 单步/跨盘复制→校验→删源三段校验不过不删源；碰撞三策略接 h2base 副本阶梯 200 重名有界终止；每动作带逆操作 undo LIFO+直删无假逆；F270 取消保已完成+可 grep 报告）；h2dnd 拖放管线（六态全生命周期封闭性遍历压测；鼠标位移 6px/触屏长按 500ms 双准入；F255 四落点嵌套取最内层；Ctrl 修饰矩阵终端落点恒确认；拖影跟随手走+Esc 复原 100 次零幽灵）；h2appctl 应用生命周期编排（F283 三拍子齐才 Running；F284 >5s 边沿一次性蒙层+恰 5s 不触发+3 连心跳滞回+卡死恢复 5 轮循环不漂移；结束清算三件套一次全做；F282 两入口同账+漂移可检出）；h2settings 域设置存储（封闭键表 12 键拒表外键+类型契约+校验和损坏自首+版本迁移链 v1→v3 逐条可对账+未知未来版本拒绝不猜格式+截断包安全+500 发畸形包 fuzz 不炸）。
- **域集成演练收束件**：h2flow 跨引擎场景编排（会话全生命周期：装配含损坏降级→设置写用→后开先关拆除→封包重启回读；用户旅程：触屏长按拖文本进终端安全确认→Esc 放弃复原→跨盘移动碰撞改名→无响应→恢复→结束清算→两入口同账；演练账失败不中断一次出全量结论；同脚本同结果可回放机判）。
- **检查项对账增补**：批次三新增检查 149 条，域检查总计 667 条（既有 518+本批 149，grep 口径现测；前报 517 差 1 为扫描口径微差如实登记）；聚合器 64 块恰好顶格（批次三十二件按 CheckSet 容量拆 h2glue/h2ui/h2svc 三块——合并块不扩容的容量纪律显性化）。
- **验证**：域内 **183/183 绿**（批次二 150 → 183）；全量回归 **5091/5091 绿**；64 块聚合绿。缺陷 9 条全即时修（🟡 5 / 🟢 4，含测试期望重算 6 处——全部测试网拦截）。

## [Unreleased] — Varix STAR I · AI-H2 H 域二分队深化批次二（F251-F300 · 引擎接线+检查项对账）

**kernel/varix/src/h2star/ 引擎五件新增 + 模块群纵深十二件 + 检查项对账总表**（2026-09-26，域 14,231 → 16,689 行，累计 33.0%）：

- **引擎五件**：h2curve 动效参数引擎（F124 四档曲线——easeOutBack 弹性档解析峰值 1.0299≈判据 1.03、采样+解析双口径互证、端点吸附；F256 惯性采样器有界停机；打断对称=反向剩余时长）；h2edit 编辑态几何（CJK 宽字符计量、F260 主名/扩展名字节级隔离、F265 内联补全三段切分跨段不越权、F272 六项固定序菜单置灰稳定、滚动锚定最小平移）；h2taskbook 传输任务簿（F269 五态状态机+块边界暂停+断点好跳坏重传+关机询问；F270 三选一+中止保已完成+可 grep 导出报告）；h2launch 启动编排（F282 未声明默认单例+两入口同函数结构一致；F283 100/200/2000ms 节拍+2s 门槛人话）；h2screen 显示编排（F277 窗随屏迁机判+焦点屏判定；F278 组合指纹记忆+浮层跨屏包围盒居中）。
- **模块群纵深**：F275 关机执行链四段状态机（跳段不认账+提交前可退）+双入口幂等闸；F295 斜率中位数去抖+跨机回拨显性化+「提醒一次」语义修正为操作级；F284 恢复滞回（3 连心跳）+结束成功率账；F281 横幅生命周期三路归档+交互去重修正为按通知实例；F297 可关+51 点单调扫描+色相保持机判；F294 多事务聚合+强拔标注生命周期；F261 锁心跳过期+单文件确认形制；F259 网格位图复用+落地重名 (2) 递增；F257 h2rank 三键排序接线+更多应用 Top5；F283 与 h2launch 常量同源对账；F271 拖出 30px/拖回命中带；F298 三引擎接线。
- **检查项对账总表**（台账 §十）：脚本化扫描全部 `run_*_checks`——**517 条检查**（五十项 402 + 引擎/底盘 115），**50/50 项全覆盖零缺漏**，每项 4-15 条判据锚断言；聚合器重构 60→61 块（基础设施五件 merge 为 h2core，域内自足合并不依赖未落位 API）。
- **验证**：域内 **150/150 绿**（批次一 134 → 150）；全量回归 **5058/5058 绿**；61 块聚合绿。缺陷 6 条全即时修（🟡 3：两条主册语义修正 F295/F281 + 弹性曲线公式重设计；🟢 3：测试构造 8 处/废话检查行/未落位 API 依赖）——判据唯一源是主册原文，首批偏差在深化复查中纠正。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队（F401-F450 深化批次 · 前端功能面）

**src/features/u1/ + U1Runtime + U1Tab**（AI-U1 泳道四一分队，2026-09-26 深化轮；
对齐 AI-U3 深化批次先例形态，v1 判据实装层（kernel uni1 6,982 行）之上补齐
「界面与交互 ~60%」工程量类别的前端功能面 + 隔离验证与检查项对账设施）：

- **十二个前端逻辑模块**（u1store/hotkeys/menus/explorerkeys/flyouts/
  viewfx/disksuite/wizards/displayav/docops/ledger）：五十项判据的界面
  生效面——参数与内核语义核同源（150ms/100ms/50ms/2s/3s/15s/±8px/24px/
  250‰/步进表/亮度下限 10……一处一事实）；状态机同源（Esc 四层逐层剥离
  与不变量、粘滞键连按 5 次→确认框→锁存合成→五组合键等效、翻页相对位
  置保持、锚点缩放不动点、加密强制导出跳不过、蓝牙确认码不跳过、还原
  点轮替最近永留）；u1store 单配置根（十五节/订阅广播/原子写/撤销栈/
  导入导出校验拒载）。
- **桌面运行时 + 设置中心面板**：U1Runtime（Esc 栈/粘滞键层/输入法徽标/
  音量电池浮层几何/Win 键开合账/Shake 判定）；U1Tab 十分组全量面板 +
  **检查项对账区**（51 块 CheckSet 机数台账 ledger.ts ↔ tally.rs 同数，
  零截断/范围完整断言前端可执行）；i18n 双语注册。
- **隔离验证与检查项对账设施**（_attic/aiu1-f401-f450/）：tally.rs 机数器
  （51 块 492 项 · 零红零截断）+《隔离验证与检查项对账.md》（复现步骤/
  123 分解算术/双通道证据/他队阻断登记）。
- **验证**：u1.spec **54/54 绿**、tsc u1 文件 **0 错误**、全仓 vitest
  **3710 过/7 败（7 败全属他队旧模块，U1 零回归）**、隔离舱 **123/123 绿**
  （深化不动 kernel 的回归证明）。
- 行数对账：深化批次 **3,095 行**（功能面 ~2,409 + 单测 686）；域累计
  **10,077 行**（19.3%，如实登记）；深化轮缺陷账 #11-#16 全闭合（含
  ledger 手填险情→机数回填的流程纠偏）。

## [Unreleased] — Varix STAR I · AI-E1 个性化域（F151-F170 深化批次三 · 集成点层）

**src/system/persona/**（AI-E1 泳道三 E 域，2026-09-26 深化批次三收口；
批次二引擎层之上补齐跨域集成点与兼容矩阵——集成点在位等待对端注入）：

- **新模块两件**：compat-matrix.ts（令牌版本兼容矩阵三态判定 + v0→v1 迁移
  引擎——11 桥全接、非法值逐项指出、降级清单前置可见、迁移前原始输入随行
  「随时可退」）；integrations.ts（F116 日落注入点 SunProvider 注册/±5min
  判定、F072 计数直连批量灌入+两源对账、F121 双路保险直接回退 vs 还原点路径
  复测、F127 vxapp 打包直通声明、F187 时钟强一致 1s 容差判定）。
- **既有引擎增补四件**：archive-engine 包体积预估（导入前字节+人话刻度+资产
  引用计数）；widgets-engine 天气断网降级「数据截至」+ 性能保护执法判定；
  imeskin F126 增节 JSON Schema + 皮肤包双向校验；font-engine 3500 字 <100ms
  扫描预算判定（归一化口径）。
- **接线增量七处**：令牌表页兼容矩阵卡、深浅切换页 F116 注入状态卡、档案页
  体积预估、小组件页断网降级与性能执法行、输入法页 schema 导出、右键菜单页
  F072 灌入演示、域总检页双路保险实跑。
- **验证**：E 域单测 **192/192 全绿**（+16）；typecheck `--incremental false`
  全新口径本域零错误；walkcheck SELFTEST PASS；检查项对账 tally 二十桶全非零
  （`_attic/aie1-f151-f170/`：隔离验证与检查项对账.md + tally.mjs 可复跑）；
  行数对账 9,687→10,348 纯功能行（49.8%，如实登记）。
- **缺陷账三笔全即时修**：v2 收口 tsc 增量缓存假阴性掩盖 2 处真实错误（本批
  抓获修复，验证规程升级为强制 `--incremental false`）+ heredoc 笔误 + 插入点
  未回读——零遗留零静默，详见对账文档 §4。
- **全树 build 观察**：本批时点 vite build 被 U3Tab 在途 WIP（未提交的
  named-export 缺失）阻塞——非 E1 任务面不代修，观察登记；E1 隔离验证口径
  （vitest 单目录 + tsc 域内过滤）不依赖全树健康。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次三 · 隔离验证+检查项对账+真实浮层）

**src/features/h4/**（AI-H4 泳道三四分队，2026-09-26 v3 批次；v2 界面壳层之上
补齐三块：隔离验证舱、检查项对账、真实浮层运行时）：

- **隔离验证舱**（h4isolation.spec，10 用例）：引擎层隔离不变量机检——
  ① import 边界（零 react/零框架/零越境 import；跨模块依赖白名单逐条
  登记理由，首航即抓出 f391→f390 漏登记项并补册）；② store 键域自域纪律
  （每模块只写自己 F 号域）+ 全域 28 键零碰撞 + h4Key 唯一入口（绕行者
  即破口）；③ 登记册↔磁盘↔一行账三处对账（50 模块/50 测试一一对应、
  无册野模块=0、H4_TITLES 同源逐字一致）。
- **检查项对账**（h4reconcile.spec，7 用例，直接读主册 1MB 原文逐字比对）：
  F400 一行账 50/50 与主册正文标题逐字一致 ✓；registry 判据摘文与主册
  「验收判据：」句 50/50 逐字一致 ✓；主册 F201-F400 基线 200 项全数在册
  且判据锚覆盖完备 ✓；三处同源真数据过闸（主册 200=一行账 200=检查点
  200）✓；**诚实边界钉死**——H1-H3 150 项只走注入通道，未注入时
  baselineComplete=false 如实「待注入」，绝不代绿。
- **GatePanel 诚实修正**：v2 面板里伪造的「H1-H3 注入项全绿」清除，改接
  reconcile.ts 的 h4DomainStatus 真状态（H4 50/50 已验 + 150 项如实待注入；
  200 基线的逐字钉死在离线对账舱，面板不复制主册文本）。
- **真实浮层运行时**（overlays.ts 逻辑层 + H4Overlays.tsx 四表面，挂入
  H4Runtime）：拾色器（真指针 elementFromPoint→computed style 解析
  rgb/rgba/hex+祖先回退链，取不到不编色；取到即复制 HEX+RGB+进 F234
  最近色+Shift 连续——f359 引擎语义）；像素标尺（真拖拽 dx/dy 读数+
  G 键 8px/20% 网格+Esc 秒退——f360 gridPaintSpec 一处定义）；快捷键
  速查（真键盘长按 Win 600ms 触发 f374 状态机，数据源=N-17
  effectiveKeymap 真源改键即反映，与 Z-12 Ctrl+/ 入口共存）；专注芯片
  （真计时徽标+到点双通道提醒+每日账防重——f363 引擎）。设置页拾色器/
  标尺行改真呼出按钮；专注面板与全局芯片 FOCUS_EVENT 同源联动。
- **验证**：H4 全域 **354/354 全绿**（引擎 311+v2 12+隔离 10+对账 7+
  浮层 14）；纯功能行数 8,259→9,034+（18.2%→19.9%，如实登记不注水；
  v3 缺陷账本 5 条全即时修，见 `_attic/aih4-f351-f400/` §九/§十）。
- **依赖**：devDependencies 增 @types/node（对账/隔离舱读主册与源码所需，
  零运行时影响）。

## [Unreleased] — Varix STAR I · AI-S2 安全加固域·后段（F186-F200 深化批次三 · 45 面 + 检查项对账机器钉数）

**kernel/varix/src/secstar2/**（AI-S2 泳道一安全后段，2026-09-26 v3 批次；
v2 两轮深化之上第三轮全模块展开——主册 G-G-16~G-G-30 剩余细节条款收网）：
域总量 **16,084 行（对上限口径 44.8%）· 检查项 846 条机器钉数（`check_ledger()`
+ 对账测试双机检）· 单元测试 242 项全绿 · 宿主/内核镜像（no_std）双口径零
错误零警告**。

- **15 模块 × 3 面全清单**（逐面判据源见报告 §v3-1）：syspart 盘符分配
  隔离/全事件审计/容量水位；clockguard 校时历史行/源池健康度/时区切换四
  项联动对账/RTC 推断黄条；logring 级别五档统计/导出命名契约/洞标记查询
  （真灌满 256KB 环取证）；selfheal2 降级默认态映射/快照协同对账/通知升
  级显目；slotview 条款卡/到期预告/B-1304 差异定位；bootaudit 预算对账/
  免查语义标记/拦截语义机检；paramwl 建议覆盖矩阵（距离 1 全命中）/三族
  文档页/审计流整行；safemode 黄条渲染契约/帮助篇三段/功能门矩阵（100 功
  能压测）；auditchain 隔离段导出/P0 工单契约/脱敏统计账；resquota 配置
  持久化往返/阶梯文档页/放宽审计流；batguard 放电斜率预估/提示卡三区/账
  目导出守恒；thermgov 24h 统计/事件-曲线对齐/传感器诊断页；recenv 三条
  件报告/返回栈/入口审计；lineage JSON 形状校验器/时间线图例/复制保真模
  拟；walkall 季报第五节生成/脚本批注册幂等握手/四态图例。
- **检查项对账机器钉数**：`secstar2::check_ledger()` 15 模块 × 3 表逐块
  计数（基检 337 + 深检 v2 333 + 深检 v3 176 = **846**），对账测试断言
  钉死总数、逐块容量 ≤64、聚合器恒 15 行——报告数字与代码永不脱节
  （本轮机器对账发现并更正 v1 报告的 315 基检误差，如实入账）。
- **缺陷账本 v3**：9 项自抓自修全闭合（300s 静默边界/洞标记取证口径/聚
  合器注释对齐/abs_diff 容差修复/统计点位/no_std 七文件导入恢复/复制模
  拟返回值/批注册类型链/主仓并行干扰登记）。
- 复现口令与逐块计数表见 `docs/AI-S2-完成报告.md`（§v3）。

## [Unreleased] — Varix STAR I · AI-C1 应用兼容域·前段（F001-F020 深化批次二 + 检查项对账）

**kernel/varix/src/compatstar/**（AI-C1 泳道二应用兼容前段第二批深化：主册
G-A-01~G-A-20【设计细节】条款继续落地 + CheckSet 检查项对账 + 隔离验证完备）：

- **二十域全部深化**：占位窗呼吸参数化+CPU 限额+无缝替换（F001）、TLS 回调
  目录解析+节惰性提交记账（F002）、delay-load 首调绑定+LRU 淘汰（F003）、
  星卡日去重（F004）、控件消息四族 21 钉值（F005）、TextMetrics/背景模式/
  ETO 选项位（F006）、FillMode 点包含判定核（F007）、OFN 标志+缺省扩展名
  推断（F008）、蜂巢 JSON 导出+污节点合并（F009）、前缀路由器+共享白名单
  （F010）、TEMP 沙盒化+值类型叠层面（F011）、标题/输出节流/关闭宽限
  （F012）、环境变量目标展开+DarwinID（F013）、组图标成员选择（F014）、
  **Big5/Shift-JIS 锚点字子集起建**+覆盖率报告（F015）、字体角色+别名规范
  化（F016）、延迟渲染（F017）、拖拽视觉/超时钉死（F018）、类对象会话级回
  收（F019）、24h 三崩/双重故障/dump 池钉死（F020）。
- **检查项对账**：CheckSet 检查项 **227 主检 + 92 深化检 = 319 项**（聚合
  `run_X_checks = merge(base, deep)`，AI-U2 先例，robust.rs 接线零改动）；
  92 项深化检逐名清单程序化提取入账（对账文档 §三）。
- **隔离验证完备**：隔离壳与主仓 compatstar 逐字节同步 + 壳 crate 根补
  `extern crate alloc;` 档位对齐；全量 213 宿主测试全绿；主仓 no_std 档
  `cargo check` compatstar 错误 = **0**（批次二新代码全过 no_std 纪律）。
- 施工缺陷账本批次二 **6 项全数闭合**（呼吸三角波断言形状/charge 丢计数
  由深化检捕获/断言读覆写缓冲/重复定义一处一事实清理/夹具语义/旧测试随状
  态演进）——对账与复现见 `_attic/aic1-f001-f020/行数对账与缺陷账本.md`；
  规模：总 17,649 行 / 纯功能 13,181 行（较批次一净增 2,365 / +1,686）。

## [Unreleased] — Varix STAR I · AI-U2 I 通用域·二分队（F451-F500 深化批次 v2）

**kernel/varix/src/genstar2/**（AI-U2 泳道四 I 通用二分队深化批：v1 判据
实装层 11,198 行 → **19,651 行**，51 文件 +8,707/-246）：

- **深化覆盖 44/50 项**：每模块追加 `run_*_deep_checks()` 深化自检
  （`CheckSet::merge` 合并接线，聚合 50/50 一行账不变）+ `mod
  deep_tests` 宿主单测；6 项为规范/登记类或深化并入主检。
- **功能面展开**（摘）：F451 批量创建与断链登记表、F452 PNG 六档校验、
  F453 同锚互斥矩阵、F455 桌面预设三套、F456 预算分解账、F459 搜索
  URL 组装、F462 浮条生命周期状态机、F463 EXIF 全 8 态、F465 别名
  展开零堆化、F468 敏感变体注入、F471 万行淘汰语义、F472 透明红线
  双判、F476 persist 线格式统一、F478 压缩管线三分支、F481 掌压注入
  矩阵、F482 三链路镜像矩阵、F484 异步握手入口、F486 隐私暂停账、
  F487 不可恢复三重闸、F491 持久化格式修复、F496 编辑态冲突消解、
  F499 解耦-共享往返、F500 L 形拓扑相邻性。
- **缺陷账本**：3 🔴（envedit persist 线格式断裂、autolum 序列化越界、
  coolgov u16 溢出）+ 9 🟡（零堆违规 8 处、lib.rs ustar3 重复声明、
  21 处在途测试期待对齐）全部即时修，零遗留零静默。
- **回归**：genstar2 域 315 passed / 0 failed；全域 5,347 passed /
  1 failed（唯一失败为 ustar3 在途 F521/F525——AI-U3 已提交代码，
  与本分队无关，已报备）。验收于独立 worktree（干净 HEAD + genstar2
  增量），口径可复现。

## [Unreleased] — Varix STAR I · AI-D1 桌面体验域·前段（F076-F092 深化批次 · 全项达标收口）

**kernel/varix/src/deskstar/**（AI-D1 泳道三桌面体验前段深化批：v1 判据
实装层 11,265 行 → **16,989 行**，17 项全部越过主册上限 90% 线）：

- **F088 搜索即输即显深化**（545→1,198）：搜索框编辑原语（光标位插
  入/退格/全选替换/Home/End）、范围胶囊键盘可达、渐进扫描首屏先行
  （512 条/拍、进度千分比、取消中拍生效、深度过滤真实执行）、Esc 两拍
  语义（清空→退出回位）、高亮渲染分段与 Accent 40% 令牌、目录变更联
  动索引失效、结果视图模式保留。
- **F092 压缩/解压深化**（1,040→1,595）：流式解压会话（F086 进度馈送、
  取消留部分、盘满暂停+恢复、路径穿越归因日志且工作量同步剔除）、密
  码卡三次口径（三误出「确认密码或换工具」）、压缩档位三选面、产物命
  名（单选=选中项名.zip/多选=父目录名）、右键菜单灰置带原因、临时物
  用后即删账、分卷 zip 检测诚实拒（差异表不承诺落地为 SplitArchive）。
- **F091 详情窗格深化**（644→1,100）：行渲染模型（标签/值/令牌/28px
  行几何）、类型与大小人话格式、日期格式注入口（F187 联动）、窄窗自
  动隐藏的记忆还原（只还原用户开过的）、EXIF 收起对称+锚行、查看菜单
  与右键双入口同账、渐进统计单调合并（回退值拒收）。
- **F090 面包屑深化**（500→880）：省略段展开状态机（点「…」展开全层
  序、中间层可点击、可收起）、下拉内搜索过滤、滚动可视窗 12 行钳底、
  补全键盘导航（焦点+Enter 接受+Esc 只关下拉）、面包屑键盘焦点环、
  「回到当前」钮、跨盘判定口（F018）、只读 tooltip 与 Err 同源。
- **F080 吸附动画深化**（919→1,192）：桌面独立分屏记忆（(桌面id,应用
  名) 键+全局回退）、记忆持久化 round-trip（脏行跳过/未知名拒收）、
  Snap Assist 互补落位、DropZone name/by_name 稳定字面量。
- **F081 任务视图深化**（876→1,113）：「+新建」250ms 展开动画账、缩略
  统一比例投影（千分比定点单系数保持相对大小感）、桌面名持久化
  round-trip（脏行/幽灵 id 跳过）。
- **F082 Alt+Tab 深化**（771→991）：候选池分页修正（「上限 24（超出滚
  动）」由截断改为 4 页池+墙页滚动）、卡内几何三件（图标左上/标题底条/
  最小化角标位）、标题字符级截断、翻页键接缝、删窗致池收缩的切片钳制。
- **F078 日历深化**（881→1,082）：周起始日区域设置（中国周一/可切周
  日）、星期表头、格几何与点击选日（补位格不误选）。
- **F087 冲突面板深化**（867→1,041）：对比卡几何（左右卡 200px+动作
  列）、时间标签（绝对到秒+相对时长直调 F072 recenteng——一处一事实）、
  批量行几何。
- **F084 图标网格深化**（622→818）：自动排列排序键（名称/类型/日期；
  缺日期诚实回退名称序；落格委托基座核心）、主题分套布局（保存/载入/
  缺套如实拒）。
- **F076 快速设置深化**（762→866）：Wi-Fi 连接中卡内转圈相位账、卡布
  局序列化投影（顺序+显隐）。
- **F083 桌面刷新深化**（547→644）：刷新原因三分类记账（F5/右键/自愈；
  被合并请求不入账）。**F089 标签页深化**（573→694）：跨标签共享枚举
  缓存（命中账/变更失效账）。
- 自检判据逐条钉死：**220 CheckSet 检查项 + 107 宿主单测全绿零警告**
  （隔离舱 `#[path]` 直挂实测 196/0/0，工作脚本清场后）。
- **接线与真实镜像预验（同日第三轮）**：域聚合器扩列 38 块（deep 与基
  础同列）、robust.rs 补挂 deskstar 单聚合（对齐 U2/J2/I4/H1 纪律）、
  隔离舱重构为单点直挂 mod.rs（197/0/0）；修复 16 文件 no_std 显式导
  入缺口（`alloc::{vec,format}`/`String`/`ToString`——v1 起该域从未在
  真实 crate 编译过，舱内 std 环境遮掩），修后实测 deskstar 面
  **错误 0、警告 0**（真实 crate 全量测试运行仍待 h1star/svstar/h2star
  在途错误清零，非 D1 面未代修）。
- **大量深化批（第三轮）**：crumbsbar/zipkit/calflyout/detailpane/
  notifctr/trashui 六项再深化（兄弟行几何与 F084 视觉族直引、DOS 时
  间戳编解码与清单预览/单文件解压/冲突预检、悬停日期聚合面与周起始
  持久化、EXIF 缓存读路径与多选渐进、历史持久化序列化边界、删除时
  间排序与 F087 引擎复用还原）——13 项超主册上限、4 项 92-99%、
  **0 项低于 90% 线**（17 项实装 17,232 行 = 域上限 102%）。
- **零 panic 红线修复**：7 处非 test 运行时 `.unwrap()` 改防御式
  （rust-best-practices 红线审计发现，v1/v2「构造保证」→ 类型保证）。
- **域聚合器扩列 44 块** + **检查项对账表**（278 条 CheckSet 检查项
  机械提取成文，`_attic/aid1-f076-f092/检查项对账表.md`——与聚合器
  同一事实源）。
- **验证覆盖缺口更正（如实入账）**：v1 批隔离舱漏挂 crumbsbar——其
  「111/111 全绿」未覆盖 crumbsbar 的 15 项且该文件从未被编译；v2 补
  挂后首次真跑暴露基座自检 7/10 红（检查索引/断言/取样时序）+ 悬垂借
  用，全部修复。批次缺陷账本 9 条全闭合（实现侧 2 + 验证缺口 1 + 检查
  侧 6），对账与复现口令见 `_attic/aid1-f076-f092/行数对账与缺陷账本.md`
  §v2，批次报告见 `docs/AI-D1-完成报告.md` §9-10。

## [Unreleased] — Varix STAR I · AI-C1 应用兼容域·前段（F001-F020 深化批次一）

**kernel/varix/src/compatstar/**（AI-C1 泳道二应用兼容前段深化，主册 A-5 节
G-A-01~G-A-20【设计细节】条款级扩展；承接已提交批次 a136dde5 判据实装层，
批次横跨两会话接续收口）：

- **兼容面深化十九域**：装载审计环 + 60s 合并记账（F001）、导入目录遍历 +
  **名字哈希 bit63 编码互斥不变量**（F002/F003）、星图替代品预填 + 拒绝缓存
  落盘自愈模型（F004）、窗口样式位表 + 高频消息面 40→61（F005）、**ROP3 真
  值表核**（16 码 wingdi 全对账）+ DC 槽位复用 + 五原语提交面（F006）、Pen/
  DashStyle + 线性/径向渐变几何参数化 + 裁剪取样核 + 灰度 AA 模型（F007）、
  **HSV↔RGB 双向绑定**（修复三角波公式 30° 色相偏移）+ HSL/hex 输入面 +
  CHOOSEFONT 选择模型（F008）、快照导出保护（F009）、审计环纪律（F010）、
  env 配置落盘模型 + PATH 双向重组 + 非法名定位（F011）、VT 扩展解析
  （F012）、LinkFlags 全 18 位 + 热键解码 + 图标缓存键（F013）、
  **dpiAwareness 元素面**（修复 `<dpiAware` 前缀碰撞误判）+ 组图标解析 +
  版本全字段（F014）、回退链展示面 + 码页-语言协商（F015）、LOGFONT 语义
  （F016）、格式号钉值 + 私有格式注册（F017）、QueryContinue 键位语义
  （F018）、进程族脏账语义（F019）、vdmp 版本校验（F020）。
- **no_std 档位收口（基线修复）**：compatstar 全域裸 `vec!`/`Vec`/`String`/
  `to_string` 在主仓非测试档报 79 个编译错误（批次一继承缺陷，`cargo test`
  档 std 遮蔽从未暴露）——按 istar 先例补 `alloc::` 导入 + 28 处 `alloc::vec!`
  全限定，收口后 `cargo check -p varix --lib` compatstar 错误 = **0**。
- 自检判据逐条钉死：**213 宿主测试全绿**（隔离壳 `_attic/aic1-verify`，
  `RUST_MIN_STACK=33554432`；批次起点 157 → +56）；施工缺陷账本 **11 项全数
  闭合**（含 xml 解析器 continue 不推进 search 死循环、HSL 除零、DC 表只涨
  不回收 10 万循环必爆——详见 `_attic/aic1-f001-f020/行数对账与缺陷账本.md`）；
  纯功能 11,495 / 上限 88,335 ≈ 13.0%（K1/K2/C1 先例口径，诚实登记）。

## [Unreleased] — Varix STAR I · AI-S1 安全加固域·前段深化批次（F171-F185 · 检查项对账层）

**kernel/varix/src/secstar/ 十五模块全量纵深 + 深化自检聚合**（2026-09-26 第二批）：

- **域 8,286 → 11,066 行（30.1%）**，15 模块逐项按主册【设计细节】补真实功能面：F171 limine.conf 解析器+环帧角度图集+圆角软点阵+资产分项账；F172 kinfo 三套件名册（9/11/10 项身份表）+项级位图+开发态文字全输出+F053 时间线节点导出；F173 帮助篇映射表+dump 包头（VXDP+校验和）+跨重启持久帧（连续计数=防循环数据面）；F174 配置指纹+F127 子集四节区打包帧+快照编号持久帧；F175 遮罩中央卡模型+诊断导出行（人话四路径）；F176 页粒度护栏映射+0xDD 毒页模型+F132 差异表豁免导出；F177 规则名解析链+F194 级联链哈希+升档引导载荷；F178 证书链三态裁决模型（fail-closed）+信任列表持久帧+任务栏同态；F179 六能力枚举语义化+审计段导出行+收回时间序；F180 开放 JSON 归档（F128 键序稳定）+8 周分布面+F061 错峰检查器；F181 三查探测引擎+实型注入口适配+面板渲染模型+F039 共用实型入口；F182 F187 状态行三态文案+HH:MM 格式化+v1.1 校验和扩展帧+置信度信任分级；F183 SMART 读取 trait+三层口径折叠说明（不许混装机器面）+月频备份提醒裁决；F184 插拔审计环（时序回放）+toast 浮层完整出路+卷元数据；F185 挂载源 trait（B-705）+「拷到 VARIX 区」动作队列+提示条替换语义。
- **检查项对账 183 → 344 条**：每模块增配 `run_*_deep_checks()`（深化层 161 条——主册【设计细节】子句逐项实算），`run_secstar_deep_checks()` 域聚合挂进 robust.rs 体检表（主/深双聚合行）。逐模块行数与检查项对账：`docs/AI-S1-行数与检查项对账.md`。
- **测试 84/84 全绿**（隔离口径：72 单测+主/深双聚合+对账 dump）；no_std kernel-image 零错误**零警告**；深化批施工伤 14 处全即时修（Copy 数组/const 索引/切片界/假绿循环断言/恒真式/跨日取模等——详对账文件 §5）。

## [Unreleased] — Varix STAR I · AI-S1 安全加固域·前段判据实装落地（F171-F185）

**kernel/varix/src/secstar/ 十五项判据实装 + 域聚合注册**（AI-S1 泳道一前段，2026-09-26）：

- **15 模块 + mod.rs 域聚合，8,286 行**：F171 图形化引导选单（图形/文字双引擎共享同一决策状态机——等价性构造保证+20×3 轮对拍）、F172 引导自检可视化（依赖链排队解锁传播/失败红闪 120ms/进度环绑定真实项数）、F173 panic「星陨」画面（自研微型 QR 编解码器 v1-v4·EC 级 L·GF(256) RS·掩码评分，编码→解码 round-trip 全绿；两级降级/连续 panic 防循环）、F174 诊断快照键（30s 账本切片/脱敏三查/看门狗放弃路径/编号跨重启单调）、F175 崩溃隔离（帧率守恒帧账模型/遮罩 ≤500ms/参数逐字节重放/焦点移交跳过全部遮罩态）、F176 内存守卫（六类越界全捕获/4096 块上限降级抽样 1/8/豁免白名单窗外照常拦/开销 <3%）、F177 能力执法可视化（四执法点/5min 聚合/风暴升档/执法播报解耦）、F178 签名角标（三态/fail-closed/装饰层不可隐藏/截图保真）、F179 权限审计页（三源注入/两步确认红钮/收回即时执法/导出脱敏）、F180 断电演练自动化（三态定向夜计划/判定三查/双盲对拍 20 轮/连续中止升级闸）、F181 交接预检器（三查白话面板/500ms 确认停留/850ms<1s 预算对账/越权 3s 置灰全链审计/fail-closed）、F182 双域时钟同步（16B 版本化快照帧/三档漂移/10 轮零漂移对拍/回拨单调签发）、F183 存储健康（原子双槽检查点断电百次零丢失/寿命三段阈值/三层口径不混装/错误突增月频 toast）、F184 热插拔（冲刷回执唯一绿灯通路/去抖 500ms/脏标记回插自动 fsck/SHARED 双确认）、F185 只读卷保护（原因三型分诊/黄底三要素提示条 4s/「拷到 VARIX 区」台阶钮）。
- **全域零堆定长实现**：16 文件零 alloc（vec!/format! 不进 secstar），宿主 std 与 no_std `--features kernel-image` 双口径零错误零警告；自检场景化 `#[inline(never)]`（栈帧峰值=单场景，域聚合嵌套不叠加 164KB 级实例帧）。
- **测试 82/82 全绿**（隔离口径 `_attic/ais1-scratch/`——183 条域自检 + 72 单测 + 域聚合全绿断言；并行分队在建代码 316 处在途错误挡主仓全量套件，按 ais2-scratch 先例建隔离验证循环，主仓 `cargo check --lib` secstar 零错误双口径收口）。
- **测试网拦截真缺陷 40+ 处全即时修**（详 `docs/AI-S1-完成报告.md` §6 缺陷账本）：🔴 QR 生成多项式越界（v4 全灭）、🔴 焦点移交只跳当前崩溃者、🔴 遮罩时限对拍错位、🔴 依赖链缺解锁传播、🔴 导出双重推进乱码、🔴 日柱滚动方向反、🔴 在途写入误进冲刷、🔴 域聚合栈溢出 + 前任批次潜伏伤（中文 byte-string 88 处/借用冲突/`?` 误用等）。
- **接线**：`lib.rs` `pub mod secstar;` + `robust.rs` 体检表 G 安全域·前段单聚合注册（同 S2 容量纪律）；跨分队接缝全走显式参数注入口（三源账本/闸门三查/冲刷回执均不复制本体）。对账：8,286/36,725 ≈ 22.6%（诚实口径，对齐 S2 19%/K2 25% 先例）。

## [Unreleased] — Varix STAR I · AI-H2 H 域二分队深化批次一（F251-F300 · 内核引擎面）

**kernel/varix/src/h2star/ 九件新增 + 四模块纵深**（2026-09-26，骨架→器官，域 10,322 → 14,231 行）：

- **基础设施四件**：h2knob 域旋钮登记表（50 项可调参数全入册、每条带主册依据摘文、F305 导入校验 + F135 文档导出）、xlog 体验日志框架（十三章：五档结论机判/狂点·死点·浮层反复·连发四类挫败指纹/攒批双闸/隐私红线——XEvent 无内容字段为类型级证明）、h2persist 原子写底盘（红线④：三段式提交/断电三段注入/撤销位回滚/损坏自首）、h2diag 域诊断（三色分级人话页/最丑角落登记/证据清单口）。
- **深化引擎五件**：h2geo 几何布局（F276 四款贴靠任意分辨率+8px 呼吸缝+分屏线拖动 20%-80% 钳制、F286 跨屏拼接分割、F298 磁贴流式、F259/F084 网格双向换算）；h2rank 排序评级（F257 默认>评级>最近用稳定序、F274 拼音二序列混排+索引跳段、F299 三来源混合+×降权——来源枚举无第四成员即「广告位=0」的结构保证、F291 3 倍异常+归因覆盖率机检）；h2cache 三层缓存（F285 真 O(1) LRU——开址散列+侵入式双向链表，修复首批 O(n) 最丑角落；前缀失效扇出/损坏自首/字节预算 F268 收口）；h2ledger 账目引擎（哈希链篡改全检出——F037 同源；F275 两路入口同账 B-2902；F269 断点逐块校验好跳坏重传；F295 最小二乘漂移斜率+回拨提醒）；h2snap 快照序列化（F253/F266/F273/F286/F298 五类四道关封包，损坏容错降级默认态不抛错——F273 判据机制化，经 h2persist 端到端演练）。
- **既有四模块纵深**：F251 心跳超时回收（仅暂停态）+逐会话音量+Now-Playing 变迁时间线；F252 按钮几何三态（Full→Icon 收缩→溢出计数供 F495）+角标 9+ 折叠；F279 轨迹均匀弧长重采样（识别与采样节奏无关）+直线度门（弯绕归滚动）+延迟账；F289 卡住提醒 60s 节流账+页进度人话+队列位置+图标 30s 收尾窗。
- **测试 105 → 134（+29）全绿；全量回归 5042/5042 绿**（隔离副本；--skip 两处别队在途包既有损坏——genstar2 自测栈溢出与其连带 robust f475，已用 HEAD 基线复现证明非本批引入）。测试网拦截缺陷 6 条全即时修：🔴 LRU 槽位泄漏（容量慢性流失，10k 压测钉死）、🟡 账目载荷措辞污染关键词查询，另四条机械——详见 `_attic/aih2-f251-f300/行数对账与缺陷账本.md` §六-§九。

## [Unreleased] — Varix STAR I · AI-D2 v3 深化批 + 隔离验证 + 检查项对账（F093-F110）

**kernel/varix/src/stard/ 四模块深化 + src/features/desktopxp/ 体验日志 + 记事本宿主**（2026-09-26 v3）：

- **内核深化销 v1 留回炉项**：mediainfo fps 联算（stts_totals × mdhd
  timescale——诚实留白销账）、thumbeng 盒式滤波缩放器（RGBA 面积加权）、
  sketchpad 圆帽扫掠栅格化（免 sqrt 越界钳制）、photolib EXIF orientation
  字节写回（最小写 + 独立读面互验 round-trip 1/3/6/8）。
- **隔离验证**：隔离舱 D:id2-scratch cargo test **133/133 全绿**
  （+4 单测）；域聚合 17 块 CheckSet **441 项**全绿（+16 检查项）。
- **十三章体验日志落地**（d2telemetry.ts）：交互细节层 + 挫败信号自动
  标记（狂点/死点/浮层反复开关/重试风暴）+ 结论字段 + 内存环节流落盘
  （日志自身零卡顿源）+ 隐私红线（字段白名单无内容列）+ 崩溃尾段恢复；
  D2Runtime 浮层遥测接线 + 设置页挫败清单纵深件。
- **F097 记事本宿主落地**（NotepadApp）：大文件只读门 + 截断载入保护、
  查找即时计数跳转、替换单个/全部、草稿防抖+恢复、BOM/编码面、
  Ctrl+F/H/S 域内键位；vwm/开始菜单/i18n 全登记（D2 第四件工具）。
- **前端深化**：mediainfo fps TS 同构（D23 mdia 层级走样修复）、HUD
  移位档/IME 仅显中英态、遥测接线；D2 前端单测 59 → **65**（+6）。
- **对账**：检查项总表（内核 425→441 / 舱 129→133 / 前端 59→65）、
  缺陷 D23-D26（层级走样/口径修正/偏移错位/测试卫生）全修全钉——
  `_attic/aid2-f093-f110/` §6；纯功能累计 ≈12,760/17,485 = 73%。

## [Unreleased] — Varix STAR I · AI-D2 桌面体验域·后段 UI 接线批（F093-F110 · 前端功能面）

**src/features/desktopxp/ + src/system/tools/ 三件 + 设置中心新标签页**（AI-D2 泳道三后段，2026-09-26 UI 接线批，对齐 J1 深化先例）：

- **前端逻辑九模块**（内核模型面 stard 的 TS 同构移植，时钟注入可测）：
  F106 键盘提示 HUD（三态计时链/风暴合并/全屏角标降级）、F107 输入法
  浮窗（8 组合/16ms 节流/避让 20 例/密码隐藏）、F110 屏幕键盘（104 键
  等效布局/Shift⊕Caps 符号层/学习模式/小键盘码位段门/30% 可读下限）、
  F108 短语库（变量白名单 9 格式符双重校验/导入三策略/round-trip 无损）、
  F094 媒体信息（五容器头部嗅探/LRU+负缓存/悬停 800ms→命中即时）、
  F095 终端核心（CJK 宽度表/头游标环形回看/虚拟提示行钉头/分屏二叉树/
  帧账）、F096 命令面板（内置 12/双路模糊/frecency/模板变量）、F093
  缩略图缓存面（2GB LRU/三级队列/热库窗口命中率 K5 口径）。
- **D2Runtime 浮层总装**（App.tsx 挂载）：锁定键 HUD 走 `vx-d2-key`
  镜像桥（KeymapOverlays 收编漏斗——零裸 keydown，键位审计新增为零）、
  输入法浮窗即开关、屏幕键盘 Ctrl+Shift+O（Z-10 useHotkey 注册）。
- **三款新 VWM 工具窗口**：终端 2.0（标签页/分屏拖拽实时重排/虚拟滚动
  只渲染可视行/命令面板 Ctrl+Shift+P/展示壳 help·seq·cjk·export——
  真实命令链归属 F012 诚实标注）、画图件（Catmull-Rom/速度笔压/45° 吸附/
  撤销 50 步+64MB 内存账/30s 草稿/导出 1x·2x）、相册（月分组/8 档缩放/
  放映状态机 250ms 交叉淡入/EXIF orientation 直读/损坏占位/旋转分辨率
  互换）；vwm/开始菜单/窗口标题/i18n 全登记。
- **设置中心「桌面体验」标签页**（DesktopD2Tab+Panels）：六分组 16 项
  三件套全量面板 + 媒体试析台/短语管理台/命令面板试搜/缩略图对账四个
  纵深件 + 判据证据包导出 + F101/F104 冻结结构守护断言。
- **既有工具外科手术补判据**：计算器历史双目标回填（F099）、便签置顶/
  上限诚实拒绝/字号（F102）、截图取色+标注撤销（F098）、时钟中心到点
  全链横幅件（F100）；剪贴板历史维持 AI-07 后端面不重复（一处一事实）。
- **测试拦截真缺陷 5 处全修**：MP4 mvhd 偏移错位、tkhd 宽高偏移、FLAC
  STREAMINFO 位拆偏移、时长取整口径（round→floor）、Scrollback 逐出
  O(n²) 性能红线（改头游标+平行字节账+虚拟提示行）；**59 项 D2 单测 +
  全仓 3608 测试全绿**；typecheck 本批文件零错误。
- **冻结登记**：F101 天气件、F104 录音件按主册 F200 存量冻结候删不落码
  （配置面/面板面/设置页三重结构守护）。对账增补：`_attic/aid2-f093-f110/`。

## [Unreleased] — Varix STAR I · AI-V1 服务守护域·前段深化批次（F111-F130 · 随闸门补深化）

**kernel/varix/src/svstar/**（AI-V1 泳道四前段，2026-09-26 深化批次）：

- **深化欠账执行**：v2 点名三大项（F125 8.7%/F126 12.6%/F128 13.6%）
  全量补齐 + 其余十二项按主册判据补真实功能面——纯功能代码 6,705
  → **10,224 行**（44.7%，v4/v5 批次滚动），wc 全文件 10,426 → 15,603 行。
- **F125 走查制度全链**：三列映射表自动生成+人工复核、回流单六态状
  态机、维度加权热力、R8 周对账（SHA-256 指纹脱节检出）、checklist
  MD/JSON 双导出、冻结面；**F126 双读过渡引擎 + schema 五族约束校验
  （JSON Pointer 错误路径）+ CI 九路负样本回放**；**F128 删除墓碑增
  量协议 + 密钥轮换双钥窗 + 续传会话表 + 每日快照调度**。
- **F116 天文面真缺陷修正**：NOAA 赤纬弧度被当角度（冬夏无差/极夜失
  效）+ 行政时区失真（经度/15 ≠ UTC+8）——修正后日落日出与真实天文
  对齐；no_std 自研 sin/cos 11 项泰勒 + acos 二分。
- **验证**：隔离舱 21/21 块 GREEN、346 条域自检项（检查项对账在册）、130 个单测函数（舱内执行 266 passed 全绿）。
  对账与 5 条新缺陷全记录：`_attic/aiv1-f111-f130/`。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队（F401-F450 五十项全域收官）

**kernel/varix/src/uni1/**（AI-U1 泳道四一分队，2026-09-26；批次一八项语义核
+ 续建四十二项合并收官；域聚合器 `run_uni1_checks` blocks=51 经 robust.rs
单聚合注册，容量 64 内冻结）：

- **五十项判据语义核全部落地**（F401-F450，50 模块 + ubase 共享底盘）：
  系统快捷键八件（自动排列/任务管理器/Win+L/E/I/X/开始菜单/Alt+F4/
  安全屏）、键盘语义与文件导航十二件（F1 帮助/Enter 族/Backspace/
  Alt+Enter/PrintScreen/回收站双件/Win 键/磁贴/固定/右键菜单/时钟），
  指示器与输入链七件（输入法指示/音量电池浮层/Esc 层级栈/Aero Shake/
  文档键位/剪贴板键/打印），视图与列表十件（F11/锚点缩放/快捷新建/
  翻页定位/键盘右键/对话框键位/下拉框/滑杆/格式化），磁盘设备向导
  十三件（盘符挂载/驱动器加密/ISO/计划任务/还原点/蓝牙配对/打印机/
  显示器排列/分辨率回滚/声音试听/麦克风/摄像头/粘滞键筛选键）——
  每模块自带 `run_*_checks` CheckSet（预算线实测注入 + 超线诚实计数，
  异常零静默）与宿主单测。
- **双通道验证全绿**：隔离舱（#[path] 直挂真实文件，51 块）
  **123/123 绿 0 警告**；真实 crate（HEAD + 仅本队接线干净基线）
  uni1 **115/115 绿**、robust 全域聚合 **20/20 绿**、全量
  **5502/5502 绿零回归**。
- **接手即审计**：前会话中断现场 29 文件 + 8 编译错逐项闭合；两处
  测试掩盖的真逻辑缺陷修正（F430 锚点缩放数学方向反、F432 翻页语义
  自相矛盾）；F424 escstack 批次一漏接线补挂。缺陷账本 15 项全闭合
  零遗留（_attic/aiu1-f401-f450/行数对账与缺陷账本.md）。
- 行数对账（诚实口径）：五十项纯功能 6,636 行 + 底盘/聚合器 346 行
  = 域合计 6,982 行；主册工程量含界面与交互 ~60%（落桌面层施工面），
  语义核判据全绿，不注水凑数（铁律 2）。

## [Unreleased] — Varix STAR I · AI-U3 I 通用域·三分队（F501-F550 深化批次 · 前端功能面）

**src/features/u3/ + U3Tab + src/styles/u3.css**（AI-U3 泳道四三分队，2026-09-26 v2
批次；对齐 AI-J1 深化批次先例形态，v1 判据实装层（kernel ustar3 19,480 行）之上补齐
「界面组件与交互层」工程量类别的前端功能面）：

- **十大逻辑模块**（deskicons/locksec/filesec/pointerfx/explorerx/copyops/winkeys/
  sysdev/clockcal/anchor）：50 项判据的逻辑面纯函数实装——F501 感知亮度选字色
  （Rec.709 sRGB 线性化 + F297 压暗联动）、F502 两行封顶换行（英文整词不拆）、
  F503 网格三档 96/80/64 + 最近格螺旋吸附、F504 PIN 冷却翻倍表状态机（30s 起逐次
  翻倍+冷却期回退密码）、F505 蓝牙动态锁（30s±5s/10s 波动豁免）、F508 防截黑块
  几何裁剪、**F510 .vxcrypt 单文件加密（WebCrypto 真实现：PBKDF2-SHA256 600k 迭代
  + AES-256-GCM 认证加密，错误密码人话提示不泄露信息）**、F511 剪贴板全清（历史+
  当前+空串诚实失败）、F512 截图历史两态（淘汰优先临时项/关机清理）、F513 Ctrl
  定位（1s 门槛+组合键豁免 20 例）、F515 查找替换（影响数预览+整批一次撤销）、
  F524 后悔窗（5s 暂存/延寿 10s×2/真释放诚实不可恢复）、F529 空间预检（10% 缓冲
  三出路）、F531 队列化（同盘串行/异盘并行/插队）、F532 四类人话归因三问结构、
  F535-F538 快捷键族（Win+数字三态/Win+T 回绕/瞥桌面 15%/Alt+Esc Z 序节流）、
  F542 ClickLock 1.1s 状态机、F543 分设备音量（40% 首发/LRU 10 台）、F545 电量
  平滑（≤2%/步+低电 1h 节流）、**F549 农历引擎（位表与春节锚点自内核 ustar3
  同源移植，位表-锚点一致性不变量 + 五年抽检 + 中秋事实锚）**。
- **u3store 单一配置根**：50 节分节存储、订阅总线、undo 栈深 3、原子写、默认值
  预填（get 永远有值）——与 j1store 同源纪律。
- **U3Runtime 运行时总装**：Ctrl 涟漪层（F335 优先平面）、声音视觉边缘光带
  （三事件三色令牌化 2 次脉冲）、Caps 双音（WebAudio 880/988 vs 440/494）、打字
  隐藏指针（100ms 淡出/2s 恢复/移动即时）、Win+逗号瞥桌面（CSS 变量通道）、
  Ctrl+Shift+Delete 剪贴板热键、蓝牙锁心跳（真实 RSSI 源随闸门登记）。
- **U3Tab 全量面板**：50 项九组三件套齐全（F474 同源）、每项带判据样张实时数据
  （五档亮度选字/冷却翻倍表/命名示例/Tooltip 农历样张）、F550 组内嵌九域自检
  引擎实时执行（锚点可执行性证据在面板可见）。
- **验证**：u3.spec 80/80 全绿（含 F510 WebCrypto round-trip/错误密码、F549 农历
  五年抽检、F550 九域自检全绿基线三重锚）；tsc 本域 0 错误；宿主全量回归中
  desktopxp 13 失败为 AI-D2 untracked 在制品（本包零卷入）；行数对账：v2 前端
  纯功能 2,566 行（非空非注释）+ v1 实装层 19,480 行 = 累计 22,046 / 46,540
  （47.4%，如实呈报；证据 _attic/u3-v2-frontend-test.log、_attic/u3-v2-tsc.log）。

## [Unreleased] — Varix STAR I · AI-J1 J 鼠标域·一分队（F601-F620 深化批次三 · 跨窗口运行时接线）

**src/features/mouse/ + MouseJ1Tab|MouseJ1Panels + 八窗口接线**（AI-J1 泳道三一分队，
2026-09-26 v3 批次；对齐 H4 批次二先例形态，v1/v2 判据逻辑层之上补齐跨窗口运行时）：

- **窗口无关运行时内核**（windowRuntime.ts，611 行）：整条指针/滚轮/侧键/手势/
  自动滚管线从桌面窗 React 组件解耦——desktop 全量层（副本/锚标/墨迹渲染）+
  app-write/mind/code/fate 锚标墨迹层 + explorer/taskbar/datavault headless，
  八窗口共用同一内核；documentElement 声明 data-app-id/data-app-class（F605 应用
  覆盖/F616 应用档案/F615 侧键作用域的真实挂点——此前全系统无人声明，解析链
  永远落默认档）。
- **动作路由中心**（actions.ts，215 行）：vx-j1-action 从「派发后零消费者」变为
  17 内置动作登记表（12 手势+5 侧键）+ 应用>全局两级路由 + 开放扩展点
  （registerActionHandler 退订即撤销、handler 抛错隔离）+ 未处理显性化（遥测
  no-feedback + 桌面窗每动作一次性提示）；Tauri 窗口管理（最小化/最大化切换）、
  编辑三兄弟、声明式 nav.up 内建接线；shortcut:/launch: 别名事件化（F244 快捷键
  路由与启动器的消费面）。
- **判据内真实补全**：F616 生效参数进管线（应用档案从此真实改变指针手感）、
  F614 首交互建档+气泡、F606 真实 deltaX 倾斜路径、F604 滚轮打断退出、
  F607/F613 Tauri 多屏缓存刷新（物理像素虚拟桌面坐标）、F610 tooltip 独立四档
  修正（基线 500ms 在档，v2 遗留缺陷）+ --hover-delay 偏离接管通道、F602 blur
  修饰键复位、deltaMode 归一化、F609 声明容器两处（设置弹窗滚动体/explorer
  文件列表）、F617 画中实时墨迹、v2 监听器逐事件重挂副作用修复。
- **验证**：本域单测 119/119 全绿（新增 j1v3.spec 33 项）；「运行时接线审计」
  面板（设置中心 → 鼠标）如实呈现八窗挂载身份/动作路由表/F609 容器/F610 变量
  通道；行数对账 4,844 / 15,660（30.9%，同一过滤口径，计数脚本归档
  _attic/aij1-f601-f620/）。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 深化批次二 · 界面壳层+运行时接线）

**src/features/h4/ + src/features/settings/H4Tab|H4Panels + src/styles/h4.css**（AI-H4
泳道三四分队，2026-09-26 v2 批次；批次一 5,637 行判据引擎层之上补齐主册
「界面组件与交互层」工程量类别——对齐 AI-J1 深化批次二先例形态）：

- **运行时状态机**（h4ui.ts，158 行，纯逻辑可测零 DOM 依赖）：开机序列编排
  （F371 recordBoot 进同源账本 + F399 countBoot/第 100 次彩蛋一生一次 +
  同会话去重守卫——StrictMode 双挂载不重复记账）、滤镜应用计划（F387
  四态→attr+CSS 单点翻译，全系统唯一滤镜位的落地面）、阅读模式三参数→
  CSS 变量换算（F386 CJK/ASCII 混合估宽）、徽标时序类（300/3000/500ms
  逐段映射）；vx-h4-filter/reading CustomEvent 广播通道（开放性扩展点）。
- **H4Runtime 接线薄壳**（106 行，App.tsx 挂载）：挂载即开机序列一次、
  开机徽标浮层按 BADGE_TIMING 真实时序淡入停留淡出、彩蛋①命中 toast
  呈现（彩蛋是情感不是门）、documentElement 单点应用滤镜与阅读变量、
  广播事件+focusin 跟随。
- **设置中心「效率与工具」标签页**（H4Tab 624 行 + H4Panels 1,119 行，
  h4tools 注册进 SettingsModal）：十个分组覆盖 F351-F400 全部 50 项——
  每行三件套、每读数即引擎输出、每按钮即引擎调用；16 个纵深交互面板：
  拾色器台（20 色板零误差对拍+最近 8 色+连续模式）、画中画台（进/排/
  停/档/退全链）、录屏台（三模式+分节账本+断电恢复演练+开销预算表）、
  下载整理台（方案预览+勾选执行+幂等自证）、任务中心、存储热点图
  （squarified SVG 可下钻+面积 ±2% 对账）、清理收口（四来源对账+回收率
  ≥90% 实证）、备份与演练台（增量链+验过能还原+零副作用演练）、菜单
  矩阵（4×6 置灰全格）、速查浮层（600ms 按住 Win 仿真）、人话时间线
  （哈希链+展开详情）、查词翻译台（离线词典+生词本+长短分界）、语言
  热切（覆盖率+回退标注+状态保持）、工作区快照（恢复计划+启动队列）、
  网页协同（待遇清单+PWA 全链+不越界审计）、彩蛋谱（三枚触发+不藏功能
  审计）、域门禁（三走查+发布判定+三处同源）。
- **样式层**（h4.css 615 行）：F151 令牌全走查（零硬编码，回退仅兜底）、
  4px 栅格高密度纵深、判定徽章绿/红语义带 ✓/✗ 形状冗余（F387 灰度下
  可辨）、开机徽标 BADGE_TIMING 真实过渡、滤镜单点与阅读变量的 CSS 侧
  消费契约。
- **深测**（h4deep.spec.ts 178 行 12 用例）：开机去重/seq 连续/彩蛋一次性
  （跨会话持久化）/徽标可关仍记账/时序边界值/滤镜四态映射+零残留/
  互斥引擎联动/阅读变量换算+每应用记忆——纯逻辑注入式，零 DOM 依赖。
- **验证**：H4 全域 **323/323 全绿**（引擎 311 + v2 深测 12）；全仓 tsc
  仅剩先于本批的 2 处非本队任务面错误（DesktopD2Panels，D2 批次归属）；
  纯功能行数 5,637→8,259（达成率 12.4%→18.2%，如实登记不注水；逐项
  对账见 `_attic/aih4-f351-f400/行数对账与缺陷账本.md` §1-§3）。

## [Unreleased] — Varix STAR I · AI-E1 个性化域（F151-F170 深化批次二 · 引擎层+全量接线）

**src/system/persona/**（AI-E1 泳道三 E 域，2026-09-26 回炉补深化收口；
v1 二十项判据实装之上并入深化引擎层与二十页全量接线）：

- **深化引擎十三件**（*-engine.ts，2,320 行，纯逻辑零 UI 依赖）：
  theme-engine（300ms 双层交叉过渡编排——亮度守恒无白屏帧+中段原子替换恰好一次、
  令牌 diff、AA 对比度自修（保持色相只调明度轴）、语义色硬编码扫描器（B-1104
  执法）、vtheme 双向桥）；wallpaper-engine（官方池下载队列——空闲窗口调度/
  三次失败静默回退、缓存 LRU、次日预载计划（换的时刻零等待）、多屏独立池、
  EXIF 方向解析）；icon-engine（缓存键含包版本 LRU、失效广播总线（订阅制不轮询）、
  SVG 资产安全审计（脚本注入向量全拒）、覆盖表版本迁移）；pointer-engine（帧播放头、
  DPR 感知渲染规划（sprite 1x/2x 选档+热点物理坐标精确）、vxpointer 序列化
  round-trip+校验和、.cur/.ani 互通评估与魔数嗅探）；audio-engine（六事件合成
  试听（零素材兜底）、事件→声音解析链、80ms 防爆音音量 ramp）；layout-engine
  （开始菜单几何计算器+预设缩略规格、任务栏几何（图标位/折叠线/托盘起点）、
  自动隐藏五态状态机（迟滞去抖+焦点钉住））；motion-monitor（帧时采样 P95/P99、
  档位自动建议（冷却防振荡）、超预算降级执法、WP-207 进度数字、预演诚实提示）；
  archive-engine（包签名链（HMAC 风格防篡改可验证）、分节校验器、V0→V1 迁移、
  每应用例外锁定令牌派生）；boot-engine（锁屏状态机（失败节流 5 次/15s）、通知
  隐私摘要、密码聚焦让位变换、确定性种子粒子模拟（mulberry32 同种子同画面
  ——预览即真话）、四幕烘帧清单）；font-engine（sfnt name 表解析（双语族名）、
  永不落空回退链、批量扫描协议（500 字/批部分结论先行））；ime-menu-engine
  （候选窗布局引擎（分页/几何/9 档翻页提示）、右键菜单总装器（二级收纳+
  系统组保序+F072 排序））；shortcut-engine（KeyboardEvent 语法解析（Esc 取消/
  单键拒绝/规范序归一）、冲突三级判定（应用级诚实指路应用内改）、vxkeymap v1
  导出导入、证据归档器）；widgets-engine（8px 吸附网格+碰撞推开+越界回收、
  更新调度（15s 抖动防同帧齐刷））。
- **二十页全量接线**（pages-*.tsx + PersonaStudio，增量 ≈1,750 行）：引擎能力
  全部落成可操作界面——AA 审计一键采纳、硬编码扫描器、应用前 diff 摘要、
  过渡帧演示、下载管线面板、失效广播事件流、DPR 渲染规划、解析链面板、
  布局几何实算、回退链、批量扫描、性能监控（注入演示）、导出签名信封+导入
  验签（篡改拒绝）、摆放引擎、锁屏状态机演示、确定性粒子 canvas 四幕重播、
  候选窗实算试打框、菜单总装预览、任务栏几何与状态机、事件语法解析重录+
  全表冲突审计+键位迁移、证据包导出（完整性门禁）。
- **类型修复**：深化批测试 6 处 TS 错误清零（未用导入 ×2、字面量窄化 ×4）。
- **验证**：E 域单测 **176/176 全绿**（v1 135 + 引擎批 41）；typecheck 本域
  零错误；`vite build` 通过；walkcheck 自检 SELFTEST PASS；行数对账 5,620→
  9,687 纯功能行（达成率 27%→46.6%，如实登记不注水；深化批次三建议拆单见
  `docs/AI-E1-完成报告.md` §4）。

## [Unreleased] — Varix STAR I · AI-V2 生态开放域·后段（F131-F150 深化批次一）

**kernel/varix/src/stareco/deep/**（AI-V2 泳道四深化层，2026-09-26 回炉补深化；
`run_stareco_deep_checks` 20 子块单聚合，随域聚合器扩容 blocks=21→22 入总账）：

- **基础批次条目补登**（原提交 2727fc6 的 CHANGELOG 段在多会话并发中遗失，
  本段补回）：`stareco/` 21 模块（ebase 共享底盘 + F131-F150 逐项一模块），
  306 CheckSet 检查项逐条钉死，隔离舱 35/35，committed 树 `cargo test
  stareco` 28/28、全量 ktest 3,963/0 零回归；行数对账与随闸门补测登记见
  `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。
- **深化层二十项**（f131d~f150d 逐项一文件，3,890 行）：F131 PR 拆分纪律 +
  commit 四条 lint + 升级窗冲突处置 + 90 天 fork 公告三件 + 公开页数据源；
  F132 影响级别量化判定（致命/降级/无感三路单函数裁决）+ 季度刷新引擎 +
  星卡反向链接；F133 重采样降级建议链 + 三态缺失迁移面；F134 导出表渲染 +
  排序器；F135 跨区倒排搜索（F071 接入面）+ 轻量着色器 + 版本史单调性 +
  覆盖报告渲染；F136 断绿归因四分类 + 新手实录质检；F137 签名指纹漂移审计
  + 迁移窗倒计时；F138 iCal 周界扩展 + 假日补偿；F139 相似聚类阈值面 +
  撞车提示；F140 上下文注释 lint + 占位符参数序校验；F141 焦点环令牌联动 +
  形状冗余判定矩阵；F142 披露窗倒计时 + 致谢页排序器；F143 资产清单 4K
  原生线校验；F144 徽标验真链 + 复检日历；F145 配额账本 + 抽查复核台账;
  F146 源健康 6h 节拍 + 回退链审计；F147 会话进度条模型 + 20 档自适应
  复验；F148 三人组构成校验 + 申诉时限钟；F149 指标环比引擎 + 趋势窗口;
  F150 基线代次 + 卡壳三分类回归面。
- **验证**：深化自检 176 检查项全绿（deep 聚合 blocks=20）；隔离舱 35→54
  （+19 deep 单测）零警告；施工期捕获并修复 2 处 no_std 宏导入缺陷（上批
  「隔离舱 std 掩盖 vec! 导入」教训的复发面，功能代码两处、committed 树
  复证通过）；行数对账 6,708→10,642（达成率 19.3%→30.7%，如实登记不注水）。

## [Unreleased] — Varix STAR I · AI-J2 J 鼠标域·二分队（F621-F640 二十项落地）

**kernel/varix/src/jstar2/**（AI-J2 泳道三 J 域后段，主册第 8 部分 J-B/J-C/J-D
组；域聚合器 `run_jstar2_checks` blocks=21 经 robust.rs 单聚合注册）：

- **J-B 鼠标自定义四件**（clickanim/wheelsnd/vtheme/dashbrd）：指针点击动效
  三档（0.94/120ms/8px/200ms、优先平面脏区零重绘、F350 对齐 + F594 独立
  开关）、滚轮交互音效（逐档 1:1、40% 两音色、E5 交互音类目、勿扰联动）、
  鼠标档案入 vxtheme（四件打包往返逐字节一致 + 三例合法性校验降级清单 +
  回退链）、鼠标设置总览仪表盘（四信息/四直达/同源对账/一键恢复确认门）。
- **J-C 自定义指针设计八件**（workshop/recolor/checker/library/themeclr/
  sharing/a11ytmpl/audit）：绘制工坊（三笔刷/双倍率同屏对拍/洋葱皮 30%/16
  帧纪律/撤销栈）、颜色重染（HSL 定点三滑杆 + 强调色众数对齐 + ΔE<1 帧同
  步 + 非破坏副本）、语义检查器（四检四修 + 三态回退链 + 修复可撤销）、方
  案库（50 上限提醒不静默删/三视图/动态缩略墙/E4 投影对账）、主题派生（描
  边 ≥3:1 闭环/令牌同源/派生可再编辑）、分享链路（.vxcur 四链 + 体检前置
  + 签名黄条 + peblock 先于解析 + F133 子集声明）、无障碍三模板（≥4.5:1/
  8×8/2px 免检映射）、渲染保真审计（24 组合走查 + 标红阈值文档化 + 一键
  重生成 2x + 内置基线先过审）。
- **J-D 第三方形状与兼容八件**（curimport/svgimport/sideload/dpicontract/
  hotspotfix/winbridge/gate/ledger）：.cur/.ani 真实二进制解析（五档位深 +
  RIFF 帧序帧率双保真 + **100 样本库逐像素对拍** + 诚实定位报错）、SVG 直
  用（矢量管线 + 双层热点标记 + 热点向导）、侧载链（不静默替换 + 卸载确认
  回退 + 双入口同库）、DPI 契约（原生优先→矢量派生→Lanczos 兜底 + 增强
  渲染诚实标注 + 缓存零开销）、热区补偿（三例检测 + ≤2px 对拍 + 非破坏补
  偿层）、Windows 迁移桥（.reg 双编码 Win10/11 等价 + 逐态 F633 保真 + 空
  清单诚实）、异形安全闸（三闸 + 熔断 O(1) 回默认 + 三振拉黑 + 100 样本
  零误拦 + 管理员只许收紧）、收官台账（640 检查点程序化注册 + 兼容台账
  四级评级 + 季度审视 + 五处边界文档 + v1.0 冻结清单联动）。
- 自检判据逐条钉死：**230 CheckSet 检查项 + 122 宿主单测全绿零警告**
  （隔离舱 `#[path]` 直挂实测，122/0/0）；施工期缺陷账本 **14 项全数闭合**
  （含 nth_root 牛顿公式错致隔离舱挂 15 分钟 CPU、多边形斜边子像素截断
  坍缩、AND 掩码位序双取反三处高危）——对账与复现口令见
  `_attic/aij2-f621-f640/行数对账与缺陷账本.md`，批次报告见
  `docs/AI-J2-完成报告.md`。

## [Unreleased] — Varix STAR I · AI-D2 桌面体验域·后段（F093-F110 十六项落地）

**kernel/varix/src/stard/**（AI-D2 泳道三桌面体验后段，主册《Varix STAR I
start.md》G-C-23~G-C-40 判据实装层）：16 项功能模块 + mod.rs 域聚合
（blocks=17 含冻结登记块），**wc -l 10,240 行 · 域自检 425 条 · 单元测试
115 项，隔离舱 129/129 全绿零警告**。复用 star::sbase 与 galaxy::math，
零复制底盘。

- **F093 缩略图引擎**：三级优先队列 + 2GB LRU + 损坏即弃自愈 + EXIF 内嵌直抽。
- **F094 媒体信息悬浮**：MP4/EBML/FLAC/MP3 五容器手写解析（stsz 轨字节实算）+ 缓存 <100ms 双路悬浮。
- **F095 终端 2.0**：CJK 宽度表混排对齐 + 10 万行环形回看（截断提示钉头）+ 分屏树拖拽重排。
- **F096 终端命令面板**：内置 12 条全可达 + 子序列/首字母双路模糊 + 收藏 frecency + 模板变量。
- **F097 记事本编辑器**：编码/行尾检测 + 原子写断电百次零损坏 + 极简正则 + 草稿崩溃恢复。
- **F098 截图工具**：三模式 + 放大镜像索取色对拍 + 马赛克两档 + 标注撤销 20 步 + 序号删中重排。
- **F099 计算器**：128 位定点 + 256 位中间积（0.1+0.2=0.3 精确）+ 60 例判据 + Windows 百分号三语境。
- **F100 时钟套件**：civil 算法 12 城全对 + 倒计时到点三件全链 + 闹钟重复规则全周矩阵 + 唤醒源白名单。
- **F102 便签**：序列化 round-trip 全字段恢复 + 防抖草稿断电零丢失 + 置顶 z 序恒前。
- **F103 画图件**：Catmull-Rom 平滑 + 速度笔压 + 撤销栈 50 步/64MB 双上限 + 45° 吸附。
- **F105 相册**：月分组时间流 + 缩放焦点几何 + 放映状态机 + EXIF orientation 写回对拍。
- **F106 键盘 HUD**：三键三态计时链 + 风暴合并不闪屏 + 色弱形状冗余 + 角标降级。
- **F107 输入法浮窗**：三态 8 组合 + 16ms 节流 + 避让 20 例 + 密码框隐私隐藏。
- **F108 短语库**：变量白名单 9 格式符 + 导入三策略 + 候选混排徽标 + round-trip 无损。
- **F109 剪贴板历史**：三型 20 条驱逐（钉选除外）+ B-3901 总闸 + 敏感拒入 + 字节位高亮。
- **F110 屏幕键盘**：104 键等效布局 + 符号层单发弹起 + 学习模式同步 + 多点并发 + 30% 可读下限。
- **冻结登记**：F101 天气件、F104 录音件按主册 F200 存量冻结候删纪律不落码，结构面守护在册。

行数对账（15 项低于主册上限 90%，如实登记留 UI 接线批回炉）与 16 处实现
侧真缺陷 + 9 处判据口径修正账见 `_attic/aid2-f093-f110/`。完成报告见
`docs/AI-D2-完成报告.md`。

## [Unreleased] — Varix STAR I · AI-V1 服务守护域·前段（F111-F130 二十项落地）

> 收口说明：本批 23 个交付文件（svstar 22 模块 + 完成报告 + 缺陷账本）
> 因多会话并发提交，内容已随 `974a8d2a`（D1 changelog 修复批）入库；
> 本条目为 V1 批次在 CHANGELOG 的正式登记（前一版条目在 CHANGELOG
> 并发合并中遗失，此处补回）。

**kernel/varix/src/svstar/**（AI-V1 泳道四，主册体验域 G-C-41~55 + 生态
开放域 G-D-01~05 判据实装层；vbase 共享底盘 + 二十项功能模块 + mod.rs
域聚合器（21 块 227 检查项）+ 隔离舱 varix-svcabin（#[path] 直挂真实验
证，182 单测 0 红））：

- **辅助五件**（magnifier/narrator/highcontrast/colorfilter/nightlight）：
  放大镜 2x-16x（<8x 最近邻/≥8x 双线性/80fps 预算/三跟随/边缘平移三档/
  16x 网格/截图所见即所得）；讲述人（两场景 100% 可读名门禁/打断队列/
  中文语境化读法/静默停用报备/进程隔离声明）；高对比双主题（WCAG 全表
  ≥7:1 实测/违例描边兜底/2px-3px-12px 规格）；色弱三滤镜（Machado 2009
  模拟矩阵对拍/daltonize 误差重分配校正/0.5ms 预算降档/混淆对分离度
  ≥+50%）；夜间模式（锚点曲线 24h 扫描/强度三档/±200K 门/伽马诚实降级/
  300ms 深色交叉）。
- **引导两件**（oobe/welcome）：五步向导（步进式提交/断电续走/双路径/
  汇总准确/网络失败跳过）；欢迎五卡（三大心智/直跳注册制/不二弹/可重置/
  插画降级）。
- **求助三件**（helpcenter/diagcenter/restorept）：MD 渲染器（四形态全支持/
  直跳标记/死链回落/50 页对拍）；诊断中心（四体检灯 F062 同源/三修复项
  注册制回滚全链/脱敏三查分卷导出/时间线连续性）；系统还原点（四触发/
  点级哈希锁定/滚动 10+锁 1/回滚重试+二次回滚/断电百次）。
- **系统三件**（updateux/aboutpage/motioncore）：更新全状态机（断点续传/
  预约关机装/校验前可取消/进度环绑真实阶段/失败页三要素）；关于本机
  （Y7000 锚点直拍/诚实留白/复制保真）；动画总谱（五曲线+三时长/Bezier
  求值器/强度三档缩放/未登记曲线门禁/帧预算降级直线）。
- **开放六件**（walkcheck/openformat/vxapp/stardata/casesub/ossreg）：
  体验域总判据（55 项映射覆盖率 100%/证据三件套强制/回流审批/20 维度
  权值）；开放格式宪法页（四规范 schema 校验器/双读条款 90 天）；vxapp
  打包工具（五形态全绿/双计哈希/签名-验签双向/退出码规范/打包缓存/
  交互向导）；星图开放数据面（JSON 快照/增量 since 协议/签名双向/90 天
  快照/镜像指南）；社区判例提交线（五态时间戳/两人复核硬门/证据哈希链/
  24h 限频）；开源项目登记册（12 字段 schema/CI 双向零 diff/GPL 隔离
  必标/法律面留痕）。
- **共享底盘 vbase**：SHA-256（FIPS 向量对拍）/WCAG 对比度/色温换算/
  semver/JSON 写出器/脱敏三查/证据哈希链——全域唯一算法源。
- 判据唯一源：docs/Varix STAR I start.md G-C-41~55 / G-D-01~05；行数
  对账与缺陷账本（17 项实录）见 _attic/aiv1-f111-f130/；完成报告见
  docs/AI-V1-完成报告.md。

## [Unreleased] — Varix STAR I · AI-D1 桌面体验域·前段（F076-F092 十七项落地）

**kernel/varix/src/deskstar/**（AI-D1 泳道三桌面体验前段，主册《Varix STAR I
start.md》G-C-06~G-C-22 判据实装层——泳道三首支进场分队）：17 项功能模块
+ dbase 共享底盘（缓动曲线族/浮层生命周期五出路状态机/防抖合并器/滑动均
速/焦点环/几何与预算口径）+ mod.rs 域聚合，**wc -l 11,265 行 · 域自检
163 条 · 单元测试 93 项，隔离舱 111/111 全绿、deskstar 面零警告**。

- **F076 快速设置**：六卡聚合 + Wi-Fi 三态 + 飞行聚合射频 + 全键盘 + 弹出 ≤100ms。
- **F077 通知中心**：toast 5s 沉历史 + 风暴合并 + 免打扰跨零点 + 高优穿透 + F072 同源时间。
- **F078 日历飞出**：格里高利历纯算法，12 月网格全对含 2028 闰二月。
- **F079 全局音效**：六事件 + 静音总闸 100% + 80ms 排队 + -18LUFS 归一。
- **F080 窗口吸附**：八落点无缝 + 过冲 105%→100% + 键鼠双路一致 + 三分屏推荐。
- **F081 任务视图**：虚拟桌面 + 跨桌焦点 + 关闭并入撤销 + 全键盘。
- **F082 Alt+Tab**：快速/停留双态 + 蛇形十二窗不乱 + 假死灰显仍可选。
- **F083 桌面刷新**：诚实重载 + 80ms 微闪 + 300ms 合并 + 增量 <5ms。
- **F084 图标网格**：双模式 + 24px 吸附 + 波浪换位 + 框选 20 刚体。
- **F085 回收站**：零真删哈希对拍 100 轮 + 已满三选 + 撤销 5s。
- **F086 复制进度**：3 并行队列 + UI 节流隔离 + 盘满三选。
- **F087 冲突面板**：后缀引擎 100 例 + 二次确认 + 操作级 undo。
- **F088 搜索即输即显**：防抖零扫描 + 高亮混排 100% + F071 声明式拼音档。
- **F089 标签页资源管理器**：重启全恢复 + 历史栈 20 步引擎（F090 共用）。
- **F090 面包屑**：兄弟下拉 + 投放移动 + 补全 <50ms + 零残留。
- **F091 详情窗格**：真 EXIF 解析器（TIFF IFD 五机型全对）+ 诚实留白。
- **F092 压缩/解压**：真 ZIP 实体（CRC32 + 完整 inflate + deflate 三档 +
  ZipCrypto 只读 + F176 穿越拦截 + 损坏抢救 N/M + round-trip 50 例）。

行数对账（14 项低于主册上限 90%，如实登记留 UI 接线批回炉）与 12 处实现
侧真缺陷账见 `_attic/aid1-f076-f092/`。

## [Unreleased] — Varix STAR I · AI-U3 I 通用域·三分队（F501-F550 五十项落地）

**kernel/varix/src/ustar3/**（AI-U3 泳道四，主册 I 通用域 F501~F550 判据实装层；
robust.rs 域函数指针表 274→324，50 域直排注册接线）：

- **F501-F503 桌面图标三件**（deskicons）：五档壁纸×两字色自动选字 +
  投影 400‰/2px + 选中底衬 + 4K DPI 缩放精度 + F297 压暗联动；两行/8 全角
  封顶换行引擎（中英混排词界回退 + 省略号三路全文可达）；96/80/64 三档
  密度 + 8px 步进自定义 + 最近格吸附重排（邻居序不变）。
- **F504-F508 安全锁屏五件**（locksec）：PIN 4-6 位 + 5 次冷却 30s 翻倍表 +
  1.5s 解锁时序预算；蓝牙动态锁 30s±5s 触发 + <10s 波动不锁 + F316 双保险；
  访客沙盒四闸 + 残留清理账 + 2h 时限提示；锁屏防截三路全拒（Policy 文档化）；
  应用防截标记黑块精确到窗口几何。
- **F509-F512 文件安全四件**（filesec）：粉碎折叠入口 + 三重警示 + 覆写/
  诚实标注两分支；.vxcrypt 三链路（加密/临时视图阅后即焚/导出显式门）+
  篡改必拦；剪贴板双入口全清 + 密码后 5s 提示；截图历史 20 条环 + 临时/
  已保存两态 + 关机清理通知一次。
- **F513/F514/F519/F520/F522/F523 指针与提示六件**（pointerfx）：Ctrl 1s
  涟漪三轮 + 20 例组合键豁免 0 误触；声音视觉孪生三色 + 8px 边缘 2 次脉冲 +
  勿扰镜像；Caps 双音色默认关；标题栏中键最小化三义分流矩阵；指针轨迹
  200/400/600ms 三档优先平面；打字 100ms 淡出/2s 恢复/30% 透明。
- **F515/F517/F521/F525-F528 资源管理器七件**（explorerx）：查找替换整批
  一次回滚 + 影响数预览；启动页三模式 + 上次文件夹记忆；截图落点三选 +
  命名规则；速查卡双格式同源导出分色标注；状态栏 24px 三段同源；导航树
  折叠持久化 + 万节点虚拟化；树列表双向同步 + 焦点/高亮分离。
- **F524/F529-F534 后悔窗与复制链七件**（copyops）：回收站 5s 撤销窗 +
  10s 延寿 + 真释放账目同步；复制前 10% 缓冲预检三选出路；复制后 FNV 校验
  （>1GB 自动开）+ 空闲 IO + 补发时序；复制队列同盘串行/异盘并行 + 插队 +
  断点续传衔接；打开失败四类人话归因（10 魔数签名表 + 12 注入用例）+
  三问出路；只读介质前置提醒一次不重复；600 字符长路径全操作 + 保尾显示。
- **F516/F518/F535-F539/F548 窗口与快捷键八件**（winkeys）：横幅三档位置 +
  堆叠自适应 + OSD 独立；输入法四方案 + Caps 600ms 双用分界 + 旧键让出；
  Win+数字十键映射三态 + Shift 变体；Win+T 五链路键盘遍历；Win+逗号瞥桌面
  （15%±3%/120ms±20ms/<1px 恢复）；Alt+Esc Z 序盲切；桌面布局锁定温和拒绝；
  任务管理器置顶不抢焦点 + 重启不记忆。
- **F540-F547 系统与设备八件**（sysdev）：内存诊断两遍标准 + 报告三要素；
  网络重置清单完整 + 90s 无整机重启 + 重配向导；ClickLock 1.1s 抓起；
  分设备音量 10 台 LRU 记忆 + 40% 首发；通知音量独立默认 50 + 总闸优先；
  蓝牙电量三处同源 + 低电小时级节流 + 无电量诚实态；设备接入三态横幅 +
  2s 就绪；音量平衡分设备记忆 + 软件平衡 <1ms。
- **F549-F550 收官两件**（clockcal/anchor）：时钟悬停三要素 + 农历离线引擎
  （2025-2030 位表 + 春节锚点 + 逐年步进一致性不变量；主册示例「八月初五」
  实为八月十五中秋，偏差已登记）；批次六 25 检查点锚点全绿入总检。
- 自检判据逐条钉死：50 域 **616 CheckSet 检查项 + 307 宿主单测全绿**；全量
  **4204/4204 PASS**（debug 通道）；f475 全域 324 域断言 PASS。实装层交付
  19,480 行，接线随闸门登记（K1/K2 同口径）；偏差与施工实缺陷修复见
  `docs/AI-U3-完成报告.md`。
## [Unreleased] — Varix STAR I · AI-H2 H 基础通用域·二分队（F251-F300 五十项落地）
=======
## [Unreleased] — Varix STAR I · AI-C1 应用兼容域·前段（F001-F020 二十项落地）

**kernel/varix/src/compatstar/**（AI-C1 泳道二，主册 A-5 深化设计报告 G-A-01~G-A-20
判据实装层，21 文件 11,622 行，20 域 CheckSet 自检 + 157 项宿主测试全绿；
完成报告见 `docs/AI-C1-完成报告.md`）：

- **F001/F002 装载底盘两件**（dblrun/peblend）：无感双击管线五态状态机
  （嗅探→peblock→装载→建窗→首帧，占位窗 500ms 规则 + 480ms 呼吸 + 60s
  防手滑合并 + Esc 零残留 + 装载零落盘 + CPU 80% 限幅）；静态 PE 全量
  （GUI/CUI/CE 三子系统 + 96 节 + FILE_ALIGN 非常规对齐 + HIGHLOW/DIR64
  重定位 + 30 枚对抗样本全拒 + 64MB 重定位预算 + 节惰性提交账本）。
- **F003/F004 加载加速与门禁两件**（pebind/wow64）：IAT 两级缓存（进程族 +
  会话共享，版本戳参与键防陈旧地址 + 64MB LRU + 命中率 60% 自愈 + WAL 断电
  回放 + delay-load 原位改写）；Wow64 门（0x014C/0x8664/0xAA64 三机器判定 +
  先过门再谈位数顺序红线 + 诚实卡片三要素 + 混合包完整归因 + 1000 条拒绝环）。
- **F005-F008 Win32 面四件**（winmgr/gdiface/gdiplus/comdlg）：窗口类原子表
  16384 + 七族控件 + 40 高频消息面 + 每窗独立队列 + 10 万次死循环判定 +
  NCHITTEST 语义 + DefWindowProc 缺省表；GDI 19 函数 + 万对象上限 + 16 高频
  ROP + 双删除失败语义；GDI+ 画刷三型 + 16k 顶点路径 + 64MP 流式 + PNG CRC
  校验 + 对拍容差（1/255 与 SSIM 950）；四枚通用对话框（过滤器解析 + 200/
  500ms 时延 + 模态语义 + F087 冲突三选 + HSV 色板）。
- **F009/F010 沙盒两件**（reghive/fsredir）：每应用蜂巢（4KB 节点 B 树 +
  WAL 断电百次 100% 回放 + 模板零写入指纹对账 + 卸载进回收站零真删 +
  256MB 告警）；三级文件重定向（前缀分类 + 系统区写拒绝 + 路径穿越防御 +
  SharedData 声明白名单 + 1000 条审计环全部可解释）。
- **F011/F012 会话与环境两件**（envsess/condrv）：环境变量表（标准名 20 项 +
  首写大小写为准 + 三层叠加 + %VAR% 链限 10 层防环 + 32KB 上限 + 会话快照
  冻结语义）；控制台子系统（VT 解析状态机 40 例全对 + SGR 256 色/真彩 +
  exit code 16 位保真 + CTRL_CLOSE_EVENT 5s 宽限 + 输出节流保最新 +
  UTF-16 转码显性化）。
- **F013-F016 文件语义四件**（lnkfile/persrc/mlangres/fontchain）：MS-SHLLINK
  解析器（18 位 LinkFlags + 六字段 + Unicode/环境变量/图标索引 + 断链三分支 +
  深度 5 防环）；PE 资源三级目录遍历（图标优先级链 256>64>48>32 禁止缩小 +
  VS_VERSIONINFO 全字段 + manifest DPI 三态容忍 BOM/命名空间）；语言回退链
  （zh-CN→zh→en-US→en→中立 + 子语言降级 + 8 码页面 + U+FFFD 显式计数）；
  字体映射 20 高频名（pt→px 96DPI 基准全域偏差 ≤1px + 字重合成标注 +
  缺字回退链 + E7 换族覆盖）。
- **F017-F020 互通与兜底四件**（clipfmt/dragdrop/comloc/excface）：剪贴板
  翻译中枢（CF_* ↔ VARIX 双向 + 优先级协商 + 延迟渲染 + 16MB 临时引用 +
  B-3901 后台读取零成功）；OLE↔VXWM 拖放（五态状态机 + 三取消 + 30s 死拖 +
  源崩溃回收 + VXWM 报文可观测）；COM 本地接口最小集（四族 CLSID 蜂巢子树 +
  QI 矩阵 + REGDB_E_CLASSNOTREG 如实返回 + 聚合拒绝 + 泄漏检测）；异常与
  调试面（六类异常样本行为全对 + VEH→SEH 顺序 + 双重故障不递归 + 帧栈 32 +
  minidump 64 帧/32 模块 LRU 20 + 24h 三崩自动建议 F036 草稿）。

接线：`lib.rs` 注册域目录；`robust.rs` 追加 20 域自检；`checks.rs`
MAX_DOMAINS 288→320（「不够即扩」纪律，注释留痕）。

## [Unreleased] — Varix STAR I · AI-U4 I 通用域·四分队（F551-F600 五十项落地）

**kernel/varix/src/istar/**（AI-U4 泳道四，主册 I 通用域 F551~F600 判据实装层；
批次七 F551-F575 + 批次八 F576-F600，50 项功能模块 + ibase 共享底盘（跨午夜
时段/12 色标签板/重名递增命名/CloneLog）+ mod.rs 域聚合器（51 块））：

- **F551/F552 特权两件**（privconfirm/adminrun）：敏感操作白名单三类 +
  内核通道判据（User 通道门外即拒）+ 会话内合并授权（诚实降级）+ 留痕环 +
  伪造注入双测；三入口提权链路 + F551 门联动 + 防护色边条 + 降权随时退 +
  理由字段缺省「未说明用途」。
- **F553-F555 交互三件**（dragbadge/dndtimer/micalm）：拖影 ×N 徽标（右下
  12px、单拖无徽标、加选实时）；勿扰计划表（跨午夜/周掩码三型/例外穿透/
  叠加取严档/到点恢复）；麦克风降噪核（稳态底噪估计 + 300-3400Hz 语音保真
  带 + 10 帧标定期 + A/B 五秒公平回放 + CPU 增量 <2% 常开门）。
- **F556-F562 迁移与工具五件**：用户目录重定向（两迁移方式/透明跟随/撤销
  回放/改回对称/属性页可见）；换机迁移（六类资产扫描完整性/源盘只读/断点
  续传/首开六类校验签发）；键盘测试（104 键点亮/计数/三键无冲 + 幽灵审计/
  凭证导出/即开 <1s）；坏点检测（五色循环/16 灰阶/网格叠加/Esc 即退）；
  节假日（2026+2027 两年内置离线表/红灰双色/更新通道挂 F122）。
- **F561/F562/F574 系统面**：快捷方式三字段高级编辑（ShowCmd 1/7/3 与 F013
  同源/黄提示不阻断/恢复默认）；恢复盘创建（源盘结构性排除/容量校验/格式化
  级确认/64 块哈希对账/进度取消/引导验证入口）；更新摘要卡（3-5 条红线/
  F132 追踪号同源对账/重启后首登时机/一键关闭）。
- **F563-F565/F567-F571 桌面体验七件**：任务视图搜索（<100ms 红线/跨桌标注
  跳转/空态出路）；锁屏时钟（数字 72px/表盘微步进 0.1°@60fps/秒针开关/三米
  可读）；登录问候（6/12/18 时段/1.5s 淡入淡出/点击穿透/星语 30 句）；定时
  静音（三档+自定义/到点恢复原值/角标倒数向上取整）；中键关闭（图标关全部/
  缩略图关单个/未保存三问联动/开关可关）；通知直达（三类去处/主区按钮命中
  分离/无去处回退首页）；显示桌面（6px+2px 热区/Peek 40%/只恢复被压窗/
  Win+D 同账）；标签恢复（三精度/崩溃注入同权/8 标签上限/精简一次）。
- **F566/F572/F573/F575-F582 通用八件**：12 色文件夹色标（三处呈现同源/色点
  过滤/图标让位 6px）；崩溃简报（三归因不甩锅/24h 计数升级/反馈预填）；候选
  三档 5/7/9（翻页联动/超界劝降/缺省 9）；批次七锚点（25 锚点表/基线判定器/
  历史脚本零冲突）；磁贴分组（600ms 合组/组内管理/拖出散组/12 组上限）；搜索
  过滤片（三片/计数/<50ms/键盘循环）；打开文件位置（lnk 指本体/断链诊断/1s
  预算）；滚动条两态（4px/44px/150ms 线性/拖动恒显）；端点双击（24px 判定区/
  中段无动作/飞掠 160-800ms/12ms 帧账）；Tab 进框全选（打字替换/方向键点击
  取消/三处同语义）。
- **F583-F585/F586-F599 收官十四件**：焦点记忆（最后交互控件/会话代边界/
  F237 互补）；标题保头截尾（CJK 2 位度量/三处同策略/F247 保尾对照/Tooltip
  全文条件出）；焦点跟随鼠标（默认关/120ms 驻留/首击透传/浮层三类豁免）；
  目录大小排序（含文件夹/估算「~」标注/三态循环/F219 联动/万目录 200ms
  预算）；保存的搜索（10 条上限/胶囊编辑/FNV 签名新鲜度）；图片粘贴（三场景
  分派/PNG 单源/重名递增）；Edge 拖拽上传（F255 四落点/计数徽标同源/系统
  不越界审计/进度归网页）；壁纸双槽（主题跟随/单槽压暗 30% 兜底/多屏叠加/
  生效变化才落盘）；输出热键（清单序循环/离线跳过/默认无绑/防爆音 40ms/
  OSD 1.5s）；截图光标开关（默认不含/三模式同管/形态如实/与 F361 独立）；
  定时截图（3/5/10 档/进度环 16ms 帧账/Esc 取消/悬停态入帧）；录屏点击高亮
  （300ms 涟漪/多点并发各自老化/令牌色/仅视频叠加/默认关）；固定重截（一键
  连拍/200ms 确认闪/会话内记忆防偏移/与 F593 组合）；登录屏输入法（精简集
  三无边界/解锁接管/词库隔离）；虚拟桌面直达（条序即号码/超界静默/320ms 同
  滑动/F235 钳界同步）；自启动错峰（2s 间隔/拖拽调序/后台就绪握手/F371 锚
  对账）；升级后设置校验（五类清单/指纹比对/漂移通知一键恢复/缺类不假静默）。
- **F600 I 域收官登记**（iregistry）：F-I 一行账 200 条账本（编号去重/容量
  诚实拒绝）+ 账册检三处同源审计（标题逐字对齐 + 600 检查点全绿基线）+
  F200 条款终版（审视范围 F001-F600）+ v1.0 冻结前置五件闸门。

**验证**：隔离舱 309 通过/0 失败/0 警告（#[path] 直挂）+ 全库集成 4,270 通过/
0 失败（干净 HEAD + istar）；纯功能 8,889 行（对上限口径如实登记）。对账与
缺陷账本见 `_attic/aiu4-f551-f600/`。

**复验轮（第二会话）**：域表注册挂接（1e706f28）后的 HEAD 集成态首次全量
认证——干净 worktree 全量跑出 5182/1，唯一红项为全域门 f475 定位到的
`F521-shot-savedir FAIL 9/11`（U3 领地两条确定性契约违约，dangling commit
溯源证实自 U3 首个提交即红）：`format_shot_name` 前缀未按 PREFIX_MAX 截断 +
seq 1-9 误产零填充 `_02`，按函数 doc 自钉契约最小手术修复（检查期望零改动）；
同轮清除 robust.rs 域表 rebase 伤（U1 注释块下错挂的 secstar2 重复注册行，
域表 350→349）。**并实证全域门静默截断**：`MAX_DOMAINS=320` < 域表 351 份
注册——f475 渲染恰 320 行、末行 F521，`istar-u4`/`genstar2`/ustar3
F522-F550 从未进过全域门；worktree 实验 `MAX_DOMAINS→384` 解封 40 域，
暴露同被吞的 `F525-hotkey-card FAIL 10/11`（检查期望错：`set_key` 改键即转
UserCustom 而检查仍按默认计数，已修正），终局 **5425 passed / 0 failed：
全域门 360 域全数 PASS、`istar-u4 PASS 51/51` 在门内实证**（istar 同时保有
直调口径 51 块聚合 + 155 单测双证据）。MAX_DOMAINS 一行落位移交 checks.rs
属主（该文件正被并行会话持有 WIP，本队不越权）。

## [Unreleased] — Varix STAR I · AI-S2 安全加固域·后段（F186-F200 十五项落地）

**kernel/varix/src/secstar2/**（AI-S2 泳道一安全后段，主册《Varix STAR I
start.md》G-G-16~G-G-30 判据实装层）：15 项功能模块 + mod.rs 域聚合（单聚
合注册进 robust.rs 内核全域体检表 274→275），**纯功能代码 6,838 行 · 域自
检 315 条 · 单元测试 96 项全绿 · 宿主/内核镜像（no_std）双口径零错误**。

- **F186 系统分区不可见**：枚举过滤零枚举（挂载照常呈现缺席）+ 管理页
  全卷表灰显 + 操作按钮=移除不是禁用 + 越权写审计留痕 + 红线不设开关。
- **F187 时区与时钟守护**：SNTP 自研（kiss-of-death 拒绝）+ 漂移三档
  （<2s/2-300s/>300s）+ 无网好值插值「估算」标注 + RTC 失效三连零推断 +
  向导三步 + mtime 历史真相不动。
- **F188 日志三环**：内核环 256KB 洞标记 + 轮转 10×2MB 封档对拍恒等式
  （边界零丢）+ 统一 QPC 时钟（u128 防溢出）+ 三泳道 O(n) 归并 + ±50ms
  对齐 + 导出 manifest 诚实缺段。
- **F189 静默自愈集**：三类损坏 5×3 注入全愈 + 报备 100% + 同项 ≥3 次/周
  升级工单（慢性病信号——成功也立票）+ 重建策略三分 + 快照协同。
- **F190 更新双槽可视**：槽卡并排 + 主册条款文案 + 空间前置拦截 + 保留期
  7 天旋钮 + 到期灰置诚实不藏 + 备用槽坏不切槽。
- **F191 安全启动链自查**：三查依赖序（免查显式不冒充绿）+ 三注入全拦 +
  「已保护」成功防御语义 + 预算 60/5/5ms 对账 + 差异字节定位 + ksha256。
- **F192 内核参数白名单**：三族 13 参编译期常量 + 20 非法样本全拒 +
  Levenshtein ≤2 零堆建议 + 越界钳制分账 + 128 字符/1KB 双防线。
- **F193 安全模式**：参数唯一源 + 黄条原因随来源 + 12 项最小集白名单 +
  白名单外灰置可解释 + 连续两次异常关机询问 + 低分辨率 GOP 保底。
- **F194 审计日志完整性**：SHA-256 序号链 + 改/删/插三攻击检出定位（删插
  后重排序号的真实篡改面）+ 跨日续链锚定 + 三方独立校验器随包。
- **F195 资源配额执行**：四级阶梯顺序对账（顶格不复燃）+ 后台 ×0.8 优先
  降（硬顶不偏置）+ 豁免 6 项零误伤 + 动作/通知两账分离（5min 合并窗）。
- **F196 电池保护策略**：两级阈值 + 30s 滑动滤波（跳变 >8‰ 判噪——老化
  电池永不误关机）+ 60s 倒计时状态机 + 可逆窗口 + 关机账目完整（取消也
  入账）+ 冲刷卡死强制下电。
- **F197 温度感知降档**：三档 75/85/95℃ + 迟滞 5℃ 零震荡 + 连续 3 次拒绝
  重定基线（基线卡死防线）+ graceful 跳过 + 温度强制优先手动档 + 95℃ 复
  用 F196 冲刷管线。
- **F198 恢复环境**：三卡全流程 + 零写问题盘（5 尝试 0 实写审计）+ 还原
  点损坏跳过标注 + 二维码降级 + 两级降级到底不伪装。
- **F199 版本与谱系页**：版本树折叠 + hex12 复制保真 + 构建记录对拍 +
  双槽并显 + 本地构建诚实标注 + 开放 JSON（F128 同语言）。
- **F200 全域总检**：锚点登记/覆盖率 1000‰/证据 TTL 过期绿按红/冻结五项
  排除考核/季检三栏/现实契机四问门槛 + **tools/vx-walkcheck-all.py** 走查
  族总集成脚本（解析 199 项→配证据→红绿一页纸→季检归档，--selftest PASS）。

详见 `docs/AI-S2-完成报告.md`（判据对账表 + 缺陷账本 8 条诚实入账 + 复现
口令）。

## [Unreleased] — Varix STAR I · AI-H4 H 基础通用域·四分队（F351-F400 五十项落地）

**src/system/h4/**（AI-H4 泳道三四分队，主册《Varix STAR I start.md》第 6 部
F351-F400 判据实装层；纯逻辑引擎 + 判据自证架构，与 UI 壳解耦，全部可在
node 环境单测）：50 项功能模块 + registry.ts 批次登记册 + internal/store.ts
共用持久化底座，**纯功能代码 5,637 行 + 单测 3,196 行，51 个测试文件
311 项测试全绿，H4 范围 tsc --noEmit 零错误**。

- **F351-F360 工作台与创造者工具十件**：工作区快照（上限 10 LRU/显示器
  签名归一化精确-比例双路恢复/未开应用 Z 序启动队列/最小化态还原）、缩略
  图窗上直接操作（三型操作集/命中区外扩 2px + 重叠审计/100ms 预算/悬停抑
  制状态机）、跨屏拖拽位置记忆（拔屏钳制回流/重接逐字段回原位/缺席提示一
  次/5 轮插拔幂等自证）、任务栏资源摘要（默认关/30s×1s 环形历史/前台让路
  准入/三图对账 ≤3%）、Edge 深度协同（下载-拖拽-待遇三管子/F255 四落点表/
  **UA 永不伪装类型级保证**/A2 判例工厂回退留痕）、PWA 应用化（manifest
  字段取用/独立窗口三属性/卸载干净度扫描/四列表平权审计）、下载收口（托盘
  步进微进度/大余量睡眠豁免/**无商店宪法不越界审计**/哈希失败重下三要素）、
  全局画中画（240×135 三档缩放/四角停靠/并发 2 + FIFO 递补/F248 置顶不抢
  焦点/时间点不跳回原窗）、屏幕拾色器（15px×8 放大环/HEX-RGB 双复制/最近
  8 色 F234 联动/色板 20 点零误差审计/Shift 连续模式）、像素标尺（量测负
  向归一化/8px 网格 20% 透明度/Esc 秒退/点击穿透恒真）。
- **F361-F370 录制与后台治理十件**：屏幕录制（三模式/诚实计时暂停不虚增/
  呼吸红框 2px 环带/麦克风指示无豁免/开销 <5fps 预算）、录屏产物管理（5
  分钟分节 >10 分钟门槛/**断电分节记账恢复**/预估误差 <10%）、专注计时器
  （25/50+自定 5-120/放弃记真实时长/每日累计逐笔对账）、下载一键整理
  （**建议制不动手：无定时器无监听无钩子的结构性保证**/勾选执行/整批撤销/
  幂等）、重复文件查找（大小预筛→全量哈希零误判/浪费空间排序/推荐保留受
  保护/回收站可反悔/空闲让路）、托盘电池（三档渲染/四段色阈值逐点对账/低
  电三态独立视觉/插电全消）、桌面分区吸附（8px 阈值参考线/四区可选/**未启
  用零差异判据**）、最小化到托盘（声明行为不骗人/真退出路径/任务栏不显示/
  F075 溢出兼容）、后台任务中心（四类系统任务全入册/两级暂停/ETA 有效性
  校验/F057 分级倒挂检出）、后台不惊扰（三次指数退避 1s→2s→4s/横幅=0/最
  终失败一条三要素/常驻 UI 白名单审计）。
- **F371-F380 反馈与键盘十件**：开机时长徽标（与实测链同源换算 <100ms/
  300-3000-500 时序/90 条历史曲线同账本）、人话时间线（8 大类事件全转译/
  缺参占位零编造/**FNV-1a 哈希链篡改定位**/24h 窗口 500 条上限）、键盘布
  局管理（三处同源/轮切=设置顺序/**100 键快速混切零吞键**/布局级选项隔
  离）、快捷键速查浮层（600ms 触发松开即隐/注册表实时视图无缓存/点击穿
  透/40 条折叠预算）、H 域总判据（三走查引擎：一致性对照表 100% 绿/20 任
  务链 ≤15 分钟零卡壳/三降级态/回炉清单/报告归档）、标题栏系统菜单（六项
  逐字对齐/**四状态×六项置灰矩阵**/2px 几何/F377 衔接；V-21 复用不重
  写）、键盘移窗（1px/10px 双档/Enter 落定零漂移/Esc 还原/300 步贴边自
  证）、列宽自适应（最长可见+12px/40px 钳制/最右列吃满/F219 记忆）、表
  头排序（三态循环/Shift 三级上限/**滚动位置恒等保持**/中文 locale 比较
  器）、空行点击（单击清空 O(1)/Ctrl 保护/背景菜单无对象操作=0 审计）。
- **F381-F390 结构控件十件**：树形三态（半选传递/勾父全选/展开态记忆/键
  盘四操作/**根视口恒展开修正**/横杠居中 60%）、对话框位置记忆（居中偏上
  1/3 基准/同类归组/跨屏完整落屏/容量 20 LRU）、弹窗排队（模态唯一/**200ms
  间隔递补**/横幅 3 条溢流/焦点唯一/FIFO 公平）、焦点陷阱（50 次 Tab 零逃
  逸/非模态不困/归还唤起者/空环如实降级）、无障碍语义树（无名可交互=0 审
  计/50 控件抽查/开关读开-关/**读屏接口文档公开**）、阅读模式（1.6/45 字/
  衬线三参数/**内容哈希零修改结构性保证**/每应用记忆）、灰度模式（三滤镜
  互斥 toggle/滤镜单点审计/形状冗余审计/BT.601 对拍）、竖屏适配（四区上
  下换形/任务栏侧边/单列默认/安全区不遮内容）、滚动长截图（重叠去重/**动
  态内容诚实失败不给错位图**/接缝评审账）、选词查词（离线零网络路径/不抢
  焦点/生词 20 条/分页加载）。
- **F391-F400 收口十件**：选文翻译（500 字符分界双路/**离线诚实提示**/RTL
  预留）、文件夹大小列（单点计量服务/超 5 万文件转估算/版本号缓存秒出/三
  功能同源）、存储热点图（squarified 面积 ±2% 对账/点选最深层命中/榜单排
  除根/空闲通道）、清理收口页（四来源对账/**不可逆项缺代价说明显式暴露**/
  回收率 ≥90% 判定/默认勾选防手滑）、U 盘健康（**读不到不编数**/20%-10%
  双阈值出路链接/多介质分列/写入累计账）、备份向导（三步校验/**增量差集
  对累计全集**/可恢复性校验/还原完备性比对/staleExtra 如实标注/提醒永不
  自动跑）、还原演练（季度 90 天/**零副作用不入账**/三卡联动/首恢复加练
  提示）、语言热切（缺译回退英文+标注/差异表 F132 联动/草稿哈希零丢失/RTL
  接口存在性）、彩蛋总谱（开机第 100 次一生一次/7 连点/star 命令/**不藏功
  能审计**/F124 谱登记）、H 域收官登记（一行账与标题 100% 一致/200 检查
  点基座/三处同源审计/F200-r2 条款修订）。
- 施工中修复的实现侧真缺陷 5 处（断言侧过时同步修正，绝不为绿屏粉饰）：
  F351 显示器签名未剥 @缩放 后缀致精确路径不可达；F353 幂等比较混入时间
  戳；F365 裸文件名 originalDir 被 `slice(0,-1)` 咬尾；F381 键盘序未并入
  根展开态致导航塌缩；F396 增量差集误对「上条 delta」而非累计全集求差。
  另 F393 榜单排除视图根、F396 还原比对改完备性语义（staleExtra 如实标
  注）。
- 非功能产物：本次施工零临时文件落盘，无需入 `_attic/`（探针/日志类产
  物为零；全部持久化走 `variable:h4:*` 键域且可注入内存实现）。

## [Unreleased] — Varix STAR I · AI-E1 个性化全域（F151-F170 二十项落地）

**src/system/persona/**（AI-E1 泳道三，主册 E-5 深化设计报告 G-E-01~G-E-20
判据实装层——TS 界面组件与交互层，主题工坊同族架构）：

- **底盘**：`store.ts` 单一配置根（十六分节、订阅制广播总线、回退栈深 3、
  落盘失败显性抛错）；`tokens.ts` F151 五类令牌全集（语义色 24 枚/间距乙-1
  六档/圆角三档/字号四档/F124 动效五曲线三时长）+ JSON Schema 发布 +
  热替换引擎 + 既有 tokens.css 变量桥接（30 界面零硬编码判据的机械通路）。
- **F152-F160 九件**：实时预览编辑器（快照/apply/discard 哈希还原/≤100ms
  延迟记账）、深浅自动切换（四态状态机/60s 预告/手动暂停当日/日落日出
  兜底推算）、壁纸每日一换（近 7 天排除表/30 天历史环形/构图保护 20 档
  分辨率矩阵）、图标包热更换（原子换指针/回退栈 3 层/诚实覆盖度）、指针
  编辑器（15 枚标准指针/热点 1px 级/双倍率 sprite）、声音混合器（六事件
  独立档位/感知对数曲线/总闸语义一致）、开始菜单预设（三官方+差异集存储/
  长辈模式 56px 达标线）、字体安全档（cmap format 4 解析/双集缺字率/5%/15%
  三档判定）、动效强度预演（100%/60%/0% 缩放单点引擎）。
- **F161-F170 十件**：档案导出（vxtheme 分节勾选/差异预览/导入哈希对拍/
  中断原子回退）、每应用例外（上限 10/锁定令牌表/诚实降级标注）、桌面小组
  件（时钟秒级准确/透明度 20% 下限/性能保护超预算降透明）、锁屏定制（三式
  时间样式/唤醒 2s 时间线/零循环动画校验）、开机动画（密度三档 1200/600/200/
  粒子色映射/底图亮度治理/8.0s±0.2s 契约）、输入法皮肤（16ms 红线自检/
  对比度 4.5:1 门禁/60% 下限护栏）、右键菜单（关键项锁定防自残/90 天使用
  计数环形/自动排序仅作用应用组）、任务栏（24/32px 双档 48→56px/贴底 200ms
  唤出 6px 热区/全屏强制显示）、快捷键查看器（默认表 60 条目四组/规范序
  Win>Ctrl>Alt>Shift/冲突 O(1) 即录即查/重录撤销栈）、域总判据（19 项
  改→生效→回退三步执法引擎，快照等值回退验证）。
- **界面**：`PersonaStudio.tsx` 双形态（overlay 自挂载 + 设置中心「个性化」
  标签页内嵌，双入口可达），五页组二十页全量控件绑定真实配置层。
- **验证**：135 项单测全绿（11 文件）；`tools/vx-walkcheck-e.py` 走查脚本
  （checklist 生成 + 结果校验 + 冒烟自证三模式）；完成报告
  `docs/AI-E1-完成报告.md`（行数对账与随闸门补测清单如实呈报）。

## [Unreleased] — Varix STAR I · AI-C2 应用兼容域·后段（F021-F040 二十项落地）

**kernel/varix/src/compatstar2/**（AI-C2 泳道二后段，主册 A-5 深化设计报告
G-A-21~G-A-40 判据实装层；目录名 compatstar2 因 compatstar/ 已被 AI-C1
（F001-F020，在途）占用，按 secstar→secstar2 先例顺延；robust.rs 域函数
指针表单行聚合注册 275→276）：

- **F021-F025 资源与协议五件**：内存语义对齐（64KB 粒度/六保护位/写时
  复制/OOM NULL 语义/VirtualQuery/大页降级日志 + 1 万轮压力单测）、时间
  定时器族（TSC 2500MHz 直通/49.7 天回绕语义/timeBeginPeriod 滥用合并
  记账/10 时区 round-trip）、Winsock 面（256 句柄上限/WSA 错误码如实翻译/
  严格轮转 50 socket 无饥饿）、TLS 证书库（三区自动分类/发行者链校验/
  OCSP 软失败可关/私钥 0600）、打印虚拟面（300/150dpi A4 预算/队列状态机/
  取消零残留/5 分钟告警）。
- **F026-F030 体验桥接五件**：音频 WinMM 面（双流无互扰/欠载 <0.1% 计数/
  pan 语义集/总闸优先/独占降级）、输入法 IMM32 面（GCS_* 桥接/三提交
  语义/16ms 节流/崩溃自恢复）、高 DPI 三态（manifest 解析/覆盖三选/
  per-monitor 单屏降级/500ms 宽限不撕裂）、多显示器前瞻（MONITORINFO
  字段级对拍/ADR-PR-001 冻结/越界如实失败码）、安装器兼容模式（三族状态
  机/权限卡前置/取消回收站式回滚/MSI 自修复）。
- **F031-F035 生命周期五件**：卸载与残留清扫（三表清单驱动/勾选外零
  残留/10 分钟撤销/清单缺失目录树路径）、开源运行时直装族（五语言并存
  显式激活/三验损坏检出/包缓存哈希去重/磁盘预检含膨胀预估）、构建工具链
  判例（四工具基线/增量构建时间戳比对/cmake 锁 ninja/clone 内存配额）、
  字符编码终局（检测链置信度三档/10 乱码判例/8 码页 round-trip/U+FFFD
  超 1% 警告）、兼容性自检向导（决策树五分类/主按钮四映射/归因记忆/
  反复失败建议移除）。
- **F036-F040 治理与账本五件**：星卡自动草稿（采样超 1% 预算自动降采样/
  脱敏三规则硬编码三类全查无/隐私总闸）、peblock 门用户可见化（解释卡
  三要素/置灰 3 秒/**FNV-1a 序号链 append-only 审计三攻击全检出**/72h
  信任窗口）、应用隔离档位（九宫格矩阵/10 分钟临时放行自动收回/配额随
  档联动）、游戏兼容前瞻面（import 表零开销探测/四步交接 30s 分段预算/
  超时降级回 VARIX）、兼容域总判据（**五类 × 10 件全名单 50/50 建档/
  季度快照不可变/攻坚名单/归档不删除**）。
- **深化语义层（run_*_deep，铁律 1 回炉补深化）**：F021 增 VirtualProtect/
  MEM_RESET/DECOMMIT/重提交/守护页；F022 增夏令时规则表/WaitableTimer
  双型/FILETIME 换算；F023 增 WSA 32 错误码全集/socket 选项/ioctlsocket/
  hostent；F024 增 12 条根名册/密钥用途位/链策略；F025 增 PDF 对象模型/
  内容流校验/页范围解析/xref；F026 增 WAVEFORMATEX/头状态机/混音线控制；
  F027 增 WM_IME_* 消息面/HIMC/CANDIDATELIST 翻页；F028 增 WM_DPICHANGED
  参数打包/缩放因子定点/三拍推进；F029 增 DISPLAY_DEVICE/DEVMODE/字段
  掩码；F030 增 MSI 五表/Inno 任务类型/NSIS 段标志/安装日志；F031 增 ARP
  登记/三族静默开关/体积统计排序；F032 增 semver 比较/五镜像表/隔离解析/
  三要素错误；F033 增 .gitattributes/.gitignore 解析器/make 变量/ninja 边；
  F034 增 GBK 区间/CP437 映射/UTF-16 代理对/BOM；F035 增运行时签名表/
  异常码分类/频率分层；F036 增 JSON 字节序列化/隐私扫描/合并策略；F037 增
  三型规则引擎/风险清单/角标状态机；F038 增能力位全集/临时放行台账/配额
  分档表；F039 增 D3D 库词表/显存预估/独占全屏闸/交接参数打包；F040 增
  JSONL 序列化/季报生成器/类别统计。
- **深化批次二（deep/f021d~f040d + deep/mod.rs 聚合器，+3,675 行）**：
  二十域执行与治理面——F021 堆句柄族（HeapAlloc/ReAlloc 账面自洽/双重
  释放检出）+ GlobalMemoryStatusEx + 工作集锁 + NTSTATUS 分类；F022
  civil 历法核（Hinnant 双向 + 万年历锚点）+ SetSystemTime 回拨闸（F187）
  + 周期定时器推进；F023 fd_set 位图/select/字节序四函数/inet 双向/UDP
  MSG_TRUNC/shutdown 半关闭；F024 SAN 通配符单层语义 + ASN.1 UTCTime +
  指纹十六进制 + OCSP 响应缓存 + PEM 分流；F025 PDF 头尾字节结构 + 字体
  子集前缀 + 页树计数不变量 + MediaBox/CropBox + Info 日期；F026 DS 环形
  锁双段 + 会话音量记忆 + 电平表（幅度口径不回绕）+ 重采样最简比；F027
  GCS_COMPATTR 游程压缩 + 候选稳定排序 + EXFORMINFO 钳制 + UTF-16 记账；
  F028 DPI 三出口分诊 + 非客户区缩放适用面 + ceil 缩放盒（防裁切不变量）
  + 变更历史单调账 + per-monitor-v2 声明；F029 枚举索引越界如实 None +
  MonitorFromPoint/Window + 15s 确认倒计时 + 拓扑形状预留；F030 MSI 特性
  树祖先传播 + 进度双权重 + DOS 保留名/非法字符目录校验 + UpgradeCode
  语义；F031 卸载进度 + 还原路径恒等账 + 蜂巢快照差集 + 强制卸载双确认；
  F032 pip freeze 行解析 + PEP 503 规范化 + engines 子集 + 磁盘聚合 +
  PATH 摘除（显式激活对称出口）；F033 依赖闭包 BFS 去重 + console 池
  （深度 1）+ reflog append-only + hunk 不重叠；F034 UTF-8 编码器（与批
  次一解码闭环）+ Big5/EUC-KR 区间 + 换行三态统计 + 转换账本；F035 失败
  联合指纹 + 向导步骤栈 + 离线清单稳定排序 + 导出脱敏闸；F036 JSONL 帧
  + 饱和计数 + 同日去重指纹 + v1→v2 字段迁移；F037 布隆前置（无假阴性
  对拍）+ deny-first 裁决 + 审计链头锚点 + 信任窗剩余；F038 进程代际
  继承 + 放行期审计 + 写路径三分类 + 生效时点；F039 exe 关键词 CI 识别
  + 全屏降级协商链 + 回程四步对称账（8+3+4+15=30s）+ 分辨率 EDID 交集
  闸；F040 季度快照差分（归档不计升降）+ semver 升级判定 + 攻坚管线
  （2 季入列/4 季建议移除）+ 快照链单调校验。
- 自检判据逐条钉死：20 域 **448 CheckSet 检查项（主 237 + 批一 110 +
  批二 101）+ 193 宿主单测全绿零警告**（隔离舱批次二轮 2026-09-27 复跑
  同口径全绿，三层聚合 0 红项）；施工缺陷账本 **25 处自纠**（批一期 6 处、
  复验轮 3 处、批次二期 7 处——含 HeapReAlloc 退账、缓冲复用脏断言、
  历法锚点对拍等，全程隔离舱逐轮收敛）与行数口径偏差
  （13,098/73,125，达成率 17.9%，判据实装层口径同 AI-K1 先例）见
  `docs/AI-C2-完成报告.md`。

## [Unreleased] — Varix STAR I · AI-J1 J 鼠标域·一分队（F601-F620 二十项落地）

**src/features/mouse/**（J 域前段全量：「实机手感与移动」F601-F614 +
「鼠标自定义」F601-F620 段共二十项；桌面运行时 + 设置面板 + 持久化三链齐全）：

- **F601 指针速度曲线谱**：四族曲线（线性 1:1/经典加速 Windows 复刻/缓启动/
  自定义贝塞尔两控制点）、20 点增益对拍表、与 F250 关加速档单一事实源。
- **F602 慢速微调**：修饰键按住恒定增益（5%/10%/20% 三档，曲线旁路）、
  F244 冲突审计登记行（粘滞键/筛选键共占方声明）。
- **F603 抬笔滤波**：抬起后 8ms 窗口末位移 50% 折算、逐事件直通零缓冲、
  注入 100 次 P95<0.5px 自检。
- **F604 中键自动滚动**：锚点 16 方位映射、8px 死区 60px 封顶、退出三路
  （中键/左右键/Esc）、F204 惯性互斥语义。
- **F605/F606/F612/F618 滚轮四件**：三档刻度语义+应用覆盖优先级、倾斜轮
  3 列/档+Shift+滚轮等效、自适应增益（双重 EMA 平滑 3→12 行封顶）、
  穿透/豁免/关闭三态+白名单类型表+零死胡同回退。
- **F607/F613 跨屏两件**：接缝护边 4px/200ms+四角 8px 豁免、跨屏落点记忆
  （EDID 指纹为键、单屏静默、屏内钳制）。
- **F608 磁吸对齐**：小目标白名单（<24px）、三档半径、视觉微移判定零偏移、默认关。
- **F609 拖拽边缘自动滚**：24px 触发带三档速度（4/10/18）、仅声明容器生效。
- **F610 悬停时序**：菜单/tooltip 双旋钮四档、点击展开永远即时、CSS 变量即时生效。
- **F611 手抖过滤**：三档 IIR+幅度阈值门、快速大幅零衰减直通、2/4/6Hz 效果谱。
- **F614/F616 档案双维**：设备档案四件套（首插克隆/LRU 上限 10）⊕ 应用档案
  增量切换（防跳变）正交矩阵。
- **F615 侧键编程**：全局/应用两级优先级、三类映射目标、F244 注册行互通、五键位。
- **F617 右键手势层**：八向编码+同向合并、12 内置手势、200 例样本 ≥95%、
  墨迹 120ms 淡出、无轨迹回退右键菜单（零误伤）、默认关。
- **F619 长按统一旋钮**：0.6x/1.0x/1.6x 三档统一缩放（触屏/磁贴/ClickLock）、
  登记纪律执法（未登记抛错）、默认档=现行值对账。
- **F620 指针衬底**：1px 感知亮度反色描边（默认开）/柔投影/3px 衬圈三开关独立、
  F335 优先平面纯 CSS 零帧耗、三底色走查样本。
- **面板与运行时**：设置中心新增「鼠标」标签页（20 项三件套全量可调、贝塞尔
  编辑器即时预览、谱表/对账表内联、能力检测显隐、恢复域默认+节级 undo）；
  桌面 J1Runtime 管线总装（抖动→抬笔→慢速→曲线→积分驱动 F335 副本层，
  点击判定永远走真实光标）。
- **质量**：86 项单测全绿（v1 63 + v2 深化 23）；行数对账（3,967 行 · 达成率 25.3%）与随闸门补测清单见 `docs/AI-J1-完成报告.md`。

### 深化批次二（v2 · 并入本批）

- **惯性引擎** `inertia.ts`：F204 平滑档余韵——速度 EMA、指数衰减（半衰期 260ms）、阈值归零、与自动滚互斥裁决；运行时动量 rAF 循环接线。
- **手势录制器** `gestureRecorder.ts`：32 点重采样/八向编码/与库查重（重复显性拒绝）/上限 24 条不静默/导出导入 round-trip；面板新增录制试笔台（canvas 实笔画）。
- **档案打包** `pack.ts`：F623 vxtheme 对接单一事实源——三例合法性判据执法（超速拒绝/超量降级列名单/非法轨迹拒绝）、中断原子性、包摘要。
- **体验日志** `telemetry.ts`：十三章执法件——交互细节层事件环（500 条）、狂点/死点/锚标弹跳/手势放弃四类挫败信号自动捕获、时间轴导出、隐私红线（只记行为不记内容、关闭态零分配）。
- **判据证据引擎** `evidence.ts`：F601 对拍表/F603 自检/F604 方位表/F605 档位矩阵/F607 护边矩阵/F611 谱表/F619 对账/F620 走查样本八表收敛成包 + 内建一致性自检。
- **模块深化**：灵敏度人群预设+示例轨迹+平滑度自检（F601）、悬停编排器 MenuHoverController（F610 点击即时红线）、护边分屏对覆盖（F607 方向无关键）、跨屏记忆点管理（F613）、手抖自动调谐（F611）、自动滚油门爬升（F604 起步柔和）、F616 前台档案挂载、F614 批量导入逐条校验、F615 冲突审计。
- **面板纵深** `MouseJ1Panels.tsx`：九个折叠式重型子面板 + 「遥测与证据」新分组；运行时全量埋点（粗粒度目标，隐私红线）。

## [Unreleased] — Varix STAR I · AI-U1 I 通用域·一分队批次一（F401-F450 前八项落地）

**kernel/varix/src/uni1/**（AI-U1 泳道四 I 域前段，「系统快捷键与通用
交互语义」簇八项 + 共享底盘；robust.rs 275 域函数指针表单行注册）：

- **F401 桌面自动排列**：四序语义核（名称/大小/类型/日期，平局按 id
  钉死）、插入队尾/删除补位、自由↔自动互斥切换、F124 弹性档（320ms）
  临时移动弹回。
- **F403 Win+L 锁屏快捷**：锁定三段预算记账（合计 <300ms 判线）、媒体
  暂停/续播对、窗口状态冻结归还、锁屏摘要只计数、F316 闲置锁屏同入口
  （来源可辨）、超预算诚实计数。
- **F404 Win+E 资源管理器快捷**：同窗新标签（默认）/总是新窗两模式、
  此机页内容清单钉死（盘符字母序 + S: 共享卷 + 常用文件夹）、连按行为
  两形制、首开 <1.5s 冷启记账、骨架先行 400ms 拍。
- **F405 Alt+F4 与桌面关机菜单**：焦点三场景分发（窗/桌面/模态陷阱
  F384 优先）、电源菜单三选项默认关机（方向键循环 + Enter + Esc）、
  F310 三问联动（脏窗不静默关）、关窗语义与 × 同路径记账。
- **F407 Win+I 设置快捷**：三场景矩阵（未开→开+搜索、已开→聚焦搜索、
  已开在搜索→保持）、单例窗口恒定 ID、搜索框光标就绪、150ms 预算账。
- **F408 Win+X 快捷菜单**：九项清单与落地页对照表钉死（一处一事实）、
  首字母直达（无重复、未命中不乱跳）、关机子菜单二级（先收子菜单再收
  菜单——F424 逐层剥离同构）、打开 <1s 记账。
- **F416 Win 键开合开始菜单**：开 <150ms、焦点直接落搜索框、80ms 连按
  去抖（机械抖动零误触）、Esc/外点/再按三出路、打字冲突零丢失硬账本、
  体验日志开合事件可回放。
- **F424 Esc 通用关闭语义**：四层语义表（浮层→面板→模态→桌面态）
  唯一登记、按一次关一层（绝不越层）、桌面态无动作无副作用、响应
  <100ms 记账、外点关闭不污染 Esc 语义。
- **共享底盘 ubase**：Chord/HotkeyTable（F244 键位注册唯一落位：登记/
  冲突拒/改键/恢复默认/快照 round-trip）、LayerStack 浮层层级栈、
  RingLog 定容事件环、Knob 旋钮（钳制 + 留痕）。
- 自检判据逐条钉死：**隔离舱 45/45 绿（#[path] 直挂真实文件）+ 真实
  crate uni1 38/38 绿**；全量 4093 项中 4078 绿（15 红全部归属
  compatstar2/stareco 在途施工模块，uni1 零失败零回归）。施工期缺陷
  账本 8 条（1 真缺陷：F424 层级不变量方向）修复与行数对账见
  `_attic/aiu1-f401-f450/行数对账与缺陷账本.md`，完成报告见
  `docs/AI-U1-完成报告.md`。批次二（其余 42 项）续建。

## [Unreleased] — Varix STAR I · AI-K1 性能域深化（F041-F057 十七域落地）

**kernel/varix/src/perfstar/**（AI-K1 泳道 B 前段，主册 B-3 深化设计报告
G-B-01~G-B-17 判据实装层；robust.rs 274 域函数指针表注册接线）：

- **F041-F044 观测面四件**：帧率账本（每帧四项打点 + 分钟桶聚合 + 24h
  降采样）、帧率归因器（四类嫌疑贡献模型回线判定）、冷启动画像（五段
  刻度 + 中位数）、预取指纹 v2（位图指纹 + 顺序区序列化）。
- **F045-F048 资源调度四件**：页缓存水位三档（`lru_bytes` 字节账/页账
  对齐）、写合并窗口三档自适应（dwell 抑制）、调度延迟预算四类分解
  （几何直方图 + aging）、CPU 频率联动（P-state + Silent 封顶唯一判据）。
- **F049-F053 底层治理五件**：空转清零、中断合并（2000ns 窗口 + 动态
  收缩 + 熔断）、4MB 大页池、堆碎片尺寸分级池（零堆 grep 自证）、启动
  四链并行。
- **F054-F057 体验四件**：图像 SIMD 解码（标量逐像素对拍）、字形光栅
  缓存（LRU + shelf packing）、合成器脏区深化（打字 P95 <5% 屏）、IO
  调度分级（三队列 EDF + fsync 硬承诺 + 反向保护时长有界 + 24h 混载
  零饥饿）。
- 自检判据逐条钉死：17 域 **156 CheckSet 检查项 + 98 宿主单测全绿**；
  全量 3897/3897 PASS（debug 通道）。施工期缺陷账本 **7 处实现侧真缺陷**
  修复（含 iotier 反向保护 resume 死锁、pagewater 字节账脱钩、cpufreq
  封顶失效三处高危）与偏差登记见 `docs/AI-K1-完成报告.md`。
- **深化批次三（2026-09-26 续：十七域判据补齐 + 十二查登记 + 用户故事场景，
  +13,050 行 → perfstar 38 文件 22,649 行）**：新增二十件——十七域 `_ext`
  深化件（帧明细与账本落盘格式/降采样回落链/打点成对自检、证据引用链与归因器
  健康状态机/误报率账、五段条形图配平与(版本,序号)画像键、开放格式指纹头与
  8MB 配额 LRU、分列 LRU 与分区脏页账/回收后悔/200MB 警戒、断电丢失窗口与三档
  断电百次演练/分档 fsync P99、优先级映射与老化自修正、_PSS 表装载与切换守卫/
  温度越权、事件源统一 fence 与 60 秒静置双指标、合并键二分与网络按 flow 合并/
  flood 70 级零丢弃、三区预算与只增不减防碎片/TLB 对拍、位图分配器与分配失败
  四阶段演练、依赖矩阵与逐链超时/动画 80% 起播、行过滤五型与 IDCT 双实现逐位
  对拍/半图拦截、3500 汉字预热与双缓冲原子换页、区间树脏区合并与弹窗合成矩形
  判据、分级双因子判定与降级审计环）+ 共用底盘 `perfkit`（诊断报备环/保留期
  轮转/旋钮清单/60 秒逐秒环）+ 通用十二查登记册 `k1walk`（三落位+理由/≤4 段
  路径链/说明句三件套/4K 四档/性能线）+ 十七个用户故事场景 `k1scene`。判据面
  升级 **541 CheckSet + 239 单测全绿**，纯功能代码 6,900 → **14,002 行**
  （上限口径达成率 11.0% → **16.0%**）；施工期 14 处缺陷闭环（含「检查项超
  MAX_CHECKS 被静默丢弃」已按零静默纪律拆段修正）与偏差登记见
  `docs/AI-K1-完成报告.md` §12，逐项锚点对账见 `docs/AI-K1-检查项对账表.md`。
- **深化批次（2026-09-26 回炉补深化，+503 行 → 17 域 9017 行）**：四件
  真实深化锚定主册设计细节——F052 分配谱直方图采集器（9 桶 × 7 天滚动 +
  告警归因四模式 + 定档复查建议，数据先行）、F054 解码缓冲落位计划器
  （大页池尾部 4MB 对齐 / 4K graceful 回退）、F051 尾部空闲槽查询
  （F054 对接面）、F042 证据桥（F041 账本帧同源引用不复制）+ 唤醒风暴
  证据接收端（F049 → F042 通道）。判据面升级 **166 CheckSet + 114 单测
  全绿**（隔离验证舱口径：工作树多 AI 并发施工态，perfstar 零错误，
  证据与勘误——v2 报告 F046「10s/仅升档」笔误更正为 8s/双向驻留——见
  `docs/AI-K1-完成报告.md` §10）。

## [Unreleased] — Varix STAR I · AI-K2 内核底盘域·后段（F058-F075 十八项落地）

**kernel/varix/src/star/**（AI-K2 泳道一 B 后段，域聚合器 `run_star_checks` blocks=19，
隔离舱实测 **143 通过 / 0 失败 / 0 警告**）：

- **F058-F067 底盘十件**（memcomp/netbatch/battery/reggate/selfcheck/touchpad/
  audiolow/wakegov/fsjournal/coldhot）：内存压缩前瞻评估件、网络小包批处理
  （SSH P99 ≤30ms 线）、应用能耗账本、基准回归门、性能自检、触控板手势前瞻、
  音频低延迟链（单流 ≤20ms）、唤醒源治理（合盖 2h ≤2 次）、FS 日志策略、
  启动 IO 冷热分离（P99 <2ms）。
- **F068-F070 底盘后段三件**：渲染资产按需装载（单主题 ≤80MB、切换无白屏）、
  性能模式三档（静音/均衡/性能 12 旋钮 + F197 75℃ 强制静音、原子生效）、
  性能域总判据（四层门 + 全帧集合分位 P95 ≤12.5ms/P99 ≤16.6ms + 版本漂移防护）。
- **F071-F075 桌面体验五件**：开始菜单搜索直达（三类首结果 10/10 + 空查询
  短路）、最近使用引擎（frecency 毫倍定点 + 三消费面同源）、任务栏预览缩略图
  （应用分组卡列 ≤5 + 2Hz/1Hz 刷新降档 + 一致性对账）、跳转清单（F072 同源
  最近区 + 应用域固定集 + 静态任务组 + 键盘可达）、托盘系统（三件套 + 折叠区 +
  幽灵图标 30s 清道夫 + 滑杆 80fps 帧账闭环 + 点击 <100ms 闭环对账）。
- 自检判据逐条钉死：每项 `run_*_checks() -> CheckSet`，聚合全绿；施工期缺陷
  账本 40 项修复记录与行数对账（含低于 90% 目标项如实登记）见
  `_attic/aik2-f058-f075/行数对账与缺陷账本.md`。

## [Unreleased] — VARIX 三体系统：M5 基建批收口（2026-09-22：S4.2 MSC + exFAT 受限直写 + last_boot 闭环 + AHCI + ushell 真壁纸）

**内核（kernel/varix）**
- **S4.2-A xHCI MSC 传输层**（`drivers/msc.rs` 新文件）：BOT + SCSI 最小指令集
  （TUR/INQUIRY/READ CAPACITY/READ10/WRITE10/REQUEST_SENSE），`BulkPipe` 泛型协议层
  与 xHCI bulk 端点扩展（EP1 OUT/IN、传输环、事件容错）解耦；数据段"提前到达 CSW"
  识别覆盖设备端 stall 失败语义；协议级 + 控制器级模拟器 12 项测试全绿。
- **S4.2-B exFAT 受限直写层**（`fs/exfat_rw.rs` 新文件）：**扇区粒度**（从不物化整簇，
  真实 560GiB exFAT 的 128KiB 簇安全）——同尺寸就地改写 + 同簇增长改写（STREAM
  Size 字段单扇区 RMW 作提交点）+ 根目录新建单簇文件（数据→FAT→位图→目录项顺序，
  断电=孤儿簇可回收）；断电注入"不可见或完整"二态不变式入 ktest（8 项）。
- **last_boot 写回闭环**（方案2 根解）：`bootcfg::splice_last_boot` 文本级拼接 +
  `record_last_boot`（MSC 优先 / QEMU 第二 NVMe 兜底）+ 引导流挂钩；QEMU ×5 交替
  raw 断言通过；Windows 侧仍写 SHARED 真相源（单一事实源不变）。
- **S4.6 AHCI 最小栈**（`drivers/ahci.rs` 新文件，追加件）：单端口单槽 LBA48 纯轮询；
  PCI 0x0106 扫描；目标态探针默认只读，写回环须 cmdline `ahci_selftest=1`；
  模拟器 6 项全绿。ktest 全量 **3146/0** + kcheck 0。
- **ushell 真壁纸**：`wallpaper.rs` 经 Limine internal module 装载 1280×720 RGB565
  （三世界同源静态帧），最近邻整屏 blit + 削角逐像素真色回填
  （FRAME_WALLPAPER/PX/FILL_RGB 三子命令）；缺席=回退色带，绝不阻塞引导。
  取证：1.8MiB include_bytes 进 .rodata 引发 RW 段布局敏感 fatal #PF、Limine
  内模块**子目录路径挂死**（根级正常）——资产模块通道双戒律入库。
- S4.1 xHCI HID 维持"模拟器全绿、真机 SOP 权威"结论；WHPX 宿主不可用已登记。

**桌面端（src-tauri / src）**
- 门禁修复：`we_wallpaper` 抽取核存在性检查改可注入谓词（WE 库搬家后夹具路径
  失效引发的机器相关测试回归）；vitest 2851 / tsc 0 / variable --lib 348 / ca-core 356。

**文档与档案**
- `docs/acceptance/INDEX.md` 建档（AI-6 档案库索引）+ 三份新验收记录
  （ai5 S4.2 批 / ai4 真壁纸 / ai6 门禁基线）+ ai1 真机冷启动 ×10 SOP 执行卡。

## [Unreleased] — VARIX 双域系统：批次收官（2026-09-18：27/28 实机走查全绿 + 58 拔出全链 + 批次任务 29-87 清账）

**内核（kernel/varix）**
- **嵌入层走查 20/20 PASS**（任务 27/28，`_attic/p27-28-shell-demo.py`）：ushell ring3
  用户态桌面壳承载 BootScreen 回放→桌面→开始菜单→文件管理器（SHARED exFAT 真实数据源
  `source=shared-exfat` 实测）→设置页（KV 读写 rc=0）→关于，全程 HMP 键盘驱动+逐屏截图。
  修复三根因：syscall ABI r8-r11 保存恢复（ushell 用户态 #PF 致命）、`SHIM_VFS_SOURCE`
  漏接线（页脚数据源恒 demo-tree）、`RamBlk` 由 PMM order-11（运行时分配失败+双倍超额）
  改 static .bss 4MiB（分配不可能失败，符合大缓冲戒律）。
- **拔出全链**（任务 58，`_attic/p58-pull-chain-drill.py`）：SHARED 盘运行中移除 → 内核
  「IO 失效卸载」（`mount::uninstall_io_failed`，与 `mount::healthcheck` PCI vendor 探测
  双通道）→ 文件管理器数据源**如实降级** demo-tree（零冒充）→ RAM KV 独立照常 → 断电
  重插恢复挂载+KV 干净重建（**10/10 PASS**）。连带修复 ushell 设置页光标重进不归零
  （与开始菜单/文件页「页面重进=顶部」约定对齐）。q35 热拔插需 guest ACPI 配合的语义
  差异如实登记于报告 json。任务 60 机型代理矩阵首轮 3/3 PASS（q35 基线/双核/i440fx
  诚实降级）。
- **性能口径**（任务 71，`scripts/perf-gate-usb.py`）：boot_ms=896 / first_frame_ms=896
  （ushell 首画帧内核时钟打点 `SHELL: first-frame ms=`）/ interaction P95=51.7ms，
  三指标全 PASS + 基线建档（×1.2 非劣回归带）。
- 任务 52 ramcache（LRU+水位回收对接+关机清零断言，实机探针 verdict=ok）、61 winecaps
  （能力收敛+申请式授权+审计，探针 verdict=ok）、62 PE 拒绝表（50 合法变体零误拦/20 恶意
  全拒具名理由）随批交付；ktest 3026 绿 / kcheck 0 告警。

**前端（src/）**
- 任务 50/51/53/54 VWM 引擎流窗口+冷启动阶段化叙事+画质三档+异常三场景状态机
  （engineModel/engineSessions/vwm，engine-model.test 全绿）；任务 55 IME 键表全量
  （kernelInput 0..57 与内核同序同源）。
- 任务 57 设置页四组 UI（DualBootTab 引导行为/软件通道/白名单/资源档位 + EngineTab 引擎组）。
- 任务 76/78/82 新增逻辑核：`skeleton.ts`（E1 骨架屏统一规格，300ms 快路径不闪）、
  `dataMigration.ts`（I3 搬家四步状态机断点续走+I4 退役两档三轮覆写双确认）、
  `journeys.ts`（M1-M6 六条旅程跑查器，能力注入缺失=如实 skip）。
- 门禁：tsc 0 错；vitest 2787 passed / 4 skipped（200 文件，含新增 23 例）。

**文档与工具**
- 任务 64 威胁清单终审（15 条对策+残余风险双栏+诚实声明页同源）、67 灾备 SOP、
  68 配置一致性校验器（selftest 4 场景）、60 QEMU 代理机型矩阵首轮三配置
  （i440fx 无 ECAM 诚实降级路径验证）；任务表/推进总表三处状态同步。

## [Unreleased] — 第四轮实机 QA：交互缺陷清零（2026-09-15：R4-0001~R4-2000）

- **Ctrl+W 改绑「关闭当前虚拟窗」**（`src/system/desktop/DesktopShell.tsx`）：捕获阶段拦截，
  不再落到 WebView2 默认行为导致整体退出；输入框聚焦时仅拦截默认行为，组件级 Ctrl+W 继续生效。
- **Del+Backspace 输入聚焦豁免**（`src/App.tsx`）：`sys://quit-request` 在可编辑元素聚焦时忽略，
  杜绝文件操作中 Delete+Backspace 组合误触真退出（R2-S1 遗留建议落地）。
- **开始菜单搜索词跨开关残留清零**（`src/system/startmenu/StartMenu.tsx`）：菜单关闭时清空查询态（R3-B6）。
- **curtain 恢复 DPI 修复**（`src-tauri/src/shell/kbdhook.rs`）：hide→show 后按显示器物理分辨率
  重设窗口尺寸与原点（新增 `restore_fullscreen_size`），125% 缩放下不再缩为逻辑全屏（R3-B10）。
- **收编窗回桌面通道**（`src-tauri/src/shell/embed.rs` + `src/lib/ipc.ts`）：新增 `desktop_raise`
  命令，把桌面 WebView2 提到全部收编子窗之上（首轮 B-3）；`embed_visible(false)` 后强制 WebView
  重绘修复白屏（首轮 B-4）。
- **虚拟窗控制钮对比度**（`src/styles/vwm.css`）：非聚焦红绿灯降饱和 0.35→0.6 + 双层描边环（R3-B5）。
- 门禁：tsc 0 错；vitest 2646 passed / 4 skipped；cargo test 253 passed；vite build 成功。
  实机第四轮 QA 报告（2000 项全新清单）：`docs/Variable系统实机QA检查报告第四轮-2000项-2026-09-15.md`。

## [Unreleased] — Steam 全量收进 Variable（2026-09-15：steam:// 与快捷方式不再落宿主桌面）

- **`open_path` 识别 Steam 产物**（`src-tauri/src/system.rs`）：`steam://` 链接本体、
  以及内容指向 `steam://` 的 `.url` 快捷方式（Steam 桌面快捷键格式）不再走
  「宿主 Windows 默认程序」打开，改走 Steam 通道——CEF 兼容态 + ShellExecute +
  `spawn_steam_adopt_watcher` 收编看护，Steam 主窗与游戏窗收进 Variable 桌面运行。
- **公共通道 `steam_open_url`**（`src-tauri/src/shell/ecosystem.rs`）：
  `steam_launch` 与 `open_path` 的 Steam 路由共用同一裁判（兼容态 + 收编看护），
  并对非 `steam://` 前缀做防御性校验。
- **资源管理器双击 `.url` 先探测**（`src/system/explorer/ExplorerWindow.tsx`）：
  命中 Steam 快捷方式直接进收编通道，不再弹出「用宿主 Windows 打开」选择器；
  非 Steam 的 .url 与读取失败时保持原有流程不变。
- 门禁：tsc 0 错；vitest 2646 passed / 4 skipped；`cargo test -p variable --lib`
  system 模块含新增 `steam_probe` 测试全绿；vite build 成功。

## [Unreleased] — 1.0sno9u.vxe（2026-09-14 代码大检查与优化：零告警回归 + 前端按需加载 + 仓库卫生）> 本轮为"不改变功能语义"的质量/性能治理：三条测试线由改前改后均全绿，
> 交付物是**更小的首屏、更少的编译告警、更干净的仓库**。

### 性能：桌面环境首屏体积下降约 40%
- **四个重视图改为按需加载**（`src/App.tsx`、`src/system/windows/VwmAppContent.tsx`）：
  写作（EditorView）/ 思维导图（MindmapView）/ 项目分析（ProjectAnalysisView +
  CodeXrefPanel）/ 命运推演（FateView）此前被静态 import 打进环境主 chunk，
  而桌面分支（`view === "desktop"`）根本不渲染它们。改为 `React.lazy` + `Suspense`
  后只在用户真正打开对应窗口时拉取，实测主 chunk **2010.79 kB → 1257.37 kB**
  （gzip **723 kB → 433 kB**，−40%），拆出的 EditorView(342 kB) / MindmapView(101 kB) /
  FateView(101 kB) / ProjectAnalysisView(90 kB) / XrefPanel(4 kB) 变为独立 chunk。
  已验证这 5 个模块无顶层副作用，视觉/操作/功能行为零变化（改前改后 vitest 均
  2629 通过 / 179 文件 / 0 失败）。

### 质量：内核编译告警清零（kcheck + ktest 双零告警）
- **lib 目标 28 处告警全清**：`shell/overlay.rs`、`shell/tbengine.rs` 的测试专用
  `render_to_string` 下沉进 `#[cfg(test)] mod tests` 并移除随之无用的 `String/Vec` 导入；
  删除死代码 `compatruntime::clamp_usize`、`fs23_mount::link_count_of/link_count_pub`；
  移除 `WakeGovernor` 中从未被读取的冗余 `whitelist` 字段（生效白名单本就走
  `policy.whitelist() ⊕ overrides`，该字段是死状态）；清理 12 处多余 `mut` /
  未用变量 / 死语句。
- **修正一处"末态读取"式弱断言**：`fs/fs23_journal.rs` 的 X05533「重放幂等」原写成
  `t.verify_and_replay() == 1; t.verify_and_replay() == 0`，前一个比较结果被丢弃
  （编译器 `unused_must_use`），实际只校验了后半句。改为 `&&` 连接，断言恢复完整语义。
- **测试目标 16 处告警全清，并诚实化 6 处恒真断言**：`render2d`/`audio`/`motion` 中
  `c[p] > 255`（u8）、`s <= 32767`（i16）、`v <= 255`（u8）这类比较被类型系统恒真，
  属"假绿"断言。改为有实质意义的确定性/信号非零断言，或删除并注明类型已保证边界。
  测试数与通过数不变（kernel 2713 全绿）。

### 仓库卫生：非功能性过程产物归位 `_attic`
- **46 个一次性调试脚本移入 `_attic/archive/tools-process/`**（`git mv` 保留历史）：
  `dbg_*.py`（17）、`realclick*/clickcancel*/clickicon/enumwins/checkhung/cancel*` 等
  PowerShell 点击探针（9）、`mark-*/stage-*/done-*/revert-aurora/strip-landscape/
  fix-ellipsis/probe-regex/aurora-halfres/check-aurora/ghdata_push/make_cmds_async/
  debug_ctx_freeze` 等一次性批处理（20）。判定口径：在 `tools/` 与 `_attic/` 之外
  零引用，且不在 `package.json` / CI / `scripts/` 中出现。功能性工具（bench / audit /
  keymap-audit / qmon / gen-* / hooks / portable 校验器等 38 个）原样保留。
- **清理被 git 跟踪的 Python 字节码缓存** `tools/portable/__pycache__/*.pyc`，
  `.gitignore` 追加 `__pycache__/` 与 `*.pyc`。

### 质量：桌面端后端（src-tauri）编译告警清零（65 → 0，含 --all-targets）
- **28 处忽略 must_use 返回值的 Win32 调用**统一改为 `let _ = ...`
  （`SetParent` / `SetWindowPos` / `ShowWindow` / `PostMessageW` / `EnumWindows` /
  `RmEndSession` / `CloseHandle` / `CancelIoEx` / `CertFreeCertificateContext` /
  `SystemParametersInfoW` / `ActivateKeyboardLayout` / `save_index` 等）——行为完全等价。
- **修复单实例守卫的无效泄漏**（`shell/single_instance.rs`）：`std::mem::forget(h)` 作用于
  `Copy` 类型（`HANDLE`）是空操作，编译器已警告。改为把句柄存进进程级 `AtomicPtr` 静态，
  显式表达"句柄生命周期 = 进程生命周期"，且不随 windows-rs 对 `HANDLE` Drop 语义的
  版本差异而改变行为（守卫语义不变，双开防护仍生效）。
- **清理死代码**：`bplustree` 的 `is_empty`（仅测试期使用，加注保留）、`schema::Migrator`
  类型别名、`schema` 与 `versions`/`diagnostic` 的未用导入、`search::MAX_MATCH_LINES`
  常量、`git_panel::ce` 与容器单测里未用的 `blob` 辅助函数；`uxv::apply_remove` 的
  `let mut pos = 0; … pos = 4;` 死存储改为 `let mut pos = 4;`（语义等价）。
- **6 处"保留待接线/说明性"条目改为显式 `#[allow(dead_code)]` + 中文注释**，而非粗暴删除：
  `embed::ensure_per_monitor_dpi`（无清单直跑档兜底入口）、`envs::nested_env`（M6 嵌套
  实例执行档入口）、`exec::default_kind`（serde 默认值字符串引用）、`uxv::SnapshotState
  ::data_tail`、`openhub::GatewayHandle::thread`、`workshop::RuntimeCtx::boot_done_ms`。
  逐条注明"为什么留、谁来接线"，避免下一次检查再误判。

### 构建：`build-windows.bat` 打包修复（CRLF 检出 + 脚本加固）
- **根因（行尾）**：`.gitattributes` 的 `* text=auto eol=lf` 无差别地把**批处理**也强制
  检出为 LF。`build-windows.bat` 在工作区实测 **0 处 CRLF / 123 处裸 LF**，而 `cmd.exe`
  解析 LF-only 批处理时会在 `if ( )` / `for` 多行块处断言失败。该属性随
  「仓库清理」一并生效后，**任何全新克隆拿到的都是坏掉的 .bat**——这正是"无法正常打包"
  的直接原因。修复：`.gitattributes` 增加 `*.bat text eol=crlf` 与 `*.cmd text eol=crlf`
  的显式覆盖（gitattributes 优先级高于 `core.autocrlf`），工作区 `build-windows.bat`
  已重写为 **173 CRLF / 0 裸 LF / 0 非 ASCII 字节**，`git ls-files --eol` 复核为
  `i/lf w/crlf attr/text eol=crlf`。
- **脚本加固（行为与四种模式语义不变）**：
  - 移除两处 cmd 解析高危写法：`copy ... || ( ... )` 的**嵌套括号 + `||`** 组合、
    以及 `echo ... ^(data ...^)` 的**块内转义括号**（改为纯 ASCII 无括号措辞）。
  - 未知模式不再静默回落 NSIS，改为打印用法并 `exit /b 2`。
  - 新增前置校验：`node_modules\.bin\tauri.cmd` 缺失时给出明确指引（避免 `npx` 联网兜底）。
  - 后端测试由 `cargo test` 改为 `cargo test --workspace`，与仓库门禁口径一致
    （覆盖 `container` crate）。
  - 构建后新增产物校验与**绝对路径回显**（NSIS / MSI / 便携），产物缺失即 `exit /b 1`，
    不再出现"报 BUILD OK 但什么都没产出"。
  - 便携模式清理 `dist-portable` 失败时给出明确报错（原实现会静默继续）。
- **仓库卫生**：`.gitignore` 追加 `dist-portable/`（`portable` 模式的构建产物，此前未被忽略）。
- **验证**：打包链路已原生端到端验证——`npx tauri build --bundles nsis` 成功产出
  `src-tauri\target\release\bundle\nsis\Variable_1.0.0_x64-setup.exe`，故工具链无问题，
  缺陷确在 `.bat` 包装层。本轮未改动任何 Rust/前端源码，三条测试线基线不变。
  说明：本沙箱禁止调用 `cmd.exe`，`.bat` 本身未能在此环境实际执行，需真机双击复核。

### 验证（如实）
- `npx tsc --noEmit` 0 错；`npx vitest run` 2629 passed / 4 skipped / 0 failed；
  `cargo test -p ca-core`（C 线）356 passed；`cargo ktest --lib`（K 线）2713 passed；
  `cargo kcheck` 与 `cargo ktest --lib` 均 **0 warning**；`cargo kbuild` 成功出 ELF；
  `npx vite build` 成功（7 个 MPA 入口产物齐全）。
- 未在本轮改动任何业务逻辑、UI 样式与交互；未做真机/QEMU 视觉验收（无 GUI 环境），
  按仓库纪律如实标注：视觉效果需真机复核。

## [Unreleased] — 1.0sno9u.vxe（2026-09-08 窗口路 AI-2 收口：W-1…5 / C-1…8 / B-27 / D-3）

> 窗口路全部批次代码级完成。真机验收项见 `docs/acceptance/ai2-窗口路验收.md`（逐批清单）。

### 修复
- **cargo test 编译失败**：`container.rs` 单测引用已不存在的 `super::win` 路径（改 `super::`）、
  `embed.rs` 两处 `EmbedSession` 初始化缺 `capture` 字段（补齐）。修复后 AI-2 域单测全绿
  （`sysmaint.rs` 2 个失败属 AI-5 域，未越界）。
- **L4 误嵌入 bug**（批次C-5 语义）：嵌入主路径 tier 路由 `_` 兜底臂吞掉 L4 → 反作弊/独占全屏
  应用被错误强制重父级嵌入。补显式 L4 让位臂：`attached:false` + hint 归因 reason，
  让位由运行时看护承担；`_` 兜底仅剩 L1 主路径。

### 新增
- **C-8 嵌入验证**（主计划 7.8）：`portable/AI5/Compat-Matrix.ps1` 新增 `-Action Run-Embed / Report-Embed`——
  逐软件启动 → 窗口样式探测分层（决策树镜像 `compat_probe.rs`），记录 `{tier, captureMs, crash, inputOk, dpiOk}`；
  `inputOk/dpiOk` 诚实输出 `todo` 待真机人工回填，脚本不臆造。真 PowerShell 5.1 已验证 4 条路径。
- **C-5 让位归因 `compat.hint`**（主计划 7.5）：`CompatInfo` 增 `hint: fullscreen | anticheat`
  （serde default 平滑升级旧 apps.json）；探测器区分反作弊服务特征与独占全屏引擎窗口，
  供看护语义分流（anticheat 停用 kbdhook + 横幅）。单测 `l4_hint_classification` 覆盖。
  前端消费闭环：LauncherManager L4 徽标（AC/FS，悬停完整说明）+ i18n zh/en。
- **C-4 bench 项**（主计划 7.4）：`tools/bench.cjs` 新增 `captureE2E`（预算 P95 <50ms），
  按 vwmOpen 先例如实 SKIPPED 并写明真机测量协议；`--no-gui` 实跑报告落盘 `docs/bench/2026-09-08.md`。
- **D-3 契约单测**（`shell_watch.rs`）：白名单 15 类唯一小写、策略三态、默认「询问」不自动回收。
- **证据归档**：`docs/acceptance/ai2-窗口路验收.md`（14 批索引）+ `w2-dpi-regression.md`（混合 DPI 10 款清单）+ `compat-top200.md`（C-8 口径与执行命令）。

### 说明
W-1…W-5 / C-1…C-6 / B-27 / D-3 主体代码由本路前序会话落地（`embed.rs`/`container.rs`/`capture.rs`/
`compat_probe.rs`/`winman.rs`/`ecosystem.rs`/`shell_watch.rs` + 前端 `vwm.ts`/`snapshots.ts` 接线），
本轮完成收口：过期测试修复、C-8 工具段、契约单测、证据归档；附录 A 勾选按 DoD 留待真机实测。

## [Unreleased] — 1.0sno9u.vxe（2026-09-08 体验路 AI5 收口：F-1…F-7 / A-1…A-5 / D-5 / B-30）

> 体验路全部代码级任务完成。真机验收项见 `docs/acceptance/b30-三宿主验收矩阵.md`。

### 基础功能完备化（F）
- **F-3 任务管理器**：进程/性能/启动项/服务四页（`src-tauri/src/shell/taskman.rs` + `src/system/taskman/TaskManApp.tsx`），系统关键进程保护。
- **F-4 全局文件搜索**：容器内索引（`fsindex.rs`，1.2s 增量轮询）+ 拼音兜底 + 扩展名/类型/大小过滤，结果只在 VWM 资源管理器打开。
- **F-5 通知动作 + 音量合成器 + 输入法指示 + 媒体控制**（`audioime.rs`）：通知条目 2 动作按钮；IAudioSessionManager2 会话级音量；任务栏 IME 语言/中英态（切换仅作用于 Variable 进程）；SMTC 媒体进度条（探测不到自动隐藏）。
- **F-6 系统维护**（`sysmaint.rs`）：计划备份（daily/weekly+小时点、跨会话补偿）、本地更新包 SHA-256 校验+失败回滚（下载边界如实声明）、备份可恢复性抽检自检。
- **F-7 无障碍**：高对比度主题、`:focus-visible` 全局焦点环、字号 80–150%、`audit.cjs` 升级为 CI i18n 门禁（缺键/缺后端即 fail）。

### 界面精修（A）
- **A-1 Design Tokens**：`src/design/tokens.css`（OKLCH 语义层 + 三亮度层 + 字阶/间距/圆角/阴影/动效全 token），文档 `docs/DESIGN.md`；audit 增加裸色值计数。
- **A-2 动效**：窗口 spring 入场（4% 过冲+阴影先行）、最小化 5% 压扁、贴靠统一 170ms；`data-reduce-motion` 全局 80ms 降级。
- **A-3 状态设计**：共享骨架屏组件（任务管理器/音量合成器加载态）、`tools/visual-audit.cjs` 像素审计（puppeteer 缺失时如实退出）。
- **A-4 声音**：Web Audio 合成 6 音（启动/通知/闹钟/错误/贴靠/回收站），音量/静音独立设置，勿扰自动静音。
- **A-5 首次体验**：OOBE 收敛三步（隐私契约逐条确认 → 壁纸+5 套一键换装预设 → 布局偏好），≤90s。

### 边界与验收（D/B）
- **D-5 接管边界诚实清单**：README「21.5 能力诚实声明」+ 设置→关于页内置（UAC 安全桌面/直跑档事后回收/反作弊让位/VM 系统对话白名单/LogonUI）。
- **B-30**：三宿主验收矩阵文档收口（`docs/acceptance/b30-三宿主验收矩阵.md`），真机项如实待办。

## [Unreleased] — 1.0sno9u.vxe

## [Unreleased] — 1.0sno9u.vxe（2026-09-07 便携系统 AI-5 交付核：主计划第 11+12 章）

> 多 AI 并行拆分（`docs/PORTABLE_AI_SPLIT_PLAN.md`）的 AI-5 交付核落地。
> AI-3（第 6+7 章）、AI-4（第 8+10 章）此前已合入；本次补上测试验收与交付运维。
> 实现进度：主计划 12 章中 10 章已落地（1/2/3/6/7/8/9/10/11/12），仅余第 4/5 章（AI-2 隔离核）。

### CI 修复：PowerShell 脚本编码（windows-latest 首次真机解析）

> **结果：CI 已双绿**（`backend` + `frontend`，run 34094563831），
> `Run-PortableTests.ps1` 在真 PowerShell 上 **74 项检查全过**。
> 首次真机执行共暴露 5 个真实缺陷（下表 1 个编码 + 4 个运行时），全部已修复。
> 明细见 `docs/AI5-测试交付.md` §4.1。

- **根因**：`portable/**/*.ps1` 全部是「UTF-8 无 BOM」。Windows PowerShell 5.1 对无 BOM
  文件按系统 ANSI 代码页（CP1252）解码，而汉字「应」的 UTF-8 末字节是 `0x94`，
  在 CP1252 中正是 `U+201D ”`；**PowerShell 把智能引号当作字符串定界符**，于是字符串被
  提前闭合，后面的 `)` 失去配对的 `(`，报 `Missing closing ')' in expression`。
  CI 报错的 `Run-PortableTests.ps1:160/168` 两行，其上一行恰好都含「应」字。
- **修复**：为全部 26 个 `.ps1` 加 UTF-8 BOM，使 PS 5.1 与 PS 7 都按 UTF-8 解码。
  同时把 `Run-PortableTests.ps1` 的 18 处反引号续行合并为单行，消除续行的额外脆弱性。
  注：BOM 变更也落到 `AI1/`、`AI4/` 的文件上——这是字节级编码前缀，不改动任何逻辑，
  属跨核同步必需的修复。
- **排障改进**：`portable.test.ts` 原先只保留最后一个 shell 的错误（`lastErr` 被覆盖），
  导致 `pwsh`(7) 的真实报错被 `powershell`(5.1) 的报错顶掉。现改为逐个 shell 记录并全部输出。
- **检查工具**（均已入库 `tools/portable/`）：新增 `ps_lex_check.py` 真正的 PowerShell 词法器（注释/单双引号/here-string/
  反引号续行/`$( )` 子表达式/智能引号定界）。旧的 `ps_struct_check.py` 只是括号计数器，
  曾对本缺陷给出 26/26 通过的误报；新词法器可复现该缺陷（修复前 3 处错误，修复后 0 处）。
- **运行时缺陷 2／数组 splatting 是位置绑定**：`& $p @ScriptArgs` 语法合法，但数组
  splatting 按**位置**传参，于是 `"-Action"` 这个字符串本身成了 `-Action` 的值
  （`The argument "-Action" does not belong to the set ...`）。改为**哈希表** splatting
  做命名绑定，开关参数映射 `$true`，`[int]` 参数去引号，共 16 处。
- **运行时缺陷 3／`Write-Host` 走信息流**：各脚本统一用 `Write-Ai5 -> Write-Host`，
  输出在信息流(6) 上，而 `2>&1 | Out-String` 只并入 stderr，捕获结果恒为空，
  导致所有 `-ExpectText` 断言必然失败。改用 `*>&1` 合并全部输出流。
- **运行时缺陷 4／变量名大小写不敏感**：`Accept-Gate.ps1` 的脚本级 `$Evidence`
  （证据根目录）与循环内每行的 `$evidence`（结论字段）在 PowerShell 里是**同一个变量**，
  循环开头 `$evidence = ""` 会把 `$Evidence` 一并清空，随后 `Join-Path $Evidence ...`
  收到空串。脚本级变量改名 `$EvidenceDir`。
- **运行时缺陷 5／Mandatory 数组逐元素校验**：`Save-Ai5Text` 的
  `[Parameter(Mandatory = $true)][string[]]$Lines` 会校验每个元素非空，而验收报告的
  Markdown 本就含空行（`$L += ""`）。加 `[AllowEmptyString()]`。
- **新增检查工具** `tools/portable/ps_case_collision_check.py`：扫描同一脚本内仅大小写不同的变量名
  （函数参数新建作用域，默认排除）。对修复前的 `Accept-Gate.ps1` 能报出风险并退出 1，
  对修复后的全仓 26 个脚本退出 0。

### 测试与验收（第 11 章）

- **兼容矩阵 Top200**：`portable/AI5/Compat-Matrix.ps1` + `Data/compat-matrix.json`，
  办公/设计/开发/工具/游戏 各 40 条、名称全局唯一。判定枚举仅 `pass/warn/fail/todo`，
  其中只有主计划 11.1 与 16.2 点名过的 14 条带既有结论，**其余 186 条保持 `todo` 待真机回填**，
  不因软件知名就打 ✅。`Run` 只测 `Data\Apps` 下能定位到主程序的条目，找不到记 `skip`。
- **混沌工程 12 场景**：`Chaos-Inject.ps1` + `Data/chaos-scenarios.json`（扩充 21 的 10 个必测场景
  + 主计划 11.2 的看门狗与 `0x80000003` 两项）。`0x80000003` 由 `[Diagnostics.Debugger]::Break()`
  在一次性子进程内真实触发。
- **性能基线**：`Bench-Perf.ps1`（顺序 / 4K 随机 / 冷热启动 / 内存）+ `-Action Gate` 门禁；
  报告归档 `docs/bench/2026-09-07-portable.md`（加 `-portable` 后缀，避免与 `tools/bench.cjs`
  的前端基线同名互相覆盖）。
- **验收门禁**：`Accept-Gate.ps1` 14 项（主计划 1.3 + 扩充 28 的 7 项全部纳入并扩展）。
  状态只有三种来源：自动脚本实测 / 人工实测录入 / 明确 `todo`。

### 交付与运维（第 12 章）

- **四阶段编排**：`Deploy-To-USB.ps1 -Action Preflight/Stage1..4/Verify/All`，复用 AI-1 的
  `Create-VHDX.ps1` 与 `Test-VM.ps1`，不复制造盘逻辑。
- **运维**：`Maintenance.ps1 -Action Status/Optimize/Backup/Restore/Schedule/Tune`，
  月度 `Optimize-VHD`、每日 `User.vhdx` 备份保留 3 份、一键还原、计划任务、调优清单。
- **联调**：`AI-Integration.ps1` 核验 AI1-5 交付物齐套性；AI-1/AI-2 尚未交付时如实标红，不代做。

### 安全加固（写进代码，不只是文档承诺）

- 危险场景（拔盘 / 宿主蓝屏 / 虚拟机内删 C 盘 / 驱动回退）标 `manual`，脚本**只出步骤卡不代为执行**；
  自检会把「`dangerous=true` 却是 `auto`」判为失败。
- 填盘演练默认 dry-run，真写需 `-AllowFill` 且受 512MB 上限 + `finally` 自动清理保护。
- 看门狗演练只杀脚本自己启动的一次性子进程，不枚举用户进程。
- 目标盘安全闸：拒绝宿主系统盘、拒绝固定磁盘、拒绝非法文件系统。
- 上盘用 `robocopy /E` 而非 `/MIR`，不镜像删除目标盘已有文件。
- 一键还原先把旧 `User.vhdx` 改名保留再覆盖。

### 验证接线

- 本会话的 GitHub App 令牌无 `workflows` 权限，改不了 `.github/workflows/ci.yml`
  （推送被远端拒绝）。但现有 CI 的 `frontend` 作业本来就在 `windows-latest` 上跑 `npm test`，
  于是把自检挂进 vitest：`portable/AI5/__tests__/portable.test.ts`
  —— `win32` 上真调 `pwsh`/`powershell` 执行 `Run-PortableTests.ps1`，非 Windows 明确 skip。
  **不需要新增 CI 作业即可在真 PowerShell 上验证。**
- `Run-PortableTests.ps1` 三段：官方 AST 解析全部 `.ps1`（含 AI-4 的 9 个）+ 数据不变量
  + 只读动作执行与一个负向用例（`Verify` 指向宿主系统盘必须判失败）。

### 验证结果（如实）

- 本地可跑的全部通过：21 个 `.ps1` 结构完整；21 项数据不变量；21 个脚本的函数调用 /
  `-Action` 名 / dot-source 路径交叉引用一致（含 143 处文档引用）；
  仓库自有检查 `tsc --noEmit` 通过、vitest 231 全绿、`tools/audit.cjs` 通过。
- 断言经过反向验证：向 `Data/*.json` 注入 4 个真实缺陷（删条目 / 假勾 pass / 危险场景改 auto /
  改 6s 预算）后，6 条断言如期失败；恢复后重新全绿。
- **本会话未在 PowerShell 上执行过任何脚本**：沙箱为 Linux，PowerShell 二进制下载域名被网络策略
  阻断（仅 npm/PyPI 可达）。PowerShell 语法与运行时由 PR 上的 `frontend` 作业在 windows-latest 验证。
- 真机验收 14 项全部保持 ⬜：无 Windows 宿主、无 1TB 目标盘、无 5 台测试机。

## [Unreleased] — 1.0sno9u.vxe（2026-09-07 便携系统 AI-1 存储核：主计划第 3+9 章）

> 多 AI 并行分工（`docs/PORTABLE_AI_SPLIT_PLAN.md`）的存储核批次；
> 详细实现说明见 `docs/AI1-存储.md`，压测口径见 `portable/AI1/Bench.md`。

### portable/AI1 存储脚本（5 支）

- **`Create-VHDX.ps1`**：1TB 1000MB/s 定版造盘 —— 固定 150GB VHDX + GPT + NTFS 64KB 簇 +
  4K 对齐校验 + Data 七目录 + 离线 CompactOS 与注册表调优（关 SysMain/WSearch/DiagTrack、
  开 LongPaths、关 LastAccess）；`-Sparse` 稀疏固定盘、`-Chain` 三级差分链（Base→Apps→User）；
- **`Tune-Guest.ps1`**：系统内一次性调优，逐项回读实测值 —— CompactOS always、TRIM、
  关休眠、待机/硬盘超时置 0、WinSxS `ResetBase`、RAM 盘缓存契约 `ramcache.json`；
- **`Link-DataApps.ps1`**：3.2 读写分离 —— 按 `linkmap.json` 建/校验/搬迁/卸载符号链接，
  幂等；探测 Data 卷文件系统，exFAT 自动回退 SymbolicLink（Junction 需 NTFS 目标卷）；
- **`Bench-Storage.ps1`**：9.1 选盘判定 —— 顺序写（WriteThrough 直写）/ 顺序读 /
  4K 随机（IOPS）/ 簇大小 / 4K 对齐 / TRIM / VHDX 膨胀率与碎片，一键出 Markdown 报告；
- **`Maintain-VHDX.ps1`**：每月维护 —— `Optimize-VHD -Mode Full` + 宿主卷 ReTrim +
  碎片率 <5% 体检 + 可选 `Merge-VHD` 扁平化。

### 复核发现并修正

- 分工表容量口径算错：1TB 实得 929.9GiB，「150GB + 800GB」超 20.1GiB → 定版 **Data 780GB**；
- 原型脚本 `-Filter "*.wim"` 会先命中 `sources\boot.wim`（字母序在前）→ 改 `install.wim`；
- 「回收后 <20GB 实占」对普通固定盘不成立（实占恒 = 虚拟大小）→ 新增 `-Sparse` 路径；
- 扩充 20.1 脚本 8 处修正：引号传参 / 返回码校验 / 分区复用 / `boot.wim` 误选 /
  4K 对齐 / 簇大小回读 / `Index` 参数化 / try-finally 卸载。

### 验证

- CI（windows-latest）`frontend` + `backend` 全绿；
- PowerShell 脚本经词法级结构检查（括号/引号/here-string/`$()` 配对）5/5 OK，
  基线：仓库既有 4 支 `.ps1` 同样 OK（确认无误报）；
- **如实边界**：沙箱为 Linux 且 `pwsh` 不可安装，PowerShell 解析器与 Windows cmdlet
  （`New-VHD`/`dism`/`fsutil`/`powercfg`）一次未执行，真机数据待 1TB 盘到货补入
  `Bench.md` 第 4 节（未编造任何压测数字）。

## [Unreleased] — 1.0sno9u.vxe（2026-09-06 实机反馈会话 2：原生图标 + 壁纸预览回退）

> 实机使用反馈第二轮（H1 家用机）。

### 环境内图标与 Windows 一致

- **exe 内嵌图标提取**：`icon_dataurl` 此前只认 `.ico/.png` 文件，第三方 exe
  登记项永远是通用占位图标（实机截图：Blender 显示绿块）。现链路
  SHGetFileInfoW(HICON) → GetIconInfo → GetDIBits(32bpp BGRA→RGBA，
  无 alpha 图标兜底不透明) → **手写 PNG 编码器**（CRC32 + stored deflate +
  Adler32，零新依赖）。前端 `fillNativeIcons` 流程不变，登记即自动补真图标；
- 顺手修复 launcher.rs 中 `"runas\x00"` 裸 NUL 字节导致的二进制误判（改为
  转义写法，文件恢复文本可维护）。

### scene 壁纸本地打开不再报错

- **预览图回退扫描**：scene 项目常缺 preview.jpg（实机三连报错）。现回退扫描
  项目目录取最大图片（WE scene 的 textures 贴图几乎必有图片，跳过 <20KB
  小图标，递归限 4000 项防拖慢）——绝大多数 scene 项目可直接本地静态渲染；
  真正一张图都没有的项目仍如实报错（不伪造）。

### 验证

- `cargo test` 79 全绿（65+7+7，含新增 PNG 编码器与预览回退 2 个单测）；
- `tsc --noEmit` 无错误；`tools/audit.cjs` 233/237 全对齐。

## [Unreleased] — 1.0sno9u.vxe（2026-09-06 实机反馈会话：壁纸三修复 + 小项三清）

> 实机使用反馈修复（H1 家用机），壁纸路径全部本地化收口。

### 壁纸实机修复（H1 反馈）

- **scene/应用型壁纸全部本地打开**：点击不再调 `wp_engine_open` 拉起 Wallpaper
  Engine 本体 + 隐藏窗口，改为项目 preview.jpg 本地图片壁纸渲染（无预览图如实
  报错）；三种语言词条同步（wpEngineLocal / wpEngineNoPreview）；
- **「系统桌面（Wallpaper Engine）」模式下线**：该模式渲染纯黑并让位隐藏，WE 未
  接管时整屏黑屏——右键菜单与设置页下拉均移除入口；历史设置残留 system 值由
  WallpaperLayer 兜底落入图片/媒体渲染路径，不再黑屏；
- **`wp_engine_open` 整链删除**（Rust 命令 + generate_handler + ipc.ts）：宿主侧
  不再启动 wallpaper64.exe，攻击面收窄。

### 快捷键双开防护

- 新增 `shell/single_instance.rs` 单实例守卫（命名互斥体 OpenMutexW，零新依赖）：
  双开时第二实例弹系统消息框并退出——此前双开导致全局快捷键整表注册全部失败
  （"注册失败（被系统或其他软件占用）"全量弹窗）的根因即在此。

### 小项三清

- **B-22 upstream 提示**：GitStatusView 新增 `has_upstream`，Git 面板在无
  upstream 时提示「在终端 git push -u 后显示 ↑↓」；
- **audit.cjs 双向 diff**：unusedBackend 此前已计算但从未输出——现打印
  `UNUSED BACKEND` 段（首跑即抓出 4 个历史冗余命令待人工确认）；
- **B-33 `--revoke-list` GUI 入口核实已存在**（设置页「吊销清单」按钮），清单项划掉。

### 验证

- `cargo test` 77 全绿（63+7+7）；`tsc --noEmit` 无错误；
- `tools/audit.cjs`：233 invoke 全有后端、237 命令全注册、无新增反向冗余。

## [Unreleased] — 1.0sno9u.vxe（2026-09-06 会话，M2…M9 + 辅助批次代码面全部收口）

> 状态仪表见 MASTER-PLAN 第 28 节：**33✅ / 1🟡（B-11 真机矩阵）/ 6☐（B-30 三宿主发版门禁 + 1.x 扩展生态）**。

### M2 容器与 8TB（B-12…B-17 + B-31）

- **Uxv 单文件容器**：自研格式 `[SuperBlock][追加区 chunk 记录][Footer 双副本]`——
  4MiB 定长切分 + BLAKE3 内容寻址去重 + 引用计数；B+ 树索引（oracle 差分测试）；
  read_range/stream 大文件通道；热 chunk 随机读实测远低于 20ms 口径；
- **掉电安全**：journal 事务（JNL1 魔数 + COMMIT）+ SuperBlock 指针原子发布 +
  重放撕裂截断——掉电注入 100 轮 0 数据丢失；
- **Schema 迁移协议**：惰性探测 / pre-migrate 快照 / 逐级升版 / 失败回滚 / 只升不降；
- **压缩与加密**：LZ4/Zstd-19 分级（不可压缩回 RAW）+ Argon2id/XChaCha20-Poly1305
  全容器加密（chunk/索引/journal 三层）+ 解码 LRU；
- **多卷条带与 GC**：主卷元数据 + 数据卷条带轮转（ChunkLoc.volume，schema v2 走迁移协议）；
  GC 标记/走查/最差卷单卷压实（停顿 <100ms）；
- **VHDX 快速档与迁移向导**：管理员/Mount-VHD 探测不可用如实降级 Uxv；
  数据目录→容器逐文件 BLAKE3 校验、失败源目录原样保留；
- **容器仪表与冷热分层**：水位线/写放大/规模统计 + freeze/unfreeze（冷层键空间分离）。

### M4 浏览器矩阵（B-18/B-19）

- 五款浏览器检测（App Paths + 路径兜底）+ 家族便携模板（--user-data-dir / -profile
  全指向容器）+ Profile 管理器（新建/克隆/焚毁删除）+ 设置页「浏览器」标签；
- 首次导入（书签 HTML / 密码 CSV 复制进容器，绝不读宿主浏览器运行数据）+
  任务栏按 profile 分组（pid 存活探测）。

### M5 大型编码体系（B-20…B-23）

- **VS Code Portable 一键部署**（离线 zip 降级 / data 模式全容器化 / 幂等登记）+
  嵌入启动复用整条 embed 通道；
- **工具链**：Python(embeddable)/Go(zip)/Rust(rustup 容器化 CARGO_HOME) 三通道 +
  PATH 统一注入（spawn_profiled 单点，冻结顺序）；
- **Git 只读面板**（git2：状态/分支/历史，仓库限容器内）+ **SSH 金库代理**
  （ed25519 生成封存 vault/ssh + GIT_SSH_COMMAND 注入，私钥不落宿主）；
- **并行全库搜索**（8 路线程池，跳依赖/二进制/>8MB）+ 大文件分块查看器 +
  PVCCE→VS Code `--goto` 行级跳转。

### M6 子环境系统（B-24…B-26）

- **环境档**：envs.json + envs/<id>/ 剖面 + `{envhome}` 执行档隔离（browsers/code/ai
  全部接入）——凭据与登录态按环境隔离；切换编排 4 步（快照/翻转/应用/重载）；
- **嵌套实例**（深度 ≤3）：独立数据根（VARIABLE_DATA_ROOT）+ 白名单继承接口 +
  嵌套回退独立窗口；**环境克隆**（剖面复制 + 设置快照）。

### M7 应用生态 2.0（B-27）

- 可移植性评估向导（目录可写/卸载注册表/本地配置三类启发式 → 绿黄红建议卡）+
  搬迁执行器（整拷 apps/ + portable.reg 快照 + 登记）；
- **Steam 库扫描**（libraryfolders.vdf + appmanifest）与 steam:// 协议直通 +
  商店 AUMID 启动 + 文件关联表（无关联宿主兜底打开）。

### M8 网络层（B-28）

- **环回 HTTP 代理**：CONNECT 隧道域名裁决 / 绝对式改写 / 默认拒绝（默认零出站落地）；
  受管进程经执行档自动注入 HTTP(S)_PROXY；
- 域名白名单规则库（子域通配 + 发起执行档记录）+ **kill-switch**（覆盖一切）+
  流量仪表（放行/拒绝/字节）；直连逃逸计数+标记（硬断网属防火墙，如实边界）。

### M9 安全分析工作台（B-29）

- 纯 Rust 手写 **PE 静态解析**（节表熵 >7.0 加壳标记 / 导入表 / 证书签名 /
  14 项可疑 API 命中 / 字符串提取）+ **iced-x86 入口反汇编只读查看器**；
- Windows Sandbox 探测与 .wsb 生成（样本映射只读+断网）+ 安全子环境预设
  （白名单清空 + kill-switch）+ Markdown 报告导出（不含样本字节）；
- 铁律：样本绝不在宿主执行。

### 辅助批次（B-31…B-35）

- **B-31** 容器 Schema 迁移协议（快照/升版/回滚，v1→v2 实走）；
- **B-32** OOBE 首次初始化向导（介质体检/口令建卷/三模板/60 秒导览）；
- **B-33** 应急能力包：CLI 四命令（--export-rescue / --repair / --force-raster /
  --revoke-list，无需 GUI 抢救容器）+ 紧急吊销清单导出 + 软件渲染开关（--force-raster
  → safeMode）；修复 residue_scan 前后端命令名不匹配的运行时必炸缺陷；
- **B-34** 诊断包导出（四节脱敏 Markdown，不含数据/凭据）+ 演示胶囊模板；
- **B-35** 发布合规：docs/false-positive.md（≤48h 闭环流程）+ docs/licenses.md
  许可清单归档。

### 验证基线

- cargo test --workspace **129**（container 52 含掉电注入 100 轮/代理端到端/手工 PE 构造）；
- vitest **218** / tsc 零错 / audit IPC+ i18n zh+en 全覆盖 / vite build 全绿；
- 历次自检归档：`docs/selfcheck/2026-09-06.md`（六个批次附录）。


### 已落地（2026-09-06 早段会话，M0 收口 + M1/M3 起步）


### 已落地（2026-09-06 会话，M0 收口 + M2 起步）

- **B-1 性能基准基线**：`tools/bench.cjs` 四项基准（冷启动/万文件索引/VWM 打开/
  内存水位，VWM 项如实 SKIPPED 待 GUI 插桩），首份基线归档 `docs/bench/2026-09-06.md`；
  `--check` 支持 >10% 回归门禁；
- **B-2 容器层骨架**：`src-tauri` 升级为 Cargo workspace，新建 `container` crate
  ——冻结 `StorageBackend` trait（蓝图 7.1 契约）+ `DirBackend`（原子写/路径沙箱/
  快照恢复）+ 8 项测试桩；
- 工程卫生：新增 `.gitattributes`（LF 归一化，消除 CRLF 幻影 diff）、清理未使用
  截图；本机补齐 Rust 工具链（rustup stable-msvc）。
- **M1 隔离执行档与凭据封存收口（B-3…B-6）**：受管进程一律经 `spawn_profiled`
  启动，HOME/凭据目录等环境变量强制重定向进容器（端到端测试证明）；apps.json v2
  （旧文件平滑升级）；五套重定向模板（Claude Code/Codex/ZCode/Git/Node）+ 设置页
  「执行档」标签（套用/编辑/干跑验证）；残留扫描器（会话差集，宿主零残留可验证）。
  详见 `docs/selfcheck/2026-09-06.md`。
- **M3 终端与云 AI 矩阵（B-7…B-11）**：AI Hub 面板（任务栏入口 + 三态卡片）；
  便携 Windows Terminal 经执行档+嵌入通道成为环境内终端（V1）；容器内 Node 运行时
  与 `ai install`（真实字节进度事件流，出站逐域授权）；Vault 2.0 账号身份库
  （Token 只存保险箱加密区）+ 多账号切换（配置目录 @label 隔离）；`ai verify`
  凭据零落宿主断言（真机矩阵待点验）。

### 已落地（2026-09-05 会话）

- **embed 嵌入子系统加固**：窗口捕获改为启动进程树匹配（Toolhelp32，覆盖
  Wallpaper Engine 等启动器型软件）、等待延长至 30s、超时绝不终止应用进程；
- **启动通道安全化**：`.lnk` 改 ShellExecuteExW（去 cmd 字符串拼接）、管理员运行
  改 ShellExecuteW runas（去 PowerShell 拼接）——仓库内已无 shell 字符串拼接；
- **按需求移除用户态守护子系统**（崩溃收尸/假死检测/Job Object 配额）及其前端
  事件面与文案；嵌入语义更新为"视觉与窗口层隔离，非沙箱"（如实边界）；
- **自检体系落地**：修复 audit.cjs 三处解析缺陷（子目录漏扫/属性断行/同行双键），
  补齐 25 中文 + 66 英文缺失 i18n 键，selfcheck 报告归档制度建立；
- **文档体系闭合**：BLUEPRINT（架构+契约+扩展协议+边界）、MASTER-PLAN（里程碑+
  状态仪表+闭合定义）、README（产品手册+文档索引）、CHANGELOG（本文件）；
- **CI**：GitHub Actions 五层验证栈（typecheck/vitest/cargo test/audit/build），
  push 即验证。

### 计划（按 MASTER-PLAN 里程碑）

- M1 隔离执行档与凭据封存 → M3 云端 AI 矩阵（Claude Code / Codex / ZCode 开箱）
  → M2 8TB 容器 → M4 浏览器矩阵 → M5 大型编码体系 → M6 子环境套娃
  → M7-M9 生态/网络层/安全分析工作台 → B-36..B-40 扩展生态。

## [1.0.0] — 2026-09-05

首个公开发布版本（详情见 README 各章）：

- 私人桌面环境：启动仪式（真实加载进度）、全屏壁纸层（静态/轮播/视频/网页/
  Wallpaper Engine 全类型接入）、桌面图标与文件架、Win11 风格任务栏与开始菜单、
  通知中心/快捷面板/硬件面板、回收站与文件管理器（VWM 内嵌）；
- VWM 虚拟窗口管理器：Z 序/贴靠/多开/最小化保活/几何持久化/红绿灯；
- 第三方软件：开始菜单扫描、便携化三级、原生图标提取、进程深度检测、
  环境内嵌入（进程树窗口捕获）；
- 四大空间：写作库（TipTap 富文本+xref 跨空间引用）、思维导图（八形节点/
  三线型/小地图）、项目分析 PVCCE（七级下钻/大白话词典/意图写回）、
  命运推演 FTPE（确定性种子/行为树）；
- 平台能力：全局快捷键（kbdhook 双 Esc 切环境、Del+Backspace 退出）、
  IM 未读提醒（只读窗口标题）、Wi-Fi/蓝牙控制、U 盘便携与拔出保护、
  保险箱/焚毁/备份恢复、三语（简/繁/英）+ 拼音搜索；
- 视觉：原生 WebGL 星空（Worker 管线）与 WebGL2 极光（Ray-Marching）；
- 发布物：NSIS / MSI / 便携目录；测试：Vitest 218 + cargo test 36 + 静态审计。
