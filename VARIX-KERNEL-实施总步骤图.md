# VARIX-KERNEL 实施总步骤图（完整版）

> 代号 **Varix**：为 Variable 打造的自研轻量内核。
> 终局形态：**Varix 内核 + Variable 系统 + Windows 三者并存**——Varix 为默认系统（开机直进），Windows 为可选环境，Variable 以三种身份全域存在。
> 配套文档：《VARIX-KERNEL-功能全景图》（功能清单）、《VARIX-KERNEL-AI分工完成图》（11 域分工与进度）。
> 状态标记：⬜ 未开始 / 🔶 进行中 / ✅ 完成

---

## 〇、Why——相比过去质变在哪

| 维度 | 过去（Variable 作为 Windows 应用） | 未来（Varix 并存体系） |
|------|------|------------|
| 主权 | 寄生在 Windows 上，受宿主规则约束 | 自有内核，规则自己定 |
| 算力 | 与 Windows 后台/遥测/Defender 抢资源 | Varix 运行期算力独占 |
| 隔离 | 七层隔离再强，理论上限被宿主锁死 | 隔离从 ring 0 开始，天花板是自己的 |
| 键位语义 | 快捷键与 Windows 抢注册 | 双键位双表在内核输入栈分流 |
| 可移植性 | 装机配环境痛苦 | 口袋级三系统：一块 U 盘即完整环境 |
| 故障边界 | Windows 蓝屏 = 全灭 | 三系统互为备份 |
| 隐私 | Windows 遥测关不干净 | Varix 无遥测，痕迹行为自定义 |

**核心结论：过去是给 Windows 做壳，未来是做操作系统本身；且是无损升级——既有资产全部保值平移。**

## 〇.一、并存总原则（什么都能保住）

三形态叠加，逐层演进、互不抛弃：

- **形态 A｜引导级并存**：引导菜单三项（Varix 默认｜Windows｜其他），共存一块盘，各自独立运行。
- **形态 B｜运行级并存（S7）**：Varix 为宿主内核，Windows 跑在 Varix 虚拟化层（hypervisor）里；Windows 窗口经"收养"嵌进 Variable 桌面（复用现有 embed 思想）。
- **形态 C｜全域同构并存**：Variable 同时以三种身份存在——① Windows 里的 Tauri 应用（现有工程，日常照用）；② Varix 上的原生系统（S6 产物）；③ Windows 客户机内的可运行环境。同一套语义（双键位表、算力策略、桌面布局）三环境行为一致。

**保值清单**：现有 Tauri/React 工程、VWM、embed 收养机制、isolation.rs 七层隔离语义、双键位双表设计、配置/壁纸/布局数据——全部平移，唯一的新造物是 Varix 内核本身。

## 〇.二、物理载体——1TB U 盘三系统布局

目标介质：1TB、1000MB/s（NVMe SSD + 硬盘盒，勿用 U 盘主控方案，持续写入会掉速）。

```
U盘\
├── EFI\Boot\BOOTX64.EFI      ← 引导菜单（512MB FAT32 EFI 分区）
├── varix\vmlinuz-varix       ← Varix 内核镜像
├── varix-sys\                ← Varix 系统分区（~64GB）
├── windows-wtg\              ← Windows To Go（~300GB，Rufus 写入）
├── variable-portable\        ← Variable 便携版（现有 portable/ 产物）
└── data\                     ← 共享数据区（配置/壁纸/布局快照，三系统可读）
```

优势：插上即三系统并存，拔掉主机零痕迹，完全满足"真机零风险"红线；1000MB/s 跑 OS 毫无压力（Varix 极轻，Windows WTG 最吃盘也绰绰有余）。

---

## 阶段总览

```
S1 内核原型 ──► S2 内存+调度 ──► S3 用户态 ──► S4 基础驱动 ──► S5 双系统引导 ──► S6 系统层移植 ──► S7 虚拟化并存
   (QEMU)        (QEMU)          (QEMU)      (QEMU→真机/U盘)   (U盘真启动)        (原生UI落地)       (形态B落成)
```

原则：
- **一切在 QEMU 中先行**，真机/U 盘验证只在 S4 之后引入，任何阶段不危及主机数据。
- **与现有工程解耦**：内核位于本仓库 `kernel/` 独立 Cargo workspace（`x86_64-unknown-none` target，编译期即隔离），不触碰 `src/`、`src-tauri/`。
- **概念平移**：VWM→S6 合成器窗口树；embed 收养→S7 Windows 客户机窗口嵌入；isolation.rs 七层隔离→Varix 用户态隔离蓝本；双键位双表→S6 输入语义。

---

## S1 内核原型 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 1.1 | `kernel/` workspace 搭建：`no_std` + `#![no_main]`，custom target，Limine 引导 | QEMU 启动到内核入口 |
| 1.2 | 帧缓冲输出：字模渲染、print! 宏、日志分级 | 屏幕输出彩色日志 |
| 1.3 | GDT/TSS 装载 | 加载无异常 |
| 1.4 | IDT + 异常处理（Page Fault 现场转储） | 触发异常可打印寄存器现场 |
| 1.5 | PIC→APIC 时钟中断 | tick 日志稳定 |
| 1.6 | PS/2 键盘输入 | 按键回显 |

**里程碑 M1**：QEMU 开机 → 内核横幅 → 键盘回显，最小可交互内核。

## S2 内存管理 + 调度器 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 2.1 | 物理页帧分配器（Bitmap/Buddy） | 分配/回收压测通过 |
| 2.2 | 内核堆 + GlobalAlloc（slab） | 压测无泄漏 |
| 2.3 | 分页：高半区、按需映射 | 保护生效 |
| 2.4 | 上下文切换 | 双内核线程交替 |
| 2.5 | 抢占式调度（优先级+时间片） | 多线程轮转可观测 |
| 2.6 | SMP 探测（本期只探测） | 日志列出全部逻辑核 |

**里程碑 M2**：多任务并发内核。

## S3 用户态进程 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 3.1 | Ring 3 切换 | 用户态程序跑通 |
| 3.2 | Syscall 接口（号表框架） | write/exit/yield 号可用 |
| 3.3 | ELF64 加载器 | 静态链接 ELF 执行成功 |
| 3.4 | init 进程 + 进程树 | init 拉起子进程 |
| 3.5 | 崩溃隔离 | 段错误进程被回收，内核存活 |

**里程碑 M3**：用户程序可运行，单进程崩溃不拖垮内核。

## S4 基础驱动 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 4.1 | AHCI/SATA | QEMU 测试盘读写 |
| 4.2 | NVMe | 真机盘识别+读写 |
| 4.3 | VFS + 只读文件系统（FAT/exFAT 或 ext2 只读；可直读 NTFS 数据分区） | 用户态可读文件 |
| 4.4 | xHCI USB → USB 键鼠 | 真机 USB 键盘可用 |
| 4.5 | GPU 帧缓冲模式设置 + 双缓冲 | 高分辨率无撕裂 |
| 4.6 | HPET/TSC 高精度计时 | 微秒级稳定时钟 |

**里程碑 M4**：**U 盘 Live 启动 Varix**——键盘可输入、可读盘、高分辨率显示。此阶段起接触真实硬件，全程只读挂载。

## S5 引导管理与三系统并存（形态 A） ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 5.1 | 引导菜单：Varix（默认倒计时）｜Windows To Go｜主机 Windows 检测项 | 三项可切 |
| 5.2 | BCD 共存安装方案（不破坏既有引导） | 互不覆盖 |
| 5.3 | Varix 内"重启到 Windows"接口（引导期变量传递） | 一键切换 |
| 5.4 | 1TB U 盘分区部署器（EFI/Varix/WTG/便携版/数据五区一键布局） | 部署脚本产出 |
| 5.5 | 回退保障：快照脚本 + Windows 修复介质 | 任意时刻可回 |

**里程碑 M5**：U 盘开机默认进 Varix，三系统随意切换，始终留回退通道。

## S6 Variable 系统层移植 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 6.1 | 原生合成器：窗口树、脏区合成、输入分发（文字 UI → 图形窗口 → 动效，分代际爬） | 原生窗口系统跑通 |
| 6.2 | Variable 桌面语义移植：桌面/任务栏/VWM 三区布局重建 | 三区布局可用 |
| 6.3 | 双键位双表：主系统表 + Windows 切换态表，焦点平面路由 | 同组合键按平面正确触发 |
| 6.4 | 算力独占调度：前景 Variable 任务全量优先 | 策略生效可测 |
| 6.5 | 条件切换引擎：预设条件触发 Windows 会话切换（经 S5 接口） | 条件触发成功 |
| 6.6 | Live 镜像 + 安装器打包 | 一键镜像产出 |

**里程碑 M6**：Varix 上运行 Variable 桌面——"Variable 系统"正式成形（形态 C 第②身份落地）。

## S7 虚拟化并存层（形态 B） ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 7.1 | 硬件虚拟化探测（VT-x/AMD-V）与最小 hypervisor 框架 | 客户机空载跑通 |
| 7.2 | Windows To Go 作为客户机引导 | Windows 在 Varix 内启动 |
| 7.3 | 客户机窗口收养：Windows 窗口嵌入 Variable 桌面（embed 思想平移） | 窗口可交互 |
| 7.4 | 算力分域调度：宿主优先、客户机限额 | 策略生效 |
| 7.5 | 快照/休眠：Windows 客户机状态保存与恢复 | 挂起恢复无损 |

**里程碑 M7**：Varix 宿主 + Windows 客机同时运行，Windows 窗口如普通应用般嵌在 Variable 桌面里——**三者真正同时并存**。

---

## 里程碑链

```
M1 最小可交互内核 ─► M2 多任务内核 ─► M3 用户态内核 ─► M4 U盘Live内核 ─► M5 三系统引导并存 ─► M6 Variable系统成形 ─► M7 宿主+客户机同时运行
```

## 风险与红线

1. **数据安全红线**：S4 之前不碰真机；S5 真机/U 盘部署前必须快照 + Windows 修复介质；写盘默认只读演练。
2. **介质红线**：U 盘必须 NVMe SSD + 硬盘盒方案，禁止 U 盘主控（持续写入掉速）。
3. **兼容性风险**：驱动是最大变数，每类驱动保留 QEMU 虚拟设备与真机双轨测试。
4. **UI 迁移风险**：WebView 无法上内核；S6 原生合成器体量最大，允许分代际爬。
5. **工程边界**：任何 AI 不修改 `src/`、`src-tauri/`；只新增或修改 `kernel/` 与本组 MD 文档。
6. **代码位置**：代码在仓库 `kernel/`，成品在 U 盘 EFI 分区（`EFI\Boot\` + `varix\`）。
