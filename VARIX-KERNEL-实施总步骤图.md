# VARIX-KERNEL 实施总步骤图

> 目标：为 Variable 打造自研轻量内核（代号 **Varix**），最终形成"Variable 为默认系统、Windows 为可切换第二系统"的真双系统格局。
> 本文件是总路线图；功能清单见《VARIX-KERNEL-功能全景图》，人力分工见《VARIX-KERNEL-AI分工完成图》。
> 状态标记：⬜ 未开始 / 🔶 进行中 / ✅ 完成

---

## 阶段总览

```
S1 内核原型 ──► S2 内存+调度 ──► S3 用户态 ──► S4 基础驱动 ──► S5 双系统引导 ──► S6 Variable 系统层移植
   (QEMU)        (QEMU)          (QEMU)         (QEMU→真机)      (真机双启动)        (原生 UI 落地)
```

原则：
- **一切在 QEMU 中先行**，真机验证只在 S4 之后逐步引入，任何阶段都不允许危及现有 Windows 数据。
- **与 Variable 现有工程解耦**：内核位于本仓库 `kernel/` 独立 Cargo workspace，不触碰 `src/`、`src-tauri/`。
- **概念平移**：阶段一/二（接管式双系统、双键位表）的语义设计直接作为 S6 的设计稿。

---

## S1 内核原型 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 1.1 | `kernel/` workspace 搭建：`no_std` + `#![no_main]`，`x86_64-custom-kernel` target，Limine/bootloader 引导 | QEMU 启动到内核入口 |
| 1.2 | 帧缓冲（Framebuffer）输出：字模渲染、print! 宏、日志分级 | 屏幕输出彩色日志 |
| 1.3 | GDT/TSS 装载 | 加载无异常 |
| 1.4 | IDT + 基础异常处理（Page Fault 等 panic 通道） | 故意触发异常可打印寄存器现场 |
| 1.5 | PIC→APIC 时钟中断 | 计时器 tick 日志 |
| 1.6 | PS/2 键盘轮询/中断输入 | 按键回显到屏幕 |

**S1 里程碑**：QEMU 中开机 → 打印内核横幅 → 键盘输入回显，形成一个"最小可交互内核"。

## S2 内存管理 + 调度器 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 2.1 | 物理内存管理（Bitmap/Buddy，来自 Limine memory map） | 分配/回收压力测试通过 |
| 2.2 | 内核堆 + GlobalAlloc（如 linked_list_allocator → 自研 slab） | 堆压测无泄漏 |
| 2.3 | 分页：内核高半区映射、按需映射框架 | 自映射 + 保护生效 |
| 2.4 | 上下文切换（保存/恢复寄存器态） | 两个内核线程交替运行 |
| 2.5 | 抢占式调度器（优先级 + 时间片） | 多线程时间片轮转可观测 |
| 2.6 | SMP 探测（CPUID/AP 启动）——本期只探测，调度可后置多核 | 日志列出全部逻辑核 |

**S2 里程碑**：内核多任务并发运行，内存子系统可承载后续驱动与用户态。

## S3 用户态进程 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 3.1 | Ring 3 切换（iret/sysret 进入用户态） | 用户态程序跑通 |
| 3.2 | Syscall 接口（syscall/sysret + 号表框架） | write/exit/yield 三个原型号 |
| 3.3 | ELF64 加载器（含内存镜像布局） | 加载并执行静态链接 ELF |
| 3.4 | init 进程 + 进程树（fork/exec 原型或 spawn 模型） | init 拉起子进程 |
| 3.5 | 崩溃隔离：用户态异常 → 进程回收，内核存活 | 故意段错误的进程被回收 |

**S3 里程碑**：内核可加载并运行用户程序，单进程崩溃不拖垮内核。

## S4 基础驱动 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 4.1 | AHCI/SATA 块设备读写 | QEMU 测试盘读写通过 |
| 4.2 | NVMe 驱动（真机主力盘） | 真机 NVMe 识别+读写 |
| 4.3 | VFS 抽象层 + 只读文件系统（先 FAT/exFAT 或 ext2 只读） | 用户态可读文件 |
| 4.4 | xHCI USB 主机控制器 → USB 键鼠 | 真机 USB 键盘可用 |
| 4.5 | GPU 帧缓冲分辨率模式设置 + 双缓冲 | 高分辨率显示无撕裂 |
| 4.6 | HPET/TSC 高精度计时 | 稳定微秒级时钟源 |

**S4 里程碑**：真机上（U 盘 Live 介质）启动 Varix，键盘可输入、可读盘、高分辨率显示。**此阶段起才允许接触真实硬件，且全程只读挂载。**

## S5 双系统引导管理器 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 5.1 | 引导菜单：Varix 默认项 + Windows 检测项，默认倒计时进 Varix | 双项可切换 |
| 5.2 | Windows BCD 共存安装方案（不破坏既有引导） | 两系统互不覆盖 |
| 5.3 | Varix 内"重启到 Windows"接口（引导期变量传递） | Varix 内一键切 Windows |
| 5.4 | 回退保障：Windows 引导修复盘 + 安装前快照脚本 | 任意时刻可回 Windows |

**S5 里程碑**：真机开机默认进 Varix，需要时切换进 Windows，且始终留有回退通道。

## S6 Variable 系统层移植 ⬜

| 步骤 | 内容 | 验收标准 |
|------|------|----------|
| 6.1 | 原生合成器：窗口树、脏区合成、输入分发（替代 WebView 的 Rust 原生 UI 运行时） | 原生窗口系统跑通 |
| 6.2 | Variable UI 概念移植：桌面/任务栏/VWM 语义在原生运行时重建 | 三区布局可用 |
| 6.3 | 双键位双表：主系统表 + Windows 切换态表，焦点路由 | 同组合键按平面正确触发 |
| 6.4 | 算力独占调度：Varix 运行期调度策略全量偏向 Variable 前景任务 | 策略生效可测 |
| 6.5 | "条件切换"：满足预设条件时唤起 Windows 侧会话（经 S5 接口） | 条件触发切换成功 |
| 6.6 | 打包：Varix Live 镜像 + 安装器（含 Windows 并存安装向导） | 一键安装镜像产出 |

**S6 里程碑**：Varix 上运行 Variable 桌面——"Variable 系统"正式成形。

---

## 里程碑时间关系图

```
M1 最小可交互内核(S1) ─► M2 多任务内核(S2) ─► M3 用户态内核(S3) ─► M4 真机 Live 内核(S4) ─► M5 真双系统(S5) ─► M6 Variable 系统成形(S6)
```

## 风险与红线

1. **数据安全红线**：S5 真机安装前必须出快照脚本 + Windows 修复介质；所有写盘操作默认只读演练。
2. **兼容性风险**：驱动是最大变数；每类驱动保留"QEMU 虚拟设备"与"真机物理设备"双轨测试。
3. **UI 迁移风险**：WebView 无法在内核上运行，S6 的原生合成器是整个计划中体量最大的未知数，允许其内部再拆代际（先文字 UI → 再图形窗口 → 再动效）。
4. **工程边界**：任何 AI 不修改 `src/`、`src-tauri/`；只新增或修改 `kernel/` 与本组 MD 文档。
