
## AI-S1 批次三深化（F171-F185）
- 形态：15 个独立批次文件 `kernel/varix/src/secstar/*_b3.rs`，不动已收口主层；批次三检查层经 `CheckSet::merge` 并入 `run_secstar_deep_checks`。
- 量：+5,183 行 → 域总量 16,249 / 36,725（44.2%）；检查项 344 → 527；单测 84 → 130 全绿；no_std 零警告；主仓 cargo check secstar 零错误。
- 修复 21 条伤账（2 条 🔴：`0x80>>col` 移位溢出、碎片率 u64 溢出→u128）——全部记录在 `docs/AI-S1-行数与检查项对账.md` §6.4。
- 纪律沉淀：b3 模块只依赖主层 pub API；fixture 数值必须先核主层常量语义（band_of 量的是已用占比不是剩余——这次踩了语义倒置）。
- 下批方向（v4）：向 36,725 上限继续深化——优先 low 达成率模块（signbadge 46.9%、diskhealth 42.2%、handoffchk 41.2%）。

## AI-S1 批次四深化（F171-F185）
- 形态：15 个 `*_b4.rs` 批次文件（批次三管「账与快」、批次四管「跑与环」）；深化聚合器改三层 merge（deep+b3+b4）。
- 量：+5,254 行 → 域总量 21,482 / 36,725（58.5%）；检查项 527 → 711；单测 130 → 175 全绿；no_std 零警告；主仓 cargo check secstar 零错误。
- 伤账 9 条自抓自修：时间线 rev() 扫描序颠倒、路径映射斜杠规范化缺失、no_std 层 Vec 违约等——全记 `docs/AI-S1-行数与检查项对账.md` §7.4。
- 经验：环回读 newest-first 用正向 i + (head+CAP-1-i)%CAP，不要 rev()（rev 是索引序反转不是时间序反转）。
- v5 方向：剩余 15,243 行缺口，优先 handoffchk（36.5%）、duoclock（36.3%）、signbadge（46.2%）等低达成模块；功能面可继续展开：引导选单鼠标域、二维码真编码器、SMART 原始寄存器面。

## AI-V2 批次三深化（F131-F150 · f 层工程化工具面）
- 形态：20 个 `deep/f1xxf.rs` + mod.rs 扩容 40→60 blocks；深化只增不改，随域聚合器入总账。
- 量：17,257 → 22,035 行（63.5%）；CheckSet 900 → 1,236；std 隔离舱 112 → 154/154 全绿零警告（新增 deep3 聚合断言 + 20 单测 + dump_red_sets3 诊断口）。
- 真缺陷 4 处（测试网拦截）：拓扑入度方向反置（Kahn 入度=自身 deps 数）、rfind(')') 被返回类型 `()` 误切（改深度配对扫描）、CVSS 归一差十倍（×100/137）、均值异常带被尖峰自拉偏（改中位数稳健带）——教训：排序/扫描/归一/统计四类算法写完必须先用一个"会背叛直觉"的样例手推一遍。
- committed 树复证被 C1 在途 clipfmt.rs:685 语法错挡住（非 V2 面）——登记随闸门补测；裸宏扫描零残留。
- no_std 试验舱结论：kernel lib 实为 cfg_attr(not(test), no_std)，宿主测试链路是 std；基础批 a11yopen 的 f64::powf 在纯 alloc 环境不可用（kernel-image 风险）——登记观察项，待 image 构建窗口统一处置。
- 下批方向（批次四 g 层）：距 90% 线差约 9,200 行——F136 五示例源码本体、F139 tracker 后端自建件、界面接线批（跨域接口）。

## AI-V1 深化批次 v6（2026-09-26 第四轮）
- svstar 纯功能 10,224 → 11,005 行（48.1%）；自检 346 → 366 条 × 21 块全绿；隔离舱 304 passed / 0 failed。
- 六项判据面落地（F125 五件/F126 五件/F128 五件/F119/F129/F130），主册判据面已全量实装，剩余为目标口径差距。
- 关键教训（V6-01 🟡）：extract_json_num「找下一个非数字」在末字段无数值后继分隔符时 find=None 误判缺字段——找「下一个 X」必须回答「没有 X 时呢」。
- 下一轮清单：F111-F123 设计细节段二阶展开盘点 → 判据锚定补面；对账生成器与五桶脚本均可重跑。

## AIU2 深化批次 v3 收口 + v4（F451-F500）
- v3 在途 16 模块收口入库（autolum 喂值序列缺陷 D-35 修复）；v4 七模块深化（webfallback/vxdict/fgmute/trashdrag/cmdhist/termcolor/scrollback），净增 ~1,054 行 → 域总量 23,568/53,690 = 43.9%。
- 检查项 1,285 项对账表落成 `docs/AIU2-检查项对账表.md`（逐 F 映射主册判据锚）；完成报告 §八 缺陷账 D-31~D-37。
- 🔴 栈纪律根因：Rust alloca 整帧常驻——EnvEditor 0.7MB / Scrollback 2.1MB 单体在同帧多实例必爆 2MiB 测试线程栈。**探测帧模式**（probe_* 每检查面一帧）根治；主仓测试栈口径 = kernel/.cargo/config.toml [env] RUST_MIN_STACK=16MiB，独立舱必须镜像。
- 隔离验证新范式：共享树他队在途破损（h1star 未入库 + svstar 红）导致干净 worktree 全都无法编译 → **独立验证舱 u2pod**（genstar2+checks.rs 同源副本小 crate，genstar2 零跨目录依赖已 grep 证实），cargo test 392/0 全绿。
- 下批方向（v5）：剩余 27 个无 v3/v4 模块继续深化（lnkwizard/foldicon/thumbbadge/foldtmpl/deskicons/pcpage/webfallback 之外仍浅的：setdesc/idiffreg/ncactrow/taskoverflow/swapbtn/xmovkey 等小文件优先），向 90% 回炉线（48,321）推进。

## AI-S1 批次五深化（F171-F185）
- 形态：15 个 `*_b5.rs` 批次文件，主攻低达成模块（signbadge/memguard/diskhealth/selftestviz/handoffchk/pwrdrill）；深化聚合器四层 merge（deep+b3+b4+b5）。
- 量：+4,409 行 → 域总量 25,714 / 36,725（70.0%）；检查项 711 → 859；单测 175 → 217 全绿；no_std 零警告；主仓 cargo check secstar 零错误。
- 伤账 11 条自抓自修（2 条 🔴：回执帧 16B 装不下双 u64→扩 24B；u64 比值溢出→u128）。
- 关键语义澄清：kinfo 名册 zombie-ok 跨套件同名是主册事实（键=套件+seq）；Z 序 index0=顶、接任=下方；节流首按 armed 旗构造解。
- v6 方向：缺口 11,011 行。剩余大头：memguard（1,551 缺）、diskhealth（1,490）、handoffchk（1,326）、pwrdrill（1,241）、selftestviz（1,186）、signbadge（1,178）。

## AIU2 深化批次 v5（F451-F500 · 第二轮）
- 11 模块深化入库（idiffreg/swapbtn/tilelongpress/avatar/wheeldir/shutbadge/taskautohide/ncactrow/hostname/touchpad/setdesc/privacysweep/shutblock 之 11 项），净增 2,138 行 → 域总量 25,706/53,690 = 47.9%；检查项 1,430（对账表已重生成）；缺陷账 D-38~D-42。
- v5 深化三主轴：①持久化通道补全（VAV/VSW/VND/VTP5/VPS 魔标 + FNV 尾 + 坏值拒收——「重启不丢」是完整性不是加分项）；②交互状态机纵深（迟滞回入/打断恢复/冷却/防抖/焦点环闭合）；③审计账（历史环 LIFO 回放 + 时间戳单调 + 预览执行对账竞态守卫）。
- 关键教训：**扩文件前先 grep 同名 API**（D-39：touchpad v2 已有 save_touchpad，撞名 + 魔标扩位忘挪偏移双债）；**注释承诺什么测试就验什么**（D-38/D-40 注释-实现/注释-检查背离）。独立舱范式稳定：一轮舱测抓全红点，未流入提交。
- 下批方向（v6）：剩余无 v3/v4/v5 模块（deskicons/pcpage/termtheme/appdata/coolgov/foldtmpl/termalias/recentmgr/thumbbadge/foldicon/searchmath/unitconv/taskoverflow/lockwall 以外的浅文件）+ 已深化模块的持久化/审计面二阶展开，向 90% 回炉线（48,321）推进。

## AI-V1 深化批次 v7（2026-09-26 第五轮）
- svstar 纯功能 11,005 → 11,307 行（49.4%）；自检 366 → 372 条全绿；隔离舱 316 passed / 0 failed。
- F125 宪章条款登记册（16 章 46 条款）+ 走查脚本生成器；F112 朗读高亮双视觉；F127 keygen 指引；F126 打印友好。
- 亮点：宪章册五查门在施工中实证抓到「D 域锚误挂 C 域册」——门禁有效性的第一次实战证明。
- 下轮候选（按空间排序）：F120（1,560/约900）→ F118（910/约470）→ F115（975/约700）→ F116（1,170/约900），主册设计细节段剩余点盘点后锚定补面。

## AIU2 深化批次 v6（F451-F500 · 第三轮）
- 3 模块深化入库（coolgov/appdata/termtheme），净增 394 行 → 域总量 26,100/53,690 = 48.6%；检查项 1,456（对账表脚本重生成）；缺陷账 D-43~D-44。
- D-44 教训（🔴）：u8/u16 域做缩放乘法必先拓宽——31*255 在 u8 直接 panic；新铁律「缩放永远先 cast u32」。
- D-43 教训：D-39 同类债再犯——「扩文件前 grep 同名 API」必须变成动作而不只是记忆（下次批次开工第一条命令就是 grep save_/load_）。
- 下批（v7）：剩余浅模块 + 48,321（90%）还差 22,221 行；独立舱范式 + 对账脚本已就绪。
- 订正（诚实口径）：v6 检查项实为 1,456（先前误写 1,459——脚本实测 1,456，手工誊写错误 3 项）。数字永远以脚本输出为准。
