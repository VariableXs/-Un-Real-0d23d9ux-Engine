# 2026-09-12

## 会话：AURORA-1000 步骤 0001~0190（工程底盘 + W1 前半）收口

- 修复 metrics.rs（步骤 0028）根因缺陷：全局计数器用 `pub const` 声明，
  const 在每个使用点内联全新零值副本 → add/get 读写不同内存、计数恒 0。
  改为 `pub static`；另给 metrics 测试加 std::sync::Mutex 串行锁防并行 flaky。
- 修复 aurora/integration.rs（W1 联调）两处断言错误：
  - text_composite_onto_screen：composite_region 计数语义是"每个可见图层像素计一次写"
    （含透明源像素），written 应为 64+64=128 而非 80。
  - image_texture_composite：BMP 2x2 解码 RGBA 断言字节索引错，dec[15] 是 alpha=255，
    蓝色分量在 dec[14]；改为检查 dec[12]/dec[14]/dec[15]。
- 清理 integration.rs 未使用 import（SnapSide/self 移入 test 模块，用 crate::aurora::window:: 路径）。
- 验证：ktest --lib 1858 绿（我域全绿）；kbuild release 零 error；
  剩余 9 个失败全在 w2_integration/w3gate/w4_integration（其他会话 WIP 域）。
- 环境事故：stable 与 1.97.1 rustup 工具链损坏（bin 只剩 cargo.exe，无 rustc），
  多会话并行 rustup install 死锁（6 进程互相锁）；杀掉死锁进程后临时用
  `rustup run nightly`（1.100.0-nightly + x86_64-unknown-none std）完成全部构建/测试。
  注意：nightly 下 `cargo ktest`（全量含 bin）会报 "unwinding panics are not supported
  without std"，用 `--lib` 跑单测即可。
- 提交 71a7ebc：metrics.rs / integration.rs / w2_integration.rs / w4_integration.rs /
  w3gate.rs / lib.rs / aurora/mod.rs / robust.rs / checks.rs / clipboard.rs /
  kernel/rust-toolchain.toml（冻结 1.97.1，已存在故未动）。
- 推送：本地代理对 github.com 持续 Connection reset / 连接失败，12 次重试中。
- 修复手段沉淀：定位"不可能失败"的测试时，先怀疑 const vs static 与测试并行竞态，
  再怀疑逻辑本身。

## QEMU 环境搭建 + 内核引导 smoke（WorkBuddy 会话）
- winget 装 QEMU 11.1.0（C:\Program Files\qemu）；下载 Limine v12.9.0 binary 到 tools/limine/limine-binary（已 gitignore）；MSYS2 便携 xorriso 1.5.8 解到 /tmp/xorr（未入库）。
- 踩坑链：pycdlib 打的 ISO Limine iso9660 驱动读不了大文件数据 → 必须用 xorriso（msys repo 的 xorriso-1.5.8-1-x86_64.pkg.tar.zst + msys2-runtime + libintl/libiconv，zstd 解包即可运行）；git-bash 调 msys xorriso 要用相对路径（绝对 D:/ 会被路径转换弄坏）；Limine v12 配置格式是 `key: value` + `/标题` 条目行，必须 `protocol: limine`，配置文件名 limine.conf（limine.cfg 老格式已废），放 ISO 根目录；ISO 里还要带 limine-bios.sys（stage3）；kernel_path 用小写（Rock Ridge 名）。
- 内核侧真坑：linker.ld 各段 ALIGN(16) 导致 RX/RW 共享同一 4K 页，Limine 拒载（"PHDRs with different permissions sharing the same memory page"）→ 四段改 ALIGN(4096)。
- 结果：varix.iso 在 QEMU q35/512M 下 BIOS 引导成功，串口 INFO（cpu/acpi/memmap/kaslr）+ VARIX 帧缓冲自检横幅，无三重故障。make-iso.sh/run-qemu.sh 已重写为可复现流程，推送 5b2b1f8。
- ktest --lib 1866 过 1 失败（sysmon::f475，W4 会话在途代码，非本会话范围）。
## 内核测试全绿收尾（W4 域自检修复）
- 用独立 1.97.1 工具链（D:/rust-dist/sysroot，含 none-target rust-std）跑通：ktest 1867+1 全绿，kcheck/kbuild 零告警。
- 修复的失败自检：apps A476/A485（calc 优先级、dict 前缀计数）、terminal A502/A504（History::down 语义、tab 上限）、netweb A608/A610（add_x 应成功、active 读数需与操作交错）、player A580（shuffle 后 current 应取 ids[0]）、editor A529/A543（fold start=0、len 阈值 17）、sysmon A679/A680/A683。
- 关键坑：CheckSet 断言里"操作 && 读状态"的复合表达式在多步可变操作后不可靠（读数全部落在末态），必须把每次读状态拆成独立 let 与操作交错（interleaved reads）。多个域同款问题。
- 工程改动：varix bin 用 feature `kernel-image` 门控（宿主测试免 panic=abort 冲突）；kernel/Cargo.toml 加 [profile] panic=abort；rust-toolchain.toml 钉 1.97.1；integration.rs 去掉未用 SnapSide 导入。
- 测试构建有 14 条 warning（test-only），kcheck/kbuild 零告警。变更未提交。
- 已提交并推送 GitHub：commit e3c5c66（aurora1000(w4) 收口），16 文件 +462/-22，含 CI kernel job 与 fuzz.rs。根目录临时 _*.txt 未纳入提交。

## 内核真机启动修复（QEMU 11.1.0，从三重故障到 boot complete）
按崩溃顺序逐一修掉 8 个启动阻塞缺陷，最终 QEMU 实测跑到 `boot complete — halting`，
13/13 启动自检 + 各域自检（cpu 9/9、mem 10/10、sched 9/9、proc 11/11、storage 10/10、
input 9/9、display 10/10、net 15/15、security 15/15）全绿；开中断后 20s 内稳定处理
10200+ 次定时器中断无异常。

1. `cpu/gdt.rs` `reload_segments()`：`push {sel:x}` 中 SEL_KERNEL_CODE 是 u16 → 实际只
   `push ax`（RSP -2），而 `retfq` 弹 16 字节 → 栈永久错位 +6，后续 kinfo! 格式化整数时
   解引用垃圾指针 #PF → 三重故障。改为 `push {sel}` + `SEL_KERNEL_CODE as u64`。
2. `cpu/apic.rs` / `cpu/clock.rs`：LAPIC/IOAPIC/HPET 的 MMIO 直接用了物理地址，高半区内核
   必须过 `mem::paging::phys_to_virt()`。修 `ApicBase::mmio_vaddr()`、`IoApic::attach()`、`Hpet::init()`。
3. `platform.rs` + `cpu/cpuinfo.rs`：cpuinfo 硬编码 `osxsave/xsave = true`，qemu64 无 XSAVE，
   `mov cr4,rax` 触发 #GP 死循环。Features 增加 xsave/osxsave（CPUID leaf1 ECX bit26/27）。
4. `mem/pmm.rs` + `mem.rs`：`FrameBitmap` 是 [u64;8192] = 64KB，boot_bitmaps 栈上放两份 +
   pmm::init 再 clone 一份 = 192KB，超出 Limine 64KB 启动栈 → 页表被踩坏。改为 static
   存储（USABLE/RESERVED/MERGED_MAP），`pmm::init()` 改为无参。宿主测试不炸是因为宿主栈 1MB。
5. `display.rs`：`fps: CLOCK.lock().fps(), worst_frame_us: CLOCK.lock().worst_us()` 同一语句
   里两个临时 guard 活到语句末 → 自旋锁自死锁。改单 guard 块。
6. `smbios.rs`：`next_struct_offset` 用单 NUL 计数，遇到无字符串的结构体（如 type 16）就
   错位。SMBIOS 规范字符串区即使为空也是两个 NUL，改为扫描连续双 NUL。
7. `cpu/idt.rs`：IDTR 指向了 Rust 结构体数组而非 16 字节门描述符 → 首个硬件中断 #GP(0x102)
   三重故障。Idt 增加 `wire: UnsafeCell<[[u64;2];256]>`，set() 写入 wire 表，descriptor()
   指向 wire 表。
8. `cpu/idt.rs` + `linker.ld`：stub 表在 `.data`（Limine 映射为 NX）→ 中断入口 `e=0x11`
   取指保护违例。linker.ld 新增页对齐 `.stubs` 段，STUBS 用
   `#[cfg_attr(target_os="none", link_section=".stubs")]`；生成完代码后
   `harden_stub_mapping()` 走 CR3+HHDM 页表把该页清 NX、清 P_WRITE 收紧成 R-X（大页则只清 NX）。
9. `cpu/idt.rs` `common_entry()`：只保存了 rbp，而 `isr_dispatch` 是 C ABI 会破坏
   RAX/RCX/RDX/RSI/RDI/R8-R11 → 定时器中断落在 `core::fmt::write` 里，返回后写者指针已死 →
   #PF(CR2=0x3c84)。改为完整保存 9 个易失寄存器；注意新增 9 次 push 后恢复要用
   `mov rsp,rbp; sub rsp,72`（只 `mov rsp,rbp` 会让 iretq 报 #GP(0)）。
10. `cpu/idt.rs` 可观测性：致命异常现在打印 `err / rip / cr2`（`has_error_code` 判断错误码
    是否入栈以定位 RIP 偏移），否则只有 "fatal exception 14" 无法定位。

### 新增/复用工具
- `_qmon.py`（根目录，未入库）：QEMU monitor 客户端，剥掉 readline 回显与 ANSI 转义，
  用法 `python _qmon.py <port> "info registers" "xp /96xg 0x..."`。
- 打包 ISO 必须带 xorriso：`PATH="/tmp/xorr/root/usr/bin:$PATH" bash scripts/make-iso.sh`
  （tools/build-iso.py 的 pycdlib 产物 Limine 读不了大文件）。
- QEMU 后台跑必须用 `run_in_background=true`，否则 Bash 工具调用结束就把子进程杀掉。

## 提交与 CI
- 提交 8a198fa（`varix(boot): 修复 8 处启动阻塞缺陷…`），14 文件 +391/-88，
  已 push 到 origin/main（远端确认 8a198fa）。
- **CI 现状（与代码无关，账号层面）**：`.github/workflows/ci.yml` 状态是 active，
  但 run #182（本次）0 秒结束、0 个 job、无产物；往前查 #100/#120/#140/#160/#170 全是
  0 job 0 秒失败，而 #66 及之前有真实 job（frontend/backend 成功或失败）。
  即 GitHub 已不再为该仓库调度 Actions job（约自 #67 之后退化为 0 job）。
  排查方向：账号 Actions 额度/账单验证/被禁用，而不是仓库 YAML 或代码。
  `gh` CLI 未登录（需 `gh auth login`），只能用未认证 REST API 查 run 元信息。

## VARIABLE-200 并入总规划（10:40 新增）
- 应用户要求产出 3 个 MD（只写规划、不开始实施）：docs/VARIABLE-200-{实施总步骤图,功能全景图,AI分工完成图}.md。
- 8 个 AI（AI-01~AI-08）× 25 项 = 200 项，覆盖 W2 用户态（AI-01~04）→ W3 桌面上内核（AI-05~07）→ W5 引导交付（AI-08）。
- 与 VARIX-500 互补零重复；并入判定 = 开机直达 Variable 桌面全程不经 Windows。
- 已提交推送 GitHub。

## VARIABLE-200 AI-04~AI-08 实施完成（本会话）
- **AI-04 srv（F076~F100）**：`kernel/varix/src/srv/{mod.rs,ipc.rs,sys.rs}`，initfs/pid1/VFS用户态面/devfs/服务管理器/看门狗/事件总线/管道/共享内存/io_uring式批量/命名服务/持久存储桥接/水位/依赖图/fuzz/三演示。自检 31/31。
- **AI-05 gfxsrv（F101~F125）**：`{mod.rs,text.rs,perf.rs}`，帧缓冲/双缓冲/脏区/VSync/合成器/软合成保底/GPU探测/字体管线/字形缓存/主题令牌图标DPI/模式热切换/动画时钟/帧预算/丢帧降级/色彩管理/截屏录屏/多显预留/功耗钩子。自检 26/26。
- **AI-06 hidsrv（F126~F150）**：`{mod.rs,keys.rs,guard.rs}`，PS2键鼠/USB预留/事件队列/时间戳/键盘状态机/键位纪律/全局快捷键/焦点系统/鼠标加速/滚轮平滑/触控手柄预留/延迟仪表/无障碍/输入安全/事件回放/洪泛防护/布局/触控板手势/手感验收。自检 27/27。
- **AI-07 vport（F151~F175）**：`{mod.rs,render.rs,verify.rs}`，Tauri API映射清单+薄壳+显式降级表/渲染后端双实现+像素级走查/窗口语义平移/shell四进程拆分/应用生命周期/资产管线/状态持久化/剪贴板/拖放/通知中心/内总线/主题运行时/动效复用/沙箱/崩溃不扩散/启动器/设置/文件管理器/兼容矩阵/双后端CI。自检 27/27。
- **AI-08 bootchain（F176~F200）**：`kernel/varix/src/bootchain.rs`（单文件 25 项），Limine默认项/开机品质线/引导时间线/快速启动预算/initrd流水线/ISO/USB产物/三系统并存20轮/A-B分区与更新回滚/完整性/签名/SecureBoot占位/恢复模式/安全模式/最小与完整镜像/首运向导/配置迁移/CI boot job/真机验收/发布纪律/作品集/交付自检。自检 25/25。
- 接线：`lib.rs` 新增 5 个 `pub mod`；`robust::run_kernel_checkup()` 纳入 5 域 CheckSet。
- 流水线：新增 `scripts/make-initfs.py`（纯 stdlib，cpio newc 一键打包 + `--check` 自校验 + 无源码时占位载荷）；`scripts/make-iso.sh` 接入 initrd 打进 ISO。
- CI：`.github/workflows/ci.yml` 新增 `boot` job（ubuntu：rust + xorriso + qemu；B1~B8 步骤产出 ISO/USB/initrd 三产物并做串口冒烟门禁 `boot complete` 且无 fatal exception）。
- 文档：三份 VARIABLE-200 规划图 F076~F200 共 125 项 ⬜→✅，AI-04~08 章节标题加 ✅。

### 本会话踩到的坑（值得记）
- **CheckSet 断言里的"末态读取"陷阱（反复踩了 5 次）**：`set.add("…", cond1 && obj.field == X, …)` 里对可变对象字段的读取**全部发生在表达式求值末尾**，即读到的是**末态**。凡是"操作→读状态→再操作"的写法必须先把中间读数存进独立 `let` 变量。本次 F158/F159/F164/F165/F183/F185/F186/F190/F191 都是这么挂的。
- `#[cfg(test)] mod tmp_dbg` 临时诊断模块很有用：把 CheckSet 的复合断言拆成逐条 `assert!(…, "tag")`，一次就能定位到具体子条件；验证完立刻删。
- **隔离验证法**：并行会话把 `syscall/mod.rs` 改坏导致整 crate 编不过时，把 `checks.rs` + 待验模块复制到 `/tmp/bcverify` 的 scratch crate（自带 `gfxsrv::rgb` shim）单独 `cargo test --lib`，可完全绕开他人编译错误。
- `Launcher::register` / `CompatMatrix::record` 这类"登记"接口必须自带**去重**，否则重复登记会静默改变计数断言。
- QEMU 冒烟：`timeout 30 qemu-system-x86_64 -cdrom varix.iso -serial file:serial_boot.log -display none -no-reboot -no-shutdown -m 512M -M q35` 前台跑即可（`timeout` 有界，不违反"必须 run_in_background"那条——那条是针对 `&` 后台作业被杀的场景）。串口日志末尾应出现 `boot complete — halting`。
