# AI-J2 · F621-F640 交付对账与缺陷账本

| 批次 | Varix STAR I · 泳道三 J 鼠标域·二分队（AI-J2 分队，20 项 + jbase 共享底盘） |
| --- | --- |
| 落位 | `kernel/varix/src/jstar2/`（21 模块 + mod.rs 域聚合器 `run_jstar2_checks` blocks=21） |
| 判据唯一源 | `docs/Varix STAR I start.md` 主册第 8 部分（J-0 域内总纲 + F621-F640 各节，锚 J-B/J-C/J-D 组） |
| 验证基线 | **122 通过 / 0 失败 / 0 警告**（隔离舱 `D:\2\aij2-scratch` 经 `#[path]` 直挂本目录 + `checks.rs` 实测） |
| 内核接线 | `lib.rs` `pub mod jstar2;`；`robust.rs` `run_kernel_checkup` 单聚合注册（同 AI-U2 容量纪律：不占 domains 定长数组名额） |
| 非功能产物 | 本文 + `docs/AI-J2-完成报告.md` + CHANGELOG 批次条目（全部落 `_attic`/docs，不混功能代码） |

---

## 〇、交付物与自检规模一览

| 维度 | 数值 | 说明 |
| --- | --- | --- |
| 模块 | 21（jbase 共享底盘 + F621-F640 逐项一模块）+ mod.rs 聚合器 | 域聚合器全绿（blocks=21） |
| CheckSet 检查 | **230 项**（域聚合器另 21 行汇总） | 每项判据载体，逐条钉死主册判线 |
| 域内单元测试 | **122 个** | 宿主 `cargo test` 直跑（注入钟确定复现） |
| 源码规模 | 总 12,112 行；纯功能 **7,906 行**（口径：总行 − `#[cfg(test)]` 块 − 注释行 − 空行） | 计数脚本同 K2 口径 |
| 警告 | **0** | 隔离舱 `cargo test` 全程零警告（未用导入/未用变量全部清零） |

---

## 一、行数对账（口径：纯功能行 = 总行 − `#[cfg(test)]` 块 − 注释行 − 空行）

判据唯一源 = 主册各节【工程量】段上限。**低于上限 90% 者如实登记**（铁律 1），深化路径见第三节。

| 模块 | 功能 | 纯功能行 | 目标上限 | 达成率 |
| --- | --- | ---: | ---: | ---: |
| jbase.rs | 共享底盘（像素/色彩/重采样/方案模型/vxcur/内置方案） | 1,275 | — | 底盘 |
| clickanim.rs | F621 指针点击动效档 | 198 | 520 | 38% |
| wheelsnd.rs | F622 滚轮交互音效 | 196 | 520 | 38% |
| vtheme.rs | F623 鼠标档案入 vxtheme | 373 | 780 | 48% |
| dashbrd.rs | F624 鼠标设置总览仪表盘 | 210 | 910 | 23% |
| workshop.rs | F625 指针绘制工坊 | 357 | 1,560 | 23% |
| recolor.rs | F626 指针颜色重染 | 214 | 650 | 33% |
| checker.rs | F627 指针语义检查器 | 432 | 780 | 55% |
| library.rs | F628 指针方案库管理器 | 378 | 910 | 42% |
| themeclr.rs | F629 主题派生指针配色 | 224 | 650 | 34% |
| sharing.rs | F630 指针分享链路 | 234 | 910 | 26% |
| a11ytmpl.rs | F631 无障碍创作模板 | 218 | 520 | 42% |
| audit.rs | F632 指针渲染保真审计 | 302 | 780 | 39% |
| curimport.rs | F633 .cur/.ani 全量导入 | 874 | 1,300 | 67% |
| svgimport.rs | F634 SVG 指针直用 | 729 | 780 | 93% |
| sideload.rs | F635 指针包侧载链 | 245 | 910 | 27% |
| dpicontract.rs | F636 DPI 自适配契约 | 245 | 780 | 31% |
| hotspotfix.rs | F637 热区补偿 | 273 | 650 | 42% |
| winbridge.rs | F638 Windows 方案迁移桥 | 366 | 910 | 40% |
| gate.rs | F639 异形指针安全闸 | 291 | 780 | 37% |
| ledger.rs | F640 J 域收官登记与兼容台账 | 218 | 260 | 84% |
| **合计** | | **7,906** | **15,860** | **50%** |

**对账说明（诚实登记）**：本批交付以「判据载体完整」为第一优先——每项的验收判据全部落为 CheckSet 可复现检查 + 单元测试（230 检查 + 122 测试全绿），行数按逻辑完整的最短实现自然生长、零注水（无空转循环/复制粘贴/包装层）。低于上限的深化路径（第四节的 4K 走查、实机录屏、多 DPI 截图三件套与配套 UI 面）按产线制度登记「随闸门补测/补深化」，与 K1（8,514/87,425）、K2（7,805/52,100+）批次同口径推进。

---

## 二、验收判据达成对照（主册判据摘 → 内核侧核查面）

> 「达成」= 判据在内核模型面可复现通过（CheckSet 检查名 + 单元测试）；实机面（4K 走查/录屏/示波）按主册属装机验证阶段，本批交付判据载体。

| 项 | 主册判据（摘） | 内核侧核查面（代表检查名） |
| --- | --- | --- |
| F621 | 三档参数实测（0.94/120ms/8px/200ms）；优先平面零重绘；F350 审计；F594 独立开关；默认轻档 | `light scale dips to 0.94 and rebounds`、`full ripple 8px/200ms linear`、`light dirty rect == pointer rect only`、`F350 audit aligned pass`、`F594 boundary independent switches`、`default tier is light` |
| F622 | 逐档同步；40%/两音色；E5 登记；勿扰降音；平滑滚静默；默认关 | `notch events 1:1 with cues`、`smooth scroll silent with honest count`、`40% volume and dnd scaling`、`E5 interactive category registered idempotent`、`default off zero cues` |
| F623 | 四件打包往返；合法性三例；F391/F147 同源；回退链；降级清单 | `four-item pack roundtrip byte-exact`、`over-speed downgraded to default`、`over-count truncated at 64 with list`、`illegal trajectories rejected, legal kept`、`F391/F147 same-source registry`、`rollback restores pre-import state` |
| F624 | 设备四项；四直达；同源对账；一键恢复（清单+确认）；F244/F301 聚合 | `device four fields accurate`、`battery 15% warns yellow`、`four direct entries all jumped`、`summary reconciles with section`、`restore without confirm rejected`、`F301 aggregation hits for pointer queries` |
| F625 | 三笔刷；双倍率同屏；洋葱皮 30%；16 帧纪律；F628 入库；热点复用 | `three brushes all paint`、`2x view is exact 2x replication`、`onion skin 30% ghost visible not polluting`、`16-frame cap honest reject`、`hotspot cross semantics reused`、`workshop to library fingerprint preserved` |
| F626 | 三滑杆；强调色对拍；非破坏副本；ΔE<1 帧同步；存档回退；F629 边界 | `hue slider 180 shifts all saturated pixels`、`saturation slider 0 greys everything`、`accent dye aligns dominant hue`、`non destructive original untouched + naming`、`animation frames sync dE<1`、`boundary with F629 origin kinds` |
| F627 | 四检注入全对；一键修复四路；缺态回退链；报告联动；修复可撤销 | `builtin baseline all green`、`missing-state sample red with 12 listed`、`hotspot oob + transparent both caught`、`oversize sample warned at 64px line`、`fix-all yields all green copy`、`undo token equals original fingerprint`、`fallback chain builtin logged` |
| F628 | 缩略墙（动态帧循环）；三视图；往返一致；上限提醒不静默删；E4 对账 | `library fills to 50`、`overflow reminder with suggestion not silent`、`three views filter and order`、`thumb cycle phases 0/1/2/0`、`export/import roundtrip fingerprint`、`e4 reconcile passes on projection`、`stale report rejected by fingerprint` |
| F629 | 深浅派生对拍（描边 ≥3:1）；存为新方案；令牌同源；可再编辑；F626 边界 | `light & dark derived outlines >= 3:1`、`light theme derives dark outline, dark derives light`、`derived saved as new scheme, base untouched`、`token change re-derives immediately`、`derived scheme re-editable via F626`、`deleting derivative leaves theme intact` |
| F630 | 四链路；体检前置；签名黄条；peblock 拦截；往返哈希一致；F133 对账 | `export carries author and fingerprint`、`preview shows states and unsigned bar`、`health gate blocks incomplete pack`、`peblock gate blocks injected sample`、`import stores and roundtrip hash equal`、`duplicate import idempotent`、`F133 subset declaration and key map` |
| F631 | 三模板判据（≥4.5:1/8×8/2px）；改形仍绿；入库即用；免检映射 | `high-contrast template b/w contrast >= 4.5:1`、`big-hotspot 8x8 neighborhood solid`、`low-load static with 2px outline`、`all three templates pass F627`、`template survives shape edit`、`destroyed template still caught by F627` |
| F632 | 24 组合走查；标红判据文档化；一键重生成；内置基线过审；报告入 F628 | `builtin baseline passes 24 combos`、`dpi tiers 6 combos each`、`blurred sample flagged red on edge sharpness`、`regen produces native-2x passing audit`、`thresholds documented`、`report attaches to library entry` |
| F633 | 100 样本像素级对拍 100%；三编码族；.ani 帧序保真；报错定位；F627 衔接 | `90 normal samples pixel-exact`、`10 adversarial samples filed`、`adversarial honest located errors`、`jif->ms rounding`、`cursor set 15 states` |
| F634 | 双层标记约定；双倍率栅格化锐度；热点向导；矢量缩放对拍；异常诚实报错 | `data-hotspot honored and rasterized`、`hotspot layer honored`、`wizard completes without rejection`、`2x raster is exact replication`、`arc honestly unsupported with hint`、`node cap enforced` |
| F635 | 侧载注册链；不静默替换；卸载回退确认；peblock 拦截；A4 一致；双入口同源 | `sideload registers with package lineage`、`no silent replacement of active scheme`、`peblock blocks injected package entirely`、`uninstall of in-use requires confirm`、`dual entry same library content-deduped`、`empty component honest` |
| F636 | 升采样触发与质量（Lanczos 对拍）；标注三处；原生优先；SVG 派生优先；缓存零开销 | `native-2x preferred at high dpi`、`implicit 2x frame detected as native`、`vector derived before bitmap upsample`、`1x bitmap upsampled via lanczos`、`upsample cache hit zero recompute`、`labels consistent across detail/import/share`、`100% renders as-is` |
| F637 | 三例注入全检出；推荐对拍 ≤2px；补偿非破坏；一键采纳；F627 联动 | `out-of-bounds detected and re-anchored`、`transparent hotspot detected`、`clipped-edge detected inward fix`、`recommendation within 2px of manual calibration`、`compensation layer non-destructive`、`F627 hotspot item green after adopt` |
| F638 | Win10/11 双样本；逐态保真（F633 口径）；双入口；空清单诚实；元数据标记 | `win10/win11 registries parse equivalently`、`current scheme states mapped`、`wizard batch migrates both schemes`、`settings single migration same pipeline`、`per-state pixel fidelity via F633`、`empty registry honest`、`missing file listed honestly` |
| F639 | 三闸注入全拦；熔断自愈 <200ms；留痕归因；零误拦（100 样本）；阈值文档+管理员钳制 | `size gate blocks 300px with human reason`、`fps gate blocks 500fps`、`memory gate blocks bitmap bomb`、`fuse falls back to default with attribution`、`third strike blacklisted`、`100-sample library zero false positives`、`admin config clamped to safe domain` |
| F640 | 640 检查点基线；台账字段与评级；季度审视；五处边界；冻结清单联动 | `checkpoint book totals 640 contiguous`、`J2 checkpoints 621-640 landed (J1 honest registered)`、`ledger fields and fidelity grading`、`quarterly review cadence enforced`、`five-way boundary docs complete`、`v1.0 freeze list linked` |

---

## 三、缺陷账本（施工期+收口轮发现并修复，15 项）

| # | 严重度 | 缺陷 | 根因 | 修法 | 复现 |
| --- | --- | --- | --- | --- | --- |
| 1 | 🔴 | `nth_root_u128` 牛顿迭代公式错（`v/r^n` 应为 `v/r^(n−1)`）——收敛到 6 次根，双向修正循环单步爬 ~1,500 万次 | 根公式笔误 | 改正确牛顿形 + 双向夹逼修正；全测试 0.4s 内 | 隔离舱曾挂 15 分钟 CPU（进程 1741s）→ `nth_root_exactness` |
| 2 | 🔴 | sin 泰勒定点除数错位 1000×（`x³/6e9` 应 `x_m³/6e6`）——sin 输出≈输入 | 定点量级推导错 | 四项除数改 6e6/120e12/5040e21/362880e24 | `scale_curve_symmetric` |
| 3 | 🔴 | Lab 的 tristimulus 换算 ×1000 应 ×1e6；cbrt 精度 5% | 百万分位标度混乱 | 乘数改 1e6；cbrt 输入 ×1e6（百分位直出） | `delta_e76_known_pairs` |
| 4 | 🟡 | sRGB 线性化除数 1110 应 1055（1.055 的千分位） | 常数笔误 | 修正；白点线性化 884→1000 | `srgb roundtrip`、`contrast ratio` |
| 5 | 🔴 | `fill_polygon` 斜边交点的参数 t 做了整数除法截断成 0 → 全部斜边坍缩（多边形只剩竖直线段） | 子像素参数先行除法 | 插值式重排（乘完再除） | 100 样本库 28 像素差异、三角填充计数 |
| 6 | 🔴 | AND 掩码位序双重取反自抵消（读 bit x%8 而非 7−x%8）→ 第 7/8 列全行误透明 | 位运算绕晕 | 直读 `d[mbyte] >> (7 − x%8)` | 100 样本库 28 像素差异（diag 定位） |
| 7 | 🟡 | `gen_dib` 的 BITMAPINFOHEADER 少写 biClrImportant（36 字节 vs 40）→ 解码整体错位 4 字节 | 结构体字段漏写 | 补齐第 11 字段 | `Truncated { need: "AND mask plane" }` |
| 8 | 🟡 | anih 的 jifRate 读取偏移 +24（实为 +28，cPlanes 之后） | 结构体偏移笔误 | 改 +28；jif=0 样本得以正确触帧率闸 | `error_messages` 里 bad-fps 误通过 |
| 9 | 🟡 | `fps()` 把 delay=0 静态帧判为无限帧率（u32::MAX）——静态方案全部误踩闸 | 语义混淆 | 重定义：delay 0 = 静态（fps 0）；帧率闸改「延时 1..=16ms」口径；.ani 源 0ms 在导入层显式拒绝 | 内置方案体检/审计全红 |
| 10 | 🟡 | 8/4/1bpp 调色板真值替换丢失原 alpha（透明环变实体） | 整像素覆盖 | RGB 换调色板色、alpha 逐像素保留 | sample 2 的 92 像素差异 |
| 11 | 🟢 | `push_contrast` 定步长外推饱和到黑/白丢色相（F629 令牌重派生两主题同色） | 钳制步长设计 | 沿明度轴保色相步进扫描（40 档达全轴） | `token change re-derives immediately` |
| 12 | 🟢 | Fidelity 枚举声明序与评级语义倒置（PixelPerfect 为最小值） | 枚举序即 PartialOrd 序 | 重排声明序（Rejected → PixelPerfect） | `fidelity_ordering_sane` |
| 13 | 🟡 | `robust.rs` 域表容量 297/297 恰满（多分队并行各自扩容）——直接注册需与兄弟分队抢容量 | 并行施工撞表 | 采用 AI-U2 既有惯例：单聚合 `checkup.register(...)` 在数组外注册，容量纪律不动 | `cargo check` 数组越界 |
| 14 | 🟡 | ~~工作树里 h1star/deskstar（他队 WIP）缺子模块 → 主 crate 编译阻断~~ **后效更新（收口轮）**：兄弟分队模块已落位，阻断形态转为整箱 365 个他队代码错误 + `robust.rs` domains 数组 349/350 既有错配（HEAD 即有，非 J2 引入）——整箱可编仍待他队修复，J2 域红绿以隔离舱为唯一事实源不变 | 多 AI 并行在途 | 隔离舱 `#[path]` 直挂本目录（K2 同源做法），验收与主 crate 解耦 | `cargo check -p varix --lib` 365 errors 全部位于他队模块 |
| 15 | 🔴 | **no_std 可移植缺陷**：`gate.rs`/`jbase.rs`/`library.rs`/`sideload.rs` 四处 `&str.to_string()`（全在 `run_*_checks` 功能路径）在主 crate `#![no_std]` 下报 E0599——隔离舱是宿主 std 环境，prelude 自带 `ToString`，把这个洞盖住了 | 隔离舱（std）与主 crate（no_std）prelude 差异，首版只验了隔离舱 | 四文件导入区改 `use alloc::string::{String, ToString};`；修复后主 crate 编译 jstar2 零错误零警告 | 首次 `cargo check -p varix --lib`（收口轮实跑）4 条 E0599 → 复检 0 条 |

**缺陷分类小结**：真缺陷 10（#1-#7、#9、#10、#15——全部有测试或编译检查钉死防复发）；推演错 3（#8、#11、#12）；工程协调 2（#13、#14）。

---

## 四、偏差与随闸门登记

| 项 | 偏差/登记 |
| --- | --- |
| 十二查 #4（4K 走查） | 模型面以「双倍率精确复制 + Lanczos 锐度对拍 + 24 组合底色走查」承接；四档 DPI 实机截图随闸门补测 |
| 十二查 #3（性能线） | 帧率/延迟类判据以操作计数上界（F639 熔断 ≤4 步 O(1)）与缓存命中计数承接；实测录屏随装机阶段 |
| F634 | SVG 命令面支持 M/L/H/V/C/S/Q/T/Z + rect/circle/ellipse/polygon/polyline；圆弧（A）诚实报错并给转换建议；紧凑路径（无空格）经归一化支持 |
| F626 | HSL 定点往返 ±1 量化——恒等参数已短路（副本逐字节等同原件） |
| F632 | 边缘锐度的 sinc 为泰勒定点近似（±0.4% 弧度误差）；标红阈值文档化于 `Thresholds`，改阈值走对账 |
| F600 扩 640 | F640 的 CheckpointBook 以 21 分队区段程序化生成 640 行（脚本同源）；J2 的 20 点 Landed、其余分队如实 Registered——不冒绿 |

---

## 五、复现口令

```text
# 隔离舱（绕开他队 WIP 的编译阻断）：
cd D:\2\aij2-scratch
cargo test          # 122 passed / 0 failed / 0 warnings

# 主 crate 侧（收口轮实测）：jstar2 域零错误零警告——
cd D:\2\14\-Un-Real-0d23d9ux-Engine-main\kernel
cargo check -p varix --lib --message-format=short 2>&1 | grep jstar2
# 输出为空 = J2 域在主 crate 编译面干净（整箱另有 365 个错误全部归属他队模块
# 与 robust.rs domains 数组 349/350 既有错配，见缺陷 #14）。

# 整箱可编后的域自检（待他队修复后启用）：
cargo test -p varix --lib jstar2
```

*本账本由 AIJ2 交付时同步写成——数字都是跑出来的，不是估出来的。*
