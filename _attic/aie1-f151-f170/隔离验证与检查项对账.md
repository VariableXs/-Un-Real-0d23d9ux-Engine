# AI-E1 · F151-F170 隔离验证与检查项对账（深化批次四收口 · 2026-09-26）

> 本目录是 AI-E1 的**非功能验证设施归档**（attic 纪律：非功能产物不入主树）。
> 复现口径、程序与证据留档，供任何人复核 **278/278** 与检查项对账表两个数字。
> 先例对齐：AI-C2 `_attic/aic2-f021-f040/隔离验证与检查项对账.md`、
> AI-V2 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。

## 1. 隔离口径（为什么可隔离、怎么隔离）

E 域落点 `src/system/persona/` 是 TS/React 域，**自包含**：
- 只依赖 `src/system/theme-studio/tokens`（对比度公式一处一事实）与
  `react`/`lucide-react`/`../../state/uiStore`（接线层）；
- 测试全走 vitest，零内核（kernel/）依赖——与 Rust 泳道的隔离舱互不相交；
- 施工期间共享树存在多方并发修改（kernel 在途改动、他人未提交文件），
  E 域验证**不依赖全树状态**：vitest 单目录跑 + tsc 域内过滤即闭环。

## 2. 复现步骤（两分钟）

```bash
cd <repo根目录>
# ① 隔离全量单测（不触碰其他域）
npx vitest run src/system/persona
# ② 本域类型检查（--incremental false 防陈旧缓存假阴性——本批教训，见 §4）
npx tsc --noEmit --incremental false 2>&1 | grep persona
# ③ 生产构建
npx vite build
# ④ F170 walkcheck 三模式
python tools/vx-walkcheck-e.py --selftest
python tools/vx-walkcheck-e.py --checklist
# ⑤ 检查项对账 tally（本目录）
node _attic/aie1-f151-f170/tally.mjs .
```

## 3. 实测记录（2026-09-26 深化批次四收口时点）

- ① vitest：`Test Files 17 passed (17) / Tests 278 passed (278)`，零失败零跳过
  （批次三 192 + 批次四 86 新用例）。
- ② tsc `--incremental false`：persona 过滤**零输出**（本域零错误，含本批
  12 个新引擎 + 5 页接线 + 3 新测试文件的严格模式全量通过）。
- ③ vite build（全树）：沿袭批次三口径——全树 build 依赖其他会话在途 WIP
  收口；E1 隔离验证（vitest 单目录 + tsc 域内过滤）不依赖全树健康。
- ④ walkcheck：`SELFTEST PASS：checklist 19 项 + verify 合法样本 PASS +
  注入失败样本被检出`（批次四复验）。
- ⑤ tally：见下表（三个新测试文件已登记 FILE_TO_F——未登记桶归零）。

### 检查项对账（tally.mjs 实测 · 去重总口径 278 · 批次四更新）

| F 项桶 | 贡献用例 | | F 项桶 | 贡献用例 |
| --- | --- | --- | --- | --- |
| F151 | 114 | | F162 | 16 |
| F152 | 30 | | F163 | 57 |
| F153 | 82 | | F164 | 41 |
| F154 | 114 | | F165 | 20 |
| F155 | 66 | | F166 | 57 |
| F156 | 63 | | F167 | 60 |
| F157 | 63 | | F168 | 44 |
| F158 | 33 | | F169 | 40 |
| F159 | 49 | | F170 | 55 |
| F160 | 37 | | F161 | 32 |
| F161 | 32 | | 底座 | 11 |

口径说明：一个用例服务多个 F 项时按贡献各计一次（tally.mjs 的 FILE_TO_F
显式映射表——新增测试文件必须登记该表，否则归「未登记」桶暴露；
批次四已登记 engines-batch4/5/6 三文件，未登记桶 = 0）。
**F151-F170 二十桶全非零**（含冻结项 F154——判据实装在位，投入工时冻结
与可用性冻结是两回事）。

## 4. 缺陷账（批次四施工期捕获 · 全部即时修，零遗留零静默）

| # | 级别 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | 🟡 | extractPalette 亮图误判 mid | 行基址误用 `y × step × 4 × width`（步进污染寻址——采样跨行读错位） | 行基址改 `y × width × 4`（步进只作用于采样密度不作用于寻址）；单测判类阈值实测兜住 |
| 2 | 🟡 | planPreload 跨午夜窗整窗丢失 | 零长判定误用 `<=`（360 ≤ 1320 把 22:00-06:00 窗整个 continue 掉——拆段分支不可达） | 改 `===`；跨天语义补全（changeAt ≤ now = 次日，窗段今日/明日双摆位求交） |
| 3 | 🟡 | UnlockMachine 第二轮冷却不翻倍 | 时长公式按累计失败数而非冷却轮次（failures=3 → floor(3/2)-1=0 → 仍 15s） | 引入 cooldownRound 计数：第 n 轮 = base × 2^(n-1)，封顶不变 |
| 4 | 🟡 | smoothDamp 长仿真产出 NaN | 私改 Unity 公式导出项错位（temp/target 关系推导错） | 改回 Unity 规范公式（含过冲钳制分支——临界阻尼语义更准） |
| 5 | 🟢 | verdict-report 引用不存在的 StepResult | 写前未回读 verdict.ts 实际类型（本批教训重现） | 全文件重写对齐 ItemVerdict/StepEvidence/DomainVerdict——一处一事实 |
| 6 | 🟢 | toCssVariables 键名与 tokens.css 惯例不符（--p-radius---p-r-control 双前缀） | RADIUS/FONT/SPACING 的 key 本就是全名，再做前缀拼接 | 直接用 key；motion 时长按档位命名（--p-motion-micro，ui 层惯例）；契约数 47→45 如实改 |
| 7 | 🟢 | fromCssVariables 把只读派生变量（ease/motion/sp）当未知键拒绝 | 导出集含派生变量、回收面未区分可改/只读 | 前缀白名单跳过（不是用户可改面——跳过不是拒绝，语义分清） |
| 8 | 🟢 | pages-lab 死代码/试写残留（require()、void 桩、zhT 未用） | 快写快改过程残留 | 全部清除——零死代码红线 |
| 9 | 🟢 | 本域 tsc 严格模式 18 处（unused/possibly-undefined/TS2554） | 新文件首过 noUncheckedIndexedAccess + noUnusedParameters | 逐处修：索引 ?? 0、非空断言（运行时恒有值处）、参数签名收敛 |
| 10 | 🟢 | pages-life/pages-misc 接线后 JSX 单父错误 | PlacementCard/AssembledMenuPreviewCard 裸 Card 返回，加兄弟节点破坏单父 | 包 fragment（<>）——接线纪律：改返回结构先看 return 形态 |

## 5. 行数对账（wc -l 原始计数 · 纯功能不含测试）

批次四增量：12 个新引擎（palette-engine / icon-atlas / cursor-physics /
sound-synthesis / widget-data / lockscreen-composer / ime-composition /
ctxnav / chord-engine / verdict-report / tokens-schema / wallpaper-solar）+
labels 双语扩容 + pages-lab 十一面板 + 五页组接线 + batch4/5/6 测试。
累计：**v1 5,620 → v2 9,687 → v3 10,348 → v4 14,072 纯功能行（67.7%）**；
测试 3,174 行 / 278 用例。批次四净增 3,724 功能行 / 86 用例。

## 6. 提交归属说明（多会话并发 · 过程透明）

批次三的入库路径历经两次并发事件：①初版被 J2 提交 6bb9f129 捎带；②J2 会话
reset 重做（3da84c3b）时甩出本批文件，漂浮于工作区后由 S1 提交 **4645c0ad**
收编——**批次三的实际载体提交 = 4645c0ad（已推送远端 main，
PUSH_CONFIRMED 验证）**。内容零丢失（工作区 == HEAD == origin/main 逐位一致）；
归属错位不影响代码与对账有效性，但按过程透明纪律如实登记。

## 7. 随闸门补测（沿袭 v2 报告 §6 清单，无新增项——批次四全部为逻辑层与接线层）
