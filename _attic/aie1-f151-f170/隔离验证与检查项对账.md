# AI-E1 · F151-F170 隔离验证与检查项对账（深化批次八收口 · 2026-09-26）

> 本目录是 AI-E1 的**非功能验证设施归档**（attic 纪律：非功能产物不入主树）。
> 复现口径、程序与证据留档，供任何人复核 **393/393** 与检查项对账表两个数字。
> 先例对齐：AI-C2 `_attic/aic2-f021-f040/隔离验证与检查项对账.md`、
> AI-V2 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。

## 1. 隔离口径（为什么可隔离、怎么隔离）

E 域落点 `src/system/persona/` 是 TS/React 域，**自包含**：
- 只依赖 `src/system/theme-studio/tokens`（对比度公式一处一事实）与
  `react`/`lucide-react`/`../../state/uiStore`（接线层）；
- 测试全走 vitest，零内核（kernel/）依赖——与 Rust 泳道的隔离舱互不相交；
- 施工期间共享树存在多方并发修改（kernel 在途改动、他人未提交文件），
  E 域验证**不依赖全树状态**：vitest 单目录跑 + tsc 域内过滤即闭环。

## 2. 复现步骤（两分钟）

```bash
cd <repo根目录>
# ① 隔离全量单测（不触碰其他域）
npx vitest run src/system/persona
# ② 本域类型检查（--incremental false 防陈旧缓存假阴性——本批教训，见 §4）
npx tsc --noEmit --incremental false 2>&1 | grep persona
# ③ 生产构建
npx vite build
# ④ F170 walkcheck 三模式
python tools/vx-walkcheck-e.py --selftest
python tools/vx-walkcheck-e.py --checklist
# ⑤ 检查项对账 tally（本目录）
node _attic/aie1-f151-f170/tally.mjs .
```

## 3. 实测记录（2026-09-26 深化批次八收口时点）

- ① vitest：`Test Files 24 passed (24) / Tests 393 passed (393)`，零失败零跳过
  （批次七 381 + 批次八 12 新用例，1 个新测试文件）。
- ② tsc `--incremental false`：persona 过滤**零输出**（本域零错误，含批次八
  3 个新文件 + pages-lab5 四面板 + verdict 页挂载 + 1 新测试文件）。
- ③ vite build（全树）：沿袭批次三口径——全树 build 依赖其他会话在途 WIP
  收口；E1 隔离验证（vitest 单目录 + tsc 域内过滤）不依赖全树健康。
- ④ walkcheck：`SELFTEST PASS：checklist 19 项 + verify 合法样本 PASS +
  注入失败样本被检出`（批次八复验）。
- ⑤ tally：26 桶全非零（engines-batch13 已登记 FILE_TO_F）。

### 检查项对账（tally.mjs 实测 · 去重总口径 335 · 批次五更新）

| F 项桶 | 贡献用例 | | F 项桶 | 贡献用例 |
| --- | --- | --- | --- | --- |
| F151 | 134 | | F162 | 36 |
| F152 | 46 | | F163 | 78 |
| F153 | 103 | | F164 | 41 |
| F154 | 114 | | F165 | 40 |
| F155 | 66 | | F166 | 57 |
| F156 | 83 | | F167 | 81 |
| F157 | 63 | | F168 | 44 |
| F158 | 33 | | F169 | 61 |
| F159 | 49 | | F170 | 76 |
| F160 | 37 | | F161 | 69 |
| F161 | 69 | | 底座 | 11 |

口径说明：一个用例服务多个 F 项时按贡献各计一次（tally.mjs 的 FILE_TO_F
显式映射表——新增测试文件必须登记该表，否则归「未登记」桶暴露；
批次四登记 engines-batch4/5/6、批次五登记 engines-batch7/8/9，未登记桶 = 0）。
**F151-F170 二十桶全非零**（含冻结项 F154——判据实装在位，投入工时冻结
与可用性冻结是两回事）。

## 4. 缺陷账（批次八施工期捕获 · 全部即时修，零遗留零静默）

| # | 级别 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | 🟢 | 扫描命中 ruleId 断言写的是扫描规则自身 id（S-*）而非词典规则 id（D-*） | 两层 id 语义未分清（S=扫描规则、D=词典规则） | 断言修正 + 注释区分 |
| 2 | 🟢 | pages-lab5 中途 import + 未用 t/state-wiring 未用参数 | 快写残留 | import 归顶、unused 清零（3 处） |
| 3 | 🟢 | tsc 严格模式 3 处 | 新文件首过 | 逐处清零 |

## 5. 行数对账（wc -l 原始计数 · 纯功能不含测试）

批次八增量：3 个新文件（state-wiring 二十页状态布线 / crosscheck-guard
format对拍+词典扫描+自动保存守护 / search-nav 全域搜索+导航树）+
pages-lab5 四面板 + verdict 页挂载 + batch13 测试。
累计：**v1 5,620 → v2 9,687 → v3 10,348 → v4 14,072 → v5 16,303 → v6 17,600
→ v7 18,112 → v8 18,577 纯功能行（89.3%）**；测试 4,409 行 / 393 用例。
批次八净增 465 功能行 / 12 用例（四文件全部高内聚紧凑）。

## 6. 提交归属说明（多会话并发 · 过程透明）

批次三的入库路径历经两次并发事件：①初版被 J2 提交 6bb9f129 捎带；②J2 会话
reset 重做（3da84c3b）时甩出本批文件，漂浮于工作区后由 S1 提交 **4645c0ad**
收编——**批次三的实际载体提交 = 4645c0ad（已推送远端 main，
PUSH_CONFIRMED 验证）**。内容零丢失（工作区 == HEAD == origin/main 逐位一致）；
归属错位不影响代码与对账有效性，但按过程透明纪律如实登记。

## 7. 随闸门补测（沿袭 v2 报告 §6 清单，无新增项——批次四全部为逻辑层与接线层）
