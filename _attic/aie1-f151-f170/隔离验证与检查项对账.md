# AI-E1 · F151-F170 隔离验证与检查项对账（深化批次十收口 · 2026-09-26）

> 本目录是 AI-E1 的**非功能验证设施归档**（attic 纪律：非功能产物不入主树）。
> 复现口径、程序与证据留档，供任何人复核 **412/412** 与检查项对账表两个数字。
> 先例对齐：AI-C2 `_attic/aic2-f021-f040/隔离验证与检查项对账.md`、
> AI-V2 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。

## 1. 隔离口径（为什么可隔离、怎么隔离）

E 域落点 `src/system/persona/` 是 TS/React 域，**自包含**：
- 只依赖 `src/system/theme-studio/tokens`（对比度公式一处一事实）与
  `react`/`lucide-react`/`../../state/uiStore`（接线层）；
- 测试全走 vitest，零内核（kernel/）依赖——与 Rust 泳道的隔离舱互不相交；
- 施工期间共享树存在多方并发修改（kernel 在途改动、他人未提交文件），
  E 域验证**不依赖全树状态**：vitest 单目录跑 + tsc 域内过滤即闭环。

## 2. 复现步骤（两分钟）

```bash
cd <repo根目录>
# ① 隔离全量单测（不触碰其他域）
npx vitest run src/system/persona
# ② 本域类型检查（--incremental false 防陈旧缓存假阴性——本批教训，见 §4）
npx tsc --noEmit --incremental false 2>&1 | grep persona
# ③ 生产构建
npx vite build
# ④ F170 walkcheck 三模式
python tools/vx-walkcheck-e.py --selftest
python tools/vx-walkcheck-e.py --checklist
# ⑤ 检查项对账 tally（本目录）
node _attic/aie1-f151-f170/tally.mjs .
```

## 3. 实测记录（2026-09-26 深化批次十收口时点）

- ① vitest：`Test Files 26 passed (26) / Tests 412 passed (412)`，零失败零跳过
  （批次九 405 + 批次十 7 新用例，1 个新测试文件）。
- ② tsc `--incremental false`：persona 过滤**零输出**（本域零错误，含批次十
  tour-preset-parity + pages-lab7 + labels 扩容 + 1 新测试文件）。
- ③ **run-tests 大测试总闸首战**（Variable 明令待办交付）：`node tools/run-tests.mjs fast`
  收编 4 脚本并行 → **vitest-e/walkcheck-e/tally-e 全 PASS，tsc-incremental-false
  FAIL（全树其他域在途 WIP 的红线——总闸首战即抓到全树红，隔离口径的价值
  实证：E 域自检全绿不依赖全树健康）**。
- ④ walkcheck：`SELFTEST PASS：checklist 19 项 + verify 合法样本 PASS +
  注入失败样本被检出`（批次十复验）。
- ⑤ tally：26 桶全非零（engines-batch15 已登记 FILE_TO_F）。

### 检查项对账（tally.mjs 实测 · 去重总口径 335 · 批次五更新）

| F 项桶 | 贡献用例 | | F 项桶 | 贡献用例 |
| --- | --- | --- | --- | --- |
| F151 | 134 | | F162 | 36 |
| F152 | 46 | | F163 | 78 |
| F153 | 103 | | F164 | 41 |
| F154 | 114 | | F165 | 40 |
| F155 | 66 | | F166 | 57 |
| F156 | 83 | | F167 | 81 |
| F157 | 63 | | F168 | 44 |
| F158 | 33 | | F169 | 61 |
| F159 | 49 | | F170 | 76 |
| F160 | 37 | | F161 | 69 |
| F161 | 69 | | 底座 | 11 |

口径说明：一个用例服务多个 F 项时按贡献各计一次（tally.mjs 的 FILE_TO_F
显式映射表——新增测试文件必须登记该表，否则归「未登记」桶暴露；
批次四登记 engines-batch4/5/6、批次五登记 engines-batch7/8/9，未登记桶 = 0）。
**F151-F170 二十桶全非零**（含冻结项 F154——判据实装在位，投入工时冻结
与可用性冻结是两回事）。

## 4. 缺陷账（批次十施工期捕获 · 全部即时修，零遗留零静默）

| # | 级别 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | 🟢 | batch15 测试文件 esbuild 转译失败 | describe 标题字符串里嵌了未转义的英文双引号（`"预设+微调"`） | 改中文引号「」——字符串字面量纪律 |
| 2 | 🟢 | pages-lab7 两卡用 t() 未声明 | 拆卡时丢了 const t = useT() | 补声明 |
| 3 | 🟢 | TourCard 跳过按钮胡写访问器（runner["tour" as never]） | 快写残留 | tour 提为模块常量 FIRST_MIN_TOUR——干净数据流 |
| 4 | 🟢 | tsc 严格模式 2 处 | 新文件首过 | 逐处清零 |

## 5. 行数对账（wc -l 原始计数 · 纯功能不含测试）

批次十增量：tour-preset-parity（导览/预设库/键鼠对等三合一）+ pages-lab7
三面板 + labels 扩容 24 键（×双语）+ batch15 测试 + **tools/run-tests.mjs
大测试总闸（Variable 明令待办——tools/ 设施不计入 persona 行数口径，
如实分列）**。
累计：**v1 5,620 → … → v8 18,577 → v9 18,981 → v10 19,228 纯功能行
（92.5%）**；测试 4,598 行 / 412 用例。批次十净增 247 功能行 / 7 用例。

## 6. 提交归属说明（多会话并发 · 过程透明）

批次三的入库路径历经两次并发事件：①初版被 J2 提交 6bb9f129 捎带；②J2 会话
reset 重做（3da84c3b）时甩出本批文件，漂浮于工作区后由 S1 提交 **4645c0ad**
收编——**批次三的实际载体提交 = 4645c0ad（已推送远端 main，
PUSH_CONFIRMED 验证）**。内容零丢失（工作区 == HEAD == origin/main 逐位一致）；
归属错位不影响代码与对账有效性，但按过程透明纪律如实登记。

## 7. 随闸门补测（沿袭 v2 报告 §6 清单，无新增项——批次四全部为逻辑层与接线层）
