# AI-E1 · F151-F170 隔离验证与检查项对账（深化批次五收口 · 2026-09-26）

> 本目录是 AI-E1 的**非功能验证设施归档**（attic 纪律：非功能产物不入主树）。
> 复现口径、程序与证据留档，供任何人复核 **335/335** 与检查项对账表两个数字。
> 先例对齐：AI-C2 `_attic/aic2-f021-f040/隔离验证与检查项对账.md`、
> AI-V2 `_attic/aiv2-f131-f150/行数对账与缺陷账本.md`。

## 1. 隔离口径（为什么可隔离、怎么隔离）

E 域落点 `src/system/persona/` 是 TS/React 域，**自包含**：
- 只依赖 `src/system/theme-studio/tokens`（对比度公式一处一事实）与
  `react`/`lucide-react`/`../../state/uiStore`（接线层）；
- 测试全走 vitest，零内核（kernel/）依赖——与 Rust 泳道的隔离舱互不相交；
- 施工期间共享树存在多方并发修改（kernel 在途改动、他人未提交文件），
  E 域验证**不依赖全树状态**：vitest 单目录跑 + tsc 域内过滤即闭环。

## 2. 复现步骤（两分钟）

```bash
cd <repo根目录>
# ① 隔离全量单测（不触碰其他域）
npx vitest run src/system/persona
# ② 本域类型检查（--incremental false 防陈旧缓存假阴性——本批教训，见 §4）
npx tsc --noEmit --incremental false 2>&1 | grep persona
# ③ 生产构建
npx vite build
# ④ F170 walkcheck 三模式
python tools/vx-walkcheck-e.py --selftest
python tools/vx-walkcheck-e.py --checklist
# ⑤ 检查项对账 tally（本目录）
node _attic/aie1-f151-f170/tally.mjs .
```

## 3. 实测记录（2026-09-26 深化批次五收口时点）

- ① vitest：`Test Files 20 passed (20) / Tests 335 passed (335)`，零失败零跳过
  （批次四 278 + 批次五 57 新用例，3 个新测试文件）。
- ② tsc `--incremental false`：persona 过滤**零输出**（本域零错误，含批次五
  10 个新引擎 + pages-lab2 十面板 + 五页组接线 + 3 新测试文件）。
- ③ vite build（全树）：沿袭批次三口径——全树 build 依赖其他会话在途 WIP
  收口；E1 隔离验证（vitest 单目录 + tsc 域内过滤）不依赖全树健康。
- ④ walkcheck：`SELFTEST PASS：checklist 19 项 + verify 合法样本 PASS +
  注入失败样本被检出`（批次五复验）。
- ⑤ tally：见下表（engines-batch7/8/9 已登记 FILE_TO_F，未登记桶 = 0）。

### 检查项对账（tally.mjs 实测 · 去重总口径 335 · 批次五更新）

| F 项桶 | 贡献用例 | | F 项桶 | 贡献用例 |
| --- | --- | --- | --- | --- |
| F151 | 134 | | F162 | 36 |
| F152 | 46 | | F163 | 78 |
| F153 | 103 | | F164 | 41 |
| F154 | 114 | | F165 | 40 |
| F155 | 66 | | F166 | 57 |
| F156 | 83 | | F167 | 81 |
| F157 | 63 | | F168 | 44 |
| F158 | 33 | | F169 | 61 |
| F159 | 49 | | F170 | 76 |
| F160 | 37 | | F161 | 69 |
| F161 | 69 | | 底座 | 11 |

口径说明：一个用例服务多个 F 项时按贡献各计一次（tally.mjs 的 FILE_TO_F
显式映射表——新增测试文件必须登记该表，否则归「未登记」桶暴露；
批次四登记 engines-batch4/5/6、批次五登记 engines-batch7/8/9，未登记桶 = 0）。
**F151-F170 二十桶全非零**（含冻结项 F154——判据实装在位，投入工时冻结
与可用性冻结是两回事）。

## 4. 缺陷账（批次五施工期捕获 · 全部即时修，零遗留零静默）

| # | 级别 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | 🔴 | bakeJobs frameCount 全 NaN / 幕覆盖对拍 false | `BOOT_DURATION_MS` 从 boot-engine 导入——但它只是**消费方**，定义在 store.ts；ESM 链接期容错成 undefined（不报错直接 NaN，隐蔽异常的活标本） | 导入源改 "./store"；此类跨模块常量一律从定义方导入 |
| 2 | 🟡 | projectFrame 用不存在的 p.alpha/p.color 元组 | 写前未回读 ParticleState 实际形状（color 是 base/highlight 标签，alpha 由渲染层按幕进度算） | 新增 particleAlpha（幕四线性淡出）+ 调色板 RGB 解析——渲染层职责归位 |
| 3 | 🟡 | missedCount 对 lastRunAt=0 返回 1（falsy 0 当"从未"） | `if (!t.lastRunAt)` 的 falsy 陷阱（0 是合法时刻） | 改 `=== null`；nextRunAt 的 anchor 同修 |
| 4 | 🟡 | nextRunAt 非整周期时刻多跳一档 | ceil((now-anchor)/interval)+1 把未到的本周期跳过 | floor(elapsed/interval)+1（严格未来的首个到期点） |
| 5 | 🟡 | 文案审计把英文逗号全判混用 | 标点规则不分语种（英文文案合法逗号被毙 23 处） | 分语种规则：zh 禁 ASCII 标点、en 禁 CJK 标点 |
| 6 | 🟢 | printableGrid 列高差 ≤1 不成立 | ceil 均分（10/3 → 4/4/2） | 余数均摊（base + 前 rem 列各 +1 → 4/3/3） |
| 7 | 🟢 | suggestFreeCombos 组合键用 "+" 连接而占用表用空格 | 键序契约不一致（十章一致性） | 统一空格分段 |
| 8 | 🟢 | stored PNG 三档字节相同——降档叙事破产 | zlib stored 模式字节只随分辨率（编码器诚实属性） | 如实改契约：密度差在烘焙 CPU 不在字节；降档 = 密度不动字节 |
| 9 | 🟢 | tsc 严格模式 6 处（unused/unknown/possibly-undefined） | 新文件首过 | 逐处清零 |
| 10 | 🟢 | pages-lab2 死代码（void 保全句、unused import） | 快写残留 | 全清 |

## 5. 行数对账（wc -l 原始计数 · 纯功能不含测试）

批次五增量：10 个新引擎（png-encode / boot-bitmap / accent-ramp /
message-catalog / persistence / preview-render / scheduler / usage-model /
cheatsheet / archive-diff / walkcheck-runner 共 11 文件）+ pages-lab2 十
实验室面板 + 五页组接线 + labels 扩容 + batch7/8/9 测试。
累计：**v1 5,620 → v2 9,687 → v3 10,348 → v4 14,072 → v5 16,303 纯功能行
（78.4%）**；测试 3,775 行 / 335 用例。批次五净增 2,231 功能行 / 57 用例。

## 6. 提交归属说明（多会话并发 · 过程透明）

批次三的入库路径历经两次并发事件：①初版被 J2 提交 6bb9f129 捎带；②J2 会话
reset 重做（3da84c3b）时甩出本批文件，漂浮于工作区后由 S1 提交 **4645c0ad**
收编——**批次三的实际载体提交 = 4645c0ad（已推送远端 main，
PUSH_CONFIRMED 验证）**。内容零丢失（工作区 == HEAD == origin/main 逐位一致）；
归属错位不影响代码与对账有效性，但按过程透明纪律如实登记。

## 7. 随闸门补测（沿袭 v2 报告 §6 清单，无新增项——批次四全部为逻辑层与接线层）
