# AI-H3 · F301-F350 交付对账与缺陷账本

| 批次 | Varix STAR I · 泳道三 H 基础通用域·三分队（AI-H3 分队，50 项 + hbase 共享底盘） |
| --- | --- |
| 落位 | `kernel/varix/src/h3star/`（37 文件，域聚合器 `run_h3star_checks` blocks=52；深化层经 `merge_sets` 并入基线聚合） |
| 判据唯一源 | `docs/Varix STAR I start.md` 主册 G-H 区段（F301-F350） |
| 验证基线 | **364 通过 / 0 失败 / 0 警告**（域内单测实数 = cargo test 367 − 3 舱内探针；隔离舱 `_attic/aih3-f301-f350/isolation/` 经 `#[path]` 直挂实测，深化批次八收口复核） |
| 交付提交 | 首批 bb6de292（后被 974a8d2a rebase 合并伤误删）→ 落位收尾批重新入册 → 深化批次三（统一调速器核/卸载彩排/路径归一化/OCR 管线/续传引擎/模糊规则表） |
| 源码规模 | 总 **27,526** 行；纯功能 **17,039** 行（口径：总行 − 空行 − 注释行 − 测试区行；其中 50 项 16,405 + hbase 531 + 域聚合器 103。**计数器 v2 程序化实数**（字符串感知——见 D-30），脚本随仓 `_attic/aih3-f301-f350/count_pure_v2.py`） |

---

## 〇、自检规模一览

### 〇·1 行数口径声明（D-23 修正）

本账本上一版登记「纯功能 20,057」为**虚账**——大于 wc 实数总行（当时 19,137），
纯功能行不可能超过总行。D-23（见 §二）认账：登记值未按实测口径程序化重算。
本版起全部行数以脚本实数为准（总/空/注释/测试块四分法），并固化纪律：
**对账表数字必须来自脚本输出，禁止手填估算**。

| 维度 | 数值 | 说明 |
| --- | --- | --- |
| 模块 | 37 文件 | hbase 共享底盘 + 50 项逐项落位（同族项合模块） |
| 域内单元测试 | **364 个**（cargo test 367 − 3 舱内探针） | 宿主 cargo test 直跑（注入钟确定复现）；批次七收口 352 → 本批 +12 |
| CheckSet 检查 | 52 块聚合（深化层经 `merge_sets` 并入），逐块 4-19 项判据检查 | 逐条钉死主册判线 |
| 警告 | 0 | cargo test 0 warning 纪律 |

---

## 一、逐项行数对账（口径：脚本实数纯功能行；合载模块按项均摊）

> 目标 = 主册【工程量】区间**上限**（分工图口径）。达成率偏低的统一原因见第三节：
> 本队交付判据实装层（状态机/账本/审计器逻辑核），界面渲染与交互层依赖 F151/F216/F436
> 控件件未就绪不得开工自绘（铁律 5）。**禁注水**：无复制粘贴、无空转包装。

| F 项 | 落位模块 | 主册目标上限 | 实际（分摊） | 达成率 |
| --- | --- | --- | --- | --- |
| F301 设置中心搜索 | `setsearch` | 910 | 370 | 41% |
| F302 设置页层级规范 | `pagehier` | 195 | 174 | 89% |
| F303 即时生效哲学 | `instantfx` | 1235 | 282 | 23% |
| F304 单页还原默认 | `pagedflt` | 715 | 244 | 34% |
| F305 设置导入导出 | `themepack` | 650 | 269 | 41% |
| F306 文件内容全文搜索 | `fulltext` | 1560 | 413 | 26% |
| F307 搜索历史与无痕 | `srchhist` | 715 | 239 | 33% |
| F308 运行框（Win+R） | `runbox` | 650 | 376 | 58% |
| F309 窗口管理快捷键族 | `winkeys` | 1040 | 397 | 38% |
| F310 关闭前保存三问 | `saveask` | 910 | 351 | 39% |
| F311 会话自动恢复 | `sesrestore` | 1040 | 319 | 31% |
| F312 拼音与容错搜索 | `pyfault` | 2340 | 687 | 29% |
| F313 符号与表情面板 | `sympanel` | 715 | 224 | 31% |
| F314 截图取字（OCR） | `ocrtake` | 2340 | 773 | 33% |
| F315 拖拽悬停前置 | `draghover` | 715 | 189 | 26% |
| F316 闲置锁屏策略 | `idlelock` | 910 | 229 | 25% |
| F317 屏幕保护进化 | `idlelock` | 780 | 229 | 29% |
| F318 电源按钮行为 | `pwbtn` | 2145 | 485 | 23% |
| F319 唤醒即回体验 | `wakeresume` | 1170 | 215 | 18% |
| F320 飞行模式 | `wakeresume` | 780 | 215 | 28% |
| F321 就近共享 | `nearshare` | 2665 | 573 | 21% |
| F322 麦克风隐私指示 | `micind` | 520 | 113 | 22% |
| F323 摄像头隐私指示 | `micind` | 1235 | 113 | 9% |
| F324 权限中心 | `micind` | 520 | 114 | 22% |
| F325 文件历史版本 | `filevers` | 910 | 365 | 40% |
| F326 输入法引擎基础 | `imecore` | 2015 | 626 | 31% |
| F327 中英文切换语义 | `cnensw` | 975 | 271 | 28% |
| F328 双拼输入支持 | `shuangpin` | 1170 | 325 | 28% |
| F329 剪贴板大对象 | `clipbig` | 1105 | 264 | 24% |
| F330 通知分组批量清除 | `ntfgrp` | 1235 | 316 | 26% |
| F331 动画降级不卡逻辑 | `animdegrade` | 780 | 410 | 53% |
| F332 帧率自适应 | `animdegrade` | 6175 | 410 | 7% |
| F333 低电量视觉模式 | `animdegrade` | 910 | 410 | 45% |
| F334 高负载保响应 | `loadresp` | 1105 | 186 | 17% |
| F335 指针渲染优先平面 | `loadresp` | 1235 | 185 | 15% |
| F336 复制文件地址 | `copypath` | 715 | 634 | 89% |
| F337 命令行与图形互通 | `copypath` | 2860 | 633 | 22% |
| F338 多选智能操作条 | `multibar` | 585 | 178 | 30% |
| F339 空格即看 | `multibar` | 780 | 177 | 23% |
| F340 预览形制统一 | `multibar` | 520 | 177 | 34% |
| F341 静音四档 | `sndmode` | 1235 | 195 | 16% |
| F342 磁盘检查与修复 | `sysgov` | 585 | 429 | 73% |
| F343 内存不足出路 | `sysgov` | 1170 | 428 | 37% |
| F344 vxapp 卸载三步 | `sysgov` | 3640 | 428 | 12% |
| F345 默认应用矩阵 | `sysgov` | 585 | 428 | 73% |
| F346 自启动管理 | `sysgov` | 1105 | 428 | 39% |
| F347 键盘重复参数 | `keyrep` | 520 | 211 | 41% |
| F348 光标指针加粗 | `caretbold` | 910 | 204 | 22% |
| F349 焦点模式进阶 | `sndmode` | 715 | 196 | 27% |
| F350 系统触感反馈谱 | `haptic` | 715 | 298 | 42% |
| **合计（50 项）** | — | **60710** | **16405（另有 hbase 531 + 域聚合器 103）** | **27.0%** |

## 二、缺陷账本（施工中发现并修复：🔴 即时修 / 🟡 本包收口前修 / 🟢 记账已清）

| # | 现象 | 位置 | 严重度 | 修法 |
| --- | --- | --- | --- | --- |
| D-01 | 混输匹配要求 CJK/ASCII 同字段，真实场景「亮度bright」跨字段命中 | setsearch::score_item | 🟡 | 条目级合账（两片段分字段命中即中英混输） |
| D-02 | 切分器裸声母段产生垃圾切分 | imecore::segmentations | 🟡 | valid_segment 要求有韵母；裸声母仅整串放行 |
| D-03 | 候选同词多切分重复刷屏（一词双账） | imecore::candidates | 🟡 | dedup_by 按词取最优分 |
| D-04 | theme_tokens 键被凭据启发式误伤 | themepack::credential_like_key | 🟢 | 凭据词表收窄（auth/secret/password/credential/api_key/session_key） |
| D-05 | Win+R 单会话二次 execute 被拒且历史不跨会话 | runbox::open | 🟢 | open 可重入（重呼出历史保留——常用词两击直达） |
| D-06 | 唤醒 reboot 无条件置脏干净标志 | sesrestore::reboot | 🟡 | reboot 只读持久化标志；崩溃场景从未置位 |
| D-07 | 双拼 q+un 与 q+iu 同键歧义直接取表序首个 | shuangpin::decode | 🟢 | 文档化（声韵搭配歧义面留待真引擎）；判据测试选无歧义键 |
| D-08 | 剪贴板大对象引用条内存增量判据为空洞断言 | clipbig::memory_budget_ok | 🟡 | 重写为「引用零增量 + 流式峰值 ≤ 单块」真账 |
| D-09 | 权限申请对未登记应用报「未说明用途」（误导） | micind::PermissionCenter::request | 🟡 | 先查登记位——未登记报「应用未登记」 |
| D-10 | F334 过载自愈以末条延迟为峰值基准（自比较恒假） | loadresp::recovered | 🟡 | 与饱和段最大值比较 |
| D-11 | Save 裁决关窗移除文档导致后续 Discard 用例空转 | saveask checks | 🟢 | 用例重新开窗（测试面修正，非实现缺陷） |
| D-12 | 预览 diff 字节区间期望按字符口径（多字节错位） | filevers checks | 🟢 | 明确字节面口径并文档化（渲染面做字符对齐） |
| D-13 | 搜索防抖首键入不触发查询（t=0 首击被吞） | setsearch::DebounceBook | 🟡 | 首键入必触发——用户等不到合并 |
| D-14 | 全文索引深化检查按词组 token 断言（CJK 分词为单字粒度） | fulltext checks | 🟢 | 检查与单字分词语义对齐；bigram 组合由多 token AND 承担 |
| D-15 | 发现账 len 把隐身设备计入可见数 | nearshare checks | 🟢 | 区分总账 len 与可见 visible().len() |
| D-16 | F338 选择语义 shift 期望整表（应为锚点范围） | multibar checks | 🟢 | 状态机逐步验证（plain→ctrl→shift 精确断言） |
| D-17 | F338 选择检查残留空洞断言（恒真式） | multibar checks | 🟡 | 整条删除，替换为精确状态机断言——零空洞纪律 |
| D-18 | pyfault 节表复核漏双字母声母整串（zh/ch/sh 被判非法） | pyfault::syllable_valid | 🟢 | 补 ALL_INITIALS 整串放行分支 |
| D-19 | rebase 合并伤：974a8d2a（他队 CHANGELOG 修复提交）误删 h3star 全部 37 文件 + CHANGELOG H3 段，注册行被 AI-U4 屏蔽，深化补批滞留工作区未提交 | git 历史 / lib.rs / robust.rs | 🟡 | 落位收尾批：深化终版 37 文件重新入册，lib.rs/robust.rs 屏蔽解除，域表 349→350（条目数程序化核对），收尾留痕注释替换屏蔽注释 |
| D-20 | 测试计数口径：189 把旧舱 lib.rs 内 4 个探针测试（dump_red_checks 等舱内诊断件）计入域内单测 | 本报告 / 完成报告 §一 | 🟢 | 修正为 185（域内单测实数），探针件属舱内工具不入账；隔离舱随仓归档可复跑复核 |
| D-21 | 基线观察：KernelCheckup::register 对 MAX_DOMAINS=320 之外的注册静默丢弃（域表现 350 项 + compositor/genstar2/istar 追加注册） | checks.rs::KernelCheckup | 🟢 | H3 不越权改动（robust.rs/checks.rs 属基线与 U4 裁决面）——仅显性化登记：域表扩容或改静默丢弃为计数上报，需属主分队拍板 |
| D-22 | H3 收尾批自身事故：76b82935 的 CHANGELOG blob 基于 71290332 快照构造，落在 12d116ad（AI-V2）之后，把其补登的深化批次段覆盖丢失——多会话并发同款事故第四起，本次责任在 H3 | CHANGELOG.md | 🟡 | 6e7b9ea1 从 12d116ad 字节级取回该段插入，三版本段落清单交叉审计全段无缺；纪律沉淀：多队共写文件（CHANGELOG）必须在提交前重取最新 HEAD 重放本队段落 |
| D-23 | **账本虚账**：上一会话把逐项对账表合计登记为纯功能 20,057 行，大于 wc 实数总行 19,137——纯功能行不可能超过总行。登记值未按实测口径程序化重算，属手填估算混入对账表 | 本账本 §一 | 🟡 | 本版全部行数改由脚本实数产出（总/空/注释/测试块四分法），50 项实数 12,402；固化纪律：**对账数字必须来自脚本输出，禁止手填** |
| D-24 | ResumeEngine 零块任务被 `max(1)` 钳成 1 块 → 0 块任务永远「未完成」（完成语义失真） | nearshare::ResumeEngine | 🟡 | total_chunks 如实存储，progress_permille 对 0 块任务显式返回 1000‰；补单测钉死 |
| D-25 | 施工即时修复四连（深化批次三自省）：① 帧成本模型断言手算错（shadow 120/4 误作 120）；② ocrtake 断行用例像素块过小被抗噪规则正确拦截（用例面错）；③ 模糊变体层序断言被 find() 短路失效（应集合断言）；④ PathNormalizer 初版用 unsafe transmute 提升生命周期（无必要 unsafe——已重写为纯安全实现） | animdegrade / ocrtake / pyfault / copypath | 🟢 | 全部当轮发现当轮修复并有对应单测钉死；④ 按内核零 unsafe 纪律重写 |
| D-26 | 深化层三 pyfault 检查第 6 项初版含 `|| true` 恒真式（空洞断言——零空洞纪律违反） | pyfault::run_pyfault_deep3_checks | 🟡 | 重写为「规则展开使命中层从 Tolerant 升至 PinyinFull」的层序真断言；探针实证后钉死 |
| D-27 | 批次四施工即时修复：① ResumeStageBook 单级超时判线拍脑袋 800ms——把合法的 900ms 后台服务级误杀（探针实证后改为 1.5×最宽正字级 = 1500ms）；② 「10 样本 1 劣化点仍绿」的 p95 容量前提数学不成立（1 劣化恰落 p95 位）——改为全绿 10 样本 vs 添 2 劣化的诚实口径；③ SentenceComposer 初版按「最短切分方案」设计对词级词库语义不成立——重写为贪心最长匹配 | wakeresume::ResumeStageBook / WakeChainSampler / imecore::SentenceComposer | 🟢 | 全部探针实证后修复并有对应单测钉死 |
| D-28 | 批次五施工即时修复：InteropHistory 脱敏规则初版只替换模式前缀不替换值段——`token=abc123` 脱敏后变 `token=█abc123`，秘密仍在账里（**隐私红线级实现缺陷**）。重写为「模式+值段（到下一 `&` 或串尾）」整体替换，并补多规则/独立环形实例单测钉死 | copypath::InteropHistory | 🟡 | 当轮发现当轮修复；红线级缺陷即时修不入池 |
| D-29 | 批次六施工即时修复：① PasteEchoVerifier 回读比较基准错位——原始斜杠形对反斜杠形恒假（统一为归一化形比对）并补引号歧义守卫（去壳后仍含外壳引号 → 诚实判红——POSIX 单引号方案对内嵌引号路径不可逆）；② Burst 采样断言取样点错（尖峰宽 100ms 在 100ms 步进下只命中 t=0）——取样语义对齐后重断言；③ Copypath/Animdegrade/Pagedflt 编译期收尾（match 臂类型/生命周期/未用导入三项零警告清理） | copypath::PasteEchoVerifier / animdegrade::LoadGenerator | 🟢 | 全部当轮修复并有单测钉死 |
| D-30 | **计数器本体缺陷**：v1 四分法解析器按整行数花括号——字符串字面量里的 `{}`（format! 模板）扰乱测试区深度追踪，copypath 深化层 338 行纯功能被误判进测试区（历史批次绝对值同受影响，方向为少计非多计）。重写计数器 v2（剥离字符串/注释后数括号），随仓归档 `_attic/aih3-f301-f350/count_pure_v2.py`，全量重算并修正本账本全部行数 | 计数工具本体 | 🟡 | v2 实数为准：50 项 15,819（v1 误报 15,027）——历史达成率全部被 v1 低估，本批起以 v2 为唯一口径 |

**合计 30 项（🟡 14 / 🟢 16），全部闭环修复或有归属裁决路径并有对应检查或单测钉死。🔴 0 项（无崩溃/丢数据/红线类缺陷；D-28 属红线级实现缺陷，发现即修，未扩散未出包）。**

## 四·五、深化批次三（2026-09-26 · 统一调速器核 / 卸载彩排 / 路径归一化 / OCR 管线 / 续传引擎 / 模糊规则表）

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe，判据载体直挂主册判线）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `FrameGovernor` 统一调速器：三机制优先级仲裁（F333 低电量 > F331 预算 > F332 分级）+ `FrameCost` 帧成本模型（降级动作→每帧成本数值直证「响应延迟不增」）+ 模式切换环形账 | animdegrade | F331/F332/F333 |
| `UninstallRehearsal` 注入测试应用全链彩排（清单→三步→关联回收→残留兜底→五项对账单）+ `ClaimArbitration` 声明权仲裁（安装不抢默认执行面）+ `DiskBusyQueue` 忙时排队与人话映射表 + `BootReplay` 冷启动重放与估算校准 | sysgov | F344/F345/F342/F346 |
| `PathNormalizer` 路径归一化核（混合分隔符/点段/越界钳制/盘符规范/UNC/POSIX/WSL 映射）+ `EscapeMatrix` 逐味转义总表 + `InteropLedger` 互通事件账（十三章语义）+ `OpenDotSemantics` open . 语义核 | copypath | F336/F337 |
| `CaptureFsm` 截取状态机（误触防呆/Esc 全路）+ `Binarizer` Otsu 二值化 + `Components` 连通域（栈式 4 邻接）+ `Feature8x8` 密度特征 + `TemplateLib` 模板最近邻匹配（置信度‰）+ `Pipeline` 端到端行序组装——**离线判据的本体面**（全程纯计算零 I/O） | ocrtake | F314 |
| `ResumeEngine` 断点续传（分块位图/fnv1a 块校验/只补缺失/零缺失才完成）+ `RejectionCleanup` 拒绝清场（幂等零残留） | nearshare | F321 |
| `FuzzySwitches` 全量模糊规则表（7 规则逐条开关/白名单外拒绝/查询变体展开）+ 变体展开使命中层从 Tolerant 升至 PinyinFull 的端到端层序证明 | pyfault | F312 |

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **265 域内通过 / 0 失败 / 0 警告**
（cargo test 总 269 含 3 个舱内探针：dump_red_checks / probe_pyfault_cases /
probe_filevers_incremental——探针件属舱内诊断工具不入账，口径沿 D-20）。

**检查项对账**：域聚合器 blocks 51→52（pyfault deep3 独立登记 F312b 块）；
52 块逐块全绿经 `h3_domain_aggregate_all_green` 与舱内 `dump_red_checks` 双探针证实。

## 四·六、深化批次四（2026-09-26 · 帧模拟器 / 干跑卸载 / 粘贴清洗 / 组句引擎）

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe；判据载体直挂主册判线）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `FrameSim` 帧步进模拟器（工作负载×降级折算→帧时序账，p95/掉帧率——「降级改善 fps」机制本体）+ `SurfaceBudgetBook` 逐表面预算分配账（指针 100‰ 保额、总账不漏 μs）+ `TierPolicyTable` 层级策略参数总表（折算单调自证） | animdegrade | F332/F331 |
| `FileTypeRegistry` 文件类型注册表 DB（用户置顶/卸载摘除/空序列回退出厂默认/无悬空）+ `SpaceLedger` 释放空间逐项账（声明对账 5% 线+进度数据面）+ `RestorePointHook` 卸载前还原点闸门（未就绪拒绝且计数可见）+ `DryRun` 干跑面（执行越出干跑承诺必须被拦——红线纪律机器面） | sysgov | F344/F345 |
| `EnvExpander` 环境变量展开双味规则（CMD `%VAR%` / POSIX `$VAR`+`${}`，未登记原样保留留痕）+ `TERMINAL_PROFILES` 终端形制能力档案 + `PasteSanitizer` 粘贴清洗器（控制字符剔除/多行拒绝防夹带命令/4KB 上限）+ `drag_multi` 多文件拖入 argv 序列 | copypath | F337/F336 |
| `SyllableMatrix` 声母×韵母合法性矩阵生成核（正字法规则显式成文：零声母独韵/ü 转写/jqx 拼真 u 拦截/bpmf 不接 ong，912 组合程序化统计）+ `ToleranceStats` 容错层命中统计账（词库缺口事件直出） | pyfault | F312 |
| `TriggerMatrix` 三触发源×双供电面动作矩阵（配置缺口显性化直出）+ `ConsequencePreview` 后果预演面 + `LongPressProgress` 长按 4s 进度账（逐拍进度/松手清零/只触发一次） | pwbtn | F318 |
| `SentenceComposer` 句子级组句（贪心最长匹配/全确定/死胡同诚实失败）+ `LearnLedger` 用户词频自学习账（历史重放收敛证明） | imecore | F326 |
| `BadgeCoverage` 延迟项徽标覆盖审计（缺口点名+覆盖率‰）+ `ApplyPendingQueue` pending 应用队列（重启落地/反悔零残留） | instantfx | F303 |
| `ParamCoordination` 参数协调器（本域两轴/越权拒绝点名——单一定义点审计）+ `PreviewTrail` 预览联动账（变更留痕/提交清空） | caretbold | F348 |
| `DegradeMatrix` 负载分级策略矩阵（五级单调自证）+ `PointerLatencyBook` 指针延迟账（p95<16ms 数据面） | loadresp | F334/F335 |
| `ResumeStageBook` 唤醒分级启动时序账（输入面先活/超时降级留痕）+ `WakeChainSampler` 全链 <2s×10 采样 p95 判定（样本不足不虚报） | wakeresume | F319 |

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **296 域内通过 / 0 失败 /
0 警告**（cargo test 总 299 含 3 个舱内探针——口径沿 D-20）。

**行数**：纯功能 13,036 → **14,355**（+1,319）；域内单测 265 → **296**（+31）；
50 项口径 12,402 → **13,721**（22.6%）。

## 四·七、深化批次五（2026-09-26 · 排队与保护 / 握手与自适应 / 版面与重试）

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `ProtectedComponents` 系统组件保护白名单（出厂六件冻结审计——卸载域红线面）+ `UninstallQueue` 卸载排队中心（FIFO+并发互斥+排队位可取消）+ `CleanupPostmortem` 关联清理复盘审计（四面全绿才算卸载干净） | sysgov | F344 |
| `InteropHistory` 互通历史面板（**凭据模式脱敏红线**：模式+值段整体替换）+ `AliasRegistry` shell 别名登记（唯一/非空/保留名三闸+可逆） | copypath | F337/F308 |
| `PairingHandshake` 配对确认码握手（确定性六位码/三次错码封禁防暴力）+ `AdaptiveChunking` 自适应分块（速度掉→块加大，档位单调自证）+ `MultiCastBook` 多接收方账（逐方独立进度/慢方直出） | nearshare | F321 |
| `LayoutAnalysis` 版面分析（水平投影切行+覆盖完整性自证）+ `RetryPolicy` 低置信重试（Otsu 偏移五档扫描，质量单调承诺）+ `BatchRegions` 批量多区域账（短板语义） | ocrtake | F314 |
| `TriggerQualityBook` 触发质量账（划过零误触/误拒直出/触发率‰） | draghover | F315 |
| `DedupPolicy` 去重双档（全局保最新位/相邻保轨迹节奏+切换留痕）+ `IncognitoGranularity` 无痕双粒度（面板隐藏 vs 完全不落盘——语义严格分离） | srchhist | F307 |
| `MediaExemptionBook` 媒体豁免采样账（心跳续窗）+ `DemoModeTimer` 演示模式 2h 倒计时账（自动退出/提前关闭留痕） | idlelock | F316/F317 |
| `StreamProgressBook` 流式读取进度账（乱序拒绝/续读起点）+ `EstimateCalibration` 体积预估校准（±20% 越界留痕） | clipbig | F329 |

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **317 域内通过 / 0 失败 /
0 警告**（cargo test 总 320 含 3 个舱内探针——口径沿 D-20）。

**行数**：纯功能 14,355 → **15,149**（+794）；域内单测 296 → **317**（+21）；
50 项口径 13,721 → **14,515**（23.9%）。

## 四·八、深化批次六（2026-09-26 · 负载发生器 / 事务回滚 / 通配与参数化）

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `LoadGenerator` 合成负载发生器（常载/正弦/突发三型确定性曲线——实机判据「Y7000 注入负载」的宿主替身）+ `OscillationAudit` 升降级振荡审计（最小驻留时间——抖动=策略缺陷事件）+ `ManualOverride` 用户手选档位让位账（手动期机制动作压制计数） | animdegrade | F332/F331 |
| `UninstallReport` 卸载人话报告（三要素+残留分支文案分岔+技术折叠）+ `UninstallTransaction` 卸载事务性（undo 日志逆序回滚——中断不留半截卸载；提交后回滚拒绝）+ `HiveReclaim` 蜂巢回收验证（残留蜂巢点名直出） | sysgov | F344 |
| `Glob` 通配符匹配核（* 与 ? 递归匹配+字典序确定性展开——不猜 [] 扩展语义）+ `ScriptParametrize` %1 脚本参数化（越位缺参显式错误+%0 转义）+ `PasteEchoVerifier` 粘贴回读保真闭环（归一化形比对+引号歧义诚实判红） | copypath | F337/F336 |
| `CycleKeyBook` 连按轮转状态机账（环回推进+80ms 去抖留痕） | winkeys | F309 |
| `ModifiedMarkerBook` 改动标记点数据面（标记账-事实账一致性审计+还原即时消退） | pagedflt | F304 |
| `HapticSceneMatrix` 场景强度矩阵（六场景三档折算唯一源）+ `ConsistencyAudit` 触感一致性审计（私设强度点名直出） | haptic | F350 |

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **332 域内通过 / 0 失败 /
0 警告**（cargo test 总 335 含 3 个舱内探针——口径沿 D-20）。

**行数**：纯功能 15,149 → **15,661**（+512）；域内单测 317 → **332**（+15）；
50 项口径 14,515 → **15,027**（24.8%）。

## 四·九、深化批次七（2026-09-26 · 全链端到端 / 事务确认门 / 路径边界）+ D-30 计数器 v2

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `EndToEndVerifier` 调速器整车试验（三型负载曲线喂 FrameGovernor——突发必响应/健康不误伤/延迟守卫全链不变式）+ `SamplingDensity` 采样密度自适应（负载变化率→加密采样，切换留痕） | animdegrade | F332/F331 |
| `ConfirmInputGate` 卸载确认输入校验（手打应用名放行——破坏性二次确认数据面，错误提示说怎么改对）+ `StageEtaBook` 阶段 ETA 账（剩余时间预估+超预估 50% 漂移显性化）+ `MultiVolumeCleanup` 多盘清理账（单盘失败不阻塞+重试翻转+失败显性） | sysgov | F344 |
| `MaxPathAudit` MAX_PATH 260 边界审计（不截断——长路径前缀 \?\ 合法出路）+ `EscapeAudit` 相对路径越界检测（`..` 段+前缀校验）+ `ConvertBatchBook` 批量转换账（失败留痕+成功率‰） | copypath | F337/F336 |
| `RecoveryBarLifecycle` 恢复条生命周期（30s TTL 自动消退/消费/超时拒绝三态留痕）+ `CleanupCompleteness` 暂存区清理完整性对账 | sesrestore | F311 |
| `GroupRules` 分组规则表（同应用才合组+跨组合法性审计）+ 时间边界人话（59/60/61 分钟分岔钉死） | ntfgrp | F330 |
| `SchemeTableAudit` 方案键位表校验（声母映射无冲突/26 键无死键集合证明/方案隔离/20 字抽样） | shuangpin | F328 |

**D-30 计数器 v2**：v1 解析器被 format! 花括号扰乱（copypath 338 行功能代码
误判进测试区）。v2 剥离字符串/注释后数括号，全量重算——50 项 15,027 →
**15,819**（历史达成率被 v1 系统性低估）。v2 脚本随仓归档为唯一口径。

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **352 域内通过 / 0 失败 /
0 警告**（cargo test 总 355 含 3 个舱内探针——口径沿 D-20）。

**行数**：纯功能 15,661 → **16,453**（+792，其中含 D-30 修正回补 338）；
域内单测 332 → **352**（+20）；50 项口径 **15,819**（26.1%）。

## 四·十、深化批次八（2026-09-26 · 配置快照 / 冲突依赖 / 书签缓存）

**本批新增功能核**（全部注入钟化/纯计算/零 unsafe）：

| 深化件 | 落位 | 判据锚 |
| --- | --- | --- |
| `GovernorConfigSnapshot` 调速器配置快照（六参数键值导出/导入，键白名单+数值域校验+round-trip 可迁移证明——十四章开放性） | animdegrade | F332 |
| `ConflictDetector` 卸载冲突检测（运行中拒绝+人话提示三要素）+ `DependencyGraph` 依赖断裂警告（影响面直出——知情卸载）+ `BatchUninstallBook` 批量卸载账（逐件裁决独立推进+完成/失败分账） | sysgov | F344 |
| `PathBookmarks` 路径书签（两闸登记/形制化解析/可逆/人话导出）+ `EnvSnapshot` 环境快照（导出/合并导入只增不覆盖+冲突留痕）+ `ConvertCache` 转换缓存（同键命中免重算+异味不误命中） | copypath | F337/F336 |
| `BatchUndoChain` 多选批量撤销链（整链粒度撤销/redo 对称/新动作截断） | multibar | F338-340 |
| `RetentionPolicy` 版本保留策略表（30 天/500 上限机制面+最旧先删+恢复备份豁免） | filevers | F325 |
| `SuggestRanker` 运行框建议排序核（别名/历史/补全三源合并去重+确定性排序） | runbox | F308 |

**隔离验证**：`_attic/aih3-f301-f350/isolation/` cargo test **364 域内通过 / 0 失败 /
0 警告**（cargo test 总 367 含 3 个舱内探针——口径沿 D-20）。

**行数**：纯功能 16,453 → **17,039**（+586）；域内单测 352 → **364**（+12）；
50 项口径 15,819 → **16,405**（27.0%）。

## 四、落位收尾补记（2026-09-26）

1. **合并伤链条还原**（D-19）：bb6de292 完整入册 → 多会话 `pull --rebase --autostash` →
   974a8d2a 冲突解决把 h3star/ 整目录连带删除（提交信息只提 D1 条目修复）→
   AI-U4 按「模块文件缺失」屏蔽 lib.rs/robust.rs 注册 → 工作区未跟踪副本成
   为唯一事实。逐文件 diff 确认工作区 = 深化终版（13 文件差异恰为深化补批清
   单；总行数 15,663 与本账本一致）。
2. **复核**：隔离舱迁入 `_attic/aih3-f301-f350/isolation/`（Cargo.toml + src/lib.rs
   `#[path]` 直挂 checks.rs 与 h3star/mod.rs，零拷贝零分叉），`cargo test`
   **185 通过 / 0 失败 / 0 警告**。
3. **注释冗余**：h3star/mod.rs 模块地图表 F309 起整段重复 26 行——删除并补
   次序说明（零冗余纪律，doc-only 改动）。
4. **接线核对**：robust.rs `domains: [fn() -> CheckSet; 350]` 数组条目数 awk
   程序化计数 = 350，恰合；lib.rs 挂载行恢复。主 crate 他队 201 项在途编译
   错 0 项位于 h3star（cargo check 全量日志 grep 证实）。

## 三、偏差登记与边界

1. **行数偏差**：本队交付**判据实装层**（逻辑核）；主册行数口径含的界面渲染与交互层（约 60%）
   依赖 F151 令牌与 F216/F436 控件件——未就绪不得开工自绘（铁律 5）。逻辑核已预留全部注入口
   （识别器注入、光标面注入、F244 键位注册表、F238 锁屏衔接、F251 媒体会话、F069 性能档、
   F107 候选窗消费面等），控件件就绪后同模块叠加。
2. **实机面判据**（录屏/示波/长跑/4K 走查）按分工图「随闸门补测」口径登记，属装机验证阶段。
3. **共享树并行施工**：提交经私有临时索引完成（单 hunk 暂存），不携带他队在途状态；robust.rs
   注册行已恢复（落位收尾批，域表 349→350），主 crate 恢复绿后 run_kernel_checkup
   域表 350 项即自动吃到 h3star 域聚合。
