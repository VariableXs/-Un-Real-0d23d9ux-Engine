## [Unreleased] — Varix STAR I · AI-J1 J 鼠标域·一分队（F601-F620 深化批次四 · 大量深化+隔离验证+检查项对账）

**src/features/mouse/ + MouseJ1Tab|MouseJ1Panels + ContextMenu 声明**（AI-J1 泳道三一分队，
2026-09-26 v4 批次；v3 跨窗口接线之上补齐自定义纵深、体感补全与对账/隔离两层纪律设施）：

- **F617 手势重绑定**（轨迹/动作解耦：画法不动、动作随换——bindings 覆盖原动作，
  清空即还原）+ **墨迹二次贝塞尔平滑**（中点法，拐角圆顺——墨迹从工程线到手写笔意）。
- **F615 快捷键录制器**：按组合键即得映射（修饰键定序 Ctrl+Alt+Shift+Meta、字母大写、
  Space 规范化、只按修饰键不收）+ F244 冲突同源预检——手填示例串退役。
- **F607 屏对护边覆盖全链**：SeamGuard 第三参 pairOverride（覆盖>全局，与 F605 同构）
  + 屏对面板（EDID 指纹配对、单屏诚实空态）——「按屏对记忆」判据从逻辑层走到 UI。
- **体感补全**：F602 慢速微调 HUD（激活即「精修」徽标跟指针——100ms 反馈红线）；
  F608 磁吸视觉渐近（微滑不瞬移，判定零偏移铁律不变）；F204/F606 横向惯性通道
  （倾斜/Shift 等效横滚共享余韵——与垂直完全对称）。
- **章十三 时间轴回放**：buildReplay 分幕（间隔>2.5s 切幕）+ 挫败信号归幕 + 面板步进
  视图——「可回放的操作故事线」从导出 JSON 走到面板。
- **检查项对账层（通用十二查的机器化）**：checklist.ts 二十项条目元数据（面板三件套
  同源文案/三落位/路径链/性能线探针/最丑角落自记一处一事实）+ evidence.twelveChecks()
  （20 项×12 查，性能线探针真执行、4K 走查/录屏如实 gated 不冒领）+ 设置面板「十二查
  对账」表——对账从文档义务变成机器设施。
- **隔离验证自动化**：j1isolation.spec.ts 常驻测试门——J1 域 import 图机械扫描
  （零他域纠缠 u3/h4/desktopxp、域内悬挂 import 拦截、反向纠缠检查、对外契约出口
  登记、防失效自检）；零 node 依赖（import.meta.glob ?raw——仓库无 @types/node）。
- **可发现性**：鼠标设置页本页关键词检索（分组级过滤——「忘了在哪时怎么找回」）；
  F618 菜单容器 data-wheel="menu" 真实声明点（一属性零行为变更）。
- **验证**：本域单测 146/146 全绿（新增 j1v4 27 项 + j1isolation 5 项）；tsc 本域
  25 文件零错误；行数对账 5,638 / 15,660（36.0%，同口径净增 794，脚本
  _attic/aij1-f601-f620/）；十二查对账全文与隔离验证执行快照见 docs/AI-J1-完成报告.md §8/§9。
