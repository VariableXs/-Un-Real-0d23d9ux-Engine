# AI-J1 v3 批次 · 对账底稿（非功能材料归档）

> 归档位置：`_attic/aij1-f601-f620/`（非功能材料入 attic 纪律）。
> 本底稿是 `docs/AI-J1-完成报告.md` §3 行数对账的原始证据，随闸门复核用。

## 统一口径

- 功能行 = 非空 + 非块注释 + 去 `//` 行注释（awk 过滤器，脚本内联于 count-*.sh）。
- 测试文件不计入功能口径（j1v3.spec.ts 363 行单列）。
- 跨版本可比：v1/v2 报告为当批次自报口径；v3 起统一用本目录脚本回测。

## 脚本清单

| 脚本 | 用途 |
| --- | --- |
| `count-domain-total.sh` | J1 域 23 个功能文件现值合计（4,844） |
| `count-v3.sh` | 本批次涉及文件全量行清单（含既有行，供抽查） |
| `count-v3-delta.sh` | git diff 新增行过滤（含他人混交行的原始读数，见下节勘误） |

## v3 净增量推演（同一过滤口径）

| 项 | 功能行 |
| --- | --- |
| HEAD（v2 收口）域内回测 | 4,169 |
| 现值（v3）域内 | 4,844 |
| **域内净增量** | **+675** |
| 跨域接线（App.tsx +3 / SettingsModal +1 / ExplorerWindow +1 / 三入口 +25） | +30 |
| **v3 批次总足迹** | **≈ +705** |
| 达成率 | 4,844 / 15,660 = **30.9%** |

## count-v3-delta.sh 原始读数勘误（重要）

delta 脚本对 `App.tsx`（+12）/`SettingsModal.tsx`（+14）/`CHANGELOG.md` 的读数**含其他 AI 会话留存在共享工作树的未提交行**（U3Runtime/D2Runtime/H4Runtime 接线、U3Tab/H4Tab/DesktopD2Tab 注册、AI-H4 changelog 条目）。报告 §3 采用的是**剔除混交行后仅本批次行数**（App +3 / SettingsModal +1 / ExplorerWindow +1）。提交时对这三个文件执行了「HEAD + 仅我方 hunk」换体提交，他人改动保持未提交原状。

## 全量测试套件复核记录（2026-09-26）

- 全量并行：`4 failed | 270 passed (274 files)`，`3 failed | 3454 passed | 4 skipped (3461 tests)`。
- 4 个失败文件：domain02（500 项超时）/ ai22 V 线聚合 / vwm-kv 持久化 / nova W-184 封顶滚动超时。
- 隔离复跑：`4 passed (4 files) / 76 passed (76 tests)`——全部为并行负载抖动，非本批次引入（四文件传递依赖零 J1 域模块）。
- 本域：`npx vitest run src/features/mouse/__tests__` → 119/119 全绿（63+23+33）。
- tsc：全仓仅 2 条错误，均来自未跟踪的他 AI 进行中文件 `src/features/settings/DesktopD2Panels.tsx`（JSX `<100ms` 解析歧义），未越权代改，§4-16 记账移交。

## 提交卫生记录

- 共享工作树中他人未提交改动（kernel/* 约 110 文件、_attic/* 数处、KeymapOverlays、vwm.ts、i18n 字典、App/SettingsModal/CHANGELOG 混交行、暂存区 aiv2/kernel 文件）**一律不裹挟**：本批次提交用 pathspec 限定范围 + 三个混交文件换体。
- 本域独占文件 15 个直接 pathspec 提交；推送 origin main。

---

## v4 批次补充对账（2026-09-26 第二批）

- **行数**：域现值 5,638（脚本扩充含 shortcutRecorder.ts 77 + checklist.ts 216）；
  同口径净增 **+794**（域内）+ ContextMenu 1（跨域声明）；
  达成率 **36.0%**（4,844 → 5,638 / 15,660）。测试不计入口径：j1v4 202 + j1isolation 94。
- **探针事件**：F609 探针初版取点 8px 踩阶梯边界（=1/3 带宽属慢档 4）——修探针不修实现（§4-18）。
- **隔离验证**：j1isolation.spec.ts 5 项常驻门全绿；执行快照与混交勘验记录见报告 §9。
- **十二查对账**：机器引擎 twelveChecks() 20×12；机器可判项全绿；gated 20 条（查4）+
  partial 40 条（查2/查9）如实登记——全文见报告 §8。
- **提交卫生**：CHANGELOG 换体提交（他人 +8 行复验轮条目保留未提交）；
  其余 15 文件均本域独占，直接 pathspec。
