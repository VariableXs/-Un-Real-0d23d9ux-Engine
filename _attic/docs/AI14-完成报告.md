# AI14 完成报告 — 开放接口组

> 日期：2026-09-08 ｜ 路线：AI-14 开放接口（Z-50…Z-56 · U-37…U-39 · U-42 · N-26…N-30）
> 分工依据：[ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md](ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md)
> 口径：代码级交付 + 自动化自检；真机验收统一待 B-30。

## 一、交付范围（任务对照）

### Z 线（开放格式与生态）

| 任务 | 交付 | 文件 |
|---|---|---|
| Z-50 主题令牌开放规范 | token schema（`--v-*` 四前缀）+ 校验器（必填/非法值/对比度 ≥4.5:1/未知变量警告）+ 规范文档 | src/lib/themeToken.ts、[AI14-主题令牌开放规范-Z50.md](AI14-主题令牌开放规范-Z50.md) |
| Z-51 本地事件流 | JSONL 追加落盘（默认关）、`openhub_stream_emit/tail` 只读消费端 | src-tauri/src/shell/openhub.rs |
| Z-52 插件运行时 2.0 | plugin.json 清单校验：id/semver/权限白名单/engine 区间/entry 相对路径（防目录逃逸） | src/lib/pluginManifest.ts |
| Z-53 开放 IPC API 网关 | 127.0.0.1 REST + WebSocket 双通道、token 鉴权、细粒度 scope（read/companion）、默认关闭零监听、随配置启停、退出即停 | openhub.rs（`gateway_*`）+ lib.rs autostart |
| Z-54 布局与配置分享格式 | 统一信封 `{kind, version, payload, checksum}`：FNV-1a 校验和、导入 schema→checksum→摘要三步、篡改必拒 | src/lib/shareFormat.ts |
| Z-55 开放数据连接器 | SQLite/JSON/CSV 只读连接器 + SQL 仅 SELECT 白名单（20 种注入模式全拦截）+ 刷新策略（手动或 ≥10s） | openhub.rs（`connector_query_inner`）+ src/lib/connectors.ts |
| Z-56 扩展兼容承诺与版本矩阵 | 三类资源向前兼容 N-1 minor、弃用 ≥2 minor 移除、矩阵自检 `matrixSelfCheck`、承诺文档 | src/lib/ecoMatrix.ts、[AI14-扩展兼容承诺与版本矩阵-Z56.md](AI14-扩展兼容承诺与版本矩阵-Z56.md) |

### U 线（开放入口）

| 任务 | 交付 | 文件 |
|---|---|---|
| U-37 协议中枢 | `variable://` 动词路由（词表即契约）、解析/注册/注销命令；注册仅便携态可用、退出自动退订（零残留） | openhub.rs + lib.rs RunEvent::Exit |
| U-38 数据开放导出 | 一键导出全部用户数据为开放格式包（JSON/CSV + 媒体原件 + manifest）、全程只读、密钥类文件跳过 | openhub.rs（`data_export_inner`） |
| U-39 资源包格式（.vxs） | 皮肤四合一开放容器（主题/壁纸/音效/图标）：zip + manifest 校验 + 按类抽取 | openhub.rs（`vxs_validate/vxs_extract`） |
| U-42 诊断导出包 | 见 N-26（cli.rs `--doctor` 数据面复用 openhub 体检） | src-tauri/src/cli.rs |

### N 线（生态通道）

| 任务 | 交付 | 文件 |
|---|---|---|
| N-26 诊断导出包 | 离线诊断子命令（`variable-cli --doctor [--json]`） | src-tauri/src/cli.rs |
| N-27 插件市场 | 清单校验 + 网关 scope 鉴权构成上架前校验面（本地侧；远端市场超本轮边界） | pluginManifest.ts + openhub.rs |
| N-28 开放 IPC API 网关 | 见 Z-53（token 重置 `gateway_token_regen`、状态查询） | openhub.rs |
| N-29 variable-cli | `--doctor [--json]` / `--api-token-gen [--json]` 离线子命令 + 帮助分组（生态/控制/应急） | src-tauri/src/cli.rs |
| N-30 浏览器伴侣 | 收件箱落点（扩展投递→本地收件箱→用户确认处置）+ 清空命令；扩展侧（Chrome/Edge manifest）为配套交付面 | openhub.rs（`companion_inbox*`） |

### 前端与接线

- `OpenHubTab.tsx`：设置弹窗新增「开放接口」标签——网关开关/端口/Token 重置、事件流开关与尾读、协议解析/注册/注销、数据导出、.vxs 校验、插件清单校验、主题令牌校验、分享信封封口/导入、兼容矩阵展示、连接器查询、伴侣收件箱。
- `ipc.ts`：17 个 `openhub_*`/`deeplink_*`/`vxs_*`/`gateway_*`/`companion_*`/`safehouse_*` 绑定 + Shell 命名空间视图类型。
- i18n：`oh*` 系列 50 键 zh/en 全部对齐。
- 后端注册：lib.rs 17 条命令 + 网关随配置自动拉起 + 退出退订协议。
- 样式：`src/styles/ai14-openhub.css`（oh-* 类族，随 global.css 引入）。

## 二、自检结果

| 检查 | 结果 |
|---|---|
| `cargo check --lib` | 通过（0 error） |
| `cargo test --lib openhub` | **7/7 通过**（协议解析/安全屋审查/越界读拒绝/写能力必拒/SQL 注入 20 模式/导出跳过密钥/事件流默认关） |
| `vitest src/lib/__tests__/openhub.test.ts` | **16/16 通过**（shareFormat 往返+篡改拒收、themeToken 对比度、connectors 刷新规范化、ecoMatrix 矩阵自检、pluginManifest 红线） |
| `tsc --noEmit`（AI-14 文件） | **0 错误**（其余 55 处错误均为其他 AI 会话测试文件既有问题，不在本路范围） |

## 三、红线遵守

- 网关与事件流**默认关闭**（零监听/零落盘），开启即明示；
- 数据导出**全程只读**，密钥/凭据类文件跳过并记录 manifest；
- 协议注册**仅便携态**可用，退出自动退订（零残留）；
- 脚本安全屋**未声明能力一律拒绝**，fs 读写围栏在数据目录内，fs.write 能力全量拒绝；
- 连接器**只读**，SQL 仅 SELECT 双重拦截。

## 四、并发与会话说明

- 期间检测到并发会话对工作区文件的回滚，已用幂等重放脚本（feel-tmp/ai14_replay.cjs）恢复关键修复；
- 自检阶段发现 AI-15 会话遗留的 envedit.rs / workshop.rs 编译错误阻塞全仓 cargo check（并发会话随后自行修复），本路未越权改动其功能代码；
- 提交仅包含 AI-14 范围文件与既有公共文件的最小接线（lib.rs / ipc.ts / SettingsModal.tsx / global.css / dictionaries.ts）。

## 五、边界（如实声明）

- 浏览器伴侣的 Chrome/Edge 扩展本体（manifest/打包上架）超出本轮代码面，当前交付环境内接收端全链路；
- 插件市场远端索引/支付不在本轮；本地提供清单校验与网关 scope 鉴权面；
- 网关 REST 端点当前覆盖状态/搜索/通知/场景只读面，写操作需 companion scope 并走收件箱确认。
