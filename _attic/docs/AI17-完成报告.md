# AI-17 视觉语言组 · 完成报告

提交：`dda542b`（main → VariableXs/-Un-Real-0d23d9ux-Engine）

## 交付清单（30 文件，+2536 行）

### CSS 层
- **U-07 / Z-02 / Z-05 / U-08 / Z-67**：`src/design/tokens.css` 交付段 —— 遮罩浓度三档（--overlay-1/2/3）、噪声纹理（--texture-grain）、控件规格（--ctl-btn/input/touch）、焦点环（--focus-ring）、动效速度（--motion-speed）；
- **U-08 材质引擎**：`src/styles/material.css` —— 实心 / 磨砂 / 深雾三档，磨砂同屏并发 >3 自动降级；
- **U-11 微交互精修**：`src/styles/interactions.css` —— 控件四态（rest/hover/active/focus/disabled）动效矩阵，`[data-reduce-motion]` 全局停用；
- **Z-64 CJK 排版**：`src/styles/cjk.css` —— 中西文间距、标点禁则；
- **Z-06 系统光标**：`src/styles/cursor.css` —— 光标语义工具类。

### TS 层（src/lib）
- **U-10 / Z-01 图标语义字典**：`iconRegistry.ts` —— 41 语义全仓唯一映射 + Segoe Fluent 对应物 + 16/18/24/32 官方网格；
- **动效图标八件套**：`components/icons/AnimatedIcons.tsx` + `animated-icons.css`（transform/opacity-only）；
- **U-12 响应式断点**：`breakpoints.ts` —— compact/standard/wide 三档 + 大字模式建议；
- **U-56 错误叙事 2.0**：`errors.ts` —— 十类错误词典四段式叙事（zh/en）+ 1s/3s/9s 退避重试；
- **Z-66 图标零闪烁**：`iconCache.ts` —— LRU + DPR 命名空间（0.25 步进）+ pending 等待态复用；
- **主题淡入**：`themeFade.ts`；**拖动清晰度**：`dragClarity.ts`；**Z-69 边缘热区**：`hotspots.ts`。

### 组件层
- **U-55 空状态系统**：`components/EmptyState.tsx`（含 sr-only 播报）；
- **U-57 引导体系**：`features/onboarding/` —— SpotlightTour 首次导览 + 一次性提示（coach marks）；
- **Z-70 帮助中心**：`features/help/` —— 主题注册表 + 分词搜索 + F1 上下文帮助；
- **视觉运行时**：`features/vision/VisionRuntime.tsx` + `EdgeHotspots.tsx`，App.tsx 挂载；
- **设置页**：`features/settings/VisionTab.tsx` —— 动效速度 / 边缘热区 / 引导重置 / 材质说明，SettingsModal 注册「视觉语言」页。

### 文档与 i18n
- `docs/ICON-SPEC.md`（图标规格：网格 / keyline / Segoe 映射）、`docs/UI-COPY.md`（文案规范：错误叙事四段式等）；
- `src/i18n/dictionaries.ts` AI-17 交付段：vt*/hs*/ob*/help*/emptyState* 共 40+ 键，zh / en 全量，zh-TW 经 convertDict 继承；
- `tools/audit.cjs` 增加 **AI-17 视觉门禁（U-60 品质关卡）**：令牌契约完整、视觉样式文件裸色值 = 0、iconRegistry 存在 —— 当前全绿。

## 自检
- vitest（AI-17 域 7 文件）：**41 passed / 1 skipped**（node 环境守卫，浏览器真机覆盖）；
- tsc --noEmit：AI-17 全部文件 **0 错误**（全仓现存错误均在其它 AI 域：A11yTab / OpenToolsTab / lunar / moodEngine / soundscape / briefing 等，未越权改动）；
- audit.cjs：视觉门禁 + TESTID 门禁通过；IPC MISSING BACKEND 与 i18n MISSING 均为其它 AI 域遗留（Workshop.tsx 的 gate*/perf* 键等），非本组范围。

## 边界说明
- 期间多次并发会话回滚 / 重写 main（3fc287c → ecc4e36 曾剥离本组文件），已重放恢复并独立提交 `dda542b` 推送成功；
- iconCache 的 createImageBitmap 路径依赖浏览器环境，Node 单测仅覆盖缓存语义与回退路径；
- 边缘热区默认仅左下角 = 开始菜单，全屏应用内自动禁用。
