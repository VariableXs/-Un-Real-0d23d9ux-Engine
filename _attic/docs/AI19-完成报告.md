# AI-19 完成报告 — 无障碍与本地化组

任务来源：`docs/ENGINE-Version-01XHI9DN.1.5xw-AI分工图.md`（U-40、U-41、M-73…M-78）。
纪律：只做 AI-19 分内任务；所有新行为默认关闭（零副作用 opt-in）；不做其他 AI 的任务。

## 交付清单

| 任务 | 内容 | 落点 |
|---|---|---|
| U-40 无障碍 2.0 | aria-live 通知区、焦点跟随提示、色觉模拟器（四类）、粘滞键序列累积器 | `src/lib/a11y.ts` |
| U-41 多语言中心 2.0 | i18n 运行时覆盖层、伪本地化、RTL 试点（设置/通知中心）、词条工作台（搜索/编辑/CSV 导入导出） | `src/i18n/dictionaries.ts` + `A11yTab.tsx` |
| M-73 系统辅助功能桥 | 只读探测系统粘滞键/筛选键/高对比度/讲述人（Windows API）；讲述人运行时自动叠加 reduce-motion；粘滞修饰键分步输入 | `src-tauri/src/shell/a11y.rs` + `src/lib/a11y.ts` |
| M-74 系统高对比度跟随 | 探针轮询 + `themeFollowHc` 设置，系统 HC 切换即时跟随 | `App.tsx` + `a11y.rs` |
| M-75 动效时长缩放 | `--dur-scale` CSS 变量全局乘入六档时长（0.5x/1x）；reduceMotion 优先级最高不受影响 | `src/design/tokens.css` + `App.tsx` |
| M-76 屏幕阅读器标注审计 | `npm run aria:audit` 静态扫描图标按钮 aria-label 覆盖率（当前 861/861 = 100%） | `tools/aria-audit.cjs` |
| M-77 简繁转换用户词表 | 上限 500 条、最长优先匹配、冲突检测、CSV 导入导出、translate 管线最后一级 | `src/i18n/s2t.ts` |
| M-78 区域格式跟随 | 日期/时间/数字/容量 Intl 本地化；容量单位 auto/binary/decimal | `src/lib/localeFormat.ts` |

## 新增/修改文件

**新增**：`src/lib/a11y.ts`、`src/lib/localeFormat.ts`、`src/features/settings/A11yTab.tsx`、`src/styles/ai19-a11y.css`、`src-tauri/src/shell/a11y.rs`、`tools/aria-audit.cjs`、`src/lib/__tests__/ai19-a11y-locale.test.ts`

**修改**：`src/lib/settings.ts`（9 个新设置项）、`src/i18n/dictionaries.ts`（运行时覆盖/伪本地化 + 71 条 a19 词条 zh/en）、`src/i18n/s2t.ts`（用户词表）、`src/App.tsx`（HC 跟随/动效缩放/CVD/RTL/词表接线）、`src/features/settings/SettingsModal.tsx`（注册 a11y 页）、`src/system/tray/QuickPanel.tsx`（RTL 试点）、`src/design/tokens.css`（--dur-scale）、`src/styles/global.css`、`src-tauri/src/lib.rs` + `src/lib/ipc.ts`（a11y_probe 命令）、`package.json`（aria:audit 脚本）

## 自检结果

- vitest AI-19 域：15/15 通过（粘滞键累积器/词表/区域格式）
- 全量 vitest：975 通过；6 个失败全部位于其他 AI 域（iconRegistry 语法错误、keymap Z-14、ai17-*、helpTopics），与 AI-19 文件无关
- tsc：AI-19 相关文件 0 错误（现存错误均来自 `src/lib/iconRegistry.ts`，非本组文件）
- cargo check / cargo test a11y：通过
- aria:audit：图标按钮标注覆盖 861/861（100%），0 个无标注图标按钮
- i18n：zh/en 各新增 71 条 a19 词条，zh-TW 经 convertDict 自动覆盖

## 已知边界

- M-73 探针为轮询式只读探测，非系统事件订阅（Windows 无公开变更事件，轮询间隔 2s）
- M-75 缩放只影响走 `--dur-*` 变量的动效；个别硬编码 transition-duration 不受影响
- M-76 审计为静态扫描（正则级），无法覆盖运行时动态渲染的按钮
- M-78 localeFormatFollow 默认关闭；已有 30 处显示点的逐点接线由开关统一启用，未逐点改造存量硬编码调用
- 伪本地化为 dev 检查工具，生产入口默认关闭
