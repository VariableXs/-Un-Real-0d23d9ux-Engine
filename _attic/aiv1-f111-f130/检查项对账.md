# AI-V1 检查项对账（自检条目 × 判据锚）· F111-F130

> 生成方式：逐模块提取 `set.add(` 条目名 → 按「模块 = 判据锚」归属 → 与主册 G-C-41~G-C-55 / G-D-01~G-D-05 对齐。
> 对账口径：每条自检 = 一条可执行判据句的机器化（CheckSet.add(name, passed, detail)），红项即判据红。
> **合计：329 条自检项 / 21 块 / 20 项功能 + 共享底盘 + 域聚合器。**

| 块 | 判据锚 | 自检条数 | 自检条目（判据句机器化） |
| --- | --- | --- | --- |
| F123 | G-C-53 关于本机页 | 11 | `Y7000 spec anchors all match`、`four groups organized`、`unreadable row honest + not copyable`、`row copy fidelity`、`unreadable row copy none`、`full report template complete`、`rename instant + empty rejected`、`live cpu freq + memory dual values`、`disk lifetime counters present`、`row height 65px + refresh 30s`、`report text four groups + honest blank` |
| F129 | G-D-04 社区判例提交线 | 8 | `e2e submit-to-accept under 30d`、`five states timestamps recorded`、`evidence hash chain locks submission`、`formal review rejection with triage`、`rate limit 5/24h per submitter`、`co-review hard gate needs 2 votes`、`rate window slides open next day`、`constants: votes + states + target` |
| F114 | G-C-44 色弱辅助滤镜 | 13 | `protan sim anchor value (230,50,40)->(79,70,39)`、`three sim types directional response`、`strength 50/100 correction interpolation`、`neutral gray preserved (3x)`、`correction improves sim-space separation >=50%`、`hotkey cycle 4 states keeps strength`、`frame budget 0.5ms auto-downgrade`、`min strength held on overload`、`tray icon follows state`、`screenshot original color default`、`overlay order + hotkey registered`、`off passthrough + preview/sim pipeline distinct`、`preview wheel paired samples` |
| F120 | G-C-50 诊断中心 | 15 | `four lamps consistent with F062 source`、`red lamp links to help`、`official repairs registered`、`three repairs executed with history`、`failed repair auto-rollback + triage text`、`repair then explicit rollback restores`、`sanitize export passes 3-check`、`unsanitized export keeps raw`、`large log splits into volumes`、`log viewer filter/search/window`、`manifest declares sanitize policy`、`timeline five segments contiguous`、`four tabs + window size`、`structured log query three axes`、`volume plan math` |
| F115 | G-C-45 专注模式 | 19 | `dim 20% on, 0% off`、`judge: normal intercept / high deliver / whitelist deliver`、`intercept count accurate (only normal)`、`pomodoro 25min auto-restore`、`hotkey hold 500ms anti-mistouch`、`focus switch within 16ms`、`closing summary honest count`、`session ledger + evict by cutoff`、`manual end restores now, stats kept`、`focus state not across reboot`、`bubbles suppressed on, screenshot unaffected`、`off state delivers all`、`per-window dim + custom clamp`、`ring progress + honest remaining text`、`quick card three states`、`rounds + break suggestion + reset`、`custom pomodoro tier + snap`、`ledger aggregate honest totals`、`hold debounce ignores re-press` |
| F119 | G-C-49 帮助中心 | 13 | `50 pages render non-empty with heading`、`md renders heading/table/code/image`、`jump marker expands to button`、`jump buttons spot-check 20/20`、`search first-hit 10/10 (full index)`、`index not ready falls back to title quick mode`、`dead jump falls back + counted`、`unclosed marker rendered literally`、`offline capable`、`window 960x640 + jump syntax doc`、`toc grouped by section preserves order`、`inverted index hits + empty-safe`、`font size tiers registered` |
| F113 | G-C-43 高对比度主题 | 15 | `both palettes all tokens >= 7:1`、`palette anchor colors exact`、`flat colors no gradient slots`、`border 2px focus 3px scrollbar 12px`、`border token independent`、`one-key toggle instant on/off`、`violation outline fallback + record`、`page scan all green`、`page scan catches low-contrast page`、`image content exempt`、`danger token meets floor both themes`、`focus rings meet floor`、`switch pipeline instant + counted`、`scan report renders with verdicts`、`icon variants registered 8 distinct` |
| F111 | G-C-41 放大镜 | 18 | `zoom 2x-16x resample per-tier`、`nearest sampling exact source pixel`、`bilinear midpoint interpolates`、`follow cycle 3 strategies keeps zoom`、`lens frame budget 12.5ms`、`zoom clamp 2..16 + grid at 16x`、`session memory restored`、`edge pan 3 tiers + top-edge trigger`、`lens mode ignores pan gesture`、`dirty region lens-bounded / fullscreen-covered`、`screenshot wysiwyg nearest`、`overlay order + hotkeys registered`、`lens feather 8px ramp`、`smooth follow lerp converges`、`lens ruler optional lines`、`toolbar four buttons availability`、`zoom remembered across off/on`、`wheel zoom same step semantics` |
| F124 | G-C-54 动画曲线总谱 | 17 | `five curve anchors exact`、`three duration tiers`、`bezier endpoints + enter monotonic`、`enter curve fast-start`、`spring overshoot 105% settles to 1`、`30-spot audit all within score`、`unregistered curve detected by gate`、`3 intensity tiers scale durations`、`progress fallback on reduced/off`、`frame budget downgrades to linear`、`use-table semantics documented`、`five curve names distinct`、`motion registry dedup + 30 sites in score`、`interrupt resumes from current progress`、`numeric progress readout`、`motion doc renders four sections`、`downgrade ledger records` |
| F112 | G-C-42 讲述人雏形 | 13 | `two scenes 100% readable names`、`template slider contextual reading`、`template disabled state`、`hotkey full cycle open-read-close`、`new focus interrupts old utterance`、`esc mute now and holds`、`rate default 1.2x clamped`、`tts failure silent-disable + report`、`no audio device greys toggle`、`voice ducks effects on open`、`full-read enqueues without interrupt`、`focus ring bold 2px`、`espeak isolation registered` |
| F116 | G-C-46 夜间模式 | 19 | `default anchors match master`、`24h curve domain + no jumps`、`drag crossing auto-sorted`、`3 strength tiers distinguishable`、`chroma meter ±200K gate`、`sunset trigger within ±5min`、`dark crossfade 300ms no-flash`、`gamma per-channel independent`、`honest degrade on gamma unsupported`、`game exemption honored`、`manual fallback times honored`、`quick tiers off/warm/warm+dark`、`overlay order consistent with F114`、`city table lookup`、`sun times noaa approximation`、`city auto applies astronomical times`、`scheduled tier independent clock`、`curve samples for chart + anchor marks`、`transition window ramps detection` |
| F117 | G-C-47 首次开机向导 | 15 | `five-step full path all done`、`all-skip path completes`、`power-loss resume from incomplete step`、`finish summary accurate`、`auto enter desktop after 5s`、`network fail skips to input step`、`esc skips current step`、`back keeps outcomes, re-commit overrides`、`no back on first step`、`notifications silenced during oobe`、`progress marks 5-dot semantics`、`card 560x420 + 4 theme cards`、`ime trytype candidates`、`progress dots three states`、`theme preview only on theme step` |
| F126 | G-D-01 开放格式宪法页 | 20 | `four spec pages complete`、`four sample packages pass validation`、`dual-read clause registered`、`semver clause + parsing`、`validator rejects 4 negative paths`、`changelog mandatory per spec`、`schema table rows render source`、`samples within 100KB cap`、`relation diagram registered`、`breaking-change ADR flow`、`ci sample replay 4/4`、`ci negative replay 9/9 rejected`、`dual-read engine window semantics`、`migration window announce duty`、`spec page render structure complete`、`changelog gate rejects bad updates`、`required fields carry constraint attrs`、`json schema full form`、`sample json full form`、`negative extended 12/12 rejected` |
| F130 | G-D-05 开源项目登记册 | 10 | `registry gate + gpl isolation mandatory`、`ci diff zero when aligned`、`ci catches unregistered and stale both ways`、`spdx families + isolation semantics`、`legal review logged per registration`、`search by name and license`、`upgrade window due scan`、`license change re-registers with re-review`、`schema 12 fields`、`sbom spdx convention` |
| F121 | G-C-51 系统还原点 | 11 | `four auto triggers snapshot`、`rollback hash matches snapshot point`、`100 power-loss injections all voided, base intact`、`rolling cap 10 + lock protects`、`rollback retries once then succeeds`、`retry exhausted rolls back the rollback`、`data layer untouched (config-only rollback)`、`rollback preview lists touched layers`、`tamper detected by point hash`、`over-budget warns and refuses`、`snapshot covers four-layer set` |
| F128 | G-D-03 星图开放数据面 | 23 | `full download-import-query loop`、`schema required fields in shard json`、`sign verify both ways + tamper rejected`、`source trust labels present`、`incremental since protocol`、`shards split under 50MB cap`、`snapshots kept 90 days`、`download resume from acked offset`、`three data entries registered`、`mirror guide four steps`、`cc-by license declared`、`ledger50 seeded 50/50 with canonical names`、`shard manifest + versioned path`、`incremental upserts+deletes full protocol`、`session table idempotent + resume`、`session table all done after fill`、`key ring rotation overlap window`、`mirror checklist four gates`、`daily snapshot scheduler + staleness`、`query three-axis + rating sort`、`seed cards 50 aligned with ledger50`、`snapshot full json 50 cards inline`、`seed sources cover all three tiers` |
| F122 | G-C-52 更新体验面 | 20 | `full flow offer-download-schedule-install-done`、`shutdown-time schedule accepted`、`power-loss mid-update rolls back usable`、`failure page triage text`、`resume from acked bytes`、`verify fail discards package`、`low battery defer suggestion`、`dnd suppresses toast`、`cancel allowed pre-verify, denied at verify`、`progress ring bound to real stage`、`whatsnew card capped at 3`、`toast summary constants`、`defer tiers schedule semantics`、`dual slot visual + confirm after reboot`、`fullscreen captions bound to stages`、`eta estimator honest semantics`、`package meta + signature gate`、`overall progress composed from stages`、`power menu badge on shutdown-schedule`、`retry clears failure and refull-flows` |
| 共享底盘 | 全域共用（无独立判据锚） | 16 | `sha256 abc vector`、`sha256 empty vector`、`sha256 streaming equals oneshot`、`hex short buffer rejects`、`contrast black/white 2100`、`contrast black/gray777 ~469`、`kelvin 6500 near white, 1900 warm`、`semver parse`、`json escape`、`sanitize user segment`、`sanitize serial`、`sanitize key`、`sanitize clean line passes`、`sanitize replace`、`chain hash progressive`、`trunc utf8 safe` |
| F127 | G-D-02 vxapp 打包工具 | 15 | `five directory shapes pack green`、`five artifacts validate green`、`tree hash deterministic + content-sensitive`、`manifest validation + exit code 1 + hints`、`missing entry reported with example`、`cache hit on unchanged, miss on change`、`sign-verify both ways + tamper rejected`、`keygen deterministic + seed-sensitive`、`wrong key rejected`、`interactive wizard fills and defaults`、`exit codes + pack budget`、`chunk boundary sensitivity`、`five form factors seeded`、`pack cache hit/miss/dedup`、`key fingerprint 16 hex` |
| F125 | G-C-55 体验域总判据 | 27 | `mapping coverage 55/55`、`checklist generated + anchors testable`、`green requires evidence trio`、`red auto-opens reflow ticket`、`reflow approve/exempt both recorded`、`20 dimensions weighted`、`dimension anchors within map`、`report render versioned + reds listed`、`weekly audit due at 7d`、`reflow queue cap 32 enforced`、`owner column covers all squads`、`tool + dir conventions registered`、`criterion anchor G-C-xx round-trip`、`three-column map auto-gen + review dual-track`、`dimension ownership full coverage`、`reflow full state machine chain`、`reflow overdue 7d alert`、`walk session audit + close guard`、`dimension score rule 3/1/0`、`weighted total bp full=10000 empty=0`、`checklist export md + json`、`anchor extractor three kinds`、`r8 drift scan + ledger rate`、`quarter cadence + freeze discipline`、`walk commands 55 join + v1 routed`、`full checklist md join map+cmd+owner`、`audit batch date registered` |
| F118 | G-C-48 欢迎中心 | 11 | `five cards carry three mindsets`、`all card copy within 60 chars, above 20`、`jump paths all consistent`、`no second auto popup (complete or exit)`、`carousel bounded + skip-all always visible`、`slide transition within 250ms`、`completion resettable for demo`、`missing illustrations degrade text-only`、`manual re-entry independent of auto flag`、`card 560x420 + 5 cards`、`swipe verdict three branches` |

## 对账结论

1. **覆盖**：21 块全部有自检（0 块空转）；329 条条目逐一归属唯一判据锚——无孤儿条目、无跨块混挂。
2. **可测性**：每条目名即判据句机器化（主册判据句 → CheckSet 断言），红项可定位到模块+条目+复现口令（WALK_COMMANDS v4 侧表）。
3. **容量**：单模块最大自检块（walkcheck 27 条）远低于 CheckSet 64 条上限——零截断（聚合器 truncated 位全 false）。
4. **十二查映射**：功能完整=各条目主体；性能线=帧预算/延迟条目（magnifier/updateux/focusmode 等）；台账证据=walkcheck 证据三件套条目；回归零破坏=聚合器 all_passed 门。
5. **证据链**：`cargo test`（隔离舱 varix-svcabin）逐块跑通即对账执行面；红项自动阻断域聚合（mod.rs all_passed 门）。
