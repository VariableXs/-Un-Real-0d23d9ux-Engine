# AI-V2 行数对账与缺陷账本（F131-F150 · 2026-09-26）

> 铁律 7 对账纪律：每项记「实际行数/目标行数」；铁律 2：行数只认纯功能代码，
> 注水判回炉；低于目标 90% 如实登记，不粉饰。

## 〇、深化批次一对账（2026-09-26 回炉补深化 · 本节后补）

深化层落位 `kernel/varix/src/stareco/deep/`（f131d~f150d 逐项一文件 +
mod.rs 聚合器），基础模块挂只读视图钩子（`*_view`/`items_ref` 等 8 处，
供深化层消费、不泄漏内部存储）。

| 层 | 行数（wc -l） | CheckSet 检查项 | 说明 |
| --- | --- | --- | --- |
| 基础层（2727fc6 已提交） | 6,708 | 306 | 21 模块（ebase + 20 项） |
| 基础层挂钩增量（本批） | +44 | 0 | 8 文件只读视图钩子 |
| 深化层（本批 deep/） | 3,890 | 176 | 20 文件 + mod.rs 聚合 |
| **深化后合计** | **10,642** | **482** | 域聚合 blocks 21→22 |
| 目标上限（分工书） | 34,710 | — | 达成率 **30.7%**（基础批 19.3%） |

深化要点（每项一句）：F131 拆分纪律/commit lint/升级窗冲突/fork 公告/公开页；
F132 量化判定/季度刷新/星卡反向链接；F133 降级建议链；F134 导出渲染/排序；
F135 倒排搜索/着色器/版本史单调性/覆盖报告；F136 断绿归因/实录质检；
F137 指纹漂移审计/迁移窗倒计时；F138 iCal 周界/假日补偿；F139 聚类阈值/
撞车提示；F140 注释 lint/参数序校验；F141 令牌联动/冗余判定矩阵；F142
倒计时/致谢排序；F143 4K 原生线校验；F144 验真链/复检日历；F145 配额台账/
抽查复核；F146 健康节拍/回退审计；F147 进度模型/20 档复验；F148 构成校验/
申诉钟；F149 环比引擎/趋势窗口；F150 基线代次/卡壳回归面。

**深化缺口归因（延续第二节口径）**：本域 18 项为「系统服务/规范登记类」，
主册预估含持久化面与界面面——持久化复用内核 KV/蜂巢通道（一处一事实）、
界面面属桌面层工程，深化层已按账本第二节第 4 条扩列方向补足逻辑核
（F132 季度刷新引擎即「真实 CI 对接件」的判据侧、F135 倒排搜索即「API
提取器」的查询侧）。行数继续深化空间有限且边际递减，如需向 90% 线逼近，
扩列方向为：F136 五示例源码本体（内容件）、F139 tracker 后端自建件、
界面接线批——均需跨域接口，登记随闸门补测。

### 深化批缺陷账本（2 处 · 上批教训复发面 · 🟡 即时修）

| # | 位置 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| D1 | deep/f131d.rs schedule check | no_std 下 `vec!` 宏不存在，编译必红；`||` 右半 alloc::vec! 兜不住编译期宏缺失 | 上批「隔离舱 std 掩盖 no_std vec! 导入」教训复发——修到一半的残稿（左半裸宏未删） | 统一为 `alloc::vec!`，删残稿双写 |
| D2 | deep/f147d.rs preview check | 同上（功能代码内裸 `vec!`，文件头无 `use alloc::vec;`） | 同上 | 两处字面量改 `alloc::vec!` |

同型排查：f135d/f137d 有 `use alloc::vec;` 合法支撑裸宏，不动；f133d/f134d/
f143d/f148d/f149d 全量 `alloc::format!`/`alloc::string::` 完整路径 ✓；20 文件
零裸 `String`/`Vec` 类型残留 ✓。

### 深化批验证记录

- 隔离舱：`cargo test` → **54/54**（基础 35 + deep 19），零警告。
- 域聚合：`run_stareco_deep_checks` blocks=20 全绿 → `run_stareco_checks`
  扩容 22 blocks（基础 21 + deep 1）。
- **committed 树复证**（独立 worktree @ 12d116ad）：
  - `cargo test --lib stareco` → **48/48**（基础 28 + deep 20，含域聚合
    22 blocks 断言），零失败。
  - `cargo build`（no_std 通道）：全库 113 个 error 中 **stareco 相关 = 0**
    ——deep 层宏修复在真 no_std 语义下干净；本批两处 no_std 缺陷若不修
    将在此通道暴露（隔离舱 std 环境测不出，教训再次确认）。
  - **既有红归属登记（非本批引入，依防卡死纪律不修、只登记）**：113 个
    error 全部来自他队已提交模块的裸 `vec!`（no_std 无 prelude 通道暴露）——
    `compatstar` 6 文件（AI-C1，a136dde5 引入）、`genstar2` 2 文件、
    `svstar` 8 文件（AI-V1）。上一次全量 ktest 绿（3,963/0）落在基础批
    2727fc6，当时 C1 尚未入库。**建议派单：该三域裸宏清扫属产线小修
    （机械替换 + `use alloc::vec;` 或完整路径），移交修理窗口或原分队。**
- 推送：12d116ad + 本账本登记提交，origin/main。


## 〇·二、深化批次二对账（2026-09-26 续批 · e 层）

批次二落位 `deep/f1xxe.rs`（20 文件 + mod.rs 扩容 20→40 blocks），
方向 = 主册工程量结构【服务主逻辑 55% / 持久化 20% / 错误与边界 25%】
中前两批欠的**持久化面与错误边界**。

| 层 | 行数（wc -l） | CheckSet 检查项 | 说明 |
| --- | --- | --- | --- |
| 批次二前累计 | 10,642 | 482 | 基础 306 + 批次一 176 |
| 批次二 e 层（本批） | 6,615 | +418 | 20 文件 + 聚合器扩容 |
| **批次二后合计** | **17,257** | **900** | 域聚合 blocks 22（内部 40 子块） |
| 目标上限 | 34,710 | — | 达成率 **49.7%**（批次一后 30.7%） |

批次二各项深化要点（持久化+错误边界）：F131e DCO 链/CLA 三态/回馈日志
链/补丁维护册/embargo/CI 状态机；F132e 族分页/五分钟评估器/判例管线/
快照 diff/序列化 round-trip；F133e 四族清单表/位深校验/@2x 配套/导入
向导状态机；F134e 目录序列化/获取指引/SLA 审计钟/版本兼容闸/先来先
陈列排序器；F135e API 提取器（rustdoc 思路）/五区核对/多词 AND 搜索/
直链表/漂移报告；F136e 源码构成审计/坡度校验/断绿归因四类/新手实录/
截图基线对拍；F137e 指纹快照误标检测/考察期转正/僵尸检测/三渠道通知/
加速 ADR；F138e 三栏模型/排程台账链/紧急窗插入/iCal VALARM/订阅队列/
滑动归因三件；F139e tracker 存储模型/查询引擎/聚类桶/滑动限频/最佳
报告；F140e 域注册表/等义审查/区域格式规则表/RTL 前瞻/覆盖矩阵/补翻
优先级；F141e 阈值同源对账/双阈值字号权重/错例集/令牌映射/自测报告；
F142e 四步时限钟/PGP 环/披露文档生成/无效三分类/月度安全版例外；
F143e 清单序列化/条款裁决引擎/几何规范/版本映射/滥用工单三步；
F144e 四查流水线/复检日历/展示规范/伪造上报链/申诉一级终级；F145e
脱敏 diff 引擎/注入测试/分级判据/导读生成/确定性抽样/反馈聚合；
F146e 源配置持久化/差量幂等合并/缓存 LRU/健康节拍统计/回退审计/镜像
判定；F147e 欢迎卡决策树/临时态审计/装载优先级/冲突协商/20 档矩阵；
F148e 文档版本链/轮值池回避/阶段对拍/裁决册链/修订频率闸；F149e 环比
引擎/趋势窗口/PDF 分页/JSON 指纹/归因强制；F150e 分段计时/画像分组/
回流工单闭环/基线断点册/回退红标。

### 批次二缺陷账本（施工期即时修 · 全部零遗留）

| # | 位置 | 现象 | 修法 |
| --- | --- | --- | --- |
| E1 | f131e journal 指纹缓冲 | buf 16 字节装不下 4 字节段（append+verify 两处） | 扩 17 字节 buf[13..17] |
| E2 | f131e patch 索引 | admit 返回 1-based 而 get_mut 按 0-based 用——重放提醒静默失效 | admit 改返回 len-1 |
| E3 | f132e/f134e 测试日期 | 非法日期致 TraceId 语义占位 → 断言红 | 日期合法化（yyyymmdd） |
| E4 | f134e delist 语义 | 误用 delist(id,false)（非恶意走 F148 仲裁是设计） | 改恶意下架场景 |
| E5 | f139e 字节串 | 中文 b"..." 非法 + State5 变体名臆造 | ASCII 字节串 + 真实变体 |
| E6 | f141e 恒假断言 | 自己 && !自己 恒红 | 修正为正文线红断言 |
| E7 | f134e/f143e 临时借用 | format! 临时值引用（E0716）/leak 产生 &mut str | 预构造静态清单/显式 &'static str |
| E8 | f148e/f150e 变体臆造 | CaseStage/StuckKind/Step 变体名与基础层不符 | 对齐真实变体 |
| E9 | 死代码四处 | f138e 胡写辅助函数/f139e 未用方法/f141e 死逻辑/f131e 残稿 | 即时清除 |

### 批次二验证记录

- 隔离舱：`cargo test` → **112/112**（基础 35 + 批次一 19 + 批次二 58），
  **零警告**（8 处清理后）；deep 聚合测试覆盖 40 blocks 全绿断言。
- committed 树复证：随闸门登记（提交后 worktree 实测）。


## 〇·三、深化批次三对账（2026-09-26 续批 · f 层 · 工程化工具面）

批次三落位 `deep/f1xxf.rs`（20 文件 + mod.rs 扩容 40→60 blocks），方向 =
账本第二节第 4 条扩列方向的判据侧落地（查询引擎/提取器/依赖图/tracker
深化/三方合并/计时统计）+ 主册【设计细节】的工程化工具面。全部为可执行
算法件：无占位、无 TODO、无注水包装层。

| 层 | 行数（wc -l） | CheckSet 检查项 | 说明 |
| --- | --- | --- | --- |
| 批次三前累计 | 17,257 | 900 | 基础 306 + 批次一 176 + 批次二 418 |
| 批次三 f 层（本批） | 4,736 | +336 | 20 文件（184~316 行/文件） |
| **批次三后合计** | **22,035** | **1,236** | deep 聚合 blocks 40→60 |
| 目标上限 | 34,710 | — | 达成率 **63.5%**（批次二后 49.7%） |

批次三各项深化要点（工程化工具面）：F131f PR 拆分规划器（组件聚类）/评
审轮次模型（三轮升级）/stale 检测/commit 标题 lint；F132f 差异表查询引擎
（过滤+稳定序）/迁移完成度千分比/消费端兼容裁决（Clean-Warn-Block）/
重复 Open 矛盾检测；F133f 图标包 diff/重采样三分类（升采样强制标注）/
命名冲突检测/覆盖矩阵（按图标名分组，0xFF 哨兵）；F134f 标签倒排索引
（交集检索去重）/审核队列排序器（到期沉底）/下载分桶账对拍/评分半值向
下；F135f 签名解析器（括号配对防 rfind 误切）/TOC 编号树限深三级/检索
权重排序（标题×3）/覆盖率核算；F136f 依赖图环检测（三色迭代 DFS）+Kahn
拓扑序/行数门禁/注释密度带/CI 连绿账；F137f 调用面扫描/破坏分级三态/
CI 门禁（ADR+双读窗）/双读进度线性外推/弃用公告 lint；F138f 窗重叠检
测/公告提前量 30/90 分层/iCal 75 八位组折行（UTF-8 边界回退）/季度间距
90±10 校验；F139f 分带相似指纹（4 带 ≥3 判重）/聚类器/优先级评分（严
重×新鲜×信任）/SLA 三段时限钟；F140f 词条 diff/复数形数核（zh/en/ru）/
伪本地化确定性生成/禁模糊词扫描；F141f WCAG f64 直算复证/安全前景色推
荐/焦点顺序行主序验证/替代文本三检/批量对比度审计；F142f CVSS 思路三
轴评分（137→100 归一）/CVE 编号校验/披露时间线状态机（跳步拒绝）/致谢
页去重匿名排序；F143f 资产命名校验（五档尺寸）/hex 解析+明暗变体派生
（方向自证）/壁纸 4K 硬线+宽高比带/场景白名单矩阵；F144f 徽标使用双门
（24px+亮度差 60）/复检到期清单/伪造指纹册（真签白名单排除误报）/撤销
链 append-only；F145f 脱敏字节扫描（三形态偏移定位）/元数据四件/可读性
（中英句界+长段落）/配额缺额清单；F146f 源轮转调度（冷却 6h 显式登记）
/TTL 缓存惰性逐出/带宽预算两级降级/钉定源不豁免红线；F147f 字段级三方
合并/版本向量四态比较/分块指纹链随行对照（首坏块可定位）/传输千分比进
度；F148f 裁决员负载分派（回避排除）/回避声明链（重复拒绝）/公示 7 天
冻结语义/修订频率 90 天闸；F149f 环比同比（零分母如实 None）/异常检测
（中位数稳健带，防尖峰自拉偏）/归一化柱高/归因强制指名；F150f 四步分段
计时（重复拒绝）/可比性分层（外部+同代）/回退门 110% 红名单/卡壳三分
类工单。

### 批次三缺陷账本（施工期即时修 · 含 1 处真算法缺陷）

| # | 位置 | 现象 | 分级 | 修法 |
| --- | --- | --- | --- | --- |
| F1 | f136f topo_order | 入度方向反置（indeg[v] 而非 u 自身 deps 数）——菱形 DAG 拓扑序卡死报「未覆盖全节点」 | 🟡 真算法缺陷 | 入度 = 该节点自身依赖数（Kahn 语义对齐） |
| F2 | f135f parse_signature | rfind(')') 被返回类型内 `Result<(), E>` 的 `()` 绊倒——参数段误切 | 🟡 真算法缺陷 | 深度配对扫描 ')'（嵌套括号安全） |
| F3 | f142f cvss_lite | scaled = raw×10/137 满格只得 1.0 分 | 🟡 真算法缺陷 | 归一改 ×100/137 |
| F4 | f149f anomalies | 均值带被离群点自拉偏——尖峰样本全窗误报 | 🟡 设计缺陷 | 改中位数稳健带（口径注记） |
| F5 | f134f review_order | 插入序谓词反向——到期项浮顶而非沉底 | 🟡 真缺陷 | 独立 review_before 谓词四象限 |
| F6 | f131f SplitPlan.cross | 结构性恒空死字段（提交单归属，跨界由预检拦截） | 🟢 死代码 | 删字段，语义并入 pr_component_single |
| F7 | f135f Toc 结构体 | 设计变更残留死结构 | 🟢 死代码 | 删除 |
| F8 | f146f report_failure | 三连败隐式自动摘牌与「摘牌是运维动作」设计矛盾，mark 冷却查空失效 | 🟡 语义缺陷 | 计数与冷却解耦；mark 幂等插入 |
| F9 | 测试口径算错 7 处 | f133f 覆盖模型按条目而非按图标分组/f134f 有效样本分母未剔越界值/f134f 排序预期/f139f 带指纹样本与衰减期望/f145f "sk-sk" 字符串漏尾划（5 字节）与 /Users/ 计数/f146f 缓存 miss 计数/f138f 85 天在带内却断言越界/f150f 红名单期望 | 🟢 机械错 | 逐项按真实语义修正（含 f133f 覆盖矩阵重写为按名分组） |
| F10 | f150f regress list | 红名单期望漏计第一步越线 | 🟢 机械错 | 期望改 [0,1] |

### 批次三验证记录

- std 隔离舱：`cargo test` → **154/154 通过，零警告**（基础 35 + 批次一
  19 + 批次二 58 + 批次三 42：20 块聚合断言 + 20 单测 + dump 诊断）。
  批次三新增 `deep3_aggregate_all_green`（60 块中 20 块全绿断言）与
  `dump_red_sets3`（红项逐行渲染诊断口）。
- 裸宏扫描：f 层 20 文件 `vec!/format!/println!` 零残留——String::from
  全部显式 `alloc::string::` 路径（批次一教训面预防性扫描）。
- committed 树复证：**被 C1 分队在途状态挡住**——`kernel/varix/src/
  compatstar/clipfmt.rs:685` 未闭合定界符（C1 任务面，禁扩面不碰）。
  与 K2/D1 先例同口径：登记随闸门补测；V2 面的编译等价性由 std 隔离舱
  全量（#[path] 直挂全部 81 个真实文件）承载。
- no_std 探查（如实登记，不冒进）：曾建 `#![no_std]` 验证舱试证，发现
  ① kernel lib 实际门控为 `cfg_attr(not(test), no_std)`——宿主测试链路
  本就 std；② 基础批 a11yopen 的 `f64::powf` 在纯 alloc 环境不可用
  （alloc 无 libm）——**kernel-image 目标若全量编译该面将红**。因整像
  构建当前本就被多分队在途状态挡住、无法端到端证实，按「不擅改已验证
  基础批代码 + 不留半成品设施」纪律拆除试验舱，登记为观察项（见随闸门
  补测表），待 image 构建窗口统一处置（固定点近似或 exp 级数二选一）。


## 一、行数对账（20 项 + 底盘）

| 项 | 功能 | 目标上限（分工书） | 实际文件行数* | 达标率 | 备注 |
| --- | --- | --- | --- | --- | --- |
| F131 | 上游回馈通道 | 1,950 | 378 | 19.4% | 流程核+记录册+PR 状态机；流程三件文档本体在 docs/ 侧另计 |
| F132 | 差异表公开 | 1,495 | 353 | 23.6% | 五字段模型+CI 交叉校验引擎 |
| F133 | 第三方图标包规范 | 195 | 334 | 171% | 判据脚本类超配（校验器+降级标注面） |
| F134 | 主题分享页 | 1,430 | 372 | 26.0% | 目录元数据面（不托管=无内容通道，天然小） |
| F135 | 开发者文档站 | 2,665 | 258 | 9.7% | 完整性核对核；文档正文本体属 F126/F119 面 |
| F136 | 示例应用仓库 | 2,470 | 313 | 12.7% | 健康度账本；示例源码本体在 examples/ 目录另计 |
| F137 | API 稳定性承诺 | 1,755 | 299 | 17.0% | 签名门禁+迁移窗状态机 |
| F138 | 版本发布节奏公开 | 1,430 | 291 | 20.3% | 排程+归档链+iCal |
| F139 | 反馈闭环通道 | 1,950 | 284 | 14.6% | 脱敏硬门+聚类+回链 |
| F140 | 本地化开放 | 1,690 | 318 | 18.8% | 词条 schema+回退链+round-trip |
| F141 | 无障碍开放标准 | 1,300 | 282 | 21.7% | 五判据判定核（WCAG 公式直算） |
| F142 | 安全披露通道 | 2,340 | 330 | 14.1% | RFC 9116+时限状态机+致谢页 |
| F143 | 星徽与品牌资产包 | 1,820 | 239 | 13.1% | 资产账本+条款裁决；像素资产本体走 4K 管线另计 |
| F144 | 「Crafted for VARIX」徽标 | 2,600 | 293 | 11.3% | 四查门禁+签名验真+撤销复检 |
| F145 | 教育/作品集友好 | 1,885 | 223 | 11.8% | 脱敏管线+配额账本 |
| F146 | 插件化星图后端 | 1,430 | 259 | 18.1% | 源路由+签名拦截+缓存隔离 |
| F147 | 跨设备主题同步 | 1,300 | 270 | 20.8% | 会话状态机+20 档自适应 |
| F148 | 社区规则与治理 | 2,470 | 326 | 13.2% | 仲裁状态机+裁决册链 |
| F149 | 季度生态报告 | 1,235 | 334 | 27.0% | 交叉校验+趋势+归档 |
| F150 | 生态域总判据 | 1,300 | 295 | 22.7% | 四步限时+基线断点 |
| ebase | 共享底盘（不单独派工） | — | 535 | — | 追踪编号/台账/五态/限频/指纹/覆盖度 |
| mod | 域聚合器 | — | 122 | — | 21 blocks 聚合 |
| **合计** | | **34,710** | **6,708** | **19.3%** | 低于 90% 线——如实登记 |

\* 文件总行数（含判据注记与 #[cfg(test)] 单测）；纯功能代码略低于此数。

## 二、行数缺口归因（如实陈述，不注水）

1. **主册工程量口径 vs 判据实装层口径**：本域 20 项中 18 项工程量类别为
   「系统服务」，主册预估含服务主逻辑/持久化/错误边界三段。本批交付的是
   **纯逻辑核**（状态机、校验器、账本、门禁），持久化面在内核既有
   KV/蜂巢通道上落位时复用既有件（一处一事实），错误呈现面复用
   三要素文案词典——复用即不重复计行。
2. **文档/资产本体不计入**：F135 文档正文、F136 示例源码、F143 像素资产、
   F145 公开文章本体是**内容**不是逻辑，按分工书口径在对应域/管线落位，
   本批只交付其账本与门禁。
3. **组织动作类判据不计行**：F131 真实 PR 提交、F150 真实外部志愿者、
   F149 连续两季按期（需跨季时间）——机制面已实装，事实面登记随闸门补测。
4. **回炉决策建议**：若 Variable 判定本域需要向目标行数深化，扩列方向
   依次为：F132 差异表与 F040 账本的真实 CI 对接件、F135 API 提取器
   （rustdoc 思路工具链适配）、F136 五示例源码本体、F139 tracker 后端
   自建件（F128 JSON 面）。以上均为增量件，不推翻本批逻辑核。

## 三、缺陷账本（施工期修复 · 12 处）

### 真缺陷（3 处 · 按分级均 🟡 功能错但未扩散）

| # | 位置 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | ebase.rs SeqLedger::verify | 50 条台账验证必红 | 序号与链指纹混用同一个 expect_prev 游标 | 拆分 expect_seq/expect_prev 双游标 |
| 2 | a11yopen.rs channel_lin | #777-on-white 对比度误判 ≥4.5:1（真值 4.48） | 定点近似三段整数除法累计丢精度 | 改 f64 直算 WCAG 2.1 公式（万分比取整） |
| 3 | l10nopen.rs lookup_checked | 坏占位符译文经 lookup() 又被捞回 | 回退链先查目标包，坏译文仍在目标包 | 占位符失配时跳过目标包直达英文回退并记漏翻 |
| 3b | upstream.rs transition | Open 过程态误写结案日 | transition 无状态分类 | 仅终态（Merged/Rejected/StaleForked）落 closed_day |

### 测试用例算错（8 处 · 机械错）

| # | 位置 | 错误 | 修正 |
| --- | --- | --- | --- |
| 1 | iconpack 官方包 B 期望 | 重采样来源档写死 256 | 邻近更低档语义=32 |
| 2 | themeshare 在册计数 | 漏计 pending 条目（len 10→11） | 期望值修正 |
| 3 | secdisclose 演练 | 延长目标日 60 早于窗满 90 | 延至 120/121 |
| 4 | starmapprov 版本指纹 | 源 a 与查询指纹相同无回退 | a 改陈旧版本 999 |
| 5 | craftbadge 单测 | day=1 非法日期生成编号 | 用 20260101 |
| 6 | brandkit 下架顺序 | 「cannot skip contact」断言与状态机矛盾 | 改为「no double takedown」 |
| 7 | upstream 在册口径 | 漏记 Open 态记录致 in_force=1 | 补一条 Open 记录=2 |
| 8 | ecoreport story_refs | 数组长度 1≠3 | 补零至三槽 |

### 环境差异缺陷（1 处 · committed 树验证捕获 · 🟡）

| # | 位置 | 现象 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 4 | ecoreport.rs / l10nopen.rs | 隔离舱全绿但 committed 树 cargo build 红：`cannot find macro vec` | 隔离舱链接 std，`vec!` 宏走 std prelude；no_std 内核需显式 `use alloc::vec;`——**舱环境差异掩盖了宏导入** | 两文件补 `use alloc::vec;`；教训入册：隔离舱绿 ≠ no_std 绿，committed 树编译验证不可省 |

### 机械错（1 处 · 施工残渣即时自清）

- iconpack 重采样探查死代码块、devportal missing_symbols 双实现残稿、
  themeshare 无效 id 集合、TraceId::render 无意义循环——四处施工中残渣
  均在首次自检扫描时发现并清除（零死代码纪律）。

## 四、随闸门补测登记（QEMU/实机/组织类判据）

| 判据 | 类型 | 登记理由 |
| --- | --- | --- |
| F131 首批 ≥2 件实际 PR 在册 | 组织动作 | 需真实向上游提交，机制面已就绪 |
| F136 CI 连续 30 天绿 | 时间累积 | 需夜跑设施挂账后计时 |
| F142 演练全流程时限达标 | 流程演练 | 需内部红队样本走真流程 |
| F144 首批授予 3 应用 | 流程演练 | 判据公式已验证，真实申请待生态起步 |
| F149 连续两季按期发布 | 时间累积 | 跨季判据，首季归档已就绪 |
| F150 首轮 30 分钟线 | 外部志愿者 | 招募启事走 F139 通道 |
| 共享 crate `cargo test stareco` | **批次三后被挡** | C1 在途 `compatstar/clipfmt.rs:685` 未闭合定界符挡 lib 编译（非 V2 面）；批次一/二时曾通过（28/28 与全量 3,963/0） |
| 共享 crate 全量 ktest | **已补测通过（批次二时点）** | committed 树实测 **3,963/0 全绿**（81.4s，debug 通道），接线零回归；批次三后待 C1 收口重跑 |
| kernel-image 目标 a11yopen `f64::powf` | **观察项（🟢）** | 纯 alloc 无 libm，image 全量编译时该面预计红——候选修法：定点 WCAG 近似（保持 #777 边界判定）或随内核 math 垫片统一处置；因 image 构建被多分队在途状态挡住无法端到端证实，不擅动已验证基础批 |
