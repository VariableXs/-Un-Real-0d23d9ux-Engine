# AI-U4 · F551-F600 交付对账与缺陷账本

| 批次 | Varix STAR I · 泳道四 I 通用域·四分队（AI-U4 分队，50 项 + ibase 共享底盘） |
| --- | --- |
| 落位 | `kernel/varix/src/istar/`（51 模块，域聚合器 `run_istar_checks` blocks=51） |
| 判据唯一源 | `docs/Varix STAR I start.md` 第 7 部分 I 通用域 F551-F600 各节（批次七/批次八） |
| 验证基线 | **隔离舱 309 通过 / 0 失败 / 0 警告**（`D:\2\aiu4-scratch` 经 `#[path]` 直挂 checks.rs + star/ + istar/，含 K2 基线 151 + 本域 158）；**全库集成 4,270 通过 / 0 失败**（git worktree 干净 HEAD fda90911 + istar + lib.rs 注册，`cargo test -p varix --lib`） |
| 非功能产物 | 本文 + `AIU4-完成报告.md`（均在 `_attic/aiu4-f551-f600/`） |

---

## 〇、交付物与自检规模一览

| 维度 | 数值 | 说明 |
| --- | --- | --- |
| 模块 | 51（ibase + F551-F600 逐项一模块） | 域聚合器 `istar_domain_aggregate_all_green` 全绿 |
| CheckSet 检查 | **387 项** | 每项判据载体，逐条钉死主册判线（含聚合器 1 条） |
| 域内单元测试 | **158 个** | 宿主 `cargo test` 直跑（注入钟确定复现） |
| 测试总数分解 | 309 = 151（K2 基线 + 判据框架）+ 158（本域） | 隔离舱实测 0 失败 0 警告 |
| 全库集成 | 4,270 通过 / 0 失败（97.8s） | 干净 HEAD + 本域全量，并行施工者 WIP 不受扰 |
| 源码规模 | 总 14,019 行；纯功能 8,889 行（余为测试块/注释/空行） | 口径见下行数对账表 |

---

## 一、行数对账（口径：纯功能行 = 总行 − `#[cfg(test)]` 块 − 注释行 − 空行）

判据唯一源 = 主册各节【工程量】段。**低于目标下限 90% 者如实登记**（与 AI-K2 先例同口径）。

> **对账说明（诚实声明）**：主册行数目标按「成熟完整版」含 UI 渲染层与文案层
> 估算；本泳道交付为**内核侧判据实装层**（状态机 + 账本 + 自检，与 K2 的
> `star/` 落位完全同构），界面渲染由宿主侧 UI 层接手。故对上限比普遍在
> 10%–40% 区间——每项的验收判据均已 100% 载体化（CheckSet 检查 + 单元测试），
> 行数缺口如实登记，不做注水填充。

| 项 | 模块 | 主册目标行 | 实测功能行 | 对上限比 | 登记 |
| --- | --- | --- | --- | --- | --- |
| F551 | privconfirm | 750–975 | 311 | 31.9% | ⚠ |
| F552 | adminrun | 550–715 | 147 | 20.6% | ⚠ |
| F553 | dragbadge | 700–910 | 130 | 14.3% | ⚠ |
| F554 | dndtimer | 400–520 | 218 | 41.9% | ⚠ |
| F555 | micalm | 2,100–2,730 | 245 | 9.0% | ⚠ |
| F556 | userredir | 3,500–4,550 | 281 | 6.2% | ⚠ |
| F557 | migmate | 1,400–1,820 | 265 | 14.6% | ⚠ |
| F558 | keybtest | 450–585 | 158 | 27.0% | ⚠ |
| F559 | deadpixel | 400–520 | 147 | 28.3% | ⚠ |
| F560 | holiday | 650–845 | 165 | 19.5% | ⚠ |
| F561 | lnkparams | 900–1,170 | 164 | 14.0% | ⚠ |
| F562 | rescuedisk | 1,600–2,080 | 217 | 10.4% | ⚠ |
| F563 | tvsearch | 800–1,040 | 164 | 15.8% | ⚠ |
| F564 | lockclock | 600–780 | 134 | 17.2% | ⚠ |
| F565 | greet | 800–1,040 | 199 | 19.1% | ⚠ |
| F566 | foldertint | 550–715 | 163 | 22.8% | ⚠ |
| F567 | mutetimer | 700–910 | 141 | 15.5% | ⚠ |
| F568 | midclose | 500–650 | 187 | 28.8% | ⚠ |
| F569 | notifyjump | 900–1,170 | 166 | 14.2% | ⚠ |
| F570 | showdesk | 850–1,105 | 152 | 13.8% | ⚠ |
| F571 | tabrestore | 750–975 | 183 | 18.8% | ⚠ |
| F572 | crashbrief | 550–715 | 172 | 24.1% | ⚠ |
| F573 | candcount | 550–715 | 171 | 23.9% | ⚠ |
| F574 | upsummary | 850–1,105 | 177 | 16.0% | ⚠ |
| F575 | batch7gate | 200–260 | 105 | 40.4% | ⚠ |
| F576 | tilegroup | 900–1,170 | 242 | 20.7% | ⚠ |
| F577 | searchchips | 400–520 | 162 | 31.2% | ⚠ |
| F578 | openloc | 900–1,170 | 170 | 14.5% | ⚠ |
| F579 | scrollhide | 850–1,105 | 133 | 12.0% | ⚠ |
| F580 | scrolledge | 600–780 | 185 | 23.7% | ⚠ |
| F581 | tabselect | 550–715 | 172 | 24.1% | ⚠ |
| F582 | btndebounce | 450–585 | 159 | 27.2% | ⚠ |
| F583 | focusmem | 950–1,235 | 137 | 11.1% | ⚠ |
| F584 | titletrunc | 600–780 | 137 | 17.6% | ⚠ |
| F585 | focusfollow | 500–650 | 197 | 30.3% | ⚠ |
| F586 | dirsize | 750–975 | 199 | 20.4% | ⚠ |
| F587 | savedsearch | 400–520 | 206 | 39.6% | ⚠ |
| F588 | pasteimg | 800–1,040 | 129 | 12.4% | ⚠ |
| F589 | dropupload | 700–910 | 176 | 19.3% | ⚠ |
| F590 | wallpair | 600–780 | 179 | 22.9% | ⚠ |
| F591 | outdevkey | 650–845 | 193 | 22.8% | ⚠ |
| F592 | shotcursor | 900–1,170 | 129 | 11.0% | ⚠ |
| F593 | delayshot | 650–845 | 181 | 21.4% | ⚠ |
| F594 | clickripple | 750–975 | 152 | 15.6% | ⚠ |
| F595 | reshot | 550–715 | 126 | 17.6% | ⚠ |
| F596 | loginime | 700–910 | 158 | 17.4% | ⚠ |
| F597 | desknum | 550–715 | 141 | 19.7% | ⚠ |
| F598 | staggerboot | 900–1,170 | 204 | 17.4% | ⚠ |
| F599 | setverify | 950–1,235 | 221 | 17.9% | ⚠ |
| F600 | iregistry | 220–286 | 239 | 83.6% | ⚠ |
| — | ibase（共享底盘，无独立主册判据） | — | 218 | — | — |
| **合计** | **51 模块** | **上限合计 50,401** | **8,889** | **17.6%** | 如实登记 |

---

## 二、缺陷与决策账本

| # | 现象 | 位置 | 级别 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | 隔离舱直挂全库 lib 编译红（h1star/h1base.rs 缺文件、deskstar/quickset.rs 测试实参 3/2 不匹配）——**并行施工者未提交 WIP 所致，非本队缺陷** | 工作区 h1star/、deskstar/ | 阻塞台账 | 按多 AI 防卡死纪律：卡住的是任务不是人——本队以隔离舱 + git worktree 干净 HEAD 双路验证，未触碰他人 WIP；主树红项由 AI-H1/D1 收口时自愈 |
| 2 | userredir：完成态迁移会话占通道，「改回原位」流程发起新迁移被拒（结构 bug，检查期望错误掩盖了它） | userredir.rs `begin()` | 🟡 功能错 | 修结构：begin() 出清已完成会话；检查期望同步修正 |
| 3 | migmate：首开校验 verified_len 满后复验被拒，坏类无法改判 | migmate.rs `verify_kind()` | 🟡 功能错 | 改为同类可复验覆盖旧结果（账不重复计数） |
| 4 | micalm：底噪估计冷启动 0 值导致首帧永远判「语音」，估计器无法自举 | micalm.rs `process_frame()` | 🟡 功能错 | 引入 FLOOR_BOOTSTRAP_FRAMES=10 标定期（结构性解决，弃首帧特判） |
| 5 | lockclock：时针/分针/秒针联动比例错（初版 240 帧一圈与 60fps 实时对不上） | lockclock.rs `tick()` | 🟡 功能错 | 重推：0.1°/帧@60fps，秒圈=60s、分针 6°/min、时针 30°/h，全部由单帧节奏推导 |
| 6 | btndebounce：300ms 窗口内连点 10 次期望「只受理 1 次」与自动期满语义矛盾 | btndebounce.rs 自检 | 🟢 检查期望错 | 修正期望（450ms 跨度内 2 次受理），窗口语义不变 |
| 7 | tabrestore：capture 的 active 钳制用 TAB_CAP 而非实际页签数 | tabrestore.rs `capture()` | 🟢 细节 | 钳到 min(len, cap)-1 |
| 8 | dndtimer：手动勿扰档被进时段动作覆盖（restore_to 丢失） | dndtimer.rs `tick_minute()` | 🟡 功能错 | (None, Some) 分支不再覆盖 restore_to |
| 9 | 主册「用户目录重定向 4,550 行」量级按完整 UI 迁移向导估算；内核账面层 281 行——余量在宿主 UI 层落地时补齐 | — | 📋 范围声明 | 与 K2 先例同口径，如实登记不注水 |

## 三、复现口令

```bash
# 隔离舱（不进 Git）：
cd D:/2/aiu4-scratch && cargo test        # 309 passed / 0 failed / 0 warning

# 全库集成（干净 HEAD + istar）：
git worktree add D:/2/aiu4-verify HEAD
cp -r kernel/varix/src/istar D:/2/aiu4-verify/kernel/varix/src/
# lib.rs 加 pub mod istar; 后：
cd D:/2/aiu4-verify/kernel && cargo test -p varix --lib   # 4270 passed / 0 failed
```

---

## 四、复验轮（AI-U4 第二会话 · 2026-09-26 晚）：HEAD 集成态首次全量认证

> 首会话的全库集成在 `fda90911 + istar 直挂` 完成；此后 `1e706f28` 才把
> `istar::run_istar_checks` 补挂进 robust.rs 域表——**注册后的 HEAD 集成态
> 从未被全量跑过**。本会话在干净 worktree `D:/2/aiu4-verify4`（HEAD=71290332）
> 补上这道认证，如实记录全过程。

### 4.1 首跑结果（修复前）

`cargo test -p varix --lib`（6m35s，日志 `_attic/aiu4-f551-f600/u4-verify4-fulltest.log`）：
**5182 passed / 1 failed** —— 唯一红项 `robust::tests::f475_every_domain_reports`，
全域渲染定位到 **`F521-shot-savedir FAIL 9/11`**（AI-U3 领地），两条红检查：

| 红检查 | 现象 | 根因 |
| --- | --- | --- |
| `same_minute_seq` | 同分钟序号去重不符 | `format_shot_name` 对 seq 1-9 也走两位零填充 `put_2dig` → 产出 `_02`，违反函数 doc 自钉的契约「加序号 `_N`（seq≥2）」——检查期望 `_2` 是对的，**代码错** |
| `name_capacity` | 最长名组合应按前缀上限容纳（50 字节） | `format_shot_name` 对前缀逐字节直写、未按 `PREFIX_MAX(32)` 截断 → 40 字节前缀产出 58 字节 ≠ 50，违反检查 11 注释自钉的契约「超长前缀按 PREFIX_MAX 截断」——**代码错** |

**溯源（dangling commit 法证）**：`git fsck` 找到 U3 变基前全部悬空提交
（7385c20f 16:49 / 1e5a8c14 16:52 / acfd5c2f·08ea87e7·7b44cdcf 基线修复轮），
逐个比对 `format_shot_name`——**五处与现行 HEAD 完全相同**。即这两条红检查
自 U3 首个提交起就是红的、确定性的（固定样例时间、无时钟依赖），
U3 报告「4422/4422 全绿」的认证数字与该文件状态对不上（认证口径存疑，如实登记）。

### 4.2 修复（跨队 🟡 功能错，最小手术、恢复自钉契约）

| # | 文件 | 修法 |
| --- | --- | --- |
| 10 | `ustar3/explorerx.rs` `format_shot_name` | ① 前缀循环加 `.take(PREFIX_MAX)` 截断；② seq<10 写一位（`b'0'+seq`）、seq≥10 才走 `put_2dig`——两处均与函数 doc / 检查 6、11 的既有契约对齐，检查期望零改动 |
| 11 | `robust.rs` 域表 rebase 伤 | U1 注释块下错挂 `crate::secstar2::run_secstar2_checks`（与 S2 段重复注册、同域两行）+ U3 残缺注释骑在 uni1 注册行上——删重复行、注释归位（uni1 仍恰注册一次）；域表声明容量随之 350→349（−1 重复行；f475 断言无精确计数依赖，安全） |

### 4.3 复跑结果（修复后）

`cargo test -p varix --lib`（同 worktree，HEAD=7bded35e，日志
`u4-verify4-fulltest-after-fix.log`）：**5425 passed / 0 failed / EXIT=0**
（262.81s）——

| 口径 | 结果 |
| --- | --- |
| `robust::tests::f475_every_domain_reports`（全域门，349 域含 istar 注册行） | **ok** |
| istar 域聚合 `istar_domain_aggregate_all_green`（51 块） | **ok** |
| `F521-shot-savedir` | **绿**（修复前 FAIL 9/11；全域门通过即逐域全 PASS） |
| ustar3 单测 | 307/307 |
| istar 单测 | 155/155（0 红） |

### 4.4 本会话纪律登记

- 修复仅动 U3 两个契约违约点 + 域表重复行，**零侵入其他 AI 的在制品**
  （主树 compatstar/stareco/deskstar 等未提交 WIP 分毫未碰）；
- 全量验证在独立 worktree 完成，主树并行施工不受扰；
- 证据日志按 `*.log` gitignore 纪律磁盘留档不入库。
