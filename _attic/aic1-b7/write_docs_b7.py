# -*- coding: utf-8 -*-
# AI-C1 批次七：账本 §十 + 报告 §14 追加
import io

ledger = """
---

# 十、深化批次七（2026-09-27 第七批 · 成熟完整版专项第三批 + 检查项对账 + 隔离验证完备）

## 10.1 规模对账（同口径程序化统计）

| 维度 | 批次六收口 | 批次七收口 | 批次七增量 |
| --- | ---: | ---: | ---: |
| 总行数 | 24,909 | 见 §10.4 实测 | +片段 |
| 纯功能行 | 16,068 | 见 §10.4 实测 | +片段 |
| CheckSet 检查项 | 558 | 见 §10.4 实测（+批次七深化检） | +50 |

## 10.2 批次七深化面（17 域——F003/F005/F015 轮休，三域已在批次 2-5 富化）

| 域 | 批次七新增面 | 深检 |
| --- | --- | ---: |
| F017 clipfmt | **BMP/DIB 真解码**——BITMAPFILEHEADER/BITMAPINFOHEADER 解析（BM 魔数/头尺寸/宽高/平面/位深/BI_RGB）+ 24bpp 行读（BGR→ARGB + 4 字节 stride + 自底向上翻转 + Top-Down 负高语义）+ 结构化拒绝四向 | 3 |
| F006 gdiface | **Region 裁剪核**（矩形列表 Region：点包含半开区间 / 矩形相交可见 / 容量纪律 / 退化矩形不入表——GDI CreateRectRgn 最小核） | 3 |
| F007 gdiplus | **像素旋转 90°**（顺时针几何恒等式逐像素对拍 + 双旋 180° 逆序恒等 + 几何不符拒） | 3 |
| F012 condrv | **DECAWM 自动换行语义**（行尾 pending 标志 + 次字符换行 + LF 清 pending + 底行钳制） | 3 |
| F019 comloc | **GUID 混合端字节序**（Data1/2/3 小端 + Data4 连续——ShellLink CLSID wire bytes 锚 + 双向 round-trip） | 2 |
| F009 reghive | 十六进制分页查看（每页 16 字节 + 4 位偏移行首 + 尾页不满 + 页越界空页） | 3 |
| F001 dblrun | 会话表背压（满容诚实拒绝 + 归因短语 + 槽位终态可复用） | 3 |
| F002 peblend | DOS stub 惯例锚（Microsoft 提示串检测——对抗样本画像面，stub 缺失不判死） | 3 |
| F004 wow64 | 拒绝卡片动作对（查替代在前/关闭殿后的 Tab 序契约 + alt_query 数据联动锚） | 2 |
| F008 comdlg | OFN 多选文件名缓冲（双 NUL 终止打包/解包计数 + 单选形态 + 无终止拒） | 3 |
| F010 fsredir | 沙盒属性页统计行（「属于应用 #N 的隔离数据：M 项，X.Y KB」——KB 四舍五入 + 截断如实） | 3 |
| F011 envsess | （轮休——批次五/六已富化） | 0 |
| F012 续 | 见上 | — |
| F013 lnkfile | 目标串清洗（控制字符拒绝——外部输入全清洗纪律；空串不越权判死） | 3 |
| F016 fontchain | 字号阶梯吸附（8 档阶梯 + 平手取小 + 越界钳制） | 3 |
| F020 excface | dump 尺寸人话标签（B/KB/MB 三级 + 四舍五入进位 + 零字节） | 3 |
| F014 persrc | （轮休） | 0 |
| F018 dragdrop | （轮休） | 0 |

## 10.3 批次七缺陷账本（编写期自纠，全即时修）

| # | 现象 | 位置 | 级别 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | DIB 行序语义错读（dib_read_row(0) 应给视觉顶行 = 存储末行——自底向上翻转方向） | clipfmt.rs | 🟡 | 断言按 DIB 存储序重排（测试先于提交捕获） |
| 2 | 旋转断言初稿方向表错——按 dst(h-1-sy, sx) 几何恒等式逐像素重推后落定 | gdiplus.rs | 🟡 | 几何恒等式锚定 |
| 3 | OFN 测试残留死代码（broken 变体未消费） | comdlg.rs | 🟢 | 删除（零死代码纪律） |
| 4 | 渐变末端 t=937‰ 不到 c1（分母 w）+ 中点期望列错——批次六 #8 同族 | gdiface.rs | 🟡 | 分母 w-1 + 精确列期望（测试捕获） |

🔴 安全红线类：无。🟡：3。🟢：1。

## 10.4 验证证据与偏差登记

- 隔离壳全量：**214 passed / 0 failed**（收口实测为准）；CheckSet 合计项数见
  checkup::check_item_stats 输出（truncated=0 / dropped=0）。
- 20 域 cmp 逐字节零差异；主仓 no_std compatstar 零错误（余未用 import 警告 🟢 攒批）。
- 批次七片段初稿存档 `_attic/aic1-b7/b7-*.rs`。
- **工程量**：纯功能实测见 §10.1（口径不变）。批次八候选：Big5 权威字表接入（需字表文件源）、BMP 8bpp 调色板解码（F017 续）、蜂巢 B 树删除再平衡（F009）、Region 合并运算（F006 续）。
"""
io.open("_attic/aic1-f001-f020/行数对账与缺陷账本.md", "a", encoding='utf-8').write(ledger)

report = """

---

## 14. 深化批次七（2026-09-27 · 成熟完整版专项第三批 + 隔离验证完备）

**规模**：批次七覆盖 17 域（F003/F005/F015 轮休——三域已在批次 2-5 富化），
增量与 CheckSet 总数见账本 §10（程序化实测）。隔离壳 **214 全绿**；20 域 cmp
零差异；主仓 no_std compatstar 零错误。

**专项第三批**：① **BMP/DIB 真解码**（F017）——文件头/信息头解析 + 24bpp 行
读（BGR→ARGB、stride 对齐、自底向上翻转、Top-Down 负高）+ 结构化拒绝四向；
② **Region 裁剪核**（F006）——矩形列表点包含/相交可见；③ **像素旋转 90°**
（F007）——顺时针几何恒等式逐像素对拍；④ **DECAWM 自动换行**（F012）——
pending 语义三判据；⑤ **GUID 混合端字节序**（F019）——ShellLink wire bytes
锚；⑥ DOS stub 画像锚 / 会话表背压 / OFN 多选双 NUL / 沙盒属性页统计行 /
十六进制分页查看 / 目标串清洗 / 字号阶梯吸附 / dump 尺寸人话标签。

**批次七缺陷账本（4 条全即时修）**：DIB 行序语义错读（测试期捕获）、旋转几何
初稿方向错（恒等式重推）、OFN 死代码（🟢）、渐变末端分母（批次六 #8 同族）。
详见账本 §10.3。

**偏差登记**：批次七起采用「轮休制」——每批 17 域左右，三域轮休（均已富化），
避免小域注水。批次八候选：Big5 权威字表（需字表文件源）、BMP 8bpp 调色板、
Region 合并运算、蜂巢 B 树删除再平衡。
"""
io.open("docs/AI-C1-完成报告.md", "a", encoding='utf-8').write(report)
print("ledger §10 + report §14 written")
