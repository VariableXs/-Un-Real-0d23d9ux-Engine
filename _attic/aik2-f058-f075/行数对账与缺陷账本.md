# AI-K2 · F058-F075 交付对账与缺陷账本

| 批次 | Varix STAR I · 泳道一 B 内核底盘域·后段（AI-K2 分队，18 项 + sbase 共享底盘） |
| --- | --- |
| 落位 | `kernel/varix/src/star/`（19 模块，域聚合器 `run_star_checks` blocks=19） |
| 判据唯一源 | `docs/Varix STAR I start.md` G-B-18～30（F058-F070）、G-C-01～05（F071-F075） |
| 验证基线 | **143 通过 / 0 失败 / 0 警告**（隔离舱 `D:\2\aik2-scratch` 经 `#[path]` 直挂本目录实测） |
| 交付提交 | `2bd8a9d`（22 文件 +13,205 行），push origin main：`e6cc7ae..2bd8a9d`，2026-09-26 |
| 非功能产物 | 本文 + 对账脚本口径（见附录） |

---

## 〇、交付物与自检规模一览

| 维度 | 数值 | 说明 |
| --- | --- | --- |
| 模块 | 19（sbase + F058-F075 逐项一模块） | 域聚合器全绿 |
| CheckSet 检查 | **257 项** | 每项判据载体，逐条钉死主册判线 |
| 域内单元测试 | **136 个** | 宿主 `cargo test` 直跑（注入钟确定复现） |
| 测试总数分解 | 143 = 136（域内）+ 1（`star_domain_aggregate_all_green`）+ 6（`crate::checks` 判据框架自测） | 0 失败 0 警告 |
| 基线轨迹 | 98（F067 收口）→ 106（F068）→ 112（F069）→ 118（F070，任务 #4 关）→ 122（F071）→ 128（F072）→ 133（F073）→ 138（F074）→ **143（F075 收工）** | 每步全绿零警告 |
| 源码规模 | 总 12,943 行；纯功能 7,805 行（余为测试块/注释/空行） | 口径见附录脚本 |

---

## 一、验收判据达成对照（主册判据原文 → 内核侧核查面）

> 说明：每项判据在 CheckSet 内有对应检查（引用真实检查名），并有单元测试覆盖。
> 「达成」= 该判据在内核模型面可复现地通过；实机面（录屏/示波/长跑采集）按主册
> 属后续装机验证阶段，本泳道交付判据载体。

| 项 | 主册验收判据（摘） | 内核侧核查面（机制 + 代表检查） | 检查/测试 |
| --- | --- | --- | --- |
| F058 | 立项判据本身可验证（工作集采集先行）；启用后解压延迟与压缩比双达标 | LZ4 级算法核 roundtrip/重复序列增益/随机序列 + 畸形输入不 panic；立项门（水位不触发/触发）；压缩池账（存取/零值/压缩比 <1.5 不压/冷扫描/禁用态）——`lz4 malformed safe`、`gate high trigger`、`pool ratio gate`、`pool cold sweep` | 13 / 9 |
| F059 | SSH 打字 P99 ≤30ms；10k pps 风暴下交互流劣化 <20% | 流分类（small→interactive / big→bulk）；ACK 分流 5ms；风暴熔断开→限 bulk→放 interactive→迟滞关；NAPI budget 64 包；单/批路径运行时热切换幂等；SSH 线路正反两侧——`storm limits bulk`、`storm passes interactive`、`napi budget`、`hot switch idempotent` | 13 / 7 |
| F060 | 折算-实测相关性 >0.8；排行前三人工一致 | 能耗权重数学（CPU×频率档+唤醒+IO）；排行序与份额；30 天归档且归档仍可排行；隐私块；相关性核算；平坦电量诚实无排行——`weight math`、`correlation high`、`archived still ranked`、`flat gauge none` | 8 / 8 |
| F061 | 注入 5 处回退全检出；误报 <5%（30 天零噪声） | 基线建立/查重/有因退役且可见；注入捕获；首夜红→次夜告警两段式；告警入账；噪声带不误报；无基线态——`injection captured`、`first night red`、`second night alarm`、`noise band pass` | 13 / 8 |
| F062 | 阈值实机标定零误报；3 类慢故障全捕获 | 空闲门（不开窗/到点开窗）；四类异常全捕获；工单产出；帧指标缺失诚实降级；3 类注入故障捕获；降采样启用；同日工单合并——`all four anomalies`、`3 faults captured`、`frame metric off`、`same-day merged` | 8 / 6 |
| F063 | 评估报告三节即达标（评估件以报告为交付） | 报文解析 + 模糊 2000 轮不 panic；长度/触点显式拒绝；双指滚动 80ms 窗判定；输入法让行；捏合缩放；80ms 边界收窗；惯性 200ms ease-out 收敛；分级需 100+ 延迟样本——`malformed fuzz 2000 rounds no panic`、`gesture yields to typing (IME)`、`inertia 200ms ease-out converges` | 13 / 11 |
| F064 | 单流 20ms 线达标；双流混音增量 <2ms | 最劣段归因（DAC/供电双路）；p95 在线内；端到端公式单源；缓冲与混音增量账——`worst segment attribution dac`、`live stats p95 within line`、`e2e formula single source` | 13 / 9 |
| F065 | 合盖 2h 唤醒 ≤2 次、掉电 ≤2%；唤醒原因 100% 可解释 | 全已知源可解释 100%；抖动计数（小时内第 4 次告警）；正常唤醒不计抖动；60s 空闲自动回睡（可关，不足 60s 保持手动）；3 次唤醒会话诚实失败——`all known sources => 100% explained`、`jitter counted, alarm on 4th in hour`、`session with 3 wakes fails honestly` | 15 / 9 |
| F066 | 断电百次零 fsck；ordered 吞吐损失 <10% | 提交窗口单源；窗口内挂起/关窗提交一记录；校验和自洽且覆盖 seq；丢失上界 ≤ 窗口；边界定向截断损失有界；用量 PPT 记账；自检标记于最近恢复；恢复日志滚动上限；ordered 模式冻结——`lost bound <= window`、`checksum covers seq`、`boundary-directed cuts bounded loss` | 16 / 8 |
| F067 | 只读区随机读 P99 <2ms；只读段耗时占比 <25% | 布局拒绝（总量不足）；乱序甘特图检出；未知资产拒绝；活动槽禁 staging；占比 20% 达标 / 35% 诚实红；样本不足不出判；双判据齐——`ro time share 20% under 25%`、`ro share 35% honestly red`、`random p99 insufficient samples => no verdict` | 15 / 9 |
| F068 | 单主题驻留 ≤80MB（4K 全套实测）；切换无白屏 | 非选中主题不驻留；Top-100 图标预载；音效全驻；freed 差额单源；LRU 豁免活动主题；损坏报告 `&'static` 化；4K 全套实测 74.6MB ≤ 80MB——`unselected theme not resident`、`top-100 icons preloaded`、`freed diff single source` | 13 / 8 |
| F069 | 三档差异实测可辨；切换无爆音无卡顿 | 三档参数表（每档 4 旋钮 ×3=12）；clamp 边界裁决；**原子生效**（单 atom_ms 承载频率/窗口/缓冲——无中间态的结构证据）；同档拒绝；F197 温度联动（75℃ 强制静音覆盖手动、70℃ 迟滞解除）；旋钮版本门（缺失/越界 KnobMissing 拒切+报备）；持久化恢复；审计账；200ms 动画——`switch applies atomic parameter set`、`same-mode switch rejected`、`switch animation 200ms` | 18 / 6 |
| F070 | 整链 50 件文档化；80fps = P95 ≤12.5ms 且 P99 ≤16.6ms；成绩单两份证据 | 50 件场景名单唯一；四层门（空层不算绿、单红封层）；verdict 红线（P95>16.6ms=Red 回炉）；超标件入 heavy 不降判；证据双门；50 件未跑满拒绝出单（Incomplete 防线）；版本漂移清零防护；成绩单版本递增——`scenario names fifty unique`、`empty layer is not green`、`heavy app listed verdict not lowered`、`incomplete run blocks scorecard` | 20 / 6 |
| F071 | 三类查询首结果 10/10；≤50ms | 设置/应用/文件三类 10/10；打分公式单源（设置精确 100/前缀 80/文件 60+加权不越界）；拼音首字母；空查询短路；防抖 149/150 双侧；分区页签过滤——`setting query first-hit 10/10`、`app query first-hit 10/10`、`file query first-hit 10/10`、`section tabs filter by kind` | 13 / 4 |
| F072 | 三消费面一致；衰减下沉 | frecency 毫倍定点（满权 freq×1000；72h 恰减半钉死）；ranked() 唯一排序口；别名 FNV-1a 归并；固定置顶不衰减；500 LRU 逐出未固定最沉底；startmenu/quick_access/jumplist 三面同 API；隐私一键清空——`half-life at 72h`、`privacy clear all` | 13 / 6 |
| F073 | 一致性抽查 10 窗全对；出图 ≤400ms；前台帧率不掉 | 400ms 出卡（399/400 双侧+首帧同步）；200ms 淡出；**应用分组卡列**（悬停图标过滤、≤5、`hidden_cards` 滚动余量、跨图标不串列、图标 0 守卫）；最小化最后帧+角标；销毁实时移除；10 窗一致性全对；2Hz 空闲/1Hz 忙降档；0.5x 降采样；缓冲不落盘（mem_bytes 唯一存储账）；几何常量——`hover 400ms opens with first frame`、`consistency 10 windows all match`、`refresh 2hz idle 1hz busy`、`multi-window max five cards` | 13 / 5 |
| F074 | 与 F072 一致（抽 5 例）；三应用任务样本全对；键盘可达 | 最近区 8 条引擎同源 + 前 5 例逐项等同；固定区置顶+图钉移除；同路径不重复；三应用任务 RunTask 全对+审计；失效任务三要素 toast；宿主失败回报 toast；无窗口无尾项；CloseAll 尾项+审计；溢出「更多」（阈值 16）；空态轻文案；方向+Enter 且激活回灌引擎（freq+1）；150ms 动画+屏顶翻转——`recent matches f072 engine sample of five`、`three app task samples all run`、`keyboard arrows and enter feed engine`、`overflow shows more row` | 13 / 5 |
| F075 | 滑杆全程 80fps；幽灵清除实测；三件套点击 <100ms | **80fps 帧账闭环**（帧报到、间隔 ≤12ms 入账、违约计账）；**点击-首帧闭环**（99/100/101/未报到四点钉死，宿主 `panel_shown` 回报）；面板互斥；格点吸附 2%（100 格）；键盘 5% 精确跳步；幽灵 30s 清道夫（29s 存活/31s 清除/心跳续期实测）；电池四态+不可读灰显悬停说明；宽上限 200px 强制折叠（手动展开无效、撤枚解除）；白名单门常驻；E6 拖拽调序；tooltip 1s 延迟+占位图文案——`slider frames booked at 80fps`、`click to first frame within 100ms`、`ghost sweep after 30s silence`、`width cap forces collapse` | 13 / 5 |
| sbase | （共享底盘，无独立主册判据） | 分钟账本 / 旋钮注册表（产品级注入口）/ 环形日志 / 分位数工具 / CheckSet 容量纪律 | 14 / 7 |

**合计：257 检查 + 136 域内单元测试。**

---

## 二、行数对账（口径：纯功能行 = 总行 − `#[cfg(test)]` 块 − 注释行 − 空行）

判据唯一源 = 主册各节【工程量】段。**低于目标下限 90% 者如实登记**。

| 项 | 模块 | 主册目标行 | 实测功能行 | 对下限比 | 登记 |
| --- | --- | --- | --- | --- | --- |
| F058 | memcomp | 4,900–6,370 | 695 | 14.2% | ⚠ 低于 90% |
| F059 | netbatch | 3,650–4,745 | 424 | 11.6% | ⚠ 低于 90% |
| F060 | battery | 3,400–4,420 | 351 | 10.3% | ⚠ 低于 90% |
| F061 | reggate | 4,450–5,785 | 353 | 7.9% | ⚠ 低于 90% |
| F062 | selfcheck | 2,600–3,380 | 296 | 11.4% | ⚠ 低于 90% |
| F063 | touchpad | 4,050–5,265 | 468 | 11.6% | ⚠ 低于 90% |
| F064 | audiolow | 4,500–5,850 | 346 | 7.7% | ⚠ 低于 90% |
| F065 | wakegov | 5,100–6,630 | 429 | 8.4% | ⚠ 低于 90% |
| F066 | fsjournal | 3,200–4,160 | 344 | 10.8% | ⚠ 低于 90% |
| F067 | coldhot | 3,400–4,420 | 351 | 10.3% | ⚠ 低于 90% |
| F068 | assetldr | 4,350–5,655 | 383 | 8.8% | ⚠ 低于 90% |
| F069 | perfmodes | 4,950–6,435 | 532 | 10.7% | ⚠ 低于 90% |
| F070 | chainjudge | 450–585 | 423 | 94.0% | ✓ |
| F071 | startsearch | 400–520 | 342 | 85.5% | ⚠ 低于 90% |
| F072 | recenteng | 450–585 | 389 | 86.4% | ⚠ 低于 90% |
| F073 | thumbprev | 900–1,170 | 314 | 34.9% | ⚠ 低于 90% |
| F074 | jumplist | 900–1,170 | 575 | 63.9% | ⚠ 低于 90% |
| F075 | traysys | 450–585 | 421 | 93.6% | ✓ |
| — | sbase（共享底盘，无独立主册目标） | — | 369 | — | — |
| **合计** | | **52,100–67,730** | **7,805** | 对下限合计 15.0% | |

**差距性质如实说明（不注水辩解）**：主册【工程量】为「成熟完整版口径」——含宿主侧界面渲染、
交互回路与完整异常面。本泳道交付物是**内核侧判据载体**：每项以结构化模型实现主册【验收判据】
全量判线（对账可复现、注入钟确定复现），并以 `run_*_checks() -> CheckSet` 自检逐条钉死判据
（对照见第一节）；界面渲染层与宿主集成面不在本泳道落位范围。**判据全绿、行数如实——不为凑
行数注水**（七条铁律之「禁注水」优先于行数达标）。若后续要求行数对齐，缺口部分须以真实功能
（宿主集成、持久化、诊断面）立项补齐，而非复制摊薄。

---

## 三、缺陷账本（施工期发现并修复，共 40 项）

### F068 assetldr（7）
1. 悬空 `impl AssetLoader` 块嵌套在主 impl 内（语法错）→ 合并
2. 检查 1 断言 pinned ≥60MB 必红（壁纸按需装载不预驻）→ 精确口径 `8×64KB+100×96KB`
3. LRU 逐出可命中活动主题（检查 11 panic）→ `position` 取第一个**非活动**主题
4. `icon_preload_boundary` 引用已删除的 "ico" → "i100"
5. E0521 `report_corrupt` `&str` 逃逸到 `&'static` → 签名 `&'static str`；顺删死参数（零死代码）
6. freed_book_window 等大主题 freed=0 → 换装前先 acquire 壁纸
7. tests 模块缺 `use alloc::vec;`

### F069 perfmodes（9）
8. kname() 以 String 拼 Knob.name（须 'static）→ `knob_names()` 静态元组
9. 引用不存在的 `sbase::remove_for_test` → `with_knob_reg_for_test`
10. SwitchEvent 缺 `PartialEq/Eq` derive
11. 测试注入口 `cfg(test)` 下产品代码不可见 → 升级产品级 `with_knob_reg`（宿主共享旋钮单例）
12. replace_all 漏 restore 测试缩进变体（809 行残留）→ 手工补修
13. 旋钮总数误写 9（实为每档 4×3=12）→ 测试改名 `knob_snapshot_lists_all_twelve`
14. `switches_in(2,0)` span=0 空窗语义 → 改 (2,1)
15. restore 测试 raw=1 恰等默认档短路 → setup 先切 Performance
16. `switches_in` 期望 1 实为 2（diag 定位）→ 断言改 ==2

### F070 chainjudge（2）
17. E0382 c82 被 map 消耗后再用 → `.as_ref()`
18. heavy-app 检查手工推演错（dev-09 为 40 劣帧非 800；总 1020 帧；P95 秩 969→11_000、
    P99 秩 1010→20_000 → Amber）→ diag 确认实现正确、断言按实际语义改

### F071 startsearch（6）
19. `query_once` 初稿混入 5 个无意义占位垃圾 → 重写后删函数
20. "10/10" 误读为 10 个查询变体 → 主册语义 = 同查询 10 次试验恒正确
21. `first_hit` 生命周期不足 → 显式 `<'a>`
22. **真缺陷**：空查询 `starts_with("")` 恒真爆全索引 → 空查询短路（测试抓到、实现修复）
23. 防抖测试 150ms 恰满 `>=` 放行 → 测试改 149/150 双侧钉死
24. mixed ranking 断言残留 +5 boost → 裸分 `SCORE_SETTING_EXACT`

### F072 recenteng（6）
25. ranked() 残留 `let _ = now;` 死代码 → 删
26. clear_all 断言算术错 → `n > 0 && count() == 0`
27. LRU 逐出注释与实际序不符 → 修注释
28. date 测试 1 天差落「天前」分支 → 改 8 天差
29. **真缺陷**：`score_milli` 漏乘 1000 毫倍因子（4 检查齐红）→ `freq × 72_000_000 / denom`
30. alias 检查 freq 推演错（old.txt 实为 11 次：首建+10）→ before==11/after==12

### F073 thumbprev（3）
31. 卡片几何比例断言乱凑（200×9≠112×16——112 为取整值）→ 改纯常量断言
32. **真缺陷**：`cards()` 无应用分组（返回全部窗口且含被悬停者，与主册「该应用窗口
    卡列」不符）→ `WinThumb` 增 `icon_id` 应用组键、`cards()` 按悬停图标过滤（开启序
    升序、头 5）、新增 `hidden_cards()` 滚动余量账、图标 0 守卫（不计时不出卡）+
    跨图标不串列探针（检查 13）
33. diag073 一次报红项名 `multi-window max five cards` 定位

### F074 jumplist（4）
34. `(&Vec,&Vec,&Vec).into()` 三元组 From 不成立 → 显式 `Vec::new()`
35. `selected` 生命周期不足（F071 同款）→ 显式 `<'a>`
36. 测试内 `let mut e` 未用 mut → 删
37. 溢出夹具推演错：忽略最近区 8 条上限（14 文件+3 任务=11 内容行到不了阈值 16）→
    夹具改 6 固定+8 最近+3 任务=17>16，统计口径补 PinnedFile

### F075 traysys（3）
38. `battery_glyph` 充电臂未用变量 `p` → `Some(_)`
39. 键盘大步夹具推演错：`slider_set(55)` 先吸附 54（偶格）再 +5=59≠60 →
    夹具改 60→65，并澄清语义：**键盘 5% 精确跳步不受 2% 拖动格吸附**
40. `push_num` 与 recenteng 重复实现 → 改用 `alloc::format!` 消除

### 缺陷分类小结
- **实现真缺陷 3 项**：F071-22（空查询全索引）、F072-29（分数毫倍因子）、
  F073-32（应用分组缺失）——均为单元测试/自检抓出后修实现。
- **断言/夹具推演错 9 项**：F070-18、F072-30、F073-31、F074-37、F075-39 等——
  实现正确，先手算错；diag 复核后改断言。
- **编译/生命周期/死代码类 28 项**：机械性修正，零警告纪律落地。

### 已知未修（如实登记）
- **F072 `ranked()` 文档/实现口径不符**：文档注释称「同分按最近时间新→旧」，
  实现按路径降序决胜（ViewRow 无 last_ts_s 字段）。修复需改 ViewRow 结构并重排
  F072 既有断言——手术式纪律下暂不动已闭舱绿件，登记待后续批次处理。
  影响面：仅同分平手时的次序语义；分数本身（唯一排序事实）无影响。

---

## 四、施工技法沉淀（可复用）

- **隔离编译舱**：`cd /d/2/aik2-scratch && cargo test`（`#[path]` 直挂真实仓库文件，
  改动即刻生效；Bash 工作目录每次重置，必须显式 cd——本次施工亦两次踩此坑）。
- **红项定位**：`CheckSet.get(i) -> Option<Check{name,passed,detail}>` 逐项遍历，
  diag 测试把红项名累加进 assert 消息：`assert!(false, "RED:{}", reds)`——一次报出
  全部红项，替代逐次二分。
- **diag 清理**：python `s.find('\n#[cfg(test)]\nmod diagNNN {')` 截断法，追加与清理
  配对成对，收工前 `cargo test` 复核零残留（本批次 diag068～075 全部清零）。
- **推演错 vs 实现错**：红项先核夹具/断言推演（F070-18、F074-37、F075-39 属此类），
  再疑实现（F071-22、F072-29、F073-32 属此类）——diag 报红后先走推演复核再动实现。
- **规格锚定**：写检查前先回读主册该节【验收判据】原文（本批次两处口径误读
  F071-20、F073-32 均因凭记忆写判据），对照表（第一节）即由此沉淀。

---

## 附录：复现口令

```bash
# 1. 测试基线（隔离舱直挂真实文件）
cd /d/2/aik2-scratch && cargo test
# 预期：143 passed; 0 failed（lib）+ 0（doc-tests），零警告

# 2. 行数统计（纯功能行 = 总行 − #[cfg(test)] 块 − 注释行 − 空行）
python -c "
import os
d = r'D:\2\14\-Un-Real-0d23d9ux-Engine-main\kernel\varix\src\star'
mods = ['sbase','memcomp','netbatch','battery','reggate','selfcheck','touchpad',
        'audiolow','wakegov','fsjournal','coldhot','assetldr','perfmodes',
        'chainjudge','startsearch','recenteng','thumbprev','jumplist','traysys']
for m in mods:
    lines = open(os.path.join(d, m + '.rs'), encoding='utf-8').read().splitlines()
    cut = next((i for i, l in enumerate(lines) if l.strip() == '#[cfg(test)]'), len(lines))
    func = sum(1 for l in lines[:cut] if l.strip() and not l.strip().startswith('//'))
    print(m, func)
"

# 3. 自检规模（检查数 / 单元测试数）
cd /d/2/14/-Un-Real-0d23d9ux-Engine-main/kernel/varix/src/star
for f in *.rs; do echo "$f checks=$(grep -c 'set\.add(' $f) tests=$(grep -c '#\[test\]' $f)"; done

# 4. 判据源
docs/Varix STAR I start.md  # G-B-18~30、G-C-01~05 各节【验收判据】【工程量】
```
